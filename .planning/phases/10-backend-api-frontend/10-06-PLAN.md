---
phase: 10-backend-api-frontend
plan: 06
type: execute
wave: 7
depends_on: ["10-05", "10-00b"]
files_modified:
  - apps/factory-ui/src/app/auth/login.component.ts
  - apps/factory-ui/src/app/shared/ui/language-toggle.component.ts
  - apps/factory-ui/src/app/shared/ui/theme-toggle.component.ts
  - apps/factory-ui/src/app/shared/ui/user-chip.component.ts
  - apps/factory-ui/src/app/shared/approval-card/approval-card.component.ts
  - apps/factory-ui/src/app/shared/approval-card/evidence-panel.component.ts
  - apps/factory-ui/src/app/shared/approval-card/approval-card.component.spec.ts
autonomous: true
gap_closure: false
requirements: [UI-02, UI-03, UI-07, UI-05, HITL-01, HITL-06, HITL-07]
must_haves:
  truths:
    - "The login page authenticates a seeded persona (incl. dev-mode persona quick-select chips), stores the JWT, connects SSE, and redirects to the persona home route"
    - "LanguageToggle switches IT/EN at runtime via LocaleService (no reload); ThemeToggle switches dark/light; UserChip shows initials + role badge — all 64px touch with data-testid"
    - "ApprovalCard renders the evidence panel (input/tool_calls/citations/confidence) and enforces a mandatory motivation (min 10 chars, HITL-07) before enabling Approve; Reject opens a confirm dialog"
    - "Every component the Playwright spec needs carries its data-testid (approval-card, approval-card-status, evidence-panel, motivation-textarea, approve-btn, reject-btn, language-toggle, theme-toggle)"
  artifacts:
    - path: "apps/factory-ui/src/app/shared/approval-card/approval-card.component.ts"
      provides: "HITL approval card with motivation gate + status states"
      contains: "motivation"
    - path: "apps/factory-ui/src/app/shared/approval-card/evidence-panel.component.ts"
      provides: "EvidencePanel rendering evidence_panel JSONB"
      contains: "rag_citations"
    - path: "apps/factory-ui/src/app/auth/login.component.ts"
      provides: "login page + dev persona quick-select"
      contains: "login"
  key_links:
    - from: "apps/factory-ui/src/app/auth/login.component.ts"
      to: "apps/factory-ui/src/app/core/auth/jwt.service.ts"
      via: "POST /auth/login then JwtService.setToken + redirect"
      pattern: "setToken|/auth/login"
    - from: "apps/factory-ui/src/app/shared/approval-card/approval-card.component.ts"
      to: "/v1/approvals"
      via: "POST decide on approve/reject"
      pattern: "approvals"
---

<objective>
Wave 6 frontend HITL UI: the login page (+ dev persona quick-select), the shared TopBar widgets (LanguageToggle/ThemeToggle/UserChip), and the centerpiece ApprovalCard + EvidencePanel. Implements UI-02/UI-03/UI-05/UI-07 surfaces and HITL-01/06/07. Un-skips approval-card.component.spec.ts.

Decisions: motivation mandatory min 10 chars (HITL-07); evidence_panel JSONB rendered per the UI-SPEC EvidencePanel interface; runtime language toggle (UI-07). Approve/reject are POST REST to the EXISTING approvals router (gray-area-2 LOCKED). Touch >=64px, WCAG AA, data-testid on every Playwright-referenced element (UI-SPEC Playwright contract).

Execution note: worktrees DISABLED — sequential on main tree. Wave 6; depends on 10-05 (services). Disjoint from 10-07 (kpi-tile/alert-feed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-UI-SPEC.md
@apps/api-gateway/src/svc_api_gateway/routers/approvals.py

<interfaces>
ApprovalCard layout + states + interaction flow: 10-UI-SPEC Component 3. EvidencePanel data interface (EvidencePanel/ToolCallEntry/RagCitation) + rendering rules: Component 4. Login layout + dev chips + copy: Component 2. LanguageToggle: Component 6. UserChip/ThemeToggle: Component 1.
Backend: POST /v1/approvals/{id}/decide (existing approvals.py — DecideResponse) for approve/reject with motivation. POST /auth/login (10-01). JwtService/LocaleService/ThemeService/SseService from 10-05.
data-testid list: UI-SPEC Playwright section (approval-card, approval-card-status, evidence-panel, motivation-textarea, approve-btn, reject-btn, language-toggle, theme-toggle, sse-indicator, user-chip).
Copy keys from it.json/en.json (10-05) via transloco — no hardcoded strings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page + dev persona quick-select + shared TopBar widgets</name>
  <files>apps/factory-ui/src/app/auth/login.component.ts, apps/factory-ui/src/app/shared/ui/language-toggle.component.ts, apps/factory-ui/src/app/shared/ui/theme-toggle.component.ts, apps/factory-ui/src/app/shared/ui/user-chip.component.ts</files>
  <action>LoginComponent (route /auth/login): centered 400px card, mat-form-field email + password (show/hide 64px), 64px full-width accent Accedi button, dev-mode chip group pre-filling the 5 seeded personas (UI-SPEC Component 2), error snackbar (transloco key), loading spinner-in-button. On submit: POST /auth/login → JwtService.setToken → SseService.connect(token) → router.navigate to the persona home (operator→/operator, supervisor/cio→/manager, technician→/technician, admin→/admin per UI-SPEC JWT table). LanguageToggle calls LocaleService.switchLang (data-testid language-toggle, 64px). ThemeToggle calls ThemeService.toggle (data-testid theme-toggle, aria-label). UserChip shows initials + role badge (data-testid user-chip, 64px). All copy via transloco.</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="login|language-toggle|theme-toggle|user-chip" --passWithNoTests 2>&1 | tail -6 && grep -q "data-testid=\"language-toggle\"" apps/factory-ui/src/app/shared/ui/language-toggle.component.ts && echo widgets-ok</automated>
  </verify>
  <done>Login authenticates + redirects per persona; toggles + user-chip render with data-testid and 64px touch; copy via transloco.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ApprovalCard + EvidencePanel</name>
  <files>apps/factory-ui/src/app/shared/approval-card/approval-card.component.ts, apps/factory-ui/src/app/shared/approval-card/evidence-panel.component.ts, apps/factory-ui/src/app/shared/approval-card/approval-card.component.spec.ts</files>
  <behavior>
    - ApprovalCard renders status states (pending/approved/rejected/loading/expired_sla) with the correct border-left color + StatusChip (data-testid approval-card-status).
    - Motivation textarea (data-testid motivation-textarea) enforces min 10 chars; Approve (data-testid approve-btn) disabled until valid; CharCounter updates; validation error shown (HITL-07).
    - Reject (data-testid reject-btn) opens a confirm dialog with the destructive copy; on confirm POSTs decide with the motivation.
    - EvidencePanel renders input (pre/code), tool_calls_log (empty→"Nessuna tool call"), rag_citations (empty→"Nessuna citazione RAG"; restricted acl→"Contenuto riservato"), confidence badge (low/medium/high thresholds).
  </behavior>
  <action>Implement EvidencePanel typed to the UI-SPEC EvidencePanel/ToolCallEntry/RagCitation interfaces with all rendering rules (Component 4 table) and data-testid evidence-panel; accordions, max-heights, source_uri link rule. Implement ApprovalCard hosting EvidencePanel (default open for pending), the motivation gate, the ActionBar (64px Approve/Reject), SLA countdown (role=status, aria-live), and POST /v1/approvals/{id}/decide on approve/reject. Update card state from SseService approval_resolved. data-testid on approval-card + every Playwright-referenced child. Un-skip approval-card.component.spec.ts.</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="approval-card" 2>&1 | tail -6</automated>
  </verify>
  <done>approval-card.component.spec.ts passes: evidence sections render; approve disabled until motivation>=10 chars; data-testid present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input → approvals API | Motivation text + decision cross to the backend |
| backend evidence JSONB → render | Untrusted JSONB rendered in the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Repudiation | approve/reject | mitigate | Mandatory motivation (min 10 chars, HITL-07) sent to the existing approvals decide endpoint which audits it. |
| T-10-06-02 | Tampering | XSS via evidence JSONB | mitigate | Render evidence via Angular text interpolation / [textContent] (auto-escaped); source_uri opened only if a valid URL; no innerHTML. |
| T-10-06-03 | Information Disclosure | restricted citations | mitigate | acl_level=restricted hides chunk_preview ("Contenuto riservato") per UI-SPEC rule. |
| T-10-06-04 | Elevation of Privilege | decide without role | mitigate | Backend approvals decide + require_roles authoritative; UI guard secondary. |
</threat_model>

<verification>
- approval-card.component.spec.ts passes.
- Login + toggles + user-chip render with data-testid and 64px touch.
</verification>

<success_criteria>
An operator can log in (incl. dev persona chips), review an approval's evidence inline, enter a mandatory motivation, and approve/reject — with runtime language + theme toggles, all to the locked design contract.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-06-SUMMARY.md` when done.
</output>
