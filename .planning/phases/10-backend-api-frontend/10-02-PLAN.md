---
phase: 10-backend-api-frontend
plan: 02
type: execute
wave: 3
depends_on: ["10-01"]
files_modified:
  - apps/api-gateway/src/svc_api_gateway/kpi/__init__.py
  - apps/api-gateway/src/svc_api_gateway/kpi/queries.py
  - apps/api-gateway/src/svc_api_gateway/routers/kpi.py
  - apps/api-gateway/src/svc_api_gateway/main.py
  - apps/api-gateway/tests/unit/test_kpi_queries.py
autonomous: true
gap_closure: false
requirements: [SRV-02, UI-04]
must_haves:
  truths:
    - "compute_kpi_snapshot(pool) returns the 6 KPIs (oee, mttr, mtbf, scrap_rate, throughput, downtime) computed by real SQL over maintenance.downtime_events, scm.historical_orders and audit.actions"
    - "GET /v1/kpi is RBAC-protected (operator/shift-supervisor/manager/admin) and returns a typed KpiSnapshot"
    - "All KPI SQL uses asyncpg $N parameters only (no f-string interpolation) and reuses the OEE availability formula from Phase 7 oee.py"
  artifacts:
    - path: "apps/api-gateway/src/svc_api_gateway/kpi/queries.py"
      provides: "6 async aggregation functions + compute_kpi_snapshot"
      contains: "oee_availability"
    - path: "apps/api-gateway/src/svc_api_gateway/routers/kpi.py"
      provides: "GET /v1/kpi endpoint + KpiSnapshot model"
      contains: "/v1/kpi"
  key_links:
    - from: "apps/api-gateway/src/svc_api_gateway/routers/kpi.py"
      to: "apps/api-gateway/src/svc_api_gateway/kpi/queries.py"
      via: "compute_kpi_snapshot(pool) called inside the endpoint"
      pattern: "compute_kpi_snapshot"
    - from: "apps/api-gateway/src/svc_api_gateway/routers/kpi.py"
      to: "apps/api-gateway/src/svc_api_gateway/security/rbac.py"
      via: "Depends(require_roles(...))"
      pattern: "require_roles"
---

<objective>
Wave 2 backend: real KPI aggregations over TimescaleDB (OEE/MTTR/MTBF/scrap/throughput/downtime) in `kpi/queries.py`, surfaced by an RBAC-protected `GET /v1/kpi`. Implements SRV-02 (REST KPI) and the data behind UI-04.

Purpose: the control-room dashboard (UI-04) and the SSE kpi stream (10-03) both consume compute_kpi_snapshot. Real aggregations per CONTEXT gray-area-4 (no mocks). Un-skips test_kpi_queries.py.

Reuse: OEE availability formula from apps/agents/maintenance/downtime-analyzer oee.py (Phase 7). OEE = Availability-only with P=1.0 and Q from the audit QUALITY_VERDICT proxy — document this approximation in the SUMMARY (per RESEARCH note).

Execution note: worktrees DISABLED — sequential on main tree. Wave 2; depends on 10-01 (require_roles + main.py wiring). Touches main.py — sequenced after 10-01 and before 10-03 to avoid concurrent main.py edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-RESEARCH.md
@apps/api-gateway/src/svc_api_gateway/dependencies.py
@apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/oee.py
@infra/migrations/timescale/008_create_downtime_events.sql

<interfaces>
get_pool (from dependencies.py) is the asyncpg pool DI: `pool = Depends(get_pool)`; pool exposes `async with pool.acquire() as conn: await conn.fetchrow(sql, *params)`.
require_roles (from security/rbac.py, plan 10-01): `principal = Depends(require_roles("operator","shift-supervisor","manager","admin"))`.
KPI thresholds and units are defined in 10-UI-SPEC "KPI Dashboard" table — the snapshot must carry value + status (green/warning/destructive) computable client-side or server-side.
SQL sources (RESEARCH Pattern 7): maintenance.downtime_events (OEE-A/MTTR/MTBF/downtime), scm.historical_orders (throughput kg/h), audit.actions action_type='QUALITY_VERDICT' evidence_panel->tool_calls->0->result->>'score' (scrap proxy).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kpi/queries.py — 6 aggregations + compute_kpi_snapshot</name>
  <files>apps/api-gateway/src/svc_api_gateway/kpi/__init__.py, apps/api-gateway/src/svc_api_gateway/kpi/queries.py</files>
  <behavior>
    - compute_kpi_snapshot(pool) returns a dict with keys oee, mttr, mtbf, scrap_rate, throughput, downtime, each a float (or None when no data).
    - Each SQL statement uses $1..$N params for time windows / thresholds (no f-string); datetimes passed as tz-aware objects.
    - OEE availability = (planned_min - SUM(downtime duration_min)) / planned_min * 100, clamped >=0 (reuse oee.py logic).
  </behavior>
  <action>Create kpi/queries.py with one async function per KPI (oee_availability, mttr, mtbf, scrap_rate, throughput, downtime_pct) taking an asyncpg connection, plus compute_kpi_snapshot(pool) that acquires one connection and runs them concurrently or sequentially and returns the assembled dict. Port the OEE availability formula from oee.py (import it if cleanly importable, else duplicate with a citation comment). Pass interval/threshold values as $N params (CR-05). Handle empty result sets → None (don't crash). NULLIF guards on divisions. Window = last 8h for OEE/downtime, 30d for MTTR/MTBF per RESEARCH Pattern 7.</action>
  <verify>
    <automated>cd apps/api-gateway && uv run pytest tests/unit/test_kpi_queries.py -x -q</automated>
  </verify>
  <done>test_kpi_queries.py passes: 6 keys returned with a mocked connection; SQL asserted parametrized ($N) — no f-string injection.</done>
</task>

<task type="auto">
  <name>Task 2: routers/kpi.py (GET /v1/kpi) + wire into build_app</name>
  <files>apps/api-gateway/src/svc_api_gateway/routers/kpi.py, apps/api-gateway/src/svc_api_gateway/main.py</files>
  <action>Create routers/kpi.py: APIRouter(prefix="/v1", tags=["kpi"]). KpiSnapshot Pydantic response model (extra="forbid", frozen=True; CR-03) with the 6 float|None fields. GET /v1/kpi depends on require_roles("operator","shift-supervisor","manager","admin") and get_pool, calls compute_kpi_snapshot, returns KpiSnapshot. Wrap unexpected exceptions → HTTPException(500,"internal_server_error") + structlog (CR-02). In main.py build_app(), add local import `from svc_api_gateway.routers import kpi as kpi_router` and `app.include_router(kpi_router.router)` after the auth router (preserve existing ordering/pattern).</action>
  <verify>
    <automated>cd apps/api-gateway && uv run pytest tests/ -q 2>&1 | tail -8</automated>
  </verify>
  <done>GET /v1/kpi registered and RBAC-protected; full backend suite regression-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /v1/kpi | Authenticated request; role enforced |
| gateway → TimescaleDB | SQL queries with user-influenced time window params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | kpi SQL | mitigate | $N params only, no f-string (CR-05); window/threshold values bound as parameters. |
| T-10-02-02 | Elevation of Privilege | /v1/kpi | mitigate | require_roles restricts to the 4 dashboard roles (V4). |
| T-10-02-03 | Information Disclosure | error body | mitigate | Generic 500 body, structlog detail (CR-02). |
| T-10-02-04 | Denial of Service | heavy aggregation | accept | Read-only bounded-window queries; full rate-limiting deferred to Phase 11. |
</threat_model>

<verification>
- test_kpi_queries.py passes.
- `uv run pytest tests/ -q` green.
</verification>

<success_criteria>
GET /v1/kpi returns the 6 real KPIs computed from TimescaleDB, RBAC-protected, parametrized SQL, OEE reusing Phase 7 logic.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-02-SUMMARY.md` when done.
</output>
