---
phase: 10-backend-api-frontend
plan: 01
type: execute
wave: 2
depends_on: ["10-00a", "10-00b"]
files_modified:
  - apps/api-gateway/src/svc_api_gateway/security/__init__.py
  - apps/api-gateway/src/svc_api_gateway/security/jwt.py
  - apps/api-gateway/src/svc_api_gateway/security/rbac.py
  - apps/api-gateway/src/svc_api_gateway/routers/auth.py
  - apps/api-gateway/src/svc_api_gateway/main.py
  - apps/api-gateway/tests/unit/test_auth_router.py
  - apps/api-gateway/tests/unit/test_rbac.py
autonomous: true
gap_closure: false
requirements: [SRV-01, HITL-02]
must_haves:
  truths:
    - "POST /auth/login with a seeded persona email+password returns 200 and a HS256 JWT carrying sub/email/role/exp claims"
    - "Invalid credentials return 401 with a generic detail (no exception leak)"
    - "require_roles(...) FastAPI dependency returns the token payload when the role is allowed and raises 403 'rbac_forbidden' otherwise; GET /auth/me echoes the principal"
    - "The JWT secret is read from API_SECRET_KEY env with a documented dev-only default — no secret hardcoded as a production value"
  artifacts:
    - path: "apps/api-gateway/src/svc_api_gateway/security/jwt.py"
      provides: "create_token/decode_token (PyJWT HS256) + SEEDED_USERS"
      contains: "HS256"
    - path: "apps/api-gateway/src/svc_api_gateway/security/rbac.py"
      provides: "require_roles dependency factory + HTTPBearer"
      contains: "rbac_forbidden"
    - path: "apps/api-gateway/src/svc_api_gateway/routers/auth.py"
      provides: "/auth/login + /auth/me endpoints"
      contains: "/auth/login"
  key_links:
    - from: "apps/api-gateway/src/svc_api_gateway/routers/auth.py"
      to: "apps/api-gateway/src/svc_api_gateway/security/jwt.py"
      via: "create_token on successful credential match"
      pattern: "create_token"
    - from: "apps/api-gateway/src/svc_api_gateway/main.py"
      to: "apps/api-gateway/src/svc_api_gateway/routers/auth.py"
      via: "app.include_router(auth_router.router) inside build_app local imports"
      pattern: "auth_router"
---

<objective>
Wave 1 backend: JWT issuance (HS256 via PyJWT) backed by seeded persona users, plus the RBAC `require_roles` dependency, wired through a new `auth.py` router included in `build_app()`. Implements SRV-01 and the HITL escalation→role mapping (HITL-02).

Purpose: every protected endpoint (KPI, SSE) and the Angular RBAC guards depend on this. Un-skips test_auth_router.py and test_rbac.py from 10-00b.

Decision traceability: D (Auth/RBAC LOCKED) — JWT dev-mode + seeded persona users + RBAC. PyJWT per post_research_resolution #5 (NOT python-jose).

Execution note: worktrees DISABLED — sequential on main tree. This is the only Wave 2 plan (it edits main.py + security/, which Wave 2 kpi/sse plans must NOT touch concurrently — they are sequenced after via depends_on).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-RESEARCH.md
@apps/api-gateway/src/svc_api_gateway/main.py
@apps/api-gateway/src/svc_api_gateway/routers/approvals.py
@apps/api-gateway/tests/test_approvals_router.py

<interfaces>
From main.py build_app(): routers are imported as LOCAL imports inside build_app() (e.g. `from svc_api_gateway.routers import approvals as approvals_router`) and registered via `app.include_router(...router.router)`. Follow this exact pattern for auth.
SEEDED_USERS (from 10-CONTEXT Dev-Mode JWT table): operator@mantis.it→operator, supervisor@mantis.it→shift-supervisor, technician@mantis.it→technician, cio@mantis.it→manager, admin@mantis.it→admin. JWT: HS256, exp 8h, payload {sub,email,role,exp,iat}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: security/jwt.py + security/rbac.py</name>
  <files>apps/api-gateway/src/svc_api_gateway/security/__init__.py, apps/api-gateway/src/svc_api_gateway/security/jwt.py, apps/api-gateway/src/svc_api_gateway/security/rbac.py</files>
  <behavior>
    - create_token(email, role) returns a PyJWT HS256 token decodable back to {sub,email,role,exp,iat}, exp ~8h ahead, tz-aware.
    - decode_token raises HTTPException 401 "token_expired" on expiry, "token_invalid" on bad signature.
    - require_roles("operator","manager") returns a dependency that returns the payload when role matches, raises 403 "rbac_forbidden" otherwise.
    - SECRET_KEY = os.environ.get("API_SECRET_KEY", "<dev-only documented default>").
  </behavior>
  <action>Create the security/ package. In jwt.py implement create_token/decode_token using PyJWT (import jwt) with HS256, TOKEN_EXPIRE_HOURS=8, tz-aware datetime.now(timezone.utc) (CR-04). Define SEEDED_USERS dict (emails/roles per the interfaces block; dev passwords like "operator123" — plaintext is acceptable ONLY for dev-mode and must be commented as such, no production secret). Read SECRET_KEY from API_SECRET_KEY env with a clearly-commented dev default; never log the secret. In rbac.py define bearer_scheme=HTTPBearer() and require_roles(*allowed) factory returning a dependency that decodes creds.credentials and checks the role claim, raising 403 "rbac_forbidden". Export create_token, decode_token, require_roles, SEEDED_USERS from __init__.py with EXACT names (CR-01).</action>
  <verify>
    <automated>cd apps/api-gateway && uv run pytest tests/unit/test_rbac.py -x -q</automated>
  </verify>
  <done>test_rbac.py passes (un-skipped): allowed role → payload, disallowed → 403 rbac_forbidden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: routers/auth.py (/auth/login + /auth/me) and wire into build_app</name>
  <files>apps/api-gateway/src/svc_api_gateway/routers/auth.py, apps/api-gateway/src/svc_api_gateway/main.py, apps/api-gateway/tests/unit/test_auth_router.py</files>
  <behavior>
    - POST /auth/login {email,password} → 200 {access_token, token_type:"bearer", role} for a matching seeded user; 401 generic "invalid_credentials" otherwise.
    - GET /auth/me with a valid Bearer token → 200 echoing {sub,email,role}.
    - Request model is Pydantic with model_config extra="forbid", frozen=True (CR-03).
  </behavior>
  <action>Create routers/auth.py: APIRouter(prefix="/auth", tags=["auth"]). LoginRequest Pydantic model (email, password) with extra="forbid", frozen=True. /auth/login looks up SEEDED_USERS, compares password (constant-time compare is fine but plain == acceptable for dev), returns create_token(...) on match else HTTPException(401, "invalid_credentials"). Wrap unexpected errors to HTTPException(500, "internal_server_error") + structlog.error with detail (CR-02 — never str(exc) in body). /auth/me uses Depends(require_roles(... all five roles ...)) or a decode-only dependency and returns the principal. In main.py build_app(), add the local import `from svc_api_gateway.routers import auth as auth_router` and `app.include_router(auth_router.router)` following the existing pattern. Un-skip test_auth_router.py.</action>
  <verify>
    <automated>cd apps/api-gateway && uv run pytest tests/unit/test_auth_router.py -x -q</automated>
  </verify>
  <done>test_auth_router.py passes: seeded login returns JWT with correct claims; bad creds → 401 generic; /auth/me echoes principal; router registered in build_app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /auth/login | Untrusted credentials cross into the gateway |
| client → protected endpoint (Bearer) | Untrusted JWT validated by require_roles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Spoofing | /auth/login | mitigate | HS256 signed token via PyJWT; secret from API_SECRET_KEY env, dev default documented (V6). |
| T-10-01-02 | Elevation of Privilege | require_roles | mitigate | Role enforced from signed JWT claim, NOT request body user_roles (CONTEXT decision); 403 on mismatch (V4). |
| T-10-01-03 | Information Disclosure | error bodies | mitigate | Generic "invalid_credentials"/"internal_server_error"; structlog logs detail server-side (CR-02). |
| T-10-01-04 | Tampering | JWT secret | mitigate | No hardcoded production secret; env-driven; not logged. |
</threat_model>

<verification>
- test_rbac.py and test_auth_router.py both pass (un-skipped from 10-00b).
- `uv run pytest tests/ -q` regression-green (existing routers unaffected).
</verification>

<success_criteria>
A seeded operator can obtain a working JWT and any endpoint can enforce role-based access via require_roles, with no secret leak and generic error bodies.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-01-SUMMARY.md` when done.
</output>
