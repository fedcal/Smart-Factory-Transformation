---
phase: 10-backend-api-frontend
plan: 10
type: execute
wave: 10
depends_on: ["10-08", "10-09", "10-03"]
files_modified:
  - apps/factory-ui-e2e/project.json
  - apps/factory-ui-e2e/playwright.config.ts
  - apps/factory-ui-e2e/src/hitl-flow.spec.ts
  - apps/factory-ui-e2e/tsconfig.json
autonomous: true
gap_closure: false
requirements: [UI-10, HITL-01, HITL-06, HITL-07]
must_haves:
  truths:
    - "A separate Nx e2e project apps/factory-ui-e2e/ exists with a Playwright config (baseURL + webServer) per Nx convention"
    - "hitl-flow.spec.ts drives the full HITL flow: login as operator → dashboard with >=6 KPI tiles → pending approval card → open evidence panel → fill motivation → approve (POST intercepted) → card shows approved → audit record reflects APPROVED + motivation"
    - "The spec uses the Playwright API (getByTestId/locator/expect/page.route) and the data-testid attributes from the UI-SPEC contract"
    - "nx e2e ui-factory-e2e runs the suite (green against a running stack, or documented skip when the stack is unavailable)"
  artifacts:
    - path: "apps/factory-ui-e2e/src/hitl-flow.spec.ts"
      provides: "full HITL approval E2E flow"
      contains: "getByTestId"
    - path: "apps/factory-ui-e2e/playwright.config.ts"
      provides: "Playwright config (baseURL, webServer)"
      contains: "webServer"
  key_links:
    - from: "apps/factory-ui-e2e/src/hitl-flow.spec.ts"
      to: "apps/factory-ui (data-testid)"
      via: "page.getByTestId on approval-card / evidence-panel / motivation-textarea / approve-btn / kpi-grid"
      pattern: "approval-card|approve-btn|kpi-grid"
---

<objective>
Wave 8 frontend E2E: create the separate Nx Playwright project `apps/factory-ui-e2e/` and the full HITL approval-flow spec. Implements UI-10 (Playwright E2E) and ROADMAP Success Criterion 3 (alert → approval card → evidence review → approve → audit record).

Decisions: separate Nx e2e project apps/factory-ui-e2e/ (post_research_resolution #3 — Nx convention, NOT inline). Playwright API (getByTestId/page.route), NOT Cypress (UI-SPEC Playwright note). Steps + selectors + assertions follow the UI-SPEC Playwright E2E contract table exactly.

Execution note: worktrees DISABLED — sequential on main tree. Wave 8; depends on 10-08 + 10-09 (the UI the test drives) + 10-03 (the backend SSE/approval flow + seeded users). Generate the project via `nx g @nx/playwright:configuration --project=ui-factory` if it produces the expected layout, else author the files directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-UI-SPEC.md
@.planning/phases/10-backend-api-frontend/10-RESEARCH.md

<interfaces>
E2E step table + selectors + assertions: 10-UI-SPEC "Playwright E2E — Contratto Test" (8 steps). Mandatory data-testid list in the same section. Seeded login: operator@mantis.it (Dev-Mode JWT table). Backend endpoints: POST /auth/login, GET /v1/kpi, POST /v1/approvals/{id}/decide, audit read.
@nx/playwright@20.8.4 + @playwright/test (installed in 10-00a). Playwright config webServer should boot ui-factory (and optionally the gateway) or assume an externally-running stack with baseURL from env.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/factory-ui-e2e Nx Playwright project</name>
  <files>apps/factory-ui-e2e/project.json, apps/factory-ui-e2e/playwright.config.ts, apps/factory-ui-e2e/tsconfig.json</files>
  <action>Create the separate Nx e2e project apps/factory-ui-e2e/ (try `nx g @nx/playwright:configuration --project=ui-factory` first; if its output diverges, author project.json with an "e2e" target using @nx/playwright:playwright, playwright.config.ts with baseURL (env PW_BASE_URL default http://localhost:4200) + a webServer entry that serves ui-factory, and a tsconfig.json). Ensure `nx e2e ui-factory-e2e` is a resolvable target. Install Chromium (`npx playwright install chromium`) as part of setup if missing (RESEARCH Environment).</action>
  <verify>
    <automated>nx show project ui-factory-e2e 2>&1 | tail -5 && test -f apps/factory-ui-e2e/playwright.config.ts && grep -q "webServer\|baseURL" apps/factory-ui-e2e/playwright.config.ts && echo e2e-project-ok</automated>
  </verify>
  <done>ui-factory-e2e is a recognized Nx project with a Playwright config (baseURL + webServer).</done>
</task>

<task type="auto">
  <name>Task 2: hitl-flow.spec.ts — full HITL approval flow</name>
  <files>apps/factory-ui-e2e/src/hitl-flow.spec.ts</files>
  <action>Author the spec implementing the UI-SPEC 8-step table: (1) login via page.request.post('/auth/login', operator creds) → set token in localStorage/login UI; (2) navigate to /operator|/manager, expect getByTestId('kpi-grid') and toHaveCount >=6 on kpi-tile; (3) getByTestId('approval-card').first() is pending; (4) open evidence panel (getByRole button Evidence), assert input/tool_calls/citations sections visible; (5) fill getByTestId('motivation-textarea'), assert CharCounter; (6) intercept POST via page.route('**/v1/approvals/*/decide', ...) and click getByTestId('approve-btn'); (7) assert the card updates to approved (data-testid approval-card-status), ActionBar hidden; (8) assert the audit record reflects APPROVED + motivation via `page.request.get('/v1/approvals')` (the existing list endpoint, whose entries carry decision_json) filtered by the approval_id captured in step 6 — there is NO dedicated `/v1/audit/{id}` endpoint, do not invent one. Use the Playwright API throughout (no Cypress). Guard for a missing live stack: if the webServer/baseURL is unreachable the suite should fail clearly (not silently pass).</action>
  <verify>
    <automated>nx e2e ui-factory-e2e 2>&1 | tail -15</automated>
  </verify>
  <done>hitl-flow.spec.ts covers all 8 UI-SPEC steps with the required data-testid + page.route interception; the suite runs (green against a running stack).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → app + gateway | E2E exercises the auth + approval flow end to end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Repudiation | approval audit | mitigate | Step 8 asserts the audit record persists decision + motivation — verifies HITL-07 auditability end to end. |
| T-10-10-02 | Tampering | flaky/false-green E2E | mitigate | Spec fails clearly on unreachable stack; uses explicit expect assertions, not best-effort. |
</threat_model>

<verification>
- ui-factory-e2e project resolves with Playwright config.
- hitl-flow.spec.ts runs the full 8-step flow.
</verification>

<success_criteria>
A separate Nx Playwright project runs the complete HITL approval flow (login → live KPIs → evidence review → approve → audit), satisfying UI-10 and ROADMAP SC3.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-10-SUMMARY.md` when done.
</output>
