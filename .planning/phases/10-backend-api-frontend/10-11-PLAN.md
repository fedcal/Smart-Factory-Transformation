---
phase: 10-backend-api-frontend
plan: 11
type: execute
wave: 11
depends_on: ["10-01", "10-02", "10-03", "10-10"]
files_modified:
  - packages/sft-contracts/package.json
  - packages/sft-contracts/project.json
  - packages/sft-contracts/scripts/generate-openapi.py
  - packages/sft-contracts/src/api-types.ts
  - packages/sft-contracts/tests/contract.spec.ts
  - apps/factory-ui-e2e/src/screenshots.spec.ts
autonomous: true
gap_closure: false
requirements: [SRV-05, UI-09]
must_haves:
  truths:
    - "The FastAPI OpenAPI 3.1 spec is exported and TypeScript types are generated from it via openapi-typescript"
    - "A contract test fails if the generated TS types diverge from the Pydantic models (SRV-05 end-to-end type safety)"
    - "A Playwright screenshot spec captures the key bilingual (IT/EN) screens (login, operator approval, manager dashboard) for the mock-UI docs (UI-09)"
  artifacts:
    - path: "packages/sft-contracts/src/api-types.ts"
      provides: "TS types generated from the gateway OpenAPI export"
      contains: "KpiSnapshot"
    - path: "packages/sft-contracts/tests/contract.spec.ts"
      provides: "Pydantic↔TS divergence guard"
      contains: "openapi"
    - path: "apps/factory-ui-e2e/src/screenshots.spec.ts"
      provides: "bilingual screenshot capture for docs"
      contains: "screenshot"
  key_links:
    - from: "packages/sft-contracts/src/api-types.ts"
      to: "apps/api-gateway (OpenAPI export)"
      via: "openapi-typescript over the exported openapi.json"
      pattern: "openapi"
    - from: "apps/factory-ui-e2e/src/screenshots.spec.ts"
      to: "apps/factory-ui (it/en)"
      via: "toggle language then page.screenshot"
      pattern: "screenshot"
---

<objective>
Wave 9: the Pydantic↔TypeScript contract test (SRV-05) and the bilingual mock-UI screenshot spec (UI-09). Satisfies ROADMAP Success Criterion 4 (OpenAPI exports, Pydantic/TS contract-tested) and the docs/screenshot deliverable.

Decisions: generate TS types from the OpenAPI export via openapi-typescript (RESEARCH Don't-Hand-Roll). Screenshots via Playwright (UI-09); screenshot generation may be CI-hardware dependent — make the spec runnable but tolerant of headless CI per RESEARCH note.

Execution note: worktrees DISABLED — sequential on main tree. Wave 9 (last); depends on all backend plans (OpenAPI surface complete) + 10-10 (e2e project for the screenshot spec).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-UI-SPEC.md
@.planning/phases/10-backend-api-frontend/10-RESEARCH.md

<interfaces>
build_app() (apps/api-gateway main.py) exposes app.openapi() → OpenAPI 3.1 dict; FastAPI also serves /openapi.json. Export it to packages/sft-contracts/openapi.json via a small Python script invoking build_app().openapi().
openapi-typescript CLI generates api-types.ts from openapi.json. Key models to assert present: KpiSnapshot (10-02), LoginRequest/token response (10-01), the SSE payload shapes, ApprovalDecide response (existing approvals.py).
Screenshot targets + IT/EN copy: 10-UI-SPEC (login, operator approval, manager dashboard). LanguageToggle data-testid for switching locale in the spec.
Existing monorepo package conventions: see other packages/* project.json for the Nx project + target shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAPI export + openapi-typescript generation + contract test</name>
  <files>packages/sft-contracts/package.json, packages/sft-contracts/project.json, packages/sft-contracts/scripts/generate-openapi.py, packages/sft-contracts/src/api-types.ts</files>
  <action>Create the packages/sft-contracts Nx project (package.json + project.json following the existing packages/* convention). generate-openapi.py imports build_app() from svc_api_gateway and writes openapi.json (json.dumps(app.openapi())). Add an Nx target (e.g. check-types / generate) that runs the export then openapi-typescript over openapi.json producing src/api-types.ts. Commit a generated api-types.ts containing the key models (KpiSnapshot etc.). Do NOT hand-edit the generated file. Add openapi-typescript as a devDependency if not present (vetted CLI).</action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && cd apps/api-gateway && uv run python ../../packages/sft-contracts/scripts/generate-openapi.py 2>&1 | tail -3 && cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && grep -q "KpiSnapshot" packages/sft-contracts/src/api-types.ts && echo openapi-ts-ok</automated>
  </verify>
  <done>OpenAPI exports; api-types.ts generated from it containing KpiSnapshot + auth models.</done>
</task>

<task type="auto">
  <name>Task 2: Contract divergence test + bilingual screenshot spec</name>
  <files>packages/sft-contracts/tests/contract.spec.ts, apps/factory-ui-e2e/src/screenshots.spec.ts</files>
  <action>contract.spec.ts: regenerate types from the current OpenAPI export and assert the committed src/api-types.ts is byte-identical (or that the generated shape contains the expected exported type names) — fail loudly on divergence (SRV-05 guard). screenshots.spec.ts (in the e2e project): log in, capture login + /operator approval card + /manager dashboard in IT, then toggle EN via getByTestId('language-toggle') and re-capture — saving to a docs screenshots dir. Tolerate headless CI; allow an env flag to skip when no display/browser per RESEARCH note, but default to capturing.</action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && nx test sft-contracts --passWithNoTests 2>&1 | tail -6 && test -f apps/factory-ui-e2e/src/screenshots.spec.ts && grep -q "language-toggle" apps/factory-ui-e2e/src/screenshots.spec.ts && echo contract-screens-ok</automated>
  </verify>
  <done>Contract test fails on Pydantic↔TS divergence; screenshot spec captures IT + EN key screens (UI-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → generated artifacts | OpenAPI/TS generation is build-time only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11-01 | Tampering | type drift | mitigate | Contract test fails when generated TS diverges from Pydantic — prevents silent API/UI contract breakage (SRV-05). |
| T-10-11-02 | Information Disclosure | screenshots | accept | Screenshots use seeded dev personas/data only — no real PII. |
| T-10-11-SC | Tampering | openapi-typescript install | mitigate | CLI is a widely-used vetted tool; pin a version; covered by the 10-00a dependency hygiene. |
</threat_model>

<verification>
- OpenAPI exports; api-types.ts generated with key models.
- Contract test guards divergence; bilingual screenshots captured.
</verification>

<success_criteria>
The FastAPI contract is exported, TypeScript types are generated and divergence-tested (SRV-05), and bilingual mock-UI screenshots are produced for the docs (UI-09) — completing ROADMAP SC4.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-11-SUMMARY.md` when done.
</output>
