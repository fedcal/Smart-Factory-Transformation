---
phase: 10-backend-api-frontend
plan: 09
type: execute
wave: 9
depends_on: ["10-08"]
files_modified:
  - apps/factory-ui/src/app/features/technician/technician.component.ts
  - apps/factory-ui/src/app/features/technician/technician.routes.ts
  - apps/factory-ui/src/app/features/admin/admin.component.ts
  - apps/factory-ui/src/app/features/admin/admin.routes.ts
  - apps/factory-ui/src/app/features/demo/persona-walkthrough.component.ts
  - apps/factory-ui/src/app/features/demo/demo.routes.ts
  - apps/factory-ui/src/app/app.routes.ts
autonomous: true
gap_closure: false
requirements: [UI-01, UI-08, HITL-04, HITL-09]
must_haves:
  truths:
    - "The /technician area shows maintenance/RCA + step-by-step procedure content with real seeded data, surfacing technician-tier approvals with their SLA (HITL-04 visibility)"
    - "The /admin area shows the audit log + user management (read-only over real audit.actions) with a governor alert when >80% of recent actions were auto-approved (HITL-09)"
    - "The /demo persona walkthrough is a 4-step mat-stepper (operator/supervisor/technician/CIO) rendering each persona's real UI (not screenshots), navigable forward/back without URL change"
    - "All three areas resolve via RBAC-guarded routes with no broken routes or missing data (ROADMAP SC5)"
  artifacts:
    - path: "apps/factory-ui/src/app/features/demo/persona-walkthrough.component.ts"
      provides: "4-step persona walkthrough stepper"
      contains: "persona-stepper"
    - path: "apps/factory-ui/src/app/features/admin/admin.component.ts"
      provides: "audit log + governor alert"
      contains: "governor-alert"
    - path: "apps/factory-ui/src/app/features/technician/technician.component.ts"
      provides: "maintenance/RCA + procedure view"
      contains: "technician"
  key_links:
    - from: "apps/factory-ui/src/app/app.routes.ts"
      to: "apps/factory-ui/src/app/features/demo/demo.routes.ts"
      via: "lazy loadChildren for /technician /admin /demo"
      pattern: "technician|admin|demo"
    - from: "apps/factory-ui/src/app/features/demo/persona-walkthrough.component.ts"
      to: "apps/factory-ui/src/app/features/operator/operator.component.ts"
      via: "demo steps embed the real persona components"
      pattern: "Operator|persona"
---

<objective>
Wave 7 frontend features (part 2): the technician area, the admin area (audit log + governor alert), and the persona walkthrough demo. Implements UI-01 (remaining persona routes), UI-08 (in-app walkthrough), and the UI surfacing of HITL-04 (per-tier SLA visibility) + HITL-09 (governor alert at >80% auto-approval). Satisfies the navigability half of ROADMAP SC5.

Note: HITL-04/09 are owned/enforced by Phase 4 backend; this plan surfaces them in the UI (governor banner reads the audit ratio; technician queue shows SLA). HITL-03 (Safety Interlock) and HITL-08 (rollback) remain Phase 4 backend concerns, not Phase 10 UI.

Decisions: governor alert copy + threshold (>80% auto-approved) from UI-SPEC Copywriting Contract; demo embeds the REAL persona components with real seeded data (UI-SPEC Component 7 — "non uno screenshot"); stepper local nav (URL unchanged).

Execution note: worktrees DISABLED — sequential on main tree. Wave 7; depends on 10-08 (operator/manager components reused by the demo; app.routes.ts edited after 10-08). Replaces the remaining /technician /admin /demo placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-UI-SPEC.md

<interfaces>
PersonaWalkthrough layout + 4 steps + persona cards + nav: 10-UI-SPEC Component 7 (data-testid persona-stepper, demo-nav-next, demo-nav-prev). Governor alert copy + 80% threshold: Copywriting Contract row "Governor alert (manager)" and admin route. Persona→home mapping: Dev-Mode JWT table.
Reused components: OperatorComponent + ManagerComponent (10-08), KpiTile/ApprovalCard (10-06/10-07). Audit data: GET against the existing audit/approvals endpoints (audit.actions). Copy via transloco.
RBAC roles per route: technician→technician; admin→admin; demo→all authenticated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Technician + Admin areas</name>
  <files>apps/factory-ui/src/app/features/technician/technician.component.ts, apps/factory-ui/src/app/features/technician/technician.routes.ts, apps/factory-ui/src/app/features/admin/admin.component.ts, apps/factory-ui/src/app/features/admin/admin.routes.ts</files>
  <action>TechnicianComponent: maintenance/RCA view + a step-by-step procedure layout, surfacing pending technician-tier approvals (ApprovalQueueFeed filtered) with their per-tier SLA countdown (HITL-04 visibility) + relevant KPIs (MTTR/MTBF KpiTiles) from real data. AdminComponent: audit log table (read-only over audit.actions via the existing endpoints, paginated/virtual-scroll) + a simple user list (the 5 seeded personas) + a governor-alert banner (data-testid governor-alert) shown when the auto-approval ratio exceeds 80% (HITL-09), computed from the audit data. Both lazy routes with rbacGuard. Copy via transloco; 64px touch; SSR-safe.</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="technician|admin" --passWithNoTests 2>&1 | tail -5 && grep -q "data-testid=\"governor-alert\"" apps/factory-ui/src/app/features/admin/admin.component.ts && echo tech-admin-ok</automated>
  </verify>
  <done>/technician + /admin render real data with RBAC; technician queue shows SLA (HITL-04); governor-alert banner present (HITL-09).</done>
</task>

<task type="auto">
  <name>Task 2: Persona walkthrough demo + final route wiring</name>
  <files>apps/factory-ui/src/app/features/demo/persona-walkthrough.component.ts, apps/factory-ui/src/app/features/demo/demo.routes.ts, apps/factory-ui/src/app/app.routes.ts</files>
  <action>PersonaWalkthrough (route /demo): horizontal mat-stepper (data-testid persona-stepper) with the 4 steps (Operatore Luca / Capo Turno Anna / Tecnico Marco / CIO Elena per UI-SPEC Component 7 + Copywriting Contract step labels) + PersonaCard (avatar initials, name, role, scenario). Each step embeds the REAL persona component (OperatorComponent / ManagerComponent / TechnicianComponent) with real seeded data — not screenshots. Forward/back via Avanti/Indietro buttons (64px, data-testid demo-nav-next/demo-nav-prev), URL unchanged, "Esci dalla demo" returns to the current role home. Finalize app.routes.ts so /technician /admin /demo loadChildren point at the real feature routes with rbacGuard (demo: all authenticated). Verify no route still points at a 10-04 placeholder.</action>
  <verify>
    <automated>nx build ui-factory --configuration=development 2>&1 | tail -5 && grep -q "data-testid=\"persona-stepper\"" apps/factory-ui/src/app/features/demo/persona-walkthrough.component.ts && grep -q "demo" apps/factory-ui/src/app/app.routes.ts && echo demo-ok</automated>
  </verify>
  <done>/demo is a navigable 4-step walkthrough of real persona UIs; all 5 persona routes wired with RBAC; app builds; no broken routes (SC5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → audit endpoints | Authenticated read of audit.actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Information Disclosure | audit log | mitigate | Admin route rbac-guarded to admin role; backend require_roles authoritative (V4). |
| T-10-09-02 | Elevation of Privilege | demo access | accept | /demo open to all authenticated personas by design (dev walkthrough); embeds only data the logged-in role may already see. |
| T-10-09-03 | Tampering | audit render | mitigate | Read-only render; text interpolation auto-escapes. |
</threat_model>

<verification>
- /technician /admin /demo render with RBAC and real data.
- governor-alert + persona-stepper data-testid present; app builds; no placeholder routes remain.
</verification>

<success_criteria>
All five persona areas are navigable with real wired data and the in-app persona walkthrough works — fulfilling ROADMAP SC5 (no broken routes or missing data).
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-09-SUMMARY.md` when done.
</output>
