---
phase: 10-backend-api-frontend
plan: 08
type: execute
wave: 8
depends_on: ["10-06", "10-07"]
files_modified:
  - apps/factory-ui/src/app/features/operator/operator.component.ts
  - apps/factory-ui/src/app/features/operator/operator.routes.ts
  - apps/factory-ui/src/app/features/manager/manager.component.ts
  - apps/factory-ui/src/app/features/manager/charts-row.component.ts
  - apps/factory-ui/src/app/features/manager/manager.routes.ts
  - apps/factory-ui/src/app/app.routes.ts
autonomous: true
gap_closure: false
requirements: [UI-01, UI-04, UI-06, HITL-01, HITL-04]
must_haves:
  truths:
    - "The /operator area shows the ApprovalQueueFeed (primary focal point) + AlertFeed + a KPI summary, all live via SSE"
    - "The /manager control room shows the 6 KPI tiles in a responsive grid (data-testid kpi-grid) + ApprovalQueueWidget + AlertFeed + a lazy-loaded ChartsRow (OEE trend line + downtime Pareto bar) using ng2-charts@8"
    - "Both areas resolve via the persona routes with RBAC guards and render correctly via SSR then hydrate"
  artifacts:
    - path: "apps/factory-ui/src/app/features/operator/operator.component.ts"
      provides: "operator dashboard (approval queue focal)"
      contains: "approval-queue"
    - path: "apps/factory-ui/src/app/features/manager/manager.component.ts"
      provides: "control room KPI grid + widgets"
      contains: "kpi-grid"
    - path: "apps/factory-ui/src/app/features/manager/charts-row.component.ts"
      provides: "lazy ng2-charts OEE trend + downtime Pareto"
      contains: "ng2-charts"
  key_links:
    - from: "apps/factory-ui/src/app/features/manager/manager.component.ts"
      to: "apps/factory-ui/src/app/shared/kpi-tile/kpi-tile.component.ts"
      via: "renders 6 KpiTiles bound to kpiSnapshot signal"
      pattern: "kpi-tile|KpiTile"
    - from: "apps/factory-ui/src/app/app.routes.ts"
      to: "apps/factory-ui/src/app/features/operator/operator.routes.ts"
      via: "lazy loadChildren for /operator and /manager"
      pattern: "operator|manager"
---

<objective>
Wave 7 frontend features (part 1): the operator area (approval-queue-focused) and the manager control room (6-KPI grid + charts). Implements UI-01 (persona routing/SSR), UI-04 (control room with live KPIs + charts), UI-06 (SSE), HITL-01/04. Satisfies ROADMAP Success Criterion 1 (operator logs in, sees live KPIs, approves with evidence) and the dashboard half of SC5.

Decisions: charts use ng2-charts@8 + chart.js (lazy-loaded — RESEARCH pin). KPI grid primary focal for manager; ApprovalQueueFeed primary focal for operator (UI-SPEC Component 1 focal point).

Execution note: worktrees DISABLED — sequential on main tree. Wave 7; depends on 10-06 (ApprovalCard/login) + 10-07 (KpiTile/AlertFeed/ApprovalQueueFeed). Replaces the lazy placeholders in app.routes.ts for /operator and /manager only — disjoint from 10-09 (technician/admin/demo). Sequence 10-08 before 10-09 since both edit app.routes.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-UI-SPEC.md

<interfaces>
Operator focal point + manager KPIDashboard layout: 10-UI-SPEC Component 1 (focal) + Component 5 (KPIDashboard: DashboardHeader, KPIGrid of 6 tiles, ApprovalQueueWidget, AlertFeed, ChartsRow). KPI thresholds in Component 5 table.
Shared components: ApprovalQueueFeed/AlertFeed/KpiTile (10-07), ApprovalCard (10-06). SseService kpiSnapshot/approvals/alerts signals (10-05).
ng2-charts@8 BaseChartDirective + chart.js LineController/BarController; lazy-load the ChartsRow component (@defer or lazy route). data-testid kpi-grid required by Playwright.
GET /v1/kpi (10-02) for initial SSR snapshot before SSE takes over.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Operator area (approval-queue focal + alert feed + KPI summary)</name>
  <files>apps/factory-ui/src/app/features/operator/operator.component.ts, apps/factory-ui/src/app/features/operator/operator.routes.ts</files>
  <action>OperatorComponent: 2-column (desktop) layout with ApprovalQueueFeed as the primary focal point (red badge count) + AlertFeed secondary + a compact KPI summary row (reuse KpiTile). Wire to SseService signals; connect SSE on init if a token exists. operator.routes.ts exports the lazy route. Copy via transloco; 64px touch; SSR-safe (no browser API in constructor). data-testid where the Playwright flow lands on /operator.</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="operator" --passWithNoTests 2>&1 | tail -5 && grep -q "approval-queue\|ApprovalQueue" apps/factory-ui/src/app/features/operator/operator.component.ts && echo operator-ok</automated>
  </verify>
  <done>/operator renders approval queue (focal) + alerts + KPI summary, live via SSE, SSR-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Manager control room (6 KPI grid + widgets + charts) + route wiring</name>
  <files>apps/factory-ui/src/app/features/manager/manager.component.ts, apps/factory-ui/src/app/features/manager/charts-row.component.ts, apps/factory-ui/src/app/features/manager/manager.routes.ts, apps/factory-ui/src/app/app.routes.ts</files>
  <action>ManagerComponent ("Sala Controllo"): DashboardHeader (title, ShiftSelector, DateRangePicker, SSE LiveIndicator) + KPIGrid (data-testid kpi-grid) of 6 KpiTiles (OEE/MTTR/MTBF/Scrap/Throughput/Downtime) bound to kpiSnapshot signal, seeded by GET /v1/kpi on SSR + updated via SSE + ApprovalQueueWidget + AlertFeed. ChartsRow (separate, LAZY via @defer or lazy route): OEE trend line chart (7 days) + Downtime Pareto bar chart using ng2-charts@8 BaseChartDirective + chart.js — register only the needed controllers (tree-shake). Update app.routes.ts so /operator and /manager loadChildren point at the real feature routes (replacing the 10-04 placeholders) with rbacGuard data.roles (manager: shift-supervisor+manager). Copy via transloco.</action>
  <verify>
    <automated>nx build ui-factory --configuration=development 2>&1 | tail -5 && grep -q "data-testid=\"kpi-grid\"" apps/factory-ui/src/app/features/manager/manager.component.ts && grep -q "ng2-charts\|BaseChartDirective" apps/factory-ui/src/app/features/manager/charts-row.component.ts && echo manager-ok</automated>
  </verify>
  <done>/manager renders the 6-tile KPI grid (data-testid kpi-grid) + widgets + lazy ng2-charts row; routes wired with RBAC; app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /v1/kpi + SSE | Authenticated dashboard data fetch + live stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Elevation of Privilege | /manager access | mitigate | rbacGuard (shift-supervisor/manager) + backend require_roles on /v1/kpi (V4). |
| T-10-08-02 | Denial of Service | chart bundle on SSR | mitigate | ChartsRow lazy-loaded (@defer/lazy route), browser-only; chart.js controllers registered selectively. |
| T-10-08-03 | Tampering | KPI value render | mitigate | Values from typed KpiSnapshot; text interpolation auto-escapes. |
</threat_model>

<verification>
- /operator + /manager render with live SSE + RBAC routing.
- kpi-grid data-testid present; ng2-charts lazy; app builds.
</verification>

<success_criteria>
The operator and manager personas have fully wired, live, RBAC-guarded dashboards — fulfilling ROADMAP SC1 (operator login → live KPIs → approve with evidence) and the dashboard half of SC5.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-08-SUMMARY.md` when done.
</output>
