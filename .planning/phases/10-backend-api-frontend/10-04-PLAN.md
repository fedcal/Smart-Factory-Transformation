---
phase: 10-backend-api-frontend
plan: 04
type: execute
wave: 5
depends_on: ["10-00a"]
files_modified:
  - apps/factory-ui/src/styles/_tokens.scss
  - apps/factory-ui/src/styles/_typography.scss
  - apps/factory-ui/src/styles/_theme.dark.scss
  - apps/factory-ui/src/styles/_theme.light.scss
  - apps/factory-ui/src/styles.scss
  - apps/factory-ui/src/app/shell/app-shell.component.ts
  - apps/factory-ui/src/app/shell/top-bar.component.ts
  - apps/factory-ui/src/app/shell/nav-rail.component.ts
  - apps/factory-ui/src/app/shell/bottom-nav.component.ts
  - apps/factory-ui/src/app/app.routes.ts
autonomous: true
gap_closure: false
requirements: [UI-01, UI-02, UI-05]
must_haves:
  truths:
    - "The design tokens (dark default + light) are defined as CSS custom properties matching the UI-SPEC palette with verified WCAG AA pairs"
    - "AppShell renders TopBar + responsive NavigationRail (>=1024px) / BottomNav (<768px) with 64px touch targets and the persona nav items"
    - "app.routes.ts declares the 5 persona routes (/operator /technician /manager /admin /demo) + /auth/login with lazy-loaded feature placeholders"
    - "The app renders via SSR (dark theme default) and hydrates without console errors"
  artifacts:
    - path: "apps/factory-ui/src/styles/_tokens.scss"
      provides: "SFT CSS custom properties (dark + light palettes, spacing, type)"
      contains: "--sft-surface"
    - path: "apps/factory-ui/src/app/shell/app-shell.component.ts"
      provides: "AppShell host with responsive nav + router outlet"
      contains: "router-outlet"
    - path: "apps/factory-ui/src/app/app.routes.ts"
      provides: "persona routes + login route with RBAC guard placeholders"
      contains: "operator"
  key_links:
    - from: "apps/factory-ui/src/styles.scss"
      to: "apps/factory-ui/src/styles/_tokens.scss"
      via: "@use of tokens + theme partials after tailwind import"
      pattern: "_tokens|tokens"
    - from: "apps/factory-ui/src/app/app.routes.ts"
      to: "apps/factory-ui/src/app/shell/app-shell.component.ts"
      via: "AppShell as the layout route hosting persona children"
      pattern: "AppShell"
---

<objective>
Wave 4 frontend foundation: author the SFT design tokens + Angular Material dark/light themes + Inter typography, build the responsive AppShell (TopBar / NavigationRail / BottomNav), and declare the persona route map. Implements UI-01 (per-persona routing + SSR), UI-02 (design system + 64px touch), UI-05 (dark/light WCAG AA).

Purpose: every feature/component plan renders inside this shell and consumes these tokens. SSR-safe: theme default applied server-side (dark), no browser API in the render path.

Decision traceability: UI-SPEC Design System + Components 1 (App Shell) + Spacing/Typography/Color tables (LOCKED contract).

Execution note: worktrees DISABLED — sequential on main tree. Wave 4; depends on 10-00a (Material+Tailwind installed). Disjoint from backend plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-UI-SPEC.md
@apps/factory-ui/src/app/app.routes.ts
@apps/factory-ui/src/app/app.config.ts

<interfaces>
Token values, spacing scale (4/8/16/24/32/48/64px), typography (28/20/16/14 px, weights 400/600, Inter), dark + light hex + WCAG pairs are FULLY specified in 10-UI-SPEC (Design System / Spacing / Typography / Color sections). Implement exactly.
Shell structure (10-UI-SPEC Component 1): TopBar 64px (logo, LanguageToggle, ThemeToggle, SSE indicator, UserChip); NavigationRail 72px desktop / 56px 768-1023; BottomNav 64px <768. Persona routes + authorized roles in the route table. Nav copy (AREA OPERATORE etc.) in the Copywriting Contract.
Theme engine: mat.theme() mixin + CSS custom properties; prefers-color-scheme + manual toggle; SSR applies dark by default.
LanguageToggle/ThemeToggle/UserChip components are authored in plan 10-05/10-06 — here, leave typed placeholder selectors or simple stubs the shell can host.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens, typography, dark/light Material themes</name>
  <files>apps/factory-ui/src/styles/_tokens.scss, apps/factory-ui/src/styles/_typography.scss, apps/factory-ui/src/styles/_theme.dark.scss, apps/factory-ui/src/styles/_theme.light.scss, apps/factory-ui/src/styles.scss</files>
  <action>Create _tokens.scss declaring all --sft-* CSS custom properties for the DARK palette under :root (default) and the LIGHT palette under a [data-theme="light"] / .theme-light selector, plus --sft-space-* and --sft-type-* tokens — exact hex/values from the UI-SPEC Color/Spacing/Typography tables. _typography.scss: load Inter, define the 4 type roles (display/heading/body/label) with the specified size/weight/line-height; enforce no text <14px. _theme.dark.scss / _theme.light.scss: Angular Material theme via `mat.theme()` (or define-theme) mapping Material color roles to the SFT palette. Update styles.scss to @use the partials AFTER the `@import "tailwindcss"` line (preserve the 64px @layer override from 10-00a). Apply dark theme by default at :root so SSR renders dark.</action>
  <verify>
    <automated>nx build ui-factory --configuration=development 2>&1 | tail -5 && grep -q -- "--sft-surface" apps/factory-ui/src/styles/_tokens.scss && grep -q "data-theme=\"light\"\|theme-light" apps/factory-ui/src/styles/_tokens.scss && echo tokens-ok</automated>
  </verify>
  <done>Build succeeds; dark + light token sets present; Material theme compiles; 64px override preserved.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell + TopBar + NavigationRail + BottomNav</name>
  <files>apps/factory-ui/src/app/shell/app-shell.component.ts, apps/factory-ui/src/app/shell/top-bar.component.ts, apps/factory-ui/src/app/shell/nav-rail.component.ts, apps/factory-ui/src/app/shell/bottom-nav.component.ts</files>
  <action>Author standalone components. AppShell hosts TopBar + (NavigationRail on >=1024px, condensed 56px on 768-1023, BottomNav on <768 via CSS/breakpoint) + <router-outlet>. TopBar (64px): logo/area title, slots for LanguageToggle/ThemeToggle/SSE-indicator/UserChip (host the real components from 10-05/10-06 if available else lightweight stub selectors). Nav items use the persona route list with 64px min-height, active = accent indicator, uppercase section labels (AREA OPERATORE…). Add data-testid where the UI-SPEC Playwright contract requires shell-level ids (language-toggle/theme-toggle/sse-indicator hosted here). All interactive elements >=64px; aria-labels on icon-only buttons; respect prefers-reduced-motion. No browser-only API in constructors (SSR-safe).</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="app-shell" --passWithNoTests 2>&1 | tail -6 && grep -q "router-outlet" apps/factory-ui/src/app/shell/app-shell.component.ts && echo shell-ok</automated>
  </verify>
  <done>Shell components compile; router-outlet present; responsive nav + 64px touch + persona items implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Persona route map</name>
  <files>apps/factory-ui/src/app/app.routes.ts</files>
  <action>Replace the scaffold routes with: a public /auth/login route; an AppShell layout route whose children are the 5 lazy-loaded persona areas (/operator /technician /manager /admin /demo) with canActivate RBAC guard placeholders (the real guard arrives in 10-05 — reference its token/import path so 10-05 only fills the implementation). Default redirect to /auth/login when unauthenticated. Use loadComponent/loadChildren lazy imports pointing at the feature dirs (created in 10-06/10-07; use placeholder lazy targets that 10-07 replaces, OR minimal empty standalone components so routing compiles now). Map authorized roles per the UI-SPEC route table.</action>
  <verify>
    <automated>nx build ui-factory --configuration=development 2>&1 | tail -5 && grep -q "operator" apps/factory-ui/src/app/app.routes.ts && grep -q "demo" apps/factory-ui/src/app/app.routes.ts && echo routes-ok</automated>
  </verify>
  <done>All 5 persona routes + login declared; app builds and SSR-renders without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SSR render | Server renders without depending on browser-only globals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Denial of Service | SSR crash from browser API | mitigate | No localStorage/EventSource in component constructors; theme default applied via CSS, not browser detection (Pitfall 1/7). |
| T-10-04-02 | Elevation of Privilege | route access | mitigate | RBAC guard placeholders on every persona route (implemented in 10-05) — defense layered with backend require_roles. |
</threat_model>

<verification>
- ui-factory builds (dev) and SSR-renders dark theme without console errors.
- Token + theme + shell + routes present.
</verification>

<success_criteria>
A responsive, themeable (dark default / light toggle-ready) app shell renders the persona route skeleton via SSR with 64px touch targets and the locked design tokens.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-04-SUMMARY.md` when done.
</output>
