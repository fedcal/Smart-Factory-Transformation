---
phase: 10-backend-api-frontend
plan: 03
type: execute
wave: 4
depends_on: ["10-01", "10-02"]
files_modified:
  - apps/api-gateway/src/svc_api_gateway/routers/sse.py
  - apps/api-gateway/src/svc_api_gateway/security/rbac.py
  - apps/api-gateway/src/svc_api_gateway/main.py
  - infra/migrations/timescale/013_create_auth_users.sql
  - infra/migrations/timescale/tests/test_migration_013.py
  - apps/api-gateway/tests/integration/test_sse.py
autonomous: true
gap_closure: false
requirements: [SRV-02, SRV-04, UI-06, HITL-10]
must_haves:
  truths:
    - "GET /v1/stream/kpi, /v1/stream/approvals and /v1/stream/alerts return text/event-stream and emit kpi_update / approval_pending / alert_new + periodic sse_heartbeat events"
    - "SSE endpoints authenticate via a JWT query param (?token=) validated identically to the Authorization header (dev-mode per post_research_resolution #2)"
    - "The alert SSE channel enforces a 12 alerts/hour/persona rate limit (HITL-10), emitting a rate_limit event when exceeded"
    - "An OpenTelemetry FastAPI middleware adds spans to every endpoint; SSE responses set X-Accel-Buffering: no"
    - "Migration 013 seeds the auth_users reference table idempotently"
  artifacts:
    - path: "apps/api-gateway/src/svc_api_gateway/routers/sse.py"
      provides: "3 EventSourceResponse stream endpoints + heartbeat + rate limit"
      contains: "EventSourceResponse"
    - path: "infra/migrations/timescale/013_create_auth_users.sql"
      provides: "seeded persona users reference table"
      contains: "operator@mantis.it"
  key_links:
    - from: "apps/api-gateway/src/svc_api_gateway/routers/sse.py"
      to: "apps/api-gateway/src/svc_api_gateway/kpi/queries.py"
      via: "compute_kpi_snapshot inside the kpi generator"
      pattern: "compute_kpi_snapshot"
    - from: "apps/api-gateway/src/svc_api_gateway/main.py"
      to: "opentelemetry.instrumentation.fastapi"
      via: "FastAPIInstrumentor / OpenTelemetryMiddleware on build_app"
      pattern: "opentelemetry"
---

<objective>
Wave 3 backend: SSE streaming endpoints (kpi/approvals/alerts via sse-starlette), a query-param JWT validator for EventSource, the HITL-10 alert rate limit, the OTEL endpoint-span middleware, and migration 013 seeding the persona users table. Implements SRV-02 (SSE), SRV-04 (OTEL spans + the existing health probes), UI-06, HITL-10.

Purpose: the live dashboard + alert/approval push (UI-04/UI-03) consume these streams. Un-skips test_sse.py.

Decisions: SSE primary (gray-area-2 LOCKED). SSE JWT via query param (post_research_resolution #2 — EventSource cannot set headers; harden to HttpOnly cookie in F11). Rate-limit 12/hr/persona on alert channel (HITL-10).

Execution note: worktrees DISABLED — sequential on main tree. Wave 3; depends on 10-01 (rbac) + 10-02 (kpi queries + prior main.py edits). Last backend plan to touch main.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-RESEARCH.md
@apps/api-gateway/src/svc_api_gateway/main.py
@apps/api-gateway/src/svc_api_gateway/dependencies.py

<interfaces>
sse-starlette: `from sse_starlette.sse import EventSourceResponse`; yield dicts {"event": "<name>", "data": json.dumps(...)} from an async generator; break on `await request.is_disconnected()`.
SSE event contract (10-UI-SPEC SSE table): kpi_update→{kpi:KpiSnapshot} on /v1/stream/kpi; approval_pending→{approval_id,agent,tier,sla_seconds} + approval_resolved on /v1/stream/approvals; alert_new→{alert_id,message,severity} on /v1/stream/alerts; sse_heartbeat every 30s on all.
require_roles + decode_token from security/ (10-01). Add a require_roles_qs(...) variant accepting token from query param, validated via the same decode_token.
compute_kpi_snapshot from kpi/queries.py (10-02).
Migration style: see existing infra/migrations/timescale/012_extend_audit_scm.sql + its test_migration_012.py (idempotent CREATE ... IF NOT EXISTS, regression-safe).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query-param JWT validator + sse.py stream endpoints</name>
  <files>apps/api-gateway/src/svc_api_gateway/security/rbac.py, apps/api-gateway/src/svc_api_gateway/routers/sse.py, apps/api-gateway/tests/integration/test_sse.py</files>
  <action>In security/rbac.py add require_roles_qs(*allowed) — a dependency factory that reads `token` from the query string (fastapi Query) and validates it via decode_token, enforcing the role claim, raising 401/403 with generic details. Do NOT remove the header-based require_roles. Create routers/sse.py: APIRouter(prefix="/v1/stream", tags=["sse"]). Three GET endpoints (kpi, approvals, alerts) each Depends(require_roles_qs(...)) + get_pool, returning EventSourceResponse(async_generator). kpi generator: loop, break on request.is_disconnected(), yield kpi_update with compute_kpi_snapshot every 5s, sse_heartbeat every 30s. approvals/alerts generators: yield from the queue/audit source (poll the approvals table or accept a NATS-backed source if wired; otherwise poll DB) producing approval_pending/approval_resolved and alert_new. Set response header X-Accel-Buffering: no (RESEARCH Pitfall 5). Generic 500 handling (CR-02). Un-skip test_sse.py (mock pool + asyncio.sleep).</action>
  <verify>
    <automated>cd apps/api-gateway && uv run pytest tests/integration/test_sse.py -x -q</automated>
  </verify>
  <done>test_sse.py passes: kpi stream yields a kpi_update event; query-param token validated; X-Accel-Buffering header set.</done>
</task>

<task type="auto">
  <name>Task 2: HITL-10 alert rate limit (12/hr/persona)</name>
  <files>apps/api-gateway/src/svc_api_gateway/routers/sse.py</files>
  <action>In the alerts generator, track per-principal (by sub/email from the JWT) alert emission timestamps in an in-process sliding-window counter (e.g. deque of timestamps within the last hour). When the count would exceed 12, suppress further alert_new events and emit a single rate_limit event {"event":"rate_limit","data":{"limit":12,"window":"1h"}} until the window frees up. Keep state per principal in a module-level dict guarded for the single-process dev gateway (document that distributed enforcement is a Phase 11 concern). Add/extend a test asserting the 13th alert in an hour is suppressed and a rate_limit event is emitted.</action>
  <verify>
    <automated>cd apps/api-gateway && uv run pytest tests/integration/test_sse.py -x -q -k "rate" 2>&1 | tail -8</automated>
  </verify>
  <done>13th alert within the hour suppressed; rate_limit event emitted (HITL-10).</done>
</task>

<task type="auto">
  <name>Task 3: OTEL middleware + migration 013 (auth_users seed)</name>
  <files>apps/api-gateway/src/svc_api_gateway/main.py, infra/migrations/timescale/013_create_auth_users.sql, infra/migrations/timescale/tests/test_migration_013.py</files>
  <action>In main.py build_app(): wire OpenTelemetry endpoint spans via FastAPIInstrumentor.instrument_app(app) (from opentelemetry.instrumentation.fastapi) using a local import (CR-01 — exact name). Keep it best-effort: guard so a missing exporter does not crash startup (spans only this phase; full OTEL stack → Phase 11). Add local import + include_router for sse (`from svc_api_gateway.routers import sse as sse_router`). Create migration 013 creating an idempotent auth.auth_users (or auth schema) reference table seeded with the 5 persona rows (email, role, display_name) using INSERT ... ON CONFLICT DO NOTHING — no password hashes stored (dev SEEDED_USERS lives in code). Write test_migration_013.py mirroring test_migration_012.py: applies twice idempotently, asserts 5 seeded rows, regression-safe.</action>
  <verify>
    <automated>cd apps/api-gateway && uv run pytest tests/ -q 2>&1 | tail -6 && cd "$OLDPWD" && python -c "print('migration test runs in integration env')"</automated>
  </verify>
  <done>OTEL instrumentor attached without breaking startup; sse router registered; migration 013 idempotent + seeds 5 personas; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /v1/stream/* (token in query) | JWT in query string crosses to gateway |
| gateway → DB/NATS | Stream sources read live data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Information Disclosure | SSE token in URL | accept (dev) / transfer (F11) | Documented A3: query-param token validated identically to header; access-log exposure accepted in dev-mode, hardened to HttpOnly cookie in Phase 11. |
| T-10-03-02 | Denial of Service | alert flood | mitigate | HITL-10 12/hr/persona sliding-window rate limit emitting rate_limit event. |
| T-10-03-03 | Denial of Service | idle SSE behind proxy | mitigate | sse_heartbeat every 30s + X-Accel-Buffering:no (Pitfall 5). |
| T-10-03-04 | Elevation of Privilege | stream access | mitigate | require_roles_qs enforces role from signed JWT claim (V4). |
| T-10-03-05 | Information Disclosure | 500 body | mitigate | Generic body + structlog (CR-02). |
</threat_model>

<verification>
- test_sse.py passes (incl. rate-limit case).
- migration 013 idempotent + seeds 5 personas.
- Full backend `uv run pytest tests/ -q` green; app starts with OTEL instrumentor attached.
</verification>

<success_criteria>
Three SSE channels stream live KPI/approval/alert events with query-param JWT auth, heartbeat, and HITL-10 rate limiting; OTEL spans cover every endpoint; persona users seeded.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-03-SUMMARY.md` when done.
</output>
