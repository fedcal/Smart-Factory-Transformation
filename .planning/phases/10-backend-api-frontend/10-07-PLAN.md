---
phase: 10-backend-api-frontend
plan: 07
type: execute
wave: 7
depends_on: ["10-05", "10-06", "10-00b"]
files_modified:
  - apps/factory-ui/src/app/shared/kpi-tile/kpi-tile.component.ts
  - apps/factory-ui/src/app/shared/kpi-tile/kpi-tile.component.spec.ts
  - apps/factory-ui/src/app/shared/alert-feed/alert-feed.component.ts
  - apps/factory-ui/src/app/shared/alert-feed/alert-feed.component.spec.ts
  - apps/factory-ui/src/app/shared/approval-queue/approval-queue-feed.component.ts
autonomous: true
gap_closure: false
requirements: [UI-02, UI-04, UI-06, HITL-04, HITL-10]
must_haves:
  truths:
    - "KpiTile renders label/value/unit/delta and a status bar colored green/warning/destructive per the UI-SPEC thresholds, updating live from the SSE kpiSnapshot signal"
    - "AlertFeed lists alerts from the SSE alerts signal, shows a rate-limit banner when the 12/hour HITL-10 limit is hit, and uses aria-live polite"
    - "ApprovalQueueFeed renders pending ApprovalCards via CDK virtual scroll with a filter/sort bar and an empty state"
    - "KpiTile and AlertFeed carry the data-testid attributes the Playwright spec requires (kpi-grid/kpi-tile-*, alert-feed, rate-limit-banner)"
  artifacts:
    - path: "apps/factory-ui/src/app/shared/kpi-tile/kpi-tile.component.ts"
      provides: "KPI tile with threshold-colored status bar"
      contains: "status"
    - path: "apps/factory-ui/src/app/shared/alert-feed/alert-feed.component.ts"
      provides: "alert feed + rate-limit banner"
      contains: "rate-limit-banner"
    - path: "apps/factory-ui/src/app/shared/approval-queue/approval-queue-feed.component.ts"
      provides: "virtual-scroll pending approvals feed"
      contains: "cdk-virtual-scroll"
  key_links:
    - from: "apps/factory-ui/src/app/shared/kpi-tile/kpi-tile.component.ts"
      to: "apps/factory-ui/src/app/core/sse/sse.service.ts"
      via: "reads kpiSnapshot signal (computed status)"
      pattern: "kpiSnapshot"
    - from: "apps/factory-ui/src/app/shared/approval-queue/approval-queue-feed.component.ts"
      to: "apps/factory-ui/src/app/shared/approval-card/approval-card.component.ts"
      via: "renders ApprovalCard per queue item"
      pattern: "ApprovalCard|approval-card"
---

<objective>
Wave 6 frontend dashboard primitives: KpiTile (threshold-colored, SSE-live), AlertFeed (with HITL-10 rate-limit banner), and the virtual-scrolled ApprovalQueueFeed. Implements UI-04 (KPI tiles), UI-06 (live updates), HITL-04 (queue), HITL-10 (rate-limit banner). Un-skips kpi-tile + alert-feed specs.

Decisions: computed() KPI status from thresholds (gray-area-3 Signals). CDK VirtualScroll for the queue (RESEARCH Don't-Hand-Roll). KPI status always icon+color (accessibility — never color-only). data-testid per UI-SPEC Playwright contract.

Execution note: worktrees DISABLED — sequential on main tree. Wave 6; depends on 10-05 (SseService). Disjoint files from 10-06; uses ApprovalCard authored in 10-06 (so this plan is sequenced after 10-06 reads despite same wave — depends_on covers 10-05; ApprovalQueueFeed only imports ApprovalCard's public selector). To keep waves clean, the executor must run 10-06 before 10-07 on the sequential tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-UI-SPEC.md

<interfaces>
KpiTile spec + thresholds: 10-UI-SPEC Component 5 (KPI Tile + thresholds table: OEE/MTTR/MTBF/Scrap/Throughput/Downtime green/warning/red bands). AlertFeed + rate-limit banner: Component 9. ApprovalQueueFeed: Component 8 (FilterBar, CDK virtual scroll, EmptyState).
SseService signals (10-05): kpiSnapshot, alerts (+ rate_limit), approvals. KpiSnapshot fields mirror backend KpiSnapshot (10-02): oee/mttr/mtbf/scrap_rate/throughput/downtime.
data-testid: kpi-grid, kpi-tile-oee/-mttr/-mtbf/-scrap/-throughput/-downtime, alert-feed, rate-limit-banner.
Copy via transloco (it.json/en.json from 10-05). ApprovalCard public selector from 10-06.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KpiTile (threshold status, SSE-live)</name>
  <files>apps/factory-ui/src/app/shared/kpi-tile/kpi-tile.component.ts, apps/factory-ui/src/app/shared/kpi-tile/kpi-tile.component.spec.ts</files>
  <behavior>
    - Given a kpi key + value, computes status green/warning/destructive per the UI-SPEC threshold table (e.g. OEE>=85 green, 80-84 warning, <80 red); null value → grey "no data".
    - Renders label/value/unit/delta + a 4px status bar; status conveyed by icon AND color (not color alone).
    - data-testid kpi-tile-<key>.
  </behavior>
  <action>Implement KpiTile as an input-driven standalone component using computed() for status from the thresholds. min-height 80px, type tokens (28px value / 14px label). Provide a status-to-icon map (check_circle/warning/error) for accessibility. Add the kpi-tile-<key> data-testid. Un-skip kpi-tile.component.spec.ts (assert threshold→status mapping + data-testid).</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="kpi-tile" 2>&1 | tail -6</automated>
  </verify>
  <done>kpi-tile spec passes: threshold→status correct for all 6 KPIs; icon+color; data-testid present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AlertFeed (+ rate-limit banner) + ApprovalQueueFeed (virtual scroll)</name>
  <files>apps/factory-ui/src/app/shared/alert-feed/alert-feed.component.ts, apps/factory-ui/src/app/shared/alert-feed/alert-feed.component.spec.ts, apps/factory-ui/src/app/shared/approval-queue/approval-queue-feed.component.ts</files>
  <behavior>
    - AlertFeed renders items from the SSE alerts signal (agent chip, message, timestamp, optional "Vai all'approvazione" 64px button), aria-live polite, data-testid alert-feed.
    - When the rate_limit event fires (or >=12 alerts in the window), a rate-limit banner (data-testid rate-limit-banner) shows the HITL-10 copy.
    - ApprovalQueueFeed: header + count badge, FilterBar (Tutti/Solo miei/Per scadenza, sort), CDK virtual-scroll of ApprovalCards, EmptyState copy.
  </behavior>
  <action>Implement AlertFeed (max 12/hour visible per HITL-10) reading SseService.alerts; show rate-limit-banner on the rate_limit signal. Implement ApprovalQueueFeed using cdk-virtual-scroll-viewport rendering ApprovalCard per pending item (from SseService.approvals), with filter/sort signals + empty state. Copy via transloco. Un-skip alert-feed.component.spec.ts (assert 13th alert triggers banner + data-testid).</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="alert-feed|approval-queue" --passWithNoTests 2>&1 | tail -6 && grep -q "cdk-virtual-scroll" apps/factory-ui/src/app/shared/approval-queue/approval-queue-feed.component.ts && echo feeds-ok</automated>
  </verify>
  <done>alert-feed spec passes (rate-limit banner on limit); queue uses CDK virtual scroll + empty state; data-testid present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE payload → render | Untrusted alert/kpi/approval payloads rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Denial of Service | alert flood UI | mitigate | Rate-limit banner + capped feed mirror the backend HITL-10 12/hr limit. |
| T-10-07-02 | Tampering | XSS via alert message | mitigate | Angular text interpolation auto-escapes; no innerHTML. |
| T-10-07-03 | Denial of Service | large queue render | mitigate | CDK virtual scroll bounds rendered cards (HITL-04 scale). |
</threat_model>

<verification>
- kpi-tile + alert-feed specs pass.
- ApprovalQueueFeed uses CDK virtual scroll; data-testid coverage present.
</verification>

<success_criteria>
Live, accessible KPI tiles, alert feed (with rate-limit banner), and a virtual-scrolled approval queue are ready for the dashboards — all SSE-driven and to the design contract.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-07-SUMMARY.md` when done.
</output>
