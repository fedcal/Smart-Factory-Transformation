---
phase: 10-backend-api-frontend
plan: 05
type: execute
wave: 6
depends_on: ["10-04", "10-00b"]
files_modified:
  - apps/factory-ui/src/app/core/auth/jwt.service.ts
  - apps/factory-ui/src/app/core/auth/jwt.interceptor.ts
  - apps/factory-ui/src/app/core/auth/rbac.guard.ts
  - apps/factory-ui/src/app/core/sse/sse.service.ts
  - apps/factory-ui/src/app/core/theme/theme.service.ts
  - apps/factory-ui/src/app/core/i18n/locale.service.ts
  - apps/factory-ui/src/app/app.config.ts
  - apps/factory-ui/src/app/core/auth/jwt.service.spec.ts
  - apps/factory-ui/src/app/core/sse/sse.service.spec.ts
  - apps/factory-ui/src/assets/i18n/it.json
  - apps/factory-ui/src/assets/i18n/en.json
autonomous: true
gap_closure: false
requirements: [UI-06, UI-07, UI-05, SRV-01]
must_haves:
  truths:
    - "JwtService stores/reads the token only in the browser (isPlatformBrowser guard), exposes role()/isAuthenticated() signals, and clears on logout"
    - "The JWT interceptor attaches the Bearer header to /v1 and /auth/me requests; RbacGuard blocks routes whose required roles do not match the token role"
    - "SseService is a no-op on the server platform; in the browser it opens EventSource with ?token=, updating kpiSnapshot / approvals / alerts signals and connectionStatus, with auto-reconnect"
    - "ThemeService toggles dark/light via the data-theme attribute persisting in localStorage; LocaleService switches IT/EN at runtime via transloco WITHOUT page reload"
  artifacts:
    - path: "apps/factory-ui/src/app/core/sse/sse.service.ts"
      provides: "Signal-based SSE client, SSR-guarded"
      contains: "isPlatformBrowser"
    - path: "apps/factory-ui/src/app/core/auth/jwt.service.ts"
      provides: "token storage + role/isAuthenticated signals"
      contains: "isPlatformBrowser"
    - path: "apps/factory-ui/src/app/core/i18n/locale.service.ts"
      provides: "transloco runtime IT/EN switch"
      contains: "transloco"
  key_links:
    - from: "apps/factory-ui/src/app/core/auth/jwt.interceptor.ts"
      to: "apps/factory-ui/src/app/core/auth/jwt.service.ts"
      via: "reads token() signal to set Authorization header"
      pattern: "Authorization"
    - from: "apps/factory-ui/src/app/core/sse/sse.service.ts"
      to: "/v1/stream"
      via: "EventSource with ?token= query param"
      pattern: "EventSource"
---

<objective>
Wave 5 frontend core services: JwtService + JwtInterceptor + RbacGuard (consume the 10-01 backend), the Signal-based SseService (consume 10-03 streams), ThemeService (dark/light), and LocaleService (transloco runtime IT/EN). Implements UI-06 (SSE), UI-07 (i18n no-reload), UI-05 (theme toggle), and the client side of SRV-01. Un-skips jwt.service.spec.ts + sse.service.spec.ts.

Decisions: Signals + injectable services, NO NgRx (gray-area-3 LOCKED). transloco for runtime i18n (post_research_resolution #1 — NOT @angular/localize for text). SSE JWT via query param (post_research_resolution #2). EventSource MUST be isPlatformBrowser-guarded (RESEARCH Pitfall 1); localStorage browser-guarded (Pitfall 7).

Execution note: worktrees DISABLED — sequential on main tree. Wave 5; depends on 10-04 (shell/routes for guard wiring) + 10-00b (specs). Edits app.config.ts (providers) — disjoint from 10-06/10-07 component files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-api-frontend/10-CONTEXT.md
@.planning/phases/10-backend-api-frontend/10-UI-SPEC.md
@.planning/phases/10-backend-api-frontend/10-RESEARCH.md
@apps/factory-ui/src/app/app.config.ts

<interfaces>
SSE event contract + reconnection (1s→2s→4s→30s backoff, banner after 5s disconnect) in 10-UI-SPEC SSE section. Signal names from sse.service.spec.ts (10-00b): kpiSnapshot, connectionStatus, plus approvals/alerts signals.
Backend: POST /auth/login (10-01) returns {access_token, role}; GET /auth/me; /v1/* needs Bearer; /v1/stream/* needs ?token= (10-03).
transloco: configure provideTransloco in app.config.ts with available langs ['it','en'], defaultLang 'it', lazy translation loader reading assets/i18n/{lang}.json. SSR renders 'it'.
RBAC route table + persona→role mapping in 10-UI-SPEC. localStorage keys: sft_locale, sft_theme, and a token key.
Copy keys: seed it.json/en.json from the 10-UI-SPEC Copywriting Contract table (login CTA, approve/reject, empty/error states, nav labels, SSE indicator, etc.).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JwtService + JwtInterceptor + RbacGuard</name>
  <files>apps/factory-ui/src/app/core/auth/jwt.service.ts, apps/factory-ui/src/app/core/auth/jwt.interceptor.ts, apps/factory-ui/src/app/core/auth/rbac.guard.ts, apps/factory-ui/src/app/core/auth/jwt.service.spec.ts</files>
  <behavior>
    - JwtService.setToken/getToken touch localStorage only when isPlatformBrowser; role() and isAuthenticated() are signals derived from the decoded (unverified, client-side exp check only) token; logout clears.
    - jwt.interceptor adds Authorization: Bearer <token> to /v1 and /auth/me requests, not to /auth/login.
    - rbac.guard (CanActivateFn) allows when JwtService.role() in route.data.roles, else redirects to /auth/login.
  </behavior>
  <action>Implement JwtService (signals, browser-guarded storage, decode exp client-side only — server still authoritative). Implement the functional jwt.interceptor.ts and rbac.guard.ts (CanActivateFn). Fill the route guards declared as placeholders in 10-04 app.routes.ts by pointing them at rbacGuard with data.roles per the UI-SPEC table. Un-skip jwt.service.spec.ts.</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="jwt.service" 2>&1 | tail -6</automated>
  </verify>
  <done>jwt.service.spec.ts passes; interceptor + guard wired; no localStorage access on server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SseService (Signal-based, SSR-guarded)</name>
  <files>apps/factory-ui/src/app/core/sse/sse.service.ts, apps/factory-ui/src/app/core/sse/sse.service.spec.ts</files>
  <behavior>
    - connect(token) returns immediately (no-op) when !isPlatformBrowser.
    - In browser, opens EventSource(`/v1/stream/kpi?token=...`) (+ approvals/alerts), maps kpi_update→kpiSnapshot signal, approval_pending/approval_resolved→approvals signal, alert_new→alerts signal, sse_heartbeat→connectionStatus 'connected'.
    - onerror sets connectionStatus 'disconnected' and triggers backoff reconnect (1→2→4→max 30s).
  </behavior>
  <action>Implement SseService with the signals from the spec. CRITICAL guard isPlatformBrowser as the first statement of connect()/any browser method (Pitfall 1). Implement exponential-backoff reconnect with a max of 30s; expose a computed disconnectedTooLong (>5s) for the banner. Un-skip sse.service.spec.ts.</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="sse.service" 2>&1 | tail -6</automated>
  </verify>
  <done>sse.service.spec.ts passes; server no-op confirmed; kpi_update sets signal; heartbeat sets connected.</done>
</task>

<task type="auto">
  <name>Task 3: ThemeService + LocaleService (transloco) + app.config providers + i18n catalogs</name>
  <files>apps/factory-ui/src/app/core/theme/theme.service.ts, apps/factory-ui/src/app/core/i18n/locale.service.ts, apps/factory-ui/src/app/app.config.ts, apps/factory-ui/src/assets/i18n/it.json, apps/factory-ui/src/assets/i18n/en.json</files>
  <action>ThemeService: toggle('dark'|'light') sets document.documentElement data-theme (browser-guarded), persists sft_theme in localStorage, defaults dark; SSR keeps dark. LocaleService: wraps transloco — switchLang('it'|'en') updates the active lang at runtime (NO reload), persists sft_locale, updates document.documentElement.lang. In app.config.ts add provideTransloco (langs it/en, default it, lazy http loader for assets/i18n/{lang}.json), provideHttpClient(withInterceptors([jwtInterceptor])), and any APP_INITIALIZER restoring persisted locale/theme on the browser. Author it.json (Italian primary) and en.json (English) from the UI-SPEC Copywriting Contract — every visible string keyed (login, approve/reject, empty/error states, nav labels, SSE indicator, rate-limit/governor banners, persona demo step labels).</action>
  <verify>
    <automated>nx test ui-factory --testPathPattern="theme|locale" --passWithNoTests 2>&1 | tail -6 && node -e "JSON.parse(require('fs').readFileSync('apps/factory-ui/src/assets/i18n/it.json'));JSON.parse(require('fs').readFileSync('apps/factory-ui/src/assets/i18n/en.json'));console.log('i18n-json-ok')"</automated>
  </verify>
  <done>Theme/locale services compile; provideTransloco + interceptor wired in app.config; it.json/en.json valid JSON covering the Copywriting Contract keys; runtime EN switch with no reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → gateway (Bearer / SSE token) | Client attaches signed JWT to API + stream requests |
| SSR server → browser globals | Server must not touch localStorage/EventSource |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Denial of Service | SSR crash | mitigate | isPlatformBrowser guards on SseService + JwtService + ThemeService browser access (Pitfall 1/7). |
| T-10-05-02 | Elevation of Privilege | route access | mitigate | RbacGuard enforces role from token; backend require_roles is the authoritative layer (defense in depth, V4). |
| T-10-05-03 | Information Disclosure | token in storage | accept (dev) / transfer (F11) | localStorage token acceptable in dev-mode; HttpOnly cookie hardening deferred to Phase 11 (A3). |
| T-10-05-04 | Tampering | client exp check | mitigate | Client exp check is advisory only; server validates signature/exp on every request. |
</threat_model>

<verification>
- jwt.service.spec.ts + sse.service.spec.ts pass (un-skipped from 10-00b).
- Theme/locale compile; i18n JSON valid; transloco provides runtime EN switch with no reload.
</verification>

<success_criteria>
All four core services (auth, SSE, theme, i18n) are operational and SSR-safe: token-bearing API calls, live Signal-driven SSE, dark/light toggle, and runtime IT/EN switch without page reload.
</success_criteria>

<output>
Create `.planning/phases/10-backend-api-frontend/10-05-SUMMARY.md` when done.
</output>
