---
phase: 07-agents-maintenance-reliability
plan: 12
plan_id: 07-12
type: tdd
wave: 5
depends_on: ["07-00", "07-03", "07-06", "07-07", "07-08", "07-09", "07-10"]
files_modified:
  - tests/e2e/maintenance/conftest.py
  - tests/e2e/maintenance/test_predictive_maintenance_scenarios.py
  - tests/e2e/maintenance/test_rca_specialist_scenarios.py
  - tests/e2e/maintenance/test_maintenance_coach_scenarios.py
  - tests/e2e/maintenance/test_downtime_analyzer_scenarios.py
  - tests/fixtures/mnt_scenarios/predictive-maintenance/happy.yaml
  - tests/fixtures/mnt_scenarios/predictive-maintenance/degraded.yaml
  - tests/fixtures/mnt_scenarios/predictive-maintenance/failure.yaml
  - tests/fixtures/mnt_scenarios/rca-specialist/happy.yaml
  - tests/fixtures/mnt_scenarios/rca-specialist/degraded.yaml
  - tests/fixtures/mnt_scenarios/rca-specialist/failure.yaml
  - tests/fixtures/mnt_scenarios/maintenance-coach/happy.yaml
  - tests/fixtures/mnt_scenarios/maintenance-coach/degraded.yaml
  - tests/fixtures/mnt_scenarios/maintenance-coach/failure.yaml
  - tests/fixtures/mnt_scenarios/maintenance-coach/auto_open.yaml
  - tests/fixtures/mnt_scenarios/downtime-analyzer/happy.yaml
  - tests/fixtures/mnt_scenarios/downtime-analyzer/degraded.yaml
  - tests/fixtures/mnt_scenarios/downtime-analyzer/failure.yaml
  - tests/fixtures/llm_responses/rca-specialist/happy.jsonl
  - tests/fixtures/llm_responses/rca-specialist/degraded.jsonl
  - tests/fixtures/llm_responses/rca-specialist/failure.jsonl
  - tests/fixtures/llm_responses/maintenance-coach/happy.jsonl
  - tests/fixtures/llm_responses/maintenance-coach/degraded.jsonl
  - tests/fixtures/llm_responses/maintenance-coach/failure.jsonl
  - tests/fixtures/llm_responses/maintenance-coach/auto_open.jsonl
autonomous: true
requirements: [MNT-01, MNT-02, MNT-03, MNT-04, MNT-06]
objective: "Phase 7 closing deliverable: replace Wave 0 stub e2e scaffolds with the full 12-scenario suite (4 agents × happy / degraded / failure each), authored deterministic scenario YAML + 6 mock LLM JSONL fixtures (only for RCASpecialist and MaintenanceCoach — PredictiveMaintenance is deterministic ML, DowntimeAnalyzer is deterministic SQL aggregation per 07-VALIDATION.md L91-95), real testcontainers-backed conftest.py (PG+TimescaleDB+NATS, with Qdrant+Neo4j inherited from Phase 5 fixtures for RCA + Coach scenarios). Mirror Phase 6 06-13 pattern exactly. Success criterion: 12/12 E2E pass via mock LLM backend (`LLM_BACKEND=mock` from Phase 6 D-X-01 / 06-03 MockReplayChatModel)."
must_haves:
  truths:
    - "13 scenario YAML files exist under `tests/fixtures/mnt_scenarios/<agent>/<scenario>.yaml` (PM/RCA/DA × 3 + maintenance-coach × 4 [happy/degraded/failure/auto_open per Open Q3 RESOLVED contract]) and validate via `yaml.safe_load`."
    - "7 mock LLM JSONL files exist under `tests/fixtures/llm_responses/{rca-specialist,maintenance-coach}/<scenario>.jsonl` (RCA × 3 + Coach × 4 [happy/degraded/failure/auto_open]; NO JSONL for predictive-maintenance — deterministic ML — and NO JSONL for downtime-analyzer — deterministic SQL aggregation; per 07-VALIDATION.md L91-95)."
    - "Each scenario YAML has top-level keys: `name`, `description`, `input` (full request body for the api-gateway endpoint), `seed` (pre-population for testcontainers: pg_audit_rows, pg_downtime_events, nats_messages, qdrant_chunks, neo4j_facts, sft_assets_overrides), `mock_llm_fixture` (path to JSONL, omitted for PM/DA deterministic), `expected` (decision, action_type, tool_calls, audit_rows, citations_min, hitl_status, OEE bounds, RUL bounds, MTTR bounds — per agent)."
    - "tests/e2e/maintenance/conftest.py exposes async fixtures: `mnt_scenario(request)` (yaml.safe_load per parametrized scenario), `mock_llm_backend(monkeypatch, mnt_scenario)` (sets LLM_BACKEND=mock + MOCK_LLM_FIXTURE), `seed_testcontainers(mnt_scenario, pg, nats, qdrant, neo4j)` (pre-populates each container from scenario.seed), `api_gateway_client(...)` (FastAPI TestClient with dependency_overrides routing to testcontainer fixtures + mock LLM)."
    - "4 E2E test modules (1 per agent), each `@pytest.mark.parametrize('scenario', ['happy', 'degraded', 'failure'])` × async function; 12 tests total."
    - "All 12 E2E tests pass via `pytest tests/e2e/maintenance/ -m 'e2e and not real-llm' --timeout=600` within ~480s on testcontainers (mirror 06-13 budget)."
    - "Per-agent scenario semantics (mirror 06-13 OPS scenarios):"
    - "PM happy: AD NATS publish on `maintenance.predict.LOOM-01` with severity=major + triggered_by_action_id → PM consumer triggers PredictiveMaintenance.__call__ → mocked sensor window from PG yields synthetic features → joblib predicts rul_cycles in band [50,125] → health_index ≥ 0.3 → Decision.AUTO + audit row RUL_ESTIMATE with triggered_by_action_id link verifiable via SQL JOIN to seeded AnomalyDetector audit row."
    - "PM degraded: forced low-RUL features → rul_cycles < 38 → health_index < 0.3 → escalate_to_supervisor invoked BEFORE audit write (Pitfall §3) → Decision.HITL_SUPERVISOR + recommended_action non-empty IT string."
    - "PM failure: malformed NATS payload (invalid PredictRequest schema, e.g. severity='unknown') OR sensor window empty (no PG rows) → consumer ack-drops or agent returns {} + structlog WARN → no audit row written, no crash."
    - "RCA happy: 3-call LLM mock yielding valid 5-Why RCAChain with all citations valid (source_uri exists in seeded PG documents table) → validator passes → escalate_to_supervisor invoked → Decision.HITL_SUPERVISOR + audit RCA_CHAIN with chain_id, hitl_status='supervisor_pending'."
    - "RCA degraded: 1st LLM call returns invalid JSON (or invalid citation source_uri) → retry corrective prompt fires → 2nd call succeeds → validated chain → still escalate → 1 retry recorded in evidence_panel.tool_calls."
    - "RCA failure: 2 LLM calls return invalid output (citation orphan or schema fail) → retries exhausted → escalate with reason='rca_validation_exhausted' + best_attempt in audit; validation_exhausted=true in response."
    - "Coach happy: 5-step intervention completes; each step LLM returns SOP guidance, technician_input='completato' resumes → final mttr_end set; compute_mttr_minutes > 0; 5 audit COACH_STEP rows + completion row."
    - "Coach degraded: technician_input contains keyword 'aiuto' mid-flow → LLM invokes request_help → graph interrupts state='awaiting_help' → resume_after_help with supervisor_input completes thread → audit row carries escalation_trigger='technician_request'."
    - "Coach failure: LLM returns guidance referencing non-existent step (e.g. step 99) → graph cannot route → agent surfaces error; intervention not completed; partial state preserved in checkpoint for forensic review."
    - "DA happy: seed 10 DowntimeEvents over 1-hour window for LOOM-01 with reason_codes from failure_modes.yaml taxonomy (07-02); seed 1 QUALITY_VERDICT audit row for the same window; invoke /v1/agents/downtime-analyzer/report → OEEReport.quality from audit source (structlog `oee_quality_source_audit`); Pareto top-3 reason_codes match seeded distribution."
    - "DA degraded: NO QUALITY_VERDICT rows (cross-cluster gap) but production_state sim metrics present → OEEReport.quality from sim fallback (structlog `oee_quality_source_simfallback`); report still produced."
    - "DA failure: invalid ReportRequest body (window_end < window_start) → 422 from api-gateway; OR malformed NATS DowntimeEvent payload → consumer ack-drops with poison-pill log, report unaffected."
    - "Audit chain verification (MNT-06): PM happy scenario asserts via SQL `SELECT FROM audit.actions WHERE action_type='RUL_ESTIMATE' AND evidence_panel->'tool_calls'->0->'args'->>'triggered_by_action_id' = <AD action_id>` returns 1 row — proves the cross-cluster Open Q1 wiring end-to-end."
  artifacts:
    - path: tests/e2e/maintenance/conftest.py
      provides: "shared fixtures: testcontainers PG+TSDB+NATS+Qdrant+Neo4j; mock_llm_backend; mnt_scenario loader; seed_testcontainers; api_gateway_client"
    - path: tests/e2e/maintenance/test_predictive_maintenance_scenarios.py
      provides: "3 scenario parametrized tests for PM"
    - path: tests/e2e/maintenance/test_rca_specialist_scenarios.py
      provides: "3 scenario parametrized tests for RCA"
    - path: tests/e2e/maintenance/test_maintenance_coach_scenarios.py
      provides: "3 scenario parametrized tests for Coach (multi-turn checkpoint replay)"
    - path: tests/e2e/maintenance/test_downtime_analyzer_scenarios.py
      provides: "3 scenario parametrized tests for DA"
    - path: tests/fixtures/mnt_scenarios/<agent>/{happy,degraded,failure}.yaml × 12
      provides: "Deterministic scenario specs"
    - path: tests/fixtures/llm_responses/{rca-specialist,maintenance-coach}/<scenario>.jsonl × 6
      provides: "Mock LLM replay fixtures (record/replay via MockReplayChatModel 06-03)"
  key_links:
    - from: tests/e2e/maintenance/test_*_scenarios.py
      to: apps/api-gateway/src/svc_api_gateway/routers/maintenance_agents.py (07-10)
      via: "FastAPI TestClient → /v1/agents/<slug>/<action>"
      pattern: "api_gateway_client.post|/v1/agents/"
    - from: tests/e2e/maintenance/test_predictive_maintenance_scenarios.py (happy)
      to: AnomalyDetector audit row (Phase 6 + 07-06 hook)
      via: "SQL JOIN on triggered_by_action_id"
      pattern: "triggered_by_action_id|RUL_ESTIMATE|ANOMALY_ALERT"
    - from: tests/fixtures/llm_responses/rca-specialist/*.jsonl
      to: packages/sft-agents/src/sft_agents/llm/mock.py (MockReplayChatModel 06-03)
      via: "LLM_BACKEND=mock + MOCK_LLM_FIXTURE env vars"
      pattern: "MockReplayChatModel|prompt_hash"
---

<objective>
Phase 7 closing deliverable: end-to-end test suite that locks in the user-visible success criteria from ROADMAP Phase 7 (4 agents × 3 scenarios = 12 E2E tests). The plan mirrors Phase 6 06-13 exactly — same testcontainers wiring philosophy, same mock LLM strategy, same scenario YAML schema — adapted to maintenance-specific semantics. This is the gate that proves the entire 07-00 → 07-11 stack works coherently against real PG/TimescaleDB/NATS containers + Phase 5 Qdrant/Neo4j fixtures for RCA + Coach.

Purpose: Mechanical verification of MNT-01 (PM RUL inference + cross-cluster trigger), MNT-02 (RCA 5-Why + always-supervisor HITL), MNT-03 (Coach checkpoint resume + MTTR + request_help), MNT-04 (DA OEE cross-cluster + Pareto + dual source) end-to-end. Plus MNT-06 audit chain (triggered_by_action_id link verifiable via SQL JOIN in PM happy scenario).
Output: 1 real conftest + 4 scenario test modules + 12 YAML + 6 JSONL fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md
@.planning/phases/07-agents-maintenance-reliability/07-RESEARCH.md
@.planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md
@.planning/phases/07-agents-maintenance-reliability/07-VALIDATION.md
@.planning/phases/07-agents-maintenance-reliability/07-{06,07,08,09,10}-PLAN.md
@.planning/phases/06-agents-operations-production/06-13-PLAN.md
@.planning/phases/06-agents-operations-production/06-03-PLAN.md
@tests/e2e/ops/conftest.py
@tests/e2e/ops/test_anomaly_detector_scenarios.py
@tests/e2e/ops/test_quality_inspector_scenarios.py
@tests/fixtures/ops_scenarios/anomaly-detector/happy.yaml
@tests/fixtures/llm_responses/quality-inspector/happy.jsonl
@packages/sft-agents/src/sft_agents/llm/mock.py
@packages/sft-agents/tests/conftest.py
@packages/sft-knowledge/tests/conftest.py

<interfaces>
From 06-13 `tests/e2e/ops/conftest.py` (verbatim pattern to mirror):
- session-scoped testcontainer fixtures: `pg_container` (TimescaleDB pg16, all migrations applied 001..009), `nats_container` (JetStream + bootstrap MAINTENANCE_STREAM via scripts), `qdrant_container` (Phase 5 reuse), `neo4j_container` (Phase 5 reuse).
- function-scoped fixtures: `mock_llm_backend(monkeypatch, scenario_fixture_path)`, `<cluster>_scenario(request)`, `seed_testcontainers(scenario, ...)`, `api_gateway_client(...)`.

From 06-03 MockReplayChatModel:
- env vars: `LLM_BACKEND=mock` + `MOCK_LLM_FIXTURE=<path/to.jsonl>` activate the mock.
- JSONL line format: `{"prompt_hash": "<sha256-or-empty>", "response": {"content": "...", "tool_calls": [...], "usage_metadata": {...}}}`.
- Hash-based lookup with ordered-fallback (06-13 Task 2 convention — empty prompt_hash + ordered replay is acceptable for test authoring tractability).

Scenario YAML schema (per 06-13 Task 1 convention):
```yaml
name: <agent>-<scenario>
description: <one-line>
input:
  endpoint: POST /v1/agents/<slug>/<action>
  body: { ... full Pydantic-valid request body ... }
seed:
  pg_audit_rows: [ ... rows to insert into audit.actions before invocation ... ]
  pg_downtime_events: [ ... rows for maintenance.downtime_events ... ]
  pg_documents: [ ... rows for documents table (source_uri lookup for RCA) ... ]
  nats_messages: [ ... messages to publish before/during invocation ... ]
  qdrant_chunks: [ ... vectors + metadata for SOP retrieval ... ]
  neo4j_facts: [ ... cypher CREATE statements for graph relations ... ]
  sft_assets_overrides: [ ... custom asset registry entries ... ]
  production_state_overrides: { good_meters: int, target_meters: int, ... }  # for OEE.P / OEE.Q fallback
mock_llm_fixture: tests/fixtures/llm_responses/<agent>/<scenario>.jsonl  # omitted for PM + DA
expected:
  status_code: int
  decision: <auto|hitl_supervisor>
  action_type: <RUL_ESTIMATE|RCA_CHAIN|COACH_STEP|DOWNTIME_VERDICT|OEE_REPORT>
  audit_rows_min: int
  tool_calls_includes: list[str]
  hitl_status: <supervisor_pending|approved|null>
  # PM-specific
  rul_cycles_band: [int, int]
  health_index_min: float | null  # null if HITL expected
  triggered_by_action_id_present: bool
  # RCA-specific
  citations_min_per_step: int
  validation_exhausted: bool
  # Coach-specific
  completed_steps_count: int
  mttr_minutes_min: int | null
  awaiting_help: bool
  escalation_trigger: <technician_request|null>
  # DA-specific
  oee_bounds: [float, float]
  quality_source: <audit|simfallback>
  pareto_top: list[str]  # expected top reason_codes
```

Test module skeleton (per 06-13 Task 4):
```python
import pytest

pytestmark = [pytest.mark.e2e, pytest.mark.integration, pytest.mark.asyncio]

@pytest.mark.timeout(120)
@pytest.mark.parametrize("scenario", ["happy", "degraded", "failure"])
async def test_predictive_maintenance_scenario(
    scenario, mnt_scenario, mock_llm_backend, seed_testcontainers, api_gateway_client
):
    spec = mnt_scenario
    response = await api_gateway_client.post(spec["input"]["endpoint"], json=spec["input"]["body"])
    assert response.status_code == spec["expected"]["status_code"]
    # Per-agent assertions (RUL bounds, audit row count + triggered_by_action_id, HITL escalation, ...)
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12 scenario YAML files (4 agents × happy / degraded / failure) replacing Wave 0 stubs</name>
  <files>
    tests/fixtures/mnt_scenarios/predictive-maintenance/happy.yaml,
    tests/fixtures/mnt_scenarios/predictive-maintenance/degraded.yaml,
    tests/fixtures/mnt_scenarios/predictive-maintenance/failure.yaml,
    tests/fixtures/mnt_scenarios/rca-specialist/happy.yaml,
    tests/fixtures/mnt_scenarios/rca-specialist/degraded.yaml,
    tests/fixtures/mnt_scenarios/rca-specialist/failure.yaml,
    tests/fixtures/mnt_scenarios/maintenance-coach/happy.yaml,
    tests/fixtures/mnt_scenarios/maintenance-coach/degraded.yaml,
    tests/fixtures/mnt_scenarios/maintenance-coach/failure.yaml,
    tests/fixtures/mnt_scenarios/maintenance-coach/auto_open.yaml,
    tests/fixtures/mnt_scenarios/downtime-analyzer/happy.yaml,
    tests/fixtures/mnt_scenarios/downtime-analyzer/degraded.yaml,
    tests/fixtures/mnt_scenarios/downtime-analyzer/failure.yaml
  </files>
  <read_first>
    tests/fixtures/ops_scenarios/anomaly-detector/happy.yaml + degraded.yaml + failure.yaml (Phase 6 06-13 reference scenarios — copy structure, adapt semantics),
    tests/fixtures/ops_scenarios/quality-inspector/*.yaml (multi-source seed pattern),
    .planning/phases/07-agents-maintenance-reliability/07-{06,07,08,09}-PLAN.md (per-agent contracts: response shapes, audit content, HITL gates),
    .planning/phases/07-agents-maintenance-reliability/07-VALIDATION.md (per-task verification map — scenarios should align with test row),
    packages/sft-domain/failure_modes.yaml (07-02 — reason_codes available for DA + Coach seeds),
    apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/models.py (PredictRequest shape for PM seed)
  </read_first>
  <action>
    Replace each Wave 0 stub YAML with a full deterministic spec per behavior described in the must_haves section.

    Concrete examples (the executor authors all 12 per spec; here the planner gives 4 representative examples + leaves the rest as systematic completions):

    **predictive-maintenance/happy.yaml** (skeleton — executor fills synthetic sensor values):
    ```yaml
    name: predictive-maintenance-happy
    description: "AD-triggered RUL inference on LOOM-01 with healthy band (rul ≈ 80, health > 0.6)"
    input:
      endpoint: POST /v1/agents/predictive-maintenance/score
      body:
        asset_id: "LOOM-01"
        triggered_by_action_id: "00000000-0000-4000-8000-000000000001"
        user_roles: ["technician"]
    seed:
      pg_audit_rows:
        - { action_id: "00000000-0000-4000-8000-000000000001", agent_id: "anomaly-detector", action_type: "ANOMALY_ALERT", cluster: "ops", decision: "auto", ts: "2026-05-23T08:00:00Z" }
      pg_sensor_events:  # TimescaleDB hypertable for QueryTimescaleTool window pull
        - { asset_id: "LOOM-01", sensor_id: "loom_temperature", value: 65.2, timestamp: "2026-05-23T08:30:00Z" }
        - { asset_id: "LOOM-01", sensor_id: "warp_tension", value: 42.1, timestamp: "2026-05-23T08:30:30Z" }
        # ... ~60 samples spanning the 60-min window
      sft_assets_overrides:
        - { asset_id: "LOOM-01", asset_family: "loom" }
      production_state_overrides:
        LOOM-01: { good_meters: 980, target_meters: 1000 }
    expected:
      status_code: 200
      decision: "auto"
      action_type: "RUL_ESTIMATE"
      audit_rows_min: 2  # AD seeded + PM new
      tool_calls_includes: ["query_timescale"]
      rul_cycles_band: [38, 125]
      health_index_min: 0.3
      triggered_by_action_id_present: true
    ```

    **predictive-maintenance/degraded.yaml**: same input, but `pg_sensor_events` populated with values close to baseline limits forcing low RUL (Ridge predicts <38 cycles); `expected.decision="hitl_supervisor"`, `health_index_min: null` (will be <0.3).

    **predictive-maintenance/failure.yaml**: input `asset_id="GHOST-99"` (unknown to sft_assets registry) → agent returns {} structlog WARN → no audit row; `expected.status_code: 200, audit_rows_min: 1` (only the AD seeded row, no PM row).

    **rca-specialist/happy.yaml**:
    ```yaml
    name: rca-specialist-happy
    description: "5-Why analysis on broken_end downtime with valid citations from SOP corpus"
    input:
      endpoint: POST /v1/agents/rca-specialist/analyze
      body:
        problem_statement: "Loom L01 ha avuto un downtime di 30 minuti per rottura del filo di ordito alle 08:00."
        downtime_event_id: "evt-001"
        asset_id: "LOOM-01"
        user_roles: ["technician", "supervisor"]
        lang: "it"
    seed:
      pg_documents:  # for source_uri lookup in RCAChainValidator
        - { source_uri: "doc://SOP-LOOM-001#step1", title: "SOP rottura filo ordito - ispezione" }
        - { source_uri: "doc://SOP-LOOM-001#step2", title: "SOP rottura filo ordito - intervento" }
        - { source_uri: "doc://MAN-LOOM-OEM#warp", title: "Manuale OEM - sistema ordito" }
      qdrant_chunks:  # for rag_search retrieval
        - { id: "chunk-1", source_uri: "doc://SOP-LOOM-001#step1", text: "Ispezione visiva...", vector_seed: 42 }
        # ...
      neo4j_facts:
        - "CREATE (m:Machine {asset_id:'LOOM-01'})-[:HAS_PART]->(p:Part {name:'warp'})-[:HAS_FAILURE_MODE]->(fm:FailureMode {id:'broken_end'})-[:DOCUMENTED_BY]->(s:SOP {id:'SOP-LOOM-001'})"
    mock_llm_fixture: tests/fixtures/llm_responses/rca-specialist/happy.jsonl
    expected:
      status_code: 202
      decision: "hitl_supervisor"
      action_type: "RCA_CHAIN"
      audit_rows_min: 1
      tool_calls_includes: ["rag_search", "traverse_graph", "escalate_to_supervisor"]
      hitl_status: "supervisor_pending"
      citations_min_per_step: 1
      validation_exhausted: false
    ```

    **rca-specialist/degraded.yaml**: 1st LLM call returns a chain with one orphan source_uri (e.g. `doc://nonexistent`) → validator raises OrphanCitationError → retry corrective prompt fires → 2nd LLM call returns valid chain → success after 1 retry. `expected.validation_exhausted: false`, audit evidence_panel contains retry marker.

    **rca-specialist/failure.yaml**: 3 consecutive LLM calls return invalid chains (orphan citations + schema fails) → retries exhausted → escalate with best_attempt; `expected.validation_exhausted: true`.

    **maintenance-coach/happy.yaml**:
    ```yaml
    name: maintenance-coach-happy
    description: "5-step intervention completed without help request, MTTR ~25min"
    input:
      endpoint: POST /v1/agents/maintenance-coach/start  # then 5 subsequent step calls
      body:
        intervention_id: "INTV-001"
        asset_id: "LOOM-01"
        sop_id: "SOP-LOOM-001"
        technician_id: "TECH-007"
        user_roles: ["technician"]
      multi_turn:  # E2E framework iterates start + 5 step calls per the JSONL replay
        - { endpoint: POST /v1/agents/maintenance-coach/step, body: { intervention_id: "INTV-001", technician_input: "Ho ispezionato il filo." } }
        - { endpoint: POST /v1/agents/maintenance-coach/step, body: { intervention_id: "INTV-001", technician_input: "Ho rimosso il filo rotto." } }
        # ... up to step 5
    seed:
      qdrant_chunks: [ ... 5 SOP step chunks ... ]
    mock_llm_fixture: tests/fixtures/llm_responses/maintenance-coach/happy.jsonl
    expected:
      status_code: 200
      decision: "auto"
      action_type: "COACH_STEP"
      audit_rows_min: 5  # 1 per step
      completed_steps_count: 5
      mttr_minutes_min: 1
      awaiting_help: false
      escalation_trigger: null
    ```

    **maintenance-coach/degraded.yaml**: mid-flow step 3, technician_input="aiuto, non capisco questo passaggio" → LLM invokes request_help → graph interrupts → resume_after_help with supervisor_input → intervention continues + completes. `expected.escalation_trigger: "technician_request"`, audit row marker present.

    **maintenance-coach/failure.yaml**: LLM at step 4 returns guidance referencing a non-existent step (SOP-LOOM-001 has only 5 steps but LLM says "vai al passo 99") → agent surfaces error response, intervention not completed; checkpoint preserved. `expected.status_code: 500` (or similar), `awaiting_help: false`, `completed_steps_count: 3`.

    **maintenance-coach/auto_open.yaml** (NUOVO — Open Q3 RESOLVED contract):
    ```yaml
    name: maintenance-coach-auto-open
    description: "RCA recommendation auto-opens intervention senza technician_id; supervisor assegna tecnico via /resume; SOP step 0 procede"
    input:
      endpoint: POST /v1/agents/maintenance-coach/start
      body:
        intervention_id: "INTV-AUTO-01"
        asset_id: "LOOM-02"
        sop_id: "SOP-LOOM-002"
        # technician_id intentionally OMITTED — simula path auto-open da RCASpecialist recommendation
        user_roles: ["supervisor"]
      multi_turn:
        - { endpoint: POST /v1/agents/maintenance-coach/resume, body: { intervention_id: "INTV-AUTO-01", technician_id: "TECH-099", supervisor_id: "SUP-01" } }
        - { endpoint: POST /v1/agents/maintenance-coach/step, body: { intervention_id: "INTV-AUTO-01", technician_input: "Iniziato controllo." } }
    seed:
      qdrant_chunks: [ ... SOP-LOOM-002 step chunks ... ]
    mock_llm_fixture: tests/fixtures/llm_responses/maintenance-coach/auto_open.jsonl
    expected:
      first_response_state: "awaiting_technician_assignment"
      first_audit_row: { decision: "hitl_supervisor", action_type: "COACH_STEP", escalation_trigger: "technician_assignment_required" }
      after_resume_state: "in_progress"
      after_resume_technician_id: "TECH-099"
      after_step_completed_steps_count: 1
    ```
    Aggiunto come 4° scenario per la maintenance-coach (totale per Coach: happy + degraded + failure + auto_open = 4 invece di 3). Inventario totale fissture: 4 agenti × varia + 1 extra = 13 scenari YAML invece di 12 (PM/DA/RCA restano 3 each, Coach diventa 4); 7 mock LLM JSONL invece di 6 (Coach +1 per auto_open).

    **downtime-analyzer/happy.yaml**:
    ```yaml
    name: downtime-analyzer-happy
    description: "OEE report with audit-source Quality + Pareto top-3 reason_codes"
    input:
      endpoint: POST /v1/agents/downtime-analyzer/report
      body:
        window_start: "2026-05-23T08:00:00Z"
        window_end: "2026-05-23T09:00:00Z"
        by_asset: true
        top_n_pareto: 5
    seed:
      pg_downtime_events:  # 10 events over the hour for LOOM-01
        - { event_id: "evt-001", asset_id: "LOOM-01", reason_code: "WEAVING-BE-001", duration_min: 5, severity: "minor", source: "simulator", timestamp: "2026-05-23T08:05:00Z" }
        # ... 9 more with varied reason_codes from failure_modes.yaml taxonomy (WEAVING-BE-001 × 3, WEAVING-MP-002 × 4, WEAVING-SF-003 × 3)
      pg_audit_rows:  # 1 QUALITY_VERDICT row for the same window
        - { action_id: "qv-001", agent_id: "quality-inspector", action_type: "QUALITY_VERDICT", cluster: "ops", decision: "auto", ts: "2026-05-23T08:30:00Z", evidence_panel: { asset_id: "LOOM-01", tool_calls: [{ result: { good_parts: 950, total_parts: 1000 } }] } }
      sft_assets_overrides:
        - { asset_id: "LOOM-01", asset_family: "loom" }
    expected:
      status_code: 200
      decision: "auto"
      action_type: "OEE_REPORT"
      audit_rows_min: 1  # OEE_REPORT only (DOWNTIME_VERDICT rows excluded — only consumer writes them, this seed bypasses consumer)
      oee_bounds: [0.7, 1.0]
      quality_source: "audit"
      pareto_top: ["WEAVING-MP-002", "WEAVING-BE-001", "WEAVING-SF-003"]
    ```

    **downtime-analyzer/degraded.yaml**: same downtime seed, NO pg_audit_rows for QUALITY_VERDICT, but `production_state_overrides.LOOM-01: { good_meters: 940, target_meters: 1000 }`. `expected.quality_source: "simfallback"`.

    **downtime-analyzer/failure.yaml**: `body.window_end < body.window_start` (inverted) → 422 from api-gateway. `expected.status_code: 422, audit_rows_min: 0`.

    **Cross-cutting** (all 12 scenarios): seeds are deterministic; UUIDs are fixed strings (not generated); timestamps are explicit ISO; mock LLM JSONL fixtures (when present) replay in order via `MockReplayChatModel`.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; python -c "import yaml, pathlib; data = [yaml.safe_load(p.read_text()) for p in sorted(pathlib.Path('tests/fixtures/mnt_scenarios').rglob('*.yaml'))]; assert len(data) == 13 and all('expected' in d and 'input' in d and 'seed' in d for d in data), f'malformed: {[i for i,d in enumerate(data) if not all(k in d for k in [\"input\",\"seed\",\"expected\"])]}'; print('13 maintenance scenarios validated')"</automated>
  </verify>
  <done>12 scenario YAML files deterministic + schema-consistent + un-stubbed.</done>
</task>

<task type="auto">
  <name>Task 2: Author 7 mock LLM JSONL fixtures (rca-specialist × 3 + maintenance-coach × 4 incl. auto_open) replacing Wave 0 stubs</name>
  <files>
    tests/fixtures/llm_responses/rca-specialist/happy.jsonl,
    tests/fixtures/llm_responses/rca-specialist/degraded.jsonl,
    tests/fixtures/llm_responses/rca-specialist/failure.jsonl,
    tests/fixtures/llm_responses/maintenance-coach/happy.jsonl,
    tests/fixtures/llm_responses/maintenance-coach/degraded.jsonl,
    tests/fixtures/llm_responses/maintenance-coach/failure.jsonl,
    tests/fixtures/llm_responses/maintenance-coach/auto_open.jsonl
  </files>
  <read_first>
    tests/fixtures/llm_responses/operator-assistant/happy.jsonl (Phase 6 06-13 reference — multi-call JSONL pattern),
    tests/fixtures/llm_responses/quality-inspector/degraded.jsonl (invalid JSON fallback pattern for retry-loop testing),
    packages/sft-agents/src/sft_agents/llm/mock.py (MockReplayChatModel hash + ordered-fallback lookup behavior),
    .planning/phases/07-agents-maintenance-reliability/07-07-PLAN.md (RCASpecialist retry contract for degraded/failure JSONL design),
    .planning/phases/07-agents-maintenance-reliability/07-08-PLAN.md (MaintenanceCoach multi-turn checkpoint replay for Coach JSONLs)
  </read_first>
  <action>
    Delete Wave 0 stub JSONLs. Author 6 full fixtures (per 07-VALIDATION.md L91-95: only RCA + Coach need mock LLM; PM is deterministic ML; DA is deterministic SQL).

    Use ordered-fallback strategy uniformly (empty `prompt_hash` field): MockReplayChatModel logs `mock_llm_hash_miss_fallback` + advances index. Mirror 06-13 Task 2 convention for tractability.

    **rca-specialist/happy.jsonl** (3-4 LLM call rounds: tool calls to rag_search/traverse_graph + final RCAChain JSON):
    ```jsonl
    {"prompt_hash":"","response":{"content":"","tool_calls":[{"id":"call_1","name":"rag_search","args":{"query":"rottura filo ordito broken end LOOM-01","user_roles":["technician","supervisor"],"category":"sop","k":5}}],"usage_metadata":{"input_tokens":400,"output_tokens":35,"total_tokens":435}}}
    {"prompt_hash":"","response":{"content":"","tool_calls":[{"id":"call_2","name":"traverse_graph","args":{"start_node":"LOOM-01","edge":"HAS_FAILURE_MODE","depth":2}}],"usage_metadata":{"input_tokens":480,"output_tokens":30,"total_tokens":510}}}
    {"prompt_hash":"","response":{"content":"{\"problem_statement\":\"Loom L01 ha avuto un downtime di 30 minuti...\",\"why_1\":{\"question\":\"Perché il filo si è rotto?\",\"answer\":\"Tensione filo eccessiva\",\"citations\":[{\"source_uri\":\"doc://SOP-LOOM-001#step1\",\"snippet\":\"Ispezione visiva...\",\"score\":0.95}],\"confidence\":0.9},\"why_2\":{\"question\":\"Perché la tensione era eccessiva?\",\"answer\":\"Calibrazione errata del regolatore\",\"citations\":[{\"source_uri\":\"doc://MAN-LOOM-OEM#warp\",\"snippet\":\"Regolatore tensione\",\"score\":0.88}],\"confidence\":0.85},\"why_3\":{\"question\":\"Perché la calibrazione era errata?\",\"answer\":\"Manca verifica preventiva\",\"citations\":[{\"source_uri\":\"doc://SOP-LOOM-001#step2\",\"snippet\":\"Verifica calibrazione\",\"score\":0.82}],\"confidence\":0.8},\"why_4\":{\"question\":\"Perché manca verifica preventiva?\",\"answer\":\"Procedura non documentata operatore\",\"citations\":[{\"source_uri\":\"doc://MAN-LOOM-OEM#warp\",\"snippet\":\"Procedura\",\"score\":0.75}],\"confidence\":0.75},\"why_5\":{\"question\":\"Perché la procedura non è documentata?\",\"answer\":\"Lacuna training operatore\",\"citations\":[{\"source_uri\":\"doc://SOP-LOOM-001#step1\",\"snippet\":\"Training\",\"score\":0.7}],\"confidence\":0.7},\"root_cause\":\"Lacuna procedurale e formativa\",\"corrective_action_recommendation\":\"Aggiornare SOP-LOOM-001 con verifica preventiva tensione e formare operatori\"}","tool_calls":[],"usage_metadata":{"input_tokens":700,"output_tokens":350,"total_tokens":1050}}}
    ```

    **rca-specialist/degraded.jsonl** (1st call: invalid JSON; 2nd: orphan citation; 3rd: valid):
    Round 1 returns a chain with `source_uri="doc://orphan"` (not in seeded pg_documents). RCAChainValidator raises OrphanCitationError. Round 2 retry corrective system message → LLM returns valid chain (use happy.jsonl Round 3 JSON as the success payload). Total 2 LLM calls minimum for retry success.

    **rca-specialist/failure.jsonl** (3 consecutive failing rounds — retry budget exhausted):
    Round 1: invalid JSON (e.g. `"{ this isn't valid"`). Round 2: chain with 3 orphan citations. Round 3: chain with only 4 WhyStep (missing why_5). After Round 3 retries exhausted → escalate with best_attempt + validation_exhausted=true.

    **maintenance-coach/happy.jsonl** (5 LLM rounds, one per SOP step):
    Each round: LLM returns guidance text for the current step (1 paragraph IT) + no tool calls. Mock the rag_search responses via Qdrant container seed (no LLM-side tool call needed if rag_search is wired pre-LLM).
    ```jsonl
    {"prompt_hash":"","response":{"content":"Passo 1: Ispeziona visivamente il filo di ordito su tutta la larghezza del telaio. Identifica il punto di rottura. Conferma quando hai completato.","tool_calls":[],"usage_metadata":{"input_tokens":350,"output_tokens":40,"total_tokens":390}}}
    {"prompt_hash":"","response":{"content":"Passo 2: Rimuovi il filo rotto dalla sezione interessata. Mantieni la tensione sui fili adiacenti. Conferma.","tool_calls":[],"usage_metadata":{"input_tokens":380,"output_tokens":35,"total_tokens":415}}}
    {"prompt_hash":"","response":{"content":"Passo 3: ...","tool_calls":[],"usage_metadata":{"input_tokens":400,"output_tokens":40,"total_tokens":440}}}
    {"prompt_hash":"","response":{"content":"Passo 4: ...","tool_calls":[],"usage_metadata":{"input_tokens":420,"output_tokens":40,"total_tokens":460}}}
    {"prompt_hash":"","response":{"content":"Passo 5 (finale): Riavvia il telaio e verifica la corretta tessitura per 30 secondi. Intervento completato.","tool_calls":[],"usage_metadata":{"input_tokens":440,"output_tokens":50,"total_tokens":490}}}
    ```

    **maintenance-coach/degraded.jsonl** (step 3 LLM detects keyword "aiuto" → invokes request_help):
    Rounds 1, 2 are normal step guidance. Round 3: LLM detects "aiuto" in the technician_input and invokes `request_help` tool with reason + context + intervention_id + current_step. After supervisor resume (via api-gateway), rounds 4-5 continue normal step guidance.
    ```jsonl
    {"prompt_hash":"","response":{"content":"Passo 1: ...","tool_calls":[],"usage_metadata":{"input_tokens":350,"output_tokens":40,"total_tokens":390}}}
    {"prompt_hash":"","response":{"content":"Passo 2: ...","tool_calls":[],"usage_metadata":{"input_tokens":380,"output_tokens":35,"total_tokens":415}}}
    {"prompt_hash":"","response":{"content":"","tool_calls":[{"id":"call_help","name":"request_help","args":{"reason":"Technician requested help on step 3","context":"Technician message: 'aiuto, non capisco questo passaggio'","intervention_id":"INTV-DEG-001","current_step":3}}],"usage_metadata":{"input_tokens":400,"output_tokens":80,"total_tokens":480}}}
    {"prompt_hash":"","response":{"content":"Passo 3 (con supporto supervisore): ...","tool_calls":[],"usage_metadata":{"input_tokens":420,"output_tokens":50,"total_tokens":470}}}
    {"prompt_hash":"","response":{"content":"Passo 4: ...","tool_calls":[],"usage_metadata":{"input_tokens":430,"output_tokens":40,"total_tokens":470}}}
    {"prompt_hash":"","response":{"content":"Passo 5 (finale): Intervento completato.","tool_calls":[],"usage_metadata":{"input_tokens":440,"output_tokens":40,"total_tokens":480}}}
    ```

    **maintenance-coach/failure.jsonl** (LLM at step 4 references non-existent step 99):
    Rounds 1-3 normal. Round 4: LLM returns guidance "Vai al passo 99 e applica le procedure di sicurezza" — agent surfaces error (step 99 doesn't exist in SOP-LOOM-001). Subsequent rounds may or may not be present (depends on whether agent retries or hard-fails — match the 07-08 agent behavior).
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; python -c "import json, pathlib; data = [list(map(json.loads, [l for l in p.read_text().splitlines() if l.strip()])) for p in sorted(pathlib.Path('tests/fixtures/llm_responses').rglob('*.jsonl')) if 'rca-specialist' in str(p) or 'maintenance-coach' in str(p)]; assert len(data) == 7 and all(len(d) >= 1 for d in data), f'malformed counts: {[len(d) for d in data]}'; print(f'7 maintenance JSONL fixtures validated, entries: {[len(d) for d in data]}')"</automated>
  </verify>
  <done>6 JSONL fixtures present, deterministic, multi-round, aligned with scenario expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement tests/e2e/maintenance/conftest.py with full testcontainers wiring + mnt_scenario loader + mock_llm + seed_testcontainers + api_gateway_client</name>
  <files>
    tests/e2e/maintenance/conftest.py
  </files>
  <read_first>
    tests/e2e/ops/conftest.py (Phase 6 06-13 conftest — DIRECT mirror, adapt to maintenance + add PG migration 008+009 + seed fixtures for new tables),
    packages/sft-agents/tests/conftest.py (pg_container + audit seed helpers from Phase 4/6),
    packages/sft-knowledge/tests/conftest.py (qdrant + neo4j fixtures from Phase 5),
    infra/migrations/timescale/008_create_downtime_events.sql (07-05 migration — must apply via _run_baseline_migrations extension),
    infra/migrations/timescale/009_extend_audit_mnt.sql (07-01 migration — must apply)
  </read_first>
  <behavior>
    Replace Wave 0 stub conftest with full implementation. Mirror 06-13 Task 3 structure with maintenance-specific extensions:

    - Session-scoped fixtures (testcontainers — expensive, share across all 12 tests):
      - `pg_container`: TimescaleDB pg16 + apply migrations 001..009 (extend Phase 6 helper to include 008 + 009).
      - `nats_container`: NATS JetStream + bootstrap MAINTENANCE_STREAM (subjects: `maintenance.downtime.>`, `maintenance.predict.*`).
      - `qdrant_container` (Phase 5 reuse).
      - `neo4j_container` (Phase 5 reuse).

    - Function-scoped fixtures (per scenario):
      - `mnt_scenario(request)`: reads YAML based on `request.param` (e.g. `("predictive-maintenance", "happy")`) from `tests/fixtures/mnt_scenarios/<agent>/<scenario>.yaml`. Returns parsed dict.
      - `mock_llm_backend(monkeypatch, mnt_scenario)`: if `mnt_scenario.get("mock_llm_fixture")` set, monkeypatch env `LLM_BACKEND=mock` + `MOCK_LLM_FIXTURE=<path>`. Else no-op (PM + DA scenarios don't need mock).
      - `seed_testcontainers(mnt_scenario, pg_container, nats_container, qdrant_container, neo4j_container)`: walks `mnt_scenario["seed"]`:
        - `pg_audit_rows` → INSERT into audit.actions via asyncpg.
        - `pg_downtime_events` → INSERT into maintenance.downtime_events (07-05 table).
        - `pg_sensor_events` → INSERT into sensor_events hypertable (Phase 3 table) — for PM's QueryTimescaleTool window pull.
        - `pg_documents` → INSERT into documents table — for RCA citation source_uri lookup.
        - `nats_messages` → publish to NATS subjects (for PM trigger from AD, or DA consumer pre-population).
        - `qdrant_chunks` → upsert to Qdrant collections (Phase 5 reuse).
        - `neo4j_facts` → run cypher CREATE (Phase 5 reuse).
        - `sft_assets_overrides` → patch in-memory registry (Phase 3 reuse).
        - `production_state_overrides` → patch sim-textile production_state singleton (Phase 6 06-09).
      - `api_gateway_client(...)`: FastAPI TestClient with dependency_overrides redirecting all dependencies to testcontainer fixtures + monkeypatched mock LLM. Returns httpx.AsyncClient or TestClient (mirror 06-13 convention).

    - Helpers (module-level):
      - `_apply_phase7_migrations(dsn)`: applies 001..009 in order; reuses Phase 6 `_run_baseline_migrations` + appends 008 + 009.
      - `_bootstrap_maintenance_stream(nc)`: idempotent `js.add_stream(name='MAINTENANCE_STREAM', subjects=['maintenance.downtime.>', 'maintenance.predict.*'], ...)`.

    Verify: `pytest tests/e2e/maintenance/ --collect-only -q -m e2e` collects (no import errors).
  </behavior>
  <action>
    Open `tests/e2e/ops/conftest.py` and copy file-by-file, renaming `ops_scenario` → `mnt_scenario`, paths from `ops_scenarios/` → `mnt_scenarios/`, add the maintenance-specific seed handlers, extend migration helper to include 008 + 009.

    For the `mock_llm_backend` fixture, the Phase 6 06-13 pattern uses `monkeypatch.setenv("LLM_BACKEND", "mock") + setenv("MOCK_LLM_FIXTURE", path)`. Verify by reading the actual implementation.

    For `seed_testcontainers`, this is the most labor-intensive piece — each seed category needs a dedicated helper. Keep them inline (one file is fine for E2E conftest — Phase 6 06-13 also has ~200-line conftest).

    For `api_gateway_client`, use FastAPI's TestClient wrapped in `async with` if the maintenance endpoints are async (they are per 07-10). If 06-13 uses httpx.AsyncClient, follow that convention.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest tests/e2e/maintenance/ --collect-only -q -m e2e 2&gt;&amp;1 | tail -20 &amp;&amp; python -c "import importlib.util; spec = importlib.util.spec_from_file_location('mnt_conftest', 'tests/e2e/maintenance/conftest.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert hasattr(m, 'mnt_scenario') and hasattr(m, 'mock_llm_backend') and hasattr(m, 'seed_testcontainers') and hasattr(m, 'api_gateway_client'); print('OK')"</automated>
  </verify>
  <done>conftest.py collects without errors; all required fixture names exposed; testcontainers wiring inherits from Phase 5 + Phase 6 patterns with Phase 7 additions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement 4 E2E scenario test modules (3 scenarios each = 12 tests total) — happy + degraded + failure for each of 4 agents</name>
  <files>
    tests/e2e/maintenance/test_predictive_maintenance_scenarios.py,
    tests/e2e/maintenance/test_rca_specialist_scenarios.py,
    tests/e2e/maintenance/test_maintenance_coach_scenarios.py,
    tests/e2e/maintenance/test_downtime_analyzer_scenarios.py
  </files>
  <read_first>
    tests/e2e/maintenance/conftest.py (Task 3 fixtures),
    tests/fixtures/mnt_scenarios/ + tests/fixtures/llm_responses/ (Tasks 1-2 spec content),
    tests/e2e/ops/test_anomaly_detector_scenarios.py (Phase 6 06-13 deterministic-agent E2E reference),
    tests/e2e/ops/test_operator_assistant_scenarios.py (Phase 6 LLM ReAct E2E reference for RCA),
    tests/e2e/ops/test_quality_inspector_scenarios.py (Phase 6 NATS consumer + multi-message E2E reference for DA),
    apps/agents/maintenance/*/src/mnt_*/agent.py (agent contracts to assert against)
  </read_first>
  <behavior>
    Replace Wave 0 stub modules. Each module follows the 06-13 Task 4 pattern with Phase 7 specifics:

    **test_predictive_maintenance_scenarios.py**:
    ```python
    pytestmark = [pytest.mark.e2e, pytest.mark.integration, pytest.mark.asyncio]

    @pytest.mark.timeout(120)
    @pytest.mark.parametrize("mnt_scenario", [
        ("predictive-maintenance", "happy"),
        ("predictive-maintenance", "degraded"),
        ("predictive-maintenance", "failure"),
    ], indirect=True)
    async def test_predictive_maintenance_scenario(mnt_scenario, mock_llm_backend, seed_testcontainers, api_gateway_client, pg_container):
        spec = mnt_scenario
        response = await api_gateway_client.post(spec["input"]["endpoint"], json=spec["input"]["body"])
        assert response.status_code == spec["expected"]["status_code"]
        if spec["expected"]["status_code"] == 200:
            body = response.json()
            if spec["expected"].get("rul_cycles_band"):
                low, high = spec["expected"]["rul_cycles_band"]
                rul = body["rul_estimate"]["rul_cycles"]
                assert low <= rul <= high
            # Audit assertions
            async with pg_container.connect() as conn:
                audit_count = await conn.fetchval("SELECT COUNT(*) FROM audit.actions WHERE action_type = $1", spec["expected"]["action_type"])
                assert audit_count >= 1 or spec["expected"]["audit_rows_min"] == 0
                if spec["expected"].get("triggered_by_action_id_present"):
                    chain_row = await conn.fetchrow("SELECT 1 FROM audit.actions WHERE action_type='RUL_ESTIMATE' AND evidence_panel->'tool_calls'->0->'args'->>'triggered_by_action_id' = $1", spec["input"]["body"]["triggered_by_action_id"])
                    assert chain_row is not None, "MNT-06 audit chain link missing"
        if spec["expected"].get("decision") == "hitl_supervisor":
            audit_row = await conn.fetchrow("SELECT decision, evidence_panel FROM audit.actions WHERE action_type='RUL_ESTIMATE' ORDER BY ts DESC LIMIT 1")
            assert audit_row["decision"] == "hitl_supervisor"
    ```

    **test_rca_specialist_scenarios.py**: similar parametrize structure. Assertions specific:
    - status_code == 202 (RCA is async HITL)
    - body["hitl_status"] == "supervisor_pending"
    - body["validation_exhausted"] matches spec
    - audit RCA_CHAIN row decision == "hitl_supervisor"
    - for degraded: assert retry count in evidence_panel (e.g. tool_calls[*].name includes 'rag_search' repeated due to retry)
    - for failure: best_attempt present in audit evidence_panel

    **test_maintenance_coach_scenarios.py**: multi-turn assertion (most complex):
    - Iterate start + N step calls per scenario's `multi_turn` list
    - After all steps: query langgraph_checkpoints for thread_id=`coach-<intervention_id>` → assert state has expected current_step + completed_steps count
    - Query audit.actions for COACH_STEP rows per intervention_id → count matches expected
    - For degraded: assert one COACH_STEP row has `escalation_trigger='technician_request'` marker
    - For happy: assert mttr_minutes_min satisfied via `compute_mttr_minutes(final_state)`
    - For failure: assert partial state preserved + checkpoint accessible

    **test_downtime_analyzer_scenarios.py**: deterministic SQL, no mock LLM:
    - status_code per spec
    - body matches OEEReport schema (parse via `OEEReport.model_validate(body)` — no ValidationError)
    - oee_bounds satisfied
    - quality_source captured via structlog event assertion (use structlog testing helper to capture logs and assert event name)
    - pareto top-N order matches spec
    - audit OEE_REPORT row present

    All 12 tests pass within ~480s total runtime (mirror 06-13 budget).
  </behavior>
  <action>
    Replace Wave 0 stub modules. Use `@pytest.mark.timeout(120)` per test (4-min total per test allows the multi-turn Coach tests breathing room).

    For `indirect=True` parametrize, the `mnt_scenario` fixture in conftest must handle `request.param` tuple `(agent_slug, scenario_name)` and load the right YAML.

    For Coach multi-turn: iterate via `for turn in spec["input"].get("multi_turn", []):` → `await api_gateway_client.post(turn["endpoint"], json=turn["body"])`; collect responses; assert final state. The `interview_id` propagates across turns.

    For structlog capture in DA quality_source test, use `structlog.testing.capture_logs()` context manager wrapped around the api call.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest tests/e2e/maintenance/ -m "e2e and not real-llm" -x --timeout=600 2&gt;&amp;1 | tail -30</automated>
  </verify>
  <done>All 12 E2E tests pass (4 agents × 3 scenarios); audit chain (MNT-06) verifiable; total runtime ~480s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML scenarios → loader | Repo-committed, yaml.safe_load only |
| JSONL fixtures → MockReplayChatModel | Repo-committed, json.loads strict |
| testcontainers → real services (PG/NATS/Qdrant/Neo4j) | Isolated per session; torn down after |
| api_gateway_client TestClient → router → supervisor → maintenance subgraph | Internal; full stack exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V7-e2e-fixture-tamper | Tampering | scenarios + JSONL files | mitigate | PR-reviewed; gitleaks pre-commit; no secrets in any fixture. |
| T-V7-e2e-test-determinism | Tampering | mock LLM + random + asyncio races | mitigate | All scenarios use mock LLM (LLM_BACKEND=mock); seed=42 universally; deterministic ML model (07-03 RANDOM_STATE=42); explicit ack waits on NATS. |
| T-V7-e2e-test-leak | Information Disclosure | testcontainers data | accept | Containers ephemeral; no persistent volumes. |
| T-V7-e2e-flaky-timing | Repudiation | NATS publish + consumer ack timing | mitigate | Coach multi-turn uses synchronous request-response (TestClient); NATS consumer tests wait for explicit ack signal (e.g. poll audit row count with bounded retry). |
| T-V7-e2e-cross-cluster-drift | Tampering | QUALITY_VERDICT JSONB path drift breaking DA happy scenario | mitigate | Phase 6 QualityInspector contract is asserted in 07-09 Task 1 + verified at E2E; if E2E fails because path drifted, that's the canary. |
| T-V7-e2e-SC | Tampering | testcontainers + pytest-asyncio already locked Phase 4 | n/a | No new pip installs. |
</threat_model>

<deviation_rules>
- **Rule 1:** 12-scenario coverage (4 agents × 3) is LOCKED per ROADMAP Phase 7 success criterion #5 + 07-VALIDATION.md L91-95. Do NOT reduce to fewer scenarios or skip the failure path.
- **Rule 1b:** Mock LLM strategy is LOCKED per CONTEXT D-X-01 / 06-03 MockReplayChatModel reuse. NO real LLM in CI (real-LLM smoke is opt-in via `pytest -m real-llm`, Phase 11 enhancement).
- **Rule 3:** Phase 6 06-13 ops E2E tests MUST stay green. Phase 5 fixtures (qdrant/neo4j conftest) MUST NOT be modified.
- **Rule 4:** Capture in SUMMARY: 12-scenario coverage table + observed runtime; any flaky tests + retry strategy; mock LLM ordered-fallback warnings observed; audit chain SQL JOIN assertion result for MNT-06; quality_source structlog observations per DA scenario.
</deviation_rules>

<verification>
- `pytest tests/e2e/maintenance/ -m "e2e and not real-llm" --timeout=600` green within ~480s.
- All 12 mnt_scenarios YAML valid (yaml.safe_load).
- All 6 maintenance JSONL valid (line-per-line json.loads).
- Each agent's 3 scenarios pass.
- Phase 6 E2E ops tests stay green (no regression: `pytest tests/e2e/ops/ -m "e2e and not real-llm" --timeout=600` still green).
- `nyquist_compliant: true` can be set on 07-VALIDATION.md frontmatter after this plan.

## Wave 0 dependency
- This plan un-stubs Wave 0 placeholders: 4 E2E test modules + conftest.py + 12 scenario YAMLs + 6 maintenance JSONL fixtures.
</verification>

<success_criteria>
- Phase 7 success criterion #5 mechanically verified: 3 scenarios × 4 agents = 12 e2e tests green.
- MNT-06 audit chain (triggered_by_action_id link AD→PM) verifiable via SQL JOIN in PM happy test.
- All scenarios deterministic (mock LLM + seeded fixtures + RANDOM_STATE=42).
- Real-LLM regenerate script not in scope (Phase 11; if needed, mirror 06-13 `scripts/regenerate-llm-fixtures.py` adapted for maintenance — note in SUMMARY).
- Wave 0 placeholders all un-stubbed.
- **Phase 7 closure**: with this plan green, all 6 phase requirements (MNT-01..06) are end-to-end verified.
</success_criteria>

<commit_strategy>
- Commit 1: `test(07-12): 12 mnt_scenarios YAML (4 agents × happy/degraded/failure)` (12 YAML files)
- Commit 2: `test(07-12): 6 mock LLM JSONL fixtures (rca + coach × 3 scenarios)` (6 JSONL files)
- Commit 3: `test(07-12): conftest.py — testcontainers PG+NATS+Qdrant+Neo4j + seed helpers + api_gateway_client` (conftest.py)
- Commit 4: `test(07-12): 4 E2E scenario modules — 12 tests covering PM/RCA/Coach/DA happy/degraded/failure (MNT-06 audit chain assertion)` (4 test modules)
</commit_strategy>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-12-SUMMARY.md` documenting: 12-scenario coverage table (pass/fail + runtime per test); total E2E suite runtime; mock LLM ordered-fallback warning counts; MNT-06 audit chain SQL JOIN result; quality_source distribution across DA scenarios; any flaky tests + retry strategy; nyquist_compliant toggle.
</output>
</content>
