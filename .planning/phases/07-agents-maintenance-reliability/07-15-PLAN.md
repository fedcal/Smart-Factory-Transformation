---
phase: 07-agents-maintenance-reliability
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/agent.py
  - apps/agents/maintenance/predictive-maintenance/tests/test_audit_integrity.py
autonomous: true
gap_closure: true
requirements: [MNT-01, MNT-06]

must_haves:
  truths:
    - "HITL_SUPERVISOR audit rows written by PredictiveMaintenance carry approval_id=None (no fabricated UUID) until a real supervisor approval is recorded"
    - "Missing asset_id in state raises an explicit ValueError, not a bare KeyError that leaks the field name in 500 responses"
  artifacts:
    - path: apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/agent.py
      provides: "approval_id=None in HITL audit path; state.get('asset_id') with explicit ValueError"
    - path: apps/agents/maintenance/predictive-maintenance/tests/test_audit_integrity.py
      provides: "Regression test for CR-03 fabricated approval_id + CR-04 KeyError leak"
  key_links:
    - from: "PredictiveMaintenance._write_audit (HITL path)"
      to: "audit.actions.approval_id"
      via: "approval_id stays None until real hitl.approvals row exists"
      pattern: "approval_id\\s*=\\s*None"
---

<objective>
Close the PredictiveMaintenance (MNT-01) audit-integrity and error-handling defects.

- CR-03 (BLOCKER): when `health_index < 0.3` triggers the HITL path, `_write_audit` fabricates `approval_id = _uuid4()` (lines 329-331) labeled "placeholder UUID ... during testing" — but it runs in production. Downstream JOINs on `audit.actions.approval_id` → `hitl.approvals` find no match, so forensic tools falsely conclude escalations were approved.
- CR-04: `asset_id = state["asset_id"]` (line 171) raises a bare KeyError that propagates to the gateway and leaks the internal field name in 500 response bodies.

Purpose: Restore SC-1 — "PredictiveMaintenance estimates Remaining Useful Life ... feature set includes ambient temperature and humidity sensors" — with audit-chain integrity and safe error handling intact.
Output: Fixed agent.py + a regression test that fails against current code and passes after the fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agents-maintenance-reliability/07-VERIFICATION.md
@.planning/phases/07-agents-maintenance-reliability/07-REVIEW.md

<interfaces>
<!-- CR-03 fix (07-REVIEW.md lines 116-132): approval_id MUST remain None for the
       initial HITL_SUPERVISOR audit row; the real approval_id is assigned later by
       the HITL system after a supervisor approves. CR-04 fix (lines 136-155): use
       state.get('asset_id') and raise ValueError with a non-leaky message. -->

PredictiveMaintenance.__call__(state) reads asset_id = state["asset_id"] (line 171).
PredictiveMaintenance._write_audit(*, estimate, decision, feature_summary):
  current HITL branch (lines 322-330):
    motivation = f"Supervisor approved escalation for asset {estimate.asset_id}"
    approval_id = _uuid4()   # <-- fabricated, CR-03
  AuditRecord(...) is constructed with that approval_id.

Note: confirm whether AuditRecord's validator permits decision=HITL_SUPERVISOR with
  approval_id=None (it does for other maintenance agents — coach and DA write
  HITL/AUTO rows with approval_id=None). If the validator requires non-null
  approval_id only for a *finalized approval*, keep motivation describing the
  pending state ("Supervisor approval required ...") rather than "approved".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing regression test for fabricated approval_id (CR-03) and KeyError leak (CR-04)</name>
  <files>apps/agents/maintenance/predictive-maintenance/tests/test_audit_integrity.py</files>
  <read_first>
    - apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/agent.py (CR-03 lines 320-346; CR-04 line 171; __call__ flow lines 152-261)
    - apps/agents/maintenance/predictive-maintenance/tests/conftest.py (fixtures: mock pool, audit_writer, asset_registry, feature_map_fn, query_tool, escalate_tool)
    - apps/agents/maintenance/predictive-maintenance/tests/test_evidence_panel.py (existing audit-record assertion patterns)
    - .planning/phases/07-agents-maintenance-reliability/07-REVIEW.md (CR-03 fix lines 116-132; CR-04 fix lines 136-155)
  </read_first>
  <action>
    Create test_audit_integrity.py with regression tests that FAIL against current code.

    Test A — `test_hitl_audit_row_has_null_approval_id`: construct PredictiveMaintenance with a feature_map_fn / model stub that forces `health_index < 0.3` (the HITL path), a mock escalate_tool, and an audit_writer mock recording the AuditRecord. Invoke `__call__` with valid state. Capture the written AuditRecord and assert `record.decision is Decision.HITL_SUPERVISOR` AND `record.approval_id is None`. Against current code approval_id is a fabricated uuid4 → test fails now.

    Test B — `test_missing_asset_id_raises_valueerror_not_keyerror`: invoke `__call__` with a state dict that omits `asset_id`. Assert it raises `ValueError` (not `KeyError`) and that `str(exc)` does NOT contain the bare repr `'asset_id'` of a KeyError (assert the message is the explicit human-readable one, e.g. mentions "requires 'asset_id'" as a controlled message rather than a leaked key). Against current code a bare KeyError is raised → test fails now.

    Use the existing conftest fixtures for pool/audit_writer/asset_registry; force the low-health branch via the injected feature_map_fn + a model stub returning a low health_index from predict_rul (or monkeypatch predict_rul).
  </action>
  <verify>
    <automated>cd apps/agents/maintenance/predictive-maintenance && uv run pytest tests/test_audit_integrity.py -m "not integration" -x</automated>
  </verify>
  <acceptance_criteria>
    - test_audit_integrity.py exists with Test A (null approval_id) and Test B (ValueError on missing asset_id).
    - Before Task 2 both tests FAIL with assertion/exception-type errors (not collection errors).
  </acceptance_criteria>
  <done>Regression tests authored and demonstrably red against current agent.py.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-03 fabricated approval_id and CR-04 KeyError in agent.py</name>
  <files>apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/agent.py</files>
  <read_first>
    - apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/agent.py (full file — modifying line 171 and _write_audit lines 320-346)
    - .planning/phases/07-agents-maintenance-reliability/07-REVIEW.md (CR-03 fix lines 116-132; CR-04 fix lines 136-155)
    - packages/sft-agents (AuditRecord validator — confirm approval_id=None is accepted for HITL_SUPERVISOR as used by coach/DA agents)
  </read_first>
  <action>
    Fix CR-03: In `_write_audit` (lines 322-331) remove the fabricated `approval_id = _uuid4()` and the `from uuid import uuid4 as _uuid4` import inside that branch. Set `approval_id = None` for the HITL_SUPERVISOR branch and change the motivation text from "Supervisor approved escalation ..." to a pending-state message such as "Supervisor approval required for asset {estimate.asset_id}" (per 07-REVIEW.md lines 125-131) — the row represents a pending escalation, not an approval. The real approval_id is populated later by the HITL system when a supervisor approves. Leave the AUTO branch unchanged.

    Fix CR-04: At line 171 replace `asset_id = state["asset_id"]` with `asset_id = state.get("asset_id")` followed by an explicit guard: `if not asset_id: raise ValueError("PredictiveMaintenance requires 'asset_id' in state")` (per 07-REVIEW.md lines 151-155). The ValueError message is a controlled string; it does not leak via a KeyError repr.

    Do not alter the ML inference path, feature mapping, or the triggered_by_action_id audit-chain wiring.
  </action>
  <verify>
    <automated>cd apps/agents/maintenance/predictive-maintenance && uv run pytest tests/test_audit_integrity.py -m "not integration" tests/test_evidence_panel.py tests/test_inference.py tests/test_consumer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Test A and Test B from Task 1 now PASS.
    - In the HITL audit path of agent.py: `grep -n "uuid4()" apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/agent.py` shows no occurrence inside _write_audit's HITL branch (the `_uuid4()` approval_id is gone). The `id=uuid4()` / `action_id=uuid4()` record identifiers may remain.
    - `grep -c "state.get(\"asset_id\")" apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/agent.py` returns >= 1 and `state[\"asset_id\"]` no longer appears.
    - Existing tests (test_evidence_panel, test_inference, test_consumer) still pass.
  </acceptance_criteria>
  <done>SC-1 audit integrity restored: HITL rows carry null approval_id; missing asset_id fails safely.</done>
</task>

</tasks>

<verification>
- Unit subset passes: `cd apps/agents/maintenance/predictive-maintenance && uv run pytest tests/test_audit_integrity.py -m "not integration" -x`
- No fabricated approval_id remains in the HITL audit path; missing asset_id raises a controlled ValueError.
</verification>

<success_criteria>
SC-1 (PredictiveMaintenance) audit-chain integrity restored: no fabricated approval_id, no field-name leak on missing asset_id.
</success_criteria>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-15-SUMMARY.md` when done.
</output>
