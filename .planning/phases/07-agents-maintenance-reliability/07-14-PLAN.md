---
phase: 07-agents-maintenance-reliability
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py
  - apps/agents/maintenance/rca-specialist/tests/test_interrupt_audit_lifecycle.py
autonomous: true
gap_closure: true
requirements: [MNT-02, MNT-06]

must_haves:
  truths:
    - "RCASpecialist writes exactly one audit row per logical invocation, and the write occurs only on the resumed (post-interrupt) execution — never skipped on first run, never double-written"
    - "tool_calls_log is populated from the ReAct result messages so the evidence panel records which tools the LLM called"
  artifacts:
    - path: apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py
      provides: "Direct interrupt() in node with audit write after resume return; populated tool_calls_log"
    - path: apps/agents/maintenance/rca-specialist/tests/test_interrupt_audit_lifecycle.py
      provides: "Regression test for CR-02 interrupt-before-audit + WR-05 empty evidence panel"
  key_links:
    - from: "RCASpecialist.__call__ interrupt()"
      to: "_write_audit"
      via: "audit write placed after interrupt() return value (executes on resume only)"
      pattern: "interrupt\\(|_write_audit"
---

<objective>
Close the RCASpecialist (MNT-02) audit-lifecycle and evidence defects.

- CR-02 (BLOCKER): `_escalate_to_supervisor` invokes the escalate tool which calls `langgraph.types.interrupt()` internally, unwinding the stack BEFORE `await self._write_audit(...)`. Result: no audit row on the first execution; one row on the resumed run; a double-write if re-executed. Violates the one-row-per-invocation audit contract the docstring (Pitfall §3) claims to honor.
- WR-05: `tool_calls_log` is initialized `[]` (line 410) and never populated, so `EvidencePanel.tool_calls` is always empty — the forensic value of the panel is absent.

Purpose: Restore SC-2 — "RCASpecialist generates a 5-Why chain ... cites knowledge base sources with provenance, and routes the corrective action recommendation to supervisor-level HITL" — with a correct, single, post-interrupt audit row carrying real tool-call evidence.
Output: Fixed agent.py + a regression test that fails against current code and passes after the fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agents-maintenance-reliability/07-VERIFICATION.md
@.planning/phases/07-agents-maintenance-reliability/07-REVIEW.md

<interfaces>
<!-- CR-02 fix pattern (07-REVIEW.md lines 88-112): replace the explicit
       _escalate_to_supervisor call (which raises GraphInterrupt on first run)
       with a DIRECT interrupt() call in the node. The line AFTER interrupt()
       only executes on RESUME, so the audit write placed there fires exactly
       once, on the resumed execution. -->

RCASpecialist.__call__(state) currently (agent.py lines 510-531):
  await self._escalate_to_supervisor(reason=..., suggested_action=..., evidence_summary=...)
  duration_ms = ...
  await self._write_audit(chain_id=..., tool_calls_log=[], ...)

RCASpecialist._invoke_react_loop(messages, *, user_roles) -> str
  internally: result = await safe_invoke(runnable, {"messages": messages}, ...)
              final_messages = result.get("messages", [])  (line 214)
  Currently returns only last_msg.content; tool-call records in final_messages
  are discarded.

WR-05 fix pattern (07-REVIEW.md lines 270-285): iterate final_messages, read
  getattr(msg, "tool_calls", None), append {name, args, result, duration_ms, ts}
  records into tool_calls_log.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing regression test for interrupt-before-audit (CR-02) and empty evidence panel (WR-05)</name>
  <files>apps/agents/maintenance/rca-specialist/tests/test_interrupt_audit_lifecycle.py</files>
  <read_first>
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py (CR-02 ordering lines 472-531; WR-05 tool_calls_log line 410; _invoke_react_loop lines 176-220)
    - apps/agents/maintenance/rca-specialist/tests/conftest.py (mock llm/tool/audit fixtures)
    - apps/agents/maintenance/rca-specialist/tests/test_evidence_panel.py (existing evidence-panel assertions to mirror)
    - .planning/phases/07-agents-maintenance-reliability/07-REVIEW.md (CR-02 fix lines 88-112; WR-05 fix lines 262-285)
  </read_first>
  <action>
    Create test_interrupt_audit_lifecycle.py with regression tests that FAIL against current code.

    Test A — `test_audit_written_once_on_resume_not_first_run`: construct RCASpecialist with mock collaborators. Make the escalate path use a real `langgraph.types.interrupt()` semantic: simulate first execution where interrupt() raises GraphInterrupt, and a resumed execution where interrupt() returns a supervisor decision value. Assert that on the FIRST execution `audit_writer.write` was NOT awaited (the node suspended at interrupt) and that after RESUME `audit_writer.write` was awaited exactly ONCE. Against current code the write either never fires on first run (escalate raises) or fires twice across executions — test fails now.

    Test B — `test_tool_calls_log_populated_from_react_messages`: stub `_invoke_react_loop` / the underlying ReAct result so `final_messages` contains an AIMessage carrying a `tool_calls` attribute (e.g. a rag_search call with args). Drive `__call__` through a successful validation path to the audit write, capture the AuditRecord passed to `audit_writer.write`, and assert `record.evidence_panel.tool_calls` is non-empty and contains a ToolCall with name "rag_search". Against current code tool_calls_log stays `[]` so tool_calls is empty — test fails now.

    Mark any test requiring a real LangGraph interrupt/resume runtime `@pytest.mark.integration`; keep Test A and B as unit tests using monkeypatched interrupt/ReAct stubs where feasible.
  </action>
  <verify>
    <automated>cd apps/agents/maintenance/rca-specialist && uv run pytest tests/test_interrupt_audit_lifecycle.py -m "not integration" -x</automated>
  </verify>
  <acceptance_criteria>
    - test_interrupt_audit_lifecycle.py exists with Test A (audit-once-on-resume) and Test B (tool_calls populated).
    - Before Task 2 both unit tests FAIL with assertion errors (not collection/import errors).
  </acceptance_criteria>
  <done>Regression tests authored and demonstrably red against current agent.py.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-02 interrupt ordering and WR-05 tool_calls_log in agent.py</name>
  <files>apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py</files>
  <read_first>
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py (full file — modifying _invoke_react_loop lines 176-220, __call__ retry loop + escalate/audit lines 406-531, _escalate_to_supervisor lines 222-239)
    - .planning/phases/07-agents-maintenance-reliability/07-REVIEW.md (CR-02 fix lines 88-112; WR-05 fix lines 262-285)
  </read_first>
  <action>
    Fix CR-02: In `__call__` replace the explicit `await self._escalate_to_supervisor(...)` call (lines 510-514) with a DIRECT `langgraph.types.interrupt(...)` call carrying the same escalate payload dict (reason, suggested_action, evidence_summary). Capture its return value as `supervisor_decision`. Because LangGraph re-runs the node from the top on resume and `interrupt()` returns only on the resumed execution, place the `await self._write_audit(...)` call AFTER the interrupt() return so it executes exactly once (on resume). Import `interrupt` from `langgraph.types` at module top (guard with the existing ImportError-tolerant pattern used in `_invoke_react_loop` so test envs without langgraph still import). Remove or repurpose `_escalate_to_supervisor` if it becomes unused; if the escalate tool still needs to be invoked for side-effects, do so without letting it raise the interrupt (the node-level interrupt() is now authoritative). Preserve the Decision.HITL_SUPERVISOR audit semantics.

    Fix WR-05: In `_invoke_react_loop`, after obtaining `final_messages = result.get("messages", [])` (line 214), accumulate tool-call records into a structure the caller can read — either return `(content, tool_call_records)` and have `__call__` extend `tool_calls_log`, or accept a `tool_calls_log` list argument and append in place. For each msg in final_messages, read `getattr(msg, "tool_calls", None)`; for each tc append `{"name": tc.get("name","unknown"), "args": tc.get("args",{}), "result": None, "duration_ms": 0, "ts": datetime.now(timezone.utc)}` (per 07-REVIEW.md lines 272-285). Ensure the populated `tool_calls_log` flows into `_write_audit` so `_build_evidence_panel` produces non-empty `tool_calls`.

    Keep immutable patterns (build new lists; do not mutate state).
  </action>
  <verify>
    <automated>cd apps/agents/maintenance/rca-specialist && uv run pytest tests/test_interrupt_audit_lifecycle.py -m "not integration" tests/test_evidence_panel.py tests/test_models.py tests/test_validators.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Test A and Test B from Task 1 now PASS.
    - `grep -c "self._escalate_to_supervisor(" apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py` returns 0 in the __call__ escalation path (replaced by direct interrupt()).
    - `grep -c "interrupt(" apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py` returns >= 1.
    - `tool_calls_log.append` appears in agent.py (grep returns >= 1), and `_invoke_react_loop` no longer discards tool-call records.
    - Existing tests (test_evidence_panel, test_models, test_validators) still pass.
  </acceptance_criteria>
  <done>SC-2 restored: single post-interrupt audit row with populated tool-call evidence.</done>
</task>

</tasks>

<verification>
- Unit subset passes: `cd apps/agents/maintenance/rca-specialist && uv run pytest tests/test_interrupt_audit_lifecycle.py -m "not integration" -x`
- No explicit pre-interrupt escalate-then-audit ordering remains; interrupt() is called directly in the node with the audit write after the resume return.
</verification>

<success_criteria>
SC-2 (RCASpecialist) restored: one audit row per invocation written only on resume, with a non-empty evidence panel tool_calls list.
</success_criteria>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-14-SUMMARY.md` when done.
</output>
