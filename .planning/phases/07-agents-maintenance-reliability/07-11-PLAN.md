---
phase: 07-agents-maintenance-reliability
plan: 11
plan_id: 07-11
type: execute
wave: 4
depends_on: ["07-06", "07-07", "07-08", "07-09"]
files_modified:
  - docs/docs/agents/maintenance/predictive-maintenance.md
  - docs/docs/agents/maintenance/rca-specialist.md
  - docs/docs/agents/maintenance/maintenance-coach.md
  - docs/docs/agents/maintenance/downtime-analyzer.md
  - docs/docs/agents/maintenance/event-taxonomy.md
  - docs/docs/en/agents/maintenance/predictive-maintenance.md
  - docs/docs/en/agents/maintenance/rca-specialist.md
  - docs/docs/en/agents/maintenance/maintenance-coach.md
  - docs/docs/en/agents/maintenance/downtime-analyzer.md
  - docs/docs/en/agents/maintenance/event-taxonomy.md
  - docs/mkdocs.yml
  - apps/agents/maintenance/predictive-maintenance/tests/test_evidence_panel.py
  - apps/agents/maintenance/rca-specialist/tests/test_evidence_panel.py
  - apps/agents/maintenance/maintenance-coach/tests/test_evidence_panel.py
  - apps/agents/maintenance/downtime-analyzer/tests/test_evidence_panel.py
autonomous: true
requirements: [MNT-05]
objective: "Ship MkDocs bilingual (IT + EN) documentation for the 4 maintenance cluster agents (8 pages) declaring per OPS-05/MNT-05: tools used, data sources, HITL tier table, KPIs impacted, audit footprint. Plus 2 bilingual pages for the maintenance event taxonomy (MNT-05 explicit deliverable). Extend mkdocs.yml nav with the maintenance section (mirror Phase 6 06-14 operations section pattern). Un-stub the 4 `test_evidence_panel.py` placeholders from Wave 0 by asserting the agent's response/audit payload carries the OPS-05 declaration fields (mirror 06-14 Task 2 pattern)."
must_haves:
  truths:
    - "8 maintenance agent docs pages exist (4 agents × IT + EN) following the 06-14 frontmatter convention `lang: it|en, agent: <slug>, requirements: [MNT-01..04], tags: [agents, maintenance, MNT-XX]`."
    - "2 event-taxonomy pages exist (IT + EN) explicitly listing each maintenance reason_code from failure_modes.yaml maintenance subkey (07-02) with description, severity, mttr_target_minutes, intervention_steps_sop_id, asset_families."
    - "Each agent doc has sections: Overview/Panoramica, Strumenti Utilizzati/Tools Used, Fonti Dati/Data Sources, HITL Tier (table 3 cols: Decisione|Tier|Approvatore OR Decision|Tier|Approver), KPI Impattati/KPIs Impacted, Invocation (POST endpoint + body), Audit Footprint."
    - "mkdocs.yml nav extended with `Agents → Maintenance` section pointing to the 4 + 1 pages (mirror existing `Agents → Operations` from 06-14)."
    - "`mkdocs build --strict --config-file docs/mkdocs.yml` succeeds with NO broken links + NO warnings."
    - "4 `test_evidence_panel.py` un-stubbed: each asserts that the agent's audit_writer-written EvidencePanel includes (via `build_ops05_evidence_panel` shipped in 07-06/07/08/09 metadata.py) the OPS-05 declaration fields — agent_id, tool_inventory, data_sources, hitl_tier, kpis_impacted — matching the MkDocs page content exactly. This is the 'docs ↔ code lockstep' enforcement per 06-14 T-V6-doc-drift mitigation."
    - "Bilingual content quality: IT is the primary canonical language (Mantis Textile Group context), EN is parallel mirror; both reference the same TOOL_INVENTORY/DATA_SOURCES/KPIS_IMPACTED constants from each agent's metadata.py (single source of truth in code, doc renders the lists)."
    - "event-taxonomy.it.md / .en.md reference Phase 5 D-65 failure_modes registry as the canonical source + cite ISO 14224 reason_code naming convention from RESEARCH.md."
    - "No brand-prohibited terms (Accenture, etc.) per Phase 12 brand-scrub gate convention; verified via grep."
  artifacts:
    - path: docs/docs/agents/maintenance/predictive-maintenance.md
      provides: "PredictiveMaintenance IT documentation"
      contains: "PredictiveMaintenance|query_timescale|MNT-01"
    - path: docs/docs/en/agents/maintenance/predictive-maintenance.md
      provides: "PredictiveMaintenance EN documentation (parallel mirror)"
      contains: "PredictiveMaintenance|query_timescale|MNT-01"
    - path: docs/docs/agents/maintenance/rca-specialist.md
      provides: "RCASpecialist IT documentation"
      contains: "RCASpecialist|rag_search|traverse_graph|MNT-02"
    - path: docs/docs/agents/maintenance/maintenance-coach.md
      provides: "MaintenanceCoach IT documentation"
      contains: "MaintenanceCoach|request_help|MNT-03"
    - path: docs/docs/agents/maintenance/downtime-analyzer.md
      provides: "DowntimeAnalyzer IT documentation"
      contains: "DowntimeAnalyzer|OEE|Pareto|MNT-04"
    - path: docs/docs/agents/maintenance/event-taxonomy.md
      provides: "Maintenance event taxonomy IT (MNT-05 explicit deliverable)"
      contains: "reason_code|failure_modes|MNT-05"
    - path: docs/mkdocs.yml
      provides: "EXTENDED nav with Agents → Maintenance section (5 pages)"
      contains: "Maintenance|predictive-maintenance|rca-specialist|maintenance-coach|downtime-analyzer|event-taxonomy"
    - path: apps/agents/maintenance/predictive-maintenance/tests/test_evidence_panel.py
      provides: "OPS-05 declaration lockstep test"
    - path: apps/agents/maintenance/rca-specialist/tests/test_evidence_panel.py
      provides: "OPS-05 declaration lockstep test"
    - path: apps/agents/maintenance/maintenance-coach/tests/test_evidence_panel.py
      provides: "OPS-05 declaration lockstep test"
    - path: apps/agents/maintenance/downtime-analyzer/tests/test_evidence_panel.py
      provides: "OPS-05 declaration lockstep test"
  key_links:
    - from: docs/docs/agents/maintenance/*.md
      to: apps/agents/maintenance/*/src/mnt_*/metadata.py
      via: "Single source of truth — metadata.py constants rendered in docs"
      pattern: "TOOL_INVENTORY|DATA_SOURCES|KPIS_IMPACTED"
    - from: apps/agents/maintenance/*/tests/test_evidence_panel.py
      to: apps/agents/maintenance/*/src/mnt_*/metadata.py (build_ops05_evidence_panel)
      via: "Test enforces docs ↔ code lockstep"
      pattern: "build_ops05_evidence_panel|TOOL_INVENTORY"
    - from: docs/docs/agents/maintenance/event-taxonomy.md
      to: packages/sft-domain/failure_modes.yaml (07-02 maintenance subkey)
      via: "Doc enumerates reason_codes from canonical registry"
      pattern: "reason_code|WEAVING-|SPINNING-|DYEING-"
---

<objective>
MNT-05 explicit deliverable: "Tassonomia eventi manutenzione documentata e usata coerentemente cross-agent." This plan ships the documentation (10 bilingual pages) + closes the docs↔code lockstep via 4 test_evidence_panel.py un-stubbing — mirroring exactly the Phase 6 06-14 ops cluster pattern. Maintenance taxonomy itself (failure_modes.yaml extension + validator) was shipped in 07-02; this plan now surfaces it to humans (developers + operators) via MkDocs.

Purpose: Satisfy MNT-05 + provide demo-ready bilingual documentation for the maintenance cluster (Phase 12 competition deliverable) + enforce that the OPS-05-style declaration in code matches the rendered docs (T-V6-doc-drift mitigation from 06-14).
Output: 8 agent docs pages + 2 event-taxonomy pages + mkdocs.yml nav update + 4 evidence-panel lockstep tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md
@.planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md
@.planning/phases/07-agents-maintenance-reliability/07-{06,07,08,09}-PLAN.md
@.planning/phases/06-agents-operations-production/06-14-PLAN.md
@.planning/PROJECT.md
@docs/mkdocs.yml
@docs/docs/agents/operations/anomaly-detector.md
@docs/docs/agents/operations/quality-inspector.md
@docs/docs/en/agents/operations/anomaly-detector.md
@apps/agents/maintenance/predictive-maintenance/src/mnt_predictive_maintenance/metadata.py
@apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/metadata.py
@apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/metadata.py
@apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/metadata.py
@packages/sft-domain/failure_modes.yaml
@packages/sft-agents/src/sft_agents/models/evidence.py

<interfaces>
From 06-14 doc page template (canonical):
```markdown
---
lang: it
agent: <slug>
requirements:
  - MNT-XX
tags:
  - agents
  - maintenance
  - MNT-XX
---

# <Agent Display Name>

## Panoramica
[1-2 paragraphs in Italian, Mantis Textile Group context]

## Strumenti Utilizzati
| Tool | Origine | Funzione |
|------|---------|----------|
| ... | ... | ... |

## Fonti Dati
- ...

## HITL Tier
| Decisione / Severità | Tier | Approvatore |
|---|---|---|
| ... | ... | ... |

## KPI Impattati
- ...

## Invocazione
Endpoint: `POST /v1/agents/<slug>/<action>` body `{...}`
Trigger: [evento-driven / on-demand / scheduler]
Thread ID: `<cluster>.<slug>.<uuid4>` (o `coach-<intervention_id>` per Coach)

## Audit Footprint
- `audit.actions` righe con `agent_id=<slug>`, `action_type=<TYPE>`, decision = `<AUTO|HITL_SUPERVISOR>`.
```

EvidencePanel from `packages/sft-agents/src/sft_agents/models/evidence.py` (Phase 4):
- carries `model`, `tool_calls`, `prompt_hash`, etc.
- 06-14 Task 2 pattern: agent's `build_ops05_evidence_panel(...)` helper returns EvidencePanel populated with OPS-05 declaration fields (either as nested attributes or via convention on tool_inventory / data_sources / hitl_tier / kpis_impacted). Verify by reading `apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/metadata.py` + `apps/agents/ops/anomaly-detector/tests/test_evidence_panel.py`.

OPS-05 / MNT-05 declaration constants per agent (already shipped in 07-06/07/08/09 metadata.py):

| Agent | TOOL_INVENTORY | DATA_SOURCES (excerpt) | KPIS_IMPACTED |
|-------|----------------|------------------------|---------------|
| predictive-maintenance | ['query_timescale','escalate_to_supervisor','log_event'] | ['TimescaleDB sensor_events','sft-ml ridge-fd001-fd003.joblib','sft-assets registry'] | ['mtbf','planned_vs_unplanned_downtime','rul_accuracy_mae'] |
| rca-specialist | ['rag_search','traverse_graph','escalate_to_supervisor','log_event'] | ['Qdrant sop_chunks','Neo4j textile graph','PG documents','maintenance.downtime_events (08)'] | ['rca_completeness_5why','citation_grounding_rate','mttr_root_cause_to_fix'] |
| maintenance-coach | ['rag_search','request_help','escalate_to_supervisor','log_event'] | ['Qdrant sop_chunks','Phase 5 SOP corpus','langgraph_checkpoints PG (005)'] | ['mttr','first_time_fix_rate','intervention_completion_rate','technician_help_request_rate'] |
| downtime-analyzer | ['query_timescale','log_event'] | ['NATS maintenance.downtime.>','PG maintenance.downtime_events (08)','PG audit.actions cross-cluster','sim-textile production_state','sft-assets'] | ['oee_availability','oee_performance','oee_quality','mtbf','top_5_downtime_reason_codes'] |

HITL tier per agent:
- predictive-maintenance: AUTO (health_index≥0.3), SUPERVISOR (health_index<0.3)
- rca-specialist: ALWAYS SUPERVISOR (D-RCA-02 literal)
- maintenance-coach: AUTO (normal step), SUPERVISOR (when request_help invoked, marker `escalation_trigger='technician_request'`)
- downtime-analyzer: AUTO always (analytics — no HITL gate; supervisor reviews via dashboard)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 8 MkDocs maintenance agent pages (4 agents × IT + EN) + 2 event-taxonomy pages (IT + EN) + update mkdocs.yml nav</name>
  <files>
    docs/docs/agents/maintenance/predictive-maintenance.md,
    docs/docs/en/agents/maintenance/predictive-maintenance.md,
    docs/docs/agents/maintenance/rca-specialist.md,
    docs/docs/en/agents/maintenance/rca-specialist.md,
    docs/docs/agents/maintenance/maintenance-coach.md,
    docs/docs/en/agents/maintenance/maintenance-coach.md,
    docs/docs/agents/maintenance/downtime-analyzer.md,
    docs/docs/en/agents/maintenance/downtime-analyzer.md,
    docs/docs/agents/maintenance/event-taxonomy.md,
    docs/docs/en/agents/maintenance/event-taxonomy.md,
    docs/mkdocs.yml
  </files>
  <read_first>
    docs/mkdocs.yml (current nav structure; bilingual i18n plugin convention),
    docs/docs/agents/operations/anomaly-detector.md (Phase 6 06-14 IT page template — mirror exactly),
    docs/docs/en/agents/operations/anomaly-detector.md (Phase 6 EN parallel page),
    docs/docs/agents/operations/quality-inspector.md (HITL tier table convention),
    .planning/PROJECT.md (Mantis Textile Group context + core value),
    apps/agents/maintenance/*/src/mnt_*/metadata.py (TOOL_INVENTORY/DATA_SOURCES/KPIS_IMPACTED — render in docs),
    packages/sft-domain/failure_modes.yaml (07-02 maintenance subkey — enumerate in event-taxonomy.md),
    .planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md (D-MNT-TAX + canonical ISO 14224 + OEE references)
  </read_first>
  <action>
    For each of the 4 agents, author 2 pages (IT + EN) using the 06-14 template above, populated from each agent's metadata.py constants (single source of truth — copy-paste from the constants, do NOT redefine).

    **predictive-maintenance.it.md / .en.md** sections (Italian template; EN is parallel mirror with same data):
    - Panoramica: "PredictiveMaintenance stima la Remaining Useful Life (RUL) di un asset tessile (telai, filatoi) usando un modello ML lightweight (Ridge regression) addestrato sul dataset NASA C-MAPSS FD001+FD003. È event-driven: viene attivato automaticamente quando AnomalyDetector (Phase 6 cluster ops) rileva un'anomalia con severità major/critical e pubblica sul subject NATS `maintenance.predict.<asset_id>`."
    - Strumenti Utilizzati table: 3 rows (query_timescale, escalate_to_supervisor, log_event) with Origine (Phase 3/Phase 6/Phase 4) + Funzione columns.
    - Fonti Dati: bullet list rendering DATA_SOURCES constant.
    - HITL Tier table: 2 rows (health_index ≥ 0.3 → AUTO / none, health_index < 0.3 → SUPERVISOR / shift supervisor).
    - KPI Impattati: bullet list rendering KPIS_IMPACTED with 1-line explanation each (MTBF, planned_vs_unplanned_downtime ratio, RUL accuracy MAE).
    - Invocazione: `POST /v1/agents/predictive-maintenance/score` body `{asset_id, triggered_by_action_id?, user_roles}` + auto-trigger from AnomalyDetector NATS publish (cross-cluster wiring documented via Open Q1 Option a).
    - Audit Footprint: `audit.actions` rows with `agent_id=predictive-maintenance`, `action_type=RUL_ESTIMATE` (from migration 009 / 07-01), `cluster=maintenance`. `evidence_panel.tool_calls[0].args.triggered_by_action_id` links to AnomalyDetector's audit row (MNT-06 audit chain).

    **rca-specialist.it.md / .en.md**:
    - Panoramica: "RCASpecialist conduce l'analisi delle cause radici (Root Cause Analysis) usando la metodologia 5-Why iterativa. Ogni passo è obbligatoriamente supportato da almeno una citazione recuperata dal knowledge base tessile (SOP, manuali) tramite rag_search; la validazione post-LLM verifica che ogni source_uri esista in PostgreSQL (Open Q5 — full lookup). La raccomandazione correttiva passa SEMPRE per approvazione supervisor (success criterion #2 letterale)."
    - Strumenti: rag_search, traverse_graph, escalate_to_supervisor, log_event.
    - HITL Tier table: 1 row ("Tutte le RCA — corrective_action_recommendation" → SUPERVISOR / shift supervisor).
    - Invocazione: `POST /v1/agents/rca-specialist/analyze` body `{problem_statement, downtime_event_id?, asset_id?, user_roles, lang}` → 202 (HITL async).
    - Audit: action_type=RCA_CHAIN; evidence_panel includes 5 WhyStep + citations.

    **maintenance-coach.it.md / .en.md**:
    - Panoramica: "MaintenanceCoach guida un tecnico passo-passo nell'esecuzione di una SOP per la riparazione di un asset. Ogni intervento è un thread LangGraph asincrono persistito in PostgreSQL (langgraph_checkpoints, migration 005), così il tecnico può pausare e riprendere anche dopo turni successivi. Il MTTR (Mean Time To Repair) è calcolato dall'inizio (mttr_start) alla chiusura (mttr_end) dell'intervento. Quando il tecnico usa parole-chiave di richiesta aiuto ('aiuto', 'sono bloccato', 'help', 'stuck'), il tool `request_help` escalates al supervisore."
    - Strumenti: rag_search, request_help (07-04), escalate_to_supervisor, log_event.
    - HITL Tier table: 2 rows (passo normale → AUTO / nessuno; request_help triggered → SUPERVISOR / shift supervisor + marker `escalation_trigger='technician_request'`).
    - Invocazione: 3 endpoints (start / step / resume). thread_id naming convention `coach-<intervention_id>` (Open Q6).
    - Audit: action_type=COACH_STEP per step; carry `escalation_trigger` marker on help-triggered rows.

    **downtime-analyzer.it.md / .en.md**:
    - Panoramica: "DowntimeAnalyzer ingerisce gli eventi di downtime dal simulatore sim-textile (subject NATS `maintenance.downtime.<asset_id>`), li persiste in PostgreSQL/TimescaleDB (hypertable `maintenance.downtime_events`, migration 008), e fornisce on-demand i report OEE (Overall Equipment Effectiveness) con decomposizione Availability × Performance × Quality + analisi Pareto top-N reason_code. La componente Quality è calcolata cross-cluster leggendo le decisioni QualityInspector (cluster ops, Phase 6) con fallback automatico ai metrics del simulatore in caso di gap (D-DA-02)."
    - Strumenti: query_timescale, log_event (no LLM — agente deterministico).
    - HITL Tier table: 1 row ("Tutti i report" → AUTO / nessuno — analytics non richiede approvazione).
    - Invocazione: `POST /v1/agents/downtime-analyzer/report` body ReportRequest (window_start/end, by_asset?, top_n_pareto?).
    - Audit: 2 action types — DOWNTIME_VERDICT per evento ingerito, OEE_REPORT per query on-demand.

    **event-taxonomy.it.md / .en.md** (MNT-05 explicit deliverable):
    - Header: "Questa pagina documenta la tassonomia degli eventi di manutenzione (reason_code) usata dal cluster maintenance. La sorgente canonica è `packages/sft-domain/failure_modes.yaml` (07-02 extension D-MNT-TAX); i validator CI assicurano unicità del reason_code + risoluzione di intervention_steps_sop_id verso il corpus Phase 5."
    - Sezione "Convenzione di Naming": riferimento ISO 14224 spirit `<MODULE>-<DEFECT_ABBR>-<NNN>` (es. WEAVING-BE-001 per broken end in weaving).
    - Tabella reason_code: leggi `packages/sft-domain/failure_modes.yaml` e enumera le 7 voci tessili (broken_end, mispick, slub, neppy, selvage_fault, shade_deviation, unlevel_dyeing). Per ogni voce: nome IT, nome EN, asset_families, reason_code, mttr_target_minutes, intervention_steps_sop_id, preventive_check_interval_hours (se presente), severity.
    - Sezione "Uso cross-agent": elenco di come il reason_code viene usato in PredictiveMaintenance (no — PM lavora su RUL non su reason_code), RCASpecialist (problem_statement spesso include reason_code), MaintenanceCoach (request_help context include reason_code), DowntimeAnalyzer (Pareto raggruppa per reason_code).
    - Sezione "Aggiungere una nuova reason_code": istruzioni step-by-step per estendere failure_modes.yaml + far passare il CI validator (`scripts/validate-failure-modes.py` di 07-02).

    **mkdocs.yml extension**:
    Add nav entries under existing Agents section (mirror 06-14 pattern):
    ```yaml
    nav:
      ...
      - Agents:
          - Operations:
              - OperatorAssistant: agents/operations/operator-assistant.md
              - ...
          - Maintenance:
              - PredictiveMaintenance: agents/maintenance/predictive-maintenance.md
              - RCASpecialist: agents/maintenance/rca-specialist.md
              - MaintenanceCoach: agents/maintenance/maintenance-coach.md
              - DowntimeAnalyzer: agents/maintenance/downtime-analyzer.md
              - Tassonomia Eventi: agents/maintenance/event-taxonomy.md
    ```
    (EN versions resolved via the i18n plugin convention already in use Phase 2/5/6 — verify exact plugin config in current mkdocs.yml; if separate nav for EN is needed, add a parallel section under `en/`.)

    Verify build: `mkdocs build --strict --config-file docs/mkdocs.yml` MUST succeed with zero broken links + zero warnings. If broken links surface (e.g. references to `agents/maintenance/*` from other pages that don't yet exist), fix the references or add the cross-link pages.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; mkdocs build --strict --config-file docs/mkdocs.yml 2&gt;&amp;1 | tail -30 &amp;&amp; ls docs/docs/agents/maintenance/*.md | wc -l | grep -q "^5$" &amp;&amp; ls docs/docs/en/agents/maintenance/*.md | wc -l | grep -q "^5$" &amp;&amp; grep -q "Maintenance:" docs/mkdocs.yml &amp;&amp; grep -q "WEAVING-BE-001" docs/docs/agents/maintenance/event-taxonomy.md &amp;&amp; ! grep -ri "Accenture" docs/docs/agents/maintenance/ &amp;&amp; ! grep -ri "Accenture" docs/docs/en/agents/maintenance/ &amp;&amp; echo OK</automated>
  </verify>
  <done>5 IT + 5 EN docs pages exist; mkdocs build --strict succeeds; nav extended; reason_codes enumerated in event-taxonomy.md; no brand-prohibited terms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Un-stub 4 test_evidence_panel.py — assert OPS-05 declaration fields present in agent response/audit (docs ↔ code lockstep, mirror 06-14 Task 2)</name>
  <files>
    apps/agents/maintenance/predictive-maintenance/tests/test_evidence_panel.py,
    apps/agents/maintenance/rca-specialist/tests/test_evidence_panel.py,
    apps/agents/maintenance/maintenance-coach/tests/test_evidence_panel.py,
    apps/agents/maintenance/downtime-analyzer/tests/test_evidence_panel.py
  </files>
  <read_first>
    apps/agents/ops/anomaly-detector/tests/test_evidence_panel.py (Phase 6 06-14 reference — verbatim test pattern to mirror),
    apps/agents/ops/quality-inspector/tests/test_evidence_panel.py (multi-tier HITL assertion pattern),
    apps/agents/maintenance/*/src/mnt_*/metadata.py (TOOL_INVENTORY/DATA_SOURCES/KPIS_IMPACTED constants),
    packages/sft-agents/src/sft_agents/models/evidence.py (EvidencePanel schema),
    docs/docs/agents/maintenance/*.md (Task 1 — assert lockstep by referencing same constants)
  </read_first>
  <behavior>
    Delete Wave 0 placeholder `test_evidence_panel.py` for each of the 4 agents. Author the lockstep tests:

    For EACH of the 4 agents, the test module:
    - Imports `TOOL_INVENTORY, DATA_SOURCES, KPIS_IMPACTED, build_ops05_evidence_panel` from the agent's `metadata` module.
    - Test 1 (`test_tool_inventory_matches_docs`): assert specific tools present per the table above (e.g. for PM: `assert 'query_timescale' in TOOL_INVENTORY` + length == 3). The actual list comparison should be against the exact values shipped by 07-06/07/08/09 metadata.py — this test is the source-of-truth gate.
    - Test 2 (`test_data_sources_include_canonical`): assert specific data sources present (e.g. PM includes 'sft-ml ridge-fd001-fd003-v1.0.joblib').
    - Test 3 (`test_kpis_impacted_include_canonical`): assert specific KPIs present.
    - Test 4 (`test_build_ops05_evidence_panel_returns_valid_evidence`): call `build_ops05_evidence_panel(input_summary='test', model_version='v1.0', tool_calls=[], decision=Decision.AUTO)` (adjust kwargs per agent's signature — read each metadata.py) and assert the returned EvidencePanel:
      - is a valid Pydantic instance (no ValidationError);
      - has `model` set (regex `^[a-z0-9_.-]+@[a-z0-9_.-]+$`);
      - has `prompt_hash` set (length 64 for hex or sentinel `0`*64);
      - has `tool_calls` as provided.
      - Note: if `build_ops05_evidence_panel` carries the OPS-05 declaration fields as embedded JSONB in `tool_calls[0].args.declaration` OR as separate EvidencePanel attributes (Phase 6 06-14 chose one path — verify), the test asserts the chosen path.
    - Test 5 (`test_hitl_tier_per_decision`): assert the tier derivation rule per agent matches docs. For PM: `_resolve_tier(decision=Decision.AUTO)` → "none" / `_resolve_tier(decision=Decision.HITL_SUPERVISOR)` → "supervisor". For RCA: always "supervisor". For Coach: AUTO → "none", HITL_SUPERVISOR → "supervisor". For DA: always "none".

    Note: the test interface depends on whether `build_ops05_evidence_panel` is the canonical helper OR if there's a separate `_resolve_tier` function. Read each agent's metadata.py + agent.py and align the test with the actual contract (the test exists to enforce the contract, not invent it).

    Pattern 06-14 had ONE `test_evidence_panel.py` per agent with ~5 assertions; mirror that scope.

    All 4 test modules turn green; agent classes do NOT need modification if 07-06/07/08/09 metadata.py is correct (which it should be — those plans shipped the constants + helper).
  </behavior>
  <action>
    Open `apps/agents/ops/anomaly-detector/tests/test_evidence_panel.py` first to confirm the exact test pattern. Mirror file-by-file with the maintenance agents' constants. Use direct `from mnt_<slug>.metadata import ...` imports — no fixture indirection needed.

    If the 06-14 reference test asserts a specific EvidencePanel attribute path (e.g. `panel.tool_calls[0].args['kpis_impacted']`) and the maintenance metadata.py uses a different path (e.g. embeds OPS-05 differently), update each agent's metadata.py to align — but with the smallest diff (a one-line attribute addition or rename). Document any code change in SUMMARY.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/maintenance/predictive-maintenance/tests/test_evidence_panel.py apps/agents/maintenance/rca-specialist/tests/test_evidence_panel.py apps/agents/maintenance/maintenance-coach/tests/test_evidence_panel.py apps/agents/maintenance/downtime-analyzer/tests/test_evidence_panel.py -x</automated>
  </verify>
  <done>All 4 evidence_panel tests green; OPS-05/MNT-05 declaration enforced in code; docs ↔ code lockstep verified for all 4 maintenance agents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/*.md → mkdocs build | Static content, repo-controlled, PR-reviewed |
| metadata.py constants → docs render | Single source of truth in code; test enforces lockstep |
| docs/event-taxonomy.md → failure_modes.yaml | Doc enumerates registry contents; validator 07-02 ensures registry integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V7-doc-drift | Repudiation | docs declare X tools, code uses Y | mitigate | Task 2 test_evidence_panel.py asserts code's TOOL_INVENTORY matches docs (constants are the single source; tests prevent unilateral changes). |
| T-V7-brand-leak | Information Disclosure | docs containing brand-prohibited terms (Accenture etc.) | mitigate | Task 1 verify includes grep gate; Phase 12 has CI brand-scrub gate too. |
| T-V7-taxonomy-doc-drift | Repudiation | event-taxonomy.md enumerates outdated reason_codes | mitigate | Doc page references failure_modes.yaml as canonical; reviewer cross-checks PR; 07-02 validator ensures registry uniqueness. |
| T-V7-mkdocs-broken-link | Information Disclosure | broken cross-references in docs | mitigate | `--strict` flag in verify command fails build on any broken link. |
</threat_model>

<deviation_rules>
- **Rule 1:** MNT-05 deliverable LOCKED — taxonomy doc + bilingual + cross-agent reference. Do NOT split into multiple pages or move to a different location.
- **Rule 3:** Phase 6 06-14 ops cluster docs MUST stay green (`mkdocs build --strict` regression check). Do NOT modify ops docs.
- **Rule 4:** Capture in SUMMARY: build_ops05_evidence_panel signature variations across agents (if any aligned in Task 2); mkdocs i18n plugin convention used (one nav vs separate nav per language); brand-scrub grep results; reason_code count documented vs registry count (must match).
</deviation_rules>

<verification>
- `mkdocs build --strict --config-file docs/mkdocs.yml` green.
- 4 evidence_panel tests green: `nx run-many --target=test --projects=mnt-predictive-maintenance,mnt-rca-specialist,mnt-maintenance-coach,mnt-downtime-analyzer -- -k evidence_panel`.
- 10 docs files exist (5 IT + 5 EN).
- mkdocs.yml nav lists 5 maintenance pages.
- No brand-prohibited terms in maintenance docs.

## Wave 0 dependency
- This plan un-stubs Wave 0 placeholders `apps/agents/maintenance/*/tests/test_evidence_panel.py` (4 files).
</verification>

<success_criteria>
- MNT-05 mechanically verifiable: every maintenance agent's response/audit panel declares the 4-field OPS-05/MNT-05 contract (tool_inventory + data_sources + hitl_tier + kpis_impacted) AND the docs render the same data.
- Bilingual docs (IT canonical + EN parallel) build clean.
- Event taxonomy documented + linked to canonical registry.
- Phase 12 brand-scrub passes (zero hits in maintenance docs).
- Wave 0 placeholder evidence_panel tests un-stubbed.
</success_criteria>

<commit_strategy>
- Commit 1: `docs(07-11): 4 maintenance agent pages IT + EN (MNT-01/02/03/04)` (8 markdown files)
- Commit 2: `docs(07-11): event-taxonomy.md IT + EN (MNT-05 explicit deliverable)` (2 markdown files)
- Commit 3: `docs(07-11): mkdocs.yml nav extension for Maintenance cluster` (mkdocs.yml)
- Commit 4: `test(07-11): un-stub 4 test_evidence_panel.py — docs ↔ code lockstep (mirror 06-14)` (4 test files)
</commit_strategy>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-11-SUMMARY.md` documenting: 10 docs pages shipped; nav extension structure; mkdocs i18n plugin convention; reason_code count rendered vs registry count; build_ops05_evidence_panel signature alignment across 4 agents (any code adjustments noted); brand-scrub verification.
</output>
</content>
