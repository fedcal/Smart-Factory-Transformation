---
phase: 07-agents-maintenance-reliability
plan: 05
plan_id: 07-05
type: tdd
wave: 2
depends_on: ["07-00", "07-02"]
files_modified:
  - infra/migrations/timescale/008_create_downtime_events.sql
  - infra/migrations/timescale/tests/test_migration_008.py
  - simulators/sim-textile/src/sim_textile/downtime_event_generator.py
  - simulators/sim-textile/src/sim_textile/models.py
  - simulators/sim-textile/tests/test_downtime_generator.py
autonomous: false
requirements: [MNT-04, MNT-05]
objective: "Two coupled deliverables: (a) migration 008 creates `maintenance.downtime_events` TimescaleDB hypertable + `maintenance.oee_hourly` continuous aggregate (D-DA-01/03), idempotent + testcontainers-verified; (b) sim-textile gains `downtime_event_generator.py` (mirror of `quality_event_generator.py`) that emits stochastic `DowntimeEvent` payloads on NATS subject `maintenance.downtime.<asset_id>`, with reason_code drawn from the failure_modes.yaml maintenance taxonomy (07-02)."

must_haves:
  truths:
    - "Migration 008 creates schema `maintenance`, table `maintenance.downtime_events` as TimescaleDB hypertable on `timestamp`, and continuous aggregate `maintenance.oee_hourly` with refresh policy 5min."
    - "Migration 008 is idempotent — running twice does not error."
    - "Table columns capture the DowntimeEvent payload: event_id (uuid PK), asset_id, reason_code, duration_min (int), severity (TEXT CHECK in {minor,major,critical}), work_order_id (TEXT NULL), dye_lot_id (TEXT NULL), source (TEXT default 'simulator'), timestamp (TIMESTAMPTZ NOT NULL), inserted_at (TIMESTAMPTZ DEFAULT NOW())."
    - "`maintenance.oee_hourly` continuous aggregate has at minimum: asset_id, hour_bucket (time_bucket('1 hour', timestamp)), total_downtime_min, event_count — plus any helper columns the 07-09 OEE.A computation requires."
    - "`downtime_event_generator.py` mirrors `quality_event_generator.py` pattern S-6 invariants (UTC-aware datetime, per-asset seeded Random, asyncio.CancelledError graceful)."
    - "Generator emits `DowntimeEvent` Pydantic payload on NATS subject `maintenance.downtime.<asset_id>` with reason_code drawn from `failure_modes.yaml` maintenance taxonomy (filtered to entries where `maintenance is not None` and asset_family matches)."
    - "Generator uses lower nominal/faulted rates than QualityEvent generator (downtime events are rarer): default `_RATE_NOMINAL_PER_MIN=2`, faulted-mode rate suggested 8/min (configurable)."
    - "DowntimeEvent Pydantic model is frozen+extra=forbid with all D-DA-01 fields."
  artifacts:
    - path: infra/migrations/timescale/008_create_downtime_events.sql
      provides: "schema + hypertable + CAGG + refresh policy DDL (idempotent)"
      contains: "maintenance.downtime_events|continuous|add_continuous_aggregate_policy"
    - path: simulators/sim-textile/src/sim_textile/downtime_event_generator.py
      provides: "async DowntimeEventGenerator + NATS publish loop"
      exports: ["DowntimeEvent", "DowntimeEventGenerator"]
    - path: simulators/sim-textile/src/sim_textile/models.py
      provides: "Extended with DowntimeEvent if models.py is the central simulator model module; OR a new module file if convention is per-feature placement"
      contains: "DowntimeEvent"
  key_links:
    - from: simulators/sim-textile/src/sim_textile/downtime_event_generator.py
      to: packages/sft-domain/failure_modes.yaml (maintenance.reason_code)
      via: "failure_modes loader filtered + sampled"
      pattern: "load_failure_modes|reason_code|maintenance.reason_code"
    - from: simulators/sim-textile/src/sim_textile/downtime_event_generator.py
      to: NATS subject maintenance.downtime.<asset_id>
      via: "nc.publish"
      pattern: "maintenance.downtime|nc.publish"
    - from: infra/migrations/timescale/008_create_downtime_events.sql (CAGG)
      to: maintenance.downtime_events (hypertable)
      via: "SELECT FROM maintenance.downtime_events GROUP BY"
      pattern: "FROM maintenance.downtime_events"
---

<objective>
Ship the data plane that DowntimeAnalyzer (07-09) will consume. The migration creates the persistence layer (hypertable + CAGG) and the simulator extension creates the live event stream — both are required for 07-09 to do anything beyond reading empty tables. Mirroring 06-09 (quality_event_generator) and 06-01 (audit migration) keeps the change surface small and reuses proven patterns.

Purpose: Satisfy MNT-04 data foundation (downtime event store + OEE materialization) + MNT-05 taxonomy usage (reason_code drawn from failure_modes.yaml maintenance subkey). Without this plan the DowntimeAnalyzer cannot be implemented or tested.
Output: 1 SQL migration + 1 testcontainers test + 1 new simulator module + DowntimeEvent Pydantic + 1 simulator test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md
@.planning/phases/07-agents-maintenance-reliability/07-RESEARCH.md
@.planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md
@.planning/phases/06-agents-operations-production/06-09-PLAN.md
@infra/migrations/timescale/001_create_sensor_events.sql
@infra/migrations/timescale/005_create_langgraph_checkpoints.sql
@infra/migrations/timescale/007_extend_audit_decisions.sql
@infra/migrations/timescale/tests/test_migration_007.py
@simulators/sim-textile/src/sim_textile/quality_event_generator.py
@simulators/sim-textile/src/sim_textile/production_state.py
@packages/sft-domain/src/sft_domain/failure_modes/_loader.py
@packages/sft-domain/failure_modes.yaml

<interfaces>
From 07-PATTERNS.md Section 9 lines 393-422 (migration 008 SQL skeleton — verbatim):
```sql
CREATE SCHEMA IF NOT EXISTS maintenance;

CREATE TABLE IF NOT EXISTS maintenance.downtime_events (
  event_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  asset_id TEXT NOT NULL,
  reason_code TEXT NOT NULL,
  duration_min INTEGER NOT NULL CHECK (duration_min >= 0),
  severity TEXT NOT NULL CHECK (severity IN ('minor','major','critical')),
  work_order_id TEXT NULL,
  dye_lot_id TEXT NULL,
  source TEXT NOT NULL DEFAULT 'simulator',
  timestamp TIMESTAMPTZ NOT NULL,
  inserted_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

SELECT create_hypertable('maintenance.downtime_events', 'timestamp', if_not_exists => TRUE);

CREATE MATERIALIZED VIEW IF NOT EXISTS maintenance.oee_hourly
  WITH (timescaledb.continuous) AS
SELECT
  asset_id,
  time_bucket('1 hour', timestamp) AS hour_bucket,
  SUM(duration_min) AS total_downtime_min,
  COUNT(*) AS event_count
FROM maintenance.downtime_events
GROUP BY asset_id, hour_bucket;

SELECT add_continuous_aggregate_policy('maintenance.oee_hourly',
  start_offset => INTERVAL '2 hours',
  end_offset => INTERVAL '5 minutes',
  schedule_interval => INTERVAL '5 minutes',
  if_not_exists => TRUE);
```

From 07-CONTEXT.md D-DA-01 (DowntimeEvent payload):
```python
class DowntimeEvent(BaseModel):
    model_config = ConfigDict(frozen=True, extra="forbid")
    event_id: str  # UUID4
    asset_id: str
    reason_code: str
    duration_min: int  # ge=0
    severity: Literal["minor","major","critical"]
    work_order_id: str | None
    dye_lot_id: str | None
    source: Literal["simulator","operator"]
    timestamp: datetime  # tz-aware
```

From 07-PATTERNS.md Section 12 lines 504-520 (downtime_event_generator mirror sub list):
- DefectType → DowntimeReasonCode (drawn from failure_modes.yaml maintenance.reason_code, filtered to entries matching the asset family)
- _RATE_NOMINAL_PER_MIN = 10 → 2 (downtime rare); faulted-mode 8
- NATS subject quality.events.<asset> → maintenance.downtime.<asset>
- Pattern S-6 invariants: datetime.now(UTC), per-asset random.Random(asset_id), asyncio.CancelledError handler graceful
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write idempotent migration 008 (downtime_events hypertable + oee_hourly CAGG) + testcontainers test</name>
  <files>
    infra/migrations/timescale/008_create_downtime_events.sql,
    infra/migrations/timescale/tests/test_migration_008.py
  </files>
  <read_first>
    infra/migrations/timescale/001_create_sensor_events.sql (hypertable pattern reference),
    infra/migrations/timescale/005_create_langgraph_checkpoints.sql (PG-only table reference),
    infra/migrations/timescale/007_extend_audit_decisions.sql (idempotency pattern from 06-01),
    infra/migrations/timescale/tests/test_migration_007.py (fixture pattern + testcontainers PG setup),
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 9 lines 393-422 verbatim SQL skeleton + Section 11 lines 491-498 test matrix for migration 008)
  </read_first>
  <behavior>
    Migration 008 SQL:
    - `CREATE SCHEMA IF NOT EXISTS maintenance;`
    - `CREATE TABLE IF NOT EXISTS maintenance.downtime_events (...)` with all 10 columns per the SQL skeleton above. Use `gen_random_uuid()` default for event_id (requires `pgcrypto` or pg14+ native; use pg14+ assumption consistent with Phase 3 baseline — check 001 migration for the convention).
    - `SELECT create_hypertable('maintenance.downtime_events', 'timestamp', if_not_exists => TRUE);` — idempotent.
    - `CREATE MATERIALIZED VIEW IF NOT EXISTS maintenance.oee_hourly WITH (timescaledb.continuous) AS SELECT ...` per skeleton. Idempotent via `IF NOT EXISTS`. Wrap in `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;` if continuous aggregate DDL doesn't natively support IF NOT EXISTS (verify against TimescaleDB 2.18.0 docs).
    - `SELECT add_continuous_aggregate_policy('maintenance.oee_hourly', ..., if_not_exists => TRUE);` — TimescaleDB native idempotency flag.
    - Sql comment header: "Migration 008 — D-DA-01 + D-DA-03; creates downtime_events hypertable + oee_hourly continuous aggregate."
    - Add indexes: `CREATE INDEX IF NOT EXISTS idx_downtime_asset_time ON maintenance.downtime_events (asset_id, timestamp DESC);` and `CREATE INDEX IF NOT EXISTS idx_downtime_reason_time ON maintenance.downtime_events (reason_code, timestamp DESC);` for Pareto query support.

    test_migration_008.py covers (mirror 07-PATTERNS.md Section 11 lines 491-498):
    1. `test_post_migration_creates_downtime_events_table` — apply 001..007 then 008, query `pg_tables WHERE schemaname='maintenance' AND tablename='downtime_events'` returns row.
    2. `test_post_migration_creates_hypertable` — query `timescaledb_information.hypertables WHERE hypertable_name='downtime_events'` returns row.
    3. `test_post_migration_creates_oee_hourly_caggr` — query `timescaledb_information.continuous_aggregates WHERE view_name='oee_hourly'` returns row.
    4. `test_post_migration_insert_downtime_event_then_refresh_caggr` — INSERT 3 rows for asset_id='LOOM-01' across a 1-hour window, then `CALL refresh_continuous_aggregate('maintenance.oee_hourly', NULL, NULL);`, then SELECT FROM oee_hourly returns 1 row with total_downtime_min = sum + event_count=3.
    5. `test_post_migration_idempotent_double_apply` — re-running 008 against the same DB is a no-op (no error).
    6. `test_post_migration_check_constraint_severity` — INSERT with severity='invalid' raises CheckViolationError.
    7. `test_post_migration_check_constraint_duration` — INSERT with duration_min=-1 raises CheckViolationError.

    Fixture pattern: reuse `fresh_dsn` + `_run_baseline_migrations(dsn)` from `test_migration_007.py`. Extend baseline helper to include 008 application before test runs (or apply 008 inside each test).
  </behavior>
  <action>
    Author migration SQL per skeleton. Verify TimescaleDB version (`SELECT extversion FROM pg_extension WHERE extname='timescaledb';`) at the top of the file as a comment annotation — target version is `timescaledb 2.18.0` (per 07-PATTERNS.md Section 11 risk callout L500).

    Author test_migration_008.py mirroring test_migration_007.py setup (lines 26-46 + lines 108-130). For the CAGG refresh test, use `await conn.execute("CALL refresh_continuous_aggregate('maintenance.oee_hourly', NULL, NULL);")` then assert oee_hourly rows match.

    If continuous aggregate creation in testcontainers Timescale image fails (unclear how the 2.18.0-pg16 image handles CAGG without recent license activation), fall back to plain MATERIALIZED VIEW with a SQL comment noting the limitation and capture in SUMMARY. Decision: prefer real continuous aggregate; only fall back if testcontainers smoke explicitly fails.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest infra/migrations/timescale/tests/test_migration_008.py -m integration -x</automated>
  </verify>
  <done>Migration 008 applied twice without error against fresh testcontainers PG; hypertable + CAGG present; INSERT + refresh + SELECT round-trip green; 7 tests pass.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: BLOCKING — Push migration 008 to dev TimescaleDB and verify CAGG creation</name>
  <files>
    infra/migrations/timescale/008_create_downtime_events.sql
  </files>
  <what-built>Migration 008 SQL file is in repo; testcontainers tests green.</what-built>
  <how-to-verify>
Run migration on dev TimescaleDB:
```bash
make migrate-timescale   # or python infra/migrations/timescale/migrate.py up
```
Then verify:
```bash
psql "$TIMESCALE_DSN" -c "SELECT hypertable_name FROM timescaledb_information.hypertables WHERE hypertable_schema='maintenance';"
psql "$TIMESCALE_DSN" -c "SELECT view_name, materialization_hypertable_name FROM timescaledb_information.continuous_aggregates WHERE view_schema='maintenance';"
psql "$TIMESCALE_DSN" -c "INSERT INTO maintenance.downtime_events (asset_id, reason_code, duration_min, severity, source, timestamp) VALUES ('LOOM-01', 'WEAVING-BE-001', 30, 'minor', 'simulator', NOW()) RETURNING event_id;"
psql "$TIMESCALE_DSN" -c "CALL refresh_continuous_aggregate('maintenance.oee_hourly', NULL, NULL); SELECT * FROM maintenance.oee_hourly LIMIT 5;"
```
The first two queries must return rows (hypertable + CAGG present). The INSERT must succeed. The final SELECT must show at least the synthesized row.
  </how-to-verify>
  <resume-signal>Type "approved — migration 008 pushed" once verified.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing tests for DowntimeEvent model + DowntimeEventGenerator</name>
  <files>
    simulators/sim-textile/tests/test_downtime_generator.py
  </files>
  <read_first>
    simulators/sim-textile/src/sim_textile/quality_event_generator.py (Phase 6 06-09 mirror — analyze patterns L1-274),
    simulators/sim-textile/src/sim_textile/production_state.py (state pattern + UTC-aware datetime),
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 12 lines 504-520 — generator mirror substitutions; Shared Pattern C L734-738 — UTC + Pitfall §3),
    .planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md (D-DA-01 lines 110-112 DowntimeEvent fields)
  </read_first>
  <behavior>
    test_downtime_generator.py covers:
    - `DowntimeEvent` frozen + extra=forbid; rejects naive datetime; severity Literal; duration_min ge=0; reason_code non-empty.
    - `DowntimeEvent` round-trip: model_dump_json → json.loads → model_validate yields equal instance.
    - `DowntimeEventGenerator(assets, rate_nominal=2, rate_faulted=8, random_seed=...)` instantiable with iterable of asset descriptors.
    - Per-asset RNG isolation: two generators with different seed but same asset list produce different reason_code sequences; same seed produces same sequence (determinism).
    - `await generator.tick(now)` (or analogous step method) when called sufficient times produces approximately `rate_nominal` events per simulated minute (loose statistical assertion — run 100 ticks of 1-minute each and assert event count within ±50% of expected).
    - `await generator.run(nc, stop_event)`: when started in nominal mode publishes events on `maintenance.downtime.<asset_id>` subject; reason_code matches one of the allowed taxonomy codes for that asset's family per failure_modes.yaml maintenance taxonomy.
    - Generator handles `asyncio.CancelledError` gracefully (test: cancel the run task, assert no exception leaks and any in-flight publish completes or is dropped cleanly).
    - Subject string is exactly `f"maintenance.downtime.{asset_id}"` (no extra wildcards).
    - Reason_code filter: when failure_modes has no `maintenance` taxonomy for the asset's family (e.g., a non-textile asset_family), generator emits no events for that asset (log + skip), does NOT crash.
    - Severity distribution: most events `minor`; ~10-20% `major`; <5% `critical` (loose assertion on 200-tick simulation).
    - Use NATS mock (no testcontainers required for unit test — mock `nc.publish` via AsyncMock; testcontainers NATS only used in 07-12 E2E).
  </behavior>
  <action>
    Delete Wave 0 placeholder. Write tests per behavior. For statistical assertions use `random.Random(42)` seed + replay 200 ticks; allow generous tolerance. For NATS publish, mock `nc = AsyncMock()`; assert `nc.publish.call_args_list` contains expected subjects + payloads.

    Tests fail (DowntimeEvent + DowntimeEventGenerator don't exist yet) — RED.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest simulators/sim-textile/tests/test_downtime_generator.py -x 2&gt;&amp;1 | grep -qE "ImportError|cannot import name|failed" &amp;&amp; echo "RED phase confirmed"</automated>
  </verify>
  <done>~10-15 tests written; all fail on import — RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement DowntimeEvent + DowntimeEventGenerator (mirror quality_event_generator.py)</name>
  <files>
    simulators/sim-textile/src/sim_textile/downtime_event_generator.py,
    simulators/sim-textile/src/sim_textile/models.py
  </files>
  <read_first>
    simulators/sim-textile/tests/test_downtime_generator.py (just-written — green target),
    simulators/sim-textile/src/sim_textile/quality_event_generator.py (full Phase 6 06-09 module to mirror — 274 lines),
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 12 lines 504-520 — substitution list verbatim),
    packages/sft-domain/src/sft_domain/failure_modes/_loader.py (load_failure_modes signature)
  </read_first>
  <behavior>
    Module structure mirrors `quality_event_generator.py`:
    - Add `DowntimeEvent(BaseModel)` to `models.py` (or to `downtime_event_generator.py` if quality_event_generator co-locates QualityEvent there — discover convention by inspection). Frozen + extra=forbid + all D-DA-01 fields.
    - `_RATE_NOMINAL_PER_MIN = 2` and faulted rate `8` as module constants (lower than QualityEvent's 10 — downtime is rare).
    - `_FAMILY_REASON_BIAS: dict[str, list[str]]` — maps asset_family → ordered list of reason_codes drawn from failure_modes.yaml maintenance taxonomy. Populated at module load time via `load_failure_modes()` filtered to entries where `maintenance is not None`; group reason_codes by `asset_families` membership. Examples:
      * "loom"/"weaving" → ["WEAVING-BE-001", "WEAVING-MP-002", "WEAVING-SF-003"]
      * "spinning" → ["SPINNING-SL-001", "SPINNING-NP-002"]
      * "dyeing" → ["DYEING-SD-001", "DYEING-UD-002"]
    - `class DowntimeEventGenerator`: keyword-only `__init__(*, assets, nominal_rate=_RATE_NOMINAL_PER_MIN, faulted_rate=_RATE_FAULTED_PER_MIN, random_seed=None)`.
    - Per-asset `self._rng[asset.asset_id] = random.Random(asset.asset_id if random_seed is None else f"{random_seed}:{asset.asset_id}")` — deterministic per-asset stream.
    - `async tick(now: datetime) -> list[DowntimeEvent]`: for each asset, sample a Poisson-ish count given the asset's mode (nominal vs faulted, where faulted state comes from `production_state` similar to quality generator); for each event pick a reason_code from `_FAMILY_REASON_BIAS[family]`; compute duration_min from the matching `MaintenanceSpec.mttr_target_minutes` ± jitter; pick severity weighted (minor 75%, major 20%, critical 5%); build DowntimeEvent.
    - `async run(nc, stop_event, period_seconds=60)`: loop ticks every `period_seconds`, on each tick publishes each generated event via `await nc.publish(subject=f"maintenance.downtime.{event.asset_id}", payload=event.model_dump_json().encode())`. Handle `asyncio.CancelledError` per Phase 6 06-09 pattern L222-224.
    - All datetime uses `datetime.now(UTC)` tz-aware (Shared Pattern C).
    - For assets with no taxonomy entry for their family, skip + structlog WARN once per asset.

    All Task 3 tests turn green. No regression on existing quality_event_generator tests.
  </behavior>
  <action>
    Open `quality_event_generator.py` and mirror its structure file-by-file. Substitute the elements per 07-PATTERNS.md Section 12 lines 508-514. Read `load_failure_modes()` at module top (cached); precompute `_FAMILY_REASON_BIAS`. Use `pydantic.BaseModel` for DowntimeEvent.

    For the `production_state` integration (faulted vs nominal mode): mirror exactly how `quality_event_generator.py` consumes it. If it imports a singleton `production_state`, same pattern. If it accepts state via constructor, same pattern.

    Run Task 3 tests + existing test_quality_generator.py for regression check.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest simulators/sim-textile/tests/test_downtime_generator.py simulators/sim-textile/tests/test_quality_generator.py -x &amp;&amp; python -c "from sim_textile.downtime_event_generator import DowntimeEvent, DowntimeEventGenerator; print('OK')"</automated>
  </verify>
  <done>~10-15 tests green; existing quality generator tests still green; module importable; reason_code taxonomy correctly drawn from failure_modes.yaml maintenance subkey.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PG client → maintenance schema | Migration runs as superuser; PR-reviewed; CHECK constraints prevent invalid severity/duration |
| Simulator → NATS publish | Trusted publisher; payload Pydantic-validated; no untrusted input crosses boundary |
| failure_modes.yaml → reason_code taxonomy | Repo-committed YAML, validator-enforced uniqueness (07-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V7-yaml-injection | Tampering | failure_modes loader | mitigate | `yaml.safe_load` (inherited 07-02). |
| T-V7-cagg-refresh-overload | Denial of Service | continuous aggregate refresh on 30 asset × 5 sensor 1Hz | mitigate | refresh_policy 5min documented; if benchmark shows overload, document deviation + raise interval. |
| T-V7-event-injection | Tampering | NATS payload | mitigate | DowntimeEvent Pydantic frozen+extra=forbid; severity CHECK constraint at DB layer (double validation). |
| T-V7-naive-datetime | Tampering | DowntimeEvent.timestamp | mitigate | tz-aware validator; mirror Pattern S-6 from quality_event_generator. |
| T-V7-rng-pollution | Tampering | random.seed global | mitigate | Per-asset `random.Random(asset_id)` — no global random.seed call (mirror 06-09 L172). |
| T-V7-sim-publish-loss | Repudiation | sim NATS slow consumer | accept | Log-only on publish error (mirror 06-09 L213-219). Prometheus counter deferred Phase 11. |
| T-V7-SC | Tampering | N/A this plan (no package installs) | n/a | Migration + simulator extension only; no new pip deps. |
</threat_model>

<deviation_rules>
- **Rule 1:** D-DA-01/03 LOCKED — DowntimeEvent payload exact shape; NATS subject pattern `maintenance.downtime.<asset_id>`; migration 008 creates BOTH hypertable AND continuous aggregate (not just one). D-MNT-TAX LOCKED — reason_codes drawn from failure_modes.yaml maintenance subkey, NOT a parallel taxonomy file.
- **Rule 3:** Existing quality_event_generator tests + Phase 1-6 migrations MUST stay green.
- **Rule 4:** Capture in 07-05-SUMMARY.md: any deviation from continuous aggregate (e.g., fallback to plain matview), reason_code-to-family mapping table, statistical thresholds chosen, dev DB push timestamp.
</deviation_rules>

<verification>
- `pytest infra/migrations/timescale/tests/test_migration_008.py -m integration -x` green.
- `pytest simulators/sim-textile/tests/test_downtime_generator.py -x` green.
- `pytest simulators/sim-textile/tests/test_quality_generator.py -x` green (regression).
- Dev DB migration applied (Task 2 manual checkpoint).
- Subject naming: `maintenance.downtime.<asset_id>` matches D-DA-01 + Phase 3 NATS subject convention.
- Reason_codes correctly drawn from failure_modes.yaml maintenance taxonomy.
</verification>

<success_criteria>
- Persistence layer ready for DowntimeAnalyzer (07-09): hypertable + CAGG + indexes.
- Live event stream ready for E2E scenarios (07-12).
- MNT-04 data foundation complete; MNT-05 taxonomy actively consumed.
- No regression on Phase 6 simulator or migrations.
</success_criteria>

<commit_strategy>
- Commit 1: `feat(07-05): migration 008 downtime_events hypertable + oee_hourly CAGG (D-DA-01/03)` (SQL + test_migration_008.py)
- Commit 2 (after Task 2 checkpoint): no commit (manual dev DB push)
- Commit 3: `feat(07-05): DowntimeEvent Pydantic model (D-DA-01)` (models.py extension)
- Commit 4: `feat(07-05): sim-textile downtime_event_generator (mirror quality_event_generator)` (downtime_event_generator.py + test_downtime_generator.py)
</commit_strategy>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-05-SUMMARY.md` documenting: migration 008 DDL summary (hypertable + CAGG + indexes), CAGG refresh policy actual value applied, reason_code-to-asset_family mapping table extracted from failure_modes.yaml, rate constants chosen, dev DB push timestamp, any test threshold tuning.
</output>
</content>
</invoke>