---
phase: 07-agents-maintenance-reliability
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/agent.py
  - apps/agents/maintenance/maintenance-coach/tests/test_coach_saver_lifecycle.py
autonomous: true
gap_closure: true
requirements: [MNT-03, MNT-06]

must_haves:
  truths:
    - "MaintenanceCoach second and subsequent step() calls succeed against a long-lived saver (no ConnectionDoesNotExistError after the first call)"
    - "resume_after_help writes exactly one authoritative audit row per resume (not two)"
  artifacts:
    - path: apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/agent.py
      provides: "Saver injected for graph lifetime; no async-with self-compile; single audit row on resume"
    - path: apps/agents/maintenance/maintenance-coach/tests/test_coach_saver_lifecycle.py
      provides: "Regression test for CR-01 saver lifecycle + WR-02 single-audit-row"
  key_links:
    - from: "MaintenanceCoach._get_graph"
      to: "injected saver (lifespan app.state.checkpointer)"
      via: "graph compiled once with long-lived saver; _get_graph raises if None"
      pattern: "RuntimeError.*saver|_graph is None"
---

<objective>
Close the MaintenanceCoach (MNT-03) runtime defects that make the agent non-functional after the first request and double-write the append-only audit table.

- CR-01 (BLOCKER): `_get_graph()` opens `AsyncPostgresSaver.from_conn_string(pg_dsn)` in an `async with` block, caches the compiled graph in `self._graph`, then exits the context manager — closing the saver connection. Every subsequent `ainvoke`/`aget_state` raises `ConnectionDoesNotExistError`.
- WR-02: `resume_after_help` calls `step()` (which writes one AUTO audit row) then writes a second HITL_SUPERVISOR row → two rows per resume on an append-only table.

Purpose: Restore SC-3 — "MaintenanceCoach retrieves the correct step-by-step procedure ... tracks MTTR ... and escalates when the technician requests it" — at runtime.
Output: Fixed agent.py + a regression test that fails against current code and passes after the fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agents-maintenance-reliability/07-VERIFICATION.md
@.planning/phases/07-agents-maintenance-reliability/07-REVIEW.md

<interfaces>
<!-- The api-gateway lifespan already creates ONE long-lived AsyncPostgresSaver:
       saver = await get_postgres_checkpointer(dsn).__aenter__()
       app.state.checkpointer = saver
     and the supervisor graph is compiled with it:
       build_supervisor_graph(checkpointer=saver, router=router)
     The production coach path must reuse this saver (constructor injection),
     NOT self-compile a per-call saver. The injected-saver branch in
     MaintenanceCoach.__init__ (saver is not None) already compiles correctly. -->

MaintenanceCoach.__init__ accepts: pool, audit_writer, llm, rag_search_tool,
  request_help_tool, escalate_tool, saver (Optional). When saver is provided,
  self._graph = build_coach_graph(...).compile(checkpointer=saver). When None,
  self._graph = None and _get_graph() currently self-compiles (BROKEN path).

MaintenanceCoach.step(request) -> CoachResponse, internally calls
  self._write_audit(..., decision="auto").
MaintenanceCoach._write_audit(*, intervention_id, step_no, completed_step,
  decision, escalation_trigger=None) -> None.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing regression test for saver lifecycle (CR-01) and single-audit-row (WR-02)</name>
  <files>apps/agents/maintenance/maintenance-coach/tests/test_coach_saver_lifecycle.py</files>
  <read_first>
    - apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/agent.py (CR-01 at lines 419-445; WR-02 at lines 862-878; injected-saver branch at lines 401-409)
    - apps/agents/maintenance/maintenance-coach/tests/conftest.py (existing fixtures / mock conventions)
    - apps/agents/maintenance/maintenance-coach/tests/test_checkpoint_resume.py (existing resume-test patterns to mirror)
    - apps/agents/maintenance/maintenance-coach/pyproject.toml (markers: integration, testcontainers)
    - .planning/phases/07-agents-maintenance-reliability/07-REVIEW.md (CR-01 fix pattern lines 57-83; WR-02 fix pattern lines 195-213)
  </read_first>
  <action>
    Create test_coach_saver_lifecycle.py with two regression tests that FAIL against current code.

    Test A — `test_get_graph_requires_injected_saver_no_self_compile` (unit, no DB):
    Construct MaintenanceCoach with saver=None and PG_DSN unset/empty in env, then await `_get_graph()`. Assert it raises RuntimeError mentioning saver injection (the post-fix contract from 07-REVIEW.md lines 77-83). Against current code this path either self-compiles via async-with or raises a different RuntimeError only when PG_DSN missing — assert the explicit "inject a pre-built saver" message so the test pins the new contract.

    Test B — `test_resume_after_help_writes_single_audit_row` (unit): construct MaintenanceCoach with a mocked saver (so self._graph compiles) and an audit_writer mock whose `.write` is an AsyncMock recording calls. Drive `resume_after_help(intervention_id, supervisor_input)` such that the inner step() returns an in_progress CoachResponse. Assert `audit_writer.write` was awaited exactly ONCE. Against current code it is called twice (one from step() decision=auto, one from resume_after_help decision=hitl_supervisor) — test fails now, passes after Task 2.

    Test C — `test_step_persists_across_calls` marked `@pytest.mark.integration`: with a real AsyncPostgresSaver from testcontainers PG (mirror test_checkpoint_resume.py setup), inject the saver at construction, call start() then step() twice with the same intervention_id; assert no ConnectionDoesNotExistError / InterfaceError is raised and the second step returns a CoachResponse. This exercises the real CR-01 defect (skipped without docker, consistent with suite convention).

    Use existing conftest fixtures for mock llm/tools/audit_writer where present; do not introduce new global fixtures.
  </action>
  <verify>
    <automated>cd apps/agents/maintenance/maintenance-coach && uv run pytest tests/test_coach_saver_lifecycle.py -m "not integration" -x</automated>
  </verify>
  <acceptance_criteria>
    - test_coach_saver_lifecycle.py exists with tests A, B, and integration-marked C.
    - Before Task 2: Test A and Test B FAIL (assertion failures, not collection errors).
    - `grep -c "pytest.mark.integration" tests/test_coach_saver_lifecycle.py` returns at least 1.
  </acceptance_criteria>
  <done>Regression tests authored and demonstrably red against the current buggy agent.py.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-01 saver lifecycle and WR-02 double audit write in agent.py</name>
  <files>apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/agent.py</files>
  <read_first>
    - apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/agent.py (full file — modifying _get_graph lines 419-445, step lines 683-839, resume_after_help lines 841-879)
    - .planning/phases/07-agents-maintenance-reliability/07-REVIEW.md (CR-01 fix lines 57-83; WR-02 fix lines 195-213)
    - apps/api-gateway/src/svc_api_gateway/lifespan.py (lines 98-118, 147 — long-lived saver is app.state.checkpointer; this is the saver to inject)
  </read_first>
  <action>
    Fix CR-01: In `_get_graph()` (lines 419-445) delete the entire `async with AsyncPostgresSaver.from_conn_string(pg_dsn) as saver:` self-compile block. Replace the body so that when `self._graph is None`, it raises a clear RuntimeError instructing callers to inject a pre-built saver at construction or wire one via the FastAPI lifespan (per 07-REVIEW.md lines 77-83). Keep the `if self._graph is not None: return self._graph` fast path. Remove the now-dead `import os` usage for PG_DSN in this method and the inline AsyncPostgresSaver import if no longer referenced. The production coach must be constructed with the long-lived `app.state.checkpointer` saver (the existing injected-saver branch at lines 401-409 already compiles correctly — no change needed there).

    Fix WR-02: Add a `skip_audit: bool = False` keyword parameter to `step()` (line 683). When True, skip the internal `_write_audit(... decision="auto" ...)` calls inside step() (the calls at the completion path ~line 745 and the in-progress path ~line 789). In `resume_after_help` (lines 862-878), call `self.step(resume_request, skip_audit=True)` and keep exactly one authoritative `_write_audit(... decision="hitl_supervisor", escalation_trigger="technician_request")` call. Net effect: one audit row per resume.

    Preserve immutability conventions (return new dicts in nodes — already the case). Do not change the public CoachResponse shape.
  </action>
  <verify>
    <automated>cd apps/agents/maintenance/maintenance-coach && uv run pytest tests/test_coach_saver_lifecycle.py -m "not integration" tests/test_checkpoint_resume.py tests/test_evidence_panel.py tests/test_mttr.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests A and B from Task 1 now PASS.
    - `grep -c "async with AsyncPostgresSaver.from_conn_string" apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/agent.py` returns 0.
    - `step()` signature includes a `skip_audit` parameter (grep `skip_audit` in agent.py returns >= 2 occurrences: definition + call from resume_after_help).
    - Existing tests (test_checkpoint_resume, test_evidence_panel, test_mttr) still pass.
  </acceptance_criteria>
  <done>SC-3 restored: coach graph uses a long-lived injected saver; resume writes a single audit row.</done>
</task>

</tasks>

<verification>
- Unit subset of the new regression test passes: `cd apps/agents/maintenance/maintenance-coach && uv run pytest tests/test_coach_saver_lifecycle.py -m "not integration" -x`
- No self-compiled async-with saver remains in agent.py.
- Integration test (Test C) is present and runnable when docker/testcontainers are available.
</verification>

<success_criteria>
SC-3 (MaintenanceCoach) restored at runtime: subsequent step() calls do not raise connection errors, and resume_after_help produces exactly one audit row.
</success_criteria>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-13-SUMMARY.md` when done.
</output>
