---
phase: 07-agents-maintenance-reliability
plan: 02
plan_id: 07-02
type: tdd
wave: 1
depends_on: ["07-00"]
files_modified:
  - packages/sft-domain/src/sft_domain/failure_modes/models.py
  - packages/sft-domain/failure_modes.yaml
  - packages/sft-domain/tests/failure_modes/test_maintenance_meta.py
  - scripts/validate-failure-modes.py
autonomous: true
requirements: [MNT-05]
objective: "Extend `failure_modes.yaml` and the Pydantic loader with the maintenance taxonomy sub-key (`maintenance: {reason_code, mttr_target_minutes, intervention_steps_sop_id, preventive_check_interval_hours?}`) per D-MNT-TAX. Additive non-breaking change — existing Phase 6 entries continue to load. Extend CI validator script to enforce `reason_code` uniqueness + `intervention_steps_sop_id` reference resolution against Phase 5 SOP corpus."

must_haves:
  truths:
    - "All existing failure_modes.yaml entries (Phase 5 baseline + Phase 6 hitl_tier extension) continue to load via the extended Pydantic loader without breaking."
    - "New `MaintenanceSpec` Pydantic model is frozen + extra=forbid + validates reason_code regex `^[A-Z][A-Z0-9-]+$` + mttr_target_minutes [0..10080] + intervention_steps_sop_id regex `^SOP-[A-Z0-9-]+$`."
    - "`maintenance:` sub-key is OPTIONAL on FailureMode (default=None) — no breaking change."
    - "Every textile defect entry in failure_modes.yaml (broken_end, mispick, slub, neppy, selvage_fault, shade_deviation, unlevel_dyeing) gains a `maintenance:` sub-key with reason_code + mttr_target_minutes + intervention_steps_sop_id populated."
    - "CI validator `scripts/validate-failure-modes.py` enforces `reason_code` cross-entry uniqueness."
    - "CI validator enforces every `intervention_steps_sop_id` value resolves to an existing SOP document id in Phase 5 corpus (path: confirm via existing knowledge layer loader; if no corpus index file, walk packages/sft-knowledge SOP fixtures)."
  artifacts:
    - path: packages/sft-domain/src/sft_domain/failure_modes/models.py
      provides: "MaintenanceSpec Pydantic + FailureMode.maintenance optional field"
      exports: ["MaintenanceSpec", "FailureMode"]
    - path: packages/sft-domain/failure_modes.yaml
      provides: "Extended YAML with maintenance: subkey on 7 textile defect entries"
      contains: "maintenance:|reason_code:"
    - path: scripts/validate-failure-modes.py
      provides: "CI validator extended with reason_code uniqueness + SOP id resolution"
  key_links:
    - from: packages/sft-domain/failure_modes.yaml
      to: packages/sft-domain/src/sft_domain/failure_modes/models.py
      via: "Pydantic FailureMode.model_validate enforces MaintenanceSpec schema"
      pattern: "MaintenanceSpec|maintenance:"
    - from: scripts/validate-failure-modes.py
      to: packages/sft-knowledge SOP corpus (path verified in implementation)
      via: "validator walks corpus + cross-refs intervention_steps_sop_id"
      pattern: "intervention_steps_sop_id|SOP-"
---

<objective>
Ship the maintenance event taxonomy extension to the existing `failure_modes.yaml` (D-MNT-TAX). Phase 7 deliberately avoids creating a parallel `maintenance_taxonomy.yaml` — failure_modes.yaml remains the single source of truth and gains an additive (optional) sub-key. Downstream Phase 7 agents (predictive-maintenance, downtime-analyzer simulator, RCASpecialist) all consume `MaintenanceSpec` via the loader; missing or invalid taxonomy data fails fast at YAML load.

Purpose: Satisfy MNT-05 (taxonomy documented + used cross-agent) + lay the data foundation for 07-05 (downtime_event_generator reads `reason_code` from this taxonomy) and 07-09 (DowntimeAnalyzer Pareto groups by `reason_code`).
Output: 1 Pydantic model addition + 1 YAML extension (additive) + 1 test module (TDD) + 1 validator script extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md
@.planning/phases/07-agents-maintenance-reliability/07-RESEARCH.md
@.planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md
@.planning/phases/06-agents-operations-production/06-04-PLAN.md
@packages/sft-domain/src/sft_domain/failure_modes/models.py
@packages/sft-domain/src/sft_domain/failure_modes/_loader.py
@packages/sft-domain/failure_modes.yaml
@packages/sft-domain/tests/test_failure_modes_loader.py
@packages/sft-domain/tests/test_failure_modes_hitl_tier.py
@scripts/validate-failure-modes.py

<interfaces>
From `packages/sft-domain/src/sft_domain/failure_modes/models.py` (current state per 07-PATTERNS.md Section 17):
```python
class FailureMode(BaseModel):
    model_config = {"frozen": True, "extra": "forbid"}
    id: Annotated[str, Field(min_length=1, pattern=r"^[a-z][a-z0-9_]*$")]
    name_it: Annotated[str, Field(min_length=1)]
    name_en: str
    asset_families: list[str]
    parts: list[str]
    severity: Literal["low","medium","high"]
    # Phase 6 extension:
    hitl_tier: Literal["auto-log","supervisor","manager+safety"] = "supervisor"
    setup_minutes: int = 0
    severity_band: dict[str, Any] = {}
```

From `packages/sft-domain/failure_modes.yaml` (current shape per 07-PATTERNS.md L645-663):
```yaml
- id: broken_end
  name_it: rottura filo ordito
  name_en: broken end
  asset_families: [weaving]
  # ... existing fields ...
```

Pattern target per 07-PATTERNS.md Section 16 + Section 17 (verbatim):
```python
class MaintenanceSpec(BaseModel):
    model_config = {"frozen": True, "extra": "forbid"}
    reason_code: Annotated[str, Field(min_length=1, pattern=r"^[A-Z][A-Z0-9-]+$")]
    mttr_target_minutes: Annotated[int, Field(ge=0, le=10080)]
    intervention_steps_sop_id: Annotated[str, Field(min_length=1, pattern=r"^SOP-[A-Z0-9-]+$")]
    preventive_check_interval_hours: Annotated[int | None, Field(default=None, ge=1)] = None

class FailureMode(BaseModel):
    # ... existing fields ...
    maintenance: Annotated[
        MaintenanceSpec | None,
        Field(default=None, description="Maintenance metadata (D-MNT-TAX Phase 7, optional)"),
    ] = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for MaintenanceSpec Pydantic + failure_modes.yaml maintenance subkey loading</name>
  <files>
    packages/sft-domain/tests/failure_modes/test_maintenance_meta.py
  </files>
  <read_first>
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 16 + 17 lines 637-702 — exact schema + risk callouts),
    .planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md (D-MNT-TAX lines 140-152),
    packages/sft-domain/tests/test_failure_modes_hitl_tier.py (Phase 6 analog test style — pytest patterns),
    packages/sft-domain/tests/test_failure_modes_loader.py (loader test fixture analog)
  </read_first>
  <behavior>
    test_maintenance_meta.py covers:
    - `MaintenanceSpec` instantiable with valid fields: reason_code="WEAVING-BE-001", mttr_target_minutes=30, intervention_steps_sop_id="SOP-LOOM-001", preventive_check_interval_hours=168 → returns frozen model.
    - `MaintenanceSpec` frozen: mutation raises FrozenInstanceError (or pydantic equivalent).
    - `MaintenanceSpec` extra=forbid: passing extra field raises ValidationError.
    - reason_code regex: "WEAVING-BE-001" valid; "weaving-be-001" lowercase → ValidationError; "" empty → ValidationError; "123-FOO" leading digit → ValidationError; "X" minimum valid (single uppercase letter).
    - mttr_target_minutes bounds: 0 valid; 10080 valid (1 week); -1 ValidationError; 10081 ValidationError.
    - intervention_steps_sop_id regex: "SOP-LOOM-001" valid; "SOP-" too short → ValidationError; "sop-LOOM-001" lowercase prefix → ValidationError; "LOOM-001" no SOP- prefix → ValidationError.
    - preventive_check_interval_hours: None default; 1 valid; 0 ValidationError; 24*365*10 valid.
    - `FailureMode.maintenance` field: defaults to None; can be set to MaintenanceSpec instance; can be set to None explicitly.
    - YAML round-trip: `load_failure_modes()` loads an entry with `maintenance:` sub-key → FailureMode.maintenance is a populated MaintenanceSpec; loads an entry WITHOUT `maintenance:` sub-key → FailureMode.maintenance is None (backward-compat).
    - `load_failure_modes()` returns at least 7 entries (the 7 textile defect entries), each with `maintenance is not None` and reason_code populated.
    - Cross-entry uniqueness check: collect all `failure_modes` reason_codes, assert len == len(set).
  </behavior>
  <action>
    Delete the Wave 0 placeholder `test_placeholder`. Write all tests described above. Use `pytest.raises(ValidationError)` for negative cases. For YAML round-trip tests, use the existing `load_failure_modes()` loader (re-importing it after package modification). Tests should currently FAIL with: (a) `ImportError: cannot import name 'MaintenanceSpec' from 'sft_domain.failure_modes.models'`, (b) loader assertions failing because the YAML hasn't been extended yet.

    Helpful imports:
    ```python
    from sft_domain.failure_modes.models import FailureMode, MaintenanceSpec
    from sft_domain.failure_modes._loader import load_failure_modes
    from pydantic import ValidationError
    ```
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest packages/sft-domain/tests/failure_modes/test_maintenance_meta.py -x 2&gt;&amp;1 | grep -qE "ModuleNotFoundError|ImportError|failed|cannot import name" &amp;&amp; echo "RED phase confirmed"</automated>
  </verify>
  <done>Tests written covering all invariants from D-MNT-TAX + 07-PATTERNS.md Section 16-17; tests fail at import/collection time — RED phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MaintenanceSpec Pydantic + extend FailureMode + extend failure_modes.yaml with 7 textile defect maintenance subkeys</name>
  <files>
    packages/sft-domain/src/sft_domain/failure_modes/models.py,
    packages/sft-domain/failure_modes.yaml
  </files>
  <read_first>
    packages/sft-domain/tests/failure_modes/test_maintenance_meta.py (just-written tests — green target),
    packages/sft-domain/src/sft_domain/failure_modes/models.py (current Phase 6 state — preserve all existing fields),
    packages/sft-domain/failure_modes.yaml (current Phase 5+6 state — preserve all existing entries),
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 17 verbatim model + Section 16 verbatim YAML extension example)
  </read_first>
  <behavior>
    All tests from Task 1 turn green. Specifically:
    - `models.py`: add `MaintenanceSpec(BaseModel)` exactly per 07-PATTERNS.md Section 17 snippet. Add `maintenance: Annotated[MaintenanceSpec | None, Field(default=None, description="Maintenance metadata (D-MNT-TAX Phase 7, optional)")] = None` to the existing `FailureMode` class without breaking any other field. Backward-compat: `default=None` is mandatory (Pattern G non-breaking — see 07-PATTERNS.md Shared Pattern G).
    - `failure_modes.yaml`: extend the 7 textile defect entries (broken_end, mispick, slub, neppy, selvage_fault, shade_deviation, unlevel_dyeing) with `maintenance:` sub-key. Reason_code naming convention follows ISO 14224 spirit: `<MODULE>-<DEFECT_ABBR>-<NNN>`. Suggested values (per CONTEXT.md examples + RESEARCH.md):
      - broken_end → reason_code: `WEAVING-BE-001`, mttr_target_minutes: 30, intervention_steps_sop_id: `SOP-LOOM-001`, preventive_check_interval_hours: 168
      - mispick → reason_code: `WEAVING-MP-002`, mttr_target_minutes: 15, intervention_steps_sop_id: `SOP-LOOM-002`
      - slub → reason_code: `SPINNING-SL-001`, mttr_target_minutes: 20, intervention_steps_sop_id: `SOP-SPIN-001`
      - neppy → reason_code: `SPINNING-NP-002`, mttr_target_minutes: 25, intervention_steps_sop_id: `SOP-SPIN-002`
      - selvage_fault → reason_code: `WEAVING-SF-003`, mttr_target_minutes: 45, intervention_steps_sop_id: `SOP-LOOM-003`, preventive_check_interval_hours: 336
      - shade_deviation → reason_code: `DYEING-SD-001`, mttr_target_minutes: 60, intervention_steps_sop_id: `SOP-DYE-001`
      - unlevel_dyeing → reason_code: `DYEING-UD-002`, mttr_target_minutes: 90, intervention_steps_sop_id: `SOP-DYE-002`, preventive_check_interval_hours: 720
    - reason_codes are unique across all entries.
    - Non-textile entries (if any) without `maintenance:` sub-key continue to load (maintenance=None).
    - Existing tests in `test_failure_modes_loader.py` + `test_failure_modes_hitl_tier.py` continue to pass.
  </behavior>
  <action>
    Implement `MaintenanceSpec` per 07-PATTERNS.md Section 17 verbatim snippet. Add the `maintenance` field to `FailureMode`. Update class docstring with reference to D-MNT-TAX. Do NOT touch any existing field.

    For `failure_modes.yaml`: read existing file; for each of the 7 textile defect entries add the `maintenance:` block per the table above. Preserve all existing fields exactly (name_it, name_en, asset_families, parts, severity, hitl_tier, setup_minutes, severity_band). Run all 3 test modules (Task 1 new + existing loader + existing hitl_tier) — all must be green.

    IMPORTANT: `intervention_steps_sop_id` values (SOP-LOOM-001, SOP-LOOM-002, SOP-LOOM-003, SOP-SPIN-001, SOP-SPIN-002, SOP-DYE-001, SOP-DYE-002) must exist in the Phase 5 SOP corpus. If they do NOT exist in the corpus today, document this in 07-02-SUMMARY.md as a known follow-up — the Task 4 validator will fail until either (a) the SOP corpus is seeded with these stub documents, or (b) the validator is given an explicit allowlist for missing-but-planned SOPs. Decision recorded here: emit the SOP ids as planned values; if 07-VALIDATION Task 4 fails, capture the failing ids and surface to user before continuing.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest packages/sft-domain/tests/failure_modes/test_maintenance_meta.py packages/sft-domain/tests/test_failure_modes_loader.py packages/sft-domain/tests/test_failure_modes_hitl_tier.py -x &amp;&amp; python -c "from sft_domain.failure_modes.models import FailureMode, MaintenanceSpec; from sft_domain.failure_modes._loader import load_failure_modes; modes = load_failure_modes(); textile_ids = {'broken_end','mispick','slub','neppy','selvage_fault','shade_deviation','unlevel_dyeing'}; mnt_entries = [m for m in modes if m.id in textile_ids]; assert len(mnt_entries) == 7 and all(m.maintenance is not None for m in mnt_entries); codes = [m.maintenance.reason_code for m in mnt_entries]; assert len(codes) == len(set(codes)); print('OK 7 textile entries with unique reason_codes')"</automated>
  </verify>
  <done>All Phase 7 + Phase 6 + Phase 5 failure_modes tests green; failure_modes.yaml extended with all 7 textile defect maintenance subkeys; `MaintenanceSpec` importable; loader returns populated maintenance metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Extend CI validator script with reason_code uniqueness + SOP id resolution</name>
  <files>
    scripts/validate-failure-modes.py
  </files>
  <read_first>
    scripts/validate-failure-modes.py (current Phase 5 validator script — preserve all existing checks),
    packages/sft-domain/src/sft_domain/failure_modes/_loader.py (loader pattern for asset reading),
    packages/sft-knowledge/ (locate the SOP corpus — typically packages/sft-knowledge/data/sop/*.md or similar; if no canonical path exists, derive from Phase 5 D-65 registry; document discovery in script comment)
  </read_first>
  <behavior>
    - Existing validator checks unchanged (e.g., asset_family cross-ref to sft-assets).
    - NEW check: `reason_code` cross-entry uniqueness. Walk every FailureMode that has maintenance is not None; collect reason_code values; assert set(codes) length equals list length.
    - NEW check: every `intervention_steps_sop_id` resolves to an existing SOP document. Discovery strategy (in order of preference):
      1. If `packages/sft-knowledge/src/sft_knowledge/registry/sop_index.yaml` (or analogous) exists, parse and use as authoritative.
      2. Else walk `packages/sft-knowledge/data/sop/` (or wherever SOPs are stored — grep for `SOP-LOOM-001` to discover the convention used).
      3. If neither exists yet, the validator MUST log a structured WARN ("sop_corpus_not_found, skipping intervention_steps_sop_id resolution") and exit 0 to avoid blocking PRs — but emit a `# TODO 07-02` comment in the validator so the check becomes enforceable once Phase 5 SOP corpus path is stable. (Decision: prefer warn-over-fail until corpus path confirmed; this is the safest non-breaking behavior consistent with Rule 3.)
    - Validator exits 0 on success, non-zero on hard failures (reason_code dup is hard fail; SOP resolution gap is WARN per fallback).
    - Validator is idempotent — multiple runs against same YAML yield same exit code.
  </behavior>
  <action>
    Extend `scripts/validate-failure-modes.py`. Use the project's existing structlog + argparse conventions. Add two new validation functions: `_check_reason_code_uniqueness(modes) -> list[str]` (returns error messages, empty list = OK) and `_check_sop_id_resolution(modes, corpus_path: Path | None) -> list[str]`. Wire into the main `validate()` entrypoint. If `corpus_path is None`, log WARN and skip (per behavior above). Add `--strict-sop` CLI flag to override the warn-only behavior and fail-on-missing-SOP (default off for PR mergeability today; can be flipped on after Phase 5 corpus stabilization).

    Add a simple smoke test inline (or in `tests/`) — if there's a `scripts/validate_failure_modes_test.py` or similar (referenced in 07-VALIDATION.md task 07-TAX-02 row), extend that. Otherwise add one mini test inside the script (`if __name__ == "__main__"` block run on the actual YAML) that asserts exit 0.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; python scripts/validate-failure-modes.py &amp;&amp; echo "validator OK (warn-only mode)" &amp;&amp; python -c "from sft_domain.failure_modes._loader import load_failure_modes; modes = [m for m in load_failure_modes() if m.maintenance is not None]; codes = [m.maintenance.reason_code for m in modes]; assert len(codes) == len(set(codes)), f'duplicate reason_codes: {codes}'; print(f'OK {len(codes)} unique reason_codes')"</automated>
  </verify>
  <done>Validator runs to completion against extended YAML; reason_code uniqueness enforced as hard fail; SOP id resolution implemented (warn-only fallback if corpus path not found); `--strict-sop` flag available for future enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| failure_modes.yaml (in repo) → loader | Read at startup; yaml.safe_load only; PR-reviewed |
| caller (agents) → MaintenanceSpec | All consumers pass through Pydantic validation with extra=forbid (frozen) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V7-yaml-injection | Tampering | failure_modes.yaml loader | mitigate | `yaml.safe_load` only (Phase 1+ T-V5-yaml convention); never `yaml.load`. |
| T-V7-tax-drift | Tampering | reason_code uniqueness | mitigate | CI validator enforces uniqueness; PR cannot merge with duplicate codes. |
| T-V7-tax-orphan-sop | Tampering | intervention_steps_sop_id orphan refs | mitigate | CI validator warns; `--strict-sop` flag escalates to hard fail post-Phase 5 stabilization. |
| T-V7-naive-extra | Tampering | MaintenanceSpec extra fields | mitigate | `model_config = {"frozen": True, "extra": "forbid"}` rejects any unknown field. |
| T-V7-SC | Tampering | N/A this plan (no package installs) | n/a | YAML + Pydantic only. |
</threat_model>

<deviation_rules>
- **Rule 1:** D-MNT-TAX is LOCKED. `maintenance:` MUST be additive sub-key on existing `failure_modes.yaml` — no new separate YAML file allowed.
- **Rule 3:** Existing tests (`test_failure_modes_loader.py` + `test_failure_modes_hitl_tier.py`) MUST stay green; do NOT weaken them to accommodate new schema.
- **Rule 4:** If SOP ids referenced in maintenance.intervention_steps_sop_id are missing from Phase 5 corpus, document in 07-02-SUMMARY.md and emit validator WARN; do NOT silently drop ids or weaken the SOP-ID regex.
</deviation_rules>

<verification>
- `nx run sft-domain:test` green (all failure_modes tests pass).
- `python -c "from sft_domain.failure_modes.models import MaintenanceSpec, FailureMode; from sft_domain.failure_modes._loader import load_failure_modes; print('OK')"` succeeds.
- failure_modes.yaml loads via `yaml.safe_load`.
- `python scripts/validate-failure-modes.py` exits 0.
- 7 textile defect entries each have populated `maintenance:` sub-key with unique reason_code.
- No existing test regressions in `packages/sft-domain/tests/`.
</verification>

<success_criteria>
- MaintenanceSpec model committed with regex-validated reason_code + SOP id + mttr bounds.
- 7 textile defects in failure_modes.yaml extended with maintenance metadata.
- CI validator gains 2 new checks (reason_code uniqueness hard fail; SOP id resolution warn/fail-flag).
- MNT-05 taxonomy data foundation ready for 07-05 (downtime_event_generator), 07-09 (DowntimeAnalyzer Pareto), 07-11 (docs site).
- Backward compatibility: Phase 5 + Phase 6 existing tests + loaders + agents unchanged.
</success_criteria>

<commit_strategy>
- Commit 1: `feat(07-02): MaintenanceSpec Pydantic + FailureMode.maintenance optional field (D-MNT-TAX)` (models.py + test_maintenance_meta.py)
- Commit 2: `feat(07-02): extend failure_modes.yaml with maintenance taxonomy for 7 textile defects` (failure_modes.yaml)
- Commit 3: `feat(07-02): CI validator — reason_code uniqueness + SOP id resolution` (scripts/validate-failure-modes.py)
</commit_strategy>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-02-SUMMARY.md` documenting: MaintenanceSpec contract, 7 reason_codes assigned (table), SOP ids referenced + corpus resolution status, any orphan SOP ids requiring Phase 5 follow-up, validator behavior (warn-only vs strict mode).
</output>
</content>
</invoke>