---
phase: 07-agents-maintenance-reliability
plan: 01
plan_id: 07-01
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - infra/migrations/timescale/009_extend_audit_mnt.sql
  - infra/migrations/timescale/tests/test_migration_009.py
  - packages/sft-agents/src/sft_agents/models/enums.py
  - packages/sft-agents/tests/test_audit_constraints.py
autonomous: false
requirements: [MNT-01, MNT-02, MNT-03, MNT-04]
objective: "DB migration 009 extends `audit.actions.action_type` CHECK constraint to include 5 new Phase 7 ActionTypes (RUL_ESTIMATE, RCA_CHAIN, COACH_STEP, DOWNTIME_VERDICT, OEE_REPORT). Migration is BLOCKING — must be pushed before any Phase 7 agent code writes new audit rows. Extends `models/enums.py` ActionType enum in lockstep. Decision enum is NOT extended (existing values sufficient per D-AE-MNT)."

must_haves:
  truths:
    - "audit.actions table accepts INSERT with action_type IN ('RUL_ESTIMATE','RCA_CHAIN','COACH_STEP','DOWNTIME_VERDICT','OEE_REPORT') after migration applied."
    - "Existing Phase 1-6 action_type values still INSERT successfully (no regression)."
    - "Python `ActionType.RUL_ESTIMATE`, `ActionType.RCA_CHAIN`, `ActionType.COACH_STEP`, `ActionType.DOWNTIME_VERDICT`, `ActionType.OEE_REPORT` are importable."
    - "Migration is idempotent — running twice does not error."
    - "Decision enum unchanged (no new values needed per D-AE-MNT)."
  artifacts:
    - path: infra/migrations/timescale/009_extend_audit_mnt.sql
      provides: "ALTER TABLE audit.actions DROP CONSTRAINT + ADD CONSTRAINT (idempotent) for action_type"
      contains: "ALTER TABLE audit.actions"
    - path: packages/sft-agents/src/sft_agents/models/enums.py
      provides: "ActionType enum extended with 5 new Phase 7 values"
      contains: "RUL_ESTIMATE|RCA_CHAIN|COACH_STEP|DOWNTIME_VERDICT|OEE_REPORT"
  key_links:
    - from: packages/sft-agents/src/sft_agents/models/enums.py
      to: infra/migrations/timescale/009_extend_audit_mnt.sql
      via: "string values match CHECK constraint values exactly"
      pattern: "RUL_ESTIMATE|RCA_CHAIN|COACH_STEP|DOWNTIME_VERDICT|OEE_REPORT"
---

<objective>
Extend the `audit.actions` table's `action_type` CHECK constraint to admit Phase 7's 5 new enum values, and extend the corresponding Python `ActionType` enum in lockstep. This plan is the gating prerequisite for Plans 07-04 (`build_maintenance_subgraph` + `request_help` tool), 07-06 (PredictiveMaintenance writes RUL_ESTIMATE), 07-07 (RCASpecialist writes RCA_CHAIN), 07-08 (MaintenanceCoach writes COACH_STEP), 07-09 (DowntimeAnalyzer writes DOWNTIME_VERDICT + OEE_REPORT).

Purpose: Without this migration, every Phase 7 audit INSERT will fail PG CHECK constraint at runtime. Mirror exact Phase 6 06-01 pattern (proven).
Output: 1 SQL migration + 1 testcontainers test + 1 enum module extension + 1 enum round-trip test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md
@.planning/phases/07-agents-maintenance-reliability/07-RESEARCH.md
@.planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md
@.planning/phases/06-agents-operations-production/06-01-PLAN.md
@.planning/phases/06-agents-operations-production/06-01-SUMMARY.md
@infra/migrations/timescale/003_create_audit_actions.sql
@infra/migrations/timescale/007_extend_audit_decisions.sql
@infra/migrations/timescale/tests/test_migration_007.py
@infra/migrations/timescale/migrate.py
@packages/sft-agents/src/sft_agents/models/enums.py
@packages/sft-agents/tests/test_audit_constraints.py

<interfaces>
From `infra/migrations/timescale/007_extend_audit_decisions.sql` (Phase 6 reference — DROP+ADD CHECK pattern):
```sql
ALTER TABLE audit.actions
  DROP CONSTRAINT IF EXISTS audit_actions_action_type_chk;

ALTER TABLE audit.actions
  ADD CONSTRAINT audit_actions_action_type_chk CHECK (
    action_type IN (
      -- Phases 1-5 baseline
      'WRITE_PLC_SETPOINT','ACTUATOR_COMMAND','FIRMWARE_DEPLOY',
      'NETWORK_ACL_CHANGE','GRAPH_RECURSION_REVIEW','GOVERNOR_ALERT',
      -- Phase 6 extensions
      'ESCALATION_REQUEST','QUALITY_VERDICT','SCHEDULE_DRAFT','ANOMALY_ALERT'
    )
  );
```

From `packages/sft-agents/src/sft_agents/models/enums.py` (Phase 6 state — verified):
```python
class ActionType(str, Enum):
    # Phases 1-5 baseline
    WRITE_PLC_SETPOINT = "WRITE_PLC_SETPOINT"
    ACTUATOR_COMMAND = "ACTUATOR_COMMAND"
    FIRMWARE_DEPLOY = "FIRMWARE_DEPLOY"
    NETWORK_ACL_CHANGE = "NETWORK_ACL_CHANGE"
    GRAPH_RECURSION_REVIEW = "GRAPH_RECURSION_REVIEW"
    GOVERNOR_ALERT = "GOVERNOR_ALERT"
    # Phase 6 extensions — keep in lockstep with migration 007.
    ESCALATION_REQUEST = "ESCALATION_REQUEST"
    QUALITY_VERDICT = "QUALITY_VERDICT"
    SCHEDULE_DRAFT = "SCHEDULE_DRAFT"
    ANOMALY_ALERT = "ANOMALY_ALERT"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write idempotent migration 009 that extends audit.actions.action_type CHECK constraint with 5 Phase 7 values + testcontainers test</name>
  <files>
    infra/migrations/timescale/009_extend_audit_mnt.sql,
    infra/migrations/timescale/tests/test_migration_009.py
  </files>
  <read_first>
    infra/migrations/timescale/007_extend_audit_decisions.sql (exact pattern to copy 1:1),
    infra/migrations/timescale/tests/test_migration_007.py (test matrix template — 6 test pattern, lines 1-120),
    infra/migrations/timescale/migrate.py (idempotent runner: applied-migrations meta table),
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 10 lines 426-466 — exact migration 009 SQL skeleton; Section 11 lines 469-500 — test matrix template),
    .planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md (D-AE-MNT lines 154-160)
  </read_first>
  <behavior>
    - Migration runs once: drops existing `audit_actions_action_type_chk` CHECK constraint and creates a new one including all Phase 1-5 + Phase 6 + 5 new Phase 7 values (`RUL_ESTIMATE`,`RCA_CHAIN`,`COACH_STEP`,`DOWNTIME_VERDICT`,`OEE_REPORT`).
    - Migration runs twice (idempotent): second run is a no-op via `DROP CONSTRAINT IF EXISTS` + migrate.py applied-migrations tracking (existing pattern).
    - After migration: `INSERT INTO audit.actions (...) VALUES (..., 'RUL_ESTIMATE', ..., 'auto', ...)` succeeds; same for the 4 other new values.
    - Decision CHECK constraint unchanged (NO modification — D-AE-MNT explicit).
    - Test verifies the 6-test matrix from 07-PATTERNS.md Section 11:
      1. pre-migration INSERT 'RUL_ESTIMATE' raises CheckViolationError
      2. post-migration INSERT 'RUL_ESTIMATE' succeeds
      3. post-migration ALL 5 new Phase 7 action_types insert successfully
      4. post-migration legacy Phase 1-5 + Phase 6 action_types still insert successfully (regression)
      5. post-migration Decision enum unchanged (sanity check — no new Decision values)
      6. idempotent double-apply: re-running 009 is a no-op
  </behavior>
  <action>
    Copy `infra/migrations/timescale/007_extend_audit_decisions.sql` as the structural template. Author `009_extend_audit_mnt.sql` per 07-PATTERNS.md Section 10 SQL (lines 432-462). SQL comment header: "Migration 009: extend audit.actions.action_type CHECK constraint for Phase 7 (D-AE-MNT). Idempotent: safe to re-run. Phase 7 ActionType labels (RUL_ESTIMATE, RCA_CHAIN, COACH_STEP, DOWNTIME_VERDICT, OEE_REPORT). Decision enum NOT extended (existing values sufficient). Sync packages/sft-agents/src/sft_agents/models/enums.py ActionType in lockstep."

    Author `test_migration_009.py` mirroring `test_migration_007.py` setup (lines 26-46 + lines 108-130 fixture pattern). Use `_REPO_ROOT.parent` chain → `from infra.migrations.timescale.migrate import migrate`. `_MIGRATION_009 = Path(__file__).parent.parent / "009_extend_audit_mnt.sql"`. Reuse `fresh_dsn` testcontainers fixture (function-scoped) + `_run_baseline_migrations(dsn)` helper to apply 001..007 before 009. Implement all 6 tests from 07-PATTERNS.md Section 11 lines 484-490.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest infra/migrations/timescale/tests/test_migration_009.py -m integration -x</automated>
  </verify>
  <done>Migration file applied twice without error against a fresh testcontainers PG; new action_type values insert successfully; old constraint values still work; SQL file checked into repo; all 6 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `models/enums.py` ActionType with 5 new Phase 7 values + extend round-trip test</name>
  <files>
    packages/sft-agents/src/sft_agents/models/enums.py,
    packages/sft-agents/tests/test_audit_constraints.py
  </files>
  <read_first>
    packages/sft-agents/src/sft_agents/models/enums.py (current enum definitions — verify Phase 6 block exists at L67-99),
    packages/sft-agents/tests/test_audit_constraints.py (existing test pattern from Phase 6 06-01),
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 3 lines 148-174 — exact enum extension snippet)
  </read_first>
  <behavior>
    - `from sft_agents.models.enums import ActionType` succeeds.
    - `ActionType.RUL_ESTIMATE.value == "RUL_ESTIMATE"`, `ActionType.RCA_CHAIN.value == "RCA_CHAIN"`, `ActionType.COACH_STEP.value == "COACH_STEP"`, `ActionType.DOWNTIME_VERDICT.value == "DOWNTIME_VERDICT"`, `ActionType.OEE_REPORT.value == "OEE_REPORT"`.
    - All existing Phase 1-6 ActionType values unchanged; backward-compat — no rename.
    - Decision enum unchanged (no edits — sanity assertion in test).
    - `test_audit_constraints.py` extended: parametrize list of valid-action-type round-trip includes the 5 new Phase 7 values and asserts they map round-trip enum.value → PG insert → SELECT → enum lookup successfully against testcontainers PG with migrations 001..009 applied.
  </behavior>
  <action>
    Edit `packages/sft-agents/src/sft_agents/models/enums.py` to append 5 new members to `ActionType`:
    - `RUL_ESTIMATE = "RUL_ESTIMATE"  # D-PM-04: predictive-maintenance audit row`
    - `RCA_CHAIN = "RCA_CHAIN"  # D-RCA-02: rca-specialist 5-Why chain audit`
    - `COACH_STEP = "COACH_STEP"  # D-MC-02: maintenance-coach step audit`
    - `DOWNTIME_VERDICT = "DOWNTIME_VERDICT"  # D-DA-01: downtime-analyzer event audit`
    - `OEE_REPORT = "OEE_REPORT"  # D-DA-03: downtime-analyzer OEE report audit`

    Add docstring block before the additions: `# Phase 7 additions — keep in lockstep with migration 009.` (mirror Phase 6 block style at L73-86 per 07-PATTERNS.md Section 3).

    Extend `test_audit_constraints.py`: include 5 new Phase 7 values in any parametrize list of valid-action-type round-trip tests. If existing test doesn't parametrize over action_type, add `@pytest.mark.parametrize("action_type_value", ["RUL_ESTIMATE","RCA_CHAIN","COACH_STEP","DOWNTIME_VERDICT","OEE_REPORT"])` test asserting INSERT into testcontainers PG (with all migrations applied including 009) succeeds. Test must apply migration 009 in setup (extend any `_run_baseline_migrations` helper or apply 009 explicitly in fixture). Also add assertion that `Decision` enum has exactly the same values as before this plan (sanity check that Decision was NOT modified).
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest packages/sft-agents/tests/test_audit_constraints.py -x &amp;&amp; python -c "from sft_agents.models.enums import ActionType, Decision; assert ActionType.RUL_ESTIMATE.value == 'RUL_ESTIMATE' and ActionType.RCA_CHAIN.value == 'RCA_CHAIN' and ActionType.COACH_STEP.value == 'COACH_STEP' and ActionType.DOWNTIME_VERDICT.value == 'DOWNTIME_VERDICT' and ActionType.OEE_REPORT.value == 'OEE_REPORT'; assert 'AUTO' in Decision.__members__ and 'HITL_SUPERVISOR' in Decision.__members__; print('OK')"</automated>
  </verify>
  <done>All 5 new ActionType values importable; existing ActionType + Decision values intact; round-trip enum→PG→enum test green for all 5 new values.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: BLOCKING — Push migration 009 to dev TimescaleDB</name>
  <files>
    infra/migrations/timescale/009_extend_audit_mnt.sql
  </files>
  <what-built>Migration 009 SQL file is in repo; Python ActionType enum updated; testcontainers tests green.</what-built>
  <how-to-verify>
Run the existing migration runner against the dev Postgres+TimescaleDB instance (`make migrate` or `python infra/migrations/timescale/migrate.py up`). Then verify:
```bash
psql "$TIMESCALE_DSN" -c "SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'audit.actions'::regclass AND conname LIKE '%action_type%';"
```
The output must contain `'RUL_ESTIMATE'`, `'RCA_CHAIN'`, `'COACH_STEP'`, `'DOWNTIME_VERDICT'`, `'OEE_REPORT'` as quoted IN list values.

And smoke-test INSERT (must NOT raise CheckViolationError):
```bash
psql "$TIMESCALE_DSN" -c "BEGIN; INSERT INTO audit.actions (action_id, action_type, agent_id, thread_id, decision, ts) VALUES (gen_random_uuid(), 'RUL_ESTIMATE', 'predictive-maintenance', 'test-thread', 'auto', NOW()); ROLLBACK;"
```
  </how-to-verify>
  <resume-signal>Type "approved — migration 009 pushed" once migration is applied to dev DB and verified.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PG client → audit.actions | Migrations execute as superuser; CHECK constraint changes are PR-reviewed |
| Python enum.value → SQL CHECK value | One-to-one mapping must remain in lockstep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V7-enum-drift | Tampering | ActionType enum vs CHECK constraint | mitigate | Task 2 round-trip test inserts every new enum value into PG and asserts success; CI catches drift on each PR. |
| T-V7-migration-replay | Tampering | migration 009 idempotency | mitigate | `DROP CONSTRAINT IF EXISTS` + migrate.py applied-migrations meta table; Task 1 test #6 runs migration twice. |
| T-V7-audit-repudiation | Repudiation | audit.actions | mitigate | Phase 4 REVOKE UPDATE,DELETE ON audit.actions FROM agent_role still in force (no change in this plan). |
| T-V7-SC | Tampering | N/A this plan (no package installs) | n/a | This plan modifies SQL + Python enum only; no npm/pip/cargo installs. |
</threat_model>

<deviation_rules>
- **Rule 1:** D-AE-MNT is LOCKED — exactly 5 new ActionType values, NO new Decision values, mirror migration 007 DROP+ADD CHECK pattern.
- **Rule 3:** Task 2 round-trip test must be EXTENDED with new values; do NOT skip pre-existing assertions on Phase 1-6 values.
- **Rule 4:** Capture all deviations in 07-01-SUMMARY.md (e.g., if migrate.py runner needs adjustment, document it).
</deviation_rules>

<verification>
- `pytest infra/migrations/timescale/tests/test_migration_009.py -m integration -x` green (6 tests).
- `pytest packages/sft-agents/tests/test_audit_constraints.py -x` green.
- Dev DB constraint verified via psql query (Task 3 manual checkpoint).
- All existing tests still green: `nx run-many --target=test --projects=sft-agents`.
- Phase 6 migration 007 tests still green (no regression).
</verification>

<success_criteria>
- Migration applied to dev DB; new ActionType values insert successfully via raw psql.
- Python ActionType enum + SQL CHECK in lockstep — round-trip test passes for all 5 new values.
- Decision enum bytewise unchanged.
- No existing test regressions in `packages/sft-agents/tests/` or `infra/migrations/timescale/tests/`.
</success_criteria>

<commit_strategy>
- Commit 1: `feat(07-01): migration 009 extend audit.actions action_type for maintenance cluster` (SQL + test_migration_009.py)
- Commit 2: `feat(07-01): extend ActionType enum with 5 Phase 7 values (lockstep with migration 009)` (enums.py + test_audit_constraints.py)
- Task 3 is checkpoint:human-action — no commit needed (dev DB push only).
</commit_strategy>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-01-SUMMARY.md` documenting: 5 new ActionType values added, SQL migration filename, dev-DB push timestamp, any deviations.
</output>
</content>
</invoke>