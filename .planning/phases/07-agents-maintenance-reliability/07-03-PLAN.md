---
phase: 07-agents-maintenance-reliability
plan: 03
plan_id: 07-03
type: tdd
wave: 1
depends_on: ["07-00"]
files_modified:
  - packages/sft-ml/pyproject.toml
  - packages/sft-ml/project.json
  - packages/sft-ml/src/sft_ml/__init__.py
  - packages/sft-ml/src/sft_ml/cmapss/__init__.py
  - packages/sft-ml/src/sft_ml/cmapss/schema.py
  - packages/sft-ml/src/sft_ml/cmapss/feature_map.py
  - packages/sft-ml/src/sft_ml/cmapss/training.py
  - packages/sft-ml/src/sft_ml/cmapss/inference.py
  - packages/sft-ml/data/c-mapss-fd001/train_FD001.txt
  - packages/sft-ml/data/c-mapss-fd001/test_FD001.txt
  - packages/sft-ml/data/c-mapss-fd001/RUL_FD001.txt
  - packages/sft-ml/data/c-mapss-fd003/train_FD003.txt
  - packages/sft-ml/data/c-mapss-fd003/test_FD003.txt
  - packages/sft-ml/data/c-mapss-fd003/RUL_FD003.txt
  - packages/sft-ml/models/ridge-fd001-fd003-v1.0.joblib
  - packages/sft-ml/models/ridge-fd001-fd003-v1.0.json
  - packages/sft-ml/MODEL_CARD.md
  - packages/sft-ml/tests/test_feature_map.py
  - packages/sft-ml/tests/test_training.py
  - packages/sft-ml/tests/test_model_smoke.py
autonomous: false
requirements: [MNT-01]
objective: "Ship the new `packages/sft-ml/` package: C-MAPSS FD001+FD003 dataset committed to repo, deterministic Ridge training pipeline + textile→C-MAPSS sensor feature mapping + inference helper + pre-trained `ridge-fd001-fd003-v1.0.joblib` model artifact + MODEL_CARD documenting cross-domain transfer caveats. Per D-PM-01/02/03 — scikit-learn lightweight (Ridge + RandomForest optional), NO PyTorch."

must_haves:
  truths:
    - "`packages/sft-ml/` package importable via `from sft_ml.cmapss import load_fd_subset, train_ridge, save_model, predict_with_ci, map_textile_window_to_cmapss`."
    - "C-MAPSS FD001 + FD003 train/test/RUL files committed under `packages/sft-ml/data/` (~10MB total)."
    - "Training pipeline is deterministic: `random_state=42` fissato; same input data → byte-equal joblib model output."
    - "`predict_with_ci(features) -> (rul_cycles, ci_lower, ci_upper)` returns int-cast cycle counts; deterministic for identical inputs."
    - "Feature map covers at minimum 2 asset families (loom + spinning) per 07-RESEARCH.md Pattern 2 mapping table."
    - "Smoke test loads the committed `.joblib` model + runs predict on synthetic window — RMSE ≤ 35 cycles on FD001 test (literature baseline)."
    - "MODEL_CARD.md cites NASA PHM 2008 dataset + documents textile cross-domain mapping caveats (LAMA-Net / Bi-Discrepancy domain shift literature)."
  artifacts:
    - path: packages/sft-ml/src/sft_ml/cmapss/feature_map.py
      provides: "TEXTILE_TO_CMAPSS_FEATURE_MAP + OP_SETTING_MAP + map_textile_window_to_cmapss()"
      exports: ["TEXTILE_TO_CMAPSS_FEATURE_MAP", "OP_SETTING_MAP", "map_textile_window_to_cmapss"]
    - path: packages/sft-ml/src/sft_ml/cmapss/training.py
      provides: "train_ridge, train_random_forest, save_model, CMAPSS_COLUMNS, load_fd_subset, compute_train_rul, RANDOM_STATE=42"
      exports: ["CMAPSS_COLUMNS","load_fd_subset","compute_train_rul","train_ridge","train_random_forest","save_model","RANDOM_STATE"]
    - path: packages/sft-ml/src/sft_ml/cmapss/inference.py
      provides: "load_model + predict_with_ci (deterministic helper)"
      exports: ["load_model","predict_with_ci"]
    - path: packages/sft-ml/src/sft_ml/cmapss/schema.py
      provides: "CMAPSSRecord Pydantic (frozen)"
      exports: ["CMAPSSRecord"]
    - path: packages/sft-ml/models/ridge-fd001-fd003-v1.0.joblib
      provides: "pre-trained scikit-learn Pipeline(StandardScaler + Ridge) ~5MB"
    - path: packages/sft-ml/MODEL_CARD.md
      provides: "NASA citation + textile domain-shift caveat + training reproduction instructions"
  key_links:
    - from: packages/sft-ml/src/sft_ml/cmapss/inference.py
      to: packages/sft-ml/models/ridge-fd001-fd003-v1.0.joblib
      via: "joblib.load + sklearn.Pipeline.predict"
      pattern: "joblib.load|ridge-fd001-fd003"
    - from: packages/sft-ml/src/sft_ml/cmapss/feature_map.py
      to: packages/sft-ml/src/sft_ml/cmapss/training.py (CMAPSS_COLUMNS)
      via: "shared CMAPSS column schema"
      pattern: "CMAPSS_COLUMNS"
---

<objective>
Ship the entire ML pipeline package that PredictiveMaintenance (07-06) will consume. This is a self-contained data-science deliverable: dataset committed, training script reproducible offline, inference helper deterministic, model artifact pre-trained and committed (NO download at runtime per D-PM-02). PyTorch is explicitly NOT used. The package decouples ML lifecycle from agent runtime — re-training is a manual CLI step that produces a new versioned `.joblib`.

Purpose: Satisfy MNT-01 ML core (RUL estimation adapted from C-MAPSS to textile). Provide the ground-truth model artifact + reproducible training pipeline so 07-06 PredictiveMaintenance agent code is purely orchestration (load → map → predict → audit).
Output: 1 new Python package (`packages/sft-ml/`) + 6 committed C-MAPSS data files (~10MB) + 1 pre-trained joblib artifact + 3 source modules + 1 MODEL_CARD + 3 test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md
@.planning/phases/07-agents-maintenance-reliability/07-RESEARCH.md
@.planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@packages/sft-knowledge/pyproject.toml
@packages/sft-knowledge/project.json
@packages/sft-knowledge/src/sft_knowledge/__init__.py

<interfaces>
From 07-RESEARCH.md Pattern 1 (C-MAPSS schema — verbatim):
```python
CMAPSS_COLUMNS = (
    ["unit_number", "time_cycles", "op_setting_1", "op_setting_2", "op_setting_3"]
    + [f"s{i}" for i in range(1, 22)]
)

def load_fd_subset(data_dir: Path, subset: str) -> tuple[pd.DataFrame, pd.DataFrame, pd.Series]:
    """Load FD001 or FD003 train/test/RUL ground truth."""

def compute_train_rul(df: pd.DataFrame) -> pd.DataFrame:
    """Add 'rul' column: max_cycle_per_unit - current_cycle (piecewise-linear truncated cap 125)."""
```

From 07-RESEARCH.md Pattern 3 (training — verbatim):
```python
RANDOM_STATE = 42

def train_ridge(train_df: pd.DataFrame) -> Pipeline:
    pipeline = Pipeline([
        ("scaler", StandardScaler()),
        ("model", Ridge(alpha=1.0, random_state=RANDOM_STATE)),
    ])
    pipeline.fit(X, y)
    return pipeline

def save_model(pipeline: Pipeline, out_path: Path, version: str = "v1.0") -> None:
    joblib.dump(pipeline, out_path, compress=("gzip", 3))
```

From 07-RESEARCH.md Pattern 2 (feature map — verbatim):
```python
TEXTILE_TO_CMAPSS_FEATURE_MAP: dict[str, dict[str, str]] = {
    "loom": {"loom_temperature": "s8", "warp_tension": "s11", "creel_speed": "s9", "broken_pick_count": "s14"},
    "spinning": {"spindle_vibration": "s9", "spindle_temperature": "s8", "ring_position": "s11"},
}
OP_SETTING_MAP = {"ambient_temperature": "op_setting_2", "ambient_humidity": "op_setting_3"}
```

Pyproject deps target per 07-RESEARCH.md "Installation" section:
```toml
[project]
dependencies = [
  "scikit-learn>=1.7.0,<1.9.0",
  "joblib>=1.5.0,<2.0.0",
  "pandas>=2.3.0,<3.0.0",
  "numpy>=1.26.0,<3.0.0",
  "pydantic>=2.7.0,<3.0.0",
]
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: BLOCKING — Verify package legitimacy for scikit-learn, joblib, pandas (per 07-RESEARCH.md Package Legitimacy Audit fallback)</name>
  <what-built>About to add `packages/sft-ml/pyproject.toml` declaring 4 new transitive-or-direct PyPI installs (scikit-learn, joblib, pandas, numpy) per 07-RESEARCH.md Standard Stack table. slopcheck was unavailable for Python mode during research (per 07-RESEARCH.md L174-189), so all packages were marked `[ASSUMED]` after `pip index versions` verification. Confirm before install.</what-built>
  <how-to-verify>
For each package, open the official PyPI URL and confirm: project metadata matches (repo URL, license MIT/BSD, sustained release cadence, no recent suspicious takeovers):
- https://pypi.org/project/scikit-learn/ — expect repo github.com/scikit-learn/scikit-learn, license BSD-3-Clause, latest ≥1.7.0
- https://pypi.org/project/joblib/ — expect repo github.com/joblib/joblib, license BSD-3-Clause, latest ≥1.5.0
- https://pypi.org/project/pandas/ — expect repo github.com/pandas-dev/pandas, license BSD-3-Clause, latest ≥2.3.0
- https://pypi.org/project/numpy/ — already locked Phase 3 (sanity check only)

If any package fails the visual sanity check (e.g., suspicious recently-uploaded variant, mismatched repo, unfamiliar maintainer), STOP and surface to user. Otherwise approve.
  </how-to-verify>
  <resume-signal>Type "approved — packages legitimate" once all four PyPI pages verified.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold packages/sft-ml/ + write failing tests for feature_map + training + smoke</name>
  <files>
    packages/sft-ml/pyproject.toml,
    packages/sft-ml/project.json,
    packages/sft-ml/src/sft_ml/__init__.py,
    packages/sft-ml/src/sft_ml/cmapss/__init__.py,
    packages/sft-ml/src/sft_ml/cmapss/schema.py,
    packages/sft-ml/tests/test_feature_map.py,
    packages/sft-ml/tests/test_training.py,
    packages/sft-ml/tests/test_model_smoke.py
  </files>
  <read_first>
    .planning/phases/07-agents-maintenance-reliability/07-RESEARCH.md (Pattern 1, 2, 3 lines 395-555 — exact schemas + pipeline code),
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 8 lines 378-390 — package layout reference),
    packages/sft-knowledge/pyproject.toml (analog package layout),
    packages/sft-knowledge/project.json (Nx project descriptor analog),
    packages/sft-knowledge/src/sft_knowledge/__init__.py (package init style)
  </read_first>
  <behavior>
    Scaffold step:
    - `pyproject.toml` mirrors `sft-knowledge/pyproject.toml` style (Hatch build, Python 3.12+), declares 5 deps from 07-RESEARCH "Installation" section exactly: `scikit-learn>=1.7.0,<1.9.0`, `joblib>=1.5.0,<2.0.0`, `pandas>=2.3.0,<3.0.0`, `numpy>=1.26.0,<3.0.0`, `pydantic>=2.7.0,<3.0.0`. Package name `sft-ml`, import name `sft_ml`. Test deps: pytest, pytest-asyncio.
    - `project.json`: Nx project descriptor mirror sft-knowledge with `test` + `lint` + `build` targets.
    - `src/sft_ml/__init__.py`: empty package init (re-exports happen at `cmapss/__init__.py`).
    - `src/sft_ml/cmapss/__init__.py`: re-export `CMAPSS_COLUMNS, load_fd_subset, compute_train_rul, train_ridge, train_random_forest, save_model, load_model, predict_with_ci, TEXTILE_TO_CMAPSS_FEATURE_MAP, OP_SETTING_MAP, map_textile_window_to_cmapss, RANDOM_STATE, CMAPSSRecord`. (Names will exist after Task 3.)
    - `src/sft_ml/cmapss/schema.py`: `CMAPSSRecord(BaseModel)` frozen + extra=forbid with all 26 C-MAPSS columns typed (`unit_number: int`, `time_cycles: int`, `op_setting_1/2/3: float`, `s1..s21: float`).

    Tests (RED phase):
    - `test_feature_map.py`: covers
      * `TEXTILE_TO_CMAPSS_FEATURE_MAP` is a dict with at minimum keys "loom" and "spinning"
      * `OP_SETTING_MAP["ambient_temperature"] == "op_setting_2"` and `OP_SETTING_MAP["ambient_humidity"] == "op_setting_3"`
      * `map_textile_window_to_cmapss(df, "loom")` returns a single-row DataFrame with all 21 sensor columns + 3 op_setting columns, populated from textile sensor means
      * unknown asset_family raises KeyError
      * empty DataFrame returns row of zeros (graceful default)
    - `test_training.py`: covers
      * `CMAPSS_COLUMNS` has exactly 26 entries
      * `load_fd_subset(data_dir, "FD001")` returns (train_df, test_df, rul_series) with shape assertions
      * `compute_train_rul(df)` adds 'rul' column with max-per-unit semantics + cap at 125 (test with synthetic 2-unit DataFrame)
      * `train_ridge(small_train_df)` returns sklearn Pipeline with StandardScaler + Ridge; predicting twice yields byte-equal output (determinism)
      * `RANDOM_STATE == 42`
      * `save_model(pipeline, tmp_path/"x.joblib")` writes file + companion .json metadata
    - `test_model_smoke.py`: covers
      * the committed `packages/sft-ml/models/ridge-fd001-fd003-v1.0.joblib` exists on disk
      * `load_model(path)` loads it without error
      * `predict_with_ci(features)` returns (int, int, int) where ci_lower ≤ rul ≤ ci_upper
      * smoke RMSE on FD001 test set ≤ 35 cycles (literature baseline for Ridge on raw C-MAPSS sensors)

    All tests currently FAIL with ImportError (modules don't yet exist) — RED.
  </behavior>
  <action>
    Create the package skeleton directories + files. Author `pyproject.toml` mirroring sft-knowledge layout. Write tests per behavior above. Use `pytest.raises` for negative cases. For test_model_smoke.py, use `pytest.importorskip("joblib")` + `pytest.skip("model artifact not yet committed")` guard if the .joblib file doesn't exist — this allows Task 2 verify to confirm RED-via-skip rather than RED-via-import-error for the smoke test path. The other 2 test files should hit ImportError on feature_map / training imports (true RED).

    Do NOT yet implement feature_map.py / training.py / inference.py — those come in Task 3.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest packages/sft-ml/tests/ -x 2&gt;&amp;1 | grep -qE "ModuleNotFoundError|ImportError|skipped|failed" &amp;&amp; python -c "from sft_ml.cmapss.schema import CMAPSSRecord; r = CMAPSSRecord(unit_number=1, time_cycles=1, op_setting_1=0.0, op_setting_2=0.0, op_setting_3=0.0, **{f's{i}': 0.0 for i in range(1,22)}); print('schema OK')" &amp;&amp; echo "RED phase confirmed (feature_map+training imports fail; smoke skipped)"</automated>
  </verify>
  <done>Package skeleton committed; pyproject.toml + project.json valid; CMAPSSRecord importable; tests written and red (ImportError on missing modules + smoke skip on missing model).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement cmapss/feature_map.py + cmapss/training.py + cmapss/inference.py + commit C-MAPSS dataset + train and commit ridge-fd001-fd003-v1.0.joblib model</name>
  <files>
    packages/sft-ml/src/sft_ml/cmapss/feature_map.py,
    packages/sft-ml/src/sft_ml/cmapss/training.py,
    packages/sft-ml/src/sft_ml/cmapss/inference.py,
    packages/sft-ml/data/c-mapss-fd001/train_FD001.txt,
    packages/sft-ml/data/c-mapss-fd001/test_FD001.txt,
    packages/sft-ml/data/c-mapss-fd001/RUL_FD001.txt,
    packages/sft-ml/data/c-mapss-fd003/train_FD003.txt,
    packages/sft-ml/data/c-mapss-fd003/test_FD003.txt,
    packages/sft-ml/data/c-mapss-fd003/RUL_FD003.txt,
    packages/sft-ml/models/ridge-fd001-fd003-v1.0.joblib,
    packages/sft-ml/models/ridge-fd001-fd003-v1.0.json
  </files>
  <read_first>
    .planning/phases/07-agents-maintenance-reliability/07-RESEARCH.md (Pattern 1 + 2 + 3 lines 395-555 — exact code skeletons),
    .planning/phases/07-agents-maintenance-reliability/07-PATTERNS.md (Section 4 lines 178-238 — feature_map.py imports; Section 8 lines 378-390 — package layout reference),
    packages/sft-ml/tests/ (just-written tests — green target)
  </read_first>
  <behavior>
    Implementation per 07-RESEARCH.md verbatim snippets:
    - `feature_map.py`: define `TEXTILE_TO_CMAPSS_FEATURE_MAP` dict (extend Pattern 2 baseline with at minimum loom + spinning families; add dyeing + warping if datasheet available — derive from packages/sft-assets registry for asset_family enumeration), `OP_SETTING_MAP`, `map_textile_window_to_cmapss(window_df, asset_family) -> pd.DataFrame` per Pattern 2 code.
    - `training.py`: define `CMAPSS_COLUMNS`, `RANDOM_STATE = 42`, `load_fd_subset(data_dir, subset)`, `compute_train_rul(df)` per Pattern 1, `train_ridge(train_df)`, `train_random_forest(train_df)`, `save_model(pipeline, out_path, version)` per Pattern 3. Add CLI entrypoint `if __name__ == "__main__":` that loads FD001+FD003, trains Ridge on the combined `concat([fd001_train, fd003_train])`, evaluates RMSE on each test set, saves to `packages/sft-ml/models/ridge-fd001-fd003-v1.0.joblib`. Document CLI usage in docstring: `python -m sft_ml.cmapss.training`.
    - `inference.py`: `load_model(path) -> Pipeline` (joblib.load); `predict_with_ci(features: pd.DataFrame, pipeline, ci_pct=0.1) -> tuple[int, int, int]` returns `(rul_cycles, ci_lower, ci_upper)`. For Ridge the CI is computed deterministically as ±`ci_pct * point_estimate` (simple percentage band — RandomForest variant can produce real std-dev later; document this as PoC simplification in module docstring).

    Dataset commitment: Download or otherwise obtain NASA C-MAPSS FD001 + FD003 files (publicly available at https://www.nasa.gov/intelligent-systems-division/discovery-and-systems-health/pcoe/pcoe-data-set-repository/). Files are space-separated text, ~3-4MB each subset (train+test+RUL = 6 files total ≈ 10MB). Commit to `packages/sft-ml/data/c-mapss-fd001/` and `c-mapss-fd003/` exactly per the project structure in 07-RESEARCH.md L290-345. If git-lfs is configured project-wide, declare these files in `.gitattributes`; if not, commit as plain text (10MB is within Git's reasonable bounds).

    Model training: run `python -m sft_ml.cmapss.training` to produce the joblib + companion .json metadata. Verify RMSE on FD001 test ≤ 35 cycles (literature baseline). Commit both `ridge-fd001-fd003-v1.0.joblib` (~5MB gzip-compressed) and `ridge-fd001-fd003-v1.0.json` (metadata).

    All tests from Task 2 turn green. test_model_smoke.py un-skips and asserts loaded model + RMSE bound.
  </behavior>
  <action>
    Implement modules per behavior. For dataset acquisition: this is a one-time bootstrap step. Two options, in priority order:
    1. **If a maintainer has local C-MAPSS files**: copy them into the data/ directories. NASA dataset is public-domain.
    2. **Otherwise** download from the NASA mirror: `https://www.nasa.gov/intelligent-systems-division/discovery-and-systems-health/pcoe/pcoe-data-set-repository/` (FD001 + FD003 zip), extract, copy `train_FD001.txt`, `test_FD001.txt`, `RUL_FD001.txt`, `train_FD003.txt`, `test_FD003.txt`, `RUL_FD003.txt` into the respective data directories.

    Document the data origin in `packages/sft-ml/data/README.md` (separate from MODEL_CARD): NASA URL, dataset reference (Saxena et al. 2008 PHM Data Challenge), license note (public domain), file SHA256 checksums.

    Run training CLI once and commit the model. Verify file size <10MB. If git-lfs is in use (check `.gitattributes` at repo root), declare `*.joblib` + `*.txt` under the `data/c-mapss-*/` path. Otherwise commit as plain files.

    Smoke test will assert RMSE ≤ 35 on FD001 test (Ridge baseline literature; adjust threshold up to 40 if necessary, document deviation in SUMMARY).
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest packages/sft-ml/tests/ -x &amp;&amp; python -c "from sft_ml.cmapss import load_fd_subset, train_ridge, save_model, predict_with_ci, map_textile_window_to_cmapss, RANDOM_STATE; assert RANDOM_STATE == 42; print('imports OK')" &amp;&amp; ls -lh packages/sft-ml/models/ridge-fd001-fd003-v1.0.joblib &amp;&amp; ls -lh packages/sft-ml/data/c-mapss-fd001/ packages/sft-ml/data/c-mapss-fd003/</automated>
  </verify>
  <done>3 source modules implemented; 6 C-MAPSS data files committed; trained joblib + metadata json committed; all 3 test modules green; smoke test confirms RMSE ≤ 35 cycles on FD001.</done>
</task>

<task type="auto">
  <name>Task 4: Author MODEL_CARD.md documenting NASA citation + textile cross-domain transfer caveats</name>
  <files>
    packages/sft-ml/MODEL_CARD.md,
    packages/sft-ml/data/README.md
  </files>
  <read_first>
    .planning/phases/07-agents-maintenance-reliability/07-RESEARCH.md (Pattern 2 honest caveat lines 492-496; Standard Stack tables for citation discipline),
    .planning/phases/07-agents-maintenance-reliability/07-CONTEXT.md (canonical_refs lines 199-202 — NASA + ISO 14224 + OEE references)
  </read_first>
  <behavior>
    `MODEL_CARD.md` covers (mirror standard model card format — Mitchell et al. 2018 Model Cards for Model Reporting):
    - **Model Details**: name `ridge-fd001-fd003-v1.0`, version, training date, framework (scikit-learn ≥1.7), Pipeline composition (StandardScaler + Ridge alpha=1.0 random_state=42), file size, joblib compression.
    - **Intended Use**: PoC textile manufacturing RUL estimation, asset families loom + spinning + (extension targets); NOT for safety-critical decisions; subordinate to HITL supervisor on health_index < 0.3.
    - **Training Data**: NASA C-MAPSS FD001 + FD003 turbofan engine simulated data (cite Saxena, A., Goebel, K., Simon, D., Eklund, N. (2008), PHM 2008 Data Challenge); URL of dataset; subset rationale (FD001 = baseline single fault/op; FD003 = HPC degradation multi fault — covers 2 textile fault families).
    - **Cross-domain Mapping Caveat**: textile sensors are mapped to C-MAPSS sensor proxies per `feature_map.py`; this is a **best-guess semantic mapping**, not domain-adapted retraining. Cite LAMA-Net (arXiv:2208.08388) and Deep Domain Adaptation for Turbofan RUL survey (arXiv:2510.03604) as future work for proper transfer. Expected behavior: qualitatively correct trend signal, NOT calibrated absolute RUL.
    - **Metrics**: RMSE on FD001 test set, FD003 test set (literature baseline ~30-35 for Ridge).
    - **Limitations**: PoC scope, no online retraining (deferred Phase 11 per D-PM-01 rejected list), no domain adaptation, joblib serialization brittle across sklearn major versions (pin enforced in pyproject.toml).
    - **Reproduction**: command `python -m sft_ml.cmapss.training` produces byte-equal model with `RANDOM_STATE=42`.

    `data/README.md` covers: NASA URL, license (public domain), file checksums, dataset citation, "to refresh: download from NASA CoE and replace files; checksums must be updated in this README" instruction.
  </behavior>
  <action>
    Author both Markdown files per the structure above. Use standard markdown headings. Citations as inline links (no BibTeX needed for PoC). Embed the LAMA-Net + survey arxiv links from 07-RESEARCH.md verbatim. Use `sha256sum packages/sft-ml/data/c-mapss-fd001/*` (and FD003) to capture real checksums after Task 3 commit.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; test -s packages/sft-ml/MODEL_CARD.md &amp;&amp; test -s packages/sft-ml/data/README.md &amp;&amp; grep -qE "NASA|C-MAPSS|PHM 2008|random_state|RANDOM_STATE" packages/sft-ml/MODEL_CARD.md &amp;&amp; grep -qE "RMSE|Limitations|Cross-domain|LAMA-Net|domain adaptation" packages/sft-ml/MODEL_CARD.md &amp;&amp; grep -qE "sha256|SHA256|checksums" packages/sft-ml/data/README.md &amp;&amp; echo OK</automated>
  </verify>
  <done>Both Markdown files exist with required sections; NASA + LAMA-Net citations present; reproduction command documented; checksums recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → packages/sft-ml | New transitive deps (scikit-learn, joblib, pandas, numpy); package legitimacy gate per Task 1 |
| C-MAPSS data files (committed) → training pipeline | Public-domain NASA dataset; checksums recorded; no runtime download |
| joblib model artifact → inference | Pickle-based; cross-Python-minor compat fragile (mitigate via version pins) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V7-SC | Tampering | npm/pip installs | mitigate | Task 1 blocking-human-verify checkpoint for scikit-learn, joblib, pandas (all `[ASSUMED]` per 07-RESEARCH.md package legitimacy audit). |
| T-V7-pickle-deser | Tampering | joblib.load on `ridge-fd001-fd003-v1.0.joblib` | mitigate | Model artifact committed by trusted PR review; companion .json metadata enables version compat check before unpickling (load_model verifies sklearn_min before joblib.load). |
| T-V7-data-tamper | Tampering | C-MAPSS .txt files | mitigate | Checksums in data/README.md; PR review on any data file change. |
| T-V7-model-drift | Repudiation | training non-determinism | mitigate | `random_state=42` fissato in Ridge; smoke test asserts deterministic predict output. |
| T-V7-version-skew | Tampering | sklearn / joblib version drift breaking model load | mitigate | pyproject pins `scikit-learn>=1.7.0,<1.9.0` + `joblib>=1.5.0,<2.0.0`; companion .json metadata records `sklearn_min` — load_model raises if installed sklearn major < committed. |
</threat_model>

<deviation_rules>
- **Rule 1:** D-PM-01/02/03 LOCKED — Ridge/RandomForest scikit-learn only; FD001+FD003 dataset committed; feature_map best-guess semantic proxy. NO PyTorch, NO runtime download, NO domain adaptation.
- **Rule 3:** Do NOT lower the RMSE threshold below literature baseline (≤35 cycles FD001) to fake passing; if real RMSE is higher document deviation in SUMMARY + open follow-up issue.
- **Rule 4:** Capture all deviations in 07-03-SUMMARY.md (e.g., if a feature_map family was added beyond loom+spinning, if RMSE threshold needed to be relaxed, if git-lfs used).
</deviation_rules>

<verification>
- `nx run sft-ml:test` green (all 3 test modules pass).
- `python -c "from sft_ml.cmapss import predict_with_ci, load_model; m = load_model('packages/sft-ml/models/ridge-fd001-fd003-v1.0.joblib'); print('model loaded OK')"` succeeds.
- Model file size <10MB.
- Dataset files present in `packages/sft-ml/data/c-mapss-fd001/` and `c-mapss-fd003/`.
- MODEL_CARD.md + data/README.md exist with required sections.
- Smoke test confirms RMSE ≤ 35 (or documented deviation in SUMMARY).
</verification>

<success_criteria>
- New `sft-ml` package importable, MNT-01 ML foundation in place.
- Determinism end-to-end: training (random_state=42) → inference (deterministic Ridge predict).
- C-MAPSS dataset reproducible offline (no network in CI).
- Cross-domain caveats fully documented (no overclaiming of transfer accuracy).
- Ready for 07-06 PredictiveMaintenance agent to consume via `from sft_ml.cmapss import ...`.
</success_criteria>

<commit_strategy>
- Commit 1: `feat(07-03): scaffold packages/sft-ml/ + cmapss schema + failing tests` (pyproject + project.json + __init__ + schema + tests)
- Commit 2: `feat(07-03): C-MAPSS FD001+FD003 dataset commit (~10MB, NASA public domain)` (6 .txt files + data/README.md)
- Commit 3: `feat(07-03): implement cmapss/feature_map + training + inference` (3 source modules)
- Commit 4: `feat(07-03): train + commit ridge-fd001-fd003-v1.0.joblib (RMSE ≤ 35 on FD001)` (model files)
- Commit 5: `docs(07-03): MODEL_CARD with cross-domain transfer caveats` (MODEL_CARD.md)
</commit_strategy>

<output>
Create `.planning/phases/07-agents-maintenance-reliability/07-03-SUMMARY.md` documenting: package layout summary, dataset commit size + checksums, model RMSE on FD001 test (and FD003 test if computed), feature_map families covered, any version-pin deviations, any threshold relaxations.
</output>
</content>
</invoke>