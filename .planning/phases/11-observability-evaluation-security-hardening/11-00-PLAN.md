---
phase: 11-observability-evaluation-security-hardening
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sft-agents/pyproject.toml
  - packages/sft-agents/src/sft_agents/otel/__init__.py
  - packages/sft-agents/src/sft_agents/otel/provider.py
  - packages/sft-agents/src/sft_agents/otel/nats_carrier.py
  - services/knowledge-ingest/pyproject.toml
  - tests/eval/conftest.py
  - tests/eval/dataset/ground_truth.jsonl
  - tests/test_otel_nats_propagation.py
  - infra/compose/obs.yml
  - infra/grafana/prometheus.yml
  - infra/grafana/tempo.yaml
  - infra/grafana/provisioning/datasources/datasources.yaml
  - infra/grafana/provisioning/dashboards/dashboards.yaml
  - infra/migrations/timescale/014_extend_audit_phase11.sql
  - infra/migrations/timescale/tests/test_migration_014.py
autonomous: true
gap_closure: false
requirements: [OBS-02, OBS-03, OBS-05, OBS-06, SEC-07]
must_haves:
  truths:
    - "setup_tracer_provider(service_name) builds a TracerProvider with an OTLP span processor and is idempotent (no double global override)"
    - "NatsHeaderCarrier round-trips a W3C traceparent: inject() then extract() reproduces the same trace context"
    - "Langfuse self-hosted OTLP endpoint reachability + RAGAS non-LLM determinism are VERIFIED (recorded as acceptance steps) before any config is hardcoded downstream"
    - "obs.yml exposes Grafana on host port 3001 (Langfuse keeps 3000) plus Prometheus and Tempo, without touching the existing Langfuse/ClickHouse services"
    - "Migration 014 adds RESTRICTED_DOC_ACCESS to the audit.actions CHECK constraint in lockstep with the enums.py value, and is idempotent"
    - "Nyquist scaffolds exist (failing or skipped) for OTEL-NATS propagation and the eval gate before implementation"
  artifacts:
    - path: "packages/sft-agents/src/sft_agents/otel/provider.py"
      provides: "setup_tracer_provider() — TracerProvider + OTLP exporter, singleton-guarded"
      contains: "def setup_tracer_provider"
    - path: "packages/sft-agents/src/sft_agents/otel/nats_carrier.py"
      provides: "NatsHeaderCarrier(MutableMapping) for OTEL inject/extract on NATS headers"
      contains: "class NatsHeaderCarrier"
    - path: "infra/migrations/timescale/014_extend_audit_phase11.sql"
      provides: "RESTRICTED_DOC_ACCESS CHECK extension (SEC-07 lockstep)"
      contains: "RESTRICTED_DOC_ACCESS"
    - path: "tests/eval/dataset/ground_truth.jsonl"
      provides: "30+ synthetic ground-truth scenarios per cluster"
      min_lines: 30
    - path: "infra/compose/obs.yml"
      provides: "Grafana(3001) + Prometheus + Tempo added without touching Langfuse"
      contains: "grafana"
  key_links:
    - from: "packages/sft-agents/src/sft_agents/otel/nats_carrier.py"
      to: "opentelemetry.propagate"
      via: "TextMap inject/extract over MutableMapping headers"
      pattern: "propagate\\.(inject|extract)"
    - from: "infra/migrations/timescale/014_extend_audit_phase11.sql"
      to: "packages/sft-agents/src/sft_agents/models/enums.py"
      via: "ActionType.RESTRICTED_DOC_ACCESS lockstep with CHECK string"
      pattern: "RESTRICTED_DOC_ACCESS"
---

<objective>
Wave 0 foundation per Phase 11: installa le nuove dipendenze nei manifest CORRETTI, crea il package `sft_agents.otel` (TracerProvider + NatsHeaderCarrier), estende `infra/compose/obs.yml` con Grafana(3001)/Prometheus/Tempo SENZA toccare i servizi Langfuse esistenti, crea la migration `014_extend_audit_phase11.sql` in lockstep con l'enum, e scaffolda i test Nyquist (eval + OTEL propagation). Include due step di VERIFICA acceptance (Langfuse OTLP curl + RAGAS non-LLM determinism) PRIMA di hardcodare config a valle.

Purpose: Ogni piano successivo (OTEL propagation, eval gate, security) dipende da: il package OTEL, le immagini di osservabilità in compose, la migration audit, e i test scaffold. Niente viene installato senza pin di versione verificato.
Output: Manifest aggiornati, package OTEL, compose esteso, migration 014, scaffold di test, e due risultati di verifica documentati nel SUMMARY.

Execution note: worktrees DISABILITATI — esecuzione SEQUENZIALE su main tree. Questo è l'unico piano di Wave 1; tutti gli altri dipendono da esso.

Critical research findings baked in:
- `opentelemetry-instrumentation-nats` NON ESISTE su PyPI — implementare il `NatsHeaderCarrier` manuale (~30 righe). NON aggiungere quel package.
- Grafana su porta 3001 (Langfuse possiede la 3000). NON modificare i healthcheck ClickHouse esistenti.
- Migration 014 specchia il pattern DROP+ADD idempotente di 010/012; lockstep enum↔SQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-observability-evaluation-security-hardening/11-CONTEXT.md
@.planning/phases/11-observability-evaluation-security-hardening/11-RESEARCH.md
@infra/compose/obs.yml
@infra/migrations/timescale/012_extend_audit_scm.sql
@packages/sft-agents/src/sft_agents/models/enums.py

<interfaces>
<!-- Pattern manuale NATS carrier — RESEARCH Pattern 1. Implementarlo come MutableMapping. -->
NatsHeaderCarrier(MutableMapping): __init__(headers: dict), __getitem__/__setitem__/__delitem__/__iter__/__len__
provider.setup_tracer_provider(service_name: str) -> TracerProvider
  env: OTEL_EXPORTER_OTLP_ENDPOINT (default http://tempo:4317), OTEL_SERVICE_NAME

<!-- Migration 012 nomina il constraint `audit_actions_action_type_chk` — riusare lo STESSO nome in 014. -->
<!-- enums.py ActionType: ultimo valore Phase 9 è COST_REPORT (riga 149). Aggiungere RESTRICTED_DOC_ACCESS dopo, con commento lockstep migration 014. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere dipendenze Phase 11 ai manifest corretti + verificare availability</name>
  <files>packages/sft-agents/pyproject.toml, services/knowledge-ingest/pyproject.toml</files>
  <action>Aggiungere a `[project].dependencies` di `packages/sft-agents/pyproject.toml` (accanto alle entry opentelemetry/langfuse/nats-py esistenti): opentelemetry-exporter-otlp-proto-grpc>=1.42,<2 ; opentelemetry-exporter-otlp-proto-http>=1.42,<2 . Aggiungere a `services/knowledge-ingest/pyproject.toml` `[project].dependencies`: bleach>=6.3,<7 . Le dipendenze eval (deepeval>=4.0,<5 ; ragas>=0.4,<0.5) NON vanno in un package di runtime: aggiungerle a un gruppo dev/test — preferire un gruppo `[dependency-groups] eval = [...]` nel pyproject del workspace root (lo stesso dove gira `tests/eval/`); se il root non ha pyproject, aggiungerle al gruppo dev di `packages/sft-agents`. NON aggiungere mai un package npm o python errato a livello root accidentalmente, e NON aggiungere `opentelemetry-instrumentation-nats` (non esiste — RESEARCH Pitfall 1). Eseguire `uv lock` nel workspace per risolvere, poi un import smoke. Tenere lo stile dell'array coerente con quello esistente.</action>
  <verify>
    <automated>uv sync --all-packages && uv run python -c "import bleach, opentelemetry.exporter.otlp.proto.grpc.trace_exporter, opentelemetry.exporter.otlp.proto.http.trace_exporter, deepeval, ragas; print('phase11-deps-ok')"</automated>
  </verify>
  <done>L'import smoke stampa phase11-deps-ok; uv.lock risolve senza conflitti; nessuna dipendenza eval finita in un manifest di runtime; nessun riferimento a opentelemetry-instrumentation-nats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Creare il package sft_agents.otel (provider + NatsHeaderCarrier) con test round-trip</name>
  <files>packages/sft-agents/src/sft_agents/otel/__init__.py, packages/sft-agents/src/sft_agents/otel/provider.py, packages/sft-agents/src/sft_agents/otel/nats_carrier.py, tests/test_otel_nats_propagation.py</files>
  <behavior>
    - Test 1 (RED→GREEN): inject di un traceparent in un dict vuoto via NatsHeaderCarrier popola la chiave `traceparent`; extract dello stesso dict riproduce un SpanContext con lo stesso trace_id.
    - Test 2: NatsHeaderCarrier soddisfa il protocollo MutableMapping (get/set/del/iter/len) su un dict di header.
    - Test 3: setup_tracer_provider("svc-x") chiamato due volte NON sovrascrive il provider globale la seconda volta (flag `_initialized` singleton) — nessun ProviderOverride warning.
  </behavior>
  <action>Creare `nats_carrier.py` con `NatsHeaderCarrier(MutableMapping)` (RESEARCH Pattern 1) che wrappa un `dict[str,str]` di header NATS per inject/extract OTEL. Creare `provider.py` con `setup_tracer_provider(service_name)` (RESEARCH Pattern 2): legge `OTEL_EXPORTER_OTLP_ENDPOINT` (default `http://tempo:4317`), crea `Resource{service.name}`, `TracerProvider`, `BatchSpanProcessor(OTLPSpanExporter(insecure=True))`, e chiama `trace.set_tracer_provider` SOLO se non già inizializzato (modulo-level `_initialized` flag — evita l'anti-pattern global provider doppio). Usare l'exporter gRPC come default con fallback HTTP se l'endpoint termina con path `/v1/traces`. `__init__.py` esporta `setup_tracer_provider` e `NatsHeaderCarrier`. NON usare formato traceparent manuale — usare `opentelemetry.propagate.inject/extract` (W3C standard). Scrivere il test `tests/test_otel_nats_propagation.py` PRIMA (RED) con i 3 casi sopra.</action>
  <verify>
    <automated>uv run pytest tests/test_otel_nats_propagation.py -x -q</automated>
  </verify>
  <done>I 3 test passano; provider idempotente; carrier round-trip verde; nessun uso di opentelemetry-instrumentation-nats.</done>
</task>

<task type="auto">
  <name>Task 3: Estendere obs.yml (Grafana 3001 + Prometheus + Tempo) + provisioning + config files</name>
  <files>infra/compose/obs.yml, infra/grafana/prometheus.yml, infra/grafana/tempo.yaml, infra/grafana/provisioning/datasources/datasources.yaml, infra/grafana/provisioning/dashboards/dashboards.yaml</files>
  <action>Aggiungere a `infra/compose/obs.yml` TRE nuovi servizi senza modificare i servizi esistenti (langfuse-pg/clickhouse/minio/langfuse-redis/langfuse-web/langfuse-worker) né i loro healthcheck (RESEARCH Pitfall 5): `prometheus` (prom/prometheus:v2.53.3, monta `./infra/grafana/prometheus.yml`), `tempo` (grafana/tempo:2.6.1, monta `./infra/grafana/tempo.yaml`, espone OTLP gRPC 4317), `grafana` (grafana/grafana:11.3.1, monta `./infra/grafana/provisioning` e `./infra/grafana/dashboards`, mappa `${GRAFANA_PORT:-3001}:3000` — MAI 3000, RESEARCH Pitfall 4, GF_AUTH_ANONYMOUS_ENABLED true solo dev). Tutti e tre sulla rete `sft-obs` esistente. Creare `prometheus.yml` (scrape job per api-gateway/ot-bridge metrics endpoint), `tempo.yaml` (config minimale receiver OTLP), `datasources.yaml` (Prometheus default + Tempo, RESEARCH Pattern 5), e `dashboards.yaml` (provider che punta a /var/lib/grafana/dashboards). NON creare ancora i dashboard JSON (sono Wave 3 / piano 11-04).</action>
  <verify>
    <automated>docker compose -f infra/compose/obs.yml config >/dev/null && python -c "import yaml,sys; d=yaml.safe_load(open('infra/compose/obs.yml')); s=d['services']; assert 'grafana' in s and 'prometheus' in s and 'tempo' in s, 'missing svc'; gp=str(s['grafana']['ports'][0]); assert '3000' not in gp.split(':')[0] and '3001' in gp, f'grafana must be host 3001: {gp}'; assert 'langfuse-web' in s and 'clickhouse' in s, 'existing svc removed'; print('obs-compose-ok')"</automated>
  </verify>
  <done>`docker compose config` valida; Grafana su host 3001; Prometheus/Tempo presenti; servizi Langfuse/ClickHouse intatti; provisioning datasource/dashboard yaml creati.</done>
</task>

<task type="auto">
  <name>Task 4: Migration 014 (RESTRICTED_DOC_ACCESS) in lockstep + test round-trip</name>
  <files>infra/migrations/timescale/014_extend_audit_phase11.sql, infra/migrations/timescale/tests/test_migration_014.py</files>
  <action>Creare `014_extend_audit_phase11.sql` specchiando ESATTAMENTE il pattern idempotente di `012_extend_audit_scm.sql`: `ALTER TABLE audit.actions DROP CONSTRAINT IF EXISTS audit_actions_action_type_chk;` poi `ADD CONSTRAINT audit_actions_action_type_chk CHECK (action_type IN (...))` includendo TUTTI i valori esistenti (Phase 1-9, copiati da 012) PIÙ il nuovo `'RESTRICTED_DOC_ACCESS'` con commento `-- SEC-07: query a chunk acl_level=restricted`. Header con commenti lockstep verso `enums.py::ActionType`. NON toccare il CHECK su Decision. Aggiungere `RESTRICTED_DOC_ACCESS = "RESTRICTED_DOC_ACCESS"` a `enums.py` NON è di questo piano (è 11-03, che dipende da questa migration) — ma il commento SQL deve citare il lockstep. Creare `test_migration_014.py` specchiando `test_migration_012.py`: assert (a) RESTRICTED_DOC_ACCESS inserisce ok, (b) i valori legacy Phase 1-9 inseriscono ancora (no regression), (c) double-apply è no-op.</action>
  <verify>
    <automated>uv run pytest infra/migrations/timescale/tests/test_migration_014.py -x -q</automated>
  </verify>
  <done>Migration 014 idempotente; RESTRICTED_DOC_ACCESS accettato; nessuna regressione sui valori legacy; nome constraint riusato da 012; commenti lockstep presenti.</done>
</task>

<task type="auto">
  <name>Task 5: Scaffold eval (conftest MockDeepEvalLLM + 30+ ground truth) + verifiche acceptance Langfuse/RAGAS</name>
  <files>tests/eval/conftest.py, tests/eval/dataset/ground_truth.jsonl</files>
  <action>Creare `tests/eval/conftest.py` con (1) `MockDeepEvalLLM(DeepEvalBaseLLM)` deterministico (RESEARCH Pattern 4) — ma NON deve ritornare sempre score=1 (anti-pattern): far variare lo score in base a un campo del fixture (es. `expected_score`) cosicché il gate possa effettivamente fallire; (2) fixture `ground_truth_dataset` che carica `tests/eval/dataset/ground_truth.jsonl`. Generare `ground_truth.jsonl` con ≥30 scenari sintetici per cluster (estendere/derivare da `tests/data/rag_eval/testset.jsonl`, 120 righe esistenti) — ciascun record: {question, contexts, ground_truth, expected_score, cluster}. ESEGUIRE E DOCUMENTARE NEL SUMMARY due verifiche acceptance PRIMA che i piani a valle hardcodino config: (A) reachability OTLP Langfuse self-hosted: `docker compose -f infra/compose/obs.yml up -d langfuse-web` poi `curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/api/public/otel/v1/traces` — annotare se richiede `Authorization: Basic pk:sk` o accetta senza auth in dev; (B) RAGAS determinism: `pip show ragas` e un mini-script che esegue `context_precision`/`context_recall` due volte sullo stesso input e asserisce identità del risultato (no-LLM determinism). Registrare entrambi gli esiti come acceptance note.</action>
  <verify>
    <automated>uv run python -c "import json; rows=[json.loads(l) for l in open('tests/eval/dataset/ground_truth.jsonl') if l.strip()]; assert len(rows)>=30, f'need 30+, got {len(rows)}'; assert all('question' in r and 'ground_truth' in r and 'cluster' in r for r in rows); print(f'ground-truth-ok:{len(rows)}')" && uv run python -c "from tests.eval.conftest import MockDeepEvalLLM; m=MockDeepEvalLLM(); assert m.get_model_name(); print('mock-llm-ok')"</automated>
  </verify>
  <done>conftest definisce MockDeepEvalLLM (score variabile, non sempre 1) + fixture dataset; ground_truth.jsonl ≥30 scenari; le due verifiche acceptance (Langfuse OTLP curl + RAGAS determinism) eseguite e documentate nel SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/pip registry → build | Codice di terze parti entra al momento dell'install |
| compose host ports → network | Grafana/Prometheus/Tempo espongono porte sull'host dev |
| migration SQL → audit DB | DDL modifica il CHECK constraint su audit.actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | pip installs (deepeval/ragas/bleach/otlp) | mitigate | Tutti i pacchetti vettati nel 11-RESEARCH Package Legitimacy Audit (PyPI verified, Approved); versioni pin esplicito; nessun [SUS]/[SLOP] — nessun checkpoint bloccante. `opentelemetry-instrumentation-nats` escluso (non esiste). |
| T-11-00-01 | Tampering | migration 014 CHECK drift | mitigate | DROP+ADD idempotente sullo stesso constraint name di 012; test_migration_014 verifica no-regression + lockstep enum. |
| T-11-00-02 | Elevation of Privilege | Grafana anonymous dev | accept | GF_AUTH_ANONYMOUS solo per dev compose (Viewer); doc in .env.example (piano 11-05). Nessun dato PII reale. |
| T-11-00-03 | Denial of Service | Tempo OTLP receiver aperto | accept | Rete `sft-obs` interna al compose dev; non esposto in prod (documentato). |
</threat_model>

<verification>
- `uv sync` risolve; import smoke phase11-deps-ok.
- pytest OTEL carrier round-trip + provider idempotente verdi.
- `docker compose -f obs.yml config` valida; Grafana su 3001; Langfuse intatto.
- Migration 014 idempotente, lockstep, no-regression.
- ground_truth.jsonl ≥30; verifiche Langfuse OTLP + RAGAS determinism documentate.
</verification>

<success_criteria>
Le fondamenta OTEL/eval/infra/migration esistono e sono testate; le due incognite di ricerca (Langfuse OTLP auth, RAGAS non-LLM determinism) sono risolte con verifica empirica prima di qualsiasi hardcoding a valle.
</success_criteria>

<output>
Create `.planning/phases/11-observability-evaluation-security-hardening/11-00-SUMMARY.md` when done. Includere i due esiti di verifica acceptance (Langfuse OTLP curl result + RAGAS determinism result).
</output>
