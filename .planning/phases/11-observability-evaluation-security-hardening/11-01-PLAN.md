---
phase: 11-observability-evaluation-security-hardening
plan: 01
type: execute
wave: 2
depends_on: ["11-00"]
files_modified:
  - apps/api-gateway/src/svc_api_gateway/lifespan.py
  - apps/api-gateway/src/svc_api_gateway/nats_publisher.py
  - packages/sft-agents/src/sft_agents/llm/langfuse_callback.py
  - packages/sft-agents/src/sft_agents/runtime/agent_runner.py
  - apps/api-gateway/tests/test_otel_propagation_e2e.py
autonomous: true
gap_closure: false
requirements: [OBS-02]
must_haves:
  truths:
    - "The gateway initializes setup_tracer_provider('sft-api-gateway') in its FastAPI lifespan, once"
    - "When the gateway publishes an agent command on NATS, a W3C traceparent header is injected via NatsHeaderCarrier"
    - "When an agent runner consumes a NATS command, it extracts the traceparent and opens a CONSUMER child span under the same trace"
    - "The Langfuse CallbackHandler config carries a 'phase11' tag so traces are identifiable; no OTLP dual-path to Langfuse is added (avoids duplicate spans)"
    - "A single trace_id propagates gateway → NATS → agent runner, verified by an automated test"
  artifacts:
    - path: "apps/api-gateway/src/svc_api_gateway/nats_publisher.py"
      provides: "publish path that injects traceparent into NATS headers"
      contains: "NatsHeaderCarrier"
    - path: "packages/sft-agents/src/sft_agents/runtime/agent_runner.py"
      provides: "consume path that extracts traceparent + opens child span"
      contains: "propagate.extract"
    - path: "apps/api-gateway/tests/test_otel_propagation_e2e.py"
      provides: "end-to-end trace_id continuity assertion"
      contains: "trace_id"
  key_links:
    - from: "apps/api-gateway/src/svc_api_gateway/nats_publisher.py"
      to: "packages/sft-agents/src/sft_agents/runtime/agent_runner.py"
      via: "W3C traceparent in NATS Msg.headers (inject → extract)"
      pattern: "traceparent"
    - from: "apps/api-gateway/src/svc_api_gateway/lifespan.py"
      to: "packages/sft-agents/src/sft_agents/otel/provider.py"
      via: "setup_tracer_provider called in lifespan"
      pattern: "setup_tracer_provider"
---

<objective>
Cablare la propagazione OTEL end-to-end (OBS-02): init del TracerProvider nel lifespan del gateway, inject del W3C traceparent negli header NATS al publish del comando agent, extract + child span CONSUMER nel runner agent, e tag "phase11" sul CallbackHandler Langfuse esistente. Un singolo trace_id deve fluire gateway → NATS → agent.

Purpose: realizza SC-1 (singola trace correlata con token/latency/HITL metadata in Langfuse). Riusa il package `sft_agents.otel` creato in 11-00 e il `langfuse_callback.py` esistente.
Output: gateway strumentato, publisher/consumer con propagazione, test e2e di continuità trace.

Execution note: SEQUENZIALE su main tree. Wave 2, dipende da 11-00. File disgiunti da 11-02/11-03 (stessa wave): questo piano tocca lifespan/nats_publisher/agent_runner/langfuse_callback; NON tocca enums.py, jwt.py, sanitizer, tests/eval.

Critical findings baked in:
- Usare il `NatsHeaderCarrier` di 11-00 (NON cercare auto-instrumentation NATS).
- NON aggiungere un OTLP exporter verso Langfuse (RESEARCH Pitfall 3: trace duplicate). Mantenere solo il CallbackHandler LangChain già wired; il TracerProvider OTLP punta a Tempo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-observability-evaluation-security-hardening/11-CONTEXT.md
@.planning/phases/11-observability-evaluation-security-hardening/11-RESEARCH.md
@.planning/phases/11-observability-evaluation-security-hardening/11-00-SUMMARY.md
@packages/sft-agents/src/sft_agents/llm/langfuse_callback.py
@apps/api-gateway/src/svc_api_gateway/lifespan.py

<interfaces>
<!-- Da 11-00: -->
from sft_agents.otel import setup_tracer_provider, NatsHeaderCarrier
from opentelemetry import propagate, trace, context as otel_context
from opentelemetry.trace import SpanKind
<!-- Publisher: propagate.inject(NatsHeaderCarrier(headers)); nc.publish(subject, payload, headers=headers) -->
<!-- Consumer: ctx = propagate.extract(NatsHeaderCarrier(dict(msg.headers or {}))); attach/detach + start_as_current_span(kind=CONSUMER) -->
<!-- langfuse_callback.build_invocation_config(thread_id, user_id, tags=[...]) — aggiungere "phase11" alla lista tags -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Init TracerProvider nel lifespan + inject traceparent al publish NATS</name>
  <files>apps/api-gateway/src/svc_api_gateway/lifespan.py, apps/api-gateway/src/svc_api_gateway/nats_publisher.py</files>
  <behavior>
    - Test 1: dopo lifespan startup, `trace.get_tracer_provider()` ha service.name "sft-api-gateway".
    - Test 2: il publish di un comando agent produce un dict di header NATS contenente la chiave `traceparent` ben formata (W3C).
  </behavior>
  <action>In `lifespan.py` chiamare `setup_tracer_provider("sft-api-gateway")` all'avvio (RESEARCH Code Example lifespan), una sola volta — il flag singleton di 11-00 protegge da doppio override. In `nats_publisher.py` (il modulo gateway che pubblica comandi agent su NATS — individuare il punto di publish esistente), prima di `nc.publish(...)` costruire `headers: dict[str,str] = {}`, chiamare `propagate.inject(NatsHeaderCarrier(headers))`, e passare `headers=headers` al publish. Se il publisher non esiste come modulo separato, individuare la funzione di publish corrente e wrappare lì; mantenere il nome file `nats_publisher.py` se è già il modulo, altrimenti modificare il modulo reale e aggiornare files_modified nel SUMMARY. NON aggiungere exporter Langfuse OTLP.</action>
  <verify>
    <automated>uv run pytest apps/api-gateway/tests/test_otel_propagation_e2e.py -k "publish or provider" -x -q</automated>
  </verify>
  <done>Lifespan inizializza il provider; il publish inietta un traceparent valido negli header NATS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract + child span nel runner agent + tag phase11 Langfuse</name>
  <files>packages/sft-agents/src/sft_agents/runtime/agent_runner.py, packages/sft-agents/src/sft_agents/llm/langfuse_callback.py</files>
  <behavior>
    - Test 1: dato un msg NATS con traceparent iniettato dal gateway, il runner estrae il contesto e apre uno span CONSUMER il cui trace_id == trace_id del publisher.
    - Test 2: `build_invocation_config(...)` include "phase11" nella lista tags.
  </behavior>
  <action>Nel runner che consuma comandi agent da NATS (individuare l'handler di subscribe esistente in `runtime/agent_runner.py` o modulo equivalente), all'arrivo del `msg`: `carrier = NatsHeaderCarrier(dict(msg.headers or {}))`, `ctx = propagate.extract(carrier)`, `token = otel_context.attach(ctx)`, poi `with tracer.start_as_current_span("agent.command", kind=SpanKind.CONSUMER):` processare il comando, infine `otel_context.detach(token)` in `finally` (RESEARCH Pattern 1). In `langfuse_callback.py` estendere `build_invocation_config` per includere `"phase11"` nella lista `tags` (RESEARCH Pattern 3) — additivo, non rimuovere tag esistenti. Se il modulo runner reale ha nome diverso, modificare quello reale e annotarlo nel SUMMARY. NON duplicare lo span se già esiste auto-instrumentation; lo span CONSUMER è il punto di aggancio della trace cross-NATS.</action>
  <verify>
    <automated>uv run pytest apps/api-gateway/tests/test_otel_propagation_e2e.py -x -q && uv run pytest packages/sft-agents/tests/test_langfuse_callback.py -k tag -x -q</automated>
  </verify>
  <done>Il runner estrae il traceparent e apre uno span CONSUMER con lo stesso trace_id del publisher; il config Langfuse porta il tag phase11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → NATS → agent | Header di trace attraversano il broker; payload comando agent |
| agent → Langfuse | Span LLM (token count, latency, HITL metadata) inviati a Langfuse self-hosted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | NATS traceparent header | mitigate | Usa `TraceContextTextMapPropagator` W3C (validazione formato inclusa); header è solo correlazione, non controllo di accesso. |
| T-11-01-02 | Information Disclosure | token count LLM in trace | accept | Trace su Langfuse self-hosted interno alla rete `sft-obs` dev; documentato nel STRIDE doc (cella Agent/Info-Disclosure, piano 11-05). |
| T-11-01-03 | Tampering | duplicate spans (OTLP+CallbackHandler) | mitigate | NON aggiunto OTLP exporter verso Langfuse (RESEARCH Pitfall 3); solo CallbackHandler. TracerProvider OTLP → Tempo. |
| T-11-01-04 | Repudiation | HITL decision senza trace | mitigate | Span CONSUMER apre la trace agent; HITL metadata già emessa via CallbackHandler con tag phase11. |
</threat_model>

<verification>
- pytest e2e: trace_id del publisher == trace_id del consumer span.
- lifespan inizializza il provider una sola volta (no doppio override).
- Langfuse config con tag phase11; nessun OTLP exporter verso Langfuse.
</verification>

<success_criteria>
Un singolo trace_id propaga gateway → NATS → agent runner (verificato dal test e2e); Langfuse riceve span taggate phase11 via CallbackHandler senza duplicazione.
</success_criteria>

<output>
Create `.planning/phases/11-observability-evaluation-security-hardening/11-01-SUMMARY.md` when done. Annotare i nomi reali dei moduli publisher/runner se differiscono dai placeholder.
</output>
