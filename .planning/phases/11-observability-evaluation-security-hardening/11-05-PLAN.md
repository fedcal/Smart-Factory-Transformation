---
phase: 11-observability-evaluation-security-hardening
plan: 05
type: execute
wave: 3
depends_on: ["11-00", "11-01", "11-02", "11-03"]
files_modified:
  - docs/security/STRIDE-threat-model.md
  - docs/security/owasp-llm-top10.md
  - infra/compose/.env.example
  - docs/security/rate-limit-scaling.md
  - .planning/phases/10-backend-api-frontend/10-SECURITY.md
  - docs/security/tests/test_stride_coverage.py
autonomous: true
gap_closure: false
requirements: [SEC-01, SEC-02, SEC-05]
must_haves:
  truths:
    - "The STRIDE threat model documents >=1 threat per category (S/T/R/I/D/E) x 3 surfaces (IT/OT, RAG ingest, agent orchestration) with code-mapped mitigations (SEC-01, SC-4)"
    - "The STRIDE doc consolidates the per-phase registers from 08/09/10 SECURITY.md"
    - "OWASP LLM Top-10 defenses are mapped to concrete code mitigations across phases (SEC-02)"
    - ".env.example documents all Phase 11 env vars (OTEL, Langfuse, Grafana) with no hardcoded secrets (SEC-05)"
    - "AR-01..AR-07 from 10-SECURITY.md are annotated closed or carried (AR-07 Redis = documentation-only)"
  artifacts:
    - path: "docs/security/STRIDE-threat-model.md"
      provides: "SEC-01 consolidated STRIDE 6x3 matrix with code-mapped mitigations"
      contains: "Elevation of Privilege"
    - path: "docs/security/owasp-llm-top10.md"
      provides: "SEC-02 OWASP LLM Top-10 → code mitigation mapping"
      contains: "LLM01"
    - path: "infra/compose/.env.example"
      provides: "SEC-05 Phase 11 env vars documented, no secrets"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
    - path: "docs/security/rate-limit-scaling.md"
      provides: "AR-07 Redis distributed limiter documented (not implemented)"
      contains: "RATE_LIMIT_BACKEND"
  key_links:
    - from: "docs/security/STRIDE-threat-model.md"
      to: "packages/sft-knowledge/src/sft_knowledge/retrieval/pipeline.py"
      via: "Information-Disclosure RAG cell maps to acl_level + RESTRICTED_DOC_ACCESS audit"
      pattern: "RESTRICTED_DOC_ACCESS"
    - from: ".env.example"
      to: "packages/sft-agents/src/sft_agents/otel/provider.py"
      via: "OTEL_EXPORTER_OTLP_ENDPOINT consumed by setup_tracer_provider"
      pattern: "OTEL_EXPORTER_OTLP_ENDPOINT"
---

<objective>
Consolidamento sicurezza + documentazione (SEC-01/02/05) e chiusura dei rischi accettati: STRIDE threat model trasversale (6 categorie × 3 superfici, mitigazioni code-mapped, consolida 08/09/10), mapping OWASP LLM Top-10 → mitigazioni concrete (SEC-02 / chiude AR-06), `.env.example` esteso con le var Phase 11 senza secret hardcoded (SEC-05), documentazione del path Redis rate-limiter (AR-07 documentation-only, NON implementato), e annotazione di chiusura AR-01..07 in 10-SECURITY.md.

Purpose: realizza SC-4 (STRIDE ≥1 per categoria × 3 superfici con mitigazione mappata a codice) e chiude i deferral di Phase 9/10.
Output: 3 doc di sicurezza + .env.example + annotazione AR + test di copertura STRIDE.

Execution note: SEQUENZIALE su main tree. Wave 3, dipende da TUTTE le wave precedenti (cita le mitigazioni implementate in 11-01/11-02/11-03 e i datasource/porte di 11-00/11-04). Questo è il piano di chiusura della fase.

Resolutions applicate:
- AR-07 (Redis rate-limiter): DOCUMENTATION-ONLY. Mantenere il limiter in-process Phase 10 + warning multi-worker; documentare il path Redis (`RATE_LIMIT_BACKEND=redis`). NON implementare Redis.
- SEC-02 OWASP: mapping a mitigazioni già implementate (prompt-injection sanitizer SEC-04, acl_level + audit SEC-07, version pin supply chain, recursion_limit, HITL).

Recurring guardrails: nessun secret in `.env.example` (solo placeholder/commenti); citare il secret guard pattern di Phase 10 `jwt.py` (RuntimeError se chiave assente e non-dev) come mitigazione SEC-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-observability-evaluation-security-hardening/11-CONTEXT.md
@.planning/phases/11-observability-evaluation-security-hardening/11-RESEARCH.md
@.planning/phases/11-observability-evaluation-security-hardening/11-00-SUMMARY.md
@.planning/phases/11-observability-evaluation-security-hardening/11-01-SUMMARY.md
@.planning/phases/11-observability-evaluation-security-hardening/11-02-SUMMARY.md
@.planning/phases/11-observability-evaluation-security-hardening/11-03-SUMMARY.md
@.planning/phases/10-backend-api-frontend/10-SECURITY.md

<interfaces>
<!-- STRIDE matrix richiesta (RESEARCH Security Domain): righe = S/T/R/I/D/E, colonne = IT/OT boundary, RAG ingest, Agent orchestration. 18 celle, ciascuna con threat + mitigazione + file:funzione mappata. -->
<!-- Mitigazioni code-mapped da citare: opcua_client subscribe-only (D-51) + AST guard (SEC-06, 11-03); sanitize_document (SEC-04, 11-03); acl_level + RESTRICTED_DOC_ACCESS audit (SEC-07, 11-03); recursion_limit=25 (CORE-03); HITL MOTIVATION_MIN; NatsHeaderCarrier W3C (11-01); MockDeepEvalLLM gate (11-02). -->
<!-- OWASP LLM Top-10 ids: LLM01 Prompt Injection ... LLM10. Mappare ciascuno a mitigazione o "N/A con rationale". -->
<!-- AR-01..AR-07 in 10-SECURITY.md Accepted Risks Log: AR-01 DoS rate-limit, AR-02 SSE token, AR-03 localStorage, AR-04 demo, AR-05 screenshot, AR-06 OWASP, AR-07 multi-worker limiter. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STRIDE threat model 6x3 code-mapped (SEC-01) + test copertura</name>
  <files>docs/security/STRIDE-threat-model.md, docs/security/tests/test_stride_coverage.py</files>
  <action>Creare `docs/security/STRIDE-threat-model.md` con una matrice STRIDE trasversale (RESEARCH Pitfall 7 + Security Domain): per ciascuna delle 6 categorie (Spoofing, Tampering, Repudiation, Information Disclosure, Denial of Service, Elevation of Privilege) e ciascuna delle 3 superfici (IT/OT boundary, RAG ingest, Agent orchestration) — almeno 1 threat con (a) descrizione, (b) mitigazione, (c) riferimento a codice `file:funzione` reale. Consolidare i register per-fase di 08/09/10 SECURITY.md (citarne la provenienza). Usare le mitigazioni implementate in questa fase (sanitize_document, AST OT guard, RESTRICTED_DOC_ACCESS audit, NatsHeaderCarrier, eval gate) e quelle preesistenti (opcua subscribe-only D-51, recursion_limit=25, HITL MOTIVATION_MIN). Creare `docs/security/tests/test_stride_coverage.py` che parsa il Markdown e asserisce che tutte le 18 celle (6×3) siano presenti e ognuna citi almeno un riferimento di codice (pattern `\.py`/`file:`).</action>
  <verify>
    <automated>uv run pytest docs/security/tests/test_stride_coverage.py -x -q</automated>
  </verify>
  <done>STRIDE doc copre 6×3=18 celle, ciascuna con threat + mitigazione + codice mappato; consolida 08/09/10; test di copertura verde (SC-4).</done>
</task>

<task type="auto">
  <name>Task 2: OWASP LLM Top-10 mapping (SEC-02) + Redis rate-limit doc (AR-07) + annotazione AR-01..07</name>
  <files>docs/security/owasp-llm-top10.md, docs/security/rate-limit-scaling.md, .planning/phases/10-backend-api-frontend/10-SECURITY.md</files>
  <action>Creare `docs/security/owasp-llm-top10.md`: per ciascun OWASP LLM Top-10 (LLM01 Prompt Injection … LLM10) mappare a una mitigazione concreta `file:funzione` o "Non applicabile" con rationale. LLM01 → sanitize_document (SEC-04). LLM02 (sensitive info disclosure) → acl_level filter + RESTRICTED_DOC_ACCESS audit (SEC-07). LLM05 (supply chain) → version pin + uv.lock + Package Legitimacy Audit. LLM06 (excessive agency) → recursion_limit=25 + HITL approval chain. Questo chiude AR-06. Creare `docs/security/rate-limit-scaling.md`: documentare il limiter in-process attuale (Phase 10 `_alert_rate_state` + RuntimeWarning se WEB_CONCURRENCY>1) e il path futuro Redis (`RATE_LIMIT_BACKEND=redis`) — DOCUMENTATION-ONLY, NON implementato (chiude AR-07 come documentato). In `10-SECURITY.md` aggiungere una nota di chiusura nella Accepted Risks Log: AR-01 (DoS rate-limit → documentato in rate-limit-scaling.md), AR-02/AR-03 (SSE token/localStorage → restano dev-mode, HttpOnly cookie ancora deferred a milestone futura, annotare lo stato), AR-06 (OWASP → owasp-llm-top10.md), AR-07 (multi-worker → rate-limit-scaling.md). NON modificare i threat CLOSED esistenti; solo aggiungere una colonna/nota di stato Phase 11.</action>
  <verify>
    <automated>python -c "import os; assert os.path.getsize('docs/security/owasp-llm-top10.md')>500; assert os.path.getsize('docs/security/rate-limit-scaling.md')>300; o=open('docs/security/owasp-llm-top10.md').read(); assert 'LLM01' in o and 'LLM06' in o; r=open('docs/security/rate-limit-scaling.md').read(); assert 'RATE_LIMIT_BACKEND' in r and 'redis' in r.lower(); s=open('.planning/phases/10-backend-api-frontend/10-SECURITY.md').read(); assert 'AR-07' in s; print('owasp-ar-ok')"</automated>
  </verify>
  <done>OWASP LLM Top-10 mappato a codice (chiude AR-06); Redis rate-limit documentato non implementato (chiude AR-07); AR-01..07 annotati in 10-SECURITY.md.</done>
</task>

<task type="auto">
  <name>Task 3: .env.example Phase 11 senza secret hardcoded (SEC-05)</name>
  <files>infra/compose/.env.example</files>
  <action>Estendere `infra/compose/.env.example` con una sezione "Phase 11 — Observability & Eval" che documenta (placeholder, NESSUN valore reale): `OTEL_EXPORTER_OTLP_ENDPOINT` (default http://tempo:4317), `OTEL_SERVICE_NAME`, `GRAFANA_PORT` (default 3001), `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY` (placeholder `pk-lf-...`/`sk-lf-...` commentati come da generare), e `EVAL_REAL_LLM` (vuoto = gate deterministico CI). Aggiungere un commento che cita il secret guard pattern di Phase 10 `jwt.py` (RuntimeError se chiave assente e non-dev) come policy SEC-05. NON inserire alcun secret reale; solo placeholder e commenti. NON rimuovere le variabili esistenti.</action>
  <verify>
    <automated>python -c "s=open('infra/compose/.env.example').read(); req=['OTEL_EXPORTER_OTLP_ENDPOINT','GRAFANA_PORT','LANGFUSE_PUBLIC_KEY','LANGFUSE_SECRET_KEY','EVAL_REAL_LLM']; [s.index(k) for k in req]; import re; assert not re.search(r'(SECRET|KEY)\s*=\s*[A-Za-z0-9]{16,}', s), 'possible hardcoded secret'; print('env-example-ok')"</automated>
  </verify>
  <done>.env.example documenta tutte le var Phase 11 con placeholder; nessun secret reale hardcoded; policy SEC-05 citata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env file → runtime config | Secret/endpoint forniti via ambiente, mai hardcoded |
| documentazione → audit/review | Il STRIDE doc è il contratto di sicurezza trasversale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | secret in .env.example | mitigate | Solo placeholder/commenti; assertion regex nel verify; secret guard runtime (jwt.py) citato. |
| T-11-05-02 | Repudiation | threat non mappato a codice | mitigate | test_stride_coverage asserisce riferimento codice in ogni cella (SC-4). |
| T-11-05-03 | Denial of Service | rate-limit multi-worker | accept | Documentato (rate-limit-scaling.md) come AR-07 documentation-only; Redis non implementato in v1.0. |
| T-11-05-04 | Elevation of Privilege | OWASP excessive agency (LLM06) | mitigate | recursion_limit=25 + HITL approval chain citati in owasp-llm-top10.md. |
</threat_model>

<verification>
- pytest test_stride_coverage: 18 celle, ognuna con codice mappato (SC-4).
- owasp-llm-top10.md mappa LLM01..LLM10 (chiude AR-06).
- rate-limit-scaling.md documenta Redis path (chiude AR-07, non implementato).
- .env.example senza secret reali; AR-01..07 annotati in 10-SECURITY.md.
</verification>

<success_criteria>
SEC-01/02/05 chiusi: STRIDE 6×3 code-mapped (SC-4), OWASP LLM Top-10 mappato, secrets documentati senza hardcoding; AR-01..07 di Phase 10 chiusi o documentati (AR-07 Redis documentation-only).
</success_criteria>

<output>
Create `.planning/phases/11-observability-evaluation-security-hardening/11-05-SUMMARY.md` when done.
</output>
