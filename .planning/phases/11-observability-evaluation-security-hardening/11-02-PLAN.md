---
phase: 11-observability-evaluation-security-hardening
plan: 02
type: execute
wave: 2
depends_on: ["11-00"]
files_modified:
  - tests/eval/test_rag_ci_gate.py
  - tests/eval/test_agent_eval.py
  - .github/workflows/ci.yml
autonomous: true
gap_closure: false
requirements: [OBS-05, OBS-06]
must_haves:
  truths:
    - "The RAG CI gate uses RAGAS non-LLM metrics (context_precision/context_recall) + DeepEval with MockDeepEvalLLM — no external LLM call in CI"
    - "The gate BLOCKS when hallucination rate >5% OR answer relevance <0.75 (SC-2), proven by a deliberately-failing fixture case"
    - "The agent eval exercises 30+ synthetic scenarios per cluster from the ground_truth.jsonl built in 11-00"
    - "A CI job runs the eval gate as a pytest step and fails the pipeline on threshold breach"
    - "A real-Ollama eval path exists but is optional/local (skipped in CI, no GPU dependency)"
  artifacts:
    - path: "tests/eval/test_rag_ci_gate.py"
      provides: "Blocking CI gate: RAGAS non-LLM + DeepEval mock"
      contains: "context_precision"
    - path: "tests/eval/test_agent_eval.py"
      provides: "30+ scenario agent eval per cluster"
      contains: "cluster"
    - path: ".github/workflows/ci.yml"
      provides: "eval gate step in CI"
      contains: "tests/eval"
  key_links:
    - from: "tests/eval/test_rag_ci_gate.py"
      to: "tests/eval/conftest.py"
      via: "MockDeepEvalLLM + ground_truth_dataset fixtures"
      pattern: "MockDeepEvalLLM|ground_truth_dataset"
    - from: ".github/workflows/ci.yml"
      to: "tests/eval/test_rag_ci_gate.py"
      via: "pytest step blocks PR on threshold breach"
      pattern: "tests/eval"
---

<objective>
Implementare il gate CI eval DeepEval + RAGAS (OBS-05/06): test deterministici che usano le metriche RAGAS non-LLM (context_precision/context_recall) + DeepEval con `MockDeepEvalLLM` (da 11-00), che BLOCCANO su hallucination >5% o relevance <0.75 (SC-2), più eval agent su 30+ scenari/cluster, e lo step CI corrispondente. Il run real-Ollama resta optional/local.

Purpose: realizza SC-2 (un PR che degrada hallucination/relevance è bloccato automaticamente) senza GPU e in modo riproducibile.
Output: due test eval + step ci.yml; gate dimostrabilmente bloccante.

Execution note: SEQUENZIALE su main tree. Wave 2, dipende da 11-00 (conftest + dataset). File disgiunti da 11-01/11-03: questo piano tocca SOLO tests/eval/* e ci.yml.

Critical findings baked in:
- MockDeepEvalLLM NON deve restituire sempre score=1 (anti-pattern: gate sempre verde). Far variare lo score dal fixture per provare il fail.
- Gate CI = metriche RAGAS non-LLM + DeepEval mock; real-LLM = job locale/opzionale (skip in CI via marker/env).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-observability-evaluation-security-hardening/11-CONTEXT.md
@.planning/phases/11-observability-evaluation-security-hardening/11-RESEARCH.md
@.planning/phases/11-observability-evaluation-security-hardening/11-00-SUMMARY.md
@tests/eval/conftest.py

<interfaces>
<!-- Da 11-00 conftest: -->
fixtures: MockDeepEvalLLM (score variabile da expected_score), ground_truth_dataset (carica tests/eval/dataset/ground_truth.jsonl)
<!-- RAGAS non-LLM (RESEARCH Code Examples): from ragas import evaluate; from ragas.metrics import context_precision, context_recall -->
<!-- DeepEval (RESEARCH): from deepeval import evaluate; from deepeval.metrics import HallucinationMetric, AnswerRelevancyMetric; LLMTestCase -->
<!-- ci.yml: job esistente usa `uv sync --all-packages` + step pytest. Aggiungere step eval con gruppo deps eval di 11-00. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RAG CI gate — RAGAS non-LLM + DeepEval mock, bloccante su SC-2</name>
  <files>tests/eval/test_rag_ci_gate.py</files>
  <behavior>
    - Test 1: `context_precision >= 0.75` e `context_recall >= 0.70` sul dataset golden (RAGAS non-LLM, deterministico).
    - Test 2: hallucination rate calcolato via DeepEval+MockDeepEvalLLM ≤ 5% sul fixture nominale.
    - Test 3 (negative): un fixture deliberatamente degradato (expected_score basso / contexts irrilevanti) fa SALIRE la hallucination rate >5% o relevance <0.75 e il gate FALLISCE — prova che il gate non è sempre-verde.
  </behavior>
  <action>Creare `tests/eval/test_rag_ci_gate.py` (RESEARCH Code Examples): usare `ragas.evaluate` con `context_precision`+`context_recall` (metriche non-LLM, deterministiche) sul `ground_truth_dataset`, assert thresholds (precision≥0.75, recall≥0.70). Usare DeepEval `HallucinationMetric(threshold=0.05, model=mock_llm)` e `AnswerRelevancyMetric(threshold=0.75, model=mock_llm)` con `MockDeepEvalLLM`, calcolare `rate = 1 - passed/total`, assert `rate <= 0.05` e relevance media ≥0.75 (SC-2). Includere un test parametrizzato/negativo che inietta un caso degradato per dimostrare il fallimento del gate. NESSUNA chiamata a LLM esterno. Marcare un eventuale test real-Ollama con `@pytest.mark.skipif(not os.environ.get("EVAL_REAL_LLM"))` così resta locale/opzionale.</action>
  <verify>
    <automated>uv run pytest tests/eval/test_rag_ci_gate.py -x -q</automated>
  </verify>
  <done>Gate verde sul golden; il caso negativo dimostra che il gate fallisce su breach SC-2; nessuna chiamata LLM esterna; real-LLM gated da env.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agent eval 30+ scenari/cluster + step CI</name>
  <files>tests/eval/test_agent_eval.py, .github/workflows/ci.yml</files>
  <behavior>
    - Test 1: il dataset fornisce ≥30 scenari per ciascun cluster presente; il test itera per cluster e applica le metriche.
    - Test 2: aggregato per cluster, le soglie SC-2 reggono sul golden e il test è deterministico (MockDeepEvalLLM).
  </behavior>
  <action>Creare `tests/eval/test_agent_eval.py`: caricare `ground_truth_dataset`, raggruppare per `cluster`, assert che ogni cluster abbia ≥30 scenari (OBS-06), eseguire le metriche deterministiche (RAGAS non-LLM + DeepEval mock) per cluster e applicare le soglie SC-2. In `.github/workflows/ci.yml` aggiungere uno step nominato es. "Run eval CI gate (OBS-05/06)" che, dopo `uv sync --all-packages` (con il gruppo eval installato), esegue `uv run pytest tests/eval/ -x -q`. Lo step deve far fallire la pipeline su breach (nessun `|| true`). NON modificare gli step IT/OT o Nx esistenti — aggiungere solo il nuovo step nella sezione appropriata.</action>
  <verify>
    <automated>uv run pytest tests/eval/test_agent_eval.py -x -q && python -c "import yaml; w=yaml.safe_load(open('.github/workflows/ci.yml')); import json; s=json.dumps(w); assert 'tests/eval' in s, 'eval step missing'; raw=open('.github/workflows/ci.yml').read(); assert '|| true' not in raw and 'continue-on-error' not in raw, 'eval gate must be blocking (no || true / continue-on-error)'; print('ci-eval-step-blocking-ok')"
  </verify>
  <done>Ogni cluster ha ≥30 scenari; soglie SC-2 verdi sul golden; lo step CI esegue il gate eval e blocca su breach.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR diff → CI gate | Un cambiamento RAG/agent attraversa il gate eval prima del merge |
| pip registry → eval deps | deepeval/ragas entrano nel build CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | gate sempre-verde (mock score=1) | mitigate | MockDeepEvalLLM con score variabile da fixture; test negativo dimostra il fallimento del gate (anti-pattern RESEARCH evitato). |
| T-11-02-02 | Denial of Service | eval che chiama LLM esterno in CI | mitigate | Solo metriche RAGAS non-LLM + mock; real-Ollama gated da env `EVAL_REAL_LLM` (skip in CI). |
| T-11-02-SC | Tampering | deepeval/ragas install | mitigate | Vettati in 11-RESEARCH (PyPI Approved); pin versione; gruppo dev/test (non runtime). |
</threat_model>

<verification>
- pytest tests/eval/ verde sul golden; caso negativo prova il blocco.
- ci.yml contiene lo step eval che fallisce su breach.
- Nessuna chiamata LLM esterna in CI.
</verification>

<success_criteria>
Il gate CI eval blocca i PR che violano SC-2 (hallucination >5% o relevance <0.75) in modo deterministico e senza GPU; eval agent copre 30+ scenari/cluster.
</success_criteria>

<output>
Create `.planning/phases/11-observability-evaluation-security-hardening/11-02-SUMMARY.md` when done.
</output>
