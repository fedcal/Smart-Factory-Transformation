---
phase: 11-observability-evaluation-security-hardening
plan: 04
type: execute
wave: 3
depends_on: ["11-00", "11-01"]
files_modified:
  - infra/grafana/dashboards/agent-kpis.json
  - infra/grafana/dashboards/factory-kpis.json
  - infra/grafana/dashboards/cost-dashboard.json
  - docs/observability/lgtm-stack.md
  - infra/grafana/tests/test_dashboards_valid.py
autonomous: true
gap_closure: false
requirements: [OBS-03, OBS-04, OBS-07]
must_haves:
  truths:
    - "Grafana auto-loads three provisioned dashboards (agent KPIs, factory KPIs, cost) from infra/grafana/dashboards via the dashboards.yaml provider"
    - "The agent KPI dashboard shows latency p50/p95/p99 + token count per agent (OBS-04/07)"
    - "The factory KPI dashboard shows OEE / MTTR / MTBF / scrap (OBS-04)"
    - "The cost dashboard shows simulated cost + token consumption per agent + latency percentiles (OBS-07)"
    - "The LGTM stack (Loki/Grafana/Tempo/Prometheus) is documented as optional (OBS-03)"
  artifacts:
    - path: "infra/grafana/dashboards/agent-kpis.json"
      provides: "agent KPI dashboard (latency p50/p95/p99 + tokens)"
      contains: "p95"
    - path: "infra/grafana/dashboards/factory-kpis.json"
      provides: "factory KPI dashboard (OEE/MTTR/MTBF/scrap)"
      contains: "OEE"
    - path: "infra/grafana/dashboards/cost-dashboard.json"
      provides: "cost dashboard (simulated cost + token per agent)"
      contains: "cost"
    - path: "docs/observability/lgtm-stack.md"
      provides: "OBS-03 LGTM stack documentation"
      min_lines: 20
  key_links:
    - from: "infra/grafana/provisioning/dashboards/dashboards.yaml"
      to: "infra/grafana/dashboards/agent-kpis.json"
      via: "Grafana dashboard provider loads JSON from mounted volume"
      pattern: "dashboards"
    - from: "infra/grafana/dashboards/agent-kpis.json"
      to: "Prometheus"
      via: "PromQL queries on agent latency/token metrics"
      pattern: "datasource"
---

<objective>
Dashboard Grafana preconfezionate (OBS-04/07) + documentazione LGTM (OBS-03): tre dashboard JSON versionate (agent KPIs con latency p50/p95/p99 + token, factory KPIs con OEE/MTTR/MTBF/scrap, cost dashboard con costo simulato + token/agente), caricate automaticamente dal provider provisioning creato in 11-00, più un doc LGTM stack.

Purpose: realizza OBS-04 (dashboard agenti+factory) e OBS-07 (cost dashboard p50/p95/p99). OBS-03 documentato come opzionale.
Output: 3 dashboard JSON + doc LGTM + test di validità JSON/schema.

Execution note: SEQUENZIALE su main tree. Wave 3, dipende da 11-00 (provisioning + compose) e 11-01 (metriche/trace agent disponibili per query). File disgiunti dagli altri piani.

Critical findings baked in:
- Le metriche budget esistono in `sft_agents/models/budget.py`; ot-bridge espone già `metrics.py` (prometheus-client). Le dashboard interrogano Prometheus/Tempo via i datasource provisioned in 11-00.
- Dashboard JSON generato preferibilmente via export Grafana UI (schema-compatibile) e committato; in CI senza UI, autorare JSON conforme allo schema Grafana v11 e validarlo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-observability-evaluation-security-hardening/11-CONTEXT.md
@.planning/phases/11-observability-evaluation-security-hardening/11-RESEARCH.md
@.planning/phases/11-observability-evaluation-security-hardening/11-00-SUMMARY.md
@.planning/phases/11-observability-evaluation-security-hardening/11-01-SUMMARY.md

<interfaces>
<!-- Da 11-00: infra/grafana/provisioning/dashboards/dashboards.yaml punta a /var/lib/grafana/dashboards; datasources.yaml fornisce Prometheus (default) + Tempo. -->
<!-- Metriche disponibili: ot-bridge metrics.py (prometheus-client); sft_agents/models/budget.py (token/cost). Dashboard JSON v11 con sezione "panels", "targets" PromQL, "datasource" uid. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard agent-kpis + factory-kpis JSON (OBS-04)</name>
  <files>infra/grafana/dashboards/agent-kpis.json, infra/grafana/dashboards/factory-kpis.json</files>
  <action>Creare `agent-kpis.json` (schema Grafana v11): pannelli per latency p50/p95/p99 per agente (PromQL `histogram_quantile(0.50|0.95|0.99, ...)` sul histogram di latenza agent) + token count per agente (sui counter budget). Creare `factory-kpis.json`: pannelli OEE, MTTR, MTBF, scrap rate (PromQL sulle metriche factory/ot-bridge). Ogni pannello referenzia il datasource Prometheus per uid (provisioned in 11-00). Mantenere JSON valido e conforme allo schema (campi `title`, `panels`, `targets`, `templating` opzionale, `schemaVersion`). Se possibile generare via export UI Grafana (`docker compose -f obs.yml up -d grafana`) e committare l'export; altrimenti autorare manualmente JSON conforme.</action>
  <verify>
    <automated>python -c "import json; [json.load(open(f)) for f in ['infra/grafana/dashboards/agent-kpis.json','infra/grafana/dashboards/factory-kpis.json']]; a=json.load(open('infra/grafana/dashboards/agent-kpis.json')); assert any('p95' in json.dumps(p) or '0.95' in json.dumps(p) for p in a.get('panels',[a])), 'p95 missing'; fk=json.load(open('infra/grafana/dashboards/factory-kpis.json')); assert 'OEE' in json.dumps(fk), 'OEE missing'; print('dash-agent-factory-ok')"</automated>
  </verify>
  <done>Entrambi i JSON sono validi e conformi; agent-kpis ha latency p50/p95/p99 + token; factory-kpis ha OEE/MTTR/MTBF/scrap; referenziano il datasource Prometheus.</done>
</task>

<task type="auto">
  <name>Task 2: Cost dashboard JSON (OBS-07) + doc LGTM (OBS-03) + test validità</name>
  <files>infra/grafana/dashboards/cost-dashboard.json, docs/observability/lgtm-stack.md, infra/grafana/tests/test_dashboards_valid.py</files>
  <action>Creare `cost-dashboard.json` (schema Grafana v11): pannelli costo simulato totale + token consumati per agente + latency p50/p95/p99 per agente (OBS-07), PromQL sui counter/histogram budget. Creare `docs/observability/lgtm-stack.md` documentando lo stack LGTM (Loki+Grafana+Tempo+Mimir/Prometheus) come OPZIONALE: avvio via `infra/compose/obs.yml`, porte (Grafana 3001, Tempo OTLP 4317, Prometheus 9090), datasource provisioned, e come le trace OTEL (da 11-01) raggiungono Tempo. Creare `test_dashboards_valid.py` che (a) carica tutti i `*.json` in infra/grafana/dashboards, asserisce JSON valido + presenza `panels`/`title`/`schemaVersion`; (b) asserisce che ogni dashboard referenzi un datasource provisioned. Verificare che il provider dashboards.yaml (11-00) punti alla cartella corretta.</action>
  <verify>
    <automated>uv run pytest infra/grafana/tests/test_dashboards_valid.py -x -q && python -c "import json; c=json.load(open('infra/grafana/dashboards/cost-dashboard.json')); assert 'cost' in json.dumps(c).lower(); import os; assert os.path.getsize('docs/observability/lgtm-stack.md')>400; print('cost-lgtm-ok')"</automated>
  </verify>
  <done>cost-dashboard valido (costo + token/agente + latency percentili); doc LGTM presente (OBS-03 opzionale); test di validità verde su tutte le dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compose host port 3001 → Grafana | Dashboard esposte in dev |
| Grafana → Prometheus/Tempo | Query read-only sui datasource |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | Grafana anonymous viewer | accept | GF_AUTH_ANONYMOUS solo dev (Viewer); read-only; nessun dato PII reale (metriche aggregate). Documentato in 11-05 .env.example. |
| T-11-04-02 | Tampering | dashboard JSON schema drift | mitigate | test_dashboards_valid asserisce JSON valido + schemaVersion + datasource provisioned. |
| T-11-04-03 | Denial of Service | query PromQL pesanti | accept | Dashboard dev su finestre temporali limitate; non esposte in prod. |
</threat_model>

<verification>
- pytest test_dashboards_valid verde su tutte e 3 le dashboard.
- agent-kpis (p50/p95/p99 + token), factory-kpis (OEE/MTTR/MTBF/scrap), cost-dashboard (costo + token/agente).
- doc LGTM presente (OBS-03).
</verification>

<success_criteria>
Grafana carica automaticamente tre dashboard valide che espongono KPI agenti, KPI factory e cost (OBS-04/07); lo stack LGTM è documentato come opzionale (OBS-03).
</success_criteria>

<output>
Create `.planning/phases/11-observability-evaluation-security-hardening/11-04-SUMMARY.md` when done.
</output>
