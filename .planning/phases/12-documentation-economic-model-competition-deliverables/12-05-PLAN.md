---
phase: 12-documentation-economic-model-competition-deliverables
plan: 05
type: execute
wave: 6
depends_on: ["12-00", "12-01", "12-02a", "12-02b", "12-03a", "12-03b", "12-04"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/docs-deploy.yml
  - docs/docs/index.md
autonomous: true
gap_closure: false
requirements: [DEL-08, DOC-02, DOC-03, DOC-15]
must_haves:
  truths:
    - "A blocking brand-scrub CI gate in ci.yml fails on any case-insensitive occurrence of the brand across tracked files EXCLUDING .planning/, with the search term built in parts to avoid self-match (DEL-08, SC-4)"
    - "The same gate (or a sibling step) asserts zero binary image files (png/jpg/jpeg/svg) under docs/docs/ (SC-5)"
    - "The .planning/ exclusion rationale is documented in the gate (internal GSD meta, not a deliverable surface)"
    - "docs-deploy.yml deploys versioned docs via mike (main + latest aliases) alongside the existing strict build (DOC-02/DOC-03)"
    - "Final `mkdocs build --strict` is green and the brand-scrub gate passes on the current tree (verified)"
    - "SC-3 traceability is reviewed: every deliverable section traces to shipped code/prior-phase SUMMARY (no aspirational content)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "DEL-08 brand-scrub blocking gate + SC-5 binary-image assertion"
      contains: "ls-files"
    - path: ".github/workflows/docs-deploy.yml"
      provides: "DOC-03 mike versioning deploy"
      contains: "mike"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tracked files (git ls-files) excluding .planning/"
      via: "grep case-insensitive on brand term built from parts"
      pattern: "ls-files"
    - from: ".github/workflows/docs-deploy.yml"
      to: "mike"
      via: "mike deploy --push --update-aliases"
      pattern: "mike"
---

<objective>
Chiudere la fase (DEL-08/SC-4 + DOC-02/03 + SC-5): aggiungere il gate brand-scrub BLOCKING in `ci.yml` (grep case-insensitive zero occorrenze sul brand su file tracciati ESCLUSO `.planning/`, termine costruito a parti per evitare self-match) + asserzione SC-5 zero immagini binarie sotto docs/docs/; aggiornare `docs-deploy.yml` con il versioning `mike` (main + latest) accanto al build strict esistente; eseguire il build strict finale + verifica che il gate passi; review SC-3 (ogni deliverable traccia al codice).

Purpose: realizza DEL-08 (zero brand verificato in CI), DOC-02/03 (deploy + mike versioning), SC-5 (zero immagini binarie), e chiude SC-3.
Output: ci.yml con gate brand-scrub+SC-5, docs-deploy.yml con mike, build finale verde, review SC-3.

Execution note: SEQUENZIALE su main tree. Wave 6; dipende da TUTTE le wave precedenti (il gate deve girare su un albero già pulito: 12-03b/12-04 hanno rimosso il brand e i PNG). Piano di chiusura della fase e del progetto.

Critical research findings baked in (RESEARCH Pitfall 5/6 + Pattern 2):
- Self-match: costruire il termine a parti (`T="accent"; T+="ure"`) così il grep che legge ci.yml non trova il valore letterale; escludere comunque `.planning/` via `git ls-files | grep -v '^\.planning/'`.
- Usare `git ls-files` (NON l'intero filesystem) per coprire solo i file tracciati.
- Documentare nel gate il rationale dell'esclusione `.planning/` (meta GSD interno, non superficie deliverable).
- `site/` è in `.gitignore` (non tracciato) → il gate copre i sorgenti, non l'output generato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-documentation-economic-model-competition-deliverables/12-CONTEXT.md
@.planning/phases/12-documentation-economic-model-competition-deliverables/12-RESEARCH.md
@.planning/phases/12-documentation-economic-model-competition-deliverables/12-00-SUMMARY.md
@.planning/phases/12-documentation-economic-model-competition-deliverables/12-01-SUMMARY.md
@.planning/phases/12-documentation-economic-model-competition-deliverables/12-04-SUMMARY.md
@.github/workflows/ci.yml
@.github/workflows/docs-deploy.yml

<interfaces>
<!-- Brand-scrub gate (RESEARCH Code Examples) — step nel job `main` di ci.yml: -->
<!--   TERM="accent"; TERM+="ure"
       MATCHES=$(git ls-files | grep -v '^\.planning/' | xargs grep -ril "$TERM" 2>/dev/null || true)
       [ -n "$MATCHES" ] && { printf '%s\n' "$MATCHES"; exit 1; } ; echo OK   -->
<!-- SC-5 step: BIN=$(git ls-files 'docs/docs/**' | grep -Ei '\.(png|jpe?g|svg|gif|webp)$' || true); [ -n "$BIN" ] && exit 1 -->
<!-- mike (RESEARCH Code Examples) — step in docs-deploy.yml dopo build strict:
       git config user.name/email; mike deploy --push --update-aliases main latest; mike set-default --push latest (working-directory: docs/) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate brand-scrub (DEL-08/SC-4) + asserzione SC-5 in ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>Aggiungere al job `main` di `ci.yml` (dopo Checkout, prima o accanto agli altri gate) DUE step blocking: (1) "Brand-scrub gate (DEL-08 / SC-4)" — costruire il termine a parti (`TERM="accent"; TERM+="ure"`) per evitare self-match (RESEARCH Pitfall 5), poi `MATCHES=$(git ls-files | grep -v '^\.planning/' | xargs grep -ril "$TERM" 2>/dev/null || true)`; se non vuoto, stampare i file e `exit 1`; includere un commento che documenta il rationale dell'esclusione `.planning/` (meta GSD interno, non superficie deliverable — 12-CONTEXT gray area 3). (2) "No binary images in docs/ (SC-5)" — `BIN=$(git ls-files 'docs/docs/*' | grep -Ei '\.(png|jpe?g|svg|gif|webp)$' || true)`; se non vuoto, stampare ed `exit 1`. (3) "Personal-ref scrub (12-CONTEXT addendum)" — `P=$(git ls-files | grep -v '^\.planning/' | xargs grep -ril -E 'fedcal|federicocalo' 2>/dev/null || true)`; se non vuoto, `exit 1` (riferimenti personali già neutralizzati a `smart-factory-transformation`). (4) "Local navigability" — assert `docs/mkdocs.yml` contiene `use_directory_urls: false`. Tutti gli step blocking, girano su PR e push (il job `main` già lo fa). NON rimuovere step esistenti.</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "import yaml; w=yaml.safe_load(open('.github/workflows/ci.yml')); steps=w['jobs']['main']['steps']; names=[s.get('name','') for s in steps]; assert any('rand-scrub' in n for n in names), 'no brand-scrub step'; assert any('inary' in n or 'SC-5' in n for n in names), 'no SC-5 step'; print('ci-gates-present')" && TERM="accent"; TERM+="ure"; M=$(git ls-files | grep -v '^\.planning/' | xargs grep -ril "$TERM" 2>/dev/null || true); test -z "$M" && echo "brand-scrub-passes-on-tree" || { echo "BRAND LEAK: $M"; exit 1; } && BIN=$(git ls-files 'docs/docs/*' | grep -Ei '\.(png|jpe?g|svg|gif|webp)$' || true); test -z "$BIN" && echo "no-binary-images" || { echo "BIN: $BIN"; exit 1; } && P=$(git ls-files | grep -v '^\.planning/' | xargs grep -ril -E 'fedcal|federicocalo' 2>/dev/null || true); test -z "$P" && echo "no-personal-refs" || { echo "PERSONAL LEAK: $P"; exit 1; } && grep -q 'use_directory_urls: false' docs/mkdocs.yml && echo "local-nav-ok"
  </verify>
  <done>ci.yml ha 2 step blocking (brand-scrub con termine a parti + esclusione .planning/ documentata; SC-5 zero immagini binarie sotto docs/docs/); entrambi passano sull'albero corrente; nessuno step esistente rimosso.</done>
</task>

<task type="auto">
  <name>Task 2: mike versioning in docs-deploy.yml (DOC-02/03)</name>
  <files>.github/workflows/docs-deploy.yml</files>
  <action>Aggiornare `docs-deploy.yml`: mantenere lo step `mkdocs build --strict` esistente; sostituire/affiancare `mkdocs gh-deploy --force --clean` con il deploy versionato `mike` (RESEARCH Code Examples) — step "Deploy versioned docs (mike)": `git config user.name "github-actions[bot]"` + email, `mike deploy --push --update-aliases main latest`, `mike set-default --push latest`, con `working-directory: docs/`. Assicurarsi che `mike` sia installato (è in `docs/requirements.txt` da 12-00; lo step di install pip deve includerlo). Mantenere `permissions: contents: write`. Documentare il custom domain come opzionale (commento) — NON configurarlo (deferred). NON rompere il trigger esistente.</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "w=open('.github/workflows/docs-deploy.yml').read(); assert 'mike' in w and 'mkdocs build --strict' in w; assert 'update-aliases' in w or 'set-default' in w, 'no mike deploy'; print('docs-deploy-mike-ok')" && grep -q "mike==2.2.0" docs/requirements.txt</automated>
  </verify>
  <done>docs-deploy.yml usa mike (main+latest aliases) accanto al build strict; mike installato da requirements; custom domain documentato come opzionale; trigger intatto.</done>
</task>

<task type="auto">
  <name>Task 3: Build strict finale + review SC-3 traceability</name>
  <files>docs/docs/index.md</files>
  <action>Eseguire il build strict finale dell'intero sito. Fare una review SC-3 (RESEARCH Codebase State Audit): verificare che ogni sezione deliverable (architettura, funzionale, casi d'uso, roadmap, economica, security, ADR, ui-mock, transformation) tracci a codice spedito / SUMMARY di fase, senza contenuto aspirazionale. Aggiornare `docs/docs/index.md` (home) per linkare le nuove sezioni deliverable (Analisi Funzionale, Casi d'Uso, Roadmap, Analisi Economica, Security, ADR, Trasformazione) come indice dei deliverable di concorso, e ribadire che le metriche sono SIMULATED TARGET. Annotare nel SUMMARY l'esito della review SC-3 (lista sezione→evidence). Eseguire un'ultima passata del gate brand-scrub + SC-5 localmente (come Task 1) per confermare verde end-to-end. Rigenerare lo script economico (`python3 docs/economic-analysis/tco_oepv.py`) e confermare che i CSV committed non cambiano (no drift).</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 docs/economic-analysis/tco_oepv.py && git diff --quiet docs/economic-analysis/tco_table.csv docs/economic-analysis/sensitivity_table.csv && (cd docs && python3 -m mkdocs build --strict) && python3 -c "h=open('docs/docs/index.md').read().lower(); assert 'economic' in h or 'economica' in h; print('home-links-ok')"</automated>
  </verify>
  <done>Build strict finale verde; CSV economici senza drift; home linka le sezioni deliverable; review SC-3 documentata nel SUMMARY (ogni sezione→evidence); gate brand-scrub+SC-5 verdi end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked files → brand-scrub gate | Il gate scansiona tutti i file tracciati non-.planning/ su PR/push |
| docs/ → SC-5 gate | Nessuna immagine binaria deve esistere sotto docs/docs/ |
| CI runner → gh-pages | mike deploy spinge il sito versionato sul branch gh-pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | brand iniettato via PR su file non coperto | mitigate | `git ls-files` copre TUTTI i file tracciati (escluso .planning/); gate gira su PR e push; blocking. |
| T-12-05-02 | Repudiation | gate falso-positivo su se stesso (ci.yml) | mitigate | Termine costruito a parti (`accent`+`ure`); il grep non trova il valore letterale nel workflow. |
| T-12-05-03 | Tampering | immagine binaria reintrodotta sotto docs/ | mitigate | Step SC-5 asserisce zero png/jpg/svg sotto docs/docs/ via git ls-files; blocking. |
| T-12-05-04 | Repudiation | contenuto aspirazionale non implementato | mitigate | Review SC-3 sezione→evidence documentata nel SUMMARY; Codebase State Audit come riferimento. |
</threat_model>

<verification>
- ci.yml: gate brand-scrub (termine a parti, esclude .planning/ con rationale) + SC-5 binary-image, entrambi blocking e verdi sull'albero.
- docs-deploy.yml: mike (main+latest) + build strict; mike in requirements.
- Build strict finale verde; CSV economici senza drift; home linka i deliverable; review SC-3 documentata.
</verification>

<success_criteria>
DEL-08/SC-4 + DOC-02/03 + SC-5 + SC-3 chiusi: gate brand-scrub blocking (zero occorrenze, .planning escluso con rationale), zero immagini binarie in docs/, deploy versionato mike, build strict verde, e ogni deliverable tracciato al codice spedito. Fase finale completata.
</success_criteria>

<output>
Create `.planning/phases/12-documentation-economic-model-competition-deliverables/12-05-SUMMARY.md` when done. Includere la review SC-3 (tabella sezione→evidence) e l'esito del gate brand-scrub/SC-5 sull'albero corrente.
</output>
