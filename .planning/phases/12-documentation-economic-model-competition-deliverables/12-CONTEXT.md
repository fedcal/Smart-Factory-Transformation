# Phase 12: Documentation, Economic Model & Competition Deliverables - Context

**Gathered:** 2026-05-25
**Status:** Ready for planning
**Mode:** Interactive discuss (4 gray areas resolved with user)

<domain>
## Phase Boundary

Final phase: complete bilingual MkDocs Material site (DOC-01..18) deployed to GitHub Pages; complete & defensible OEPV economic model + TCO + ribasso simulator (ECO-01..08; ECO-02/05 already done in Phase 9 — finalize the legal/deliverable precision deferred from F9); the 6 competition deliverables (DEL-01..08); brand-scrub CI (DEL-08/SC-4); all diagrams as Mermaid/D2 text (DOC-15/SC-5).

This is a documentation + economic-model + deliverable-bundling phase. The UI was built in Phase 10 — here it is documented, not rebuilt. No ui-phase.
</domain>

<decisions>
## Implementation Decisions

### Economic model reproducibility (gray area 1) — LOCKED
Reproducible Python script/notebook + generated tables.
- `docs/economic-analysis/` contains a runnable Python script/notebook that, from configurable parameters (Base d'Asta €108,000; GPU amortization over 3 years; electricity 0.25 EUR/kWh under continuous inference; 1 FTE partial ops allocation; ribasso 10-15% below base with written justification), generates: 3-year TCO, OEPV 70/30 scoring, and the non-linear sensitivity analysis table (SC-2). Output CSV + markdown tables committed.
- REUSE the Phase 9 parametric OEPV simulator at `apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/oepv.py` (do not re-derive the formula).
- Finalize the F9-deferred ribasso anomaly-threshold (Codice Appalti): document the threshold + rationale; if a precise formula is used, cite the source.
- **Why:** defensible, reproducible (ECO-08), single source of truth for the numbers.

### Value-driver numbers (gray area 2) — LOCKED
BOTH synthetic Mantis baseline AND literature benchmarks with citations.
- Value drivers (downtime/scrap/MTTR/training-time/knowledge-reuse reduction — ECO-04) computed from the synthetic Mantis baseline + simulator KPIs (Phase 9), cross-referenced against cited industry-4.0 textile literature ranges.
- Every assumption stated in an Assumption Register (DOC-12/DEL-07); improvement percentages framed as SIMULATED TARGETS, not promises (avoid over-claim per SC-3).
- **Why:** grounded in the actual system AND externally credible.

### Brand-scrub CI (gray area 3) — LOCKED
"Accenture" (case-insensitive), zero occurrences across tracked deliverable files, BLOCKING CI gate.
- CI grep gate: zero case-insensitive "accenture" across tracked files (docs/, source, README, configs, generated docs). Blocking.
- **IMPORTANT scope nuance:** "Accenture" currently appears ONLY in `.planning/` (the internal GSD planning meta-layer that records the original competition track). `.planning/` is NOT a deliverable surface. The plan must decide: either (a) exclude `.planning/` from the gate with a documented rationale (it is internal meta, not shipped docs), or (b) scrub `.planning/` too. Recommended: gate covers all NON-`.planning/` tracked files (the actual repo/deliverable surface); `.planning/` is internal and excluded with a one-line justification in the gate.
- DOC-17 (document the transformation from the original track) MUST describe the transformation WITHOUT naming the brand (e.g. "the original competition track / challenge") so the gate stays green.
- **Why:** satisfies SC-4 zero-occurrence enforcement on the shipped surface without falsifying the planning history.

### Diagrams & screenshots (gray area 4) — LOCKED
All diagrams Mermaid/D2 (text); user-journey as Mermaid; UI screenshots EXCLUDED from docs/.
- Architecture (C4 context/container/component — DOC-04), data-flow, end-to-end workflows, user journeys: Mermaid or D2 source committed as text (DOC-15/SC-5).
- NO binary diagram images in docs/. The Phase 10 UI screenshots (PNGs from UI-09) must NOT live in docs/ — Mock UI/User Journey (DOC-08) uses Mermaid flow diagrams + component references; any actual screenshots live under a non-docs artifacts/ path (or are regenerated by the Playwright screenshot spec on demand, outside docs/).
- **Why:** satisfies SC-5 (zero binary diagram images) literally.

### Carried forward / cross-cutting (NOT re-discussed)
- Reuse existing `docs/` + `docs/mkdocs.yml` (already extended through Phases 5/8/9/10), the bilingual IT/EN layout (docs/docs/... + docs/docs/en/...), and `mkdocs-static-i18n`.
- Consolidate prior-phase docs: knowledge/supply cluster docs, security (STRIDE/OWASP from Phase 11), eval results (Phase 5/11), UI mock (Phase 10).
- DEL-01..06 map to docs sections: Target Architecture (DOC-04), End-to-End Workflows (DOC-06), Use Cases (DOC-07), Mock UI/User Journey (DOC-08), Adoption Roadmap (DOC-09), Economic Evaluation (DOC-10). SC-3: no aspirational content not implemented in code — each deliverable claim must trace to shipped code.
- DOC-13 ADRs, DOC-14 README, DOC-16 CONTRIBUTING/CODE_OF_CONDUCT/LICENSE(Apache 2.0), DOC-18 bilingual glossary.
- DOC-02/03: GitHub Actions build + deploy to gh-pages; `mike` versioning.
- Execution: worktrees DISABLED — sequential on main tree. Python deps via uv (NEVER npm for Python libs). Apply review guardrails.
</decisions>

<code_context>
## Existing Code Insights
- `docs/` + `docs/mkdocs.yml` — MkDocs Material + i18n, already has agents/architecture/domain/security/eval/assumptions/en sections.
- `apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/oepv.py` — Phase 9 parametric OEPV simulator (70/30 + non-linear curve + sensitivity) to REUSE for the economic model.
- Phase 11 docs/security/{STRIDE-threat-model.md, owasp-llm-top10.md} — feed DOC-11 Security & Governance.
- Phase 5/11 eval docs; Phase 10 docs/ui-mock + the Playwright screenshot spec.
- "Accenture" appears only in `.planning/*` (internal meta) — NOT in shipped docs/source.
</code_context>

<canonical_refs>
## Canonical References (downstream agents MUST read)
- `.planning/REQUIREMENTS.md` — DOC-01..18, ECO-01..08, DEL-01..08.
- `.planning/ROADMAP.md` — Phase 12 goal + 5 success criteria.
- `apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/oepv.py` — OEPV simulator to reuse.
- `docs/mkdocs.yml` + existing docs/ sections to consolidate.
- Prior phase SUMMARY/SECURITY/VERIFICATION files — source of truth for "what is actually implemented" (SC-3 no-aspirational-content traceability).
</canonical_refs>

<user_addendum>
## User Addendum (2026-05-25) — LOCKED
1. **Local navigability:** the documentation site MUST be navigable locally — `docs/mkdocs.yml` sets `use_directory_urls: false` so the built `site/` is browsable directly via `file://` (no web server) and `mkdocs serve` works offline. The 12-05 final-build gate must assert `use_directory_urls: false` is present and that the built site opens without a server.
2. **No personal references:** every reference to the maintainer's personal domain/handle/email has been scrubbed — `federicocalo.dev` (never present), `fedcal` (GitHub handle in mkdocs site_url/repo_url/links + docs content + helm charts + package.json), and the personal email `fedcal01@gmail.com` are ALL replaced with neutral org placeholders (`smart-factory-transformation`, `team@smart-factory-transformation.example`). The 12-05 brand-scrub CI gate MUST extend the zero-occurrence check to `fedcal` and `federicocalo` (case-insensitive) over tracked non-`.planning` files, alongside the `accenture` check, all blocking.
</user_addendum>

<deferred>
## Deferred Ideas
- Custom domain for GitHub Pages (DOC-03 "optional") — document, optional.
- Real-LLM/live-stack screenshot regeneration — human/CI item.
</deferred>
