---
phase: 02-domain-modeling-synthetic-corpus
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docs/docs/domain/index.md
  - docs/docs/domain/processes/weaving.md
  - docs/docs/domain/processes/spinning.md
  - docs/docs/domain/processes/warping.md
  - docs/docs/domain/processes/dyeing.md
  - docs/docs/domain/processes/finishing.md
  - docs/docs/domain/roles/operator.md
  - docs/docs/domain/roles/technician.md
  - docs/docs/domain/roles/quality-manager.md
  - docs/docs/domain/roles/shift-supervisor.md
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_domain_pages.py
autonomous: true
requirements: [DOC-05]
must_haves:
  truths:
    - "docs/docs/domain/index.md exists with overview + high-level process flow"
    - "Five process pages exist under docs/docs/domain/processes/ (weaving, spinning, warping, dyeing, finishing) — IT body"
    - "Four role pages exist under docs/docs/domain/roles/ (operator, technician, quality-manager, shift-supervisor) — IT body"
    - "Every process page contains a Mermaid flowchart LR block with accDescr (accessibility)"
    - "Every process page contains H2 sections: Process flow, Asset coinvolti, KPI, Pain point"
    - "Every role page contains H2 sections: Responsabilità, Interazione tipica, Decisione critica giornaliera, Pain point"
    - "Every domain page contains exactly one `!!! note \"Mantis context\"` admonition (D-23)"
  artifacts:
    - path: "docs/docs/domain/index.md"
      provides: "Domain overview + high-level process flow diagram"
      min_lines: 30
    - path: "docs/docs/domain/processes/weaving.md"
      provides: "Weaving process page (flow + asset + KPI + pain point + Mantis context)"
      contains: "flowchart LR"
    - path: "docs/docs/domain/processes/dyeing.md"
      provides: "Dyeing process page"
      contains: "flowchart LR"
    - path: "tests/test_domain_pages.py"
      provides: "pytest structural assertions on domain page H2 sections + Mermaid presence + Mantis admonition"
      contains: "test_process_sections"
  key_links:
    - from: "docs/docs/domain/processes/*.md"
      to: "mermaid flowchart fence"
      via: "pymdownx.superfences custom_fence (mkdocs.yml line 69-73)"
      pattern: "```mermaid"
    - from: "tests/test_domain_pages.py"
      to: "docs/docs/domain/processes/"
      via: "pathlib.Path.rglob"
      pattern: "rglob.*\\.md"
---

<objective>
Author the Italian-language Domain Analysis content (DOC-05) per D-21 split: 1 index + 5 process pages + 4 role pages = 10 pages total. Each process page follows D-22 structure (process flow Mermaid + asset + KPI + pain point); each role page covers responsabilità + interazione tipica + decisione critica + pain point. Every page carries a `!!! note "Mantis context"` admonition (D-23). pytest structural tests assert sections + Mermaid + admonition presence — this is the test scaffold Plan 06 will wire into CI.

EN translation is deferred to Plan 05 (Wave 3) to keep this plan within ~50% context — Plan 02 ships IT only and parks the EN scaffolding.

Purpose: DOC-05 requires the textile domain to be modeled in `docs/`. D-21 split provides retrieval-grain pages that Phase 5 (RAG/BGE-M3) can chunk-index naturally.

Output: 10 IT-only Markdown pages + 1 pytest test module that downstream plans (Plan 06 CI wiring) will run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-CONTEXT.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-RESEARCH.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-PATTERNS.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-01-PLAN.md
@docs/docs/architecture/overview.md
@docs/docs/getting-started.md
@docs/mkdocs.yml

<interfaces>
<!-- The 10 page files must obey a shared structural contract enforced by tests/test_domain_pages.py. -->

Process page H2 contract (IT):
- `## Process flow` (Mermaid flowchart LR follows immediately, max 8 nodes, with `accDescr:` first line)
- `## Asset coinvolti` (bullet list — family, esempio modello tipico, parametri operativi indicativi)
- `## KPI` (3-5 KPI bullets — name, unità, range tipico, formula breve)
- `## Pain point` (3-5 narrative bullets — esempio concreto + impatto operativo)

Role page H2 contract (IT):
- `## Responsabilità`
- `## Interazione tipica con asset e processi`
- `## Decisione critica giornaliera`
- `## Pain point`

Admonition (every page, exactly one):
- `!!! note "Mantis context"` callout, 2-4 lines, mentioning yarn types / market segment / shift pattern relevant to that process/role.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 process pages + index (IT)</name>
  <files>docs/docs/domain/index.md, docs/docs/domain/processes/weaving.md, docs/docs/domain/processes/spinning.md, docs/docs/domain/processes/warping.md, docs/docs/domain/processes/dyeing.md, docs/docs/domain/processes/finishing.md</files>
  <read_first>
    - docs/docs/architecture/overview.md (lines 1-30 — voice + Mermaid pattern; pattern source per 02-PATTERNS.md lines 662-688)
    - docs/docs/getting-started.md (lines 1-20 — Italian voice + admonition pattern)
    - docs/mkdocs.yml (lines 60-75 — confirm `pymdownx.superfences` custom_fence `mermaid` + `admonition`)
    - D-21, D-22, D-23, D-28 in 02-CONTEXT.md
    - 02-RESEARCH.md § Pitfall 2 Mermaid limits (lines 709-718) — `accDescr` mandate, ≤8 nodes
    - 02-PATTERNS.md § "Domain pages" (lines 660-689)
  </read_first>
  <action>
    For each page use Italian standard (no dialect; Mantis-context admonition may use Lombardy industrial jargon if applicable per D-claudes_discretion). Pattern source: 02-PATTERNS.md lines 662-688 (per D-22 layout) and per RESEARCH.md Pitfall 2 (`accDescr:` mandatory).

    docs/docs/domain/index.md (~400 words per D-21):
      - H1: `# Dominio: analisi tessile manifatturiera`
      - Intro paragraph: scope, perché esiste, link a processi e ruoli
      - `## Mappa ad alto livello` with a Mermaid `graph LR` showing pipeline weaving→spinning→warping→dyeing→finishing→quality (≤8 nodes, with `accDescr:`)
      - `## Pagine processo` bullet list linking to /processes/{name}.md
      - `## Pagine ruolo` bullet list linking to /roles/{name}.md
      - One `!!! note "Mantis context"` (turni 3×8h, mercato outdoor, blend cotton/wool/linen, etc., per D-23)

    Each processes/*.md page (~300-500 words):
      - H1: italian process name
      - `## Process flow`: a `flowchart LR` Mermaid block with `accDescr:` on first content line, ≤8 nodes, ≤7 edges. Use bolded textile terms that are present in the Plan 01 glossary (e.g., **warp yarn**, **weft yarn**, **pick density** for weaving; **warp tension** etc.). Bold every domain term so Plan 06 glossary coverage check is satisfied. Choose vocabulary that EXISTS in Plan 01 bootstrap glossary — do NOT introduce new terms here (Plan 05 expands glossary later).
      - `## Asset coinvolti`: 3-5 bullets, each `**Asset name** — esempio modello / parametri operativi range industria`
      - `## KPI`: 3-5 KPI bullets, format `**KPI name** (unità) — range tipico — formula breve`
      - `## Pain point`: 3-5 narrative bullets, each `**Pain title** — esempio concreto + impatto operativo`
      - One `!!! note "Mantis context"` admonition per D-23.
      - Bottom `## Riferimenti` linking to relevant /glossary entries or /sop/index.md.

    Boundary: do NOT cite specific Mantis warp tension values / dyeing temperatures (D-28 boundary — only "range tipici industria"). Real Mantis numbers are deferred to Phase 7.

    Process choices:
      - weaving.md: focus loom-centric workflow (warp/weft/pick density)
      - spinning.md: ring spinning frame, drafting, twist
      - warping.md: creel, warp beam, beaming
      - dyeing.md: jet dyeing, bath, color matching, fastness
      - finishing.md: stenter, calandratura, finissaggio chimico/meccanico
  </action>
  <verify>
    <automated>for f in docs/docs/domain/index.md docs/docs/domain/processes/{weaving,spinning,warping,dyeing,finishing}.md; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; grep -q '!!! note "Mantis context"' "$f" || { echo "MISSING Mantis note: $f"; exit 1; }; done; for f in docs/docs/domain/processes/*.md; do grep -q '```mermaid' "$f" || { echo "MISSING mermaid: $f"; exit 1; }; grep -q '^## Process flow' "$f" || { echo "MISSING Process flow H2: $f"; exit 1; }; grep -q '^## KPI' "$f" || { echo "MISSING KPI H2: $f"; exit 1; }; done; echo "OK 6 pages"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: all 6 files exist
    - Source assertion: each processes/*.md contains exactly one ` ```mermaid ` fenced block
    - Source assertion: each processes/*.md contains H2 sections "Process flow", "Asset coinvolti", "KPI", "Pain point"
    - Source assertion: each processes/*.md contains exactly one `!!! note "Mantis context"` line (`grep -c` filtered for comment lines; use `grep -v '^#' file | grep -c '!!! note "Mantis context"'` == 1 per file)
    - Source assertion: each Mermaid block in processes/*.md contains `accDescr:` (`grep -A1 'flowchart LR' file | grep -q 'accDescr:'`)
    - Source assertion: each Mermaid block in processes/*.md has ≤8 node identifiers (heuristic: count lines between ```mermaid and ``` matching `^\s+[A-Z][A-Z0-9_]*\[` ≤8)
    - Source assertion: docs/docs/domain/index.md contains H1 `# Dominio:` and links to processes + roles
    - Hygiene: NO occurrence of Accenture brand (`grep -i "accenture" docs/docs/domain/` returns nothing)
  </acceptance_criteria>
  <done>6 IT process+index pages committed with the structural contract enforced. Mermaid diagrams render via pymdownx.superfences (will be verified end-to-end in Plan 07 mkdocs build).</done>
</task>

<task type="auto">
  <name>Task 2: Author the 4 role pages (IT) + pytest structural tests + repo-level test scaffold</name>
  <files>docs/docs/domain/roles/operator.md, docs/docs/domain/roles/technician.md, docs/docs/domain/roles/quality-manager.md, docs/docs/domain/roles/shift-supervisor.md, tests/__init__.py, tests/conftest.py, tests/test_domain_pages.py</files>
  <read_first>
    - docs/docs/domain/index.md (just authored in Task 1 — for cross-links)
    - 02-VALIDATION.md § Per-Task Verification Map (line 46) — `pytest tests/test_domain_pages.py::test_process_sections` is referenced
    - 02-PATTERNS.md § Tests sections (lines 254-291) — first repo pytest under tests/; reuse root pyproject.toml `[tool.pytest.ini_options]`
    - D-22, D-23 for role-page section requirements
  </read_first>
  <action>
    Author 4 role pages following the role H2 contract from <interfaces>:

    roles/operator.md — front-line worker on loom/spinning frames; responsabilità (avvio macchina, monitoraggio difetti, alert escalation), interazione tipica (loom + warping creel + HMI), decisione critica (when to halt for **broken end** vs continue), pain point.
    roles/technician.md — manutentore preventivo/correttivo; calibrazione spindle, sostituzione subbio, troubleshooting.
    roles/quality-manager.md — 4-point grading owner, defect taxonomy enforcement, lot acceptance.
    roles/shift-supervisor.md — turno coordination, handover, escalation HITL Tier 2.

    Each page: ~250-400 words, ITA standard, same admonition discipline (one `!!! note "Mantis context"` per page).

    Repo-level pytest scaffold (FIRST repo pytest under `tests/` — per 02-PATTERNS.md):
      - tests/__init__.py — empty
      - tests/conftest.py — fixture `DOMAIN_DIR = pathlib.Path("docs/docs/domain")` provided as module-scoped fixture
      - tests/test_domain_pages.py — at minimum 6 tests:
        - `test_index_exists` — assert docs/docs/domain/index.md exists and contains `# Dominio:`
        - `test_process_sections` — parametrize over the 5 process pages; for each assert presence of all 4 required H2 sections (regex `^##\s+(Process flow|Asset coinvolti|KPI|Pain point)\s*$`) — must find ≥4 distinct matches per file
        - `test_process_has_mermaid` — for each process page assert exactly one ` ```mermaid ` fenced block and `accDescr:` inside
        - `test_process_mermaid_node_count` — for each process page assert ≤8 node identifiers (heuristic regex)
        - `test_role_sections` — parametrize over the 4 role pages; assert presence of Responsabilità, Interazione tipica, Decisione critica, Pain point H2s
        - `test_mantis_admonition` — assert each of the 10 pages contains exactly one line matching `!!! note "Mantis context"`

    Use `pathlib` (no string concat — security V12 path-traversal mitigation). Use `re.MULTILINE` for H2 detection. All asserts emit informative messages with file path.

    Verify pytest collection works from repo root: `uv run pytest tests/test_domain_pages.py --collect-only` should list 6+ tests (parametrized cases counted individually).
  </action>
  <verify>
    <automated>uv run pytest tests/test_domain_pages.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: all 4 role pages exist; each contains H2s `^## Responsabilità$`, `^## Interazione tipica con asset e processi$`, `^## Decisione critica giornaliera$`, `^## Pain point$`
    - Source assertion: tests/test_domain_pages.py contains test names `test_index_exists`, `test_process_sections`, `test_process_has_mermaid`, `test_process_mermaid_node_count`, `test_role_sections`, `test_mantis_admonition`
    - Source assertion: NO `os.path.join` or `os.path.exists` in tests/ — only `pathlib.Path` (security V12)
    - Behavior: `uv run pytest tests/test_domain_pages.py -v` reports all parametrized cases passed (≥1 case per process/role for parametrized tests; total ≥ 25 individual cases when parametrized over 5 processes + 4 roles)
    - Behavior: `uv run pytest --collect-only tests/test_domain_pages.py` exits 0
    - Hygiene: no "Accenture" anywhere in docs/docs/domain/
  </acceptance_criteria>
  <done>4 role pages + pytest structural test suite committed. Plan 06 will wire `tests/test_domain_pages.py` into CI alongside the validate scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored Markdown → MkDocs build pipeline | Content authored by Claude + human-reviewed in PR; rendered to gh-pages |
| Markdown bold tokens → glossary coverage check (Plan 06) | Domain pages MUST use only terms present in Plan 01 bootstrap glossary, otherwise downstream CI fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | docs/docs/domain/**.md (LLM-drafted content) | mitigate | D-25 hybrid review pattern (human reviews each draft before merge); deferred ideas D-28 boundary explicit (no fabricated Mantis numeric values, only "range tipici industria") |
| T-02-07 | Repudiation | Mermaid diagrams without accDescr | mitigate | Task 1 acceptance enforces `accDescr:` first content line per RESEARCH Pitfall 2 |
| T-02-08 | Information Disclosure | Mantis context admonitions | mitigate | Generic textile-sector context only (yarn families, market segment); no real customer data, no PII; gitleaks hook (Phase 1) covers credential patterns |
| T-02-09 | Tampering | brand contamination (Accenture / original brand) | mitigate | Acceptance criteria explicit `grep -i accenture` returns nothing per page batch; D-08 from Phase 1 enforced |
| T-02-10 | Tampering | path-traversal in pytest test file walking | mitigate | `pathlib.Path` API exclusively, no string concat, root-relative paths only (V12 file & resources) |
</threat_model>

<verification>
- All 10 IT domain pages exist with required H2 contract.
- Each process page has exactly one Mermaid `flowchart LR` block with `accDescr:` and ≤8 nodes.
- Each page has exactly one `!!! note "Mantis context"` admonition.
- pytest collects + passes ≥25 parametrized cases over `tests/test_domain_pages.py`.
- No Accenture / original brand reference anywhere in `docs/docs/domain/`.
</verification>

<success_criteria>
- DOC-05 IT side complete (processes + roles + index).
- pytest structural tests scaffolded for downstream CI wiring.
- All domain bold tokens already present in Plan 01 bootstrap glossary (no glossary-coverage debt introduced).
</success_criteria>

<output>
Create `.planning/phases/02-domain-modeling-synthetic-corpus/02-02-SUMMARY.md` when done.
</output>
