---
phase: 02-domain-modeling-synthetic-corpus
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docs/assumptions/register.yaml
  - scripts/validate-assumption-schema.py
  - scripts/validate-assumption-components.py
  - scripts/generate-assumption-pages.py
  - tests/test_assumption_register.py
autonomous: true
requirements: [DOC-12]
must_haves:
  truths:
    - "docs/assumptions/register.yaml contains exactly 30 schema-valid assumption entries (A-001..A-030) seeded across the D-36 distribution buckets"
    - "scripts/validate-assumption-schema.py exits 0 against register.yaml and exits 1 if a required field is removed"
    - "scripts/validate-assumption-components.py builds a known-component allowlist from nx projects + infra services and exits 0 on the register"
    - "scripts/generate-assumption-pages.py supports --dry-run, --check, and idempotent regeneration (double-run produces zero git diff)"
    - "Each generated assumption detail page contains `hidden: true` in frontmatter (per Open Question #3 — excluded from MkDocs nav)"
  artifacts:
    - path: "docs/assumptions/register.yaml"
      provides: "Source-of-truth YAML for the assumption register (30 seed entries — Plan 06 expands to 50)"
      contains: "A-001"
    - path: "scripts/validate-assumption-schema.py"
      provides: "jsonschema Draft 2020-12 validation of every entry in register.yaml"
      contains: "Draft202012Validator"
    - path: "scripts/validate-assumption-components.py"
      provides: "Cross-validates affected_components[] against nx + infra allowlist"
      contains: "nx show projects"
    - path: "scripts/generate-assumption-pages.py"
      provides: "Idempotent generator: index table + per-A-NNN detail pages (IT+EN mirror)"
      contains: "--check"
  key_links:
    - from: "scripts/validate-assumption-schema.py"
      to: "packages/sft-domain/src/sft_domain/schemas/assumption.schema.json"
      via: "json.loads + Draft202012Validator"
      pattern: "assumption.schema.json"
    - from: "scripts/generate-assumption-pages.py"
      to: "docs/docs/assumptions/A-NNN.md + docs/docs/en/assumptions/A-NNN.md"
      via: "pathlib.Path.write_text"
      pattern: "docs/docs.*assumptions"
---

<objective>
Stand up the Assumption Register (DOC-12) per D-33/D-34/D-35/D-36: canonical `docs/assumptions/register.yaml` with 30 seed entries, schema validator script, component-name validator script, and an idempotent generator that produces both IT and EN MkDocs pages — one per assumption (`hidden: true` in frontmatter so they don't pollute the sidebar nav, per Open Question #3) plus an index table per language.

Plan 06 (Wave 3) expands to ~50 entries and wires the validators into CI; this plan ships the schema + 30 entries + the three scripts.

Purpose: DOC-12 requires explicit assumptions on data quality / simulation / scope. D-36 mandates ~50 entries; this plan seeds the first 30 (the more critical buckets: security 4 + scope-limit 10 + regulatory 6 + data-quality 10 = 30 entries). Plan 06 adds simulation 8 + external-dependency 8 + performance 3 + cost 1 = 20 → total 50.

Output: register.yaml + 3 scripts (validate-schema, validate-components, generate-pages) + pytest module for register sanity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-CONTEXT.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-RESEARCH.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-PATTERNS.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-01-PLAN.md
@scripts/sync-python-versions.py
@scripts/validate-nx-graph.py
@packages/sft-domain/src/sft_domain/schemas/assumption.schema.json

<interfaces>
<!-- All 3 scripts MUST follow scripts/sync-python-versions.py + scripts/validate-nx-graph.py shape (analog per 02-PATTERNS.md lines 48-213). -->

CLI shape per script:
- shebang `#!/usr/bin/env python3`
- module docstring with Usage + Exit codes block
- `WORKSPACE_ROOT = pathlib.Path(__file__).parent.parent`
- argparse parser, --dry-run for generator, no --dry-run for validators
- `main() -> None`, calls core function returning `success: bool`, then `sys.exit(0 if success else 1)`
- generator additionally supports `--check` (exit 2 on drift)
- errors accumulated to list, printed grouped to stderr at end, suggest `Fix: ...` line
- YAML loaded only via `yaml.safe_load` (security V12)
- All output via `path.relative_to(WORKSPACE_ROOT)` (no absolute paths)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed register.yaml with 30 entries + validate-assumption-schema.py</name>
  <files>docs/assumptions/register.yaml, scripts/validate-assumption-schema.py, tests/test_assumption_register.py</files>
  <read_first>
    - packages/sft-domain/src/sft_domain/schemas/assumption.schema.json (Plan 01 output — required fields + category enum)
    - D-33, D-34, D-35, D-36 in 02-CONTEXT.md (full schema + 50-entry distribution table)
    - scripts/validate-nx-graph.py (analog — exit codes, error accumulation pattern, lines 1-90)
    - 02-PATTERNS.md § scripts/validate-* (lines 136-213)
    - tests/conftest.py if exists from Plan 02 (otherwise create)
  </read_first>
  <action>
    Author docs/assumptions/register.yaml as a top-level YAML list of 30 entries. Distribution for this plan (Plan 06 adds the other 20):
      - data-quality: 10 (A-001 .. A-010) — NaN injection from sensor, drift, missing tags, timezone, missing units, IoT-bridge buffer overflow, dataset replay completeness, schema evolution backward compat, sample rate variability, time skew tra PLC (verbatim from D-36)
      - scope-limit: 10 (A-011 .. A-020) — Mantis-only deployment first, no multi-tenant, no real PLC integration in MVP, no real material flow tracking, no real labor cost data, no real ERP integration, single-language interface per session, no production-grade SLOs, no SOC2/ISO27001 in MVP, no formal verification of HITL safety
      - regulatory: 6 (A-021 .. A-026) — GDPR PII boundaries, employee data retention limits, Italian labor law D.lgs 81/2008 art. 15, EU AI Act high-risk categorization, audit retention 7y vs 30d, right-to-explanation HITL coverage
      - security: 4 (A-027 .. A-030) — data-diode OT bridge enforcement runtime, secrets in Kubernetes (SealedSecrets vs Vault), Langfuse self-hosted vs cloud trade-off, Qwen weights supply-chain

    Each entry uses the fields from D-33: id, statement (sentence), category, affected_components (array — see Task 2 for allowed names), rationale (cite source / Phase / standard), validation_method (test or process), risk_if_wrong (consequence), status: active (all 30), created_in_phase: 2, last_reviewed_in_phase: 2, superseded_by: null.

    Component name budget for this plan (verified-existing infra services + known-future Nx projects, all listed in 02-RESEARCH.md Pitfall 6 line 760): orchestrator, ot-bridge, sim-textile, anomaly-detector, predictive-maintenance, langfuse, qdrant, postgresql, timescaledb, nats, redis, ollama, vllm, clickhouse, minio, hitl-queue, audit-stream, k8s, sealed-secrets, helm. Use these names ONLY (Plan 06's validator will enforce against `nx show projects` + infra allowlist).

    Stable ordering: list sorted by `id` for deterministic git diffs.

    Author scripts/validate-assumption-schema.py copying shape from scripts/validate-nx-graph.py (02-PATTERNS.md lines 144-213). Core function:
      1. Read `packages/sft-domain/src/sft_domain/schemas/assumption.schema.json` → `Draft202012Validator(schema)`
      2. Read `docs/assumptions/register.yaml` via `yaml.safe_load`
      3. Assert top-level is a list; for each entry validate via `list(validator.iter_errors(entry))`
      4. Additionally enforce: ids are unique, ids match `^A-[0-9]{3}$`, ids form a contiguous range starting at A-001 (no gaps)
      5. Accumulate ALL errors with `path.relative_to(WORKSPACE_ROOT)` + entry id + field; emit grouped to stderr + suggest `Fix: ...`
      6. Print `OK: validated N entries` on success
      7. Exit 0 / 1

    Add args:
      - `--register-file` (Path, default Path("docs/assumptions/register.yaml"))
      - `--schema-file` (Path, default points at assumption.schema.json)

    Author tests/test_assumption_register.py with at least 4 tests:
      - test_register_yaml_loads (yaml.safe_load returns list)
      - test_register_has_30_entries (current plan invariant)
      - test_register_ids_contiguous (A-001..A-030, no gaps)
      - test_register_ids_unique
      Use the same conftest.py pattern as Plan 02.
  </action>
  <verify>
    <automated>python3 scripts/validate-assumption-schema.py &amp;&amp; uv run pytest tests/test_assumption_register.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^- id: A-' docs/assumptions/register.yaml` equals 30
    - Source assertion: ids A-001 through A-030 each appear exactly once (`for n in $(seq -f '%03g' 1 30); do grep -c "^- id: A-$n$" docs/assumptions/register.yaml; done` returns 30 ones)
    - Source assertion: every entry has `status: active`, `created_in_phase: 2`, `last_reviewed_in_phase: 2`, `superseded_by: null`
    - Source assertion: scripts/validate-assumption-schema.py contains `yaml.safe_load` and does NOT contain `yaml.load(` or `Loader=` (V12)
    - Source assertion: scripts/validate-assumption-schema.py contains `Draft202012Validator`
    - Behavior: `python3 scripts/validate-assumption-schema.py` exit 0 with `OK: validated 30 entries`
    - Behavior: `uv run pytest tests/test_assumption_register.py -v` reports ≥4 passed
    - Behavior (negative): adding a malformed entry (e.g. missing `rationale`) temporarily and re-running validator returns exit code 1 with `Fix:` hint (manual sanity — note in commit)
  </acceptance_criteria>
  <done>30 assumption entries committed, schema validator + pytest sanity green.</done>
</task>

<task type="auto">
  <name>Task 2: validate-assumption-components.py (nx + infra allowlist cross-check)</name>
  <files>scripts/validate-assumption-components.py</files>
  <read_first>
    - scripts/validate-nx-graph.py (uses `nx show projects --json` equivalent via tmp/graph.json — line 19-30)
    - 02-RESEARCH.md § Pitfall 6 (lines 756-762) — exact infra service allowlist
    - D-34 in 02-CONTEXT.md — cross-axis tagging requirement
    - 02-VALIDATION.md row "DOC-12 / `affected_components` references existing Nx project"
  </read_first>
  <action>
    Author scripts/validate-assumption-components.py following the validator shape pattern. Core algorithm:
      1. Build the allowlist:
         - Nx projects: run `npx nx show projects --json` (subprocess.check_output with timeout 30s) and parse the JSON list
         - Infra services hardcoded constant: `INFRA_SERVICES = {"langfuse", "qdrant", "postgresql", "timescaledb", "nats", "redis", "ollama", "vllm", "clickhouse", "minio", "hitl-queue", "audit-stream", "k8s", "sealed-secrets", "helm"}` (from RESEARCH.md Pitfall 6)
         - Combined: allowlist = set(nx_projects) | INFRA_SERVICES
      2. Load register.yaml via yaml.safe_load
      3. For each entry collect every component in `affected_components[]`
      4. For each component, assert `component in allowlist`. On miss: append to errors list with entry id + offending component + a `Fix:` suggesting which closest allowlist match (use difflib.get_close_matches)
      5. Emit grouped stderr report; print `OK: all N components valid` on success
      6. Args: `--register-file`, optional `--no-nx` flag (CI may pre-write graph.json) for environments where `npx nx` is unavailable — in that case fall back to a pre-generated `tmp/nx-projects.json` produced by `npx nx show projects --json > tmp/nx-projects.json` (caller's responsibility)
      7. Exit 0 / 1

    Subprocess safety: always pass `shell=False`, use `subprocess.run(["npx","nx","show","projects","--json"], check=True, timeout=30, capture_output=True, text=True)`. Catch FileNotFoundError (npx absent) and CalledProcessError separately, emit actionable message.

    Document in module docstring that this script is the implementation of D-34 + RESEARCH Pitfall 6 and that the INFRA_SERVICES list must be kept in sync with `docker-compose.yml` / Helm values as new services are added.
  </action>
  <verify>
    <automated>python3 scripts/validate-assumption-components.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -F 'subprocess.run' scripts/validate-assumption-components.py` exits 0 with `shell=False` implied (no `shell=True` anywhere)
    - Source assertion: `grep -E 'shell\s*=\s*True' scripts/validate-assumption-components.py` returns nothing (V12 — no shell injection surface)
    - Source assertion: `grep -F 'yaml.safe_load' scripts/validate-assumption-components.py` exits 0
    - Source assertion: `grep -F 'INFRA_SERVICES' scripts/validate-assumption-components.py` exits 0 and the set literal contains at least "langfuse", "qdrant", "postgresql", "nats", "ollama", "ot-bridge" is sourced from nx_projects (sim-textile is sft-* via nx)
    - Behavior: `python3 scripts/validate-assumption-components.py` exit 0 against the 30 entries from Task 1 with `OK: all N components valid`
    - Behavior (negative manual): temporarily add an entry referencing `affected_components: [nonexistent-svc]`; validator returns exit code 1 with `Fix:` line suggesting close match
  </acceptance_criteria>
  <done>Component validator script committed; passes on the 30 seed entries; ready to be wired into CI by Plan 06.</done>
</task>

<task type="auto">
  <name>Task 3: generate-assumption-pages.py (idempotent IT+EN MkDocs renderer, hidden detail pages per Open Question #3)</name>
  <files>scripts/generate-assumption-pages.py</files>
  <read_first>
    - scripts/sync-python-versions.py (full file — argparse + dry-run + idempotent + error accumulation pattern, exact lines 1-135)
    - 02-RESEARCH.md § Pattern 2 Idempotent Generator (lines 402-462) — `--check` exit code 2 contract
    - D-33 in 02-CONTEXT.md — output paths: docs/docs/assumptions/index.md + docs/docs/assumptions/A-NNN.md + EN mirror under docs/docs/en/assumptions/
    - 02-RESEARCH.md § Open Question #3 — one page per assumption with `hidden: true` to keep them out of MkDocs nav while preserving deep-link audit URLs
    - 02-PATTERNS.md § scripts/generate-* (lines 48-133)
  </read_first>
  <action>
    Author scripts/generate-assumption-pages.py following the sync-python-versions.py shape (02-PATTERNS.md lines 48-133). Core algorithm:

    1. Constants: WORKSPACE_ROOT, REGISTER = Path("docs/assumptions/register.yaml"), OUTPUTS = {"it": Path("docs/docs/assumptions"), "en": Path("docs/docs/en/assumptions")}.
    2. `render_index(entries: list[dict], lang: str) -> str`: returns Markdown with H1, brief intro, then a sortable MkDocs Material table with columns id, category, statement (truncated 80 chars), affected_components (joined ", "), status. Add `<!-- AUTOGENERATED from docs/assumptions/register.yaml — DO NOT EDIT -->` at the top. NO timestamps (idempotency per RESEARCH Pattern 2).
    3. `render_detail(entry: dict, lang: str) -> str`: returns Markdown with frontmatter block (YAML) containing `hidden: true` (per Open Question #3) plus the id/category/status fields for MkDocs Material tags integration. Body has all D-33 fields rendered as a description list. Same AUTOGENERATED header.
    4. Sort entries by id for deterministic output.
    5. CLI flags: `--dry-run` (print what would be written, no I/O), `--check` (compare each output file to would-be content; exit 2 on any drift with `DRIFT: <path>` to stderr).
    6. Localized strings IT vs EN: IT uses "Identificativo / Categoria / Affermazione / Componenti / Stato"; EN uses "ID / Category / Statement / Components / Status". The detail page section headers translate accordingly.
    7. Both languages MUST be generated in a single run so they always stay symmetric (Plan 07 will run `--check` in CI as the bilingual drift gate).
    8. Always print `Updated <path.relative_to(WORKSPACE_ROOT)>` or `[dry-run] WOULD update ...` per sync-python-versions.py convention. Accumulate any write errors and report grouped at end.

    Idempotency guarantee: re-run produces identical bytes. Sort stable, no timestamps, deterministic frontmatter key order, line endings always `\n`.

    Acceptance contract: this script will be run by Plan 07 once register.yaml is final (after Plan 06 expansion to 50). For now, Plan 03 invokes it to generate pages for the 30 current entries so we can verify idempotency + integration before Plan 06's expansion.
  </action>
  <verify>
    <automated>python3 scripts/generate-assumption-pages.py &amp;&amp; python3 scripts/generate-assumption-pages.py --check &amp;&amp; python3 scripts/generate-assumption-pages.py &amp;&amp; git diff --quiet -- docs/docs/assumptions docs/docs/en/assumptions || { echo "IDEMPOTENCY FAIL"; exit 1; }; ls docs/docs/assumptions/A-0{01,30}.md docs/docs/en/assumptions/A-0{01,30}.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -F 'AUTOGENERATED from docs/assumptions/register.yaml' scripts/generate-assumption-pages.py` exits 0
    - Source assertion: `grep -F 'hidden: true' scripts/generate-assumption-pages.py` exits 0 (Open Question #3 — detail pages excluded from nav)
    - Source assertion: `grep -F 'yaml.safe_load' scripts/generate-assumption-pages.py` exits 0; `grep -E 'yaml\.load\(|Loader=' scripts/generate-assumption-pages.py` returns nothing
    - Source assertion: script supports both `--dry-run` and `--check` (`grep -F 'add_argument("--check"' scripts/generate-assumption-pages.py` exits 0)
    - Source assertion: NO timestamp call (`grep -E 'datetime\.now|time\.time' scripts/generate-assumption-pages.py` returns nothing — idempotency)
    - Behavior: first run writes `docs/docs/assumptions/{index,A-001..A-030}.md` and EN mirror (32 files per language)
    - Behavior: `python3 scripts/generate-assumption-pages.py --check` exits 0 immediately after run
    - Behavior: second invocation of generator + `git diff --quiet -- docs/docs/assumptions docs/docs/en/assumptions` exits 0 (true idempotency)
    - Source assertion: each generated `docs/docs/assumptions/A-NNN.md` starts with YAML frontmatter containing `hidden: true`
  </acceptance_criteria>
  <done>Generator script committed; produces 32 IT pages + 32 EN pages from the 30 entries; idempotent on double-run; ready for Plan 06's 50-entry expansion + Plan 07's final regeneration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| register.yaml → Python validators/generators | Git-tracked YAML; parsed via safe_load |
| `npx nx show projects` → component allowlist | Subprocess call; no shell=True; timeout enforced |
| Generated Markdown → MkDocs build pipeline | Output files committed; `--check` mode detects drift in CI (Plan 06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering / EoP | scripts/validate-assumption-schema.py + generate-assumption-pages.py YAML loading | mitigate | Mandatory `yaml.safe_load`; enforced by acceptance grep (V12, CWE-502) |
| T-02-12 | Tampering | scripts/validate-assumption-components.py subprocess call to `npx nx` | mitigate | `subprocess.run([...], shell=False, timeout=30, check=True)`; no string interpolation; allowlist enforced via acceptance grep `shell\s*=\s*True` returns nothing |
| T-02-13 | Tampering | docs/assumptions/register.yaml content (assumptions become retrieval ground truth) | mitigate | D-25 hybrid review for security/scope-limit/regulatory entries (≈20 of 30 in this plan); D-35 living-doc audit trail via git |
| T-02-14 | Information Disclosure | register.yaml entries citing infra details | mitigate | Generic infra/component names only; no credentials/IPs/host names; gitleaks hook continues to scan |
| T-02-15 | Repudiation | Drift between register.yaml and generated pages | mitigate | `--check` flag exits 2; Plan 06/07 wires into CI step |
</threat_model>

<verification>
- register.yaml contains 30 schema-valid entries (A-001..A-030).
- validate-assumption-schema.py exits 0 against register.yaml.
- validate-assumption-components.py exits 0 with `affected_components` allowlist enforced.
- generate-assumption-pages.py is idempotent (double-run produces zero git diff) and `--check` mode works.
- pytest module `test_assumption_register.py` reports ≥4 passed.
</verification>

<success_criteria>
- DOC-12 partially satisfied (30 of 50 entries seeded — Plan 06 closes to 50).
- D-33/D-34/D-35 schema + tagging + living-doc invariants enforced by validators.
- Open Question #3 resolved: detail pages have `hidden: true` and are reachable only via index table (no MkDocs sidebar pollution).
- Generators and validators ready for CI wiring by Plan 06.
</success_criteria>

<output>
Create `.planning/phases/02-domain-modeling-synthetic-corpus/02-03-SUMMARY.md` when done.
</output>
