---
phase: 02-domain-modeling-synthetic-corpus
plan: 06
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - docs/assumptions/register.yaml
  - scripts/validate-glossary-schema.py
  - scripts/validate-glossary-coverage.py
  - scripts/generate-glossary-pages.py
  - packages/sft-domain/project.json
  - Makefile
  - .github/workflows/ci.yml
  - pyproject.toml
autonomous: true
requirements: [DOC-12, DOC-18, KNW-10]
must_haves:
  truths:
    - "docs/assumptions/register.yaml contains exactly 50 entries (A-001..A-050) covering D-36 full distribution"
    - "scripts/validate-glossary-schema.py validates IT + EN glossary YAML against packages/sft-domain/src/sft_domain/schemas/glossary.schema.json and exits 0"
    - "scripts/validate-glossary-coverage.py implements D-32 (bold token extraction with Pitfall 5 mitigations: skip code blocks, skip admonition keywords, skip numeric-only bold) and exits 0 against the full Plan 05 corpus + docs"
    - "scripts/generate-glossary-pages.py is idempotent (double-run produces zero git diff), supports --dry-run and --check (exit 2 on drift)"
    - "Nx targets added to sft-domain project.json: validate-glossary-schema, validate-glossary-coverage, generate-glossary; validate-assumption-schema, validate-assumption-components, generate-assumptions"
    - "Makefile has new targets: validate-glossary, validate-corpus, validate-assumptions, validate-all, generate-glossary, generate-assumptions"
    - "ci.yml has a new 'Validate content' step that runs all 9 Phase 2 validators after the existing Validate Nx dependency graph step"
    - "pyproject.toml has pytest entry in .PHONY equivalents; tests/test_*.py modules ran in CI"
  artifacts:
    - path: "scripts/validate-glossary-coverage.py"
      provides: "D-32 coverage gate — every **bold** has glossary entry (per lang)"
      contains: "BOLD_RE"
    - path: "scripts/validate-glossary-schema.py"
      provides: "Validates it.yaml and en.yaml against glossary.schema.json"
      contains: "Draft202012Validator"
    - path: "scripts/generate-glossary-pages.py"
      provides: "Idempotent docs/docs/glossary.md + en/glossary.md from YAML"
      contains: "--check"
    - path: "packages/sft-domain/project.json"
      provides: "6 new validate/generate Nx targets"
      contains: "validate-glossary-coverage"
    - path: ".github/workflows/ci.yml"
      provides: "Validate content step running 9 Phase 2 validators + pytest"
      contains: "Validate content"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/validate-*.py and scripts/generate-*.py --check"
      via: "GH Actions run block"
      pattern: "Validate content"
    - from: "Makefile"
      to: "scripts/validate-*.py"
      via: "make target"
      pattern: "validate-all"
    - from: "scripts/validate-glossary-coverage.py"
      to: "sft_domain.glossary.load_terms_dict"
      via: "Python import"
      pattern: "from sft_domain"
---

<objective>
Close DOC-12 (assumption register at 50 entries — adds remaining 20 entries to Plan 03's 30), ship the remaining Phase 2 validation/generation scripts (validate-glossary-schema, validate-glossary-coverage, generate-glossary-pages), expand sft-domain's Nx targets to expose all 6 generators/validators, extend Makefile with the new targets, and wire all 9 Phase 2 validators into CI as a single `Validate content` step.

Plan 07 (Wave 4) does the final integration run: mkdocs nav update + full generator execution + `mkdocs build --strict` end-to-end green + SOP promotion to `status: reviewed` after user review batch.

Purpose: D-36 mandates ~50 assumptions; Plan 03 shipped 30, Plan 06 closes the remaining 20. D-32 glossary coverage CI check must go live before Plan 07 runs the strict build. Nx targets + CI wiring make the validators executable per-PR.

Output: 50-entry register, 3 new scripts, project.json + Makefile + ci.yml extensions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-domain-modeling-synthetic-corpus/02-CONTEXT.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-RESEARCH.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-PATTERNS.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-03-PLAN.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-05-PLAN.md
@docs/assumptions/register.yaml
@scripts/sync-python-versions.py
@scripts/validate-nx-graph.py
@packages/sft-domain/src/sft_domain/schemas/glossary.schema.json
@packages/sft-domain/project.json
@.github/workflows/ci.yml
@Makefile
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand register.yaml to 50 entries + author validate-glossary-schema.py and validate-glossary-coverage.py</name>
  <files>docs/assumptions/register.yaml, scripts/validate-glossary-schema.py, scripts/validate-glossary-coverage.py</files>
  <read_first>
    - docs/assumptions/register.yaml (Plan 03 output — 30 entries A-001..A-030)
    - D-36 in 02-CONTEXT.md (full 50-entry distribution; Plan 03 covered data-quality+scope-limit+regulatory+security)
    - 02-RESEARCH.md § Pattern 5 (lines 567-608) — glossary coverage script logic
    - 02-RESEARCH.md § Pitfall 5 (lines 747-754) — bold extraction false positives
    - scripts/validate-nx-graph.py (validator shape analog)
    - packages/sft-domain/src/sft_domain/schemas/glossary.schema.json (Plan 01 — schema for IT/EN validation)
  </read_first>
  <action>
    Append 20 entries to docs/assumptions/register.yaml (A-031..A-050) covering remaining D-36 buckets:
      - simulation: 8 entries (A-031..A-038) — sim-textile fidelity boundaries, NASA C-MAPSS applicability to textile, UCI Manufacturing as proxy, ambient noise model realism, fault injection coverage gaps, OPC-UA mock vs real PLC differences, simulated network latency boundaries, simulator scale-out limit
      - external-dependency: 8 entries (A-039..A-046) — Ollama uptime SLA, Qwen2.5 weights stability, Qdrant scaling cliffs, Langfuse v3 ClickHouse cost, Postgres extension availability on managed services, Helm chart upstream version pin freshness, GitHub Actions free tier limits, GPU availability for vLLM fallback
      - performance: 3 entries (A-047..A-049) — LangGraph checkpointer Postgres scaling, embedding compute cost, vector store sharding decisions deferred
      - cost: 1 entry (A-050) — GPU vs CPU inference operational cost crossover point

    Field discipline identical to Plan 03 (all fields required by assumption.schema.json from Plan 01). Use `affected_components` names from the allowlist enforced by Plan 03's validate-assumption-components.py.

    After append, `python3 scripts/validate-assumption-schema.py` and `python3 scripts/validate-assumption-components.py` (both Plan 03 outputs) MUST exit 0 against the 50 entries.

    scripts/validate-glossary-schema.py: validator shape from validate-nx-graph.py. Args: `--glossary-dir` (default `packages/sft-domain/src/sft_domain/glossary`), `--schema-file` (default schemas/glossary.schema.json). For each of `it.yaml` and `en.yaml`: load via `yaml.safe_load`; assert top-level is a list; validate each entry via `Draft202012Validator.iter_errors`. Additionally enforce uniqueness of `term` field per language (case-insensitive). Accumulate errors with `path.relative_to(WORKSPACE_ROOT)` + entry index + term + field; emit grouped stderr with `Fix:` suggestion. Exit 0/1. Print `OK: validated N IT terms + M EN terms`.

    scripts/validate-glossary-coverage.py: copy logic skeleton from 02-RESEARCH.md § Pattern 5 (lines 573-607). Critical Pitfall-5 hardening:
      1. Strip fenced code blocks BEFORE applying BOLD_RE: `text = re.sub(r"```.*?```", "", text, flags=re.DOTALL)`
      2. Allowlist of admonition keywords to skip: `ADMONITION_BOLD = {"WARNING", "DANGER", "NOTE", "CAUTION", "TIP", "INFO", "BUG", "EXAMPLE", "ABSTRACT", "QUESTION", "SUCCESS", "FAILURE"}` — skip if normalized form matches any (case-insensitive)
      3. Skip numeric-only bold: skip if `re.fullmatch(r"[\d\s°.,/-]+", token)` matches
      4. Skip if token has <2 alphabetic chars (matches Pitfall 5 guidance)
      5. NORMALIZE: lowercase + strip punctuation/whitespace + singular (drop trailing `s` if word, simple heuristic)
      Use `from sft_domain.glossary import load_terms_dict` — must be importable in CI venv (assured by Plan 01's pydantic install + uv sync).
      Args: `--corpus-dir` (default `simulators/synthetic-corpus`), `--docs-dir` (default `docs/docs`), `--stale-threshold` (float, default 0.05) — soft warning on >5% terms in glossary but unreferenced (D-32 says warning-only, NOT fail).
      Exit codes: 0 on success or stale-only; 1 if any required term missing from glossary.
      Per-error message: `MISSING GLOSSARY: '<bold>' in <relpath> (lang=<it|en>)` + `Fix: add term to packages/sft-domain/src/sft_domain/glossary/<lang>.yaml or remove **bold** from source`.

    Important: validate-glossary-coverage.py runs IN CI before mkdocs build. Plan 05 already pre-populated the glossary to cover all bold tokens — therefore the script MUST exit 0 after this plan ships. If it exits 1, the offending bold tokens must be added to glossary YAML (NOT removed from corpus).
  </action>
  <verify>
    <automated>python3 scripts/validate-assumption-schema.py &amp;&amp; python3 scripts/validate-assumption-components.py &amp;&amp; python3 scripts/validate-glossary-schema.py &amp;&amp; python3 scripts/validate-glossary-coverage.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^- id: A-' docs/assumptions/register.yaml` == 50
    - Source assertion: every id A-001..A-050 appears exactly once
    - Source assertion: `grep -c 'category: simulation' docs/assumptions/register.yaml` == 8
    - Source assertion: `grep -c 'category: external-dependency' docs/assumptions/register.yaml` == 8
    - Source assertion: `grep -c 'category: performance' docs/assumptions/register.yaml` == 3
    - Source assertion: `grep -c 'category: cost' docs/assumptions/register.yaml` == 1
    - Source assertion: both new scripts contain `yaml.safe_load`; neither contains `yaml.load(` or `Loader=` (V12)
    - Source assertion: validate-glossary-coverage.py contains all 4 hardening patterns: code-block strip (`re.DOTALL`), ADMONITION_BOLD set, numeric-only skip (regex), <2-alpha-char skip
    - Source assertion: validate-glossary-coverage.py imports `from sft_domain.glossary import load_terms_dict`
    - Behavior: `python3 scripts/validate-assumption-schema.py` exit 0 with `OK: validated 50 entries`
    - Behavior: `python3 scripts/validate-glossary-schema.py` exit 0 with `OK: validated >=150 IT terms + >=150 EN terms`
    - Behavior: `python3 scripts/validate-glossary-coverage.py` exit 0 (Plan 05 pre-populated the glossary to satisfy coverage)
    - Behavior (negative manual): adding `**fakenewterm**` to a SOP body causes validate-glossary-coverage.py exit 1 with `MISSING GLOSSARY: 'fakenewterm'` + `Fix:` line
    - Behavior (negative — stale term, D-32 warning-only): adding a spurious unused term (e.g. `term: spurious_stale_test_term`) to `packages/sft-domain/src/sft_domain/glossary/it.yaml`, then running `python3 scripts/validate-glossary-coverage.py`, prints a line starting with `STALE:` to stdout AND exits with code 0 (warning-only per D-32 — stale terms must NOT fail the build, only the threshold-percentage overrun would warn; per-term staleness is informational). Cleanup: remove the spurious entry before commit.
  </acceptance_criteria>
  <done>Register at 50 entries, schema + coverage validators committed and green.</done>
</task>

<task type="auto">
  <name>Task 2: generate-glossary-pages.py + extend sft-domain project.json with 6 Nx targets</name>
  <files>scripts/generate-glossary-pages.py, packages/sft-domain/project.json</files>
  <read_first>
    - scripts/sync-python-versions.py (full file — generator analog shape per 02-PATTERNS.md lines 48-133)
    - 02-RESEARCH.md § Pattern 2 (lines 402-462) — idempotent generator
    - D-29, D-30 in 02-CONTEXT.md (output paths + material-tags rendering)
    - 02-PATTERNS.md § sft-domain/project.json MODIFY (lines 401-451)
    - packages/sft-domain/project.json (current — Plan 01 left dependencies + no validate targets)
  </read_first>
  <action>
    scripts/generate-glossary-pages.py — same shape as Plan 03's generate-assumption-pages.py + Pattern 2 from 02-RESEARCH.md:
      - Constants: WORKSPACE_ROOT, OUTPUTS = {"it": Path("docs/docs/glossary.md"), "en": Path("docs/docs/en/glossary.md")}
      - Imports `from sft_domain.glossary import load_terms, Category` (must be on PYTHONPATH — Plan 01 ensures via uv workspace)
      - `render(lang: str) -> str`: header `<!-- AUTOGENERATED from packages/sft-domain/src/sft_domain/glossary/{lang}.yaml — DO NOT EDIT -->`, H1 (IT: `# Glossario`, EN: `# Glossary`), brief intro, then for each category (iterate Category enum in declared order):
        - H2 with category label (IT/EN strings)
        - For each Term whose `category == this`, sorted alphabetically by `term`:
          - Material-tags annotation (per D-30): `<span class="md-tag">{category.value}</span>` after term
          - Definition block: `**{term}** — {definition}`
          - If `examples`: italics list
          - If `related_terms`: see-also list with anchor links
          - If `source`: small-text reference
      - CLI: `--dry-run` (print diff summary), `--check` (exit 2 on drift), default writes files
      - Idempotent: stable sort, no timestamps, deterministic key order, `\n` line endings
      - Print `Updated <path>` or `[dry-run] WOULD update <path>` or `DRIFT: <path>` (stderr) for --check failure

    packages/sft-domain/project.json — extend the `targets` block per 02-PATTERNS.md lines 401-451. Append (NOT replace) these 6 targets inside the existing `targets` object:
      - `validate-glossary-schema` (nx:run-commands → `python3 scripts/validate-glossary-schema.py`)
      - `validate-glossary-coverage` (nx:run-commands → `python3 scripts/validate-glossary-coverage.py`)
      - `validate-assumption-schema` (nx:run-commands → `python3 scripts/validate-assumption-schema.py`)
      - `validate-assumption-components` (nx:run-commands → `python3 scripts/validate-assumption-components.py`)
      - `generate-glossary` (nx:run-commands → `python3 scripts/generate-glossary-pages.py`)
      - `generate-assumptions` (nx:run-commands → `python3 scripts/generate-assumption-pages.py`)

    For each target add `inputs` array for nx affected cache invalidation:
      - validate-glossary-schema: `["{projectRoot}/src/sft_domain/glossary/{it,en}.yaml", "{projectRoot}/src/sft_domain/schemas/glossary.schema.json", "{workspaceRoot}/scripts/validate-glossary-schema.py"]`
      - validate-glossary-coverage: `["{projectRoot}/src/sft_domain/glossary/{it,en}.yaml", "{workspaceRoot}/scripts/validate-glossary-coverage.py", "{workspaceRoot}/docs/docs/**/*.md", "{workspaceRoot}/simulators/synthetic-corpus/**/*.md"]`
      - generate-glossary: same inputs as validate-glossary-coverage; outputs: `["{workspaceRoot}/docs/docs/glossary.md", "{workspaceRoot}/docs/docs/en/glossary.md"]`
      - assumption ones analogous, rooted at `{workspaceRoot}/docs/assumptions/register.yaml` + `{workspaceRoot}/packages/sft-domain/src/sft_domain/schemas/assumption.schema.json`

    Preserve existing `test` and `lint` targets verbatim. Keep `implicitDependencies: []` unless adding cross-project edges is needed (it's not — sft-domain is leaf-most).
  </action>
  <verify>
    <automated>python3 scripts/generate-glossary-pages.py &amp;&amp; python3 scripts/generate-glossary-pages.py --check &amp;&amp; python3 scripts/generate-glossary-pages.py &amp;&amp; git diff --quiet -- docs/docs/glossary.md docs/docs/en/glossary.md || { echo "GLOSSARY IDEMPOTENCY FAIL"; exit 1; }; npx nx show project sft-domain --json &gt; /tmp/sftd.json &amp;&amp; for t in validate-glossary-schema validate-glossary-coverage validate-assumption-schema validate-assumption-components generate-glossary generate-assumptions; do jq -e ".targets[\"$t\"]" /tmp/sftd.json &gt; /dev/null || { echo "MISSING TARGET $t"; exit 1; }; done; npx nx run sft-domain:validate-glossary-schema &amp;&amp; npx nx run sft-domain:validate-glossary-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: scripts/generate-glossary-pages.py contains `AUTOGENERATED` header string and `--check` flag
    - Source assertion: no timestamps in generator (`grep -E 'datetime\.now|time\.time' scripts/generate-glossary-pages.py` returns nothing)
    - Behavior: generate-glossary-pages.py is idempotent (double-run → empty git diff on docs/docs/glossary.md + en/glossary.md)
    - Behavior: `python3 scripts/generate-glossary-pages.py --check` exits 0 after fresh generation
    - Source assertion: sft-domain project.json contains all 6 new targets (verified via jq in CI block)
    - Source assertion: existing `test` and `lint` targets are still present (`jq '.targets | keys' packages/sft-domain/project.json` contains both)
    - Behavior: `npx nx run sft-domain:validate-glossary-schema` exits 0
    - Behavior: `npx nx run sft-domain:validate-glossary-coverage` exits 0
    - Behavior: `npx nx run sft-domain:generate-glossary` regenerates files idempotently
  </acceptance_criteria>
  <done>Glossary generator + 6 Nx targets committed; glossary pages can be regenerated via nx; CI can now invoke per-project targets.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Makefile + wire Validate content step into .github/workflows/ci.yml</name>
  <files>Makefile, .github/workflows/ci.yml</files>
  <read_first>
    - Makefile (current state — 02-PATTERNS.md lines 540-589 documents append-points)
    - .github/workflows/ci.yml (current state — 02-PATTERNS.md lines 593-621 documents insert-point after "Validate Nx dependency graph" step)
  </read_first>
  <action>
    Makefile — APPEND new section after the `## Docs` block (per 02-PATTERNS.md lines 540-589). Italian comment style matching existing voice ("Esegue ...", "Build ..."). Use `## Section name` header comments:

```makefile
## Content validation & generation (Phase 2)
# -----------------------------------------------------------------------

# Genera pagine glossario IT+EN dal YAML in sft-domain (idempotente)
generate-glossary:
	python3 scripts/generate-glossary-pages.py

# Genera index + pagine dettaglio assumption register dal YAML (idempotente)
generate-assumptions:
	python3 scripts/generate-assumption-pages.py

# Valida schema + coverage del glossario
validate-glossary:
	python3 scripts/validate-glossary-schema.py
	python3 scripts/validate-glossary-coverage.py

# Valida frontmatter SOP + pairing IT/EN + bilingual mirror docs
validate-corpus:
	python3 scripts/validate-corpus-frontmatter.py --corpus-dir simulators/synthetic-corpus
	python3 scripts/validate-corpus-pairing.py --corpus-dir simulators/synthetic-corpus
	python3 scripts/validate-bilingual-mirror.py --docs-dir docs/docs

# Valida schema + cross-reference componenti dell'assumption register
validate-assumptions:
	python3 scripts/validate-assumption-schema.py
	python3 scripts/validate-assumption-components.py

# Esegue tutti i validator Phase 2 + pytest content tests (entry point CI)
validate-all: validate-glossary validate-corpus validate-assumptions
	uv run pytest tests/test_domain_pages.py tests/test_corpus_inventory.py tests/test_assumption_register.py -v
```

    APPEND new targets to the existing `.PHONY` line (do NOT duplicate existing entries). Per 02-PATTERNS.md line 586-588.

    ci.yml — INSERT a new step between "Validate Nx dependency graph (Python<->TS edges)" and "Nx Affected Lint" (per 02-PATTERNS.md lines 607-621). The new step:

```yaml
- name: Validate content (Phase 2 — glossary, corpus, assumptions, register)
  run: |
    python3 scripts/validate-glossary-schema.py
    python3 scripts/validate-glossary-coverage.py
    python3 scripts/validate-corpus-frontmatter.py --corpus-dir simulators/synthetic-corpus
    python3 scripts/validate-corpus-pairing.py --corpus-dir simulators/synthetic-corpus
    python3 scripts/validate-bilingual-mirror.py --docs-dir docs/docs
    python3 scripts/validate-assumption-schema.py
    python3 scripts/validate-assumption-components.py
    python3 scripts/generate-glossary-pages.py --check
    python3 scripts/generate-assumption-pages.py --check

- name: Run Phase 2 content pytest suite
  run: |
    uv run pytest tests/test_domain_pages.py tests/test_corpus_inventory.py tests/test_assumption_register.py packages/sft-domain/tests/ -v --tb=short
```

    Two separate steps (clearer CI logs): validators first (fast — ~5-10s), then pytest (slightly slower — ~10-20s). Both fail-the-build on non-zero exit.

    Preserve existing steps verbatim. Do NOT modify Setup Python / Install uv / Install Python dependencies steps (already operational from Phase 1).

    Update pyproject.toml root: ensure `[tool.pytest.ini_options]` includes the new test discovery paths. If current config has `testpaths = ["tests"]` (per 02-PATTERNS.md line 264), append `"packages/sft-domain/tests"` to the list. If `testpaths` is absent, add a minimal block to keep CI deterministic.
  </action>
  <verify>
    <automated>make validate-all 2&gt;&amp;1 | tail -20 &amp;&amp; grep -F 'Validate content (Phase 2' .github/workflows/ci.yml &amp;&amp; grep -F 'Run Phase 2 content pytest suite' .github/workflows/ci.yml &amp;&amp; grep -F 'validate-all' Makefile &amp;&amp; for t in generate-glossary generate-assumptions validate-glossary validate-corpus validate-assumptions validate-all; do grep -q "^$t:" Makefile || { echo "MISSING make target $t"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: Makefile contains the 6 new targets (`generate-glossary`, `generate-assumptions`, `validate-glossary`, `validate-corpus`, `validate-assumptions`, `validate-all`) — verified per target via `grep -q "^<name>:" Makefile`
    - Source assertion: `.PHONY` line in Makefile includes the 6 new targets
    - Source assertion: ci.yml contains step name `Validate content (Phase 2 — glossary, corpus, assumptions, register)` and `Run Phase 2 content pytest suite`
    - Source assertion: ci.yml `Validate content` step runs all 9 Phase 2 validators (verify via grep against each script name)
    - Behavior: `make validate-all` exit 0 from a clean working tree (all validators + pytest green)
    - Behavior: yamllint / GH Actions schema on ci.yml unchanged (no syntactic regression — verified by `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exit 0)
    - Source assertion: pyproject.toml `testpaths` covers both root `tests` and `packages/sft-domain/tests`
  </acceptance_criteria>
  <done>Makefile + CI wired with all 9 validators + pytest. The next PR will exercise the gates automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| register.yaml → CI validator | safe_load + jsonschema; component allowlist enforced |
| Bold tokens in any *.md → validate-glossary-coverage.py | Pitfall-5-hardened extractor; fails CI on uncovered terms |
| ci.yml run blocks → bash | Single-line python3 invocations; no shell interpolation of untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27 | Tampering / EoP | scripts/validate-glossary-coverage.py + validate-glossary-schema.py + generate-glossary-pages.py YAML loading | mitigate | `yaml.safe_load` mandatory; acceptance grep enforces; V12 + CWE-502 |
| T-02-28 | Tampering | regex false-positive bold capture (Pitfall 5) yielding false "missing glossary" errors | mitigate | 4 hardening patterns: strip code blocks, admonition allowlist, numeric-only skip, <2-alpha-char skip — verified by acceptance grep on script source |
| T-02-29 | Repudiation | Glossary drift between YAML and generated docs/docs/glossary.md | mitigate | CI step runs `--check` on both generators; exit 2 fails the build with `DRIFT: <path>` |
| T-02-30 | Information Disclosure | register.yaml citing infra components in CI logs | accept | Component names are generic (langfuse, qdrant, postgresql etc.); no secrets/IPs; existing gitleaks hook continues to scan |
| T-02-31 | Tampering | ci.yml `run: |` block injection via untrusted file paths | accept | All paths are static literals; no user-supplied path interpolation; standard GitHub Actions practice |
| T-02-32 | Denial of Service | validate-glossary-coverage.py scanning entire docs/ + corpus on every PR | accept | nx affected `inputs` field on validate-glossary-coverage Nx target enables caching when docs/corpus untouched; full scan is <5s anyway per 02-VALIDATION.md latency budget |
</threat_model>

<verification>
- register.yaml at exactly 50 entries (A-001..A-050) validating against schema + components.
- validate-glossary-schema.py + validate-glossary-coverage.py + generate-glossary-pages.py committed and green.
- generate-glossary-pages.py is idempotent (--check after fresh run = exit 0).
- sft-domain project.json has 6 new Nx targets executable via `npx nx run sft-domain:<target>`.
- Makefile has 6 new targets; `make validate-all` exit 0.
- ci.yml has 2 new steps; yaml syntactically valid.
</verification>

<success_criteria>
- DOC-12 closed: 50 assumption entries seeded across all 8 D-36 buckets, validated, generator-renderable.
- DOC-18 fully gated: glossary-coverage + schema CI checks operational.
- KNW-10 fully gated: 9 content validators wired into CI step; corpus drift visible per-PR.
- D-32 glossary coverage CI check is LIVE.
</success_criteria>

<output>
Create `.planning/phases/02-domain-modeling-synthetic-corpus/02-06-SUMMARY.md` when done.
</output>
