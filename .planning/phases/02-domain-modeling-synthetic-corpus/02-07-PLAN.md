---
phase: 02-domain-modeling-synthetic-corpus
plan: 07
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified:
  - docs/mkdocs.yml
  - docs/docs/glossary.md
  - docs/docs/en/glossary.md
  - docs/docs/sop/index.md
  - docs/docs/en/sop/index.md
  - docs/docs/assumptions/index.md
  - docs/docs/en/assumptions/index.md
  - docs/docs/assumptions/A-001.md
  - docs/docs/en/assumptions/A-001.md
  - simulators/synthetic-corpus/it/loom/SOP-LOOM-001-troubleshoot-broken-end-it.md
  - simulators/synthetic-corpus/en/loom/SOP-LOOM-001-troubleshoot-broken-end-en.md
  - .gitignore
autonomous: false
requirements: [DOC-05, DOC-12, DOC-18, KNW-10]
must_haves:
  truths:
    - "docs/mkdocs.yml has appended nav entries for Dominio, Procedure (SOP), Assumption Register, Glossario with corresponding EN nav_translations and material-tags plugin enabled"
    - "All 100 generated assumption pages (50 IT + 50 EN) + 2 index pages + 2 glossary pages exist and reflect register.yaml + glossary YAML"
    - "Bilingual SOP index pages exist explaining corpus scope + link to simulators/synthetic-corpus/"
    - "`mkdocs build --strict` exits 0 from the docs/ directory with the new content + navigation"
    - "After user-driven review batch via checkpoint, SOPs accepted by the user are promoted to `status: reviewed` (D-25 contract honored)"
    - ".gitignore covers the original-brief file `Smart Factory Transformation.md` (per 02-RESEARCH.md Project Constraints)"
    - "All 9 Phase 2 validators + content pytest + mkdocs strict are green end-to-end as a single make validate-all + make docs run"
  artifacts:
    - path: "docs/mkdocs.yml"
      provides: "Extended navigation + nav_translations + tags plugin"
      contains: "Dominio"
    - path: "docs/docs/glossary.md"
      provides: "Generated IT glossary page (committed)"
      contains: "AUTOGENERATED"
    - path: "docs/docs/en/glossary.md"
      provides: "Generated EN glossary page (committed)"
      contains: "AUTOGENERATED"
    - path: "docs/docs/sop/index.md"
      provides: "IT SOP index page linking to simulators/synthetic-corpus/ + explaining D-25 status contract"
      contains: "draft-unreviewed"
    - path: "docs/docs/en/sop/index.md"
      provides: "EN SOP index page mirror"
      contains: "draft-unreviewed"
    - path: "docs/docs/assumptions/index.md"
      provides: "Generated IT assumption index table (50 entries)"
      contains: "AUTOGENERATED"
  key_links:
    - from: "docs/mkdocs.yml"
      to: "all docs/docs/{domain,sop,assumptions,glossary}.md + EN mirror"
      via: "nav block"
      pattern: "domain/index.md"
    - from: "mkdocs build --strict"
      to: "the full Phase 2 + Phase 1 doc tree"
      via: "make docs"
      pattern: "mkdocs build"
---

<objective>
Final integration: extend `docs/mkdocs.yml` with the 4 new top-level nav entries + bilingual translations + material-tags plugin (D-30); author the 2 IT + 2 EN SOP index pages; regenerate all autogenerated content (glossary IT/EN + assumption pages × 50 × 2 langs); run user review batch on the 20 IT + 20 EN SOPs (D-25 hybrid gate — checkpoint), promote accepted SOPs to `status: reviewed`; ensure `Smart Factory Transformation.md` (original brief) is git-ignored or moved per Project Constraints; finally run `mkdocs build --strict` + `make validate-all` end-to-end and confirm Phase 2 is GREEN.

This plan has a `checkpoint:human-verify` for the SOP review (D-25 hybrid). User chooses promote-all, promote-some-keep-rest-draft (D-25 fallback explicit), or reject-and-rework. Autonomous=false reflects this.

Purpose: All 4 requirements (DOC-05, DOC-12, DOC-18, KNW-10) close at this plan. The site renders the Phase 2 content; downstream phases (Phase 4 agents, Phase 5 RAG) can ingest the substrate.

Output: mkdocs nav update, SOP index pages, regenerated derived content, status promotions, strict-build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-domain-modeling-synthetic-corpus/02-CONTEXT.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-RESEARCH.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-PATTERNS.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-04-PLAN.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-05-PLAN.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-06-PLAN.md
@docs/mkdocs.yml
@docs/docs/index.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs/mkdocs.yml (nav + nav_translations + tags plugin) and author SOP index pages</name>
  <files>docs/mkdocs.yml, docs/docs/sop/index.md, docs/docs/en/sop/index.md, .gitignore</files>
  <read_first>
    - docs/mkdocs.yml (current state — Phase 1 lines 1-94)
    - 02-PATTERNS.md § docs/mkdocs.yml MODIFY (lines 455-533) — exact APPEND-only structure
    - 02-RESEARCH.md § Pitfall 4 (lines 729-745) — nav_translations completeness
    - D-claudes_discretion (synthetic-corpus i18n) in 02-CONTEXT.md line 434 — corpus does NOT go through MkDocs i18n; sop/index.md is a reference page only
    - 02-RESEARCH.md § Project Constraints (lines 991-993) — gitignore for `Smart Factory Transformation.md`
    - .gitignore (current state)
  </read_first>
  <action>
    docs/mkdocs.yml — APPEND-only changes per 02-PATTERNS.md lines 477-501. Preserve all existing entries verbatim. Do NOT reorder.

    Under `nav:` APPEND (after `Architettura`, before `Contributing`):
```yaml
  - Dominio:
    - Indice: domain/index.md
    - Processi:
      - Tessitura: domain/processes/weaving.md
      - Filatura: domain/processes/spinning.md
      - Orditura: domain/processes/warping.md
      - Tintoria: domain/processes/dyeing.md
      - Finissaggio: domain/processes/finishing.md
    - Ruoli:
      - Operatore: domain/roles/operator.md
      - Tecnico: domain/roles/technician.md
      - Quality Manager: domain/roles/quality-manager.md
      - Caposquadra: domain/roles/shift-supervisor.md
  - Procedure (SOP): sop/index.md
  - Assumption Register: assumptions/index.md
  - Glossario: glossary.md
```

    Under `plugins:` APPEND `tags` entry per 02-PATTERNS.md lines 527-532 (right after `search`, before `i18n`). This enables D-30 material-tags rendering for glossary categories.

    Under `plugins.i18n.languages[1].nav_translations` (or equivalent path for the EN language section): APPEND verbatim per 02-PATTERNS.md lines 504-521. Every new IT nav key MUST have an EN translation; Pitfall 4 explicit. Quality Manager + Assumption Register stay English-only (no translation needed — already English).

    Author docs/docs/sop/index.md (IT):
      - H1 `# Procedure (SOP) sintetiche`
      - Intro paragraph: scope (KNW-10 deliverable), what it is + what it is not
      - Section `## Dove vivono le SOP`: corpus path `simulators/synthetic-corpus/`; folder layout (`{it,en}/{loom,dyeing,spinning,quality_grading}/`); GitHub link per browse
      - Section `## Stato e revisione`: D-25 hybrid review explanation; `status: reviewed` vs `draft-unreviewed`; Open Question #5 retrieval contract (default-filter to reviewed in Phase 5)
      - Section `## Schema frontmatter`: link to `packages/sft-domain/src/sft_domain/schemas/sop.schema.json`; required fields table
      - Section `## Esempi`: short bullet list of 4-5 SOP titles + ids + links to GitHub (use raw GitHub paths since SOPs are NOT under docs/ — they're in simulators/)
      - One `!!! note "Mantis context"` admonition
      - Bold the bilingue terms (only terms already in glossary)

    Author docs/docs/en/sop/index.md: English mirror. H1 `# Synthetic SOP corpus`. Same sections (translated headers). Validate-bilingual-mirror.py requires H1+H2 levels match.

    Update .gitignore: add `Smart Factory Transformation.md` (the original brief at repo root — currently untracked per the git status header). This prevents accidental commit of the original brand-tracked source material (D-08 + DEL-08 compliance — verified at Phase 12 by brand-scrub CI but mitigated proactively here). If `.gitignore` already covers it, no-op.
  </action>
  <verify>
    <automated>cd docs &amp;&amp; mkdocs build --strict 2&gt;&amp;1 | tee /tmp/mkdocs-build.log | tail -30; test "${PIPESTATUS[0]}" -eq 0 || exit 1; grep -F 'Dominio' mkdocs.yml &amp;&amp; grep -F 'Procedure (SOP):' mkdocs.yml &amp;&amp; grep -F 'tags' mkdocs.yml &amp;&amp; cd .. &amp;&amp; python3 scripts/validate-bilingual-mirror.py &amp;&amp; git check-ignore "Smart Factory Transformation.md" &gt;/dev/null 2&gt;&amp;1 || grep -F 'Smart Factory Transformation.md' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: docs/mkdocs.yml contains all 4 new top-level nav keys (`Dominio`, `Procedure (SOP)`, `Assumption Register`, `Glossario`)
    - Source assertion: docs/mkdocs.yml `nav_translations` contains every IT key with a corresponding EN translation (Pitfall 4 — verify per key with grep)
    - Source assertion: docs/mkdocs.yml `plugins:` includes `tags` (or `- tags`)
    - Source assertion: docs/docs/sop/index.md exists with H1 `# Procedure (SOP)`, mentions `draft-unreviewed` and `reviewed` and `Phase 5`
    - Source assertion: docs/docs/en/sop/index.md exists with matching H2 levels for validate-bilingual-mirror
    - Behavior: `cd docs && mkdocs build --strict` exits 0 — no broken links, no warnings critical, all nav entries resolve
    - Behavior: `python3 scripts/validate-bilingual-mirror.py` exit 0 (sop/index.md pair satisfies D-24)
    - Source assertion: `.gitignore` contains the line `Smart Factory Transformation.md` OR `git check-ignore "Smart Factory Transformation.md"` exits 0 — original brief is not commitable
    - Hygiene: `grep -i accenture docs/docs/sop/ docs/docs/en/sop/` returns nothing
  </acceptance_criteria>
  <done>mkdocs.yml has the appended nav + translations + tags plugin; SOP index pages exist bilingually; .gitignore covers the original brief; mkdocs strict build green.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate all autogenerated content + commit derived pages</name>
  <files>docs/docs/glossary.md, docs/docs/en/glossary.md, docs/docs/assumptions/index.md, docs/docs/en/assumptions/index.md, docs/docs/assumptions/A-001.md, docs/docs/en/assumptions/A-001.md</files>
  <read_first>
    - scripts/generate-glossary-pages.py (Plan 06 output)
    - scripts/generate-assumption-pages.py (Plan 03 output, exercised against 50 entries from Plan 06)
    - 02-RESEARCH.md § Pattern 2 (idempotency contract)
  </read_first>
  <action>
    Run the two generators against the final source-of-truth state (post-Plan 05 glossary expansion + post-Plan 06 register expansion):

    1. `python3 scripts/generate-glossary-pages.py` — produces:
       - docs/docs/glossary.md (IT, ≥150 terms grouped by 9 categories)
       - docs/docs/en/glossary.md (EN mirror)

    2. `python3 scripts/generate-assumption-pages.py` — produces:
       - docs/docs/assumptions/index.md (IT sortable table, 50 entries)
       - docs/docs/en/assumptions/index.md (EN mirror)
       - docs/docs/assumptions/A-001.md ... A-050.md (50 IT detail pages with `hidden: true` frontmatter)
       - docs/docs/en/assumptions/A-001.md ... A-050.md (50 EN detail pages)

    Total generated artifacts: 2 glossary pages + 2 index pages + 100 detail pages = 104 files.

    Verify idempotency: re-run both generators; `git diff --quiet -- docs/docs/glossary.md docs/docs/en/glossary.md docs/docs/assumptions docs/docs/en/assumptions` MUST exit 0.

    Verify `--check` mode: both `python3 scripts/generate-glossary-pages.py --check` and `python3 scripts/generate-assumption-pages.py --check` MUST exit 0 (no drift).

    Commit all 104 files in a single conventional commit: `feat(02-07-render): generate glossary + assumption pages from canonical YAML`.

    The `files_modified` frontmatter intentionally lists only a representative subset (the index pages + A-001 as a marker for the 100 detail files); the actual commit includes all 104 generated files.
  </action>
  <verify>
    <automated>python3 scripts/generate-glossary-pages.py &amp;&amp; python3 scripts/generate-assumption-pages.py &amp;&amp; python3 scripts/generate-glossary-pages.py --check &amp;&amp; python3 scripts/generate-assumption-pages.py --check &amp;&amp; test "$(find docs/docs/assumptions -name 'A-*.md' | wc -l)" -eq 50 &amp;&amp; test "$(find docs/docs/en/assumptions -name 'A-*.md' | wc -l)" -eq 50 &amp;&amp; test -f docs/docs/glossary.md &amp;&amp; test -f docs/docs/en/glossary.md &amp;&amp; cd docs &amp;&amp; mkdocs build --strict</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `find docs/docs/assumptions -name 'A-*.md' | wc -l` == 50
    - Source assertion: same for `docs/docs/en/assumptions`
    - Source assertion: every `docs/docs/assumptions/A-*.md` starts with YAML frontmatter containing `hidden: true` (Open Question #3 — keeps detail pages out of sidebar nav)
    - Source assertion: docs/docs/glossary.md and en/glossary.md both contain `AUTOGENERATED` header
    - Behavior: `python3 scripts/generate-glossary-pages.py --check` exit 0
    - Behavior: `python3 scripts/generate-assumption-pages.py --check` exit 0
    - Behavior: double-run produces zero git diff on all generated paths (idempotency)
    - Behavior: `cd docs && mkdocs build --strict` exit 0 with the new content
  </acceptance_criteria>
  <done>104 generated artifacts committed; idempotency verified; mkdocs strict build still green with full Phase 2 content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User review batch — promote SOPs from `draft-unreviewed` to `reviewed` (D-25 hybrid gate)</name>
  <files>simulators/synthetic-corpus/it/**/*.md, simulators/synthetic-corpus/en/**/*.md</files>
  <action>Halt autonomous execution. Hand control to the user for the D-25 review batch described in `<what-built>` and `<how-to-verify>` below. The user performs in-place edits to SOP frontmatter `status` fields (no automated mutation by Claude during this checkpoint). When user resumes, Task 4 inherits the post-review state of the corpus.</action>
  <verify>Resume signal received from user matches `approved: N reviewed, M draft` with N + M = 20 (Plan 07 acceptable distributions: 20/0, 10/10, or any explicit mix). Claude does NOT auto-verify the technical correctness of SOPs — that is the human reviewer's responsibility per D-25.</verify>
  <done>User has resumed with an explicit `approved:` count; SOP frontmatter `status` fields reflect user decisions; symmetric IT/EN status preserved per Open Question #5 retrieval contract.</done>
  <what-built>
    All 40 SOPs in `simulators/synthetic-corpus/` were authored by Claude with `status: draft-unreviewed` (D-25 fallback default). This checkpoint runs the explicit D-25 review pass: user evaluates technical correctness of each SOP (no fabricated dyeing temperatures, no impossible warp tensions, no Mantis-specific values disguised as industry range) and decides per-SOP whether to promote `status: reviewed`.

    Pre-checkpoint state:
      - 20 IT SOPs + 20 EN SOPs at status: draft-unreviewed
      - sop/index.md publishes the Phase 5 retrieval contract (Open Question #5): default-filter `reviewed`; drafts only via opt-in
      - validate-corpus-frontmatter.py accepts both statuses (sop.schema.json enum has `reviewed | draft-unreviewed | deprecated`)
  </what-built>
  <how-to-verify>
    Reviewer (user) opens each of the 20 IT SOP files in `simulators/synthetic-corpus/it/` (translations follow IT decisions). For each SOP:
      1. Read the body — verify factory-floor realism (D-28), no fabricated specific Mantis values, units of measure correct, procedure logically coherent.
      2. Verify Mermaid-free sections render cleanly in MkDocs preview (`make docs-serve` → http://127.0.0.1:8000/sop/).
      3. Decide:
         - Promote: edit frontmatter `status: draft-unreviewed` → `status: reviewed`. Apply the SAME edit to the EN counterpart at `simulators/synthetic-corpus/en/<family>/<same-id>-en.md` (status field must stay symmetric to honor Open Question #5 retrieval contract — agents shouldn't see an IT-reviewed but EN-draft asymmetry).
         - Keep draft (D-25 fallback explicit): leave `status: draft-unreviewed`. Phase 5 will not surface this SOP unless caller opts in.
         - Reject + rework: open an issue / annotate the SOP file; do NOT promote; document in this checkpoint resume message.

    After the review pass, the user MAY commit the status edits in a single commit `docs(02-07-review): promote reviewed SOPs (N of 20)`. Acceptable outcome distributions per D-25 fallback authorization:
      - All 20 reviewed (preferred — KNW-10 ideal)
      - 10 reviewed + 10 draft-unreviewed (D-25 explicit fallback)
      - Any mix between — surface count in resume message
  </how-to-verify>
  <resume-signal>
    Type one of:
      - `approved: N reviewed, M draft` (where N + M = 20) — Plan 07 continues to Task 4
      - `rework SOP-XXX-NNN with notes: ...` — Plan 07 pauses; you author corrections, then re-issue `approved: ...`
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: End-to-end validation: make validate-all + mkdocs strict build + Phase 2 sign-off summary</name>
  <files>(verification-only — no new files)</files>
  <read_first>
    - Makefile target `validate-all` (Plan 06 output)
    - .github/workflows/ci.yml `Validate content` step (Plan 06 output)
    - 02-VALIDATION.md § Validation Sign-Off
  </read_first>
  <action>
    Execute the full Phase 2 sign-off chain (must complete after Task 3 resume):

    1. `make validate-all` — runs all 9 Phase 2 validators + the 3 content pytest modules. Must exit 0. Captures: validate-glossary-schema, validate-glossary-coverage, validate-corpus-frontmatter, validate-corpus-pairing, validate-bilingual-mirror, validate-assumption-schema, validate-assumption-components, generate-glossary-pages --check, generate-assumption-pages --check, then `uv run pytest tests/test_domain_pages.py tests/test_corpus_inventory.py tests/test_assumption_register.py packages/sft-domain/tests/`.

    2. `make docs` — runs `mkdocs build --strict`. Must exit 0.

    3. Print the per-requirement closure table:
       - DOC-05: 10 IT + 10 EN domain pages; validate-bilingual-mirror.py exit 0
       - DOC-12: 50 assumption entries in register.yaml; 100 generated detail pages; index pages bilingual
       - DOC-18: ≥150 IT + ≥150 EN glossary entries; coverage gate green; loader API operational
       - KNW-10: 20 IT + 20 EN SOPs; schema + pairing + bilingual gates green; status field gates Phase 5 retrieval per Open Question #5

    4. Update `.planning/STATE.md`:
       - position fields → Phase 2 plans complete
       - log review outcome from Task 3 (N reviewed / M draft) under `## Accumulated Context → Decisions`
       - reset `Next command` to `/gsd-execute-phase 3`

    5. Update `.planning/ROADMAP.md` Progress table: Phase 2 plans count `7/7`, Status `Complete`, Completed date filled.

    6. Update `.planning/REQUIREMENTS.md` Traceability table: mark DOC-05, DOC-12, DOC-18, KNW-10 as `Complete`.

    7. Update `02-VALIDATION.md` § Validation Sign-Off: tick `nyquist_compliant: true` in frontmatter; tick all sign-off boxes.

    No new file creation; this task is the integration audit + project-state propagation.
  </action>
  <verify>
    <automated>make validate-all 2&gt;&amp;1 | tail -50; make docs 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `make validate-all` exit 0
    - Behavior: `make docs` (== `cd docs && mkdocs build --strict`) exit 0
    - Source assertion: `.planning/STATE.md` shows Phase 2 plans completed (7 of 7)
    - Source assertion: `.planning/ROADMAP.md` Progress table shows Phase 2 status `Complete`
    - Source assertion: `.planning/REQUIREMENTS.md` shows DOC-05, DOC-12, DOC-18, KNW-10 status `Complete`
    - Source assertion: `02-VALIDATION.md` frontmatter has `nyquist_compliant: true`
    - Behavior (regression): existing Phase 1 tests still pass (`uv run pytest packages/sft-domain/tests/` continues to report all green including Plan 01 loader tests + Plan 02 domain tests + Plan 03 assumption tests + Plan 04 corpus tests)
    - Behavior: docs/site/ (mkdocs output) contains expected paths: index.html, domain/index.html, sop/index.html, assumptions/index.html, glossary.html + EN mirror under en/
  </acceptance_criteria>
  <done>Phase 2 closed: all 4 requirements satisfied, all validators green, mkdocs site builds clean. Project ready to execute Phase 3 (IT/OT Simulation Layer).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mkdocs build → gh-pages deploy | Phase 1's docs-deploy.yml will publish on next push to main |
| SOP `status: reviewed` promotion → Phase 5 retrieval ground truth | Checkpoint:human-verify is the explicit D-25 gate |
| Original brief file → repository commits | .gitignore + grep enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-33 | Tampering | SOP promotion to `status: reviewed` without true human review | mitigate | `checkpoint:human-verify` with explicit per-SOP decision; resume-signal requires explicit count `N reviewed, M draft`; D-25 fallback acknowledges that draft-unreviewed is acceptable and gates downstream retrieval |
| T-02-34 | Information Disclosure | Smart Factory Transformation.md (original brief, brand-tracked) committed by accident | mitigate | Task 1 adds to .gitignore OR `git check-ignore` confirms coverage; verified by acceptance criterion |
| T-02-35 | Tampering | mkdocs-static-i18n nav drift IT/EN (Pitfall 4) | mitigate | Every new IT nav key has explicit nav_translations entry; mkdocs build --strict catches missing pages but NOT missing translations — task acceptance grep enforces every new IT key has a translation pair |
| T-02-36 | Tampering | Generated docs drift from source YAML | mitigate | `--check` mode on both generators exits 2 on drift; wired in CI by Plan 06; verified in Task 2 acceptance |
| T-02-37 | Repudiation | Phase 2 closure without proper requirement traceability | mitigate | Task 4 updates REQUIREMENTS.md traceability table; sign-off checklist on 02-VALIDATION.md |
| T-02-38 | Tampering | brand contamination (Accenture) in any new SOP index or generated page | mitigate | Acceptance grep `grep -i accenture docs/docs/sop/ docs/docs/en/sop/` returns nothing; brand-scrub final pass per Phase 12 CI gate |
</threat_model>

<verification>
- docs/mkdocs.yml extended with 4 new top-level nav entries, EN translations, tags plugin.
- 2 SOP index pages + 2 glossary pages + 102 assumption pages exist and reflect canonical YAML.
- Generators idempotent and `--check` clean.
- `make validate-all` exit 0; `make docs` exit 0.
- User-reviewed SOP count surfaced and traced to git commits.
- STATE.md / ROADMAP.md / REQUIREMENTS.md / 02-VALIDATION.md updated to reflect Phase 2 closure.
</verification>

<success_criteria>
- All 4 requirements (DOC-05, DOC-12, DOC-18, KNW-10) marked Complete in REQUIREMENTS.md traceability.
- Phase 2 plans count 7/7 in ROADMAP.md.
- STATE.md `Next command` = `/gsd-execute-phase 3`.
- D-25 hybrid review pattern documented + executed; Open Question #5 retrieval contract operational.
- mkdocs Material site renders the full Phase 1 + Phase 2 content bilingually.
</success_criteria>

<output>
Create `.planning/phases/02-domain-modeling-synthetic-corpus/02-07-SUMMARY.md` when done.
</output>
