---
phase: 02-domain-modeling-synthetic-corpus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sft-domain/pyproject.toml
  - packages/sft-domain/project.json
  - packages/sft-domain/src/sft_domain/glossary/__init__.py
  - packages/sft-domain/src/sft_domain/glossary/models.py
  - packages/sft-domain/src/sft_domain/glossary/loader.py
  - packages/sft-domain/src/sft_domain/glossary/it.yaml
  - packages/sft-domain/src/sft_domain/glossary/en.yaml
  - packages/sft-domain/src/sft_domain/schemas/__init__.py
  - packages/sft-domain/src/sft_domain/schemas/glossary.schema.json
  - packages/sft-domain/src/sft_domain/schemas/sop.schema.json
  - packages/sft-domain/src/sft_domain/schemas/assumption.schema.json
  - packages/sft-domain/tests/__init__.py
  - packages/sft-domain/tests/conftest.py
  - packages/sft-domain/tests/test_glossary_loader.py
  - packages/sft-domain/tests/test_glossary_schema.py
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [DOC-18]
must_haves:
  truths:
    - "sft_domain.glossary.load_terms('it') returns a non-empty list[Term] parsed from packages/sft-domain/src/sft_domain/glossary/it.yaml"
    - "sft_domain.glossary.load_terms('en') returns a non-empty list[Term] parsed from packages/sft-domain/src/sft_domain/glossary/en.yaml"
    - "sft_domain.glossary.load_terms_dict('it') returns a dict keyed by lowercased term string with O(1) lookup"
    - "Three JSON Schema Draft 2020-12 files (glossary, sop, assumption) exist under packages/sft-domain/src/sft_domain/schemas/ and pass jsonschema meta-validation"
    - "uv sync --all-packages exits 0 after pydantic and pyyaml are added as runtime deps to sft-domain"
    - "pytest in packages/sft-domain exits 0 with at least 4 tests collected (loader + schema)"
  artifacts:
    - path: "packages/sft-domain/src/sft_domain/glossary/models.py"
      provides: "Pydantic Term + Category enum (frozen, extra=forbid)"
      min_lines: 30
    - path: "packages/sft-domain/src/sft_domain/glossary/loader.py"
      provides: "load_terms(lang), load_terms_dict(lang) with lru_cache"
      min_lines: 25
    - path: "packages/sft-domain/src/sft_domain/glossary/it.yaml"
      provides: "Bootstrap IT glossary (~70 entries spanning 9 categories) seeded ahead of Wave 2 SOP authoring"
      contains: "term:"
    - path: "packages/sft-domain/src/sft_domain/glossary/en.yaml"
      provides: "Bootstrap EN glossary mirror of it.yaml"
      contains: "term:"
    - path: "packages/sft-domain/src/sft_domain/schemas/glossary.schema.json"
      provides: "Draft 2020-12 schema for Term entries"
      contains: "$schema"
    - path: "packages/sft-domain/src/sft_domain/schemas/sop.schema.json"
      provides: "Draft 2020-12 schema for SOP frontmatter"
      contains: "SOP-"
    - path: "packages/sft-domain/src/sft_domain/schemas/assumption.schema.json"
      provides: "Draft 2020-12 schema for assumption register entries"
      contains: "affected_components"
  key_links:
    - from: "packages/sft-domain/src/sft_domain/glossary/loader.py"
      to: "packages/sft-domain/src/sft_domain/glossary/{it,en}.yaml"
      via: "yaml.safe_load + Pydantic Term.model_validate"
      pattern: "yaml\\.safe_load"
    - from: "packages/sft-domain/tests/test_glossary_loader.py"
      to: "sft_domain.glossary.load_terms"
      via: "pytest import"
      pattern: "from sft_domain"
---

<objective>
Establish the data substrate of Phase 2: JSON Schemas, Pydantic Term model, glossary loader with lru_cache, bootstrap bilingual glossary (~70 terms per language pre-seeded with all bold tokens that the Wave 2 example SOPs will reference), and pytest scaffolding. After this plan all downstream plans can load + validate content without inventing infrastructure.

Purpose: Single source of truth for textile + agentic terminology is YAML in sft-domain (D-29). Resolves Open Question 2 by exposing both `load_terms(lang)` and `load_terms_dict(lang)`. Resolves Open Question 4 by pre-seeding bootstrap glossary with all bold tokens needed by the 5 example SOPs of Plan 04 (avoids CI ping-pong on glossary coverage check).

Output: pydantic + pyyaml installed as sft-domain runtime deps; 3 JSON Schemas; glossary models + loader + bootstrap YAML (~70 IT + ~70 EN); pytest tests confirming loader and schema integrity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-CONTEXT.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-RESEARCH.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-PATTERNS.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-VALIDATION.md
@packages/sft-domain/pyproject.toml
@packages/sft-domain/project.json
@packages/sft-domain/src/sft_domain/__init__.py
@pyproject.toml
@scripts/sync-python-versions.py

<interfaces>
<!-- Public API surface this plan creates; downstream plans (02–07) import from these. -->

From packages/sft-domain/src/sft_domain/glossary/__init__.py (NEW):
```python
from sft_domain.glossary.loader import load_terms, load_terms_dict
from sft_domain.glossary.models import Category, Term
__all__ = ["Category", "Term", "load_terms", "load_terms_dict"]
```

From packages/sft-domain/src/sft_domain/glossary/models.py (NEW):
- `class Category(str, Enum)` with 9 members: TEXTILE_PROCESS, TEXTILE_ASSET, TEXTILE_DEFECT, TEXTILE_KPI, TEXTILE_TOOL_PPE, TEXTILE_MATERIAL, AGENTIC_PLATFORM, AGENTIC_TOOL, REGULATORY
- `class Term(BaseModel)` with `model_config = {"frozen": True, "extra": "forbid"}` and fields: `term: str`, `definition: str`, `category: Category`, `related_terms: list[str] = []`, `examples: list[str] = []`, `source: str | None = None`

From packages/sft-domain/src/sft_domain/glossary/loader.py (NEW):
- `load_terms(lang: Literal["it","en"]) -> list[Term]` — lru_cache(maxsize=2), uses yaml.safe_load
- `load_terms_dict(lang: Literal["it","en"]) -> dict[str, Term]` — lru_cache(maxsize=2), keys are `term.lower()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire runtime dependencies (pydantic, pyyaml) into sft-domain and dev deps (jsonschema, python-frontmatter, pyyaml) into root</name>
  <files>packages/sft-domain/pyproject.toml, pyproject.toml, uv.lock</files>
  <read_first>
    - packages/sft-domain/pyproject.toml (current state — dependencies = [])
    - pyproject.toml (root, lines 34-41 — current `[dependency-groups] dev`)
    - 02-PATTERNS.md § `packages/sft-domain/pyproject.toml (MODIFY)` (lines 295-323)
    - 02-PATTERNS.md § `pyproject.toml (root, MODIFY)` (lines 625-657)
    - 02-RESEARCH.md § Standard Stack (lines 132-191) for exact version bounds
    - D-29 in 02-CONTEXT.md
  </read_first>
  <behavior>
    - After modification, `uv sync --all-packages` exits 0 with no warnings about deprecated dev-dependencies.
    - `uv pip list --project packages/sft-domain` shows pydantic >=2.10,<3 and pyyaml >=6.0,<7.
    - `uv run --project . python -c "import jsonschema, frontmatter, yaml; print('ok')"` exits 0 from repo root.
  </behavior>
  <action>
    Edit packages/sft-domain/pyproject.toml: replace `dependencies = []` with `dependencies = ["pydantic>=2.10,<3", "pyyaml>=6.0,<7"]`. Do NOT touch [build-system], [tool.hatch.build.targets.wheel], or the version field (Changesets-controlled). Per D-29 single-source-of-truth.

    Edit root pyproject.toml `[dependency-groups] dev` block (lines ~34-41 in current state): APPEND three entries `"jsonschema>=4.23,<5"`, `"python-frontmatter>=1.1,<2"`, `"pyyaml>=6.0,<7"` after existing `"ruff>=0.11"`. Preserve existing entries in original order. Per 02-RESEARCH.md § Standard Stack line 165-178.

    Run `uv sync --all-packages` to regenerate uv.lock. Commit both pyproject.toml files and uv.lock together.
  </action>
  <verify>
    <automated>uv sync --all-packages 2>&amp;1 | tee /tmp/uv-sync.log; grep -q "deprecat" /tmp/uv-sync.log &amp;&amp; exit 1 || true; uv run --project . python -c "import yaml, jsonschema, frontmatter, pydantic; print('all deps importable')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -F 'pydantic>=2.10,<3' packages/sft-domain/pyproject.toml` exits 0
    - Source assertion: `grep -F 'pyyaml>=6.0,<7' packages/sft-domain/pyproject.toml` exits 0
    - Source assertion: `grep -F 'jsonschema>=4.23,<5' pyproject.toml` exits 0
    - Source assertion: `grep -F 'python-frontmatter>=1.1,<2' pyproject.toml` exits 0
    - Behavior: `uv sync --all-packages` exit code 0
    - Behavior: `uv run --project . python -c "import yaml, jsonschema, frontmatter, pydantic"` exit code 0
    - Source assertion: `git diff --quiet -- uv.lock` is non-zero (lockfile changed)
    - Hygiene: no `dev-dependencies` (legacy) appears under any `[tool.uv]` section
  </acceptance_criteria>
  <done>sft-domain has pydantic + pyyaml as runtime deps; root dev group has jsonschema + python-frontmatter + pyyaml; uv.lock regenerated; downstream tasks can import all four libraries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create JSON Schemas (glossary, sop, assumption) Draft 2020-12 with meta-validation tests</name>
  <files>packages/sft-domain/src/sft_domain/schemas/__init__.py, packages/sft-domain/src/sft_domain/schemas/glossary.schema.json, packages/sft-domain/src/sft_domain/schemas/sop.schema.json, packages/sft-domain/src/sft_domain/schemas/assumption.schema.json, packages/sft-domain/tests/__init__.py, packages/sft-domain/tests/conftest.py, packages/sft-domain/tests/test_glossary_schema.py</name>
  <read_first>
    - 02-RESEARCH.md § Pattern 3 SOP Frontmatter Schema Validation (lines 464-520) for sop.schema.json field shape
    - D-26 in 02-CONTEXT.md (lines 137-181) — SOP frontmatter required fields
    - D-29/D-30 in 02-CONTEXT.md (lines 220-273) — glossary Term shape + 9 categories
    - D-33/D-34 in 02-CONTEXT.md (lines 308-348) — assumption schema fields + category enum + affected_components array
    - 02-PATTERNS.md § "No Analog Found" (lines 794-806) — RESEARCH.md is source-of-truth
    - 02-VALIDATION.md § Wave 0 Requirements (lines 64-107)
  </read_first>
  <behavior>
    - Each schema file declares `"$schema": "https://json-schema.org/draft/2020-12/schema"`.
    - `jsonschema.Draft202012Validator.check_schema(schema)` succeeds for all three.
    - sop.schema.json requires fields: id (pattern `^SOP-[A-Z]+-[0-9]{3}$`), title, version (pattern `^[0-9]+\.[0-9]+$`), lang (enum it/en), asset, asset_family (enum **weaving/spinning/warping/dyeing/finishing/quality_grading** — 6 values; covers both D-21 process families AND cross-cutting inspection scope `quality_grading`), role (enum operator/technician/quality-manager/shift-supervisor), hazard_level (enum low/medium/high/critical), estimated_duration_min (integer 1..480), status (enum reviewed/draft-unreviewed/deprecated), created_in_phase (integer ≥1). `additionalProperties: true` to permit tags/prerequisites/related_glossary/audience. **Schema description note (in `description` field of `asset_family` property):** `asset_family` is a SOP-scope tag that covers both textile process families (weaving, spinning, warping, dyeing, finishing — per D-21) AND cross-cutting inspection scopes (quality_grading — operates across processes per D-27). `process` (D-21) and `asset_family` (SOP scope) are distinct concepts: every process is a valid asset_family, but not every asset_family is a process.
    - glossary.schema.json requires: term, definition, category (enum of 9 D-30 values), optional related_terms (array of strings), examples (array of strings), source (string|null). `additionalProperties: false`.
    - assumption.schema.json requires: id (pattern `^A-[0-9]{3}$`), statement, category (enum data-quality/simulation/scope-limit/external-dependency/regulatory/security/performance/cost), affected_components (array minItems 1), rationale, validation_method, risk_if_wrong, status (enum active/validated/invalidated/superseded), created_in_phase, last_reviewed_in_phase, superseded_by (string|null). `additionalProperties: false`.
    - test_glossary_schema.py defines 3 tests: `test_glossary_schema_self_valid`, `test_sop_schema_self_valid`, `test_assumption_schema_self_valid` — each loads JSON, calls `Draft202012Validator.check_schema(...)`.
  </behavior>
  <action>
    Create packages/sft-domain/src/sft_domain/schemas/__init__.py with a single docstring line "JSON Schemas (Draft 2020-12) for glossary, SOP frontmatter, assumption register.".

    Author the three .schema.json files per the field specs above. For SOP schema use the field set from D-26 verbatim (id, title, version, lang, asset, asset_family, role, hazard_level, estimated_duration_min, prerequisites, related_glossary, tags, audience, status, created_in_phase). Required = the subset from RESEARCH.md Pattern 3 line 502. additionalProperties=true on SOP only (tags/prerequisites optional).

    Create packages/sft-domain/tests/__init__.py (empty) and packages/sft-domain/tests/conftest.py providing shared fixtures: `sample_term_dict` (a valid Term dict), `sample_sop_frontmatter` (a valid SOP frontmatter dict matching SOP-LOOM-001 from D-26), `sample_assumption_dict` (a valid A-001 matching D-33 example). Use `@pytest.fixture` and return module-scoped dicts.

    Create packages/sft-domain/tests/test_glossary_schema.py with three tests: each test loads its schema via `json.loads(Path(...).read_text())` and asserts `Draft202012Validator.check_schema(schema) is None` (method returns None on success).

    Schema files: pretty-print with 2-space indent for git readability. No timestamps inside schemas (idempotency).
  </action>
  <verify>
    <automated>uv run --project packages/sft-domain pytest packages/sft-domain/tests/test_glossary_schema.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `jq '."$schema"' packages/sft-domain/src/sft_domain/schemas/glossary.schema.json` outputs `"https://json-schema.org/draft/2020-12/schema"`
    - Source assertion: same for sop.schema.json and assumption.schema.json
    - Source assertion: `jq '.required | length' packages/sft-domain/src/sft_domain/schemas/sop.schema.json` ≥ 11
    - Source assertion: `jq '.properties.asset_family.enum | length' packages/sft-domain/src/sft_domain/schemas/sop.schema.json` equals 6
    - Source assertion: `jq '.properties.asset_family.enum' packages/sft-domain/src/sft_domain/schemas/sop.schema.json` contains ALL of weaving, spinning, warping, dyeing, finishing, quality_grading (and contains NEITHER bare `quality` NOR any value outside this 6-tuple)
    - Source assertion: `jq '.properties.category.enum | length' packages/sft-domain/src/sft_domain/schemas/glossary.schema.json` equals 9
    - Source assertion: `jq '.properties.category.enum | length' packages/sft-domain/src/sft_domain/schemas/assumption.schema.json` equals 8
    - Behavior: `uv run --project packages/sft-domain pytest packages/sft-domain/tests/test_glossary_schema.py -v` reports 3 passed
    - Security (V12): no use of `yaml.load` or `Loader=` anywhere — `grep -RE "yaml\.load\(|Loader=" packages/sft-domain/` returns nothing (only `safe_load` allowed; verified later in Task 3)
  </acceptance_criteria>
  <done>Three Draft 2020-12 schemas committed, self-validate, pytest collects and passes 3 tests, fixtures available for downstream tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Pydantic Term model + lru-cached glossary loader + bootstrap bilingual YAML (~70 entries each)</name>
  <files>packages/sft-domain/src/sft_domain/glossary/__init__.py, packages/sft-domain/src/sft_domain/glossary/models.py, packages/sft-domain/src/sft_domain/glossary/loader.py, packages/sft-domain/src/sft_domain/glossary/it.yaml, packages/sft-domain/src/sft_domain/glossary/en.yaml, packages/sft-domain/tests/test_glossary_loader.py</name>
  <read_first>
    - 02-RESEARCH.md § Pattern 1 Pydantic Glossary Loader (lines 341-400) for exact code shape
    - D-29 + D-30 + D-31 in 02-CONTEXT.md for YAML schema and term distribution (~100 textile + ~40 agentic + ~10 economia)
    - 02-PATTERNS.md § `glossary/__init__.py` (lines 216-235) — module exports pattern
    - 02-PATTERNS.md § Shared Patterns "YAML safe load" (lines 742-749) — security mandate
    - 02-PATTERNS.md § Shared Patterns "Immutability for Pydantic models" (lines 781-788)
    - 02-RESEARCH.md § Open Questions #2 + #4 — expose both `load_terms` and `load_terms_dict`; bootstrap must cover Wave 2 SOP bold tokens
  </read_first>
  <behavior>
    - `from sft_domain.glossary import Category, Term, load_terms, load_terms_dict` succeeds.
    - `Term` has `model_config = {"frozen": True, "extra": "forbid"}` (instances immutable, unknown fields rejected).
    - `load_terms("it")` returns list[Term] of length ≥ 70.
    - `load_terms("en")` returns list[Term] of length ≥ 70.
    - `load_terms_dict("it")["pick density"]` returns the matching Term (case-insensitive key normalization).
    - `load_terms("xx")` raises FileNotFoundError or pydantic.ValidationError (invalid lang).
    - `load_terms.cache_info().hits ≥ 1` after second call (lru_cache works).
    - The IT and EN bootstrap glossaries contain ALL bold-eligible tokens for Plan 04's 5 example SOPs: loom troubleshooting, dyeing bath preparation, spindle calibration, 4-point grading, broken end detection. Minimum required terms per language (case-insensitive):
      textile-process: weaving, spinning, warping, dyeing, finishing
      textile-asset: loom, ring spinning frame, jet dyeing machine, warping creel, stenter
      textile-defect: slub, broken end, mispick, selvage fault, shade deviation, neppy, unlevel dyeing
      textile-kpi: oee, mttr, mtbf, pick density, throughput, scrap rate
      textile-tool-ppe: calibro digitale, picometro, conta-trama, igrometro (IT) / digital caliper, pick counter, hygrometer (EN); PPE basics
      textile-material: warp yarn, weft yarn, cotton, tex, nm
      agentic-platform: hitl, langgraph, embedding, retrieval, audit trail, checkpoint, interrupt
      agentic-tool: tool, function calling
      regulatory: gdpr
  </behavior>
  <action>
    Create models.py per RESEARCH.md Pattern 1 code (lines 347-372): Category enum with 9 members matching D-30 string values exactly (`"textile-process"`, etc.); Term BaseModel with `model_config = {"frozen": True, "extra": "forbid"}`, fields `term: str`, `definition: str`, `category: Category`, `related_terms: list[str] = Field(default_factory=list)`, `examples: list[str] = Field(default_factory=list)`, `source: str | None = None`.

    Create loader.py per RESEARCH.md Pattern 1 code (lines 374-397): module-level `_GLOSSARY_DIR = Path(__file__).parent`; `@lru_cache(maxsize=2)` on `load_terms(lang: Literal["it","en"]) -> list[Term]` — opens `_GLOSSARY_DIR / f"{lang}.yaml"`, raises FileNotFoundError if missing, uses `yaml.safe_load` (NEVER `yaml.load`), returns `[Term.model_validate(item) for item in raw]`; `@lru_cache(maxsize=2)` on `load_terms_dict(lang) -> dict[str, Term]` — builds `{t.term.lower(): t for t in load_terms(lang)}`.

    Create __init__.py per 02-PATTERNS.md lines 227-234: docstring line + 2 import lines + `__all__ = ["Category", "Term", "load_terms", "load_terms_dict"]`. Do NOT add `__version__` (lives only at `sft_domain/__version__.py`).

    Author it.yaml and en.yaml: ~70 entries each (target distribution ~50 textile + ~20 agentic), MUST cover the term list in the <behavior> minimum-required set. Use YAML list form per D-29 example. Sort entries alphabetically by `term` for deterministic git diffs. Each entry on its own block, fields in stable order (term, definition, category, related_terms?, examples?, source). For terms with no clean Italian equivalent (e.g. some agentic tooling), keep English in IT yaml and add `note: no-direct-equivalent` in a comment ABOVE the entry (NOT in the data — schema forbids extras).

    Create test_glossary_loader.py with at least 6 tests:
      - test_load_terms_it_returns_list_min_70 (asserts `len(load_terms("it")) >= 70`)
      - test_load_terms_en_returns_list_min_70
      - test_load_terms_dict_lookup_pick_density (`load_terms_dict("en")["pick density"]` returns Term with category TEXTILE_KPI)
      - test_term_is_frozen (instantiate, then `pytest.raises(pydantic.ValidationError)` on attribute assignment OR check `Term.model_config["frozen"] is True`)
      - test_unknown_lang_raises (e.g., pytest.raises with `load_terms("xx")` — Literal type doesn't enforce at runtime; rely on `FileNotFoundError` for nonexistent file)
      - test_lru_cache_hits (call `load_terms.cache_clear()`, call twice, assert `load_terms.cache_info().hits == 1`)
  </action>
  <verify>
    <automated>uv run --project packages/sft-domain pytest packages/sft-domain/tests/ -v --tb=short &amp;&amp; grep -RE "yaml\.load\(|Loader=" packages/sft-domain/src/ &amp;&amp; exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^- term:' packages/sft-domain/src/sft_domain/glossary/it.yaml` ≥ 70
    - Source assertion: `grep -c '^- term:' packages/sft-domain/src/sft_domain/glossary/en.yaml` ≥ 70
    - Source assertion: `grep -F 'yaml.safe_load' packages/sft-domain/src/sft_domain/glossary/loader.py` exits 0
    - Source assertion: `grep -RE "yaml\.load\(|Loader=" packages/sft-domain/src/sft_domain/` returns nothing (security — V12 + RESEARCH Pitfall 3)
    - Source assertion: `grep -F 'frozen": True' packages/sft-domain/src/sft_domain/glossary/models.py` exits 0
    - Source assertion: `grep -F '"extra": "forbid"' packages/sft-domain/src/sft_domain/glossary/models.py` exits 0
    - Source assertion: every minimum-required term from the <behavior> list appears in BOTH it.yaml and en.yaml (verified inline by the loader test_lookup tests)
    - Behavior: `uv run --project packages/sft-domain pytest packages/sft-domain/tests/` reports ≥ 9 passed (3 schema + 6 loader)
    - Behavior: `uv run --project packages/sft-domain python -c "from sft_domain.glossary import load_terms; print(len(load_terms('it')), len(load_terms('en')))"` prints two integers ≥ 70
    - Behavior: `uv run --project packages/sft-domain python -c "from sft_domain.glossary import load_terms_dict; print(load_terms_dict('en')['pick density'].category)"` prints `Category.TEXTILE_KPI`
  </acceptance_criteria>
  <done>Glossary loader + bootstrap YAML + tests committed; downstream plans can call `sft_domain.glossary.load_terms(lang)` and rely on ≥70 terms per language covering all Wave 2 SOP bold tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file → Python loader | Untrusted-by-default git-tracked content parsed at runtime; mitigated by safe_load + frozen Pydantic models |
| JSON Schema file → jsonschema validator | git-tracked; validated as Draft 2020-12 to catch schema mistakes |
| New pip deps → uv lockfile | pydantic, pyyaml, jsonschema, python-frontmatter cross supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | packages/sft-domain/src/sft_domain/glossary/loader.py | mitigate | Mandatory `yaml.safe_load` (never `yaml.load`/`Loader=`); enforced by Task 3 acceptance grep (V12, CWE-502, Bandit B506) |
| T-02-02 | Tampering | packages/sft-domain/src/sft_domain/glossary/models.py | mitigate | Pydantic `model_config = {"frozen": True, "extra": "forbid"}` rejects unexpected fields and prevents post-construction mutation (V5 input validation, V14 strict schema) |
| T-02-03 | Information Disclosure | packages/sft-domain/src/sft_domain/glossary/{it,en}.yaml | mitigate | Phase 1 gitleaks pre-commit hook already scans staged files; this plan does NOT add credential-like patterns; explicit review checklist for bootstrap YAML |
| T-02-04 | Tampering | new runtime/dev deps (pydantic, pyyaml, jsonschema, python-frontmatter) | mitigate | All four are PyPI mainstream packages (Approvato per 02-RESEARCH.md § Package Legitimacy Audit lines 197-203); upper version bounds set in pyproject.toml; Phase 1 license-scan.yml gates licenses |
| T-02-05 | Denial of Service | load_terms() on malformed YAML | accept | Phase 2 is content-engineering; failures are CI-visible exit 1 with file path. No runtime user input → no DoS surface. |
| T-02-SC | Tampering | pip installs (pydantic, pyyaml, jsonschema, python-frontmatter) | mitigate | Audit table in 02-RESEARCH.md lines 197-203 dispositioned all four as Approvato; lockfile (uv.lock) commits exact resolved versions; no [ASSUMED]/[SUS] packages remain — no blocking-human checkpoint needed |
</threat_model>

<verification>
- All three schemas parse as valid Draft 2020-12.
- Loader returns ≥70 terms per language and supports O(1) dict lookup.
- pytest suite exits 0 with ≥9 tests.
- `uv sync --all-packages` exits clean (no deprecation warnings).
- No `yaml.load` / `Loader=` anywhere under packages/sft-domain/.
</verification>

<success_criteria>
- DOC-18: bootstrap bilingual glossary committed under sft-domain canonical path; loader API operational; schemas validate the data.
- Wave 2 unblocked: Plan 02 (domain) + Plan 03 (assumptions) + Plan 04 (example SOPs) can all import the schemas + loader without scaffolding.
- Open Question #2 resolved: both `load_terms` and `load_terms_dict` exposed.
- Open Question #4 resolved: bootstrap glossary pre-seeds every bold token needed by the 5 example SOPs of Plan 04.
</success_criteria>

<output>
Create `.planning/phases/02-domain-modeling-synthetic-corpus/02-01-SUMMARY.md` when done.
</output>
