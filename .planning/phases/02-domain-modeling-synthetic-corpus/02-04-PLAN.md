---
phase: 02-domain-modeling-synthetic-corpus
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - simulators/synthetic-corpus/project.json
  - simulators/synthetic-corpus/README.md
  - simulators/synthetic-corpus/it/loom/SOP-LOOM-001-troubleshoot-broken-end-it.md
  - simulators/synthetic-corpus/it/dyeing/SOP-DYE-001-bath-preparation-it.md
  - simulators/synthetic-corpus/it/spinning/SOP-SPN-001-spindle-calibration-it.md
  - simulators/synthetic-corpus/it/quality_grading/SOP-QLT-001-four-point-grading-it.md
  - simulators/synthetic-corpus/it/loom/SOP-LOOM-002-warp-tension-drift-it.md
  - scripts/validate-corpus-frontmatter.py
  - scripts/validate-corpus-pairing.py
  - scripts/validate-bilingual-mirror.py
  - tests/test_corpus_inventory.py
autonomous: true
requirements: [KNW-10]
must_haves:
  truths:
    - "simulators/synthetic-corpus is a registered Nx project with validate-frontmatter + validate-pairing targets (Open Question #1 resolved)"
    - "Five IT SOPs exist (1 per asset family from D-27: loom troubleshoot + loom warp-tension + dyeing + spinning + quality) — D-25 hybrid review pattern: drafts marked `status: draft-unreviewed` by default until user review pass"
    - "Every SOP frontmatter validates against packages/sft-domain/src/sft_domain/schemas/sop.schema.json"
    - "Every SOP body has the 7 required H2 sections from D-26 (Scope, Prerequisites, Tools and PPE, Step-by-step Procedure, Verification, Troubleshooting, References)"
    - "scripts/validate-corpus-frontmatter.py and scripts/validate-corpus-pairing.py and scripts/validate-bilingual-mirror.py all exit 0 against the 5 SOPs + the domain pages from Plan 02 (validators ship in Task 1 BEFORE SOPs are written in Task 2)"
    - "simulators/synthetic-corpus/README.md documents D-25 hybrid workflow, D-28 style, frontmatter schema link, and the Open Question #5 retrieval filter contract for Phase 5"
  artifacts:
    - path: "simulators/synthetic-corpus/project.json"
      provides: "Nx project with validate-frontmatter, validate-pairing targets; implicitDependencies on sft-domain (schema)"
      contains: "validate-frontmatter"
    - path: "simulators/synthetic-corpus/README.md"
      provides: "Authoring guidelines + D-25 hybrid workflow + Open Question #5 retrieval contract"
      contains: "draft-unreviewed"
    - path: "scripts/validate-corpus-frontmatter.py"
      provides: "Per-file jsonschema validation of every .md frontmatter under simulators/synthetic-corpus + required H2 section enforcement"
      contains: "Draft202012Validator"
    - path: "scripts/validate-corpus-pairing.py"
      provides: "Per `id`, asserts exactly one lang=it and one lang=en (Pitfall 7 mitigation)"
      contains: "lang"
    - path: "scripts/validate-bilingual-mirror.py"
      provides: "Per docs/docs/*.md asserts docs/docs/en/*.md mirror exists with matching H1 + first 5 H2"
      contains: "headings"
  key_links:
    - from: "simulators/synthetic-corpus/it/*/SOP-*.md"
      to: "packages/sft-domain/src/sft_domain/schemas/sop.schema.json"
      via: "python-frontmatter + jsonschema Draft202012Validator"
      pattern: "sop.schema.json"
    - from: "scripts/validate-corpus-pairing.py"
      to: "simulators/synthetic-corpus/{it,en}/**/*.md"
      via: "pathlib.Path.rglob + id grouping"
      pattern: "rglob"
---

<objective>
Stand up the synthetic SOP corpus (KNW-10) per D-25/D-26/D-27/D-28 with vertical-slice coverage: 1 SOP per asset family (5 IT files — loom×2 for troubleshoot vs warp-tension to balance D-27 mix, dyeing, spinning, quality_grading per Plan 02-01 sop.schema.json enum). Pre-marked `status: draft-unreviewed` per D-25 fallback — Plan 07 promotes to `status: reviewed` after user pass. **Task ordering (revised):** Task 1 ships the three corpus-validation scripts FIRST (frontmatter + pairing + bilingual-mirror) so Task 2 can validate every SOP at write-time.

EN translations of the 5 SOPs are deferred to Plan 05 alongside the remaining 15 IT SOPs. Plan 04 ships IT-only and the validators TOLERATE missing EN pairings via a `--allow-missing-en` flag that Plan 05 will drop.

Open Question #1 resolved: synthetic-corpus is a real Nx project (project.json + README, no pyproject.toml/src) so `nx affected --target=validate-frontmatter` correctly skips when the corpus is untouched.

Open Question #5 resolved: README.md publishes the contract that Phase 5 retrieval default-filters `status: reviewed`; agents wishing to surface drafts must opt-in via explicit query parameter.

Purpose: KNW-10 requires ≥20 bilingual SOPs. This plan ships 5 IT examples + the validation framework that will gate all 40 files (20 IT + 20 EN) once Plans 05/07 complete the corpus.

Output: synthetic-corpus Nx project + README + 5 IT SOPs + 3 validator scripts + pytest inventory test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-domain-modeling-synthetic-corpus/02-CONTEXT.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-RESEARCH.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-PATTERNS.md
@.planning/phases/02-domain-modeling-synthetic-corpus/02-01-PLAN.md
@simulators/sim-textile/project.json
@simulators/sim-textile/README.md
@scripts/validate-nx-graph.py
@scripts/sync-python-versions.py
@packages/sft-domain/src/sft_domain/schemas/sop.schema.json

<interfaces>
<!-- Filename convention per D-26 + RESEARCH Pitfall 7 -->
- Path: simulators/synthetic-corpus/{it,en}/{loom,dyeing,spinning,quality_grading}/SOP-{FAMILY}-{NNN}-{slug}-{lang}.md (4 subdirectories: loom + dyeing + spinning + quality_grading; weaving is grouped under loom-asset SOPs, warping is currently out-of-scope for Phase 2 examples but the asset_family enum supports it for future phases)
- id pattern (frontmatter): ^SOP-[A-Z]+-[0-9]{3}$
- Naming: `SOP-LOOM-001-troubleshoot-broken-end-it.md`; the EN counterpart will be `SOP-LOOM-001-troubleshoot-broken-end-en.md` under simulators/synthetic-corpus/en/loom/

<!-- Required SOP frontmatter (subset per D-26 — full schema in Plan 01 sop.schema.json) -->
id, title, version (1.0), lang (it|en), asset, asset_family (weaving|spinning|warping|dyeing|finishing|quality_grading — 6 values per Plan 02-01 sop.schema.json), role, hazard_level, estimated_duration_min, prerequisites (optional), related_glossary (optional), tags, audience, status (draft-unreviewed default per D-25 fallback), created_in_phase (2).

<!-- Required H2 sections per D-26 (fixed order) -->
1. ## Scope
2. ## Prerequisites
3. ## Tools and PPE
4. ## Step-by-step Procedure
5. ## Verification
6. ## Troubleshooting
7. ## References
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 3 corpus validators (frontmatter+H2, pairing, bilingual-mirror) + pytest inventory test scaffold</name>
  <files>scripts/validate-corpus-frontmatter.py, scripts/validate-corpus-pairing.py, scripts/validate-bilingual-mirror.py, tests/test_corpus_inventory.py</files>
  <read_first>
    - scripts/validate-nx-graph.py (full file — analog shape per 02-PATTERNS.md lines 144-213)
    - 02-RESEARCH.md § Pattern 3 (lines 464-520) — frontmatter validator code
    - 02-RESEARCH.md § Pattern 4 (lines 521-565) — bilingual mirror H1+H2 logic
    - 02-RESEARCH.md § Pitfall 7 (lines 764-770) — pairing check (one it + one en per id, naming convention)
    - D-24 + D-26 in 02-CONTEXT.md
    - 02-VALIDATION.md § Per-Task Verification Map
    - packages/sft-domain/src/sft_domain/schemas/sop.schema.json (Plan 02-01 output — exact required fields including `asset_family` enum with 6 values weaving/spinning/warping/dyeing/finishing/quality_grading)
  </read_first>
  <action>
    **Task ordering rationale:** This task ships FIRST (before Task 2 authors the 5 SOPs) so that every SOP is schema-validated the moment it is written. Reverse of the prior plan version — committing SOPs without a validator allows shape/H2/enum drift to slip in undetected.

    scripts/validate-corpus-frontmatter.py (follows validate-nx-graph.py shape):
      - args: `--corpus-dir` (Path, default `simulators/synthetic-corpus`), `--schema-file` (Path, default `packages/sft-domain/src/sft_domain/schemas/sop.schema.json`)
      - Load schema via json.loads + Draft202012Validator
      - For every `*.md` under corpus-dir (recursive via pathlib.Path.rglob):
        1. `frontmatter.load(md_path)` → post.metadata + post.content
        2. `list(validator.iter_errors(post.metadata))` — accumulate errors
        3. Additional H2 check: extract H2 lines via regex `re.compile(r"^##\s+(.+?)\s*$", re.MULTILINE)` from post.content; assert the sequence equals exactly `["Scope", "Prerequisites", "Tools and PPE", "Step-by-step Procedure", "Verification", "Troubleshooting", "References"]` (D-26). Mismatch → accumulate descriptive error including expected vs got.
        4. Filename invariant: filename must match `^SOP-[A-Z]+-[0-9]{3}-[a-z0-9-]+-(it|en)\.md$` and the `lang` suffix must equal frontmatter `lang` field (Pitfall 7 mitigation).
      - If corpus-dir is empty (no `*.md` yet — Task 2 hasn't run): exit 0 with `OK: no SOPs found yet (corpus-dir empty)` — script is safe to run before content exists.
      - Emit grouped stderr; print `OK: validated N SOPs (M errors)` on success; suggest `Fix:` line on every error
      - Exit 0 / 1

    scripts/validate-corpus-pairing.py (Pitfall 7):
      - args: `--corpus-dir` (default `simulators/synthetic-corpus`), `--allow-missing-en` (boolean — used by Plan 02-04 since EN translations come in Plan 02-05; Plan 05 drops this flag after EN ships)
      - For every `*.md`, parse frontmatter via python-frontmatter, group by `id` field. For each id assert:
        - exactly one entry with `lang: it` (always required)
        - exactly one entry with `lang: en` UNLESS `--allow-missing-en` (then warn on stdout, not error)
        - if both exist: their frontmatter `asset`, `asset_family`, `role`, `hazard_level` must MATCH (semantic pairing)
      - If corpus-dir is empty: exit 0 (analogous to validate-corpus-frontmatter.py).
      - Accumulate errors; emit grouped report; `Fix:` line on each
      - Exit 0 / 1

    scripts/validate-bilingual-mirror.py (D-24):
      - args: `--docs-dir` (Path, default `docs/docs`), `--allow-missing-en` (boolean)
      - Copy logic from RESEARCH.md Pattern 4 lines 528-563 verbatim with the heading H1+H2 match (max H1+5 H2). EXCLUDE set: `{"en", "assets"}`. For every `docs/docs/X.md` (recursive) NOT under `en/` or `assets/`, find counterpart at `docs/docs/en/X.md`. Assert:
        - counterpart exists (unless `--allow-missing-en` is set, then warn on stdout, not error)
        - H1 + first 5 H2 levels match (NOT text — translations differ; per Claude's discretion in CONTEXT.md line 429-430)
        - heading counts match
      - Exit 0 / 1
      - Plan 02-05 drops the `--allow-missing-en` flag after EN ships. Document this contract in the script docstring + 02-VALIDATION.md note.

    tests/test_corpus_inventory.py (extends pytest scaffold from Plan 02-02):
      - test_corpus_dir_exists — assert `simulators/synthetic-corpus` directory exists (it WILL exist after Task 2 runs; for now, skip via `pytest.importorskip` style or `pytest.mark.skipif(not corpus_path.exists())` — the test is parked here, exercised after Task 2)
      - test_distribution_phase04 — parametrized: for each asset_family in [weaving, dyeing, spinning, quality_grading], count IT files under simulators/synthetic-corpus/it/{family-subdir}/ and assert ≥1 (relaxed for Plan 04; Plan 05 tightens to ==5). Note: `weaving` files live under `it/loom/` (asset directory), `quality_grading` under `it/quality_grading/`.
      - test_all_status_draft_unreviewed_or_reviewed — frontmatter status field must be one of `{"reviewed", "draft-unreviewed", "deprecated"}` (matches sop.schema.json enum); skip if corpus is empty.
      - test_filename_matches_frontmatter_lang — pairing helper (D-26 + Pitfall 7); skip if corpus is empty.

    **Bootstrap behavior:** when run BEFORE Task 2 (no SOPs exist yet), all three validators exit 0 (graceful empty-corpus handling) and pytest tests skip — they go green automatically after Task 2 populates the corpus.
  </action>
  <verify>
    <automated>python3 scripts/validate-corpus-frontmatter.py &amp;&amp; python3 scripts/validate-corpus-pairing.py --allow-missing-en &amp;&amp; python3 scripts/validate-bilingual-mirror.py --allow-missing-en &amp;&amp; uv run pytest tests/test_corpus_inventory.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: all three scripts contain `yaml.safe_load` OR `frontmatter.load` (which uses safe load internally); none contain `yaml.load(` or `Loader=` (V12)
    - Source assertion: validate-corpus-frontmatter.py contains `Draft202012Validator` and uses `frontmatter.load`
    - Source assertion: validate-corpus-pairing.py and validate-bilingual-mirror.py BOTH expose `--allow-missing-en` flag (`grep -F 'allow-missing-en' scripts/validate-corpus-pairing.py scripts/validate-bilingual-mirror.py` exits 0 with 2 matches)
    - Source assertion: `pathlib.Path` used everywhere (no `os.path.join`) — `grep -E 'os\.path\.(join|exists)' scripts/validate-corpus-*.py scripts/validate-bilingual-mirror.py` returns nothing
    - Source assertion: no `shell=True` anywhere (`grep -E 'shell\s*=\s*True' scripts/validate-corpus-*.py scripts/validate-bilingual-mirror.py` returns nothing)
    - Behavior: from a clean state (no SOPs yet), `python3 scripts/validate-corpus-frontmatter.py` exits 0 with `OK: no SOPs found yet` (graceful empty-corpus handling)
    - Behavior: `python3 scripts/validate-corpus-pairing.py --allow-missing-en` exits 0 against empty corpus
    - Behavior: `python3 scripts/validate-bilingual-mirror.py --allow-missing-en` exits 0 against the Plan 02-02 domain pages (which have no EN counterparts yet)
    - Behavior: `uv run pytest tests/test_corpus_inventory.py -v` reports tests collected (≥4) and either pass or skip depending on whether Task 2 has run
    - Behavior (negative, manual — exercised in Task 2 acceptance): adding a SOP with missing `## References` H2 causes validate-corpus-frontmatter.py exit 1 with `Fix: add ## References section`
  </acceptance_criteria>
  <done>Three corpus validators committed and operational against empty corpus + Plan 02-02 domain pages; pytest inventory test scaffold parked, ready to exercise the 5 SOPs of Task 2 immediately upon write.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap synthetic-corpus Nx project + README + 5 IT example SOPs (draft-unreviewed) — validators from Task 1 gate every write</name>
  <files>simulators/synthetic-corpus/project.json, simulators/synthetic-corpus/README.md, simulators/synthetic-corpus/it/loom/SOP-LOOM-001-troubleshoot-broken-end-it.md, simulators/synthetic-corpus/it/loom/SOP-LOOM-002-warp-tension-drift-it.md, simulators/synthetic-corpus/it/dyeing/SOP-DYE-001-bath-preparation-it.md, simulators/synthetic-corpus/it/spinning/SOP-SPN-001-spindle-calibration-it.md, simulators/synthetic-corpus/it/quality_grading/SOP-QLT-001-four-point-grading-it.md</files>
  <read_first>
    - simulators/sim-textile/project.json (full file — 02-PATTERNS.md lines 332-358 — analog for synthetic-corpus/project.json)
    - simulators/sim-textile/README.md (analog for tone + scope callout — 02-PATTERNS.md lines 390-397)
    - D-25, D-26, D-27, D-28 in 02-CONTEXT.md
    - 02-RESEARCH.md § Open Question #1 (Nx project structure) + #5 (status filter contract) — both marked RESOLVED with reference to this plan
    - 02-PATTERNS.md § synthetic-corpus (lines 326-397)
    - packages/sft-domain/src/sft_domain/schemas/sop.schema.json (Plan 02-01 output — exact required fields; asset_family enum is 6 values: weaving/spinning/warping/dyeing/finishing/quality_grading)
    - scripts/validate-corpus-frontmatter.py (Task 1 output — run this after every SOP write)
  </read_first>
  <action>
    **Task ordering rationale:** Task 1 already created the validators. This task authors content AND runs the validator after every file write to catch enum/H2/filename mistakes immediately.

    project.json: copy template from 02-PATTERNS.md lines 360-381 verbatim. `name: "synthetic-corpus"`, `projectType: "library"`, `sourceRoot: "simulators/synthetic-corpus"`, two targets `validate-frontmatter` and `validate-pairing` invoking `python3 scripts/validate-corpus-frontmatter.py --corpus-dir simulators/synthetic-corpus` (resp. validate-corpus-pairing.py) via `nx:run-commands`. `implicitDependencies: ["sft-domain"]` because the schema lives in sft-domain. Add `inputs` per target for nx affected cache invalidation: `["{projectRoot}/**/*.md", "{workspaceRoot}/packages/sft-domain/src/sft_domain/schemas/sop.schema.json", "{workspaceRoot}/scripts/validate-corpus-frontmatter.py"]` on validate-frontmatter and analog for pairing.

    README.md (sibling-style adaptation from sim-textile/README.md per 02-PATTERNS.md lines 389-397): title `# synthetic-corpus`; one-line description "Bilingual (IT+EN) synthetic SOP corpus for textile manufacturing — retrieval target for Phase 5 Knowledge Layer."; sections required:
      - `## Scope` — what this corpus is (KNW-10 deliverable), what it is NOT (real Mantis IP, deep-tech parameters)
      - `## Directory layout` — `it/{loom,dyeing,spinning,quality_grading}/`, `en/{loom,dyeing,spinning,quality_grading}/`, filename convention `SOP-{FAMILY}-{NNN}-{slug}-{lang}.md`. Note: directory names map to asset / asset_family scope; the asset_family enum in sop.schema.json is 6 values (weaving, spinning, warping, dyeing, finishing, quality_grading) but Phase 2 examples cover only 4 subdirs.
      - `## Frontmatter schema` — link to `packages/sft-domain/src/sft_domain/schemas/sop.schema.json`; list of required frontmatter fields with one-line semantic; pointer to D-26. Document the 6-value asset_family enum and the `process` (D-21, 5 values) vs `asset_family` (SOP scope, 6 values) distinction: `quality_grading` is a cross-cutting inspection activity (not a textile process) that operates across all processes per D-27.
      - `## Authoring workflow (D-25 hybrid)` — bullet flow: Claude draft IT → human review IT → translate EN → human review EN pass-2 (faster); fallback option: ship as `status: draft-unreviewed` if review backlog; explain that the SOP `status` field gates Phase 5 retrieval
      - `## Style (D-28)` — factory-floor realistic, industrial jargon allowed, units of measure `g/m²`, `tex`, `Nm`, `picks per cm`; range tipici industria NOT exact Mantis values
      - `## Validation` — three commands: `npx nx run synthetic-corpus:validate-frontmatter`, `npx nx run synthetic-corpus:validate-pairing`, `python3 scripts/validate-bilingual-mirror.py --docs-dir docs/docs`
      - `## Retrieval contract for Phase 5 (Open Question #5)` — explicit paragraph: "Phase 5 (Knowledge Layer) ingestion pipelines MUST default-filter `status: reviewed` SOPs when surfacing to agents. SOPs with `status: draft-unreviewed` are visible only through an explicit opt-in query parameter or developer mode. Rationale: draft content has not passed domain-expert sanity check and must NOT become false ground truth in agent eval/test suites."

    Five IT example SOPs (one per asset family from D-27, with loom×2 to balance future expansion):
      - SOP-LOOM-001-troubleshoot-broken-end-it.md (asset: loom, asset_family: **weaving**, role: operator, hazard_level: low, estimated_duration_min: 15)
      - SOP-LOOM-002-warp-tension-drift-it.md (asset: loom, asset_family: **weaving**, role: technician, hazard_level: medium, estimated_duration_min: 30)
      - SOP-DYE-001-bath-preparation-it.md (asset: jet dyeing machine, asset_family: **dyeing**, role: technician, hazard_level: medium, estimated_duration_min: 45)
      - SOP-SPN-001-spindle-calibration-it.md (asset: ring spinning frame, asset_family: **spinning**, role: technician, hazard_level: low, estimated_duration_min: 25)
      - SOP-QLT-001-four-point-grading-it.md (asset: inspection table, asset_family: **quality_grading** — NOT `quality`; the enum value is `quality_grading` per Plan 02-01 sop.schema.json, role: quality-manager, hazard_level: low, estimated_duration_min: 20)

    Each SOP:
      - Frontmatter (YAML between `---` lines) with EVERY required field from sop.schema.json + `status: draft-unreviewed` (D-25 fallback default) + `created_in_phase: 2` + `lang: it` + `version: "1.0"` + `tags: [...]` + `audience: operations` + `related_glossary: [...]` listing terms that EXIST in Plan 02-01 bootstrap glossary (e.g. for SOP-LOOM-001: `[broken_end, pick_density, warp_yarn, weft_yarn]`).
      - All 7 H2 sections from D-26 IN FIXED ORDER: Scope, Prerequisites, Tools and PPE, Step-by-step Procedure, Verification, Troubleshooting, References.
      - Body uses **bold** for every textile/agentic term it references — and those bold tokens MUST all exist in Plan 02-01 bootstrap it.yaml glossary (no new terms introduced; Plan 05 expands glossary). Cross-reference Plan 02-01's minimum-required term list before authoring.
      - Italian standard, factory-floor style per D-28; range tipici industria only, NO specific Mantis dyeing temperatures / tensions (D-28 boundary).
      - Verification section: concrete operator-side checks (e.g., "verifica visiva: assenza di **broken end** sui successivi 20 m di tessuto").
      - References section: bullet links to /docs/glossary IT entries (using anchors `../../docs/docs/glossary.md#term-slug`) and to related SOP files in the corpus.

    **After each SOP file is written:** run `python3 scripts/validate-corpus-frontmatter.py --corpus-dir simulators/synthetic-corpus` (the Task 1 validator) and fix any error before moving to the next SOP. Final state: all 5 SOPs pass schema + H2 + filename validation.

    Boundary: Phase 2 LLM-drafted; user pass on technical content happens before promotion to `status: reviewed` in Plan 07. The `draft-unreviewed` flag is the contract that gates Phase 5 retrieval.
  </action>
  <verify>
    <automated>test -f simulators/synthetic-corpus/project.json &amp;&amp; test -f simulators/synthetic-corpus/README.md &amp;&amp; npx nx show project synthetic-corpus --json &gt; /tmp/sc-show.json &amp;&amp; jq -e '.targets["validate-frontmatter"]' /tmp/sc-show.json &amp;&amp; jq -e '.targets["validate-pairing"]' /tmp/sc-show.json &amp;&amp; for f in simulators/synthetic-corpus/it/loom/SOP-LOOM-00{1,2}-*.md simulators/synthetic-corpus/it/dyeing/SOP-DYE-001-*.md simulators/synthetic-corpus/it/spinning/SOP-SPN-001-*.md simulators/synthetic-corpus/it/quality_grading/SOP-QLT-001-*.md; do test -f "$f" || { echo MISSING $f; exit 1; }; for h in Scope Prerequisites "Tools and PPE" "Step-by-step Procedure" Verification Troubleshooting References; do grep -q "^## $h$" "$f" || { echo "MISSING H2 $h in $f"; exit 1; }; done; done; grep -q "draft-unreviewed" simulators/synthetic-corpus/README.md &amp;&amp; grep -q "status: reviewed" simulators/synthetic-corpus/README.md &amp;&amp; grep -q "quality_grading" simulators/synthetic-corpus/README.md &amp;&amp; python3 scripts/validate-corpus-frontmatter.py --corpus-dir simulators/synthetic-corpus &amp;&amp; python3 scripts/validate-corpus-pairing.py --corpus-dir simulators/synthetic-corpus --allow-missing-en &amp;&amp; uv run pytest tests/test_corpus_inventory.py -v --tb=short &amp;&amp; echo "OK 5 SOPs + Nx project + validators green"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `npx nx show project synthetic-corpus` succeeds and lists targets `validate-frontmatter` + `validate-pairing`
    - Source assertion: `jq '.implicitDependencies' simulators/synthetic-corpus/project.json` contains `"sft-domain"`
    - Source assertion: all 5 SOP files exist at the exact paths in `files_modified` (note: SOP-QLT-001 lives under `it/quality_grading/`, NOT `it/quality/`)
    - Source assertion: each SOP file contains all 7 required H2 sections in fixed order (verify with awk/sed: extract H2s, compare to expected sequence)
    - Source assertion: each SOP frontmatter contains `status: draft-unreviewed` (D-25 fallback default)
    - Source assertion: each SOP frontmatter `id` matches `^SOP-[A-Z]+-[0-9]{3}$`
    - Source assertion: SOP-QLT-001 frontmatter has `asset_family: quality_grading` (NOT `quality`) — `grep -q 'asset_family: quality_grading' simulators/synthetic-corpus/it/quality_grading/SOP-QLT-001-*.md`
    - Source assertion: `grep -RE '^asset_family:\s*quality\s*$' simulators/synthetic-corpus/` returns nothing (no bare `quality` value — only `quality_grading` per Plan 02-01 enum)
    - Source assertion: `grep -l 'Mantis' simulators/synthetic-corpus/it/` returns 0 results OR if Mantis is mentioned, it's only in a `!!! note "Mantis context"` admonition (no fabricated specific values)
    - Source assertion: `grep -i "accenture" simulators/synthetic-corpus/` returns nothing (brand-scrub)
    - Source assertion: README.md contains "draft-unreviewed", "reviewed", "Phase 5", "quality_grading", AND "process" vs "asset_family" distinction language
    - Source assertion: README.md links to `packages/sft-domain/src/sft_domain/schemas/sop.schema.json`
    - Behavior: `python3 scripts/validate-corpus-frontmatter.py --corpus-dir simulators/synthetic-corpus` exits 0 (all 5 SOPs pass schema + H2 + filename gate from Task 1 validator)
    - Behavior: `python3 scripts/validate-corpus-pairing.py --corpus-dir simulators/synthetic-corpus --allow-missing-en` exits 0
    - Behavior: `python3 scripts/validate-corpus-pairing.py --corpus-dir simulators/synthetic-corpus` (no flag) exits 1 (because EN files don't exist yet — pre-Plan-05 state)
    - Behavior: `uv run pytest tests/test_corpus_inventory.py -v` reports ≥4 cases passed (no longer skipped — corpus now populated)
    - Behavior (negative): manually edit one SOP to drop the `## References` H2 — validate-corpus-frontmatter.py exits 1 with `Fix: add ## References section` (revert before commit)
  </acceptance_criteria>
  <done>synthetic-corpus Nx project bootstrapped; 5 IT example SOPs cover D-27 distribution (1 per family + 1 extra loom) with `asset_family: quality_grading` per Plan 02-01 enum; README publishes Open Question #5 retrieval contract AND documents `process` vs `asset_family` distinction; every SOP gated by the Task 1 validator at write-time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOP Markdown (LLM-drafted) → Phase 5 RAG retrieval source | Status field gates exposure; draft-unreviewed → not surfaced by default |
| SOP frontmatter YAML → Python validators | python-frontmatter uses safe_load internally; jsonschema validates structure |
| `npx nx show project` → Nx project recognition | subprocess in CI environment; no shell=True |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | LLM-drafted SOP technical content (fabricated dyeing temperatures, impossible warp tensions — Phase 5 retrieval ground-truth risk) | mitigate | D-25 hybrid review pattern: all SOPs ship as `status: draft-unreviewed` from this plan; Phase 5 retrieval contract published in README.md filters to `status: reviewed` by default (Open Question #5); user pass before Plan 07 promotes to `reviewed` |
| T-02-17 | Tampering / EoP | SOP frontmatter YAML parsing | mitigate | python-frontmatter uses PyYAML safe_load internally; verified by acceptance grep; jsonschema Draft 2020-12 schema enforces field types |
| T-02-18 | Repudiation | SOP id collision across lang (one IT id reused for different EN content per Pitfall 7) | mitigate | validate-corpus-pairing.py enforces 1 IT + 1 EN per id with matching asset/family/role/hazard fields; filename convention embeds both id and lang for `ls`-visible drift |
| T-02-19 | Information Disclosure | SOP body content (Mantis-specific operational data) | mitigate | D-28 boundary explicit: range tipici industria only, NO real Mantis values; review checklist before promotion; gitleaks hook scans for credentials |
| T-02-20 | Tampering | path traversal in script discovery | mitigate | `pathlib.Path.rglob` only; no string concat; `--corpus-dir` argparsed as `type=Path` |
| T-02-21 | Tampering | brand contamination (Accenture / original brand) in SOP corpus | mitigate | Acceptance grep `grep -i accenture simulators/synthetic-corpus/` returns nothing; D-08 from Phase 1 enforced |
</threat_model>

<verification>
- synthetic-corpus is a registered Nx project with validate-frontmatter + validate-pairing targets.
- 5 IT example SOPs cover the 4 asset families (loom×2 + dyeing + spinning + quality).
- All 5 SOPs validate against sop.schema.json + 7 required H2 sections in fixed order.
- 3 validator scripts exit 0 with `--allow-missing-en` (since EN ships in Plan 05).
- pytest inventory tests pass ≥6 parametrized cases.
- README.md publishes Open Question #5 retrieval contract.
</verification>

<success_criteria>
- KNW-10 5/20 SOPs seeded with draft-unreviewed status (Plan 05 adds 15 more + EN translations of all 20).
- D-25 hybrid workflow operationalized — all corpus content gated by `status` field.
- D-26 schema + H2 contract enforced by frontmatter validator.
- D-27 distribution scaffold in place (1 per family + loom extra).
- D-28 style boundaries enforced (no fabricated Mantis values).
- Open Questions #1 + #5 resolved in code + docs.
</success_criteria>

<output>
Create `.planning/phases/02-domain-modeling-synthetic-corpus/02-04-SUMMARY.md` when done.
</output>
