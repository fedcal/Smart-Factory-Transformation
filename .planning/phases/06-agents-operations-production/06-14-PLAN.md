---
phase: 06-agents-operations-production
plan: 14
plan_id: 06-14
type: execute
wave: 5
depends_on: ["06-00", "06-06", "06-07", "06-08", "06-10"]
files_modified:
  - docs/agents/operations/operator-assistant.it.md
  - docs/agents/operations/operator-assistant.en.md
  - docs/agents/operations/production-planner.it.md
  - docs/agents/operations/production-planner.en.md
  - docs/agents/operations/quality-inspector.it.md
  - docs/agents/operations/quality-inspector.en.md
  - docs/agents/operations/anomaly-detector.it.md
  - docs/agents/operations/anomaly-detector.en.md
  - docs/mkdocs.yml
  - apps/agents/ops/anomaly-detector/tests/test_evidence_panel.py
  - apps/agents/ops/quality-inspector/tests/test_evidence_panel.py
  - apps/agents/ops/production-planner/tests/test_evidence_panel.py
  - apps/agents/ops/operator-assistant/tests/test_evidence_panel.py
autonomous: true
requirements: [OPS-05]
objective: "Ship MkDocs bilingual (IT + EN) documentation for each of the 4 OPS agents (8 pages) declaring: tools used, data sources, HITL tier, KPIs impacted (per OPS-05). Extend `docs/mkdocs.yml` nav. Add per-agent `test_evidence_panel.py` asserting the agent's EvidencePanel-shaped response includes the required fields."

must_haves:
  truths:
    - "Every agent has 2 MkDocs pages (IT + EN) explicitly listing: tools used, data sources, HITL tier (default + per-severity), KPIs impacted."
    - "mkdocs.yml nav includes the 8 new pages under Agents → Operations cluster section."
    - "`mkdocs build --strict` succeeds with no broken links."
    - "Each agent has a `test_evidence_panel.py` unit test asserting the response payload includes: agent_id, tool_inventory, data_sources, hitl_tier, kpis_impacted."
    - "Docs reference Mantis Textile Group context per PROJECT.md."
    - "All IT pages have a `lang: it` frontmatter field; EN pages have `lang: en`."
  artifacts:
    - path: docs/agents/operations/operator-assistant.it.md
      provides: "OperatorAssistant IT documentation"
      contains: "OperatorAssistant"
    - path: docs/agents/operations/operator-assistant.en.md
      provides: "OperatorAssistant EN documentation"
      contains: "OperatorAssistant"
    - path: docs/mkdocs.yml
      provides: "nav updated with 8 new pages"
      contains: "operator-assistant"
  key_links:
    - from: docs/agents/operations/*.md
      to: apps/agents/ops/*/src/
      via: "Agent slug + tool list"
      pattern: "tools used|data sources|HITL tier"
---

<objective>
OPS-05 mandates each agent declare its tools, data sources, HITL tier, and KPIs. Document this in bilingual MkDocs pages + enforce it via unit tests on the agent's EvidencePanel response.

Purpose: Satisfy OPS-05 + provide demo-ready documentation for competition deliverable (Phase 12).
Output: 8 docs pages + nav extension + 4 unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@.planning/PROJECT.md
@docs/mkdocs.yml
@docs/index.it.md
@apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/
@apps/agents/ops/quality-inspector/src/ops_quality_inspector/
@apps/agents/ops/production-planner/src/ops_production_planner/
@apps/agents/ops/operator-assistant/src/ops_operator_assistant/
@packages/sft-agents/src/sft_agents/models/evidence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 8 MkDocs agent pages (4 agents × IT+EN) + update mkdocs.yml nav</name>
  <files>
    docs/agents/operations/operator-assistant.it.md,
    docs/agents/operations/operator-assistant.en.md,
    docs/agents/operations/production-planner.it.md,
    docs/agents/operations/production-planner.en.md,
    docs/agents/operations/quality-inspector.it.md,
    docs/agents/operations/quality-inspector.en.md,
    docs/agents/operations/anomaly-detector.it.md,
    docs/agents/operations/anomaly-detector.en.md,
    docs/mkdocs.yml
  </files>
  <read_first>
    docs/mkdocs.yml (current nav structure),
    docs/index.it.md (existing IT page conventions),
    .planning/PROJECT.md (Mantis Textile Group context + core value),
    apps/agents/ops/*/src/*/agent.py + prompts.py (tool inventory, system prompts)
  </read_first>
  <action>
    Each MkDocs page follows this structure (sections in target language):
    ```markdown
    ---
    lang: it  # or en
    agent: <slug>
    requirements: [OPS-05]
    ---

    # <Agent Display Name>

    ## Overview
    [1-2 paragraphs describing role + value-prop in Mantis Textile Group context]

    ## Tools Used
    - `rag_search` — Phase 5; searches Qdrant SOPs with ACL filter.
    - `traverse_graph` — Phase 5; navigates Machine→Part→FailureMode→SOP.
    - ... [list ALL tools registered in the agent]

    ## Data Sources
    - PostgreSQL+TimescaleDB (sensor_events hypertable; audit.actions)
    - NATS JetStream (subjects consumed/published)
    - Qdrant collections (sop_chunks, ...)
    - Neo4j (textile entity graph)
    - YAML config: orders.yaml / anomaly_baselines.yaml / failure_modes.yaml as applicable

    ## HITL Tier
    | Severity / Decision | Tier | Approver Role |
    |---|---|---|
    | minor (auto) | none | n/a |
    | major | SUPERVISOR | shift supervisor |
    | critical | MANAGER + SAFETY_INTERLOCK | manager + safety officer |

    ## KPIs Impacted
    - MTTR (mean time to recovery) — affected via X
    - OEE (overall equipment effectiveness) — affected via Y
    - First-time-fix rate — affected via Z
    - [list KPIs per agent function]

    ## Invocation
    Endpoint: `POST /v1/agents/<slug>/<action>` body `{...}`
    Trigger: [manual operator / scheduler / NATS consumer]
    Thread ID: `ops.<slug>.<uuid4>`

    ## Audit Footprint
    - `audit.actions` rows with `agent_id=<slug>`, `action_type=<TYPE>` per scan/event.
    ```

    Concrete agent specifics:
    - **operator-assistant.it / .en**: 5 tools, MTTR + first-time-fix + knowledge-reuse KPIs; HITL via escalate_to_supervisor.
    - **production-planner.it / .en**: rag_search only (read-only knowledge use); on-time-delivery + OEE; HITL Tier=SUPERVISOR always.
    - **quality-inspector.it / .en**: rag_search + LLM grader; defect-rate + scrap-rate + dye-lot-deviation KPIs; HITL tier per severity table.
    - **anomaly-detector.it / .en**: query_timescale + AnomalyBaseline; MTBF + alert-fatigue rate KPIs; rate limit 12/h.

    Update `docs/mkdocs.yml`: add nav entries under existing Agents section:
    ```yaml
    nav:
      ...
      - Agents:
          - Operations:
              - OperatorAssistant: agents/operations/operator-assistant.md
              - ProductionPlanner: agents/operations/production-planner.md
              - QualityInspector: agents/operations/quality-inspector.md
              - AnomalyDetector: agents/operations/anomaly-detector.md
    ```
    (Use the existing i18n plugin pattern — IT/EN versions resolved automatically per Phase 2/5 convention.)
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; mkdocs build --strict --config-file docs/mkdocs.yml 2>&amp;1 | tail -20 &amp;&amp; ls docs/agents/operations/*.md | wc -l | grep -q "^8$"</automated>
  </verify>
  <done>8 docs pages exist; mkdocs build --strict succeeds; nav lists all 4 agents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 4 test_evidence_panel.py unit tests asserting OPS-05 declaration fields</name>
  <files>
    apps/agents/ops/anomaly-detector/tests/test_evidence_panel.py,
    apps/agents/ops/quality-inspector/tests/test_evidence_panel.py,
    apps/agents/ops/production-planner/tests/test_evidence_panel.py,
    apps/agents/ops/operator-assistant/tests/test_evidence_panel.py
  </files>
  <read_first>
    packages/sft-agents/src/sft_agents/models/evidence.py (EvidencePanel schema from Phase 4),
    apps/agents/ops/*/src/*/agent.py (response shape from each agent)
  </read_first>
  <behavior>
    Each `test_evidence_panel.py` asserts that the agent's response (returned from `__call__` or HTTP endpoint) includes — either as top-level fields or nested in an `evidence_panel` object — the OPS-05 declaration:
    - `agent_id` (slug)
    - `tool_inventory` (list of tool names)
    - `data_sources` (list of strings like "TimescaleDB sensor_events", "Qdrant sop_chunks")
    - `hitl_tier` (the tier actually invoked or "none")
    - `kpis_impacted` (list of KPI names)

    This may require adding these fields to each agent's response model (06-06/07/08/10) — if so, this task EXTENDS those modules with a small `_build_evidence_panel()` helper rather than re-implementing. Use module-level constants for tool_inventory + data_sources + kpis_impacted per agent (the same lists documented in MkDocs pages — single source of truth in code).
  </behavior>
  <action>
    Delete Wave 0 placeholders. Write unit tests per behavior. If the agent's existing response model lacks the field, add a constant + helper to the agent module + propagate in `__call__` return. Keep changes minimal — do not refactor existing logic.

    Concrete tool_inventory + data_sources + kpis_impacted constants live in each agent's `__init__.py` or a new `metadata.py` per agent for cleanliness:
    - operator-assistant: tools=[rag_search, traverse_graph, query_timescale, escalate_to_supervisor, log_event]; data_sources=[Qdrant sop_chunks, Neo4j textile graph, TimescaleDB sensor_events]; kpis_impacted=[mttr, first_time_fix_rate, knowledge_reuse_rate].
    - production-planner: tools=[rag_search]; data_sources=[orders.yaml, asset_capacity.yaml, failure_modes.yaml]; kpis_impacted=[on_time_delivery_rate, oee_availability].
    - quality-inspector: tools=[rag_search]; data_sources=[QualityEvent NATS, failure_modes.yaml]; kpis_impacted=[defect_rate_4pt, scrap_rate, dye_lot_deviation].
    - anomaly-detector: tools=[query_timescale]; data_sources=[TimescaleDB sensor_events, anomaly_baselines.yaml]; kpis_impacted=[mtbf, alert_fatigue_rate].
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/ops/anomaly-detector/tests/test_evidence_panel.py apps/agents/ops/quality-inspector/tests/test_evidence_panel.py apps/agents/ops/production-planner/tests/test_evidence_panel.py apps/agents/ops/operator-assistant/tests/test_evidence_panel.py -x</automated>
  </verify>
  <done>All 4 evidence panel tests green; OPS-05 declaration enforced in code + docs in lockstep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs (.md) → mkdocs build | Static content, repo-controlled |
| evidence_panel constants → agent response | Server-side declared, not user-modifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-doc-drift | Repudiation | docs vs code declaration | mitigate | test_evidence_panel.py + docs share the same constants (planner authored both lists in lockstep); reviewer cross-checks PR. |
| T-V6-brand-leak | Information Disclosure | docs mentioning "Accenture" | mitigate | Phase 12 ships CI brand-scrub gate; this plan avoids the prohibited brand explicitly. |
</threat_model>

<verification>
- `mkdocs build --strict --config-file docs/mkdocs.yml` green.
- 4 evidence-panel unit tests green.
- 8 docs files + nav updated.
- Brand scrub (Accenture grep) returns 0 hits.
</verification>

<success_criteria>
- OPS-05 mechanically verifiable: every agent response includes the 4 declared fields.
- Bilingual docs render correctly (IT default + EN parallel).
- No broken links in mkdocs build.
</success_criteria>

<output>
Create `.planning/phases/06-agents-operations-production/06-14-SUMMARY.md` documenting: 8 doc pages, evidence panel field schema, KPI list per agent.
</output>
