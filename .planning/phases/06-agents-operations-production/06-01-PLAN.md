---
phase: 06-agents-operations-production
plan: 01
plan_id: 06-01
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - infra/migrations/timescale/007_extend_audit_decisions.sql
  - infra/migrations/timescale/tests/test_migration_007.py
  - packages/sft-agents/src/sft_agents/models/enums.py
autonomous: false
requirements: [OPS-04, OPS-05]
objective: "DB migration 007 extends `audit.actions.decision` CHECK constraint to include `suppressed` + `escalation_request` + `logged` AND `action_type` to include new ops action types. Migration is BLOCKING (must be pushed before any code writes new enum values). Also extends `models/enums.py` Decision + ActionType enums in lockstep."

must_haves:
  truths:
    - "audit.actions table accepts INSERT with decision IN ('suppressed','logged') after migration applied."
    - "audit.actions table accepts INSERT with action_type IN ('ESCALATION_REQUEST','QUALITY_VERDICT','SCHEDULE_DRAFT','ANOMALY_ALERT')."
    - "Python `Decision.SUPPRESSED`, `Decision.LOGGED`, `ActionType.ESCALATION_REQUEST`, `ActionType.QUALITY_VERDICT`, `ActionType.SCHEDULE_DRAFT`, `ActionType.ANOMALY_ALERT` are importable."
    - "Migration is idempotent — running twice does not error."
  artifacts:
    - path: infra/migrations/timescale/007_extend_audit_decisions.sql
      provides: "ALTER TABLE audit.actions DROP CONSTRAINT + ADD CONSTRAINT (idempotent)"
      contains: "ALTER TABLE audit.actions"
    - path: packages/sft-agents/src/sft_agents/models/enums.py
      provides: "Decision + ActionType enum classes"
      contains: "SUPPRESSED|LOGGED|ESCALATION_REQUEST|QUALITY_VERDICT|SCHEDULE_DRAFT|ANOMALY_ALERT"
  key_links:
    - from: packages/sft-agents/src/sft_agents/models/enums.py
      to: infra/migrations/timescale/007_extend_audit_decisions.sql
      via: "string values match CHECK constraint values exactly"
      pattern: "suppressed|logged|escalation_request"
---

<objective>
Extend the `audit.actions` table's `decision` and `action_type` CHECK constraints to admit Phase 6's new enum values, and extend the corresponding Python `Decision`/`ActionType` enums in lockstep. This plan is the gating prerequisite for Plans 06-02 (RateLimiter writes `Decision.SUPPRESSED`), 06-05 (`EscalateToSupervisorTool` uses `ActionType.ESCALATION_REQUEST`), 06-06/07/08 (audit writes with new action_types).

Purpose: Without this migration, every other Phase 6 audit INSERT will fail PG CHECK constraint at runtime.
Output: 1 SQL migration + 1 test + 1 enum module extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@.planning/phases/06-agents-operations-production/06-RESEARCH.md
@.planning/phases/06-agents-operations-production/06-PATTERNS.md
@infra/migrations/timescale/003_create_audit_actions.sql
@infra/migrations/timescale/migrate.py
@packages/sft-agents/src/sft_agents/models/enums.py
@packages/sft-agents/tests/test_audit_constraints.py

<interfaces>
From `infra/migrations/timescale/003_create_audit_actions.sql` lines 30-50 (verified):
```sql
decision TEXT NOT NULL CHECK (
  decision IN (
    'auto',
    'hitl_operator',
    'hitl_supervisor',
    'hitl_manager',
    'interlock_reject',
    'rolled_back',
    'timed_out',
    'governor_alert',
    'escalated'
  )
)
```

From `packages/sft-agents/src/sft_agents/models/enums.py` (verified):
```python
class Decision(str, Enum):
    AUTO = "auto"
    HITL_OPERATOR = "hitl_operator"
    HITL_SUPERVISOR = "hitl_supervisor"
    HITL_MANAGER = "hitl_manager"
    INTERLOCK_REJECT = "interlock_reject"
    ROLLED_BACK = "rolled_back"
    TIMED_OUT = "timed_out"
    GOVERNOR_ALERT = "governor_alert"
    ESCALATED = "escalated"

class ActionType(str, Enum):
    WRITE_PLC_SETPOINT = "WRITE_PLC_SETPOINT"
    ACTUATOR_COMMAND = "ACTUATOR_COMMAND"
    FIRMWARE_DEPLOY = "FIRMWARE_DEPLOY"
    NETWORK_ACL_CHANGE = "NETWORK_ACL_CHANGE"
    GRAPH_RECURSION_REVIEW = "GRAPH_RECURSION_REVIEW"
    GOVERNOR_ALERT = "GOVERNOR_ALERT"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write idempotent migration 007 that extends audit.actions decision + action_type CHECK constraints</name>
  <files>
    infra/migrations/timescale/007_extend_audit_decisions.sql,
    infra/migrations/timescale/tests/test_migration_007.py
  </files>
  <read_first>
    infra/migrations/timescale/003_create_audit_actions.sql (current CHECK constraint definitions, lines 30-65),
    infra/migrations/timescale/006_create_ingest_state.sql (idempotent migration pattern example),
    infra/migrations/timescale/migrate.py (idempotent runner: how it tracks applied migrations),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 298-303 — migration approach for CHECK constraint)
  </read_first>
  <behavior>
    - Migration runs once: drops the old `decision` CHECK constraint and creates a new one including `'suppressed'`, `'logged'`, `'escalation_request_pending'` (any value name `escalation_request` not used as Decision — see Task 2 note).
    - Migration runs twice (idempotent): second run is a no-op via guard `IF EXISTS` / conditional via `pg_constraint` system catalog query OR via Phase 4 migration runner's applied-migrations table (existing pattern in `migrate.py`).
    - After migration: `INSERT INTO audit.actions (...) VALUES (..., 'suppressed', ...)` succeeds; same for `'logged'`.
    - Action-type CHECK constraint: drop+recreate to include `'ESCALATION_REQUEST'`, `'QUALITY_VERDICT'`, `'SCHEDULE_DRAFT'`, `'ANOMALY_ALERT'` (if such CHECK exists on action_type — verify in 003 first; if action_type is unconstrained TEXT, skip this portion of the migration).
    - Test verifies before-and-after: SELECT old constraint fails to admit `'suppressed'` (pg14+ EXCEPTION), migration applied, SELECT new constraint admits the new values.
  </behavior>
  <action>
    Inspect 003_create_audit_actions.sql to determine the exact name of the `decision` CHECK constraint (e.g., `audit_actions_decision_check`). Inspect for action_type CHECK constraint similarly. Write `007_extend_audit_decisions.sql` using `ALTER TABLE audit.actions DROP CONSTRAINT IF EXISTS <name>;` followed by `ALTER TABLE audit.actions ADD CONSTRAINT <name> CHECK (decision IN ('auto','hitl_operator','hitl_supervisor','hitl_manager','interlock_reject','rolled_back','timed_out','governor_alert','escalated','suppressed','logged'));`. Mirror the same DROP+ADD pattern for action_type if a CHECK constraint exists. Idempotency: `DROP CONSTRAINT IF EXISTS` is idempotent on its own; the migrate.py runner also tracks applied migrations by filename in a meta table — defer to existing tracking. The test file `infra/migrations/timescale/tests/test_migration_007.py` uses the testcontainers postgres fixture (existing Phase 3 conftest) to: (a) apply 001..006, (b) attempt `INSERT INTO audit.actions ... 'suppressed' ...` and assert raises `asyncpg.exceptions.CheckViolationError`; (c) apply 007; (d) retry insert — succeeds; (e) re-run 007 — succeeds without error.

    Document in a SQL comment header: "Phase 6 D-AD-03 (suppressed) + D-OA-02 (logged); Decision enum sync in packages/sft-agents/src/sft_agents/models/enums.py."
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest infra/migrations/timescale/tests/test_migration_007.py -m integration -x</automated>
  </verify>
  <done>Migration file applied twice without error against a fresh testcontainers PG; new decision values insert successfully; old constraint values still work; SQL file checked into repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `models/enums.py` Decision + ActionType with new Phase 6 values</name>
  <files>
    packages/sft-agents/src/sft_agents/models/enums.py,
    packages/sft-agents/tests/test_audit_constraints.py
  </files>
  <read_first>
    packages/sft-agents/src/sft_agents/models/enums.py (current enum definitions),
    packages/sft-agents/tests/test_audit_constraints.py (test pattern for enum-to-DB synchronization),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 280-303 — Decision + ActionType extension instructions),
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (lines 1156-1160 — Open Question 1 resolution)
  </read_first>
  <behavior>
    - `from sft_agents.models.enums import Decision, ActionType` succeeds.
    - `Decision.SUPPRESSED.value == "suppressed"`, `Decision.LOGGED.value == "logged"`.
    - `ActionType.ESCALATION_REQUEST.value == "ESCALATION_REQUEST"`, `ActionType.QUALITY_VERDICT.value == "QUALITY_VERDICT"`, `ActionType.SCHEDULE_DRAFT.value == "SCHEDULE_DRAFT"`, `ActionType.ANOMALY_ALERT.value == "ANOMALY_ALERT"`.
    - Existing values unchanged; backward-compat — no rename.
    - `test_audit_constraints.py` extended: existing test parametrize includes new values and asserts they map round-trip enum.value → PG insert → SELECT → enum lookup successfully.
  </behavior>
  <action>
    Edit `packages/sft-agents/src/sft_agents/models/enums.py` to append two new members to `Decision` (`SUPPRESSED = "suppressed"`, `LOGGED = "logged"`) and four new members to `ActionType` (`ESCALATION_REQUEST = "ESCALATION_REQUEST"`, `QUALITY_VERDICT = "QUALITY_VERDICT"`, `SCHEDULE_DRAFT = "SCHEDULE_DRAFT"`, `ANOMALY_ALERT = "ANOMALY_ALERT"`). Update the class docstring with comment: "Phase 6 extensions: SUPPRESSED (D-AD-03), LOGGED (D-OA-02), ESCALATION_REQUEST (D-OA-02/Pitfall §9), QUALITY_VERDICT (D-QI-02), SCHEDULE_DRAFT (D-PP-03), ANOMALY_ALERT (D-AD-01). Migration 007 syncs CHECK constraint."

    Extend `test_audit_constraints.py` to include the 6 new values in any parametrize list of valid-decision and valid-action-type round-trip tests. If the existing test doesn't parametrize, add a new `@pytest.mark.parametrize("decision_value", ["suppressed", "logged"])` test that asserts INSERT into testcontainers PG succeeds. Mirror an `action_type` parametrize.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest packages/sft-agents/tests/test_audit_constraints.py -x &amp;&amp; python -c "from sft_agents.models.enums import Decision, ActionType; assert Decision.SUPPRESSED.value == 'suppressed' and Decision.LOGGED.value == 'logged' and ActionType.ESCALATION_REQUEST.value == 'ESCALATION_REQUEST' and ActionType.QUALITY_VERDICT.value == 'QUALITY_VERDICT' and ActionType.SCHEDULE_DRAFT.value == 'SCHEDULE_DRAFT' and ActionType.ANOMALY_ALERT.value == 'ANOMALY_ALERT'; print('OK')"</automated>
  </verify>
  <done>All 6 new enum values importable; existing Decision/ActionType values intact; round-trip enum→PG→enum test green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: BLOCKING — Push migration 007 to dev TimescaleDB</name>
  <files>
    infra/migrations/timescale/007_extend_audit_decisions.sql
  </files>
  <what-built>Migration 007 SQL file is in repo; Python enums updated; tests green.</what-built>
  <how-to-verify>
Run the existing migration runner against the dev Postgres+TimescaleDB instance (`make migrate` or equivalent — check the project Makefile for the exact target; otherwise `python infra/migrations/timescale/migrate.py up`). Then verify:
```bash
psql "$TIMESCALE_DSN" -c "SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'audit.actions'::regclass AND conname LIKE '%decision%';"
```
The output must contain `'suppressed'` and `'logged'` as quoted IN list values.
And:
```bash
psql "$TIMESCALE_DSN" -c "INSERT INTO audit.actions (action_id, action_type, agent_id, thread_id, decision, ts) VALUES (gen_random_uuid(), 'ANOMALY_ALERT', 'anomaly-detector', 'test-thread', 'suppressed', NOW()); ROLLBACK;"
```
Must NOT raise CheckViolationError.
  </how-to-verify>
  <resume-signal>Type "approved — migration pushed" once migration 007 is applied to dev DB and verified.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PG client → audit.actions | Migrations execute as superuser; CHECK constraint changes are PR-reviewed |
| Python enum.value → SQL CHECK value | One-to-one mapping must remain in lockstep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-enum-drift | Tampering | Decision/ActionType enums vs CHECK constraint | mitigate | Task 2 round-trip test inserts every enum value into PG and asserts success; CI catches drift on each PR. |
| T-V6-migration-replay | Tampering | migration 007 idempotency | mitigate | `DROP CONSTRAINT IF EXISTS` + migrate.py applied-migrations meta table; Task 1 test runs migration twice. |
| T-V6-audit-repudiation | Repudiation | audit.actions | mitigate | Phase 4 REVOKE UPDATE,DELETE ON audit.actions FROM agent_role still in force (no change in this plan). |
</threat_model>

<verification>
- `pytest infra/migrations/timescale/tests/test_migration_007.py -m integration -x` green.
- `pytest packages/sft-agents/tests/test_audit_constraints.py -x` green.
- Dev DB constraint verified via psql query (Task 3 manual checkpoint).
- All existing tests still green: `nx run-many --target=test --projects=sft-agents`.
</verification>

<success_criteria>
- Migration applied to dev DB; new enum values insert successfully via raw psql.
- Python enums + SQL CHECK in lockstep — round-trip test passes for all 6 new values.
- No existing test regressions in `packages/sft-agents/tests/`.
</success_criteria>

<output>
Create `.planning/phases/06-agents-operations-production/06-01-SUMMARY.md` documenting: enum values added, SQL migration filename, dev-DB push timestamp, any deviations.
</output>
