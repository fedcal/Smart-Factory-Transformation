---
phase: 06-agents-operations-production
plan: 02
plan_id: 06-02
type: tdd
wave: 2
depends_on: ["06-00", "06-01"]
files_modified:
  - packages/sft-agents/src/sft_agents/runtime/rate_limit.py
  - packages/sft-agents/src/sft_agents/runtime/__init__.py
  - packages/sft-agents/tests/runtime/test_rate_limiter.py
autonomous: true
requirements: [OPS-04]
objective: "Implement `RateLimiter` PG-backed sliding-window 12 alerts/hour at `packages/sft-agents/src/sft_agents/runtime/rate_limit.py` with concrete `check_and_emit(action_type) -> tuple[allowed, current_count]` that survives restart by reading the source-of-truth `audit.actions` table (per D-AD-03)."

must_haves:
  truths:
    - "After 12 audit.actions rows with `agent_id='anomaly-detector', action_type='ANOMALY_ALERT'` in the last 60 minutes, `RateLimiter.check_and_emit('ANOMALY_ALERT')` returns `(False, 12)`."
    - "Rows older than `window_minutes` are excluded from the count (sliding window)."
    - "RateLimiter uses only SQL constants + `$1..$N` placeholders (no f-string injection)."
    - "Restart resilience — limiter is stateless beyond PG; new instance with same agent_id reads same count."
  artifacts:
    - path: packages/sft-agents/src/sft_agents/runtime/rate_limit.py
      provides: "class RateLimiter with check_and_emit"
      min_lines: 30
      exports: ["RateLimiter"]
    - path: packages/sft-agents/tests/runtime/test_rate_limiter.py
      provides: "Integration tests with testcontainers PG: 12/h boundary + sliding window + restart"
  key_links:
    - from: packages/sft-agents/src/sft_agents/runtime/rate_limit.py
      to: audit.actions
      via: "asyncpg pool COUNT(*) query"
      pattern: "SELECT COUNT.*audit\\.actions"
---

<objective>
Build the PG-backed sliding-window rate limiter (D-AD-03) that lets AnomalyDetector enforce 12 alerts/hour as a per-agent global threshold without per-process state. The count is derived from `audit.actions` rows, so it survives container restarts and is itself audit-evidence (no Redis dep).

Purpose: Enable Plan 06-06 AnomalyDetector to suppress excess alerts, write `Decision.SUPPRESSED` to audit, and continue scanning without flooding the HITL queue.
Output: 1 production module + 1 integration test (testcontainers PG).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@.planning/phases/06-agents-operations-production/06-RESEARCH.md
@.planning/phases/06-agents-operations-production/06-PATTERNS.md
@packages/sft-agents/src/sft_agents/runtime/governor.py
@packages/sft-agents/src/sft_agents/runtime/__init__.py
@packages/sft-agents/tests/conftest.py
@packages/sft-agents/tests/test_rate_limit_audit_query.py

<interfaces>
From `packages/sft-agents/src/sft_agents/runtime/governor.py` (analog, verified Pattern 8 + PATTERNS):
```python
_GOVERNOR_SCAN_SQL: str = (
    "SELECT count(*) FILTER (WHERE decision='auto') AS auto_count, "
    "count(*) AS total "
    "FROM audit.actions "
    "WHERE ts > NOW() - INTERVAL '1 hour' "
    "AND decision NOT IN ('escalated','governor_alert','timed_out')"
)
# Class init keyword-only; pool: Any; private attrs; structlog convention.
```

`audit.actions` schema (verified in 003_create_audit_actions.sql):
- `agent_id TEXT NOT NULL`
- `action_type TEXT NOT NULL`
- `ts TIMESTAMPTZ NOT NULL DEFAULT NOW()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing integration tests for RateLimiter sliding-window 12/h enforcement</name>
  <files>
    packages/sft-agents/tests/runtime/test_rate_limiter.py
  </files>
  <read_first>
    packages/sft-agents/tests/conftest.py (testcontainers PG fixture + audit.actions seed helpers),
    packages/sft-agents/tests/test_rate_limit_audit_query.py (existing audit query test — same SQL primitive),
    packages/sft-agents/src/sft_agents/runtime/governor.py (analog: pool injection, SQL constants pattern),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (Pattern 8 lines 195-246)
  </read_first>
  <behavior>
    - Test 1 `test_check_and_emit_under_limit`: Seed 5 audit.actions rows (agent_id='anomaly-detector', action_type='ANOMALY_ALERT', ts=NOW()). `RateLimiter(pool, agent_id="anomaly-detector").check_and_emit("ANOMALY_ALERT")` → `(True, 5)`.
    - Test 2 `test_check_and_emit_at_limit`: Seed 12 rows in last hour. Returns `(False, 12)`.
    - Test 3 `test_check_and_emit_above_limit`: Seed 15 rows. Returns `(False, 15)`.
    - Test 4 `test_sliding_window_excludes_old_rows`: Seed 12 rows with `ts=NOW() - INTERVAL '2 hours'` and 3 rows with `ts=NOW()`. Returns `(True, 3)`.
    - Test 5 `test_per_agent_isolation`: Seed 12 rows with agent_id='other-agent'. Returns `(True, 0)` for agent_id='anomaly-detector'.
    - Test 6 `test_per_action_type_isolation`: Seed 12 rows action_type='ANOMALY_ALERT', then call `check_and_emit("WRITE_PLC_SETPOINT")` → `(True, 0)`.
    - Test 7 `test_restart_resilience`: Create new RateLimiter instance with same pool — count carries from PG (not in-memory).
    - All tests marker `@pytest.mark.integration` use existing testcontainers PG fixture.
    - All tests truncate `audit.actions` between cases to ensure isolation.
  </behavior>
  <action>
    Delete the Wave 0 `test_placeholder` body. Add module docstring referencing D-AD-03 + Pattern 8. Write the 7 tests described in `<behavior>`. Use existing `pool` fixture from packages/sft-agents/tests/conftest.py. Use a parametrize for tests 1-3 over `(seed_count, expected_allowed, expected_count)` tuples if it reads cleanly. Each test must use `await conn.execute("INSERT INTO audit.actions (action_id, action_type, agent_id, thread_id, decision, ts) VALUES ($1, $2, $3, $4, $5, $6)", ...)` for seeding — parameterized, no f-string. Use `Decision.AUTO` value `'auto'` to avoid coupling to migration 007. Tests MUST currently fail (rate_limit.py does not exist).
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest packages/sft-agents/tests/runtime/test_rate_limiter.py -m integration -x 2&gt;&amp;1 | grep -qE "ModuleNotFoundError|ImportError|7 failed|7 errors" &amp;&amp; echo "RED phase confirmed"</automated>
  </verify>
  <done>7 tests written; all currently fail with ImportError (rate_limit module absent) — RED phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RateLimiter to pass all 7 tests</name>
  <files>
    packages/sft-agents/src/sft_agents/runtime/rate_limit.py,
    packages/sft-agents/src/sft_agents/runtime/__init__.py
  </files>
  <read_first>
    packages/sft-agents/src/sft_agents/runtime/governor.py (constructor pattern, SQL constant pattern, pool acquire pattern),
    packages/sft-agents/src/sft_agents/runtime/__init__.py (existing exports list),
    packages/sft-agents/tests/runtime/test_rate_limiter.py (the 7 tests just written — Task 1),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (Shared Patterns: SQL constants $1..$N + datetime.now(UTC))
  </read_first>
  <behavior>
    - Class `RateLimiter` constructor accepts keyword-only args: `pool: asyncpg.Pool`, `agent_id: str`, `limit: int = 12`, `window_minutes: int = 60`. Raises ValueError if `pool is None` or `not agent_id`.
    - Method `async def check_and_emit(self, action_type: str) -> tuple[bool, int]`: computes `cutoff = datetime.now(timezone.utc) - timedelta(minutes=self._window_minutes)`; executes the COUNT(*) SQL with placeholders `$1, $2, $3`; returns `(count < self._limit, int(count))`.
    - Concurrency note in docstring per D-AD-03: PG MVCC race accepted (~12 not strict).
    - All 7 tests pass.
  </behavior>
  <action>
    Create `rate_limit.py`. Module docstring per Pattern 8 + reference D-AD-03 + Pitfall §3 note (no audit write inside this module — caller writes Decision.SUPPRESSED separately). Module-level SQL constant `_COUNT_RECENT_SQL: str = "SELECT COUNT(*) FROM audit.actions WHERE agent_id = $1 AND action_type = $2 AND ts >= $3"`. Module-level `_log = structlog.get_logger("runtime.rate_limit")`.

    Class `RateLimiter` with keyword-only `__init__` validating `pool` not None and `agent_id` non-empty. Store private attrs `_pool`, `_agent_id`, `_limit`, `_window` (as timedelta). Method `check_and_emit(action_type)` async: build `cutoff = datetime.now(timezone.utc) - self._window`; `async with self._pool.acquire() as conn: count = await conn.fetchval(_COUNT_RECENT_SQL, self._agent_id, action_type, cutoff)`; coerce count to int (None → 0); log structured event `_log.debug("rate_limit_check", agent_id=..., action_type=..., count=count, limit=self._limit)`; return `(count < self._limit, count)`.

    Extend `runtime/__init__.py` to add `from sft_agents.runtime.rate_limit import RateLimiter` and `RateLimiter` to `__all__`.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest packages/sft-agents/tests/runtime/test_rate_limiter.py -m integration -x &amp;&amp; python -c "from sft_agents.runtime import RateLimiter; print('OK')"</automated>
  </verify>
  <done>All 7 tests green; `RateLimiter` importable from `sft_agents.runtime`; module follows SQL-constants + structlog + tz-aware datetime conventions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (AnomalyDetector) → RateLimiter | `agent_id` + `action_type` originate from agent constructor + enum |
| RateLimiter → audit.actions | read-only COUNT query; no writes from this module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-throttle | Tampering | RateLimiter window bypass via concurrency | accept (per D-AD-03 documented intent) | Docstring documents MVCC race; "approximately 12" semantic explicit; Phase 11 may upgrade to SELECT FOR UPDATE if observability shows >13 spikes. |
| T-V6-sql-injection | Tampering | _COUNT_RECENT_SQL | mitigate | Module-level constant only; `$1,$2,$3` parameters; no f-string interpolation (T-V5-sql convention). |
| T-V6-naive-datetime | Tampering | cutoff datetime | mitigate | `datetime.now(timezone.utc)` mandatory per Pitfall 7 / T-03-03-tz-naive; CI ruff config flags naive datetimes. |
| T-V6-pool-leak | Denial of Service | asyncpg pool acquire | mitigate | `async with self._pool.acquire() as conn` releases connection on exception. |
</threat_model>

<verification>
- `pytest packages/sft-agents/tests/runtime/test_rate_limiter.py -m integration -x` green (7 tests).
- `from sft_agents.runtime import RateLimiter` succeeds.
- `nx run sft-agents:test` still green (no regression).
- ruff + mypy strict on new file pass (existing pre-commit gate).
</verification>

<success_criteria>
- All 7 integration tests pass deterministically (no flake on testcontainers PG).
- RateLimiter is importable via canonical path.
- No new top-level dependency added (uses asyncpg already locked Phase 3).
</success_criteria>

<output>
Create `.planning/phases/06-agents-operations-production/06-02-SUMMARY.md` documenting: file paths, SQL constant chosen, test coverage, any deviations.
</output>
