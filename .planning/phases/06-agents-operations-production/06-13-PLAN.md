---
phase: 06-agents-operations-production
plan: 13
plan_id: 06-13
type: tdd
wave: 5
depends_on: ["06-00", "06-03", "06-06", "06-07", "06-08", "06-09", "06-10", "06-11", "06-12"]
files_modified:
  - tests/e2e/ops/conftest.py
  - tests/e2e/ops/test_operator_assistant_scenarios.py
  - tests/e2e/ops/test_production_planner_scenarios.py
  - tests/e2e/ops/test_quality_inspector_scenarios.py
  - tests/e2e/ops/test_anomaly_detector_scenarios.py
  - tests/fixtures/ops_scenarios/operator-assistant/happy.yaml
  - tests/fixtures/ops_scenarios/operator-assistant/degraded.yaml
  - tests/fixtures/ops_scenarios/operator-assistant/failure.yaml
  - tests/fixtures/ops_scenarios/production-planner/happy.yaml
  - tests/fixtures/ops_scenarios/production-planner/degraded.yaml
  - tests/fixtures/ops_scenarios/production-planner/failure.yaml
  - tests/fixtures/ops_scenarios/quality-inspector/happy.yaml
  - tests/fixtures/ops_scenarios/quality-inspector/degraded.yaml
  - tests/fixtures/ops_scenarios/quality-inspector/failure.yaml
  - tests/fixtures/ops_scenarios/anomaly-detector/happy.yaml
  - tests/fixtures/ops_scenarios/anomaly-detector/degraded.yaml
  - tests/fixtures/ops_scenarios/anomaly-detector/failure.yaml
  - tests/fixtures/llm_responses/operator-assistant/happy.jsonl
  - tests/fixtures/llm_responses/operator-assistant/degraded.jsonl
  - tests/fixtures/llm_responses/operator-assistant/failure.jsonl
  - tests/fixtures/llm_responses/production-planner/happy.jsonl
  - tests/fixtures/llm_responses/production-planner/degraded.jsonl
  - tests/fixtures/llm_responses/production-planner/failure.jsonl
  - tests/fixtures/llm_responses/quality-inspector/happy.jsonl
  - tests/fixtures/llm_responses/quality-inspector/degraded.jsonl
  - tests/fixtures/llm_responses/quality-inspector/failure.jsonl
  - tests/fixtures/llm_responses/anomaly-detector/happy.jsonl
  - tests/fixtures/llm_responses/anomaly-detector/degraded.jsonl
  - tests/fixtures/llm_responses/anomaly-detector/failure.jsonl
  - scripts/regenerate-llm-fixtures.py
autonomous: true
requirements: [OPS-06]
objective: "Replace Wave 0 stub e2e scenarios with full 12-scenario suite (3 × 4 agents = happy/degraded/failure each), authored deterministic scenario YAML + mock LLM JSONL fixtures, real testcontainers-backed fixture for Qdrant + Neo4j + TimescaleDB + NATS + PG. Each scenario asserts agent contract per success criterion #5."

must_haves:
  truths:
    - "All 12 e2e scenarios pass with `LLM_BACKEND=mock` and ~480s total runtime."
    - "Each scenario asserts: (a) agent invoked, (b) expected tool_calls made, (c) expected HITL tier (if any), (d) audit row(s) written, (e) for OperatorAssistant: citations present + lang matches query."
    - "Happy path: operator-assistant happy = Italian query → cited loom troubleshooting; planner happy = SPT for 5 orders + supervisor HITL queue; quality-inspector happy = slub minor → auto-log; anomaly-detector happy = 1 anomaly emitted + audit."
    - "Degraded path: rag returns 0 hits / LLM produces invalid JSON / sensor stream gap / mid-LLM tool failure — each gracefully degrades + logs warning + emits fallback response."
    - "Failure path: requires escalation (operator-assistant escalate_to_supervisor) / safety-interlock rejects (quality-inspector critical) / rate limit hit (anomaly-detector 12/h) / supervisor rejects schedule (planner)."
    - "All testcontainers wired in `tests/e2e/ops/conftest.py` and torn down between sessions."
    - "Companion `scripts/regenerate-llm-fixtures.py` rebuilds JSONL from real Qwen2.5 (Pitfall §10 mitigation; opt-in)."
  artifacts:
    - path: tests/e2e/ops/conftest.py
      provides: "shared fixtures: testcontainers Qdrant+Neo4j+TSDB+NATS+PG; mock_llm_backend; ops_scenario loader"
    - path: tests/e2e/ops/test_operator_assistant_scenarios.py
      provides: "3 scenario parametrized tests"
    - path: tests/fixtures/ops_scenarios/operator-assistant/happy.yaml
      provides: "Italian query happy scenario per success criterion #1"
  key_links:
    - from: tests/e2e/ops/test_operator_assistant_scenarios.py
      to: apps/agents/ops/operator-assistant/src/ops_operator_assistant/agent.py
      via: "agent invoked through api-gateway TestClient or direct __call__"
      pattern: "OperatorAssistantAgent|/v1/agents/operator-assistant/chat"
---

<objective>
End-to-end test suite that locks in success criterion #5 ("Each agent's E2E test covers three scenarios: happy / degraded / failure"). This plan ships real testcontainers wiring and scenario fixtures. Mock LLM keeps CI deterministic + fast.

Purpose: Verifiable proof that all 4 OPS agents work end-to-end + complete OPS-06.
Output: Real conftest + 4 scenario test modules + 12 YAML + 12 JSONL + 1 helper script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@.planning/phases/06-agents-operations-production/06-RESEARCH.md
@.planning/phases/06-agents-operations-production/06-PATTERNS.md
@.planning/phases/06-agents-operations-production/06-VALIDATION.md
@tests/e2e/test_hitl_cycle.py
@packages/sft-agents/tests/conftest.py
@packages/sft-knowledge/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12 scenario YAML files (deterministic inputs + expected outputs)</name>
  <files>
    tests/fixtures/ops_scenarios/operator-assistant/happy.yaml,
    tests/fixtures/ops_scenarios/operator-assistant/degraded.yaml,
    tests/fixtures/ops_scenarios/operator-assistant/failure.yaml,
    tests/fixtures/ops_scenarios/production-planner/happy.yaml,
    tests/fixtures/ops_scenarios/production-planner/degraded.yaml,
    tests/fixtures/ops_scenarios/production-planner/failure.yaml,
    tests/fixtures/ops_scenarios/quality-inspector/happy.yaml,
    tests/fixtures/ops_scenarios/quality-inspector/degraded.yaml,
    tests/fixtures/ops_scenarios/quality-inspector/failure.yaml,
    tests/fixtures/ops_scenarios/anomaly-detector/happy.yaml,
    tests/fixtures/ops_scenarios/anomaly-detector/degraded.yaml,
    tests/fixtures/ops_scenarios/anomaly-detector/failure.yaml
  </files>
  <read_first>
    .planning/phases/06-agents-operations-production/06-CONTEXT.md (specifics + 5 success criteria),
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (§11 Validation Architecture lines 1207-1245 — req→test mapping),
    apps/agents/ops/*/src/*/models.py (request/response shapes for each agent)
  </read_first>
  <action>
    Replace Wave 0 stub YAMLs with full deterministic scenarios. Each YAML has top-level keys:
    - `name: <agent>-<scenario>`
    - `description: <one-line>`
    - `input: { ... full request payload ... }`
    - `seed: { qdrant_chunks: [...], neo4j_facts: [...], tsdb_rows: [...], audit_seed: [...] }` — what to pre-populate testcontainers with
    - `mock_llm_fixture: tests/fixtures/llm_responses/<agent>/<scenario>.jsonl`
    - `expected: { decision: <auto|hitl_supervisor|...>, action_type: ..., tool_calls: [<list of tool names invoked>], audit_rows: <count>, citations_min: <int>, lang: <it|en>, anomalies_min: ..., severity: ..., unscheduled_min: ... }`

    Concrete scenarios:
    - **operator-assistant/happy**: query="Come risolvo una rottura del filo di ordito sul telaio L01?" user_roles=["technician"] → expected lang=it, citations_min=1, response_md contains `[1]`, tool_calls includes rag_search.
    - **operator-assistant/degraded**: query=Italian query but qdrant_chunks=[] (no SOPs) → expected response_md non-empty + warning logged + citations_missing=true.
    - **operator-assistant/failure**: query requests production stop ("Ferma il telaio L01") → LLM calls escalate_to_supervisor → expected decision=hitl_supervisor + audit row with action_type=ESCALATION_REQUEST.
    - **production-planner/happy**: horizon_days=3, strategy=spt, 5 orders seeded → expected ScheduleDraft.items count=5, supervisor HITL queued.
    - **production-planner/degraded**: 5 orders but only 1 asset eligible → expected unscheduled_min>=1, supervisor HITL still queued with rationale flagging gap.
    - **production-planner/failure**: supervisor rejects (mock decision=rejected) → expected audit decision=rolled_back.
    - **quality-inspector/happy**: QualityEvent slub small length → expected severity=minor, decision=auto.
    - **quality-inspector/degraded**: LLM mock returns invalid JSON → expected fallback verdict (severity=major), supervisor tier.
    - **quality-inspector/failure**: QualityEvent broken_end full_width=true → expected severity=critical, manager tier + safety interlock check.
    - **anomaly-detector/happy**: 1 LOOM-01 sensor row outside band → expected anomalies count=1, audit ANOMALY_ALERT.
    - **anomaly-detector/degraded**: TSDB returns 0 rows in window → expected anomalies=[] + structlog event "no_samples_in_window".
    - **anomaly-detector/failure**: 12 audit rows seeded in last hour + 1 new anomaly → expected suppressed + audit SUPPRESSED row.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; python -c "import yaml; import pathlib; data = [yaml.safe_load(p.read_text()) for p in sorted(pathlib.Path('tests/fixtures/ops_scenarios').rglob('*.yaml'))]; assert len(data) == 12 and all('expected' in d and 'input' in d for d in data); print(f'OK 12 scenarios validated')"</automated>
  </verify>
  <done>12 scenario YAMLs deterministic and schema-consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Author 12 mock LLM JSONL fixtures matching each scenario + regeneration script</name>
  <files>
    tests/fixtures/llm_responses/operator-assistant/happy.jsonl,
    tests/fixtures/llm_responses/operator-assistant/degraded.jsonl,
    tests/fixtures/llm_responses/operator-assistant/failure.jsonl,
    tests/fixtures/llm_responses/production-planner/happy.jsonl,
    tests/fixtures/llm_responses/production-planner/degraded.jsonl,
    tests/fixtures/llm_responses/production-planner/failure.jsonl,
    tests/fixtures/llm_responses/quality-inspector/happy.jsonl,
    tests/fixtures/llm_responses/quality-inspector/degraded.jsonl,
    tests/fixtures/llm_responses/quality-inspector/failure.jsonl,
    tests/fixtures/llm_responses/anomaly-detector/happy.jsonl,
    tests/fixtures/llm_responses/anomaly-detector/degraded.jsonl,
    tests/fixtures/llm_responses/anomaly-detector/failure.jsonl,
    scripts/regenerate-llm-fixtures.py
  </files>
  <read_first>
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (§Pattern 2 lines 415-419 — JSONL format),
    packages/sft-agents/src/sft_agents/llm/mock.py (MockReplayChatModel hash-keyed lookup + ordered fallback),
    tests/fixtures/ops_scenarios/<agent>/<scenario>.yaml (Task 1 — derive expected LLM behavior per scenario)
  </read_first>
  <action>
    For each of the 12 scenarios author the corresponding JSONL. Per scenario, the JSONL needs 1-3 LLM "rounds" (initial ReAct → tool_calls → final answer; or grader → verdict; or planner → rationale).

    Example shapes:
    - **operator-assistant/happy.jsonl** (3 entries):
      ```
      {"prompt_hash":"<auto-fill>","response":{"content":"","tool_calls":[{"id":"call_1","name":"rag_search","args":{"query":"rottura filo ordito telaio","user_roles":["technician"],"category":"sop","k":5}}],"usage_metadata":{"input_tokens":350,"output_tokens":40,"total_tokens":390}}}
      {"prompt_hash":"<auto-fill>","response":{"content":"Per risolvere la rottura del filo di ordito su un telaio: 1. Arresta il telaio in sicurezza [1]. 2. Identifica il filo rotto. 3. ...","tool_calls":[],"usage_metadata":{"input_tokens":520,"output_tokens":150,"total_tokens":670}}}
      ```
      Use empty `prompt_hash: ""` (or omit) and rely on ordered fallback — the MockReplayChatModel falls back to ordered replay when hash misses. This makes JSONL authoring tractable without computing real sha256 of prompts (which vary with messages format).
    - **operator-assistant/failure.jsonl**: first entry tool_calls escalate_to_supervisor with structured args.
    - **production-planner/happy.jsonl**: 1 entry with content=JSON rationale_md + citations.
    - **quality-inspector/happy.jsonl**: 1 entry with content=valid QualityVerdict JSON (score=1, severity=minor).
    - **quality-inspector/degraded.jsonl**: 1 entry with content="{ this isn't valid JSON" → forces fallback path.
    - **anomaly-detector/*.jsonl**: minimal — only 1 entry since AnomalyDetector doesn't call LLM in non-LLM path; but if QualityInspector/OperatorAssistant indirectly trigger LLM via the supervisor graph, ensure ordered-fallback covers any inadvertent invocation.

    Use the ordered-fallback strategy uniformly: set `prompt_hash` to empty string (or omit field); MockReplayChatModel logs `mock_llm_hash_miss_fallback` warning + advances index. This is acceptable for tests; production never uses mock backend.

    `scripts/regenerate-llm-fixtures.py`: skeleton typer CLI. `python scripts/regenerate-llm-fixtures.py --agent operator-assistant --scenario happy --backend ollama` → runs the agent against a real Qwen2.5 (via Ollama), captures LLM messages + responses, writes JSONL with real `prompt_hash` values. Document usage in module docstring. The script is opt-in (not run in CI); used pre-release to refresh JSONL when prompts change (Pitfall §10).
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; python -c "import json; import pathlib; data = [list(map(json.loads, [l for l in p.read_text().splitlines() if l.strip()])) for p in sorted(pathlib.Path('tests/fixtures/llm_responses').rglob('*.jsonl'))]; assert len(data) == 12 and all(len(d) >= 1 for d in data); print(f'OK 12 fixtures with entries: {[len(d) for d in data]}')"</automated>
  </verify>
  <done>12 JSONL fixtures present + regenerate script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement tests/e2e/ops/conftest.py with full testcontainers wiring + ops_scenario loader + mock_llm fixture</name>
  <files>
    tests/e2e/ops/conftest.py
  </files>
  <read_first>
    tests/e2e/test_hitl_cycle.py (Phase 4 testcontainers usage),
    packages/sft-knowledge/tests/conftest.py (Qdrant + Neo4j testcontainer fixtures from Phase 5),
    packages/sft-agents/tests/conftest.py (PG testcontainer + audit seed helpers),
    .planning/phases/06-agents-operations-production/06-VALIDATION.md (Wave 0 stubs + sampling rate)
  </read_first>
  <behavior>
    - `conftest.py` defines fixtures (session-scoped where possible):
      - `pg_container` (Phase 3 reuse): testcontainers postgres+timescaledb with all 7 migrations applied (incl. 06-01 007).
      - `nats_container`: testcontainers NATS JetStream + bootstrap QUALITY_STREAM + AUDIT_STREAM + SENSOR_STREAM via `scripts/nats-bootstrap-streams.py` invoked programmatically.
      - `qdrant_container` (Phase 5 reuse): testcontainers Qdrant + 4 collections + seed chunks per scenario.
      - `neo4j_container` (Phase 5 reuse): testcontainers Neo4j + seed Machine/Part/FailureMode/SOP nodes per scenario.
      - `mock_llm_backend(scenario_fixture_path, monkeypatch)`: sets LLM_BACKEND=mock + MOCK_LLM_FIXTURE=<path>.
      - `ops_scenario(request)`: scope=function; reads YAML from `tests/fixtures/ops_scenarios/{agent}/{scenario}.yaml`; returns dict with input/expected/seed/mock_llm_fixture.
      - `seed_testcontainers(ops_scenario, pg, nats, qdrant, neo4j)`: pre-loads each container from `scenario.seed.*`.
      - `api_gateway_client(...)`: FastAPI TestClient with all dependencies overridden to use the testcontainer fixtures + mock LLM.
  </behavior>
  <action>
    Replace Wave 0 stub conftest with full implementation. Use existing testcontainer fixtures by import where they live in packages/sft-knowledge/tests + packages/sft-agents/tests (compose them).
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest tests/e2e/ops/ --collect-only -q -m e2e 2>&amp;1 | head -50 &amp;&amp; python -c "from tests.e2e.ops.conftest import ops_scenario, mock_llm_backend; print('OK')"</automated>
  </verify>
  <done>conftest collects without import errors + exposes the expected fixture names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement 4 E2E scenario test modules (3 scenarios each = 12 tests total)</name>
  <files>
    tests/e2e/ops/test_operator_assistant_scenarios.py,
    tests/e2e/ops/test_production_planner_scenarios.py,
    tests/e2e/ops/test_quality_inspector_scenarios.py,
    tests/e2e/ops/test_anomaly_detector_scenarios.py
  </files>
  <read_first>
    tests/e2e/ops/conftest.py (just-written fixtures),
    tests/fixtures/ops_scenarios/ + tests/fixtures/llm_responses/ (Tasks 1-2),
    apps/agents/ops/*/src/*/agent.py (agent contracts to assert against)
  </read_first>
  <behavior>
    Each of the 4 test modules contains:
    ```python
    @pytest.mark.e2e
    @pytest.mark.integration
    @pytest.mark.parametrize("scenario", ["happy", "degraded", "failure"])
    async def test_<agent_slug>_scenario(scenario, ops_scenario, mock_llm_backend, seed_testcontainers, api_gateway_client):
        spec = ops_scenario  # dict from YAML
        # 1. mock_llm_backend already set LLM_BACKEND=mock + fixture path
        # 2. seed_testcontainers already populated DB/qdrant/neo4j/nats per spec.seed
        # 3. Invoke the agent via the api-gateway endpoint
        response = api_gateway_client.post(spec["input"]["endpoint"], json=spec["input"]["body"])
        # 4. Assert expected outcomes from spec.expected
        assert response.status_code in (200, 202)
        # 5. Assert audit rows
        audit_count = ... query PG ...
        assert audit_count >= spec["expected"]["audit_rows"]
        # 6. Per-agent specific assertions (citations, severity, anomalies, ...)
    ```

    Per-agent specific assertions:
    - operator-assistant: check `lang == expected.lang`, `len(citations) >= expected.citations_min`, response_md contains `[N]` for happy.
    - production-planner: check ScheduleDraft items count, supervisor HITL queue insert.
    - quality-inspector: publish a QualityEvent to NATS quality.events.* and wait for consumer to process (with timeout); check audit + severity.
    - anomaly-detector: invoke scan + check anomalies count + rate-limit suppression in failure.

    All 12 tests pass within 480s on testcontainers.
  </behavior>
  <action>
    Replace Wave 0 stub modules. Implement per behavior. Use `@pytest.mark.timeout(120)` per test. Use `await asyncio.sleep` for NATS consumer drainage in quality-inspector.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest tests/e2e/ops/ -m "e2e and not real-llm" -x --timeout=600</automated>
  </verify>
  <done>All 12 e2e tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML scenarios → loader | Repo-committed, yaml.safe_load only |
| JSONL fixtures → MockReplayChatModel | Repo-committed, json.loads strict |
| testcontainers → real services | Isolated per session; torn down after |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-fixture-tamper | Tampering | scenarios + JSONL | mitigate | PR-reviewed; gitleaks pre-commit; no secrets allowed. |
| T-V6-test-determinism | Tampering | langdetect / random / asyncio races | mitigate | All scenarios use mock LLM (no real LLM in CI); seed=42 universally; sleep with explicit ack on NATS. |
| T-V6-test-leak | Information Disclosure | testcontainers data | accept | Containers ephemeral; no persistent volumes. |
</threat_model>

<verification>
- `pytest tests/e2e/ops/ -m "e2e and not real-llm"` green within ~480s.
- All 12 scenario YAMLs and JSONLs valid.
- Each agent's 3 scenarios pass.
- nyquist_compliant: true can be set in 06-VALIDATION.md frontmatter.
</verification>

<success_criteria>
- Success criterion #5 mechanically verified: 3 scenarios × 4 agents = 12 e2e tests green.
- All scenarios deterministic (mock LLM + seeded fixtures).
- Real-LLM scripts available via `scripts/regenerate-llm-fixtures.py` for opt-in smoke.
</success_criteria>

<output>
Create `.planning/phases/06-agents-operations-production/06-13-SUMMARY.md` documenting: 12-scenario coverage table, total runtime, fixture refresh procedure, any flaky tests.
</output>
