---
phase: 06-agents-operations-production
plan: 06
plan_id: 06-06
type: tdd
wave: 3
depends_on: ["06-00", "06-01", "06-02", "06-04", "06-05"]
files_modified:
  - apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/__init__.py
  - apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/agent.py
  - apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/baseline.py
  - apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/models.py
  - apps/agents/ops/anomaly-detector/tests/test_anomaly_detector.py
  - apps/agents/ops/anomaly-detector/tests/test_rate_limit.py
  - apps/agents/ops/anomaly-detector/tests/test_baseline.py
  - apps/agents/ops/anomaly-detector/pyproject.toml
autonomous: true
requirements: [OPS-04, OPS-05]
# OPS-05 partial: AnomalyDetector emits audit events with tools/data-source declarations + KPI tags via LogEventTool; full evidence_panel completion in 06-14.
objective: "Implement the AnomalyDetector agent at `apps/agents/ops/anomaly-detector/` per D-AD-01..03: LangGraph node `async __call__(state: OpsState) -> dict` that pulls TimescaleDB window via `query_timescale`, compares samples against YAML baseline (per-asset+per-machine override), enforces 12-alert/hour rate limit, writes Decision.ANOMALY_ALERT (or Decision.SUPPRESSED) audit, returns `{anomalies: [...]}`."

must_haves:
  truths:
    - "AnomalyDetector emits an Anomaly only when sample value is outside baseline band for the (asset_family, sensor_id) combination."
    - "Per-machine override in anomaly_baselines.yaml takes precedence over per-asset-family baseline."
    - "After 12 anomalies emitted in last hour, additional anomalies are SUPPRESSED (audit row decision=SUPPRESSED) without HITL emit."
    - "Detector handles missing baselines gracefully (no exception; logs structlog event)."
    - "Detector input is OpsState with `window_minutes` (default 15)."
    - "No false-positive on normal high-frequency loom vibration when baseline matches the asset's nominal band."
  artifacts:
    - path: apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/agent.py
      provides: "class AnomalyDetector with async __call__"
      exports: ["AnomalyDetector"]
    - path: apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/baseline.py
      provides: "Baseline comparator helpers"
    - path: apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/models.py
      provides: "AnomalyScanRequest, AnomalyScanResponse"
  key_links:
    - from: apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/agent.py
      to: packages/sft-agents/src/sft_agents/runtime/rate_limit.py
      via: "RateLimiter import + check_and_emit usage"
      pattern: "RateLimiter|check_and_emit"
    - from: apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/agent.py
      to: packages/sft-tools/src/sft_tools/timescale/query.py
      via: "QueryTimescaleTool injection"
      pattern: "QueryTimescaleTool|query_timescale"
---

<objective>
First of the 4 OPS agents to ship. AnomalyDetector is fully deterministic (no LLM call) — pure baseline check + rate-limited alert emission. This validates the agent skeleton pattern downstream agents will follow (06-07/08/09).

Purpose: Satisfy success criterion #3 (per-machine calibration, no false positive on loom vibration, 12-alert/h cap).
Output: 4 modules under the agent package + 3 test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@.planning/phases/06-agents-operations-production/06-RESEARCH.md
@.planning/phases/06-agents-operations-production/06-PATTERNS.md
@packages/sft-agents/src/sft_agents/runtime/rate_limit.py
@packages/sft-agents/src/sft_agents/runtime/governor.py
@packages/sft-domain/src/sft_domain/ops/anomaly.py
@packages/sft-tools/src/sft_tools/timescale/query.py
@packages/sft-assets/src/sft_assets/registry.py
@apps/agents/ops/anomaly-detector/pyproject.toml

<interfaces>
From `packages/sft-domain/ops/anomaly.py` (plan 06-04):
```python
class Anomaly(BaseModel): id, asset_id, sensor_id, value, baseline_low, baseline_high, timestamp, severity
class AnomalyBaseline(BaseModel): asset_family, sensor_id, low, high, unit, severity_mapping
    def is_within_band(value) -> bool
    def severity_for(value) -> Literal["minor","major","critical"]
def load_anomaly_baselines(path=None) -> dict[tuple[str,str], AnomalyBaseline]
```

From `packages/sft-agents/runtime/rate_limit.py` (plan 06-02):
```python
class RateLimiter:
    def __init__(self, *, pool, agent_id, limit=12, window_minutes=60)
    async def check_and_emit(action_type: str) -> tuple[bool, int]
```

From `packages/sft-tools/timescale/query.py` (Phase 3):
```python
class QueryTimescaleTool(BaseTool):
    async def _arun(asset_id, time_range, ...) -> pandas.DataFrame
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for AnomalyDetector agent + baseline.py helpers</name>
  <files>
    apps/agents/ops/anomaly-detector/tests/test_anomaly_detector.py,
    apps/agents/ops/anomaly-detector/tests/test_rate_limit.py,
    apps/agents/ops/anomaly-detector/tests/test_baseline.py
  </files>
  <read_first>
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Example 2 lines 1036-1077 — full AnomalyDetector sketch),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 545-558 — adaptations),
    apps/agents/ops/anomaly-detector/pyproject.toml (verify dependencies + nx target name),
    packages/sft-agents/src/sft_agents/runtime/governor.py (analog: pool injection + audit write convention),
    packages/sft-tools/src/sft_tools/timescale/query.py (QueryTimescaleTool contract)
  </read_first>
  <behavior>
    test_anomaly_detector.py (mostly integration with testcontainers PG + mock timescale tool):
    - test_happy_path_emits_anomaly: seed asset registry with 1 LOOM-01; baseline {vibration: low=0.0, high=0.8}; mock QueryTimescaleTool returns DataFrame with one row value=1.2 → __call__ returns `{"anomalies": [Anomaly]}` with severity computed.
    - test_no_false_positive_on_normal_loom_vibration: same setup but value=0.5 (in-band) → `{"anomalies": []}`.
    - test_per_machine_override_applied: anomaly_baselines.yaml entry with `machine_id: LOOM-01` and tighter band overrides asset_family band.
    - test_missing_baseline_logs_warning_no_crash: sensor with no baseline → structlog warning "missing_baseline"; no anomaly emitted; no exception.
    - test_handles_multiple_assets_in_one_scan: 3 assets, 2 with anomalies → 2 anomalies returned in order.
    - test_window_minutes_parameter_passed_to_timescale_tool: state.window_minutes=30 → QueryTimescaleTool._arun called with (now-30min, now).
    - test_state_delta_only_no_mutation: __call__ returns dict containing only `anomalies` key (and audit metadata) — does NOT mutate state messages.

    test_rate_limit.py:
    - test_12h_window_caps_emission: Seed PG with 12 ANOMALY_ALERT audit rows in last hour; trigger AnomalyDetector with 5 new anomalies → all 5 suppressed (decision=SUPPRESSED audit rows written); return `{"anomalies": []}`.
    - test_11_existing_plus_1_new_emits: 11 existing → 1 new emits with decision=ANOMALY_ALERT.
    - test_suppressed_audit_row_includes_original_payload: assert payload column includes original anomaly fields (asset_id, sensor_id, value).

    test_baseline.py (unit, no DB):
    - test_is_within_band_inclusive: band [0.0, 0.8], value 0.0 and 0.8 in-band; 0.81 and -0.01 out.
    - test_severity_for_uses_mapping: severity_mapping {minor: 0.1, major: 0.3, critical: 0.6} → value 0.9 (delta 0.1 from upper) → minor; value 1.2 (delta 0.4) → major; value 1.5 (delta 0.7) → critical.
    - test_select_baseline_machine_override_wins: helper `select_baseline(baselines, asset_family, sensor_id, machine_id)` returns the per-machine baseline when both per-family and per-machine entries exist.
  </behavior>
  <action>
    Delete Wave 0 placeholders. Write tests per behavior. For test_anomaly_detector.py use `@pytest.mark.integration` + testcontainers PG fixture (from packages/sft-agents/tests/conftest.py or repo conftest). For mock QueryTimescaleTool use `AsyncMock` returning a pandas.DataFrame built from Pydantic-shaped dicts. Tests fail (module absent) — RED.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/ops/anomaly-detector/tests/ -x 2&gt;&amp;1 | grep -qE "ModuleNotFoundError|ImportError|failed" &amp;&amp; echo "RED phase confirmed"</automated>
  </verify>
  <done>~15 tests written; all fail on import — RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AnomalyDetector agent + baseline.py + models.py + pyproject deps</name>
  <files>
    apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/__init__.py,
    apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/agent.py,
    apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/baseline.py,
    apps/agents/ops/anomaly-detector/src/ops_anomaly_detector/models.py,
    apps/agents/ops/anomaly-detector/pyproject.toml
  </files>
  <read_first>
    apps/agents/ops/anomaly-detector/tests/ (just-written tests — green target),
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Example 2 lines 1036-1077),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 545-558),
    packages/sft-agents/src/sft_agents/runtime/governor.py (class init pattern + structlog),
    packages/sft-tools/src/sft_tools/timescale/query.py (tool input/output)
  </read_first>
  <behavior>
    All tests green. Specifically:
    - `agent.py`: class `AnomalyDetector` with keyword-only `__init__(*, pool, baselines, asset_registry, query_tool=None, audit_writer=None, rate_limiter=None)`. `query_tool` defaults to `QueryTimescaleTool()`; `rate_limiter` defaults to `RateLimiter(pool=pool, agent_id="anomaly-detector", limit=12)`. `async __call__(self, state: OpsState) -> dict`: window_minutes = state.get("window_minutes", 15); now = datetime.now(timezone.utc); time_range = (now - timedelta(minutes=window_minutes), now); iterate assets; for each: df = await query_tool._arun(asset_id=asset.asset_id, time_range=time_range); for each row: baseline = select_baseline(baselines, asset.asset_family.value, row.sensor_id, asset.machine_id if available); if baseline is None: log warning + continue; if `baseline.is_within_band(row.value)`: continue; build Anomaly; call `(allowed, count) = await rate_limiter.check_and_emit("ANOMALY_ALERT")`; if allowed: write audit row (decision=Decision.AUTO? or new Decision.ANOMALY_ALERT — but Decision only has AUTO etc. So write decision=Decision.AUTO + action_type=ActionType.ANOMALY_ALERT) + append to anomalies; else write audit row decision=Decision.SUPPRESSED + action_type=ActionType.ANOMALY_ALERT + payload includes original anomaly + count; structlog `anomaly_suppressed`. Return `{"anomalies": anomalies}`.
    - `baseline.py`: `select_baseline(baselines, asset_family, sensor_id, machine_id=None) -> AnomalyBaseline | None`: look up `(machine_id, sensor_id)` first if machine_id present; else `(asset_family, sensor_id)`; else None.
    - `models.py`: `AnomalyScanRequest(BaseModel)` frozen+extra=forbid: window_minutes (int default 15, ge=1 le=180), triggered_by (Literal["scheduler","operator","agent"]). `AnomalyScanResponse` with anomalies (list[Anomaly]), suppressed_count (int).
    - `__init__.py` re-exports.
    - `pyproject.toml`: ensure dependencies include `sft-agents`, `sft-domain`, `sft-tools`, `sft-assets`, `asyncpg`, `pandas`, `structlog`, `pydantic`. Use `[tool.uv.sources]` workspace = true for sft-* packages (Phase 1 convention).
  </behavior>
  <action>
    Implement modules per behavior. Use Pattern Example 2 from RESEARCH as starting point but adapt to use the proper AuditWriter signature (read its actual signature from Phase 4 audit_writer.py before implementing). For audit writes, write rows directly via the pool if AuditWriter signature is too complex; safest: use existing AuditWriter.write keyword API.

    Verify all tests green: integration tests use testcontainers PG fixture; unit tests are dependency-free.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/ops/anomaly-detector/tests/ -x &amp;&amp; python -c "from ops_anomaly_detector import AnomalyDetector; print('OK')"</automated>
  </verify>
  <done>All ~15 tests green; AnomalyDetector + baseline helpers importable; pyproject dependencies declared; ruff/mypy strict pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TimescaleDB → AnomalyDetector | Read-only via parameterized QueryTimescaleTool (Phase 3 already mitigates SQL injection) |
| anomaly_baselines.yaml → load_anomaly_baselines | YAML in repo, PR-reviewed, schema-validated |
| RateLimiter → audit.actions | Read-only COUNT query; writes happen via AuditWriter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-baseline | Tampering | anomaly_baselines.yaml | mitigate | YAML in repo (PR-reviewed), Pydantic schema validation on load (06-04), CI cross-ref validator. |
| T-V6-throttle | Denial of Service | AnomalyDetector alert storm | mitigate | RateLimiter 12/h with DB-backed sliding window (06-02); excess writes Decision.SUPPRESSED for audit retention. |
| T-V6-naive-datetime | Tampering | time_range tuple | mitigate | `datetime.now(timezone.utc)` mandatory; QueryTimescaleTool input schema rejects naive datetimes. |
| T-V6-audit-double-write | Repudiation | audit on retry | mitigate | AnomalyDetector is on-demand only (no NATS consumer); no retry path → no double-write risk Pitfall §3. |
| T-V6-acl-bypass | Information Disclosure | TimescaleDB sensor data | mitigate | QueryTimescaleTool reads only sensor data (no PII); ACL not applicable. |
</threat_model>

<verification>
- `nx run ops-anomaly-detector:test` green.
- All ~15 tests pass.
- AnomalyDetector handles 12-cap correctly (Test test_12h_window_caps_emission).
- No false positive on in-band value (Test test_no_false_positive_on_normal_loom_vibration).
- No top-level new dependency (uses libs already locked).
</verification>

<success_criteria>
- Success criterion #3 mechanically verified: per-machine calibration via select_baseline; no false positive in unit test; 12/h cap enforced via integration test.
- Pure-Python deterministic: same DataFrame + baselines → same anomalies list (no LLM call).
- pyproject dependencies declared; nx affected detects the package.
</success_criteria>

<output>
Create `.planning/phases/06-agents-operations-production/06-06-SUMMARY.md` documenting: AnomalyDetector contract, baseline override logic, audit decision values used, test counts.
</output>
