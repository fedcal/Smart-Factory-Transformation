---
phase: 06-agents-operations-production
plan: 08
plan_id: 06-08
type: tdd
wave: 3
depends_on: ["06-00", "06-01", "06-03", "06-04", "06-05"]
files_modified:
  - apps/agents/ops/production-planner/src/ops_production_planner/__init__.py
  - apps/agents/ops/production-planner/src/ops_production_planner/agent.py
  - apps/agents/ops/production-planner/src/ops_production_planner/prompts.py
  - apps/agents/ops/production-planner/src/ops_production_planner/models.py
  - apps/agents/ops/production-planner/tests/test_production_planner.py
  - apps/agents/ops/production-planner/pyproject.toml
autonomous: true
requirements: [OPS-02, OPS-05]
# OPS-05 partial: ProductionPlanner ScheduleDraft includes rationale_md + citations + audit; full evidence_panel completion in 06-14.
objective: "Implement ProductionPlanner agent per D-PP-01..04: orchestrates deterministic SPT/EDD greedy heuristic (`packages/sft-domain/scheduling/`) + LLM rationale generation with rag_search SOP citations + supervisor-tier HITL approval for the resulting ScheduleDraft. Trigger via `POST /v1/agents/production-planner/plan` body `{horizon_days, strategy: 'spt'|'edd'}`."

must_haves:
  truths:
    - "Agent loads orders + asset_capacity + failure_modes from packages/sft-domain at __init__ (cached)."
    - "Given strategy='spt', agent calls schedule_spt; given 'edd', schedule_edd."
    - "Generated ScheduleDraft is reproducible given identical inputs (deterministic schedule_id derivation)."
    - "LLM populates only `rationale_md` and references citations from rag_search SOPs — never modifies items list."
    - "All draft items + rationale + citations are bundled into ProposedAction(action_type=SCHEDULE_DRAFT) and routed to human_approval_node with Tier.SUPERVISOR."
    - "Audit row written via dual-write (PG + NATS) with full draft in payload."
    - "Unscheduled orders (Pitfall §8) surfaced in draft.unscheduled_orders and explicitly mentioned in rationale_md."
  artifacts:
    - path: apps/agents/ops/production-planner/src/ops_production_planner/agent.py
      provides: "class ProductionPlanner with async __call__(state) -> dict"
      exports: ["ProductionPlanner"]
    - path: apps/agents/ops/production-planner/src/ops_production_planner/prompts.py
      provides: "Rationale prompt template + few-shot examples"
    - path: apps/agents/ops/production-planner/src/ops_production_planner/models.py
      provides: "PlanRequest, PlanResponse Pydantic models"
  key_links:
    - from: apps/agents/ops/production-planner/src/ops_production_planner/agent.py
      to: packages/sft-domain/src/sft_domain/scheduling/heuristic.py
      via: "schedule_spt + schedule_edd import"
      pattern: "schedule_spt|schedule_edd"
    - from: apps/agents/ops/production-planner/src/ops_production_planner/agent.py
      to: packages/sft-agents/src/sft_agents/hitl/interrupt.py
      via: "human_approval_node Tier.SUPERVISOR"
      pattern: "human_approval_node|Tier\\.SUPERVISOR"
---

<objective>
ProductionPlanner is a thin orchestrator: deterministic algorithm runs first, then LLM produces rationale, then ProposedAction routes to supervisor HITL. The greedy heuristic lives in `packages/sft-domain` (06-04); this plan wires the agent class + LLM rationale prompt + HITL gate.

Purpose: Satisfy success criterion #4 (ScheduleDraft routed to supervisor HITL before release).
Output: 4 modules + 1 test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@.planning/phases/06-agents-operations-production/06-RESEARCH.md
@.planning/phases/06-agents-operations-production/06-PATTERNS.md
@packages/sft-domain/src/sft_domain/scheduling/heuristic.py
@packages/sft-domain/src/sft_domain/ops/schedule.py
@packages/sft-agents/src/sft_agents/hitl/interrupt.py
@packages/sft-agents/src/sft_agents/llm/factory.py
@packages/sft-knowledge/src/sft_knowledge/tools/rag.py
@apps/agents/ops/production-planner/pyproject.toml

<interfaces>
From `packages/sft-domain/scheduling/heuristic.py` (plan 06-04):
```python
def schedule_spt(orders, capacity, failure_modes, horizon_start, horizon_end) -> ScheduleDraft
def schedule_edd(orders, capacity, failure_modes, horizon_start, horizon_end) -> ScheduleDraft
# rationale_md initially empty; citations initially empty
```

From `packages/sft-agents/hitl/interrupt.py` (Phase 4):
```python
async def human_approval_node(state, proposed_action, tier, queue_writer, audit_writer, ...) -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for ProductionPlanner orchestration + LLM rationale + HITL routing</name>
  <files>
    apps/agents/ops/production-planner/tests/test_production_planner.py
  </files>
  <read_first>
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Pattern 5 lines 654-666 — rationale prompt + lines 30-46 D-PP-01..04),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 495-519 — adaptations),
    packages/sft-domain/src/sft_domain/scheduling/heuristic.py + ops/schedule.py (plan 06-04 outputs),
    packages/sft-agents/src/sft_agents/hitl/interrupt.py (human_approval_node signature)
  </read_first>
  <behavior>
    Tests:
    - test_spt_strategy_invokes_schedule_spt: state with strategy="spt", horizon_days=7 → schedule_spt called once.
    - test_edd_strategy_invokes_schedule_edd: strategy="edd" → schedule_edd called.
    - test_invalid_strategy_raises_validation: strategy="random" → ValidationError at state validation OR ValueError raised by agent.
    - test_horizon_dates_computed_from_horizon_days: horizon_start=now, horizon_end=now+horizon_days.
    - test_llm_rationale_populated_in_draft: mock LLM JSONL returns {"content": '{"rationale_md": "Bullet 1...", "citations": []}'}; agent updates draft via model_copy update={"rationale_md":...}; assert draft.rationale_md non-empty.
    - test_rag_search_called_for_sop_citations: mock rag_pipeline.search → assert called once with category="sop" and query containing "scheduling" or strategy.
    - test_citations_attached_to_draft: rag_pipeline returns 3 citations → draft.citations has 3 entries.
    - test_human_approval_node_called_with_supervisor_tier: monkeypatch human_approval_node; assert called once with tier=Tier.SUPERVISOR and proposed_action.action_type=ActionType.SCHEDULE_DRAFT.
    - test_proposed_action_args_contain_full_draft: assert proposed_action.args includes schedule_id + items + rationale_md + strategy.
    - test_unscheduled_orders_surfaced_in_rationale: schedule with 2 unscheduled → assertion that rationale_md or proposed_action.args includes the unscheduled order_ids.
    - test_deterministic_schedule_id: invoke agent twice with same inputs → same schedule_id returned (heuristic determinism via 06-04).
    - test_safety_middleware_not_invoked_for_schedule_draft: ScheduleDraft is not a safety-critical write → safety check NOT called (success criterion #4 says HITL approval; safety check is only for write to PLC which we don't do).
  </behavior>
  <action>
    Delete Wave 0 placeholder. Write tests with mocks: AsyncMock for rag_pipeline; MockReplayChatModel from 06-03 for LLM; MagicMock for queue_writer/audit_writer; monkeypatch human_approval_node.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/ops/production-planner/tests/test_production_planner.py -x 2&gt;&amp;1 | grep -qE "ModuleNotFoundError|ImportError|failed" &amp;&amp; echo "RED phase confirmed"</automated>
  </verify>
  <done>~12 tests written; RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ProductionPlanner agent + prompts.py + models.py</name>
  <files>
    apps/agents/ops/production-planner/src/ops_production_planner/__init__.py,
    apps/agents/ops/production-planner/src/ops_production_planner/agent.py,
    apps/agents/ops/production-planner/src/ops_production_planner/prompts.py,
    apps/agents/ops/production-planner/src/ops_production_planner/models.py,
    apps/agents/ops/production-planner/pyproject.toml
  </files>
  <read_first>
    apps/agents/ops/production-planner/tests/test_production_planner.py (green target),
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Pattern 5 lines 654-666),
    packages/sft-domain/src/sft_domain/scheduling/heuristic.py + ops/schedule.py
  </read_first>
  <behavior>
    All tests from Task 1 green. Specifically:
    - `prompts.py`: `RATIONALE_PROMPT` template (English; user-facing rationale can be IT or EN — D-OA-03 detect on operator preference is Phase 10 concern; default EN here). Template inputs: strategy, schedule_dump, citations_block. Output schema enforces JSON `{"rationale_md": str, "citation_ids": list[int]}`.
    - `models.py`: `PlanRequest` frozen+extra=forbid: `horizon_days: Annotated[int, Field(ge=1, le=30)]`, `strategy: Literal["spt","edd"]`, `user_roles: list[str] = []`. `PlanResponse`: `draft: ScheduleDraft`, `hitl_thread_id: str`, `proposed_action_id: UUID`.
    - `agent.py`: class `ProductionPlanner` with `__init__(*, rag_pipeline, audit_writer, queue_writer, nats, model=None)`. `async def __call__(self, state) -> dict`:
      1. Validate request via `PlanRequest.model_validate(state)`.
      2. `orders = load_orders(); capacity = load_asset_capacity(); failure_modes = load_failure_modes()`.
      3. `now = datetime.now(timezone.utc); horizon_start = now; horizon_end = now + timedelta(days=request.horizon_days)`.
      4. `schedule_fn = schedule_spt if request.strategy == "spt" else schedule_edd`. `draft = schedule_fn(orders, capacity, failure_modes, horizon_start, horizon_end)`.
      5. `citations = await rag_pipeline.search({"query": f"textile production scheduling {request.strategy} best practices", "user_roles": request.user_roles, "category": "sop", "k": 5})`.
      6. `prompt_messages = [SystemMessage(RATIONALE_PROMPT), HumanMessage(content=f"Strategy: {request.strategy}\\nDraft:\\n{draft.model_dump_json(indent=2)}\\n\\nCitations:\\n{citations_block(citations)}\\n\\nUnscheduled orders: {draft.unscheduled_orders}")]`. `model = self._model or build_chat_model(temperature=0.3, seed=42)`. `raw = await model.ainvoke(prompt_messages)`. Parse JSON; on ValidationError → rationale_md = "LLM rationale unavailable; falling back to algorithmic explanation: see items list."
      7. `final_draft = draft.model_copy(update={"rationale_md": rationale_md, "citations": citations})`.
      8. `action = ProposedAction(action_type=ActionType.SCHEDULE_DRAFT, args=final_draft.model_dump(mode="json"), target_subject=None)`.
      9. `approval_result = await human_approval_node(state, proposed_action=action, tier=Tier.SUPERVISOR, queue_writer=self._queue_writer, audit_writer=self._audit_writer, ...)`. Return `{"schedule_draft": final_draft, "approval": approval_result}`.
    - `__init__.py` re-exports ProductionPlanner.
    - `pyproject.toml`: dependencies sft-agents, sft-domain, sft-knowledge, structlog, pydantic.

    All 12 tests from Task 1 green.
  </behavior>
  <action>
    Implement modules per behavior. Use `model_copy(update={...})` for immutable Pydantic updates. Wrap external LLM call in try/except for fallback rationale.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/ops/production-planner/tests/ -x &amp;&amp; python -c "from ops_production_planner import ProductionPlanner; print('OK')"</automated>
  </verify>
  <done>12 tests green; ProductionPlanner importable; LLM rationale path covered + fallback path covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API request → PlanRequest | Pydantic validates strategy Literal + horizon_days range |
| orders.yaml + asset_capacity.yaml → loaders | YAML in repo; cross-ref CI validator (06-04) |
| LLM output → rationale_md | Treated as text; never executed; clamped to draft.rationale_md field |
| rag_pipeline → citations | ACL-filtered Phase 5 D-72; user_roles propagated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-injection | Tampering | PlanRequest | mitigate | Pydantic frozen+extra=forbid; strategy Literal; horizon_days bounded. |
| T-V6-llm-hallucination | Tampering | LLM rationale_md | mitigate | LLM only updates rationale_md (string field); items/dates determined by deterministic algorithm; LLM cannot change schedule. |
| T-V6-acl-leak | Information Disclosure | rag_pipeline ACL | mitigate | user_roles propagated from state to rag_pipeline.search; Phase 5 D-72 filter applies. |
| T-V6-citation | Tampering | citations | mitigate | citations from rag_pipeline output (not LLM); validator ensures non-empty for facts. |
| T-V6-hitl-bypass | Elevation of Privilege | ScheduleDraft auto-approve | mitigate | Always routes to human_approval_node Tier.SUPERVISOR; no auto-approve path; success criterion #4 enforced. |
</threat_model>

<verification>
- `nx run ops-production-planner:test` green.
- 12 tests pass including determinism + HITL routing + LLM fallback.
- Deterministic schedule_id verified.
- LLM never modifies draft.items.
</verification>

<success_criteria>
- Success criterion #4 mechanically verifiable: every ScheduleDraft passes through human_approval_node tier=SUPERVISOR before any audit decision=approved.
- Greedy heuristic determinism preserved end-to-end (LLM is rationale-only).
- Unscheduled orders explicitly surfaced for operator visibility.
</success_criteria>

<output>
Create `.planning/phases/06-agents-operations-production/06-08-SUMMARY.md` documenting: orchestration flow, LLM fallback behavior, HITL routing, test counts.
</output>
