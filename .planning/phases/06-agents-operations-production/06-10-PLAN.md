---
phase: 06-agents-operations-production
plan: 10
plan_id: 06-10
type: tdd
wave: 3
depends_on: ["06-00", "06-01", "06-03", "06-04", "06-05"]
files_modified:
  - apps/agents/ops/operator-assistant/src/ops_operator_assistant/__init__.py
  - apps/agents/ops/operator-assistant/src/ops_operator_assistant/agent.py
  - apps/agents/ops/operator-assistant/src/ops_operator_assistant/prompts.py
  - apps/agents/ops/operator-assistant/src/ops_operator_assistant/validators.py
  - apps/agents/ops/operator-assistant/src/ops_operator_assistant/lang_detect.py
  - apps/agents/ops/operator-assistant/src/ops_operator_assistant/models.py
  - apps/agents/ops/operator-assistant/tests/test_operator_assistant.py
  - apps/agents/ops/operator-assistant/tests/test_validators.py
  - apps/agents/ops/operator-assistant/tests/test_lang_detect.py
  - apps/agents/ops/operator-assistant/pyproject.toml
autonomous: false
requirements: [OPS-01, OPS-05]
user_setup:
  - service: pypi-langdetect
    why: "Language detection for IT/EN response coherence (D-OA-03)."
    env_vars: []
    dashboard_config: []
    note: "[ASSUMED] langdetect>=1.0.9 — verify on PyPI before install (Task 1 blocking checkpoint)."
objective: "Implement OperatorAssistant agent per D-OA-01..04: `langgraph.prebuilt.create_react_agent` with full 5-tool toolbelt + recursion_limit=5 via safe_invoke + cross-lingual IT/EN response (langdetect seed=42) + citation validator with 1-retry replan loop."

must_haves:
  truths:
    - "OperatorAssistant exposes `async __call__(state) -> dict` invoking `safe_invoke(react_runnable, ...)` with recursion_limit=5."
    - "ReAct agent has all 5 tools registered (rag_search, traverse_graph, query_timescale, escalate_to_supervisor, log_event)."
    - "Tools are instantiated PER REQUEST (Pitfall §2) so user_roles ACL is current."
    - "Language of response matches language of query via `langdetect.detect` with `DetectorFactory.seed=42`."
    - "When LLM invokes rag_search but response_md lacks `[N]` citation reference → validator replans once with prompt augmentation."
    - "After replan, if citations still missing → response emitted with `citations_missing=True` flag."
    - "Citations propagated through to EvidencePanel-shaped structure."
  artifacts:
    - path: apps/agents/ops/operator-assistant/src/ops_operator_assistant/agent.py
      provides: "class OperatorAssistantAgent"
      exports: ["OperatorAssistantAgent"]
    - path: apps/agents/ops/operator-assistant/src/ops_operator_assistant/validators.py
      provides: "async validate_or_replan"
    - path: apps/agents/ops/operator-assistant/src/ops_operator_assistant/lang_detect.py
      provides: "detect_language helper"
  key_links:
    - from: apps/agents/ops/operator-assistant/src/ops_operator_assistant/agent.py
      to: "langgraph.prebuilt.create_react_agent"
      via: "ReAct factory"
      pattern: "create_react_agent"
---

<objective>
OperatorAssistant is the conversational "primo punto di contatto" — full ReAct loop with the largest toolbelt of all 4 OPS agents. Wires `create_react_agent` from `langgraph.prebuilt` with 5 tools (3 from prior phases + 2 from 06-05) plus two ops-specific layers: language detection (so the answer language matches the question) and a citation validator that forces grounded responses when rag_search has been called.

Purpose: Satisfy success criterion #1 (Italian-language query → cited loom troubleshooting procedure).
Output: 6 modules + 3 test modules + 1 langdetect-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@.planning/phases/06-agents-operations-production/06-RESEARCH.md
@.planning/phases/06-agents-operations-production/06-PATTERNS.md
@packages/sft-knowledge/src/sft_knowledge/tools/rag.py
@packages/sft-knowledge/src/sft_knowledge/tools/graph.py
@packages/sft-tools/src/sft_tools/timescale/query.py
@packages/sft-agents/src/sft_agents/tools/hitl.py
@packages/sft-agents/src/sft_agents/tools/audit.py
@packages/sft-agents/src/sft_agents/llm/factory.py
@packages/sft-agents/src/sft_agents/runtime/supervisor.py
@apps/agents/ops/operator-assistant/pyproject.toml

<interfaces>
LangGraph `create_react_agent` (locked D-OA-01):
```python
from langgraph.prebuilt import create_react_agent
runnable = create_react_agent(model=..., tools=..., checkpointer=..., prompt=SYSTEM_PROMPT_BILINGUAL)
```

Phase 4 `safe_invoke`:
```python
from sft_agents.runtime.supervisor import safe_invoke
result = await safe_invoke(runnable, payload, config={"recursion_limit": 5})
```

5 tools per D-OA-02: RagSearchTool, TraverseGraphTool, QueryTimescaleTool, EscalateToSupervisorTool, LogEventTool.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING — Verify langdetect>=1.0.9 PyPI legitimacy</name>
  <what-built>Plan declares langdetect as a new dep ([ASSUMED] in research). Pre-install human verification per package legitimacy gate.</what-built>
  <how-to-verify>
Open https://pypi.org/project/langdetect/ and confirm: package name `langdetect` (no typosquat); maintainer github.com/Mimino666/langdetect; MIT license; version >= 1.0.9; downloads > 1M/month; repo has recent activity. Approve only if all hold.
  </how-to-verify>
  <resume-signal>Type "approved — langdetect verified" once PyPI + GitHub confirm legitimate package.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add langdetect dep + implement lang_detect.py with deterministic seed</name>
  <files>
    apps/agents/ops/operator-assistant/pyproject.toml,
    apps/agents/ops/operator-assistant/src/ops_operator_assistant/lang_detect.py,
    apps/agents/ops/operator-assistant/tests/test_lang_detect.py
  </files>
  <read_first>
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 462-481),
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Pitfall §6 lines 936-941),
    apps/agents/ops/operator-assistant/pyproject.toml
  </read_first>
  <behavior>
    - `pyproject.toml`: add `"langdetect>=1.0.9"` to project.dependencies.
    - `lang_detect.py`: at import time set `DetectorFactory.seed = 42`. Function `detect_language(text: str) -> Literal["it","en"]` returns `"it"` if `detect(text).startswith("it")` else `"en"`; exception → `"en"` fallback.
    - Tests parametrize over (text, expected): IT phrases, EN phrases, empty, whitespace, mixed. Determinism test: 100 detects of same text → same result.
  </behavior>
  <action>
    Update pyproject. Implement lang_detect.py exactly per behavior with module-level seed. Write 7-8 tests including determinism loop.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/ops/operator-assistant/tests/test_lang_detect.py -x</automated>
  </verify>
  <done>langdetect installed; lang_detect passes all tests deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing tests + implement citation validator + bilingual system prompt</name>
  <files>
    apps/agents/ops/operator-assistant/tests/test_validators.py,
    apps/agents/ops/operator-assistant/src/ops_operator_assistant/validators.py,
    apps/agents/ops/operator-assistant/src/ops_operator_assistant/prompts.py
  </files>
  <read_first>
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Pattern 7 lines 722-754),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 452-461),
    packages/sft-knowledge/src/sft_knowledge/tools/rag.py (ToolMessage shape)
  </read_first>
  <behavior>
    Tests (5):
    - test_no_rag_call_skips_validation
    - test_inline_citation_passes
    - test_missing_inline_triggers_replan (one retry)
    - test_replan_max_one_retry_emits_warning_flag (citations_missing=True after second failure)
    - test_warning_event_emitted (structlog citation_missing_after_replan)

    Implementation:
    - `prompts.py`: `SYSTEM_PROMPT_BILINGUAL` instructing language coherence + citation rule + tool inventory + escalation rules. `REPLAN_AUGMENT` instructing LLM to re-emit answer with `[N]` refs.
    - `validators.py`: `async def validate_or_replan(state, response, *, react_agent, config, retries=0, max_retries=1) -> AIMessage`. Detects rag tool usage via ToolMessage(name="rag_search") presence; regex `\[\d+\]` on response.content; check additional_kwargs["citations"]. On miss: augmented re-invoke once; on still-miss: copy response with citations_missing flag + log warning.
  </behavior>
  <action>
    Delete Wave 0 placeholder. Write 5 tests with AsyncMock react_agent. Implement validators.py + prompts.py.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/ops/operator-assistant/tests/test_validators.py -x</automated>
  </verify>
  <done>5 validator tests green; SYSTEM_PROMPT_BILINGUAL covers language + citation + tools + escalation rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write failing tests + implement OperatorAssistantAgent with create_react_agent + per-request tools</name>
  <files>
    apps/agents/ops/operator-assistant/tests/test_operator_assistant.py,
    apps/agents/ops/operator-assistant/src/ops_operator_assistant/agent.py,
    apps/agents/ops/operator-assistant/src/ops_operator_assistant/models.py,
    apps/agents/ops/operator-assistant/src/ops_operator_assistant/__init__.py
  </files>
  <read_first>
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Pattern 1 lines 346-408 — full create_react_agent example + Pitfall §1 §2 §6),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 429-449 — adaptations),
    packages/sft-agents/src/sft_agents/runtime/supervisor.py (safe_invoke contract),
    apps/agents/ops/operator-assistant/src/ops_operator_assistant/validators.py + prompts.py (from Task 3)
  </read_first>
  <behavior>
    Tests:
    - test_init_with_collaborators: build agent with mocks; assert no exception.
    - test_call_per_request_tools_instantiated: invoke `__call__` twice with different user_roles; assert RagSearchTool instantiated 2 times (Pitfall §2).
    - test_safe_invoke_used_with_recursion_limit_5: monkeypatch safe_invoke; assert config["recursion_limit"] == 5.
    - test_lang_detect_called_on_query: monkeypatch detect_language; assert called with state["query"].
    - test_response_lang_matches_query: query "Il telaio ha problemi" → response.metadata["lang"] == "it".
    - test_citation_validator_invoked_on_response: monkeypatch validate_or_replan; assert called once with state + response.
    - test_response_includes_citations_field: response dict has `citations: list[RagCitation]`.
    - test_response_includes_lang_field: response dict has `lang: "it" | "en"`.

    Implementation:
    - `models.py`: `OperatorChatRequest` frozen+extra=forbid: query (Field min_length=1, max_length=2000), user_roles (list[str]), thread_id (str), target_agent (Optional[str] default None). `OperatorChatResponse` frozen: response_md, citations (list[RagCitation]), citations_missing (bool default False), lang (Literal["it","en"]), tool_calls_count (int).
    - `agent.py`: class `OperatorAssistantAgent` with `__init__(*, rag_pipeline, neo4j_client, pool, audit_writer, queue_writer, nats, safety_middleware, checkpointer)`. Method `_build_tools(user_roles)` returns the 5-tool list with fresh instances per call. Method `_build_runnable(tools)` returns `create_react_agent(model=build_chat_model(), tools=tools, checkpointer=self._checkpointer, prompt=SYSTEM_PROMPT_BILINGUAL)`. Method `async __call__(state) -> dict`: validate request via OperatorChatRequest; detect lang; build tools + runnable; messages = [HumanMessage(state.query)]; result = await safe_invoke(runnable, {"messages": messages}, config={"recursion_limit": 5, "configurable": {"thread_id": state.thread_id}, "callbacks": [langfuse_callback]}); response = result["messages"][-1]; validated = await validate_or_replan(state, response, react_agent=runnable, config=config); citations = validated.additional_kwargs.get("citations", []); return {"response_md": validated.content, "citations": citations, "citations_missing": validated.additional_kwargs.get("citations_missing", False), "lang": detected_lang, "tool_calls_count": len([m for m in result["messages"] if isinstance(m, ToolMessage)])}.
    - `__init__.py` re-exports OperatorAssistantAgent.
  </behavior>
  <action>
    Delete Wave 0 placeholder. Write 8 tests with extensive mocking. Implement agent.py + models.py per behavior. Tools instantiated per call (Pitfall §2). Use `create_react_agent` import from `langgraph.prebuilt`.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest apps/agents/ops/operator-assistant/tests/test_operator_assistant.py -x &amp;&amp; python -c "from ops_operator_assistant import OperatorAssistantAgent; from ops_operator_assistant.models import OperatorChatRequest, OperatorChatResponse; print('OK')"</automated>
  </verify>
  <done>8 agent tests green; OperatorAssistantAgent importable; per-request tool instantiation verified; recursion_limit=5 propagated via safe_invoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → OperatorChatRequest | Pydantic Field min/max_length + extra=forbid |
| user_roles → RagSearchTool ACL | Propagated per-request, fresh tool instance (Pitfall §2) |
| LLM tool_call → 5 tools | All tools have Pydantic args_schema; SafetyInterlockMiddleware on escalate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-injection | Tampering | OperatorChatRequest | mitigate | Pydantic frozen+extra=forbid; query length-capped 2000. |
| T-V6-acl-leak | Information Disclosure | rag_search ACL | mitigate | Per-request tool instantiation; user_roles closure fresh per call (Pitfall §2). |
| T-V6-citation | Tampering | LLM output citations | mitigate | validate_or_replan + max 1 retry + audit flag `citations_missing` (D-OA-04). |
| T-V6-recursion-bomb | Denial of Service | ReAct loop | mitigate | safe_invoke enforces recursion_limit=5; overflow escalates to HITL (Phase 4 D-53). |
| T-V6-escalate-bypass | Elevation of Privilege | escalate_to_supervisor in ReAct | mitigate | EscalateToSupervisorTool wraps SafetyInterlockMiddleware.check (06-05); audit dual-write. |
| T-V6-lang-flake | Tampering | langdetect concurrent | mitigate | seed=42 at module import; asyncio single-thread (Pitfall §6). |
| T-V6-prompt-injection | Tampering | operator query → LLM | accept | Prompt system messages instruct LLM to never follow user-supplied instructions that contradict tool inventory; escalation still requires SafetyInterlock. |
</threat_model>

<verification>
- `nx run ops-operator-assistant:test` green.
- 5 + 8 + 8 = ~21 tests pass.
- create_react_agent invoked with all 5 tools per request.
- Tools fresh per request (verified by test counting RagSearchTool constructor calls).
- safe_invoke wraps invocation with recursion_limit=5.
- Citation validator replan path covered.
- langdetect seeded deterministically.
</verification>

<success_criteria>
- Success criterion #1 mechanically verifiable: Italian query → response with `[N]` citation → citations list non-empty.
- Cross-lingual: rag_search invoked with lang=None (BGE-M3 cross-lingual Phase 5 D-64).
- Per-request tool instantiation prevents ACL leak.
- 5-tool toolbelt registered (no missing tool).
</success_criteria>

<output>
Create `.planning/phases/06-agents-operations-production/06-10-SUMMARY.md` documenting: tool list with versions/sources, system prompt key lines, citation validator behavior, langdetect seed, test counts.
</output>
