---
phase: 06-agents-operations-production
plan: 09
plan_id: 06-09
type: tdd
wave: 2
depends_on: ["06-00", "06-04"]
files_modified:
  - simulators/sim-textile/src/sim_textile/production_state.py
  - simulators/sim-textile/src/sim_textile/quality_event_generator.py
  - simulators/sim-textile/src/sim_textile/emitter.py
  - simulators/sim-textile/tests/test_quality_generator.py
  - simulators/sim-textile/tests/test_production_state.py
  - simulators/sim-textile/pyproject.toml
autonomous: true
requirements: [OPS-03]
objective: "Extend sim-textile with (a) `production_state.py` (`ProductionState` dataclass holding `current_dye_lot_id` per asset, rotates every 60 min sim-time per D-QI-04) and (b) `quality_event_generator.py` (stochastic QC event publisher on NATS subject `quality.events.<asset_id>`, payload = QualityEvent Pydantic from 06-04). Plug generator into existing emitter loop."

must_haves:
  truths:
    - "ProductionState.maybe_rotate(now) updates current_dye_lot_id to `DL-<asset_id>-<YYYYMMDD>-<hex>` when rotation_interval has elapsed."
    - "quality_event_generator publishes valid QualityEvent JSON to `quality.events.<asset_id>` NATS subject."
    - "Generator throttles to ≤10 events/min/asset under nominal profile; ≤30 events/min/asset under faulted profile."
    - "Every emitted event includes a non-null dye_lot_id matching the regex."
    - "Generator uses asset.fault_profile to bias defect_type distribution (e.g., dyer assets prefer shade_deviation/unlevel_dyeing)."
    - "Integration test: with sim-textile running for 5 sim-minutes, ≥1 quality.events.* message appears on the NATS subject."
  artifacts:
    - path: simulators/sim-textile/src/sim_textile/production_state.py
      provides: "ProductionState dataclass + dye_lot_id rotation"
      exports: ["ProductionState"]
    - path: simulators/sim-textile/src/sim_textile/quality_event_generator.py
      provides: "async quality_event_emitter(asset, profile, production_state, nc, ...) task"
      exports: ["quality_event_emitter"]
  key_links:
    - from: simulators/sim-textile/src/sim_textile/quality_event_generator.py
      to: packages/sft-domain/src/sft_domain/ops/quality.py
      via: "QualityEvent model + dye_lot_id regex"
      pattern: "QualityEvent|dye_lot_id"
    - from: simulators/sim-textile/src/sim_textile/quality_event_generator.py
      to: "NATS subject quality.events.<asset_id>"
      via: "nc.publish"
      pattern: "quality\\.events\\."
---

<objective>
sim-textile already publishes sensor data via OPC-UA → OT Bridge → NATS sensor.events.*. This plan adds parallel QC event publication so QualityInspector (06-07) has a continuous data source. Dye lot rotation is in-process state; no PG persistence Phase 6 (D-QI-04).

Purpose: Satisfy success criterion #2 dye_lot_id requirement + provide steady-stream QC events for E2E demo.
Output: 2 new modules + 1 emitter wiring extension + 2 test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-agents-operations-production/06-CONTEXT.md
@.planning/phases/06-agents-operations-production/06-RESEARCH.md
@.planning/phases/06-agents-operations-production/06-PATTERNS.md
@simulators/sim-textile/src/sim_textile/emitter.py
@packages/sft-domain/src/sft_domain/ops/quality.py
@services/ot-bridge/src/svc_ot_bridge/main.py
@scripts/nats-bootstrap-streams.py

<interfaces>
From `packages/sft-domain/ops/quality.py` (plan 06-04):
```python
class QualityEvent(BaseModel):
    event_id: UUID
    asset_id: str
    dye_lot_id: Annotated[str, Field(pattern=r"^DL-[A-Z0-9-]+-\d{8}-[0-9a-f]+$")]
    defect_type: Literal["broken_end","mispick","slub","neppy","selvage_fault","shade_deviation","unlevel_dyeing"]
    defect_length_inches: float
    full_width: bool
    position_meters: float
    timestamp: datetime  # tz-aware
    source: Literal["simulator","operator"]
```

Existing `simulators/sim-textile/src/sim_textile/emitter.py` shape (verified pattern):
```python
async def asset_emitter(asset, profile, vars_map, *, time_scale=1.0):
    state = EmitterState()
    dt = (1.0 / profile.sample_rate_hz) / time_scale
    while True:
        now = datetime.now(UTC)
        # publish sensor samples to OPC-UA
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for ProductionState dye_lot_id rotation + quality_event_generator publish behavior</name>
  <files>
    simulators/sim-textile/tests/test_production_state.py,
    simulators/sim-textile/tests/test_quality_generator.py
  </files>
  <read_first>
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Pattern 10 lines 828-870 — full sketch),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 612-633 — adaptations),
    simulators/sim-textile/src/sim_textile/emitter.py (loop pattern),
    packages/sft-domain/src/sft_domain/ops/quality.py (QualityEvent model)
  </read_first>
  <behavior>
    test_production_state.py:
    - test_initial_dye_lot_id_format: ProductionState(asset_id="LOOM-01", current_dye_lot_id=...) → current_dye_lot_id matches regex `^DL-[A-Z0-9-]+-\d{8}-[0-9a-f]+$`.
    - test_maybe_rotate_no_rotation_before_interval: now = init + 30 min, rotation_interval = 60 min → returns False, dye_lot_id unchanged.
    - test_maybe_rotate_rotation_after_interval: now = init + 61 min → returns True, dye_lot_id changes to new format including new date if crossing midnight.
    - test_rotation_idempotent_within_interval_after_rotation: rotate once, then call maybe_rotate again immediately → returns False.
    - test_seq_token_uses_secrets_module: assert generated seq is hex (4 chars) and varies between rotations.

    test_quality_generator.py (`@pytest.mark.integration` with NATS testcontainer):
    - test_publishes_to_correct_subject: start generator for LOOM-01 with mock fault profile that forces a defect; assert message arrives on `quality.events.LOOM-01`.
    - test_payload_validates_as_quality_event: subscribe + receive message → QualityEvent.model_validate_json(msg.data) succeeds; source="simulator".
    - test_includes_current_dye_lot_id: production_state.current_dye_lot_id="DL-LOOM-01-20260523-abcd"; emitted event.dye_lot_id == that value.
    - test_rate_limited_under_nominal: nominal profile (no fault) for 30 sim-seconds → ≤5 events (well under 10/min rate).
    - test_defect_type_biased_by_fault_profile: dyer asset with shade_deviation fault active → ≥70% of emitted defects are shade_deviation or unlevel_dyeing.
    - test_full_width_field_present: emitted event has `full_width` field (default False) and varies per stochastic logic.
  </behavior>
  <action>
    Delete Wave 0 placeholders. Write tests. For NATS integration tests reuse existing testcontainer fixture from packages/sft-agents/tests/conftest.py or services/ot-bridge tests. RED phase confirmed by ModuleNotFoundError.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest simulators/sim-textile/tests/test_production_state.py simulators/sim-textile/tests/test_quality_generator.py -x 2&gt;&amp;1 | grep -qE "ModuleNotFoundError|ImportError|failed" &amp;&amp; echo "RED phase confirmed"</automated>
  </verify>
  <done>11 tests written; RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement production_state.py + quality_event_generator.py + hook into emitter</name>
  <files>
    simulators/sim-textile/src/sim_textile/production_state.py,
    simulators/sim-textile/src/sim_textile/quality_event_generator.py,
    simulators/sim-textile/src/sim_textile/emitter.py,
    simulators/sim-textile/pyproject.toml
  </files>
  <read_first>
    simulators/sim-textile/tests/ (just written — green target),
    .planning/phases/06-agents-operations-production/06-RESEARCH.md (Pattern 10 lines 828-870),
    .planning/phases/06-agents-operations-production/06-PATTERNS.md (lines 612-633),
    simulators/sim-textile/src/sim_textile/emitter.py (existing asyncio task structure),
    services/ot-bridge/src/svc_ot_bridge/main.py (NATS connection pattern)
  </read_first>
  <behavior>
    All tests from Task 1 green.
    - `production_state.py`: `ProductionState` dataclass with fields per Pattern 10 (asset_id, current_dye_lot_id, rotation_interval default 60 min, _last_rotation default `datetime.now(UTC)`); method `maybe_rotate(now: datetime) -> bool` per Pattern 10 sketch using `secrets.token_hex(2)` for seq.
    - `quality_event_generator.py`: `async def quality_event_emitter(asset, profile, production_state: ProductionState, nc, *, time_scale: float = 1.0)`. Loop pattern matches `asset_emitter`: while True; now = datetime.now(UTC); production_state.maybe_rotate(now); compute fault-biased probability of emitting a QC event in this dt window; if random < prob: pick defect_type biased by profile (dyer → shade_deviation more likely; spinner → slub more likely; etc.); compute defect_length_inches randomized (lognormal with mean varying by defect_type); set full_width = random < 0.05 (5% chance); build QualityEvent; await nc.publish(f"quality.events.{asset.asset_id}", event.model_dump_json().encode()); sleep dt. Rate limiting: clamp emit prob so expected events/min ≤ 10 nominal or ≤ 30 faulted.
    - `emitter.py` extension: minimal — only export the helper that the simulator entrypoint can use to start QC event tasks in parallel. Do not restructure existing asset_emitter; add a complementary `start_quality_event_tasks(assets, profiles, nc, production_states)` function.
    - `pyproject.toml`: ensure dependencies include `sft-domain` (workspace ref) for QualityEvent model.
  </behavior>
  <action>
    Implement modules per behavior. Use module-level structlog logger `_log = structlog.get_logger("sim-textile.quality-events")`. Deterministic randomization: seed with `random.Random(asset_id_hash)` per asset so tests are reproducible.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Smart Factory Transformation" &amp;&amp; pytest simulators/sim-textile/tests/ -x &amp;&amp; python -c "from sim_textile.production_state import ProductionState; from sim_textile.quality_event_generator import quality_event_emitter; print('OK')"</automated>
  </verify>
  <done>11 tests green; both modules importable; emitter helper available for simulator entrypoint to wire (entrypoint wiring optional — full hook deferred until QualityInspector E2E in 06-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sim-textile internal state → NATS publish | sim-textile generates payloads internally; no external untrusted input |
| dye_lot_id format → NATS payload | Format generated by ProductionState; consumer regex-validates again |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-dos-event-flood | Denial of Service | quality_event_generator | mitigate | Rate-limited via emit probability clamp (≤10/min nominal, ≤30/min faulted); QualityInspector ack_wait further bounds consumer load. |
| T-V6-injection | Tampering | NATS payload integrity | mitigate | All payloads constructed via QualityEvent Pydantic (`.model_dump_json()`); cannot inject control chars or oversize fields. |
| T-V6-naive-datetime | Tampering | event.timestamp | mitigate | `datetime.now(timezone.utc)` mandatory; QualityEvent rejects naive. |
| T-V6-replay | Tampering | NATS message replay | accept | sim-textile is dev-only; in production, real factory PLC feeds events, not the simulator. |
</threat_model>

<verification>
- `nx run sim-textile:test` green.
- ProductionState rotation correctness verified.
- NATS publish to `quality.events.LOOM-01` verified via testcontainer subscription.
- Defect_type biased per fault profile.
</verification>

<success_criteria>
- dye_lot_id rotates every ~60 min sim-time per asset.
- All emitted events validate as QualityEvent.
- Rate limit prevents flood (≤10/min nominal, ≤30/min faulted).
- No new top-level deps (uses nats-py + sft-domain already locked).
</success_criteria>

<output>
Create `.planning/phases/06-agents-operations-production/06-09-SUMMARY.md` documenting: ProductionState rotation behavior, defect_type bias table per fault profile, rate-limit constants, test counts.
</output>
