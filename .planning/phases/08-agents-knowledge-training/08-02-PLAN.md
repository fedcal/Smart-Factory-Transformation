---
phase: 08-agents-knowledge-training
plan: 02
type: execute
wave: 2
depends_on: ["08-00a", "08-00b"]
files_modified:
  - apps/agents/knowledge/shift-handover/src/trn_shift_handover/models.py
  - apps/agents/knowledge/shift-handover/src/trn_shift_handover/aggregator.py
  - apps/agents/knowledge/shift-handover/src/trn_shift_handover/metadata.py
  - apps/agents/knowledge/shift-handover/src/trn_shift_handover/prompts.py
autonomous: true
requirements: [TRN-03, TRN-05]
must_haves:
  truths:
    - "ShiftAggregator builds a structured handover report from audit.actions (cross-cluster) + maintenance.downtime_events within the shift window — NO ops.alerts/ops.work_orders tables (D-SH-02)"
    - "Alerts are derived from audit.actions WHERE action_type='ANOMALY_ALERT'; work-order/quality/downtime data from corresponding action_type rows + downtime_events"
    - "All asyncpg datetime params are passed as datetime objects, never .isoformat() (WR-03)"
  artifacts:
    - path: "apps/agents/knowledge/shift-handover/src/trn_shift_handover/aggregator.py"
      provides: "Cross-cluster shift-window aggregation (ClassVar parameterized SQL)"
      contains: "ANOMALY_ALERT"
    - path: "apps/agents/knowledge/shift-handover/src/trn_shift_handover/models.py"
      provides: "ShiftWindow + HandoverReport frozen Pydantic models with RagCitation"
      contains: "model_config"
  key_links:
    - from: "aggregator.py"
      to: "audit.actions"
      via: "parameterized window query"
      pattern: "BETWEEN \\$1 AND \\$2"
---

<objective>
Build ShiftHandover's data layer: frozen Pydantic models (ShiftWindow, HandoverReport), the cross-cluster ShiftAggregator (D-SH-02), metadata/evidence-panel helper (TRN-05), and the report prompt. NO new ops.alerts/ops.work_orders tables — derive everything from audit.actions + maintenance.downtime_events per the resolved D-SH-02 decision.

Purpose: The deterministic aggregation backbone that the dual-HITL agent (08-04) compiles and signs off, kept fast to protect the <3 min SLA.
Output: models.py, aggregator.py, metadata.py, prompts.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agents-knowledge-training/08-CONTEXT.md
@.planning/phases/08-agents-knowledge-training/08-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-PATTERNS.md

<interfaces>
From sft_agents.models.evidence: RagCitation (source_uri, snippet, score, retrieved_at), EvidencePanel, ToolCall, TokenUsage.
audit.actions(ts, agent_id, cluster, action_type, thread_id, evidence_panel JSONB, decision); maintenance.downtime_events(event_id, asset_id, reason_code, duration_min, severity, work_order_id, timestamp).
asyncpg pool.acquire() then conn.fetch(sql, *params) with datetime objects (WR-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShiftWindow + HandoverReport models</name>
  <files>apps/agents/knowledge/shift-handover/src/trn_shift_handover/models.py</files>
  <read_first>
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/models.py (frozen model + tz validator analog)
    - 08-PATTERNS.md section "trn_shift_handover/models.py" (lines 563-588) and Pattern D
    - apps/agents/knowledge/shift-handover/tests/test_aggregator.py (Wave 0 contract)
  </read_first>
  <behavior>
    - ShiftWindow rejects naive datetime (tz-aware validator); rejects shift_end <= shift_start.
    - HandoverReport carries counts (open_alerts, completed_work_orders, downtime_events, quality_events), equipment_status, narrative summary, and citations: list[RagCitation] for TRN-05.
    - All models frozen=True, extra="forbid".
  </behavior>
  <action>
    Define ShiftWindow (shift_start, shift_end tz-aware datetimes, boundary_label) with field_validator enforcing tz-awareness and a model_validator enforcing end>start. Define HandoverReport with structured count fields, equipment_status dict, narrative_summary str, source_events list, and citations list[RagCitation] (TRN-05 source_uri+timestamp). Both use ConfigDict(frozen=True, extra="forbid").
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -c "from trn_shift_handover.models import ShiftWindow, HandoverReport; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ShiftWindow with naive datetime raises ValidationError; with end<=start raises ValidationError.
    - HandoverReport has a citations field typed list[RagCitation].
  </acceptance_criteria>
  <done>Frozen models import and enforce tz/window/citation constraints.</done>
</task>

<task type="auto">
  <name>Task 2: ShiftAggregator + metadata + prompts (D-SH-02, TRN-05)</name>
  <files>apps/agents/knowledge/shift-handover/src/trn_shift_handover/aggregator.py, apps/agents/knowledge/shift-handover/src/trn_shift_handover/metadata.py, apps/agents/knowledge/shift-handover/src/trn_shift_handover/prompts.py</files>
  <read_first>
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/repository.py (ClassVar SQL + asyncpg + WR-03 analog)
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/metadata.py (TOOL_INVENTORY/DATA_SOURCES/build evidence panel)
    - 08-PATTERNS.md sections "trn_shift_handover/aggregator.py" (lines 498-559), "metadata.py" (lines 592-632), Pattern C and E
    - 08-CONTEXT.md D-SH-02 (NO new tables; derive from audit.actions + downtime_events), 08-RESEARCH.md RF-4
  </read_first>
  <action>
    In aggregator.py define ShiftAggregator(pool) with ClassVar parameterized SQL constants: _SQL_AUDIT_WINDOW selecting ts, agent_id, cluster, action_type, thread_id, evidence_panel, decision FROM audit.actions WHERE ts BETWEEN $1 AND $2 ORDER BY ts ASC; _SQL_DOWNTIME_WINDOW selecting downtime_events columns WHERE timestamp BETWEEN $1 AND $2. Implement fetch_audit_window and fetch_downtime_events passing datetime objects directly (WR-03, never .isoformat()). Implement build_report(audit_rows, downtime_rows) deterministically: open_alerts = count of action_type='ANOMALY_ALERT'; completed_work_orders/quality/downtime derived from corresponding action_type rows (QUALITY_VERDICT, DOWNTIME_VERDICT) plus downtime_events; equipment_status from latest per-asset rows; collect RagCitations from evidence_panel JSONB rag_citations for TRN-05. Keep this path LLM-free so it stays under 30s (Pitfall §4). In metadata.py define AGENT_ID="shift-handover", TOOL_INVENTORY, DATA_SOURCES = ('audit.actions (all clusters)', 'maintenance.downtime_events') ONLY — do NOT list ops.alerts or ops.work_orders (D-SH-02 prohibits new tables; alerts are derived from audit.actions action_type='ANOMALY_ALERT'), KPIS_IMPACTED, HITL_TIER_DEFAULT="supervisor", and build_trn05_evidence_panel mirroring the rca-specialist signature. In prompts.py define the deterministic report template plus a short narrative-summary prompt (LLM used only for the 3-5 line executive summary, capped).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/knowledge/shift-handover/tests/test_aggregator.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test_aggregator.py passes: mock asyncpg rows yield a HandoverReport with correct derived counts.
    - aggregator SQL uses only $N placeholders (no f-string interpolation); grep finds no ops.alerts / ops.work_orders table references.
    - build_trn05_evidence_panel returns the 5 required evidence keys and never lets caller `extra` overwrite them.
    - `grep metadata.py: DATA_SOURCES does not contain 'ops.alerts'` (D-SH-02 prohibits new tables): `grep -q "ops.alerts" apps/agents/knowledge/shift-handover/src/trn_shift_handover/metadata.py` returns non-zero (absent).
  </acceptance_criteria>
  <done>ShiftAggregator derives the handover report from existing tables only, fast and LLM-free, with TRN-05 citations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to aggregator | shift_start/shift_end window supplied via API; server validates bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | shift-window SQL | mitigate | Parameterized $N placeholders only; Pydantic frozen ShiftWindow validates bounds |
| T-08-05 | Information Disclosure | cross-cluster audit read | accept | Dev-mode; audit is internal operational data, no PII (Phase 11 adds JWT/RBAC) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 8 |
</threat_model>

<verification>
- `python -m pytest apps/agents/knowledge/shift-handover/tests/test_aggregator.py -x -q` green.
- Aggregator references only audit.actions + maintenance.downtime_events.
</verification>

<success_criteria>
ShiftHandover data layer aggregates cross-cluster events into a citation-bearing report from existing tables only.
</success_criteria>

<output>
Create `.planning/phases/08-agents-knowledge-training/08-02-SUMMARY.md` when done.
</output>
