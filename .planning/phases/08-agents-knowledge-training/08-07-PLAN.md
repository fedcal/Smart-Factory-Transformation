---
phase: 08-agents-knowledge-training
plan: 07
type: execute
wave: 2
depends_on: ["08-00"]
files_modified:
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/models.py
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/event_aggregator.py
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/sop_builder.py
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/translator.py
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/validators.py
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/metadata.py
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/prompts.py
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/agent.py
  - apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/__init__.py
autonomous: true
requirements: [TRN-04, TRN-05]
must_haves:
  truths:
    - "Source events selected by failure_mode + asset within a configurable window by parsing audit.actions evidence_panel JSONB at query time — no schema change (SC-4, D-DS-02)"
    - "IT SOP generated with fixed sections (Scopo, Prerequisiti, Passi, Sicurezza, Riferimenti) and [SRC:N] anchors; EN produced by a translate pass that preserves every anchor (D-DS-01/03)"
    - "SOPCitationValidator rejects any EN output missing an IT [SRC:N] anchor (citation drift) and any claim lacking source_uri+timestamp (TRN-05, Pitfall §1)"
    - "Qdrant indexing happens ONLY after the HITL interrupt resumes; SOP_DRAFT audit row written after resume (D-DS-03, CR-02)"
  artifacts:
    - path: "apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/translator.py"
      provides: "IT->EN translation with citation re-anchoring"
      contains: "SRC"
    - path: "apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/agent.py"
      provides: "DocumentationSynthesizer node with pre-index HITL"
      contains: "SOP_DRAFT"
  key_links:
    - from: "agent.py"
      to: "QdrantIndexer.upsert"
      via: "called only after interrupt resume"
      pattern: "interrupt"
---

<objective>
Implement DocumentationSynthesizer: aggregate historical RCA/downtime/coach events by failure_mode + asset (D-DS-02, JSONB query), generate a fixed-section IT SOP with [SRC:N] anchors, translate to EN with citation re-anchoring (D-DS-01, Pitfall §1), validate citations (TRN-05), and gate indexing behind supervisor HITL (D-DS-03).

Purpose: Closes SC-4 — bilingual SOP draft from historical events routed to HITL approval before indexing, every output carrying source_uri + timestamp.
Output: models.py, event_aggregator.py, sop_builder.py, translator.py, validators.py, metadata.py, prompts.py, agent.py, __init__.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agents-knowledge-training/08-CONTEXT.md
@.planning/phases/08-agents-knowledge-training/08-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-PATTERNS.md

<interfaces>
From sft_knowledge.retrieval.pipeline.RetrievalPipeline.search(query, user_roles=["technician"], category="sop", k=5, asset_family=...) -> list[RagCitation] (source_uri + retrieved_at populated).
QdrantIndexer.upsert(...) (Phase 5) for indexing after approval.
From sft_agents.models.enums: ActionType.SOP_DRAFT, Decision.HITL_SUPERVISOR, Tier.SUPERVISOR.
audit.actions: action_type IN (RCA_CHAIN, COACH_STEP, DOWNTIME_VERDICT); evidence_panel->>'failure_mode' and ->>'asset_id' (JSONB at query time, RESEARCH Open Q4). INTERVAL not parameterizable — compute window start datetime in Python, pass as $1 TIMESTAMPTZ.
langgraph.types.interrupt (ImportError fallback shim, Pattern G).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SOPDraft models + event aggregator + sop_builder + translator + validators (D-DS-01/02, TRN-05)</name>
  <files>apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/models.py, apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/event_aggregator.py, apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/sop_builder.py, apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/translator.py, apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/validators.py</files>
  <read_first>
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/repository.py (asyncpg JSONB query analog)
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/validators.py (validator/exception class analog)
    - 08-PATTERNS.md sections "documentation-synthesizer/.../models.py" (lines 1127-1165), "event_aggregator.py" (1055-1081), "validators.py" (1084-1124), "sop_builder.py"/"translator.py" entries
    - 08-CONTEXT.md D-DS-01/02/03; 08-RESEARCH.md Pitfall §1 (citation re-anchoring), Open Q4 (JSONB query)
  </read_first>
  <behavior>
    - SOPDraft frozen; sections_it/sections_en must each contain all 5 SECTION_KEYS_IT or raise ValueError; carries anchor_map and citations.
    - SOPCitationValidator: any [SRC:N] in IT missing from EN raises MissingAnchorError; anchor not in anchor_map raises CitationDriftError; any claim with no source_uri/timestamp rejected (TRN-05).
    - event_aggregator parameterizes the window as a Python-computed start datetime ($1 TIMESTAMPTZ), filters failure_mode + asset via JSONB operators.
  </behavior>
  <action>
    models.py: define SECTION_KEYS_IT=("Scopo","Prerequisiti","Passi","Sicurezza","Riferimenti") and frozen SOPDraft (sop_id, failure_mode, asset_id, title_it/en, lang_primary Literal "it", sections_it/en dicts, citations list[RagCitation], anchor_map dict, generated_at, approved=False) with a field_validator asserting all 5 section keys present. event_aggregator.py: HistoricalEventAggregator(pool) with ClassVar SQL selecting ts/agent_id/action_type/evidence_panel FROM audit.actions WHERE action_type IN (RCA_CHAIN, COACH_STEP, DOWNTIME_VERDICT) AND ts >= $1 AND ($2 IS NULL OR evidence_panel->>'failure_mode'=$2) AND ($3 IS NULL OR evidence_panel->>'asset_id'=$3) ORDER BY ts DESC LIMIT 100; compute window start in Python (WR-03 datetime). sop_builder.py: build the fixed-section IT SOP from events + RAG citations, embedding [SRC:N] anchors and producing the anchor_map {[SRC:N]: source_uri} (Pitfall §1). translator.py: translate IT sections to EN via the LLM with an instruction to preserve every [SRC:N] marker in place, then re-anchor by validating anchors survived; raise on drift. validators.py: MissingAnchorError, CitationDriftError, and SOPCitationValidator.validate(sop_draft, anchor_map) regex-extracting [SRC:N] from IT/EN and enforcing parity + source_uri/timestamp presence (TRN-05), mirroring RCAChainValidator.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/knowledge/documentation-synthesizer/tests/test_translator.py apps/agents/knowledge/documentation-synthesizer/tests/test_citation_provenance.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test_translator.py passes: anchors preserved; a dropped anchor raises MissingAnchorError.
    - test_citation_provenance.py passes: opaque (uncited) output rejected.
    - SOPDraft missing a required section raises ValueError; event_aggregator SQL uses $N placeholders and JSONB operators (no schema change).
  </acceptance_criteria>
  <done>Event aggregation, fixed-section IT SOP, EN re-anchored translation, and citation validation are implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: DocumentationSynthesizer node (pre-index HITL) + metadata/prompts (D-DS-03, CR-02)</name>
  <files>apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/agent.py, apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/metadata.py, apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/prompts.py, apps/agents/knowledge/documentation-synthesizer/src/trn_documentation_synthesizer/__init__.py</files>
  <read_first>
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py (CR-02 interrupt-then-audit, _write_audit)
    - 08-PATTERNS.md section "documentation-synthesizer/.../agent.py" (lines 1002-1051), Patterns A/G/H/I
    - 08-CONTEXT.md D-DS-03 (HITL before indexing)
  </read_first>
  <action>
    Define DocumentationSynthesizer(pool, audit_writer, llm, retrieval_pipeline, indexer, event_aggregator, validator, saver) with AGENT_ID="documentation-synthesizer", CLUSTER="knowledge". In __call__ follow PATTERNS lines 1008-1050 ordering: fetch historical events (event_aggregator), RAG grounding citations (retrieval_pipeline.search), generate IT SOP + anchor_map (sop_builder), translate EN re-anchoring (translator), validate (validator), THEN interrupt(SUPERVISOR) DIRECTLY (CR-02). Only AFTER the interrupt resumes call indexer.upsert(validated) and then _write_audit(SOP_DRAFT, approval_id=None, Decision.HITL_SUPERVISOR, motivation supervisor decision) — indexing and audit both strictly post-resume. Guard saver-None with RuntimeError (CR-01). metadata.py: AGENT_ID, TOOL_INVENTORY, DATA_SOURCES (audit.actions RCA/coach/downtime rows, Qdrant sop collection), KPIS_IMPACTED (sop_coverage, doc_authoring_time), HITL_TIER_DEFAULT="supervisor", build_trn05_evidence_panel. prompts.py: IT generation prompt (fixed sections + [SRC:N] anchors) and EN translation prompt (preserve [SRC:N] in place). Export DocumentationSynthesizer from __init__.py.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/knowledge/documentation-synthesizer/tests/test_hitl_preindex.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test_hitl_preindex.py passes: indexer.upsert NOT called before interrupt returns; exactly 1 SOP_DRAFT row written after resume.
    - grep agent.py: indexer.upsert and _write_audit both appear after the interrupt() call; saver=None raises RuntimeError.
  </acceptance_criteria>
  <done>DocumentationSynthesizer gates indexing + audit behind supervisor HITL per the corrected ordering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical events to LLM | RCA/downtime text fed into SOP generation |
| LLM translation output to index | EN text + citations crossing into Qdrant on approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Repudiation | citation spoofing | mitigate | SOPCitationValidator verifies anchors against anchor_map; source_uri from RAG citations, not trusting LLM free-text (Pitfall §1) |
| T-08-14 | Tampering | unapproved indexing | mitigate | indexer.upsert only after HITL interrupt resumes (D-DS-03) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 8 |
</threat_model>

<verification>
- `python -m pytest apps/agents/knowledge/documentation-synthesizer/tests/ -x -q` green.
- Indexing and audit occur only after the interrupt resumes.
</verification>

<success_criteria>
SC-4 satisfied: bilingual SOP draft from historical events, citation-anchored, HITL-gated before indexing.
</success_criteria>

<output>
Create `.planning/phases/08-agents-knowledge-training/08-07-SUMMARY.md` when done.
</output>
