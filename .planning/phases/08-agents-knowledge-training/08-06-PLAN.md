---
phase: 08-agents-knowledge-training
plan: 06
type: execute
wave: 2
depends_on: ["08-00"]
files_modified:
  - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/dedup.py
  - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/staleness.py
  - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/reuse_rate.py
  - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/models.py
  - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/metadata.py
  - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/agent.py
  - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/__init__.py
autonomous: true
requirements: [TRN-05]
must_haves:
  truths:
    - "Exact-dup detection uses SHA-256 of normalized text; near-dup uses BGE-M3 cosine via direct Qdrant query_points with a configurable threshold (default 0.92) (SC-3, D-KC-01)"
    - "Staleness flags a doc past its per-type threshold (SOP 365d / runbook 180d / note 90d, configurable) (D-KC-02)"
    - "Reuse-rate KPI = distinct cited source_uri / total indexed docs over a rolling window from audit.actions evidence_panel (D-KC-03)"
    - "KnowledgeCurator is AUTONOMOUS: KNOWLEDGE_DEDUP / STALE_FLAG audit rows written immediately, NO interrupt() anywhere (D-KC-04)"
  artifacts:
    - path: "apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/dedup.py"
      provides: "ExactDedupChecker (SHA-256) + NearDedupChecker (BGE-M3 cosine)"
      contains: "sha256"
    - path: "apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/agent.py"
      provides: "Autonomous KnowledgeCurator node (no HITL)"
      contains: "KNOWLEDGE_DEDUP"
  key_links:
    - from: "dedup.py NearDedupChecker"
      to: "Qdrant query_points"
      via: "BGE-M3 dense vector + score_threshold"
      pattern: "score_threshold"
---

<objective>
Implement KnowledgeCurator: hybrid SHA-256 + BGE-M3 cosine dedup (D-KC-01), per-document-type staleness flags (D-KC-02), reuse-rate KPI from citation history (D-KC-03), AUTONOMOUS audit writes with no HITL (D-KC-04). Mirror the DowntimeAnalyzer autonomous pattern.

Purpose: Closes SC-3 — detect duplicate document, flag stale document beyond threshold, track knowledge reuse rate.
Output: dedup.py, staleness.py, reuse_rate.py, models.py, metadata.py, agent.py, __init__.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agents-knowledge-training/08-CONTEXT.md
@.planning/phases/08-agents-knowledge-training/08-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-PATTERNS.md

<interfaces>
From sft_knowledge.embedding.bge_m3.BgeM3Embedder.encode(texts, return_dense=True, return_sparse=False) -> EncodeOutput.dense_vecs[0] (1024D np.ndarray). Set BGE_M3_DEVICE=cpu in tests.
Qdrant AsyncQdrantClient.query_points(collection_name, query=vec.tolist(), using="dense", limit, with_payload=False, score_threshold) -> result.points.
From sft_agents.models.enums: ActionType.KNOWLEDGE_DEDUP, ActionType.STALE_FLAG, Decision.AUTO.
audit.actions evidence_panel JSONB rag_citations[].source_uri for reuse-rate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dedup + staleness + reuse_rate + models (D-KC-01/02/03)</name>
  <files>apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/dedup.py, apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/staleness.py, apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/reuse_rate.py, apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/models.py</files>
  <read_first>
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/validators.py (class + ClassVar SQL analog)
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/oee.py and repository.py (pure computation + SQL aggregate analogs)
    - 08-PATTERNS.md sections "dedup.py" (lines 833-907), "staleness.py" (lines 911-951), "reuse_rate.py" (lines 955-997)
    - 08-CONTEXT.md D-KC-01/02/03; 08-RESEARCH.md RF-1 (exact Qdrant near-dup flow) and Pitfall §6 (threshold)
  </read_first>
  <behavior>
    - ExactDedupChecker.normalize lowercases + collapses whitespace; sha256 deterministic; same text after reformatting hashes equal.
    - NearDedupChecker.check returns is_near_dup=True when Qdrant returns points above threshold, False otherwise; threshold configurable (default 0.92).
    - is_stale returns True only when age > per-type threshold; accepts injected now for determinism.
    - reuse_rate = distinct cited / total indexed; 0.0 when no docs indexed.
  </behavior>
  <action>
    In dedup.py implement ExactDedupChecker (static normalize + sha256, ClassVar lookup SQL) and NearDedupChecker(qdrant_client, embedder, cosine_threshold=0.92, collection_name="sop") whose async check encodes normalized_text (dense only) and calls query_points with score_threshold, returning a DedupResult (is_duplicate, dup_type, source_uri, score) — direct Qdrant call, not RetrievalPipeline (RF-1). In staleness.py implement pure is_stale(*, doc_type, last_updated, thresholds=None, now=None) with _DEFAULT_THRESHOLDS {sop:365, runbook:180, note:90} and a default of 90 for unknown types. In reuse_rate.py implement ReuseRateKPI(pool) with ClassVar parameterized SQL counting distinct evidence_panel rag_citations source_uri over a window and total indexed docs, returning 0.0 when total is zero. In models.py define frozen IngestRequest and CurationReport (dedup_result, staleness_result, reuse_rate, citations list[RagCitation]).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/knowledge/knowledge-curator/tests/test_dedup.py apps/agents/knowledge/knowledge-curator/tests/test_staleness.py apps/agents/knowledge/knowledge-curator/tests/test_reuse_rate.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test_dedup.py::test_exact_dup and ::test_near_dup_threshold pass (threshold boundary covered per Pitfall §6).
    - test_staleness.py boundary tests pass with injected now.
    - test_reuse_rate.py passes; returns 0.0 on zero indexed docs.
    - All SQL uses $N placeholders only.
  </acceptance_criteria>
  <done>Hybrid dedup, per-type staleness, and reuse-rate KPI implemented as testable units.</done>
</task>

<task type="auto">
  <name>Task 2: Autonomous KnowledgeCurator node + metadata (D-KC-04)</name>
  <files>apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/agent.py, apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/metadata.py, apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/__init__.py</files>
  <read_first>
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/agent.py (autonomous, no-HITL, immediate audit analog)
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/metadata.py (metadata + evidence panel)
    - 08-PATTERNS.md section "knowledge-curator/.../agent.py" (lines 797-829), Pattern I
    - 08-CONTEXT.md D-KC-04 (autonomous, no HITL)
  </read_first>
  <action>
    Define KnowledgeCurator(pool, audit_writer, dedup, staleness, reuse_rate) with AGENT_ID="knowledge-curator", CLUSTER="knowledge". In __call__: run dedup.check and staleness.check on the ingest request, compute reuse_rate; write KNOWLEDGE_DEDUP audit row when a duplicate is found and STALE_FLAG when stale — IMMEDIATELY (before return), with Decision.AUTO, never via interrupt() (D-KC-04). There must be NO import or call of langgraph interrupt in this agent. Return a CurationReport. metadata.py: AGENT_ID, TOOL_INVENTORY (dedup, staleness, reuse_rate), DATA_SOURCES (Qdrant collections, audit.actions citations, ingest_state), KPIS_IMPACTED (knowledge_reuse_rate, duplicate_rate, stale_doc_count), HITL_TIER_DEFAULT="none", build_trn05_evidence_panel. Export KnowledgeCurator from __init__.py.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -c "from trn_knowledge_curator.agent import KnowledgeCurator; import inspect; src=inspect.getsource(KnowledgeCurator); assert 'interrupt' not in src, 'autonomous agent must not call interrupt'; print('autonomous OK')"</automated>
  </verify>
  <acceptance_criteria>
    - KnowledgeCurator imports; source contains no interrupt() usage (autonomous D-KC-04).
    - Audit rows (KNOWLEDGE_DEDUP / STALE_FLAG) written immediately with Decision.AUTO before return.
    - CurationReport returned carries dedup + staleness + reuse_rate.
  </acceptance_criteria>
  <done>Autonomous curator writes dedup/stale audit rows immediately with no HITL and tracks reuse rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest document to dedup | untrusted document text hashed + embedded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | dedup threshold | mitigate | Threshold is server-side config, never in the API request body |
| T-08-12 | Information Disclosure | Qdrant near-dup query | mitigate | with_payload=False on the cosine query (no chunk content leaked in dedup path) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 8 |
</threat_model>

<verification>
- `python -m pytest apps/agents/knowledge/knowledge-curator/tests/ -x -q` green.
- KnowledgeCurator source contains no interrupt() (autonomous).
</verification>

<success_criteria>
SC-3 satisfied: duplicate detection, staleness flagging, and reuse-rate KPI operate autonomously with immediate audit.
</success_criteria>

<output>
Create `.planning/phases/08-agents-knowledge-training/08-06-SUMMARY.md` when done.
</output>
