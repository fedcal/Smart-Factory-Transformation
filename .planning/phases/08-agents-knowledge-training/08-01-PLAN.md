---
phase: 08-agents-knowledge-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sft-agents/src/sft_agents/runtime/clusters.py
  - packages/sft-agents/tests/runtime/test_build_knowledge_subgraph.py
autonomous: true
requirements: [TRN-02, TRN-03, TRN-04, TRN-05]
must_haves:
  truths:
    - "build_knowledge_subgraph routes state['target_agent'] to the matching child callable"
    - "Unknown/missing target_agent falls back to knowledge-curator (autonomous, D-KC-04) with a structlog warning"
    - "Constructing the subgraph without knowledge-curator raises ValueError"
  artifacts:
    - path: "packages/sft-agents/src/sft_agents/runtime/clusters.py"
      provides: "build_knowledge_subgraph + _KNW_DEFAULT_AGENT"
      contains: "build_knowledge_subgraph"
  key_links:
    - from: "build_knowledge_subgraph"
      to: "_KNW_DEFAULT_AGENT"
      via: "fallback route on unknown target_agent"
      pattern: "knowledge-curator"
---

<objective>
Add build_knowledge_subgraph to clusters.py mirroring build_maintenance_subgraph (D-X-04), with knowledge-curator as the safe fallback default agent (autonomous, D-KC-04).

Purpose: Provides the routing layer that the gateway router (08-08) wires the four knowledge agents into.
Output: build_knowledge_subgraph function, _KNW_DEFAULT_AGENT constant, __all__ update, and unit tests for routing + fallback + validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agents-knowledge-training/08-CONTEXT.md
@.planning/phases/08-agents-knowledge-training/08-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-PATTERNS.md

<interfaces>
From packages/sft-agents/src/sft_agents/runtime/clusters.py (build_maintenance_subgraph is the verbatim template, PATTERNS lines 178-244):
- build_maintenance_subgraph(child_callables: Mapping[str, Callable[[AgentState], Awaitable[dict[str, Any]]]]) -> StateGraph
- Uses StateGraph(AgentState), add_node per slug, add_conditional_edges(START, _route, mapping), add_edge(slug, END)
- _route reads state.get("target_agent"); falls back to module default with a structlog warning
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append build_knowledge_subgraph to clusters.py</name>
  <files>packages/sft-agents/src/sft_agents/runtime/clusters.py</files>
  <read_first>
    - packages/sft-agents/src/sft_agents/runtime/clusters.py (build_maintenance_subgraph lines 176-251, _log, __all__)
    - 08-PATTERNS.md section "clusters.py" (full function pattern, lines 174-247)
    - 08-CONTEXT.md D-X-04 (dedicated subgraph) and D-KC-04 (curator autonomous => safe fallback)
  </read_first>
  <action>
    Append _KNW_DEFAULT_AGENT = "knowledge-curator" with the rationale comment (autonomous, no irreversible side effects), then build_knowledge_subgraph copying build_maintenance_subgraph structure verbatim: raise ValueError if child_callables empty or missing knowledge-curator; build StateGraph(AgentState); add a node per slug; define _route reading state.get("target_agent") returning _KNW_DEFAULT_AGENT with a knw_route_unknown_target structlog warning when missing/unknown; add_conditional_edges(START, _route, slug->slug mapping); add_edge(slug, END) for each; return the uncompiled graph. Add "build_knowledge_subgraph" to __all__.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -c "from sft_agents.runtime.clusters import build_knowledge_subgraph, _KNW_DEFAULT_AGENT; assert _KNW_DEFAULT_AGENT=='knowledge-curator'; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - build_knowledge_subgraph importable; _KNW_DEFAULT_AGENT == "knowledge-curator".
    - Calling with empty mapping raises ValueError; calling without knowledge-curator key raises ValueError.
    - "build_knowledge_subgraph" present in clusters.__all__.
  </acceptance_criteria>
  <done>build_knowledge_subgraph mirrors build_maintenance_subgraph with curator fallback; exported.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for routing + fallback + validation</name>
  <files>packages/sft-agents/tests/runtime/test_build_knowledge_subgraph.py</files>
  <read_first>
    - packages/sft-agents/tests/runtime/ (existing build_maintenance_subgraph test if present — mirror it)
    - 08-PATTERNS.md section "clusters.py"
  </read_first>
  <action>
    Create tests asserting: (1) compiling the subgraph and invoking with target_agent set to each of the 4 slugs routes to that child callable; (2) missing/unknown target_agent routes to knowledge-curator; (3) empty child_callables raises ValueError; (4) child_callables without knowledge-curator raises ValueError. Use simple async stub callables that record which slug ran. Compile with an in-memory checkpointer (or no checkpointer where allowed) following the maintenance subgraph test analog.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest packages/sft-agents/tests/runtime/test_build_knowledge_subgraph.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All routing, fallback, and ValueError tests pass.
    - Fallback test proves an unknown target reaches knowledge-curator.
  </acceptance_criteria>
  <done>Subgraph routing + fallback + validation verified green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supervisor to knowledge subgraph | target_agent (client-influenced) selects the child callable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Elevation of Privilege | _route target selection | mitigate | Unknown target routes to autonomous curator (no irreversible action), not to a HITL/state-changing agent |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 8 |
</threat_model>

<verification>
- `python -m pytest packages/sft-agents/tests/runtime/test_build_knowledge_subgraph.py -x -q` green.
- build_knowledge_subgraph importable and exported in __all__.
</verification>

<success_criteria>
Knowledge cluster routing layer exists, validated, with the autonomous-curator safe fallback.
</success_criteria>

<output>
Create `.planning/phases/08-agents-knowledge-training/08-01-SUMMARY.md` when done.
</output>
