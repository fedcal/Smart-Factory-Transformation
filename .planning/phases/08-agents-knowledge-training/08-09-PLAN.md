---
phase: 08-agents-knowledge-training
plan: 09
type: execute
wave: 5
depends_on: ["08-04", "08-05", "08-06", "08-07", "08-08"]
files_modified:
  - apps/api-gateway/tests/test_knowledge_cluster_e2e.py
  - docs/agents/knowledge-cluster.it.md
  - docs/agents/knowledge-cluster.en.md
  - mkdocs.yml
autonomous: true
requirements: [TRN-02, TRN-03, TRN-04, TRN-05]
must_haves:
  truths:
    - "An E2E test exercises all four knowledge agents end-to-end (ShiftHandover dual sign-off, TrainingCoach pass+signoff, KnowledgeCurator autonomous dedup/stale, DocumentationSynthesizer pre-index HITL) with mock LLM + testcontainers"
    - "The test suite REJECTS opaque (uncited) TRN outputs — every agent output asserted to carry source_uri + timestamp (SC-5, TRN-05)"
    - "Bilingual IT+EN docs describe the knowledge cluster and mkdocs build is green"
  artifacts:
    - path: "apps/api-gateway/tests/test_knowledge_cluster_e2e.py"
      provides: "Four-agent end-to-end + citation-provenance assertions"
      contains: "source_uri"
    - path: "docs/agents/knowledge-cluster.it.md"
      provides: "IT cluster documentation"
      contains: "ShiftHandover"
  key_links:
    - from: "test_knowledge_cluster_e2e.py"
      to: "all 4 agent outputs"
      via: "source_uri + timestamp assertion"
      pattern: "source_uri"
---

<objective>
Close Phase 8: an end-to-end test across all four knowledge agents that also enforces TRN-05 (no opaque outputs), plus bilingual IT+EN cluster documentation and mkdocs nav wiring.

Purpose: Closes SC-5 (citation provenance enforced; no opaque outputs accepted) and provides the phase's E2E gate.
Output: test_knowledge_cluster_e2e.py, IT+EN docs, mkdocs.yml nav update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agents-knowledge-training/08-CONTEXT.md
@.planning/phases/08-agents-knowledge-training/08-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
E2E analog: apps/api-gateway tests E2E from Phase 7 (testcontainers + mock LLM backend LLM_BACKEND=mock). Knowledge endpoints from 08-08. Agent outputs all carry citations (RagCitation source_uri + retrieved_at). Docs analog: 07-11 bilingual IT canonical / EN parallel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-agent E2E + TRN-05 opaque-output rejection (SC-5)</name>
  <files>apps/api-gateway/tests/test_knowledge_cluster_e2e.py</files>
  <read_first>
    - apps/api-gateway tests — the Phase 7 maintenance cluster E2E (testcontainers + mock LLM wiring) and conftest
    - 08-RESEARCH.md Validation Architecture (full suite command, sampling) + Test Map TRN-05 row
    - 08-CONTEXT.md (all D-* behaviors the E2E must exercise)
  </read_first>
  <action>
    Create an E2E test (marked integration) bringing up the gateway against testcontainers PostgreSQL (timescale/timescaledb:2.18.0-pg16) with migrations 001-010 applied and LLM_BACKEND=mock. Exercise: ShiftHandover compile then simulate the two sequential supervisor resumes and assert exactly 2 HANDOVER_SIGNOFF audit rows; TrainingCoach session that passes then resume and assert 1 TRAINING_SIGNOFF row; KnowledgeCurator ingest of a known duplicate + a stale doc and assert KNOWLEDGE_DEDUP + STALE_FLAG rows with no HITL; DocumentationSynthesizer draft then resume and assert SOP_DRAFT row written only after resume and Qdrant upsert occurred post-approval. For EVERY agent output, assert the response/evidence carries source_uri + a timestamp; add an explicit negative assertion that an output lacking source_uri is rejected by SOPCitationValidator / the citation provenance test (TRN-05, SC-5 — no opaque outputs accepted).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/api-gateway/tests/test_knowledge_cluster_e2e.py --co -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - E2E collects; with Docker the four-agent flow passes including the audit-row counts per agent.
    - A negative test asserts an uncited output is rejected (TRN-05).
    - mock LLM backend is used (no live LLM dependency).
  </acceptance_criteria>
  <done>End-to-end coverage of all four agents plus enforced citation provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Bilingual cluster docs + mkdocs nav</name>
  <files>docs/agents/knowledge-cluster.it.md, docs/agents/knowledge-cluster.en.md, mkdocs.yml</files>
  <read_first>
    - .planning/phases/07-agents-maintenance-reliability/07-11-PLAN.md (bilingual IT canonical / EN parallel docs pattern)
    - existing docs/agents/ pages and mkdocs.yml nav structure
  </read_first>
  <action>
    Write docs/agents/knowledge-cluster.it.md (IT canonical) describing the four agents — tools used, data sources, HITL level, KPIs impacted — sourced from each agent's metadata.py (TOOL_INVENTORY/DATA_SOURCES/KPIS_IMPACTED), explicitly noting ShiftHandover dual-supervisor sign-off, TrainingCoach deterministic quiz + supervisor sign-off, KnowledgeCurator autonomous dedup/staleness/reuse-rate, DocumentationSynthesizer bilingual SOP + pre-index HITL, and the TRN-05 citation guarantee. Write the EN parallel page. Add both pages to the mkdocs.yml nav under the agents section. Avoid any Accenture/original-brand reference (DEL-08 standing rule).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && test -f docs/agents/knowledge-cluster.it.md && test -f docs/agents/knowledge-cluster.en.md && python -m mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both IT and EN pages exist and are in mkdocs nav.
    - `mkdocs build --strict` succeeds (no broken links).
    - grep finds zero "Accenture" references in the new pages.
  </acceptance_criteria>
  <done>Bilingual knowledge-cluster docs build cleanly and are navigable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness to DB/Qdrant | E2E applies migrations + seeds via testcontainers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Repudiation | TRN-05 enforcement | mitigate | E2E negative test rejects uncited outputs; per-agent audit-row counts asserted |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 8 |
</threat_model>

<verification>
- `python -m pytest apps/api-gateway/tests/test_knowledge_cluster_e2e.py --co -q` collects; full integration run green with Docker.
- `mkdocs build --strict` green.
</verification>

<success_criteria>
SC-5 satisfied: all TRN outputs carry source_uri + timestamp and no opaque output is accepted; the four-agent cluster is exercised end-to-end and documented bilingually.
</success_criteria>

<output>
Create `.planning/phases/08-agents-knowledge-training/08-09-SUMMARY.md` when done.
</output>
