---
phase: 08-agents-knowledge-training
plan: 08
type: execute
wave: 4
depends_on: ["08-01", "08-04", "08-05", "08-06", "08-07"]
files_modified:
  - apps/api-gateway/src/svc_api_gateway/routers/knowledge_agents.py
  - apps/api-gateway/src/svc_api_gateway/dependencies.py
  - apps/api-gateway/src/svc_api_gateway/lifespan.py
  - apps/api-gateway/src/svc_api_gateway/main.py
  - apps/api-gateway/tests/test_knowledge_agents_router.py
autonomous: true
requirements: [TRN-02, TRN-03, TRN-04, TRN-05]
must_haves:
  truths:
    - "Four knowledge endpoints exist: shift-handover/compile (202), training-coach/session + /resume (200), knowledge-curator/ingest (200), documentation-synthesizer/draft (202) (D-X-04)"
    - "Each endpoint sets state['target_agent'] and invokes the supervisor graph wired via build_knowledge_subgraph with the 4 agent callables"
    - "Manual ShiftHandover trigger is served by the /compile endpoint (complements the NATS scheduled trigger, D-SH-01)"
  artifacts:
    - path: "apps/api-gateway/src/svc_api_gateway/routers/knowledge_agents.py"
      provides: "Knowledge cluster HTTP router"
      contains: "/shift-handover/compile"
  key_links:
    - from: "lifespan.py"
      to: "build_knowledge_subgraph"
      via: "knowledge_children dict injected into the supervisor graph"
      pattern: "knowledge_children"
---

<objective>
Expose the four knowledge agents via a dedicated knowledge_agents.py FastAPI router, wire the agent callables into build_knowledge_subgraph through lifespan DI, and register the router in build_app (D-X-04). Mirror Phase 7 maintenance_agents.py / 07-10.

Purpose: Provides the HTTP surface for the cluster, including the manual ShiftHandover trigger (D-SH-01) and the async HITL endpoints.
Output: knowledge_agents.py router, dependencies + lifespan + main wiring, router tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agents-knowledge-training/08-CONTEXT.md
@.planning/phases/08-agents-knowledge-training/08-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-PATTERNS.md

<interfaces>
From 08-01: build_knowledge_subgraph(child_callables) requiring a knowledge-curator key.
Agent callables from 08-04/05/06/07: ShiftHandover, TrainingCoach, KnowledgeCurator, DocumentationSynthesizer (each __call__(state) -> dict).
Gateway helpers (existing): get_supervisor_graph, get_idempotency_cache, check_idempotency_cache, store_idempotent_response, jsonable, build_invocation_config, _require_state.
Router analog: apps/api-gateway/src/svc_api_gateway/routers/maintenance_agents.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: knowledge_agents.py router + request models</name>
  <files>apps/api-gateway/src/svc_api_gateway/routers/knowledge_agents.py</files>
  <read_first>
    - apps/api-gateway/src/svc_api_gateway/routers/maintenance_agents.py (verbatim router/endpoint/error-helper analog)
    - 08-PATTERNS.md section "routers/knowledge_agents.py" (lines 251-361 — header, imports, request model, endpoint, error helpers)
    - 08-CONTEXT.md D-X-04 (endpoint set); 08-RESEARCH.md RF-6 (endpoint table + status codes)
  </read_first>
  <action>
    Create knowledge_agents.py mirroring maintenance_agents.py: APIRouter(prefix="/v1/agents", tags=["knowledge-agents"]). Define frozen request models (ShiftHandoverCompileRequest with tz-aware shift_start/shift_end + model_validator end>start; TrainingSessionRequest with persona_role + user_roles; TrainingResumeRequest with thread_id + decision; KnowledgeCuratorIngestRequest with document_text + doc_type + last_updated; DocumentationDraftRequest with failure_mode + asset_id + window_days). Implement endpoints setting state["target_agent"] to the slug and invoking supervisor_graph.ainvoke with build_invocation_config + idempotency: POST /shift-handover/compile (202, manual trigger D-SH-01), POST /training-coach/session (200), POST /training-coach/resume (200, resume after competency HITL), POST /knowledge-curator/ingest (200, autonomous synchronous), POST /documentation-synthesizer/draft (202, async pre-index HITL). Copy _handle_recursion_error and _handle_agent_error verbatim, changing the log key prefix from maintenance_agent_ to knowledge_agent_.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -c "from svc_api_gateway.routers import knowledge_agents as r; paths={route.path for route in r.router.routes}; need={'/v1/agents/shift-handover/compile','/v1/agents/training-coach/session','/v1/agents/knowledge-curator/ingest','/v1/agents/documentation-synthesizer/draft'}; missing=need-paths; assert not missing, missing; print('routes OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All four required endpoints registered with the correct status codes (202/200/200/202).
    - Each endpoint sets state["target_agent"] to its slug.
    - Request models are frozen + extra=forbid; ShiftHandover window validated end>start.
  </acceptance_criteria>
  <done>Knowledge router exposes the four agent endpoints mirroring the maintenance router.</done>
</task>

<task type="auto">
  <name>Task 2: DI wiring (dependencies + lifespan + main) + router test</name>
  <files>apps/api-gateway/src/svc_api_gateway/dependencies.py, apps/api-gateway/src/svc_api_gateway/lifespan.py, apps/api-gateway/src/svc_api_gateway/main.py, apps/api-gateway/tests/test_knowledge_agents_router.py</files>
  <read_first>
    - apps/api-gateway/src/svc_api_gateway/dependencies.py (get_maintenance_children analog, lines 76-90)
    - apps/api-gateway/src/svc_api_gateway/lifespan.py (maintenance agent construction + subgraph wiring)
    - apps/api-gateway/src/svc_api_gateway/main.py (include_router for maintenance_agents)
    - 08-PATTERNS.md sections "dependencies.py" (365-386), "main.py" (390-401)
  </read_first>
  <action>
    Add get_knowledge_children(request) returning app.state.knowledge_children (or 503) mirroring get_maintenance_children. In lifespan.py construct the 4 knowledge agents (injecting pool, audit_writer, llm, retrieval_pipeline, indexer, escalate_tool, saver as each requires), assemble app.state.knowledge_children dict keyed by slug (must include knowledge-curator), and wire build_knowledge_subgraph into the supervisor graph alongside the maintenance subgraph. In main.py add the include_router(knowledge_agents_router.router) call after the maintenance router include. Create test_knowledge_agents_router.py using the FastAPI TestClient with a stubbed supervisor graph (AsyncMock ainvoke) asserting each endpoint returns its expected status and that the autonomous knowledge-curator/ingest path needs no HITL resume.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/api-gateway/tests/test_knowledge_agents_router.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Router test passes for all four endpoints with a stubbed graph.
    - get_knowledge_children present; lifespan builds knowledge_children including knowledge-curator and wires build_knowledge_subgraph.
    - main.py includes the knowledge router.
  </acceptance_criteria>
  <done>The four knowledge agents are reachable over HTTP and wired through build_knowledge_subgraph via lifespan DI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client to gateway | request bodies cross into agent invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Spoofing | endpoint auth | accept | Dev-mode user_roles in body; JWT/RBAC deferred to Phase 11 |
| T-08-16 | Tampering | request payloads | mitigate | Frozen + extra=forbid Pydantic models; idempotency cache on POSTs |
| T-08-17 | Denial of Service | unbounded recursion | mitigate | build_invocation_config recursion_limit; _handle_recursion_error returns structured error |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 8 |
</threat_model>

<verification>
- `python -m pytest apps/api-gateway/tests/test_knowledge_agents_router.py -x -q` green.
- All four endpoints registered; lifespan wires build_knowledge_subgraph.
</verification>

<success_criteria>
The knowledge cluster is exposed via a dedicated router and wired through build_knowledge_subgraph, including the manual ShiftHandover trigger.
</success_criteria>

<output>
Create `.planning/phases/08-agents-knowledge-training/08-08-SUMMARY.md` when done.
</output>
