---
phase: 08-agents-knowledge-training
plan: 04
type: execute
wave: 3
depends_on: ["08-00a", "08-00b", "08-02"]
files_modified:
  - apps/agents/knowledge/shift-handover/src/trn_shift_handover/agent.py
  - apps/agents/knowledge/shift-handover/src/trn_shift_handover/consumer.py
  - apps/agents/knowledge/shift-handover/src/trn_shift_handover/__init__.py
autonomous: true
requirements: [TRN-03, TRN-05]
must_haves:
  truths:
    - "ShiftHandover compiles a report then performs dual-supervisor SEQUENTIAL sign-off: two interrupt() calls, with the first HANDOVER_SIGNOFF audit row written BETWEEN them and the second after the second resume (SC-1, D-SH-03)"
    - "Scheduled trigger arrives via a NATS consumer on shift.boundary.> ; manual trigger via the API endpoint (D-SH-01)"
    - "interrupt() is called directly in __call__ with audit written AFTER each resume (CR-02); approval_id=None (CR-03); saver injected via DI (CR-01)"
  artifacts:
    - path: "apps/agents/knowledge/shift-handover/src/trn_shift_handover/agent.py"
      provides: "ShiftHandover dual-supervisor HITL node"
      contains: "interrupt"
    - path: "apps/agents/knowledge/shift-handover/src/trn_shift_handover/consumer.py"
      provides: "NATS shift.boundary.> consumer (scheduled trigger)"
      contains: "shift.boundary"
  key_links:
    - from: "agent.py __call__"
      to: "audit.actions HANDOVER_SIGNOFF (x2)"
      via: "write after each interrupt resume"
      pattern: "HANDOVER_SIGNOFF"
---

<objective>
Implement the ShiftHandover agent node with the novel dual-supervisor SEQUENTIAL HITL pattern (D-SH-03, no prior codebase precedent) and the NATS shift.boundary consumer for the scheduled trigger (D-SH-01). Mirror the Phase 7 corrected interrupt-then-audit ordering exactly.

Purpose: Closes SC-1 — auto-compiled handover with dual-supervisor sign-off under the <3 min SLA.
Output: agent.py, consumer.py, package __init__ exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agents-knowledge-training/08-CONTEXT.md
@.planning/phases/08-agents-knowledge-training/08-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-PATTERNS.md
@.planning/phases/07-agents-maintenance-reliability/07-VERIFICATION.md

<interfaces>
From 08-02: ShiftAggregator(pool).fetch_audit_window/fetch_downtime_events/build_report -> HandoverReport; ShiftWindow; build_trn05_evidence_panel.
From sft_agents.models.enums: ActionType.HANDOVER_DRAFT, ActionType.HANDOVER_SIGNOFF, Decision.HITL_SUPERVISOR, Tier.SUPERVISOR.
From sft_agents.models.audit: AuditRecord (approval_id=None allowed for pending HITL).
langgraph.types.interrupt (with ImportError fallback shim, Pattern G).
NATS consumer analog: apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/consumer.py (da-consumer / pm-consumer event-driven pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShiftHandover dual-supervisor HITL node (SC-1, D-SH-03)</name>
  <files>apps/agents/knowledge/shift-handover/src/trn_shift_handover/agent.py, apps/agents/knowledge/shift-handover/src/trn_shift_handover/__init__.py</files>
  <read_first>
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/agent.py (CR-02 interrupt-then-audit, _write_audit, AGENT_ID/CLUSTER)
    - apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/agent.py (CR-01 saver DI RuntimeError guard, lines 419-449)
    - 08-PATTERNS.md section "trn_shift_handover/agent.py" (lines 405-495, dual-interrupt pattern), Patterns A/B/G/H/I
    - 08-RESEARCH.md RF-2 (dual-supervisor sequential authoritative pattern) and Pitfall §2
  </read_first>
  <behavior>
    - First interrupt (outgoing supervisor); on resume write HANDOVER_SIGNOFF audit row #1.
    - Second interrupt (incoming supervisor); on resume write HANDOVER_SIGNOFF audit row #2.
    - Exactly 2 HANDOVER_SIGNOFF rows; row #1 written strictly between the two interrupts; neither written on the first (pre-resume) execution.
    - approval_id=None on both; cluster="knowledge".
  </behavior>
  <action>
    Define ShiftHandover(pool, audit_writer, llm, aggregator, saver) with AGENT_ID="shift-handover", CLUSTER="knowledge", and the langgraph interrupt ImportError fallback (Pattern G). Guard saver-None with RuntimeError per CR-01 (Pattern B). In __call__: build ShiftWindow from state, call aggregator to compile HandoverReport (LLM only for the capped narrative summary), then perform the dual sequential sign-off exactly per RF-2/PATTERNS: interrupt(outgoing_approval) -> after resume _write_audit(HANDOVER_SIGNOFF, motivation outgoing, approval_id=None) -> interrupt(incoming_confirmation) -> after resume _write_audit(HANDOVER_SIGNOFF, motivation incoming, approval_id=None). Also write exactly one HANDOVER_DRAFT row for the compiled draft: write the HANDOVER_DRAFT row AFTER the second interrupt() returns (per the RESEARCH.md RF-2 architecture sequence — the draft is persisted only once the handover is fully signed off, never before a resume, consistent with CR-02), so it is written once and not replayed. Return {"handover_report": report, "shift_status": "signed_off"}. _write_audit builds AuditRecord with datetime.now(timezone.utc) (WR-03), build_trn05_evidence_panel, Decision.HITL_SUPERVISOR. Export ShiftHandover from __init__.py.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/knowledge/shift-handover/tests/test_dual_signoff.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test_dual_signoff.py passes: exactly 2 HANDOVER_SIGNOFF writes; first written between the interrupts; zero writes on first (interrupt-raising) run.
    - grep confirms approval_id=None in both audit writes and no _write_audit call precedes its interrupt.
    - saver=None raises RuntimeError at construction.
    - HANDOVER_DRAFT row count == 1, written after the second resume (never before a resume).
  </acceptance_criteria>
  <done>Dual-supervisor sequential sign-off implemented per the CR-02 corrected ordering with exactly two audit rows.</done>
</task>

<task type="auto">
  <name>Task 2: NATS shift.boundary consumer (D-SH-01 scheduled trigger)</name>
  <files>apps/agents/knowledge/shift-handover/src/trn_shift_handover/consumer.py</files>
  <read_first>
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/consumer.py (NATS consumer event-driven analog)
    - 08-CONTEXT.md D-SH-01 (NATS consumer on shift.boundary.> — resolved), 08-RESEARCH.md Open Question 3
  </read_first>
  <action>
    Implement a NATS JetStream consumer subscribing to shift.boundary.> that, on each boundary event, parses the boundary payload into a ShiftWindow (server-derived from configured boundary times; reject client-supplied windows that violate bounds) and invokes the supervisor graph with target_agent="shift-handover". Mirror the da-consumer durable-subscription + ack + structlog pattern. Manual on-demand triggering is handled by the gateway endpoint (08-08) — this consumer covers only the scheduled path. Keep it injectable (pass the graph/invoke callable) so it is unit-testable without a live NATS server.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -c "from trn_shift_handover.consumer import *; print('consumer import OK')" && grep -rq "shift.boundary" apps/agents/knowledge/shift-handover/src/trn_shift_handover/consumer.py && echo "subject OK"</automated>
  </verify>
  <acceptance_criteria>
    - Consumer module imports; subscribes to a shift.boundary.> subject.
    - Boundary handler invokes the graph with target_agent="shift-handover" and a server-derived ShiftWindow.
    - Handler is injectable (graph/invoke passed in) for testability.
  </acceptance_criteria>
  <done>Scheduled trigger arrives via NATS shift.boundary consumer; manual trigger deferred to gateway.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NATS boundary event to agent | scheduled trigger payload crosses into agent invocation |
| supervisor human to HITL | two sequential approvals resume the suspended thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Repudiation | dual sign-off audit | mitigate | Two immutable HANDOVER_SIGNOFF rows with motivation per supervisor; approval_id=None (CR-03) |
| T-08-07 | Tampering | boundary event window | mitigate | Server derives ShiftWindow from configured boundary times; ShiftWindow validates bounds |
| T-08-08 | Denial of Service | <3 min SLA | mitigate | Aggregation LLM-free/capped (<30s) leaving headroom for two human approvals (Pitfall §4) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 8 |
</threat_model>

<verification>
- `python -m pytest apps/agents/knowledge/shift-handover/tests/test_dual_signoff.py -x -q` green.
- Consumer subscribes to shift.boundary.> and invokes the graph with target_agent="shift-handover".
</verification>

<success_criteria>
SC-1 satisfied: auto-compiled handover with dual-supervisor sequential sign-off, scheduled via NATS, under the <3 min SLA.
</success_criteria>

<output>
Create `.planning/phases/08-agents-knowledge-training/08-04-SUMMARY.md` when done.
</output>
