---
phase: 08-agents-knowledge-training
plan: 05
type: execute
wave: 2
depends_on: ["08-00a", "08-00b"]
files_modified:
  - apps/agents/knowledge/training-coach/src/trn_training_coach/quiz.py
  - apps/agents/knowledge/training-coach/src/trn_training_coach/difficulty.py
  - apps/agents/knowledge/training-coach/src/trn_training_coach/models.py
  - apps/agents/knowledge/training-coach/src/trn_training_coach/metadata.py
  - apps/agents/knowledge/training-coach/src/trn_training_coach/prompts.py
  - apps/agents/knowledge/training-coach/src/trn_training_coach/agent.py
  - apps/agents/knowledge/training-coach/src/trn_training_coach/__init__.py
autonomous: true
requirements: [TRN-02, TRN-05]
must_haves:
  truths:
    - "Quiz scoring is deterministic: operator_answer_index == correct_answer_index, no LLM in the scoring path (SC-2, D-TC-01, Pitfall §3)"
    - "Difficulty rises on correct / falls on incorrect within the session, capped at extremes (D-TC-02)"
    - "On pass (score >= configurable threshold default 0.80) a single TRAINING_SIGNOFF audit row is written AFTER the supervisor HITL interrupt resumes (D-TC-03, CR-02)"
    - "Persona content selection uses SOP frontmatter role (no new personas registry, D-X-03); every quiz question carries source_uri (TRN-05)"
  artifacts:
    - path: "apps/agents/knowledge/training-coach/src/trn_training_coach/quiz.py"
      provides: "MCQQuestion/MCQSession + deterministic score_session"
      contains: "correct_answer_index"
    - path: "apps/agents/knowledge/training-coach/src/trn_training_coach/agent.py"
      provides: "TrainingCoach node with single-interrupt competency sign-off"
      contains: "TRAINING_SIGNOFF"
  key_links:
    - from: "agent.py score path"
      to: "quiz.score_session"
      via: "deterministic index comparison (no LLM)"
      pattern: "score_session"
---

<objective>
Implement TrainingCoach: deterministic multiple-choice quiz (D-TC-01), per-role content selection + dynamic difficulty (D-TC-02), configurable pass threshold (default 0.80) with supervisor HITL sign-off before recording (D-TC-03). Personas derive from SOP frontmatter role (D-X-03). Every question carries source_uri (TRN-05).

Purpose: Closes SC-2 — contextual coaching session, competency assessed via quiz, sign-off routed to supervisor HITL before recording.
Output: quiz.py, difficulty.py, models.py, metadata.py, prompts.py, agent.py, __init__.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-agents-knowledge-training/08-CONTEXT.md
@.planning/phases/08-agents-knowledge-training/08-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-PATTERNS.md

<interfaces>
From sft_knowledge.retrieval.pipeline.RetrievalPipeline.search(query, user_roles, category="training", k=5, asset_family=..., lang=...) -> list[RagCitation].
From sft_agents.models.enums: ActionType.TRAINING_SESSION, ActionType.TRAINING_SIGNOFF, Decision.HITL_SUPERVISOR, Tier.SUPERVISOR.
Persona roles: SOP frontmatter role field (e.g. tessitore, tintore, manutentore); asset families loom/spinning/warping/dyeing/finishing from sft-assets registry (D-X-03, RESEARCH RF-5).
langgraph.types.interrupt (ImportError fallback shim, Pattern G).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic quiz + dynamic difficulty + models (D-TC-01/02)</name>
  <files>apps/agents/knowledge/training-coach/src/trn_training_coach/quiz.py, apps/agents/knowledge/training-coach/src/trn_training_coach/difficulty.py, apps/agents/knowledge/training-coach/src/trn_training_coach/models.py</files>
  <read_first>
    - apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/mttr.py (pure-computation analog)
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/models.py (frozen model analog)
    - 08-PATTERNS.md sections "quiz.py" (lines 722-766), "difficulty.py" (lines 770-793), Pattern D
    - apps/agents/knowledge/training-coach/tests/test_quiz_scoring.py and test_difficulty.py (Wave 0 contracts)
  </read_first>
  <behavior>
    - score_session returns correct/total via index equality; 0.0 for empty; no LLM call.
    - next_difficulty("medium", answered_correctly=True) -> "hard"; ("hard", True) -> "hard" (capped); ("easy", False) -> "easy" (capped).
    - MCQQuestion requires correct_answer_index (frozen), options length 2..6, source_uri (TRN-05), difficulty label.
  </behavior>
  <action>
    In quiz.py define frozen MCQQuestion (question_text, options list min2/max6, correct_answer_index ge0, source_uri, difficulty) and frozen MCQSession (session_id, persona_role, questions, answers default []); implement pure score_session(session) -> float computing correct/total by index comparison only (Pitfall §3 — no LLM). In difficulty.py define _DIFFICULTY_LEVELS=("easy","medium","hard") and pure next_difficulty(current, *, answered_correctly) returning a NEW capped value (immutability). In models.py define frozen TrainingSession and CompetencyResult (persona_role, score, passed, threshold, citations list[RagCitation] for TRN-05).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/knowledge/training-coach/tests/test_quiz_scoring.py apps/agents/knowledge/training-coach/tests/test_difficulty.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test_quiz_scoring.py and test_difficulty.py pass.
    - score_session is a pure function (no async, no LLM import in quiz.py — grep finds no llm/interrupt usage).
    - next_difficulty caps at easy/hard and never mutates input.
  </acceptance_criteria>
  <done>Deterministic scoring + capped dynamic difficulty + frozen models verified.</done>
</task>

<task type="auto">
  <name>Task 2: TrainingCoach node (RAG quiz gen + single-interrupt sign-off) + metadata/prompts</name>
  <files>apps/agents/knowledge/training-coach/src/trn_training_coach/agent.py, apps/agents/knowledge/training-coach/src/trn_training_coach/metadata.py, apps/agents/knowledge/training-coach/src/trn_training_coach/prompts.py, apps/agents/knowledge/training-coach/src/trn_training_coach/__init__.py</files>
  <read_first>
    - apps/agents/maintenance/maintenance-coach/src/mnt_maintenance_coach/agent.py (single-interrupt HITL + constructor lines 366-410)
    - apps/agents/maintenance/rca-specialist/src/mnt_rca_specialist/metadata.py (metadata + evidence panel)
    - 08-PATTERNS.md section "training-coach/src/trn_training_coach/agent.py" (lines 670-718), Patterns A/G/H/I — NOTE: the PATTERNS stub shows TRAINING_SESSION written at step 6 BEFORE interrupt() (pre-W4). The action text below is AUTHORITATIVE: on the pass path TRAINING_SESSION MUST be written AFTER interrupt() returns (W4 / CR-02 ordering).
    - 08-CONTEXT.md D-TC-01/02/03, D-X-03; 08-RESEARCH.md RF-5 (personas) + Open Question 2 (generate-at-session-start, frozen)
  </read_first>
  <action>
    Define TrainingCoach(pool, audit_writer, llm, retrieval_pipeline, escalate_tool, pass_threshold=0.80) per the maintenance-coach constructor shape. In __call__: select persona_role from state (validate against SOP frontmatter role values, D-X-03); RAG-retrieve SOP content via retrieval_pipeline.search(category="training" or "sop", user_roles, asset_family for the persona); generate quiz questions from retrieved content via LLM and FREEZE them into an MCQSession with correct_answer_index set (RESEARCH Open Q2 — generate-at-session-start; LLM only for generation, never scoring); deliver and score deterministically via score_session; adapt difficulty across questions via next_difficulty. Then handle the audit writes in CR-02 corrected order so nothing is double-written on LangGraph resume: if score >= pass_threshold, FIRST interrupt(SUPERVISOR competency sign-off) DIRECTLY in __call__, and AFTER the interrupt resumes write the TRAINING_SESSION row AND a single TRAINING_SIGNOFF row (both with approval_id=None, CR-03, Decision.HITL_SUPERVISOR) — placing the TRAINING_SESSION write after interrupt() prevents it from being replayed/double-written when the node resumes on a passing session (W4, mirrors the RESEARCH RF-2 architecture and CR-02 ordering). On fail (score < threshold) there is no interrupt: write exactly one TRAINING_SESSION row immediately after scoring, before return, and no TRAINING_SIGNOFF. Net result: exactly 1 TRAINING_SESSION row total in both the pass and fail paths. Return the TrainingSession + hitl_status. metadata.py: AGENT_ID="training-coach", TOOL_INVENTORY, DATA_SOURCES (Qdrant training/sop collections, SOP frontmatter roles), KPIS_IMPACTED (training_time, competency_pass_rate), build_trn05_evidence_panel. prompts.py: quiz-generation prompt that emits closed MCQ with an explicit correct option and the source_uri per question (TRN-05). Export TrainingCoach from __init__.py.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/knowledge/training-coach/tests/test_hitl_lifecycle.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test_hitl_lifecycle.py passes: on pass exactly 1 TRAINING_SIGNOFF row AND exactly 1 TRAINING_SESSION row total, both written AFTER the interrupt resumes; none on the interrupt-raising run (no double-write, W4).
    - On fail (score < threshold) no TRAINING_SIGNOFF row and no interrupt.
    - Scoring path never calls the LLM (grep agent.py: score uses quiz.score_session, LLM used only in the generation step).
    - Each generated MCQQuestion carries a source_uri (TRN-05).
  </acceptance_criteria>
  <done>TrainingCoach delivers a deterministic-scored quiz with supervisor sign-off on pass, per-role + adaptive, citation-bearing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOP content to LLM quiz generation | retrieved SOP text fed to the generation prompt |
| operator answers to scorer | answer indices crossing into deterministic scoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | prompt injection in quiz content | mitigate | Scoring is index-comparison only (LLM not in scoring path); section/answer keys fixed at generation (Pitfall §3) |
| T-08-10 | Repudiation | competency sign-off | mitigate | Single TRAINING_SIGNOFF audit row after supervisor resume; approval_id=None (CR-03) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 8 |
</threat_model>

<verification>
- `python -m pytest apps/agents/knowledge/training-coach/tests/ -x -q` green (scoring, difficulty, HITL lifecycle).
- LLM appears only in the quiz-generation path, never the scoring path.
</verification>

<success_criteria>
SC-2 satisfied: contextual coaching session, deterministic competency quiz, supervisor HITL sign-off before recording.
</success_criteria>

<output>
Create `.planning/phases/08-agents-knowledge-training/08-05-SUMMARY.md` when done.
</output>
