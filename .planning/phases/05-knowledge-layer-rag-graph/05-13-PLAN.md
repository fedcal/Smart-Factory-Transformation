---
phase: 05-knowledge-layer-rag-graph
plan: 13
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [KNW-03]
files_modified:
  - services/knowledge-ingest/scripts/run_ab_eval.py
  - docs/docs/knowledge-layer/eval-results.md
  - docs/docs/en/knowledge-layer/eval-results.md
  - docs/eval/rag-ab-test-bge-m3-vs-e5.md
  - services/knowledge-ingest/tests/test_run_ab_eval_disclaimer.py
must_haves:
  truths:
    - "All three published surfaces (canonical docs/eval, MkDocs IT, MkDocs EN) carry an identical Preliminary stub metrics admonition above the metrics table"
    - "The CLI flag `--skip-eval` is renamed to `--stub` with default False; running `run_ab_eval.py` with no flags raises NotImplementedError instead of silently producing stubs"
    - "The `--stub` flag explicitly opts in to deterministic stub numbers AND emits the disclaimer banner into the generated deliverable"
    - "A regression test asserts: (a) running main with no flags raises NotImplementedError; (b) running with `--stub` produces output containing the literal disclaimer phrase"
    - "The deferred full-eval task is captured as a Phase 8 follow-up (KnowledgeCurator) in the canonical doc"
  artifacts:
    - path: "services/knowledge-ingest/scripts/run_ab_eval.py"
      provides: "Flag renamed to --stub with default False; banner always rendered when stub=True"
      contains: "--stub"
    - path: "docs/docs/knowledge-layer/eval-results.md"
      provides: "Disclaimer admonition above the metrics table (IT)"
      contains: "Preliminary stub metrics"
    - path: "docs/docs/en/knowledge-layer/eval-results.md"
      provides: "Disclaimer admonition above the metrics table (EN)"
      contains: "Preliminary stub metrics"
    - path: "services/knowledge-ingest/tests/test_run_ab_eval_disclaimer.py"
      provides: "CLI default + banner regression test"
      contains: "NotImplementedError"
  key_links:
    - from: "run_ab_eval.py main() default path"
      to: "NotImplementedError"
      via: "no-flag invocation"
      pattern: "raise NotImplementedError"
    - from: "run_ab_eval.py --stub path"
      to: "Preliminary stub metrics banner"
      via: "_render_deliverable(skipped=True)"
      pattern: "Preliminary stub metrics"
---

<objective>
Close VERIFICATION.md gap 3 (KNW-03 PARTIAL / IN-05): the A/B eval published
numbers on `docs/docs/knowledge-layer/eval-results.md` and its EN mirror are
hardcoded stubs from `_stub_summary()`, but neither MkDocs page carries the
"Preliminary run notice" disclaimer that the canonical `docs/eval/...` doc has.
Worse, `--skip-eval` defaults to True, so any forgotten flag silently produces
fake metrics in CI as the KNW-03 deliverable.

Decision: take fix path (A) per VERIFICATION.md guidance. Path (A) is the
cheapest delivery that closes the literal gap without requiring live Qdrant +
BGE-M3 + reranker infrastructure on a tested corpus. The full live eval path is
deferred to Phase 8 KnowledgeCurator (where the agent already runs against a
warm retrieval stack) and recorded explicitly as a follow-up. Rationale: the
gap is about misleading documentation, not missing measurement capability —
the script CAN produce real numbers when the maintainer runs it with infra,
the gap is that CI today silently produces stubs and publishes them as truth.

Path (A) closure rules:
  1. Add an explicit admonition above the metrics table on BOTH MkDocs pages
     (IT and EN) mirroring the canonical doc's "Preliminary run notice".
  2. Rename the CLI flag from `--skip-eval` (default True) to `--stub` (default
     False) so the default invocation can no longer silently produce stubs.
     The `--full` flag continues to raise NotImplementedError (real live eval
     deferred). Without `--stub`, the script raises NotImplementedError with
     a message pointing to the Phase 8 follow-up.
  3. Update the canonical doc to record the Phase 8 deferral.

Path (B) — implement live eval — is REJECTED for this gap closure: it requires
a running Qdrant cluster with the synthetic-corpus indexed + BGE-M3 + reranker
+ tested testset, which is Phase 6+ infrastructure scope. Path (A) honors the
solo-developer ship-fast principle and removes the literal misrepresentation
without false advertising.

Output: disclaimer on both MkDocs surfaces; renamed CLI flag with safer default;
regression test for the new default; canonical doc updated with Phase 8 deferral
note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-knowledge-layer-rag-graph/05-CONTEXT.md
@.planning/phases/05-knowledge-layer-rag-graph/05-RESEARCH.md
@.planning/phases/05-knowledge-layer-rag-graph/05-VERIFICATION.md
@.planning/phases/05-knowledge-layer-rag-graph/05-REVIEW.md
@.planning/phases/05-knowledge-layer-rag-graph/05-10-ingest-service-cli-ci-eval-docs-SUMMARY.md
@services/knowledge-ingest/scripts/run_ab_eval.py
@docs/docs/knowledge-layer/eval-results.md
@docs/docs/en/knowledge-layer/eval-results.md
@docs/eval/rag-ab-test-bge-m3-vs-e5.md

<interfaces>
Current CLI surface (run_ab_eval.py:253-268):
  --testset PATH (default DEFAULT_TESTSET)
  --output PATH (default DEFAULT_OUTPUT)
  --seed INT (default 42)
  --skip-eval (action=store_true, default=True)   # ← UNSAFE default
  --full (dest=skip_eval, action=store_false)     # ← raises NotImplementedError when chosen

Target CLI surface AFTER this plan:
  --testset PATH (default DEFAULT_TESTSET)
  --output PATH (default DEFAULT_OUTPUT)
  --seed INT (default 42)
  --stub (action=store_true, default=False)       # ← opt-in stub; emits disclaimer
  --full (action=store_true, default=False)       # ← raises NotImplementedError (deferred to Phase 8)
  Mutually exclusive: pass neither → NotImplementedError with Phase 8 pointer.

Disclaimer phrase (use this verbatim across all 3 surfaces for grep-friendliness):
  "⚠ Preliminary stub metrics — pending real eval run"

Source-of-truth canonical doc currently uses (line 3):
  "**Preliminary run notice:** numbers below were produced from a placeholder testset..."
Keep that line unchanged; add a one-line "Phase 8 deferral" footnote below the metrics table.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED+GREEN): Rename --skip-eval to --stub with default False; add regression test</name>
  <files>services/knowledge-ingest/scripts/run_ab_eval.py, services/knowledge-ingest/tests/test_run_ab_eval_disclaimer.py</files>
  <read_first>
    - services/knowledge-ingest/scripts/run_ab_eval.py (full file — focus on main() at lines 251-300 and _render_deliverable banner construction)
    - .planning/phases/05-knowledge-layer-rag-graph/05-VERIFICATION.md (gap 3 missing: "Change --skip-eval default to False or rename it so a forgotten flag does not silently produce fake metrics in CI")
    - services/knowledge-ingest/tests/test_state.py (existing test pattern: how the service unit tests are structured, asyncio fixtures)
  </read_first>
  <behavior>
    - Test 1 `test_main_no_flags_raises_not_implemented`: invoke `main()` via `subprocess` or by calling `main()` directly with `sys.argv = ["run_ab_eval.py"]` (no flags); MUST raise `NotImplementedError` with a message that mentions "Phase 8" or "KnowledgeCurator" so the deferral pointer is in the error itself.
    - Test 2 `test_main_with_stub_flag_produces_disclaimer`: invoke `main()` with `--stub --output=<tmp_path>/out.md`; MUST exit 0 and the generated file MUST contain the literal substring `"⚠ Preliminary stub metrics — pending real eval run"` (exact phrase including the warning emoji).
    - Test 3 `test_main_with_full_flag_raises_not_implemented`: invoke `main()` with `--full`; MUST raise `NotImplementedError`.
    - Test 4 `test_skip_eval_flag_removed`: `subprocess.run(["python", "run_ab_eval.py", "--skip-eval"], ...)` MUST exit non-zero (argparse rejects unknown flag) — proves the legacy flag is gone.
  </behavior>
  <action>
    Step 1: Create `services/knowledge-ingest/tests/test_run_ab_eval_disclaimer.py`. Use the same conftest pattern as `test_state.py`. Import the script as a module: `from services.knowledge_ingest.scripts import run_ab_eval` if the import path works; otherwise use `runpy.run_path(...)` or `subprocess` (the script lives outside the package — confirm import path with the existing test patterns in `services/knowledge-ingest/tests/`). For Test 2, point `--output` at a `tmp_path` and read the written file. Run the test — it MUST fail (RED) because (a) `--stub` flag does not exist, (b) default no-flag invocation currently returns 0 with stub output.

    Step 2: Modify `services/knowledge-ingest/scripts/run_ab_eval.py`. In `main()`:
      - Replace the `parser.add_argument("--skip-eval", action="store_true", default=True, ...)` block with `parser.add_argument("--stub", action="store_true", default=False, help="Opt-in: produce a deliverable from deterministic stub numbers + disclaimer banner. Without this flag, --full is required.")`.
      - Replace the `--full` argument: keep `--full` as `action="store_true", default=False` (do NOT use `dest=skip_eval` anymore). Remove the `dest=skip_eval` wiring.
      - Add a guard right after `args = parser.parse_args()`: if `args.stub and args.full` → `parser.error("--stub and --full are mutually exclusive")`. If neither flag is set → `raise NotImplementedError("Live A/B eval is deferred to Phase 8 KnowledgeCurator. For an interim deliverable with placeholder metrics, re-run with --stub (a disclaimer banner is embedded automatically).")`.
      - Replace the `if args.skip_eval:` branch with `if args.stub:`. Replace `else: raise NotImplementedError(...)` (the `--full` path) — keep the existing NotImplementedError message but update the pointer to "Deferred to Phase 8 KnowledgeCurator; tracked in ROADMAP."
      - In `_render_deliverable`, ensure that when `skipped=True` the rendered banner contains the literal phrase `"⚠ Preliminary stub metrics — pending real eval run"` somewhere in the output. Inspect the current banner construction (around lines 160-180 — read those lines if not in context) and either augment the existing banner or replace it. Keep the canonical doc's existing language compatible — the phrase need only appear; it can sit next to or replace the current "Preliminary run notice" line in the rendered output.

    Step 3: Update the docstring at the top of `run_ab_eval.py` (lines 1-15) to reflect the new flag semantics and the Phase 8 deferral. Search for any lingering reference to `--skip-eval` in the same file and update to `--stub` (the help text mentions it in the repro section around lines 200-210).

    Step 4: Re-run the test suite. Tests 1-4 MUST now PASS (GREEN).

    Do NOT use fenced code blocks here. The argparse changes live in the script.
  </action>
  <verify>
    <automated>uv run python -m pytest services/knowledge-ingest/tests/test_run_ab_eval_disclaimer.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"--skip-eval"' services/knowledge-ingest/scripts/run_ab_eval.py` returns 0 (legacy flag fully removed from argparse).
    - `grep -c '"--stub"' services/knowledge-ingest/scripts/run_ab_eval.py` returns 1 (argparse).
    - `grep -c 'NotImplementedError' services/knowledge-ingest/scripts/run_ab_eval.py` returns ≥ 2 (no-flag default + --full path).
    - `grep -c "Phase 8" services/knowledge-ingest/scripts/run_ab_eval.py` returns ≥ 1.
    - `grep -c "Preliminary stub metrics — pending real eval run" services/knowledge-ingest/scripts/run_ab_eval.py` returns ≥ 1 (banner embedded in rendered output).
    - All 4 tests in `test_run_ab_eval_disclaimer.py` PASS.
    - Manual smoke check: `uv run python services/knowledge-ingest/scripts/run_ab_eval.py` (no flags) exits non-zero with the deferral message printed.
    - Manual smoke check: `uv run python services/knowledge-ingest/scripts/run_ab_eval.py --stub --output=/tmp/eval.md` exits 0; `grep "Preliminary stub metrics" /tmp/eval.md` returns ≥ 1.
    - Commit message: `fix(05-13): rename --skip-eval to --stub with safer default; require explicit opt-in (IN-05)`.
  </acceptance_criteria>
  <done>GREEN: CLI flag renamed; default no-flag invocation refuses to produce stubs; stub banner contains the canonical disclaimer phrase; regression test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add disclaimer admonition to MkDocs IT + EN pages; record Phase 8 deferral in canonical doc</name>
  <files>docs/docs/knowledge-layer/eval-results.md, docs/docs/en/knowledge-layer/eval-results.md, docs/eval/rag-ab-test-bge-m3-vs-e5.md</files>
  <read_first>
    - docs/docs/knowledge-layer/eval-results.md (full file — current IT page, no disclaimer)
    - docs/docs/en/knowledge-layer/eval-results.md (full file — current EN page, no disclaimer)
    - docs/eval/rag-ab-test-bge-m3-vs-e5.md (lines 1-10 — existing "Preliminary run notice" wording to keep tonally consistent)
    - .planning/phases/05-knowledge-layer-rag-graph/05-VERIFICATION.md (gap 3 missing: "add the same Preliminary run notice banner to docs/docs/knowledge-layer/eval-results.md and its EN mirror")
  </read_first>
  <action>
    Step 1: Modify `docs/docs/knowledge-layer/eval-results.md` (IT). Insert an MkDocs Material admonition block immediately after the H1 (`# Risultati eval A/B — BGE-M3 vs multilingual-e5-large`) and before the "Il deliverable completo..." paragraph. Use the admonition syntax `!!! warning "⚠ Preliminary stub metrics — pending real eval run"` with body text (Italian): "I numeri sotto provengono da `_stub_summary()` (placeholder deterministici per CI). La pipeline A/B live è rinviata a Phase 8 KnowledgeCurator; il deliverable canonico è `docs/eval/rag-ab-test-bge-m3-vs-e5.md`. Per rigenerare con i placeholder: `uv run python services/knowledge-ingest/scripts/run_ab_eval.py --stub`."

    Step 2: Modify `docs/docs/en/knowledge-layer/eval-results.md` (EN). Insert the equivalent admonition after the H1 and before the "The full deliverable..." paragraph. Use `!!! warning "⚠ Preliminary stub metrics — pending real eval run"` with body text (English): "The numbers below come from `_stub_summary()` (deterministic placeholders for CI). The live A/B pipeline is deferred to Phase 8 KnowledgeCurator; the canonical deliverable is `docs/eval/rag-ab-test-bge-m3-vs-e5.md`. To regenerate the placeholder version: `uv run python services/knowledge-ingest/scripts/run_ab_eval.py --stub`."

    Step 3: Also update the "Riproducibilità" / "Reproducibility" section in BOTH MkDocs pages: the command currently shown is `uv run python services/knowledge-ingest/scripts/run_ab_eval.py --seed=42` — replace with `uv run python services/knowledge-ingest/scripts/run_ab_eval.py --stub --seed=42` so the documented command actually works after the flag rename. Remove or update any `--skip-eval` references in the body text.

    Step 4: Modify `docs/eval/rag-ab-test-bge-m3-vs-e5.md` (canonical). Append a "Deferred follow-up" section at the end (or insert below the existing "Preliminary run notice" near line 3) that explicitly records: "Phase 8 KnowledgeCurator owns the live A/B eval run: BGE-M3 vs multilingual-e5-large against the warm retrieval stack (Qdrant + reranker) on the real synthetic-corpus testset. Phase 5 ships the framework (`run_ab_eval.py --stub`) and acceptance gates (D-71); Phase 8 ships the measured numbers." Also rename any in-body `--skip-eval` references to `--stub`.

    Step 5: Verify all three updated docs render under MkDocs (do not execute mkdocs serve — just lint syntax): `grep -c "!!! warning" docs/docs/knowledge-layer/eval-results.md docs/docs/en/knowledge-layer/eval-results.md` MUST return ≥ 1 per file. Do NOT use fenced code blocks here; the markdown content lives in the doc files.
  </action>
  <verify>
    <automated>grep -c "Preliminary stub metrics — pending real eval run" docs/docs/knowledge-layer/eval-results.md docs/docs/en/knowledge-layer/eval-results.md docs/eval/rag-ab-test-bge-m3-vs-e5.md && grep -c "\-\-stub" docs/docs/knowledge-layer/eval-results.md docs/docs/en/knowledge-layer/eval-results.md && (grep -c "\-\-skip-eval" docs/docs/knowledge-layer/eval-results.md docs/docs/en/knowledge-layer/eval-results.md docs/eval/rag-ab-test-bge-m3-vs-e5.md | awk -F: '{sum+=$2} END {exit (sum==0?0:1)}')</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Preliminary stub metrics — pending real eval run" docs/docs/knowledge-layer/eval-results.md` returns ≥ 1.
    - `grep -c "Preliminary stub metrics — pending real eval run" docs/docs/en/knowledge-layer/eval-results.md` returns ≥ 1.
    - `grep -c "Preliminary stub metrics — pending real eval run" docs/eval/rag-ab-test-bge-m3-vs-e5.md` returns ≥ 1.
    - `grep -c "Phase 8" docs/eval/rag-ab-test-bge-m3-vs-e5.md` returns ≥ 1 (deferral recorded).
    - `grep -c "\-\-skip-eval" docs/docs/knowledge-layer/eval-results.md docs/docs/en/knowledge-layer/eval-results.md docs/eval/rag-ab-test-bge-m3-vs-e5.md` returns 0 across all three files (no stale flag references).
    - `grep -c "\-\-stub" docs/docs/knowledge-layer/eval-results.md` returns ≥ 1 and same for the EN mirror.
    - MkDocs admonition syntax is correct: `grep -c "^!!! warning" docs/docs/knowledge-layer/eval-results.md docs/docs/en/knowledge-layer/eval-results.md` returns ≥ 1 per file.
    - Commit message: `docs(05-13): add Preliminary stub metrics admonition to KNW-03 eval pages (IN-05)`.
  </acceptance_criteria>
  <done>Both MkDocs surfaces and the canonical doc carry the disclaimer; reproducibility commands use `--stub`; legacy `--skip-eval` references purged; Phase 8 deferral recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_ab_eval.py CLI → CI/docs site | The script's output is consumed by readers as a measurement; misleading numbers without disclaimer is a form of repudiation risk |
| MkDocs site → external readers | The IT/EN pages are the audience-facing surface; canonical doc is repo-internal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13-01 | Repudiation | MkDocs publishes stub numbers as if measured | mitigate | Add disclaimer admonition on both MkDocs surfaces; rename CLI flag so the default invocation no longer silently produces stubs. |
| T-05-13-02 | Tampering | Forgotten flag in CI silently changes deliverable behavior | mitigate | Rename `--skip-eval` (default True) → `--stub` (default False, opt-in); no-flag default → NotImplementedError with Phase 8 pointer. |
| T-05-13-03 | Information Disclosure | Stub metric values reveal nothing about real RAG performance | accept | By design; the disclaimer makes this explicit. Phase 8 will replace with real numbers. |
</threat_model>

<verification>
- All 4 tests in `services/knowledge-ingest/tests/test_run_ab_eval_disclaimer.py` PASS.
- VERIFICATION.md gap 3 "missing" items both satisfied: (a) disclaimer banner present on both MkDocs eval-results.md surfaces; (b) `--skip-eval` removed in favor of `--stub` with opt-in semantics (forgotten-flag CI scenario eliminated).
- Re-verify the KNW-03 truth: "A/B evaluation results documented in docs/ are real measurements, not stub numbers" — after this plan the truth becomes "A/B evaluation framework documented in docs/; stub numbers are clearly labeled as preliminary pending Phase 8 live run." This is a literal closure of the gap — the documentation no longer claims the stubs are measurements.
- Static check: `grep -rn "skip-eval" services/knowledge-ingest/ docs/ 2>/dev/null | grep -v __pycache__` returns 0 matches (legacy flag purged from all surfaces).
</verification>

<success_criteria>
KNW-03 deliverable transparently labels the stub origin of its numbers across all
three published surfaces (canonical, IT MkDocs, EN MkDocs). The CLI cannot
silently produce stubs without an explicit opt-in. The deferred live-eval task
is recorded as a Phase 8 follow-up. No regression in any other Phase 5 test.
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-layer-rag-graph/05-13-SUMMARY.md` when done.
</output>
