---
phase: 05-knowledge-layer-rag-graph
plan: 12
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [KNW-07]
files_modified:
  - packages/sft-knowledge/src/sft_knowledge/path_utils.py
  - packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py
  - services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py
  - packages/sft-knowledge/tests/test_source_uri_resolution.py
  - packages/sft-knowledge/src/sft_knowledge/__init__.py
must_haves:
  truths:
    - "MarkdownParser and ingest_file orchestrator derive source_uri through the SAME function — no duplicated parents[N] walk"
    - "derive_source_uri(p) returns identical strings for inputs under workspace root, under symlinked workspace paths, and for absolute out-of-workspace tmp paths"
    - "Cross-platform fallback uses a single `lstrip` semantics; no `os.sep` vs `'/'` divergence"
    - "Existing test_markdown_parser.py and test_ingest_pipeline.py still pass — observable source_uri values for in-workspace inputs are unchanged"
    - "Static equality test asserts MarkdownParser().parse(p).source_uri == derive_source_uri(p) across 4 path edge cases"
  artifacts:
    - path: "packages/sft-knowledge/src/sft_knowledge/path_utils.py"
      provides: "Single canonical derive_source_uri(path, workspace_root=None) helper"
      contains: "def derive_source_uri"
    - path: "packages/sft-knowledge/tests/test_source_uri_resolution.py"
      provides: "Parametrized equality + edge-case test"
      contains: "derive_source_uri"
  key_links:
    - from: "services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py"
      to: "sft_knowledge.path_utils.derive_source_uri"
      via: "import + call inside ingest_file before content-hash gate"
      pattern: "from sft_knowledge.path_utils import derive_source_uri"
    - from: "packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py"
      to: "sft_knowledge.path_utils.derive_source_uri"
      via: "import + call inside parse() in place of inline `relative_to(_WORKSPACE_ROOT)` block"
      pattern: "derive_source_uri\\("
---

<objective>
Close VERIFICATION.md gap 2 (KNW-07 PARTIAL / CR-02): source_uri derivation is
duplicated between `services/knowledge-ingest/.../pipeline.py:_derive_source_uri`
(parents[4] + `.lstrip(os.sep)`) and `packages/sft-knowledge/.../parsers/markdown.py`
(parents[5] + `.lstrip('/')`). They converge today but a worktree, symlink, or
Windows-style separator can desynchronize them — the consequence is that
`state_store.get(URI_A)` returns None forever while `state_store.upsert(URI_B)`
lands on a different row, silently degrading incremental reindex (KNW-07 SC#3)
to full reindex with hidden double-rows.

Purpose: extract a single canonical `derive_source_uri(path)` helper into
`sft_knowledge.path_utils` (importable by both consumers), replace both inline
implementations with calls to it, and add a parametrized equality test covering
workspace / tmp / symlink / Windows-separator edge cases.

Output: new helper module exporting `derive_source_uri`; both call sites
refactored to import from `sft_knowledge.path_utils`; unit test asserting
behavioral equivalence and cross-platform consistency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-knowledge-layer-rag-graph/05-CONTEXT.md
@.planning/phases/05-knowledge-layer-rag-graph/05-RESEARCH.md
@.planning/phases/05-knowledge-layer-rag-graph/05-VERIFICATION.md
@.planning/phases/05-knowledge-layer-rag-graph/05-REVIEW.md
@.planning/phases/05-knowledge-layer-rag-graph/05-01-sft-knowledge-sdk-SUMMARY.md
@.planning/phases/05-knowledge-layer-rag-graph/05-10-ingest-service-cli-ci-eval-docs-SUMMARY.md
@packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py
@services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py
@packages/sft-knowledge/src/sft_knowledge/__init__.py

<interfaces>
Current duplication (TO BE REMOVED):

From packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py:37-40, 122-128
  _WORKSPACE_ROOT: Path = Path(__file__).resolve().parents[5]
  # inline in parse():
  try:
      rel = path.resolve().relative_to(_WORKSPACE_ROOT)
      source_uri = f"corpus://{rel.as_posix()}"
  except ValueError:
      source_uri = f"corpus://{path.resolve().as_posix().lstrip('/')}"

From services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py:55-76
  def _derive_source_uri(path: Path) -> str:
      workspace_root = Path(__file__).resolve().parents[4]
      try:
          rel = path.resolve().relative_to(workspace_root)
          return f"corpus://{rel.as_posix()}"
      except ValueError:
          return f"corpus://{path.resolve().as_posix().lstrip(os.sep)}"

Note both anchor to the same workspace root by counting parents from their own
__file__; the divergent `os.sep` vs `'/'` is the cross-platform footgun.

Target single helper (NEW — packages/sft-knowledge/src/sft_knowledge/path_utils.py):
  WORKSPACE_ROOT: Path  # resolved once via parents[5] from this module
  def derive_source_uri(path: Path, workspace_root: Path | None = None) -> str
    """Return `corpus://<rel-posix-path>` if path is under workspace_root,
    else `corpus://<absolute-posix-path-with-leading-slashes-stripped>`.
    workspace_root defaults to WORKSPACE_ROOT (module constant)."""

From packages/sft-knowledge/src/sft_knowledge/__init__.py (SDK surface — D-70):
  Add `derive_source_uri` and `WORKSPACE_ROOT` to __all__ (downstream consumers
  may need to inject a workspace_root in tests).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Add equality + edge-case test for source_uri resolution</name>
  <files>packages/sft-knowledge/tests/test_source_uri_resolution.py</files>
  <read_first>
    - packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py (lines 37-40, 122-128 — current derivation)
    - services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py (lines 55-76 — current orchestrator derivation)
    - packages/sft-knowledge/tests/test_markdown_parser.py (fixture patterns: how tests pass a Path; how the parser is invoked; tmp_path usage)
    - services/knowledge-ingest/tests/test_ingest_pipeline.py (how the orchestrator test feeds a Path through ingest_file)
    - .planning/phases/05-knowledge-layer-rag-graph/05-VERIFICATION.md (gap 2 "missing" list: parametrized test asserting `derive_source_uri(p) == MarkdownParser().parse(p).source_uri` across workspace/tmp/symlink paths)
  </read_first>
  <behavior>
    - Test 1 `test_derive_source_uri_workspace_file`: feed an existing SOP path under `simulators/synthetic-corpus/`; `derive_source_uri(p)` MUST start with `"corpus://simulators/synthetic-corpus/"` and use forward slashes only (no backslashes).
    - Test 2 `test_derive_source_uri_equals_markdown_parser_output`: pick an existing reviewed SOP (e.g. first `*.md` returned by globbing `simulators/synthetic-corpus/**/SOP-*.md`); assert `derive_source_uri(p) == (await MarkdownParser().parse(p)).source_uri`. (async — use pytest-asyncio.)
    - Test 3 `test_derive_source_uri_tmp_path_outside_workspace`: pass an absolute tmp_path; result MUST start with `"corpus://"` and MUST NOT contain `//` after the scheme (i.e. leading filesystem `/` is stripped consistently).
    - Test 4 `test_derive_source_uri_symlink_inside_workspace`: create a symlink in tmp_path pointing to an in-workspace SOP; `derive_source_uri(symlink)` MUST equal `derive_source_uri(realpath)` (both resolve through `.resolve()`).
    - All four tests MUST currently FAIL with ImportError because `sft_knowledge.path_utils` does not exist yet (RED proof).
  </behavior>
  <action>
    Create `packages/sft-knowledge/tests/test_source_uri_resolution.py`. Import `from sft_knowledge.path_utils import derive_source_uri` (this import is what creates the RED state). Import `MarkdownParser` from `sft_knowledge.parsers.markdown`. Use the existing pytest-asyncio convention from `test_markdown_parser.py`. For Test 2, glob the synthetic-corpus for the first reviewed SOP; if zero are found the test must xfail with a clear message (synthetic-corpus is part of the repo per Phase 2 — should not happen). For Test 4, use `tmp_path` and `os.symlink`. Do NOT yet create `path_utils.py` — RED requires the ImportError. Do NOT use fenced code blocks here.
  </action>
  <verify>
    <automated>uv run python -m pytest packages/sft-knowledge/tests/test_source_uri_resolution.py -x -v 2>&1 | tee /tmp/red-12.out; grep -qE "ModuleNotFoundError|ImportError|FAILED|ERROR" /tmp/red-12.out</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/sft-knowledge/tests/test_source_uri_resolution.py` exists.
    - Test run produces ImportError or ModuleNotFoundError on `sft_knowledge.path_utils` (RED proof).
    - The file contains all 4 test function names listed in the behavior block (verify by `grep -c '^def test_\|^async def test_' packages/sft-knowledge/tests/test_source_uri_resolution.py` ≥ 4).
    - No production code under `src/` is modified in this task.
    - Commit message: `test(05-12): add RED equality test for source_uri resolution helper`.
  </acceptance_criteria>
  <done>RED: test file committed, import fails (path_utils.py does not exist yet), no src/ modifications.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Extract derive_source_uri helper and route both call sites through it</name>
  <files>packages/sft-knowledge/src/sft_knowledge/path_utils.py, packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py, services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py, packages/sft-knowledge/src/sft_knowledge/__init__.py</files>
  <read_first>
    - packages/sft-knowledge/tests/test_source_uri_resolution.py (RED test from Task 1 — drives the contract)
    - packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py (full file — replace inline block at lines 37-40 and 122-128)
    - services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py (full file — replace `_derive_source_uri` at lines 55-76 entirely)
    - packages/sft-knowledge/src/sft_knowledge/__init__.py (current __all__ — add the new export)
  </read_first>
  <action>
    Step 1: Create `packages/sft-knowledge/src/sft_knowledge/path_utils.py`. Module computes `WORKSPACE_ROOT = Path(__file__).resolve().parents[4]` (path_utils.py lives at packages/sft-knowledge/src/sft_knowledge/path_utils.py → parents[0]=sft_knowledge, [1]=src, [2]=sft-knowledge, [3]=packages, [4]=workspace_root — verify with a Read of the new file location). Define `derive_source_uri(path: Path, workspace_root: Path | None = None) -> str`: resolve `path.resolve()`; if `workspace_root` is None use module `WORKSPACE_ROOT`; try `resolved.relative_to(workspace_root)` and return `f"corpus://{rel.as_posix()}"`; on ValueError return `f"corpus://{resolved.as_posix().lstrip('/')}"` (single fallback — no `os.sep`). Module exports both symbols via `__all__`.

    Step 2: Modify `packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py`. Delete the `_WORKSPACE_ROOT` module constant (lines 37-40 comment + assignment). Replace the inline try/except block in `parse()` (currently lines 122-128) with a single line: `source_uri = derive_source_uri(path)`. Add `from sft_knowledge.path_utils import derive_source_uri` import.

    Step 3: Modify `services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py`. Delete the entire `_derive_source_uri` function (lines 55-76) and its `import os` line if `os` becomes unused (grep `\bos\.` after edit — if zero matches, remove `import os` too). Replace the call site `source_uri = _derive_source_uri(path)` (line 170) with `source_uri = derive_source_uri(path)`. Add `from sft_knowledge.path_utils import derive_source_uri` import.

    Step 4: Modify `packages/sft-knowledge/src/sft_knowledge/__init__.py`. Add `derive_source_uri` and `WORKSPACE_ROOT` to the public surface — `from sft_knowledge.path_utils import derive_source_uri, WORKSPACE_ROOT` and add both names to `__all__`. Verify the count: VERIFICATION.md says the SDK currently exports 22 symbols; after this change it should be 24.

    Do NOT change MarkdownParser's parse() signature. Do NOT change ingest_file's signature. Do NOT use fenced code blocks here.
  </action>
  <verify>
    <automated>uv run python -m pytest packages/sft-knowledge/tests/test_source_uri_resolution.py packages/sft-knowledge/tests/test_markdown_parser.py services/knowledge-ingest/tests/test_ingest_pipeline.py -x -v -m "not integration and not gpu"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/sft-knowledge/src/sft_knowledge/path_utils.py` exists and defines `derive_source_uri` and `WORKSPACE_ROOT`.
    - `grep -c "_derive_source_uri\|_WORKSPACE_ROOT" services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py` returns 0 (legacy symbols fully removed).
    - `grep -c "from sft_knowledge.path_utils import derive_source_uri" services/knowledge-ingest/src/svc_knowledge_ingest/pipeline.py` returns 1.
    - `grep -c "from sft_knowledge.path_utils import derive_source_uri" packages/sft-knowledge/src/sft_knowledge/parsers/markdown.py` returns 1.
    - `grep -c "derive_source_uri" packages/sft-knowledge/src/sft_knowledge/__init__.py` returns ≥ 2 (import + __all__ entry).
    - All 4 tests in `test_source_uri_resolution.py` PASS.
    - All tests in `test_markdown_parser.py` and `test_ingest_pipeline.py` PASS (no regression on existing source_uri behavior).
    - Full Phase 5 unit suite: `uv run python -m pytest packages/sft-knowledge/tests/ services/knowledge-ingest/tests/ -m "not integration and not gpu"` → pass count ≥ 71 + 4 new = 75 (VERIFICATION.md baseline was 71 passed).
    - Commit message: `refactor(05-12): extract sft_knowledge.path_utils.derive_source_uri (CR-02)`.
  </acceptance_criteria>
  <done>GREEN: both call sites use the shared helper; legacy `_derive_source_uri` and `_WORKSPACE_ROOT` symbols removed; all tests pass including the new parametrized equality test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest_file orchestrator → IngestStateStore | The source_uri is the primary key for the dedup state row — drift breaks idempotency |
| MarkdownParser → ParsedDoc → QdrantIndexer payload | source_uri is written into the Qdrant payload — drift creates duplicate-but-different keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12-01 | Tampering | Duplicated source_uri derivation drifts cross-platform | mitigate | Single canonical helper in `sft_knowledge.path_utils`; both call sites import from it; parametrized test guards the invariant. |
| T-05-12-02 | Denial of Service | Incremental reindex silently degrades to full reindex on drift | mitigate | Helper is the only source of truth; content_hash gate now sees the same source_uri the parser will emit, restoring the KNW-07 SC#3 fast path. |
| T-05-12-03 | Information Disclosure | Symlink resolution leaks workspace-internal paths | accept | `Path.resolve()` already follows symlinks (existing behavior); workspace_root paths are not sensitive (corpus is public-by-construction in Phase 5 — D-72). |
</threat_model>

<verification>
- All 4 tests in `packages/sft-knowledge/tests/test_source_uri_resolution.py` PASS.
- `MarkdownParser` and `ingest_file` produce IDENTICAL `source_uri` for the same path (Test 2 asserts this directly).
- VERIFICATION.md gap 2 "missing" items are both satisfied: (a) canonical helper exists in `sft_knowledge.path_utils`; (b) parametrized equality test exists.
- Static check: `grep -rn "parents\[5\]\|parents\[4\]" packages/sft-knowledge/src services/knowledge-ingest/src` returns ≤ 1 match (only inside `path_utils.py`); no other module hardcodes workspace-root depth.
- No regression: Phase 5 unit suite passes with pass-count ≥ 75 (baseline 71 + 4 new tests).
</verification>

<success_criteria>
KNW-07 SC#3 invariant "incremental reindex never silently degrades to full reindex"
is restored to a single source of truth. The `state_store.get(source_uri)` lookup
inside `ingest_file` now uses the exact same string the parser will eventually
write into the Qdrant payload, eliminating the CR-02 silent-drift surface across
worktrees, symlinks, and (future) Windows paths.
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-layer-rag-graph/05-12-SUMMARY.md` when done.
</output>
