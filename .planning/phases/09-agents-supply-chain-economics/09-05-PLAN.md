---
phase: 09-agents-supply-chain-economics
plan: 05
type: execute
wave: 5
depends_on: ["09-00a", "09-00b", "09-01", "09-02"]
files_modified:
  - apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/metadata.py
  - apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/models.py
  - apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/holt_winters.py
  - apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/mape.py
  - apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/repository.py
  - apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/agent.py
  - apps/agents/supply/demand-forecaster/pyproject.toml
autonomous: true
requirements: [SCM-04]
must_haves:
  truths:
    - "DemandForecaster produces a demand plan for >=2 fabric SKU groups via deterministic Holt-Winters (numpy, fixed alpha/beta/gamma, seasonal-naive fallback for short series) and tracks a rolling MAPE accuracy KPI (SCM-04)"
    - "The approved demand plan is published to ProductionPlanner by including it in the returned state delta (state['demand_plan']) — cross-cluster via state, no direct invocation (RESEARCH Open Question 2)"
    - "HITL ordering is interrupt-then-audit: no audit before resume; on resume exactly one DEMAND_PLAN_DRAFT then one DEMAND_PLAN_SIGNOFF, correlated by a STABLE plan_id from thread_id (CR-04); AuditRecord positional (CR-02); approval_id=None pending (CR-03); MAPE clamped to <=100 before model build (CR-05)"
  artifacts:
    - path: "apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/holt_winters.py"
      provides: "forecast_holt_winters + seasonal-naive fallback (deterministic, numpy)"
      contains: "seasonal_naive"
    - path: "apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/agent.py"
      provides: "DemandForecaster.__call__ HITL agent publishing to ProductionPlanner via state"
      contains: "demand_plan"
  key_links:
    - from: "agent.py"
      to: "state['demand_plan'] (ProductionPlanner)"
      via: "approved plan placed in returned state delta after sign-off"
      pattern: "demand_plan"
---

<objective>
Implement DemandForecaster (SCM-04): deterministic Holt-Winters forecasting (numpy, fixed params, seasonal-naive fallback), rolling MAPE KPI, asyncpg repository over scm.historical_orders, and the HITL agent that publishes the approved demand plan to ProductionPlanner via the returned state delta — interrupt-then-audit, stable ids.

Purpose: Fourth supply agent; the only cross-cluster publisher (to apps/agents/ops/production-planner via state, not direct invocation). Files disjoint from 09-02/03/04; runs sequentially (worktrees disabled).
Output: metadata.py, models.py, holt_winters.py, mape.py, repository.py, agent.py, pyproject.toml (add numpy dep).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agents-supply-chain-economics/09-CONTEXT.md
@.planning/phases/09-agents-supply-chain-economics/09-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-REVIEW.md

<interfaces>
From sft_agents.models.audit / enums / proposed_action: AuditRecord + AuditWriter.write(record) POSITIONAL (CR-02); ActionType.DEMAND_PLAN_DRAFT / DEMAND_PLAN_SIGNOFF (from 09-00a); Decision.HITL_SUPERVISOR; ProposedAction.from_payload.
HITL interrupt: Pattern G shim (NotImplementedError in tests, never MagicMock — WR-01). Stable id: sha256(f"{AGENT_ID}.{state.get('thread_id','unknown')}")[:32] (Pattern 3).
forecast_holt_winters / HoltWintersConfig / ForecastResult / _seasonal_naive_fallback: 09-RESEARCH.md Pattern 9 (verbatim — numpy, fixed alpha=0.3/beta=0.1/gamma=0.3/season=12/min_periods=24; fallback when len(series)<min_periods; non-negative forecasts).
compute_mape: 09-RESEARCH.md Pattern 10 (per-point contribution clamped to 1.0; 0.0 on empty/no-valid pairs). Clamp final MAPE to <=100 before MapeReport (CR-05/Pitfall 6).
Repository: 09-RESEARCH.md "Query ordini storici per DemandForecaster" — fetch_monthly_orders(sku_group, months_back) DATE_TRUNC('month', order_date) SUM(quantity_kg); $N params, datetime objects.
ProductionPlanner target: apps/agents/ops/production-planner — read its AgentState contract; publish the approved plan into state['demand_plan'] (Open Question 2 — cross-cluster via state, no direct call).
numpy: add numpy>=1.26.0,<3.0.0 to demand-forecaster pyproject.toml (RESEARCH Open Question 1).
HITL agent shape: mirror 09-02 InventoryManager.agent.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: holt_winters + mape pure functions + models + repository + numpy dep</name>
  <files>apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/holt_winters.py, apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/mape.py, apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/models.py, apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/repository.py, apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/metadata.py, apps/agents/supply/demand-forecaster/pyproject.toml</files>
  <read_first>
    - apps/agents/supply/demand-forecaster/tests/test_holt_winters.py and test_mape.py (Wave 0 contracts — exact expected values + fallback tag)
    - 09-RESEARCH.md Pattern 9 (forecast_holt_winters + fallback verbatim) and Pattern 10 (compute_mape) and Pitfall 6 (clamp)
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/repository.py (ClassVar SQL + DATE_TRUNC aggregation analog)
    - apps/agents/supply/demand-forecaster/pyproject.toml (current dependencies = [] — add numpy)
  </read_first>
  <behavior>
    - forecast_holt_winters: deterministic for fixed series + fixed params; method tag "holt_winters" when len>=min_periods else "seasonal_naive"; forecasts non-negative (max(0,...)).
    - compute_mape: matched pairs with actual>0; per-point contribution min(|a-f|/a, 1.0); returns 0.0 on empty; MapeReport.mape clamped to <=100 before construction.
    - DemandPlan covers >=2 SKU groups; frozen + extra="forbid"; plan_id str set by agent.
  </behavior>
  <action>
    In holt_winters.py implement forecast_holt_winters + HoltWintersConfig + ForecastResult + _seasonal_naive_fallback exactly per 09-RESEARCH.md Pattern 9 (numpy, fixed params, fallback below min_periods, rounded non-negative forecasts). In mape.py implement compute_mape per Pattern 10. In models.py define SkuForecast, DemandPlan (list of SkuForecast covering >=2 groups), MapeReport (mape field with le=100 constraint — clamp before building), all frozen + extra="forbid"; plan_id str field. In repository.py define DemandRepository(pool) with ClassVar _SQL_MONTHLY_ORDERS doing DATE_TRUNC('month', order_date) SUM(quantity_kg) FROM scm.historical_orders WHERE sku_group=$1 AND order_date >= NOW() - INTERVAL '1 month' * $2 GROUP BY 1 ORDER BY 1; fetch_monthly_orders(sku_group, months_back) returns ordered monthly series. In metadata.py define AGENT_ID="demand-forecaster", CLUSTER="supply", DATA_SOURCES=('scm.historical_orders',), HITL_TIER_DEFAULT="supervisor", KPIS_IMPACTED (rolling MAPE), build_evidence_panel. Add numpy>=1.26.0,<3.0.0 to pyproject.toml dependencies.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/supply/demand-forecaster/tests/test_holt_winters.py apps/agents/supply/demand-forecaster/tests/test_mape.py -x -q 2>&1 | tail -10 && python -c "import tomllib; d=tomllib.load(open('apps/agents/supply/demand-forecaster/pyproject.toml','rb')); deps=' '.join(d['project'].get('dependencies',[])); assert 'numpy' in deps, deps; print('numpy dep OK')"</automated>
  </verify>
  <acceptance_criteria>
    - test_holt_winters.py (incl. seasonal_naive fallback) and test_mape.py pass with exact values.
    - numpy is declared in demand-forecaster pyproject dependencies.
    - repository SQL uses $N placeholders; DemandPlan supports >=2 SKU groups.
  </acceptance_criteria>
  <done>Holt-Winters, MAPE, models, repository and metadata land with the forecasting + KPI tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DemandForecaster HITL agent + ProductionPlanner publish (interrupt-then-audit)</name>
  <files>apps/agents/supply/demand-forecaster/src/scm_demand_forecaster/agent.py</files>
  <read_first>
    - apps/agents/supply/demand-forecaster/tests/test_demand_hitl.py (Wave 0 HITL + plan-in-state contract)
    - apps/agents/supply/inventory-manager/src/scm_inventory_manager/agent.py (HITL template to mirror)
    - apps/agents/ops/production-planner (AgentState contract — what key the planner reads the demand plan from)
    - 09-RESEARCH.md Open Question 2 (publish via state['demand_plan']) and Patterns 2/3/4; 08-REVIEW.md CR-02/03/04/05
  </read_first>
  <behavior>
    - Forecast >=2 SKU groups from scm.historical_orders; compute rolling MAPE (clamped <=100); derive stable plan_id from thread_id; interrupt with the demand plan; NO audit before resume.
    - On resume: exactly one DEMAND_PLAN_DRAFT then one DEMAND_PLAN_SIGNOFF row, both carrying plan_id; approval_id None pending; positional AuditRecord.
    - The approved plan is placed in the returned state delta as state['demand_plan'] so the gateway can route it to ProductionPlanner (cross-cluster via state).
  </behavior>
  <action>
    Implement DemandForecaster(pool, audit_writer, llm=None) with async __call__(state). Read sku_groups + horizon + months_back via state.get with defaults (no KeyError, CR-03); fetch monthly series per group; run forecast_holt_winters per group (fallback automatic for short series); assemble a DemandPlan covering >=2 groups; compute rolling MAPE on the held-out tail and clamp to <=100 before MapeReport (CR-05). Derive plan_id = sha256(f"{AGENT_ID}.{state.get('thread_id','unknown')}")[:32] (CR-04). interrupt({"demand_plan": ..., "mape": ..., "agent_id": AGENT_ID}); ONLY after resume build a full DEMAND_PLAN_DRAFT AuditRecord (Decision.HITL_SUPERVISOR, approval_id=None, tz-aware ts) and `await self._audit.write(record)` POSITIONAL, then a DEMAND_PLAN_SIGNOFF record with decision_actor from the resume payload. Return {"demand_plan": demand_plan.model_dump(), "mape_report": mape_report.model_dump()} — the demand_plan key is the ProductionPlanner publish channel (Open Question 2). Use the key name the production-planner AgentState actually reads (verify in read_first).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/supply/demand-forecaster/tests/test_demand_hitl.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test_demand_hitl.py passes: 0 audit writes before resume; exactly 1 DEMAND_PLAN_DRAFT + 1 DEMAND_PLAN_SIGNOFF; stable plan_id across replay; approval_id None pending; positional AuditRecord; demand_plan for >=2 groups present in the returned state delta.
    - grep confirms no inline uuid4 correlation id; MAPE clamped before model build.
  </acceptance_criteria>
  <done>DemandForecaster produces a multi-group demand plan, gates it through supervisor HITL, and publishes the approved plan to ProductionPlanner via state (SCM-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/state to agent | sku_groups + horizon via gateway state; agent must not KeyError |
| agent to ProductionPlanner | approved plan crosses clusters via state['demand_plan'] (no direct invocation) |
| agent to audit.actions | DEMAND_PLAN_DRAFT + DEMAND_PLAN_SIGNOFF rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-18 | Tampering | monthly-orders SQL | mitigate | asyncpg $N params; DATE_TRUNC aggregation; datetime objects |
| T-09-19 | Repudiation | HITL audit correlation | mitigate | Stable plan_id from thread_id; positional AuditRecord (CR-02/CR-04) |
| T-09-20 | Tampering | MAPE overflow into frozen model | mitigate | Per-point clamp + final clamp to <=100 before MapeReport (CR-05/Pitfall 6) |
| T-09-21 | Tampering | cross-cluster plan injection | accept | Plan published via state only after supervisor sign-off; ProductionPlanner re-validates (Phase 10) |
| T-09-SC | Tampering | numpy dependency | accept | numpy already in workspace (sft-ml); RESEARCH Package Legitimacy Audit: approved |
</threat_model>

<verification>
- `pytest apps/agents/supply/demand-forecaster/tests/test_holt_winters.py test_mape.py -x -q` green.
- `pytest apps/agents/supply/demand-forecaster/tests/test_demand_hitl.py -x -q` green.
- demand_plan present in returned state delta for >=2 groups; numpy declared in pyproject.
</verification>

<success_criteria>
DemandForecaster forecasts >=2 SKU groups deterministically, tracks rolling MAPE, and publishes the supervisor-approved plan to ProductionPlanner via state (SCM-04).
</success_criteria>

<output>
Create `.planning/phases/09-agents-supply-chain-economics/09-05-SUMMARY.md` when done.
</output>
