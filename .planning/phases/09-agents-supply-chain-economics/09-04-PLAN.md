---
phase: 09-agents-supply-chain-economics
plan: 04
type: execute
wave: 4
depends_on: ["09-00a", "09-00b", "09-01"]
files_modified:
  - apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/metadata.py
  - apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/models.py
  - apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/oepv.py
  - apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/cost_aggregator.py
  - apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/agent.py
autonomous: true
requirements: [SCM-03, ECO-02, ECO-05]
must_haves:
  truths:
    - "CostAnalyzer aggregates downtime + scrap + energy cost into an ROI breakdown AND produces a PARAMETRIC OEPV ribasso simulation (70% technical / 30% economic, non-linear ribasso curve, sensitivity analysis) with all coefficients supplied via OepvConfig (no hardcoded legal values — F12 boundary) (SCM-03, ECO-02, ECO-05)"
    - "CostAnalyzer is AUTONOMOUS and read-only: it NEVER calls interrupt(), writes audit with Decision.AUTO, and does not write to scm.* (it is the safe fallback agent of the supply subgraph)"
    - "The ribasso anomaly threshold is a CONFIGURABLE warning (is_anomaly_warning), explicitly NOT the definitive Codice Appalti rule (deferred to F12)"
  artifacts:
    - path: "apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/oepv.py"
      provides: "compute_oepv + build_sensitivity_table + OepvConfig (parametric)"
      contains: "OepvConfig"
    - path: "apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/agent.py"
      provides: "CostAnalyzer.__call__ autonomous agent (Decision.AUTO, no HITL)"
      contains: "Decision.AUTO"
  key_links:
    - from: "agent.py"
      to: "audit.actions"
      via: "positional AuditRecord with Decision.AUTO (no interrupt)"
      pattern: "Decision.AUTO"
---

<objective>
Implement CostAnalyzer (SCM-03, ECO-02, ECO-05): the parametric OEPV ribasso simulator (70/30 scoring + non-linear curve + sensitivity), the cost aggregator over audit.actions, frozen models, and the AUTONOMOUS read-only agent (Decision.AUTO, no HITL). This is the supply subgraph's safe fallback.

Purpose: The ROI dashboard + the parametric OEPV simulator that F12 will refine legally. Files disjoint from 09-02/03/05; runs sequentially (worktrees disabled). Wave 4 because the subgraph fallback (09-01) must exist and the other agents land first in the sequential tree, but it only hard-depends on 09-00a/00b/01.
Output: metadata.py, models.py, oepv.py, cost_aggregator.py, agent.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agents-supply-chain-economics/09-CONTEXT.md
@.planning/phases/09-agents-supply-chain-economics/09-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-REVIEW.md

<interfaces>
From sft_agents.models.audit / enums / proposed_action: AuditRecord + AuditWriter.write(record) POSITIONAL (CR-02); Decision.AUTO; ProposedAction.from_payload. CostAnalyzer writes its AUTO row with ActionType.COST_REPORT — the dedicated Phase 9 autonomous cost-report action type added in migration 012 / Plan 09-00a (CostAnalyzer is NOT one of the 7 HITL action types; COST_REPORT is the 8th Phase 9 value, Decision.AUTO). Use ActionType.COST_REPORT explicitly so the 09-07 E2E can assert it deterministically.
compute_oepv / OepvConfig / OepvResult / build_sensitivity_table: 09-RESEARCH.md Pattern 8 (verbatim — parametric, all coefficients in OepvConfig: base_d_asta_eur=108000, weight_technical=0.70, weight_economic=0.30, pe_max=30, lambda_curve, ribasso_ref_pct, anomaly_threshold_pct). math.exp non-linear curve. ValueError on out-of-range inputs.
Cost aggregation: read-only over audit.actions (downtime/scrap/energy cost rows). Analog: apps/agents/maintenance/downtime-analyzer aggregator for the read-only audit query shape; knowledge-curator agent.py for the AUTONOMOUS (Decision.AUTO, no interrupt) __call__ shape.
numpy is available (sft-ml); the curve uses math.exp (no new dep). If pyproject of cost-analyzer needs numpy for vectorized sensitivity, add numpy>=1.26.0,<3.0.0 (RESEARCH Open Question 1) — but math-only is acceptable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OEPV parametric simulator + models + cost aggregator (ECO-02/ECO-05)</name>
  <files>apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/oepv.py, apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/models.py, apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/cost_aggregator.py, apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/metadata.py</files>
  <read_first>
    - apps/agents/supply/cost-analyzer/tests/test_oepv.py (Wave 0 contract — exact expected scores + sensitivity)
    - 09-RESEARCH.md Pattern 8 (compute_oepv + OepvConfig + OepvResult + build_sensitivity_table verbatim)
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/repository.py (read-only audit aggregation analog)
    - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/metadata.py (autonomous agent metadata shape)
  </read_first>
  <behavior>
    - compute_oepv: total_score = weight_technical*pt + weight_economic*pe; pe = pe_max*(1-exp(-lambda_curve*Ri/ribasso_ref_pct)); offer_eur = BA*(1-Ri/100); is_anomaly_warning = Ri >= anomaly_threshold_pct; sensitivity dict for ±1/5/10%; ValueError when ribasso outside [0,100] or pt outside [0, 100*weight_technical].
    - All coefficients come from OepvConfig (no hardcoded literals in the formula path).
    - CostBreakdown + OepvResult + SensitivityTable frozen + extra="forbid".
  </behavior>
  <action>
    In oepv.py implement compute_oepv, build_sensitivity_table, OepvConfig, OepvResult, SensitivityRow exactly per 09-RESEARCH.md Pattern 8 (parametric; math.exp curve; ValueError guards; sensitivity for ±1/5/10%). Add an explicit module docstring noting the anomaly threshold is a CONFIGURABLE warning, NOT the definitive Codice Appalti rule (F12). In models.py define CostBreakdown (downtime_cost_eur, scrap_cost_eur, energy_cost_eur, total_eur, roi fields) and a Pydantic OepvReport wrapping OepvResult + sensitivity, frozen + extra="forbid". In cost_aggregator.py define HistoricalCostAggregator(pool) (NOTE the exact class name — verify the test imports it; avoid the CR-01 import-name-mismatch trap) with ClassVar parameterized SQL reading cost-relevant rows from audit.actions (downtime/scrap/energy) WHERE ts in window, $N params, datetime objects; aggregate into a CostBreakdown. In metadata.py define AGENT_ID="cost-analyzer", CLUSTER="supply", DATA_SOURCES=('audit.actions',), HITL_TIER_DEFAULT=None (autonomous), KPIS_IMPACTED, build_evidence_panel helper.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/supply/cost-analyzer/tests/test_oepv.py -x -q 2>&1 | tail -8 && python -c "from scm_cost_analyzer.oepv import compute_oepv, OepvConfig; r=compute_oepv(10.0, 50.0, OepvConfig()); assert abs(r.total_score-(0.70*50.0+0.30*r.pe))<1e-6; assert set(r.sensitivity.keys())>={'+1%','-1%','+5%','-5%','+10%','-10%'}; print('OEPV OK')"</automated>
  </verify>
  <acceptance_criteria>
    - test_oepv.py passes including the 70/30 weighting, non-linear pe curve, sensitivity ±1/5/10%, and ValueError on out-of-range inputs.
    - All formula coefficients sourced from OepvConfig (grep finds no magic numbers inside compute_oepv besides config attribute reads).
    - cost_aggregator exports HistoricalCostAggregator (name verified against the test import — CR-01).
  </acceptance_criteria>
  <done>Parametric OEPV simulator + cost aggregator + models land with the OEPV test green; anomaly threshold documented as configurable warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CostAnalyzer autonomous agent (Decision.AUTO, no HITL, read-only)</name>
  <files>apps/agents/supply/cost-analyzer/src/scm_cost_analyzer/agent.py</files>
  <read_first>
    - apps/agents/supply/cost-analyzer/tests/test_cost_analyzer_agent.py (Wave 0 contract)
    - apps/agents/knowledge/knowledge-curator/src/trn_knowledge_curator/agent.py (autonomous Decision.AUTO __call__, positional AuditRecord, no interrupt)
    - 08-REVIEW.md CR-01/CR-02
  </read_first>
  <behavior>
    - __call__ NEVER calls interrupt(); aggregates cost via HistoricalCostAggregator; runs compute_oepv with an OepvConfig (defaults or from state); writes exactly one audit row with Decision.AUTO via a positional AuditRecord; performs NO scm.* writes.
    - Returns state delta with the CostBreakdown + OepvReport (model_dump).
  </behavior>
  <action>
    Implement CostAnalyzer(pool, audit_writer, llm=None) with async __call__(state). Read optional OEPV inputs (ribasso_pct, pt, OepvConfig overrides) from state.get with sensible defaults (no KeyError, CR-03). Aggregate cost over the window via HistoricalCostAggregator; compute the OEPV result + sensitivity table. Build a full AuditRecord with Decision.AUTO (decision_actor=None, approval_id=None, tz-aware ts, an appropriate existing cost/report ActionType) and `await self._audit.write(record)` POSITIONAL — no interrupt anywhere in the method. Return {"cost_breakdown": ..., "oepv_report": ...}. Keep the whole path LLM-free (read-only analytics).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/supply/cost-analyzer/tests/test_cost_analyzer_agent.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test_cost_analyzer_agent.py passes: no interrupt called; exactly one audit row with Decision.AUTO; AuditRecord passed positionally; no scm.* write.
    - agent.py imports HistoricalCostAggregator by its exact exported name (no ImportError — CR-01).
  </acceptance_criteria>
  <done>CostAnalyzer runs fully autonomously, emits a Decision.AUTO cost+OEPV audit row, and is the safe fallback agent of the supply subgraph (SCM-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/state to agent | OEPV inputs (ribasso, pt, config) via state; validated by compute_oepv guards |
| agent to audit.actions | read-only cost aggregation + a single Decision.AUTO report row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Tampering | OEPV input ranges | mitigate | compute_oepv ValueError on out-of-range ribasso/pt; coefficients from typed OepvConfig |
| T-09-16 | Repudiation | autonomous cost row | mitigate | Single positional AuditRecord with Decision.AUTO; deterministic LLM-free computation |
| T-09-17 | Elevation of Privilege | OEPV anomaly threshold misread as legal rule | accept | Documented as configurable warning, not Codice Appalti rule; legal precision deferred to F12 |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | numpy optional + already in workspace (sft-ml); RESEARCH audit: no new package |
</threat_model>

<verification>
- `pytest apps/agents/supply/cost-analyzer/tests/test_oepv.py -x -q` green.
- `pytest apps/agents/supply/cost-analyzer/tests/test_cost_analyzer_agent.py -x -q` green.
- No interrupt in agent.py; Decision.AUTO audit row; HistoricalCostAggregator import resolves.
</verification>

<success_criteria>
CostAnalyzer aggregates ROI cost and runs a parametric OEPV ribasso simulation autonomously (Decision.AUTO, no HITL), serving as the supply subgraph fallback (SCM-03, ECO-02, ECO-05).
</success_criteria>

<output>
Create `.planning/phases/09-agents-supply-chain-economics/09-04-SUMMARY.md` when done.
</output>
