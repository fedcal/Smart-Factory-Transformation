---
phase: 09-agents-supply-chain-economics
plan: 03
type: execute
wave: 3
depends_on: ["09-00a", "09-00b", "09-01"]
files_modified:
  - apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/metadata.py
  - apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/models.py
  - apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/enpi.py
  - apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/repository.py
  - apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/agent.py
autonomous: true
requirements: [SCM-02]
must_haves:
  truths:
    - "EnergyOptimizer computes ISO 50001 EnPI (kWh/kg) for dyeing and finishing against the scm.enpi_baseline target, reports deviation + off-peak %, and recommends off-peak scheduling via a HITL-gated proposal (SCM-02)"
    - "HITL ordering is interrupt-then-audit: no audit before resume; on resume exactly one ENERGY_PROPOSAL then one ENERGY_SIGNOFF, correlated by a STABLE proposal_id from thread_id (CR-04); AuditRecord positional (CR-02); approval_id=None pending (CR-03)"
    - "compute_enpi handles missing kg_processed slots safely and never produces an EnPI that breaks a frozen model constraint (clamp discipline, CR-05)"
  artifacts:
    - path: "apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/enpi.py"
      provides: "compute_enpi pure function + EnpiReport"
      contains: "enpi_actual"
    - path: "apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/agent.py"
      provides: "EnergyOptimizer.__call__ HITL agent"
      contains: "interrupt"
  key_links:
    - from: "agent.py"
      to: "audit.actions"
      via: "positional AuditRecord written after interrupt() returns"
      pattern: "self._audit.write\\(record\\)"
---

<objective>
Implement EnergyOptimizer (SCM-02): frozen models, ISO 50001 EnPI computation (pure function), asyncpg repository over scm.energy_readings + scm.enpi_baseline, and the HITL agent that proposes off-peak scheduling — interrupt-then-audit, stable ids.

Purpose: Second supply HITL agent, reusing the InventoryManager HITL pattern. Files disjoint from 09-02/04/05; runs sequentially (worktrees disabled).
Output: metadata.py, models.py, enpi.py, repository.py, agent.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agents-supply-chain-economics/09-CONTEXT.md
@.planning/phases/09-agents-supply-chain-economics/09-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-REVIEW.md

<interfaces>
From sft_agents.models.audit / enums / proposed_action: AuditRecord + AuditWriter.write(record) POSITIONAL (CR-02); ActionType.ENERGY_PROPOSAL / ENERGY_SIGNOFF (from 09-00a); Decision.HITL_SUPERVISOR; ProposedAction.from_payload.
HITL interrupt: Pattern G shim (NotImplementedError in tests, never MagicMock — WR-01).
Stable id: sha256(f"{AGENT_ID}.{state.get('thread_id','unknown')}")[:32] (Pattern 3).
asyncpg query: 09-RESEARCH.md "Query aggregazione EnPI per periodo" — fetch_energy_readings(process, ts_from, ts_to) passing datetime objects (never .isoformat(), Pitfall 7), $N params only.
compute_enpi: 09-RESEARCH.md Pattern 7 (verbatim — filters slots with kg>0, raises ValueError if none, deviation_pct, off_peak_kwh_pct).
HITL agent shape: reuse 09-02 InventoryManager.agent.py once it lands; analog apps/agents/knowledge/shift-handover agent.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: models + compute_enpi pure function + repository + metadata</name>
  <files>apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/enpi.py, apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/models.py, apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/repository.py, apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/metadata.py</files>
  <read_first>
    - apps/agents/supply/energy-optimizer/tests/test_enpi.py (Wave 0 contract — exact expected values)
    - 09-RESEARCH.md Pattern 7 (compute_enpi + EnpiReport verbatim) and "Query aggregazione EnPI per periodo"
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/repository.py (ClassVar SQL + datetime params)
    - apps/agents/knowledge/shift-handover/src/trn_shift_handover/metadata.py (metadata shape)
  </read_first>
  <behavior>
    - compute_enpi: enpi_actual = sum(kwh)/sum(kg) over slots with kg>0; deviation_pct = (actual-baseline)/baseline*100; is_above_baseline boolean; off_peak_kwh_pct from is_peak_hour flags; raises ValueError when no kg>0 slot.
    - OffPeakProposal + EnpiReport frozen + extra="forbid"; proposal_id is a str field set by the agent.
    - repository passes datetime objects to asyncpg; SQL $N only.
  </behavior>
  <action>
    In enpi.py implement compute_enpi + EnpiReport exactly per 09-RESEARCH.md Pattern 7 (frozen dataclass; deterministic; ValueError on no valid kg slot). In models.py define EnergyReading, EnpiReport (Pydantic frozen mirror if separate from the dataclass), and OffPeakProposal frozen + extra="forbid" with proposal_id str + recommended off-peak window + expected savings. In repository.py define EnergyRepository(pool) with ClassVar _SQL_READINGS selecting ts, asset_id, kwh, kg_processed, is_peak_hour FROM scm.energy_readings WHERE process=$1 AND ts >= $2 AND ts < $3 ORDER BY ts; fetch_energy_readings(process, ts_from, ts_to) passing datetime objects directly; add fetch_baseline(process) reading scm.enpi_baseline. In metadata.py define AGENT_ID="energy-optimizer", CLUSTER="supply", DATA_SOURCES=('scm.energy_readings','scm.enpi_baseline'), HITL_TIER_DEFAULT="supervisor", KPIS_IMPACTED, and build_evidence_panel helper.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/supply/energy-optimizer/tests/test_enpi.py -x -q 2>&1 | tail -8 && python -c "from scm_energy_optimizer.metadata import AGENT_ID; from scm_energy_optimizer.models import OffPeakProposal; assert AGENT_ID=='energy-optimizer'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - test_enpi.py passes with exact expected values; ValueError raised when no kg>0 slot.
    - repository SQL uses only $N placeholders; datetime params passed as objects (grep finds no .isoformat()).
    - DATA_SOURCES lists only scm.energy_readings + scm.enpi_baseline.
  </acceptance_criteria>
  <done>EnPI computation, models, repository and metadata land with the pure-function test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EnergyOptimizer HITL agent (interrupt-then-audit, stable id)</name>
  <files>apps/agents/supply/energy-optimizer/src/scm_energy_optimizer/agent.py</files>
  <read_first>
    - apps/agents/supply/energy-optimizer/tests/test_energy_hitl.py (Wave 0 HITL contract)
    - apps/agents/supply/inventory-manager/src/scm_inventory_manager/agent.py (the just-landed HITL template to mirror) and 08-REVIEW.md CR-02/CR-03/CR-04
    - 09-RESEARCH.md Patterns 2/3/4
  </read_first>
  <behavior>
    - Compute EnPI for the requested process (dyeing/finishing) over the window; derive stable proposal_id from thread_id; interrupt with the proposal; NO audit before resume.
    - On resume: exactly one ENERGY_PROPOSAL then one ENERGY_SIGNOFF row, both carrying proposal_id; approval_id None pending; AuditRecord positional.
    - The LLM (if used) only writes a short rationale string — never makes the decision (LLM-free core).
  </behavior>
  <action>
    Implement EnergyOptimizer(pool, audit_writer, llm=None) with async __call__(state) mirroring InventoryManager: read process + window (ts_from/ts_to) via state.get with defaults (no bare KeyError, CR-03); fetch readings + baseline; compute_enpi; derive proposal_id = sha256(f"{AGENT_ID}.{state.get('thread_id','unknown')}")[:32] (CR-04); build OffPeakProposal recommending shifting peak-hour consumption to off-peak; interrupt({"proposal": ..., "agent_id": AGENT_ID}); ONLY after resume build a full AuditRecord (ENERGY_PROPOSAL, Decision.HITL_SUPERVISOR, approval_id=None, tz-aware ts) and `await self._audit.write(record)` POSITIONAL, then an ENERGY_SIGNOFF record with decision_actor from the resume payload; return {"energy_proposal": proposal.model_dump()}. Clamp any ratio before model construction (CR-05).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/supply/energy-optimizer/tests/test_energy_hitl.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test_energy_hitl.py passes: 0 audit writes before resume; exactly 1 ENERGY_PROPOSAL + 1 ENERGY_SIGNOFF; stable proposal_id across replay; approval_id None pending; positional AuditRecord.
    - grep confirms no inline uuid4 correlation id; state access via .get() with defaults.
  </acceptance_criteria>
  <done>EnergyOptimizer proposes off-peak scheduling via supervisor HITL with replay-safe audit correlation (SCM-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/state to agent | process + time window via gateway state; agent must not KeyError on missing fields |
| agent to audit.actions | append-only ENERGY_PROPOSAL + ENERGY_SIGNOFF rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Tampering | energy-readings window SQL | mitigate | asyncpg $N params; datetime objects; tz-aware window enforced at gateway (09-06) |
| T-09-13 | Repudiation | HITL audit correlation | mitigate | Stable proposal_id from thread_id; positional AuditRecord (CR-02/CR-04) |
| T-09-14 | Tampering | EnPI overflow into frozen model | mitigate | compute_enpi guards kg>0 + clamp before model build (CR-05) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 9 |
</threat_model>

<verification>
- `pytest apps/agents/supply/energy-optimizer/tests/test_enpi.py -x -q` green.
- `pytest apps/agents/supply/energy-optimizer/tests/test_energy_hitl.py -x -q` green.
</verification>

<success_criteria>
EnergyOptimizer computes ISO 50001 EnPI and routes off-peak proposals to supervisor HITL with correct audit correlation (SCM-02).
</success_criteria>

<output>
Create `.planning/phases/09-agents-supply-chain-economics/09-03-SUMMARY.md` when done.
</output>
