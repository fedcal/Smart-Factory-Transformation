---
phase: 09-agents-supply-chain-economics
plan: 06
type: execute
wave: 6
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05"]
files_modified:
  - apps/api-gateway/src/svc_api_gateway/routers/supply_agents.py
  - apps/api-gateway/src/svc_api_gateway/dependencies.py
  - apps/api-gateway/src/svc_api_gateway/lifespan.py
  - apps/api-gateway/src/svc_api_gateway/main.py
  - apps/api-gateway/tests/test_supply_agents_router.py
autonomous: true
requirements: [SCM-01, SCM-02, SCM-03, SCM-04, SEC-02]
must_haves:
  truths:
    - "Five supply endpoints exist mirroring knowledge_agents.py: inventory-manager/check (+resume), energy-optimizer/optimize (+resume), cost-analyzer/analyze (200 autonomous, no resume), demand-forecaster/forecast (+resume) — each sets state['target_agent'] and invokes the supervisor graph wired via build_supply_subgraph"
    - "Request models are frozen + extra=forbid with tz-aware @field_validator on datetime fields (WR-02), user_roles ACL field (WR-03); the 500 handler returns a generic body and never leaks str(exc) (WR-05)"
    - "lifespan imports each agent class by its EXACT exported name (CR-01) and wires app.state.supply_children including the cost-analyzer fallback into build_supply_subgraph"
  artifacts:
    - path: "apps/api-gateway/src/svc_api_gateway/routers/supply_agents.py"
      provides: "Supply cluster HTTP router"
      contains: "/cost-analyzer/analyze"
  key_links:
    - from: "lifespan.py"
      to: "build_supply_subgraph"
      via: "supply_children dict injected into the supervisor graph"
      pattern: "supply_children"
---

<objective>
Expose the four supply agents via a dedicated supply_agents.py FastAPI router, wire the agent callables into build_supply_subgraph through lifespan DI, and register the router in build_app. Mirror Phase 8 knowledge_agents.py / 08-08 and apply every Phase 8 review fix at the boundary (tz validators, user_roles, generic 500, exact import names).

Purpose: HTTP surface for the supply cluster, including the autonomous CostAnalyzer endpoint and the three HITL endpoints with their resume paths. Worktrees disabled — runs after all agents (09-02..05) on the sequential tree.
Output: supply_agents.py router, dependencies + lifespan + main wiring, router tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agents-supply-chain-economics/09-CONTEXT.md
@.planning/phases/09-agents-supply-chain-economics/09-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-REVIEW.md

<interfaces>
From 09-01: build_supply_subgraph(child_callables) requiring a cost-analyzer key.
Agent callables (09-02/03/04/05): InventoryManager, EnergyOptimizer, CostAnalyzer (exact name HistoricalCostAggregator dependency lives in cost_aggregator), DemandForecaster — each __call__(state) -> dict. VERIFY each class's exact exported name before importing (CR-01).
Gateway helpers (existing): get_supervisor_graph, get_idempotency_cache, check_idempotency_cache, store_idempotent_response, jsonable, build_invocation_config, _require_state, _handle_recursion_error.
Router analog: apps/api-gateway/src/svc_api_gateway/routers/knowledge_agents.py (verbatim — apply CR-03/WR-02/WR-03/WR-05 fixes that already landed there).
DI analog: dependencies.py get_knowledge_children; lifespan.py knowledge agent construction + subgraph wiring; main.py include_router.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: supply_agents.py router + request models (WR-02/WR-03/WR-05)</name>
  <files>apps/api-gateway/src/svc_api_gateway/routers/supply_agents.py</files>
  <read_first>
    - apps/api-gateway/src/svc_api_gateway/routers/knowledge_agents.py (verbatim router/endpoint/error-helper analog WITH the CR-03/WR-02/WR-03/WR-05 fixes applied)
    - 09-RESEARCH.md Architecture diagram (endpoint set) + Pitfalls 4 (tz validator) and 5 (generic 500 body)
  </read_first>
  <action>
    Create supply_agents.py mirroring knowledge_agents.py: APIRouter(prefix="/v1/agents", tags=["supply-agents"]). Define frozen request models (ConfigDict(frozen=True, extra="forbid")), EACH with a `user_roles: list[str] = Field(default_factory=list)` ACL field (WR-03) and a tz-aware `@field_validator` on every datetime field rejecting naive datetimes (WR-02): InventoryCheckRequest (sku_ids optional list + user_roles); InventoryResumeRequest (thread_id + decision + decision_actor); EnergyOptimizeRequest (process + ts_from + ts_to tz-aware + user_roles) + EnergyResumeRequest; CostAnalyzeRequest (ribasso_pct + pt + optional OEPV config overrides + ts_from/ts_to tz-aware + user_roles); DemandForecastRequest (sku_groups + horizon + months_back + user_roles) + DemandResumeRequest. Implement endpoints setting state["target_agent"] to the slug and invoking supervisor_graph.ainvoke with build_invocation_config + idempotency: POST /inventory-manager/check (202) + /inventory-manager/resume (200); POST /energy-optimizer/optimize (202) + /energy-optimizer/resume (200); POST /cost-analyzer/analyze (200, autonomous synchronous, NO resume endpoint); POST /demand-forecaster/forecast (202) + /demand-forecaster/resume (200, publishes plan to ProductionPlanner via the returned state). Copy _handle_recursion_error verbatim and add a _handle_agent_error that logs str(exc) server-side only and returns a GENERIC body {"error":"internal_agent_error","thread_id":...} (WR-05 — never str(exc) in the body). Log key prefix supply_agent_.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -c "from svc_api_gateway.routers import supply_agents as r; paths={route.path for route in r.router.routes}; need={'/v1/agents/inventory-manager/check','/v1/agents/energy-optimizer/optimize','/v1/agents/cost-analyzer/analyze','/v1/agents/demand-forecaster/forecast'}; missing=need-paths; assert not missing, missing; src=open('apps/api-gateway/src/svc_api_gateway/routers/supply_agents.py').read(); assert 'user_roles' in src and 'tzinfo' in src; assert 'str(exc)' not in src.split('JSONResponse')[1] if 'JSONResponse' in src else True; print('router OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All four core endpoints registered (plus resume endpoints for the 3 HITL agents); cost-analyzer has NO resume endpoint.
    - Every request model is frozen + extra=forbid, has user_roles, and tz-aware validators on datetime fields.
    - The 500 handler returns a generic body (no str(exc) leak — WR-05).
  </acceptance_criteria>
  <done>Supply router exposes the agent endpoints with the Phase 8 boundary fixes applied.</done>
</task>

<task type="auto">
  <name>Task 2: DI wiring (dependencies + lifespan + main) + router test (CR-01)</name>
  <files>apps/api-gateway/src/svc_api_gateway/dependencies.py, apps/api-gateway/src/svc_api_gateway/lifespan.py, apps/api-gateway/src/svc_api_gateway/main.py, apps/api-gateway/tests/test_supply_agents_router.py</files>
  <read_first>
    - apps/api-gateway/src/svc_api_gateway/dependencies.py (get_knowledge_children analog)
    - apps/api-gateway/src/svc_api_gateway/lifespan.py (knowledge agent construction + build_knowledge_subgraph wiring) — and 08-REVIEW.md CR-01 (import the EXACT exported class name)
    - apps/api-gateway/src/svc_api_gateway/main.py (include_router for knowledge_agents)
    - the four supply agent packages' __init__.py / module __all__ to confirm exact class names before importing
  </read_first>
  <action>
    Add get_supply_children(request) returning app.state.supply_children (or 503) mirroring get_knowledge_children. In lifespan.py construct the 4 supply agents (InventoryManager, EnergyOptimizer, CostAnalyzer, DemandForecaster) injecting pool, audit_writer, optional llm, and saver as each requires — importing each class by its EXACT exported name verified against its package __all__ (CR-01, avoid the EventAggregator-style mismatch). Assemble app.state.supply_children dict keyed by slug (MUST include cost-analyzer, the fallback) and wire build_supply_subgraph into the supervisor graph alongside the existing cluster subgraphs. In main.py add include_router(supply_agents_router.router) after the knowledge router include. Create test_supply_agents_router.py using the FastAPI TestClient with a stubbed supervisor graph (AsyncMock ainvoke) asserting: each endpoint returns its expected status; cost-analyzer/analyze needs no resume; a naive datetime on energy/cost requests returns 422 (not 500) (WR-02); a forced agent exception returns a generic 500 body without str(exc) (WR-05).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/api-gateway/tests/test_supply_agents_router.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Router test passes for all endpoints with a stubbed graph, including the 422-on-naive-datetime and generic-500-body assertions.
    - get_supply_children present; lifespan builds supply_children including cost-analyzer and wires build_supply_subgraph; imports resolve (no ImportError at boot — CR-01).
    - main.py includes the supply router.
  </acceptance_criteria>
  <done>The four supply agents are reachable over HTTP and wired through build_supply_subgraph via lifespan DI, with the Phase 8 boot/boundary bugs guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client to gateway | request bodies cross into agent invocation |
| lifespan import to agent modules | wrong class name crashes boot (CR-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-22 | Tampering | request payloads | mitigate | Frozen + extra=forbid models; tz-aware validators reject naive datetimes (422 not 500, WR-02) |
| T-09-23 | Information Disclosure | 500 error body | mitigate | Generic body {"error":"internal_agent_error"}; str(exc) only server-side log (WR-05) |
| T-09-24 | Denial of Service | unbounded recursion | mitigate | build_invocation_config recursion_limit; _handle_recursion_error |
| T-09-25 | Spoofing/EoP | endpoint auth + ACL | accept | Dev-mode user_roles in body propagated to state; JWT/RBAC enforcement deferred to Phase 11 (WR-03 keeps the field present) |
| T-09-SC | Supply-Chain | SEC-02 OWASP LLM supply-chain | mitigate | Supply core logic is LLM-free (Inventory/Cost/Demand) or LLM-rationale-only (Energy); OEPV params from config, no external model dependency |
</threat_model>

<verification>
- `python -m pytest apps/api-gateway/tests/test_supply_agents_router.py -x -q` green.
- All endpoints registered; lifespan wires build_supply_subgraph; tz/generic-500 boundary fixes verified.
</verification>

<success_criteria>
The supply cluster is exposed via a dedicated router and wired through build_supply_subgraph, with the Phase 8 review lessons (CR-01, WR-02, WR-03, WR-05) applied at the HTTP boundary.
</success_criteria>

<output>
Create `.planning/phases/09-agents-supply-chain-economics/09-06-SUMMARY.md` when done.
</output>
