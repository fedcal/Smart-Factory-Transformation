---
phase: 09-agents-supply-chain-economics
plan: 01
type: execute
wave: 2
depends_on: ["09-00a"]
files_modified:
  - packages/sft-agents/src/sft_agents/runtime/clusters.py
  - packages/sft-agents/tests/test_build_supply_subgraph.py
  - infra/migrations/timescale/seed/scm_mantis_seed.sql
  - infra/migrations/timescale/tests/test_scm_mantis_seed.py
autonomous: true
requirements: [SCM-01, SCM-02, SCM-04, SCM-05]
must_haves:
  truths:
    - "build_supply_subgraph mirrors build_knowledge_subgraph: conditional START→_route→[slug]→END, fallback to 'cost-analyzer' (autonomous/read-only), ValueError when the fallback slug is absent or child_callables empty, structlog 'scm_route_unknown_target' on unknown target"
    - "The Mantis synthetic seed populates all 5 scm.* tables with realistic textile-SME values, including >=18 months of monthly orders for >=2 SKU groups (jersey, twill) and ISO 50001 EnPI baselines for dyeing+finishing"
    - "The seed file is NON-numbered (not a numbered migration) so numbered migrations stay idempotent; the seed is dev/test-only and explicitly labeled synthetic"
  artifacts:
    - path: "packages/sft-agents/src/sft_agents/runtime/clusters.py"
      provides: "build_supply_subgraph + _SCM_DEFAULT_AGENT constant"
      contains: "_SCM_DEFAULT_AGENT"
    - path: "infra/migrations/timescale/seed/scm_mantis_seed.sql"
      provides: "Mantis synthetic dataset for all 5 scm.* tables"
      contains: "SYNTHETIC"
  key_links:
    - from: "clusters.py build_supply_subgraph"
      to: "cost-analyzer fallback"
      via: "_SCM_DEFAULT_AGENT membership check (ValueError if absent)"
      pattern: "_SCM_DEFAULT_AGENT"
---

<objective>
Wave 2 foundation: add `build_supply_subgraph` to clusters.py (mirroring `build_knowledge_subgraph`, fallback `cost-analyzer`) and create the Mantis synthetic seed dataset for the scm.* schema. These are the routing backbone + queryable data the four agents consume.

Purpose: The supply cluster router (used by the gateway in 09-06) and the realistic synthetic data feeding reorder/EnPI/forecast logic and the SCM-05 documentation. Worktrees disabled — runs sequentially after 09-00a (needs scm.* schema to seed).
Output: build_supply_subgraph + its unit test, scm_mantis_seed.sql + its seed smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agents-supply-chain-economics/09-CONTEXT.md
@.planning/phases/09-agents-supply-chain-economics/09-RESEARCH.md

<interfaces>
From clusters.py (existing): build_knowledge_subgraph(child_callables) with _KNW_DEFAULT_AGENT="knowledge-curator", StateGraph(AgentState), add_conditional_edges(START, _route, {slug:slug}), _log warning on unknown target. Mirror exactly for supply with _SCM_DEFAULT_AGENT="cost-analyzer".
scm.* tables created by 09-00a migration 011: sku_master, inventory_levels, energy_readings, historical_orders, enpi_baseline (see 09-RESEARCH.md Pattern 5 for columns).
Mantis synthetic values: 09-RESEARCH.md Pattern 11 (SKU master, EnPI baseline dyeing 3.80/4.12 + finishing 2.20/2.18, capacity, 18-month orders jersey ~12000kg/mo +35% summer/+20% winter, twill ~8000kg/mo CV~12%).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_supply_subgraph (mirror build_knowledge_subgraph)</name>
  <files>packages/sft-agents/src/sft_agents/runtime/clusters.py, packages/sft-agents/tests/test_build_supply_subgraph.py</files>
  <read_first>
    - packages/sft-agents/src/sft_agents/runtime/clusters.py (build_knowledge_subgraph verbatim, lines ~264-331, and the _KNW_DEFAULT_AGENT constant pattern)
    - packages/sft-agents/tests/ existing test for build_knowledge_subgraph (routing + fallback + ValueError tests)
    - 09-RESEARCH.md Pattern 1 (verbatim build_supply_subgraph + _SCM_DEFAULT_AGENT="cost-analyzer")
  </read_first>
  <action>
    In clusters.py add `_SCM_DEFAULT_AGENT: str = "cost-analyzer"` next to the other cluster default constants, then add `build_supply_subgraph(child_callables)` mirroring build_knowledge_subgraph byte-for-byte except the constant and the warning event name `scm_route_unknown_target`: raise ValueError if child_callables empty; raise ValueError if _SCM_DEFAULT_AGENT not in child_callables (message lists provided slugs); build StateGraph(AgentState), add each slug as a node, define `_route(state)` returning the target if present in children else _SCM_DEFAULT_AGENT (with _log.warning), add_conditional_edges(START, _route, {slug: slug}), add_edge(slug, END) for each, return the uncompiled graph. Then create test_build_supply_subgraph.py mirroring the knowledge subgraph test: (a) routes to the requested slug when present, (b) falls back to cost-analyzer on unknown/missing target_agent, (c) raises ValueError when cost-analyzer absent, (d) raises ValueError on empty child_callables.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -c "from sft_agents.runtime.clusters import build_supply_subgraph, _SCM_DEFAULT_AGENT; assert _SCM_DEFAULT_AGENT=='cost-analyzer'; import pytest; 
try:
    build_supply_subgraph({})
    raise SystemExit('expected ValueError on empty')
except ValueError: pass
try:
    build_supply_subgraph({'inventory-manager': (lambda s: s)})
    raise SystemExit('expected ValueError missing fallback')
except ValueError: pass
print('subgraph guards OK')" && python -m pytest packages/sft-agents/tests/test_build_supply_subgraph.py -x -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - build_supply_subgraph importable; _SCM_DEFAULT_AGENT == "cost-analyzer".
    - ValueError on empty child_callables and on missing cost-analyzer fallback.
    - Test asserts routing to a present slug and fallback to cost-analyzer on unknown target.
  </acceptance_criteria>
  <done>The supply cluster router exists, mirrors the knowledge subgraph, and falls back to the autonomous cost-analyzer.</done>
</task>

<task type="auto">
  <name>Task 2: Mantis synthetic seed dataset + seed smoke test (SCM-05 data side)</name>
  <files>infra/migrations/timescale/seed/scm_mantis_seed.sql, infra/migrations/timescale/tests/test_scm_mantis_seed.py</files>
  <read_first>
    - infra/migrations/timescale/011_create_scm_schema.sql (the exact scm.* columns to insert into)
    - 09-RESEARCH.md Pattern 11 (Mantis synthetic SKU master, EnPI baselines, capacity, 18-month order series) and Open Question 3 (seed = non-numbered file, dev/test only)
    - infra/migrations/timescale/tests/test_migration_011.py (testcontainers fixture to reuse for the seed smoke test)
  </read_first>
  <action>
    Create infra/migrations/timescale/seed/scm_mantis_seed.sql (NON-numbered — must NOT match the `[0-9][0-9][0-9]_*.sql` migration glob, so numbered migrations stay idempotent). Top-of-file comment in BOTH IT and EN stating the data is SYNTHETIC, generated for Mantis Textile Group demonstration, contains no real company data. Use INSERT ... ON CONFLICT DO NOTHING for re-run safety. Seed: (1) scm.sku_master with the 6 SKUs from Pattern 11 (SKU-YARN-NE20-BLU, SKU-YARN-NE30-BIA, SKU-DYE-REACT-BLU, SKU-SPARE-NEEDLE-L, SKU-FAB-JERSEY-BLU, SKU-FAB-TWILL-GRY) with their reorder_point/reorder_qty/lead_time/unit_cost and sku_group ('jersey'/'twill' for the fabrics); (2) scm.enpi_baseline rows for dyeing (target 3.80, ytd 4.12) and finishing (target 2.20, ytd 2.18), baseline_year 2024; (3) scm.inventory_levels recent rows including at least one fabric/yarn SKU currently BELOW its reorder_point (so InventoryManager has a live reorder case) and others above; (4) scm.energy_readings spanning dyeing+finishing with kwh + kg_processed + is_peak_hour mix (enough rows to compute a meaningful EnPI and off-peak %); (5) scm.historical_orders with >=18 months of monthly orders for sku_group 'jersey' (~12000 kg/mo, +35% summer, +20% winter seasonality) and 'twill' (~8000 kg/mo, low CV) using generate_series for month timestamps. Then create test_scm_mantis_seed.py (integration marker, testcontainers timescale/timescaledb:2.18.0-pg16): apply migrations through 012 then the seed file; assert each scm.* table is non-empty, >=2 distinct sku_group values in historical_orders, >=18 monthly buckets per group, at least one inventory row below its SKU reorder_point, and both EnPI baseline processes present. Re-applying the seed is a no-op (ON CONFLICT).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -c "from pathlib import Path; s=Path('infra/migrations/timescale/seed/scm_mantis_seed.sql').read_text(); up=s.upper(); assert 'SYNTHETIC' in up; req=['scm.sku_master','scm.enpi_baseline','scm.inventory_levels','scm.energy_readings','scm.historical_orders','ON CONFLICT']; missing=[r for r in req if r not in s]; assert not missing, missing; assert 'jersey' in s and 'twill' in s; import re; assert not re.match(r'.*[0-9]{3}_.*', Path('infra/migrations/timescale/seed/scm_mantis_seed.sql').name), 'seed must be non-numbered'; print('seed OK')" && python -m pytest infra/migrations/timescale/tests/test_scm_mantis_seed.py --co -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - Seed file is in seed/ (non-numbered), labeled SYNTHETIC (IT+EN), uses ON CONFLICT DO NOTHING.
    - All 5 scm.* tables seeded; jersey + twill groups present with >=18 monthly buckets each; >=1 inventory row below reorder_point; dyeing + finishing EnPI baselines present.
    - Seed smoke test collects; with Docker is green and re-runnable.
  </acceptance_criteria>
  <done>The Mantis synthetic dataset seeds all scm.* tables with realistic, clearly-labeled-synthetic values supporting reorder, EnPI, and forecast scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supervisor graph to supply subgraph | target_agent routing; untrusted target falls back to safe autonomous agent |
| seed runner to DB | dev/test-only synthetic INSERTs into scm.*; never run in prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | unknown target routing | mitigate | _route falls back to cost-analyzer (read-only, no irreversible side effect); structlog warning emitted |
| T-09-07 | Denial of Service | missing fallback agent | mitigate | ValueError at graph build time if cost-analyzer absent (fail fast, not at request time) |
| T-09-08 | Information Disclosure | synthetic data mislabeled as real | mitigate | Seed header (IT+EN) + SCM-05 docs explicitly label all values synthetic |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 9 |
</threat_model>

<verification>
- `pytest packages/sft-agents/tests/test_build_supply_subgraph.py -x -q` green.
- `pytest infra/migrations/timescale/tests/test_scm_mantis_seed.py --co -q` collects; integration run green with Docker.
- Seed is non-numbered and labeled SYNTHETIC.
</verification>

<success_criteria>
The supply cluster has a routing subgraph mirroring the knowledge cluster and a realistic, clearly-synthetic Mantis dataset seeded across all scm.* tables.
</success_criteria>

<output>
Create `.planning/phases/09-agents-supply-chain-economics/09-01-SUMMARY.md` when done.
</output>
