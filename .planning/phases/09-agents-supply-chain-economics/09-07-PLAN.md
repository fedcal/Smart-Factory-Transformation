---
phase: 09-agents-supply-chain-economics
plan: 07
type: execute
wave: 7
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06"]
files_modified:
  - apps/api-gateway/tests/test_supply_cluster_e2e.py
autonomous: true
requirements: [SCM-01, SCM-02, SCM-03, SCM-04, SCM-05]
must_haves:
  truths:
    - "An E2E test exercises all four supply agents end-to-end against testcontainers + the Mantis synthetic seed: InventoryManager reorder→draft→signoff, EnergyOptimizer EnPI→proposal→signoff, CostAnalyzer autonomous ROI+OEPV (Decision.AUTO), DemandForecaster forecast→signoff→plan-in-state-for-ProductionPlanner"
    - "Per-agent audit-row counts are asserted: exactly 1 DRAFT + 1 SIGNOFF for each HITL agent, exactly 1 Decision.AUTO row for CostAnalyzer, all correlated by a stable id; no double-write on replay"
    - "The DemandForecaster approved plan covers >=2 SKU groups and appears in the state delta routed to ProductionPlanner"
  artifacts:
    - path: "apps/api-gateway/tests/test_supply_cluster_e2e.py"
      provides: "Four-agent end-to-end + per-agent audit-row + OEPV + cross-cluster plan assertions"
      contains: "DEMAND_PLAN_SIGNOFF"
  key_links:
    - from: "test_supply_cluster_e2e.py"
      to: "scm_mantis_seed.sql"
      via: "testcontainers applies migrations 011/012 + seed before exercising agents"
      pattern: "scm_mantis_seed"
---

<objective>
Close the Phase 9 implementation gate: an end-to-end test across all four supply agents against the Mantis synthetic seed, asserting per-agent audit-row counts (no double-write), the OEPV simulation, and the cross-cluster demand-plan publish to ProductionPlanner.

Purpose: The phase's E2E acceptance gate; proves the four ROADMAP success criteria 1-4 hold against realistic synthetic data. Worktrees disabled — runs after the router (09-06) on the sequential tree.
Output: test_supply_cluster_e2e.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agents-supply-chain-economics/09-CONTEXT.md
@.planning/phases/09-agents-supply-chain-economics/09-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
E2E analog: apps/api-gateway/tests/test_knowledge_cluster_e2e.py (testcontainers timescale/timescaledb:2.18.0-pg16 + mock LLM LLM_BACKEND=mock + migrations applied + supervisor graph wiring).
Supply endpoints from 09-06; build_supply_subgraph from 09-01; Mantis seed from 09-01 (scm_mantis_seed.sql).
Audit action types from 09-00a: REORDER_ALERT, PURCHASE_RECOMMENDATION_DRAFT, PURCHASE_SIGNOFF, ENERGY_PROPOSAL, ENERGY_SIGNOFF, DEMAND_PLAN_DRAFT, DEMAND_PLAN_SIGNOFF.
ROADMAP Phase 9 success criteria 1-4 are the behaviors to exercise.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-agent supply cluster E2E against Mantis seed</name>
  <files>apps/api-gateway/tests/test_supply_cluster_e2e.py</files>
  <read_first>
    - apps/api-gateway/tests/test_knowledge_cluster_e2e.py (testcontainers + migrations + mock LLM wiring + supervisor graph fixture to mirror)
    - 09-RESEARCH.md Validation Architecture (full suite command, sampling, integration marker) + the Test Map rows
    - 09-CONTEXT.md (all locked behaviors the E2E must exercise) and ROADMAP Phase 9 success criteria 1-4
    - infra/migrations/timescale/seed/scm_mantis_seed.sql (the seeded reorder/EnPI/order scenarios to assert against)
  </read_first>
  <action>
    Create an E2E test (marked integration) bringing up the gateway against testcontainers PostgreSQL (timescale/timescaledb:2.18.0-pg16) with migrations 001-012 applied AND scm_mantis_seed.sql loaded, LLM_BACKEND=mock, and the supply agents wired via build_supply_subgraph. Exercise each agent and assert: (1) InventoryManager check on the seeded below-reorder SKU fires a REORDER_ALERT, then simulate the supervisor resume and assert exactly 1 PURCHASE_RECOMMENDATION_DRAFT + 1 PURCHASE_SIGNOFF row sharing one stable recommendation_id (no double-write on a simulated replay); (2) EnergyOptimizer optimize for dyeing over the seeded window returns an EnPI above baseline (the seeded 4.12 vs 3.80) and after resume writes exactly 1 ENERGY_PROPOSAL + 1 ENERGY_SIGNOFF; (3) CostAnalyzer analyze returns a CostBreakdown + an OepvResult (70/30 score, sensitivity present, offer_eur from BA 108000) and writes exactly 1 Decision.AUTO audit row with NO interrupt/resume; (4) DemandForecaster forecast over the jersey+twill seed produces a demand plan for >=2 SKU groups with a rolling MAPE <=100, and after resume writes exactly 1 DEMAND_PLAN_DRAFT + 1 DEMAND_PLAN_SIGNOFF and the approved plan is present in the response state delta under the ProductionPlanner publish key. Use the mock LLM backend (no live LLM).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/api-gateway/tests/test_supply_cluster_e2e.py --co -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - E2E collects; with Docker the four-agent flow passes including the per-agent audit-row counts and the OEPV + multi-group plan assertions.
    - CostAnalyzer path asserts Decision.AUTO and no interrupt; HITL agents assert single DRAFT + single SIGNOFF with stable correlation id.
    - mock LLM backend is used (no live LLM dependency).
  </acceptance_criteria>
  <done>End-to-end coverage of all four supply agents against the Mantis seed, with per-agent audit correctness and cross-cluster plan publish asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness to DB | E2E applies migrations 001-012 + Mantis seed via testcontainers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-26 | Repudiation | per-agent audit completeness | mitigate | E2E asserts exact DRAFT/SIGNOFF/AUTO row counts per agent and stable correlation ids |
| T-09-27 | Tampering | replay double-write | mitigate | E2E simulates a resume/replay and asserts no duplicate audit rows |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 9 |
</threat_model>

<verification>
- `python -m pytest apps/api-gateway/tests/test_supply_cluster_e2e.py --co -q` collects; full integration run green with Docker.
</verification>

<success_criteria>
ROADMAP Phase 9 success criteria 1-4 are exercised end-to-end against realistic synthetic data with correct, replay-safe audit behavior.
</success_criteria>

<output>
Create `.planning/phases/09-agents-supply-chain-economics/09-07-SUMMARY.md` when done.
</output>
