---
phase: 09-agents-supply-chain-economics
plan: 02
type: execute
wave: 3
depends_on: ["09-00a", "09-00b", "09-01"]
files_modified:
  - apps/agents/supply/inventory-manager/src/scm_inventory_manager/metadata.py
  - apps/agents/supply/inventory-manager/src/scm_inventory_manager/models.py
  - apps/agents/supply/inventory-manager/src/scm_inventory_manager/reorder.py
  - apps/agents/supply/inventory-manager/src/scm_inventory_manager/repository.py
  - apps/agents/supply/inventory-manager/src/scm_inventory_manager/agent.py
autonomous: true
requirements: [SCM-01]
must_haves:
  truths:
    - "InventoryManager fires a reorder alert when a SKU's latest quantity < reorder_point, builds a purchase-recommendation draft, and routes it to procurement-supervisor HITL before any order action (SCM-01)"
    - "HITL ordering is interrupt-then-audit: no audit write before resume; on resume exactly one PURCHASE_RECOMMENDATION_DRAFT then one PURCHASE_SIGNOFF, correlated by a STABLE recommendation_id derived from thread_id (CR-04); AuditRecord passed positionally (CR-02); approval_id=None on the pending row (CR-03)"
    - "Repository queries scm.inventory_levels + scm.sku_master with asyncpg passing datetime objects, never .isoformat() (Pitfall 7); all SQL parameterized ($N)"
  artifacts:
    - path: "apps/agents/supply/inventory-manager/src/scm_inventory_manager/reorder.py"
      provides: "check_reorder pure function + ReorderSignal"
      contains: "is_below_threshold"
    - path: "apps/agents/supply/inventory-manager/src/scm_inventory_manager/agent.py"
      provides: "InventoryManager.__call__ HITL agent"
      contains: "interrupt"
  key_links:
    - from: "agent.py"
      to: "audit.actions"
      via: "positional AuditRecord written after interrupt() returns"
      pattern: "self._audit.write\\(record\\)"
---

<objective>
Implement InventoryManager (SCM-01): frozen models, deterministic reorder-point logic (pure function), asyncpg repository over scm.inventory_levels + scm.sku_master, and the HITL agent that drafts a purchase recommendation and routes it to procurement-supervisor sign-off — interrupt-then-audit, stable ids.

Purpose: First supply agent; establishes the HITL pattern reused by EnergyOptimizer and DemandForecaster. Worktrees disabled — runs sequentially; files disjoint from 09-03/04/05.
Output: metadata.py, models.py, reorder.py, repository.py, agent.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agents-supply-chain-economics/09-CONTEXT.md
@.planning/phases/09-agents-supply-chain-economics/09-RESEARCH.md
@.planning/phases/08-agents-knowledge-training/08-REVIEW.md

<interfaces>
From sft_agents.models.audit: AuditRecord (id, ts, action_id, agent_id, thread_id, cluster, action_type:str, evidence_panel, decision, decision_actor, motivation, budget_snapshot, approval_id). AuditWriter.write(record: AuditRecord) — POSITIONAL, single arg (CR-02).
From sft_agents.models.enums: ActionType.REORDER_ALERT / PURCHASE_RECOMMENDATION_DRAFT / PURCHASE_SIGNOFF (from 09-00a); Decision.HITL_SUPERVISOR.
From sft_agents.models.proposed_action: ProposedAction.from_payload(thread_id, action_type, args, target_subject).
HITL: langgraph.types.interrupt (Pattern G shim raising NotImplementedError in tests, never MagicMock — WR-01).
Stable id: hashlib.sha256(f"{AGENT_ID}.{state.get('thread_id','unknown')}".encode()).hexdigest()[:32] (Pattern 3 — never uuid4 inline for correlation ids; AuditRecord.id itself may be uuid4).
asyncpg: pool.acquire() then conn.fetch(sql, *params) with datetime objects (Pitfall 7). See 09-RESEARCH.md "Query asyncpg su scm.inventory_levels" for the DISTINCT ON latest-level join.
Reorder analog: apps/agents/knowledge/shift-handover agent.py for the HITL __call__ + _write_audit shape; reorder.py from 09-RESEARCH.md Pattern 6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: models + reorder pure function + repository + metadata</name>
  <files>apps/agents/supply/inventory-manager/src/scm_inventory_manager/models.py, apps/agents/supply/inventory-manager/src/scm_inventory_manager/reorder.py, apps/agents/supply/inventory-manager/src/scm_inventory_manager/repository.py, apps/agents/supply/inventory-manager/src/scm_inventory_manager/metadata.py</files>
  <read_first>
    - apps/agents/supply/inventory-manager/tests/test_reorder.py (Wave 0 contract — exact expected values)
    - 09-RESEARCH.md Pattern 6 (check_reorder + ReorderSignal verbatim) and "Query asyncpg su scm.inventory_levels"
    - apps/agents/maintenance/downtime-analyzer/src/mnt_downtime_analyzer/repository.py (ClassVar parameterized SQL + asyncpg + datetime-object analog)
    - apps/agents/knowledge/shift-handover/src/trn_shift_handover/metadata.py (AGENT_ID/TOOL_INVENTORY/DATA_SOURCES/HITL_TIER_DEFAULT + build evidence panel)
  </read_first>
  <behavior>
    - check_reorder: is_below_threshold == (current_qty < reorder_point); deficit_qty == max(0, reorder_point - current_qty); estimated_cost_eur == reorder_qty * unit_cost_eur; Decimal arithmetic, deterministic, no LLM.
    - ReorderRecommendation model frozen + extra="forbid"; carries sku_id, current_qty, reorder_point, reorder_qty, lead_time_days, estimated_cost_eur, recommendation_id.
    - repository passes datetime objects to asyncpg (never .isoformat()); SQL uses only $N placeholders.
  </behavior>
  <action>
    In reorder.py implement check_reorder + ReorderSignal exactly per 09-RESEARCH.md Pattern 6 (frozen dataclass, Decimal arithmetic). In models.py define ReorderRecommendation and InventoryAlert as frozen Pydantic (ConfigDict(frozen=True, extra="forbid")) with the SCM-01 fields; recommendation_id is a plain str field set by the agent (the stable id). In repository.py define InventoryRepository(pool) with ClassVar _SQL_CURRENT_LEVELS doing the DISTINCT ON (sku_id) latest-level join of scm.inventory_levels to scm.sku_master filtered by ANY($1::text[]); fetch_current_levels(sku_ids) returns list[dict]; keep the never-.isoformat() rule for any future ts param. In metadata.py define AGENT_ID="inventory-manager", CLUSTER="supply", TOOL_INVENTORY, DATA_SOURCES=('scm.inventory_levels','scm.sku_master'), KPIS_IMPACTED, HITL_TIER_DEFAULT="supervisor", and a build_evidence_panel helper mirroring shift-handover (the 5 evidence keys, caller cannot overwrite them).
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/supply/inventory-manager/tests/test_reorder.py -x -q 2>&1 | tail -8 && python -c "from scm_inventory_manager.models import ReorderRecommendation; from scm_inventory_manager.metadata import AGENT_ID; assert AGENT_ID=='inventory-manager'; print('models+metadata OK')"</automated>
  </verify>
  <acceptance_criteria>
    - test_reorder.py passes with exact expected Decimal values.
    - ReorderRecommendation is frozen + extra=forbid; repository SQL uses only $N placeholders (grep finds no f-string interpolation).
    - metadata DATA_SOURCES lists only scm.inventory_levels + scm.sku_master.
  </acceptance_criteria>
  <done>Reorder logic, models, repository and metadata land with the pure-function test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InventoryManager HITL agent (interrupt-then-audit, stable id)</name>
  <files>apps/agents/supply/inventory-manager/src/scm_inventory_manager/agent.py</files>
  <read_first>
    - apps/agents/supply/inventory-manager/tests/test_inventory_hitl.py (Wave 0 HITL contract)
    - apps/agents/knowledge/shift-handover/src/trn_shift_handover/agent.py (HITL __call__, interrupt-then-audit, _write_audit building a positional AuditRecord) — and 08-REVIEW.md CR-04 fix on stable id
    - 09-RESEARCH.md Patterns 2/3/4 (interrupt-then-audit, stable id from thread_id, AuditWriter positional)
  </read_first>
  <behavior>
    - On a SKU below reorder point: compute ReorderSignal, derive a STABLE recommendation_id from thread_id, then interrupt({recommendation, agent_id}); NO audit write occurs before interrupt returns.
    - On resume: write exactly one PURCHASE_RECOMMENDATION_DRAFT row, then one PURCHASE_SIGNOFF row, both carrying the same recommendation_id; approval_id=None on the pending row; AuditRecord built fully then passed positionally to write().
    - REORDER_ALERT is emitted; return state delta includes the recommendation. No order action is taken before sign-off.
  </behavior>
  <action>
    Implement InventoryManager(pool, audit_writer, llm=None) with async __call__(state). Import interrupt via a Pattern G shim that raises NotImplementedError when langgraph is absent (NEVER MagicMock — WR-01). Steps: (1) read sku_ids from state (state.get with sensible defaults — never bare state["..."] that can KeyError, CR-03; if absent default to all monitored SKUs from the repository); (2) fetch current levels via InventoryRepository; (3) run check_reorder per SKU, select those below threshold; (4) derive recommendation_id = sha256(f"{AGENT_ID}.{state.get('thread_id','unknown')}")[:32] (CR-04, stable across replay); (5) build ReorderRecommendation + evidence panel; (6) interrupt({"recommendation": ..., "agent_id": AGENT_ID}) — RAISES on first run, RETURNS on resume; (7) ONLY after interrupt returns, build a full AuditRecord (action_type=ActionType.PURCHASE_RECOMMENDATION_DRAFT.value, decision=Decision.HITL_SUPERVISOR, approval_id=None, ts tz-aware) and `await self._audit.write(record)` POSITIONAL, then a second AuditRecord for PURCHASE_SIGNOFF with the supervisor decision_actor from the resume payload; (8) also write/emit the REORDER_ALERT (per CONTEXT the alert is the trigger row); (9) return {"reorder_recommendation": recommendation.model_dump()}. Clamp/validate any numeric so frozen models never raise (CR-05 discipline). Keep the path LLM-free except an optional short rationale string.
  </action>
  <verify>
    <automated>cd "/run/media/federicocalo/D/prj/Smart Factory Transformation" && python -m pytest apps/agents/supply/inventory-manager/tests/test_inventory_hitl.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test_inventory_hitl.py passes: 0 audit writes before resume; exactly 1 PURCHASE_RECOMMENDATION_DRAFT + 1 PURCHASE_SIGNOFF after resume; both share a recommendation_id stable across a simulated replay; approval_id None on pending; write() called with a single positional AuditRecord.
    - grep confirms no inline uuid4 used for recommendation_id (only thread_id-derived hash).
    - state access uses .get() with defaults (no bare KeyError-prone subscripting on request fields).
  </acceptance_criteria>
  <done>InventoryManager drafts a HITL purchase recommendation with correct interrupt-then-audit ordering and stable correlation id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/state to agent | sku_ids + thread_id supplied via gateway state; agent must not KeyError on missing fields |
| agent to audit.actions | append-only audit rows for reorder draft + sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | reorder SQL (sku_id params) | mitigate | asyncpg $N parameters only; ANY($1::text[]) bind, no string interpolation |
| T-09-10 | Repudiation | HITL audit correlation | mitigate | Stable recommendation_id from thread_id correlates DRAFT+SIGNOFF; positional AuditRecord (CR-02/CR-04) |
| T-09-11 | Tampering | double-write on replay | mitigate | interrupt-then-audit ordering; test asserts single DRAFT + single SIGNOFF |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 9 |
</threat_model>

<verification>
- `pytest apps/agents/supply/inventory-manager/tests/test_reorder.py -x -q` green.
- `pytest apps/agents/supply/inventory-manager/tests/test_inventory_hitl.py -x -q` green.
- No inline uuid4 correlation id; AuditRecord passed positionally.
</verification>

<success_criteria>
InventoryManager fires reorder alerts and routes purchase-recommendation drafts to supervisor HITL with correct, replay-safe audit correlation (SCM-01).
</success_criteria>

<output>
Create `.planning/phases/09-agents-supply-chain-economics/09-02-SUMMARY.md` when done.
</output>
