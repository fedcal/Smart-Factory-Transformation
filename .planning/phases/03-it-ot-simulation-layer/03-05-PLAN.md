---
phase: 03-it-ot-simulation-layer
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - infra/migrations/timescale/001_create_sensor_events.sql
  - infra/migrations/timescale/migrate.py
  - infra/migrations/timescale/__init__.py
  - infra/migrations/timescale/pyproject.toml
  - infra/migrations/timescale/tests/__init__.py
  - infra/migrations/timescale/tests/conftest.py
  - infra/migrations/timescale/tests/test_migration_idempotent.py
  - scripts/timescale-migrate.py
  - Makefile
autonomous: false
requirements:
  - IOT-06
tags:
  - timescaledb
  - hypertable
  - compression
  - retention
  - blocking-schema-push

must_haves:
  truths:
    - "Hypertable `sensor_events` creata con `create_hypertable(..., chunk_time_interval=>'1 day', if_not_exists=>TRUE)` — D-49"
    - "Compression policy attiva con `compress_segmentby='asset_id, tag_id'`, `compress_orderby='timestamp_utc DESC'`, `compress_after=INTERVAL '7 days'` — D-49"
    - "Retention policy `add_retention_policy(..., drop_after=INTERVAL '90 days', if_not_exists=>TRUE)` attiva — D-49"
    - "Indici composti `idx_sensor_events_asset_time` e `idx_sensor_events_tag_time` per query pattern agent"
    - "Migration SQL è idempotent: ri-eseguibile multiple volte senza errori (CREATE TABLE IF NOT EXISTS + DO block + if_not_exists=>TRUE)"
    - "`scripts/timescale-migrate.py` applica le migration in ordine; supporta `--dry-run`; usa `$1..$N` placeholders dove serve query parametrizzata"
    - "Test pytest verifica idempotency: doppia esecuzione del migrate ritorna exit 0 e nessun errore"
    - "OQ2 resolved: SINGLE hypertable con `source` column ('live', 'replay_cmapss', 'replay_uci') — no separate replay hypertable in Phase 3"
  artifacts:
    - path: "infra/migrations/timescale/001_create_sensor_events.sql"
      provides: "DDL hypertable + compression + retention idempotent (D-49 SQL)"
      contains: "create_hypertable"
      min_lines: 40
    - path: "infra/migrations/timescale/migrate.py"
      provides: "Idempotent migration runner asyncpg + ordering by filename"
      contains: "asyncpg.connect"
    - path: "scripts/timescale-migrate.py"
      provides: "Wrapper CLI che invoca infra/migrations/timescale/migrate.py con env TIMESCALE_DSN"
      contains: "WORKSPACE_ROOT"
    - path: "infra/migrations/timescale/tests/test_migration_idempotent.py"
      provides: "Test pytest con testcontainers TimescaleDB che verifica idempotency e schema (hypertable + compression + retention)"
      contains: "testcontainers"
  key_links:
    - from: "infra/migrations/timescale/migrate.py"
      to: "infra/migrations/timescale/*.sql"
      via: "glob + sorted execution"
      pattern: "sorted.*glob"
    - from: "scripts/timescale-migrate.py"
      to: "infra/migrations/timescale/migrate.py"
      via: "subprocess o import-and-run"
      pattern: "infra/migrations/timescale"
---

<objective>
Creare migration TimescaleDB idempotente per la hypertable `sensor_events` con compression (7d) + retention (90d) + indici composti, e fornire un runner Python (`infra/migrations/timescale/migrate.py`) testato con testcontainers per garantire idempotenza. Risolve OQ2 (single hypertable + `source` column).

Purpose (D-49 + IOT-06): è il **prerequisito BLOCKING** di Plan 03-06 (E2E test) — senza hypertable creata, `ot-bridge` non può ingerire. La migration deve essere ri-eseguibile in CI e in prod (Phase 11) senza side-effects.

Output: SQL DDL idempotente + Python runner + test testcontainers verde + Makefile target `make migrate-timescale`. **[BLOCKING] schema-push task incluso** (executor lancia il runner contro TimescaleDB del compose stack prima di proseguire a Plan 03-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md
@.planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md
@.planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md
@.planning/phases/03-it-ot-simulation-layer/03-VALIDATION.md

# Analoghi
@scripts/sync-python-versions.py
@Makefile
@infra/compose/core.yml
@.planning/phases/01-foundation-monorepo/01-02-compose-SUMMARY.md

<interfaces>
<!-- TimescaleDB schema definitivo Phase 3 -->

Table `sensor_events` (D-49 + OQ2):
| Column | Type | Constraint |
|--------|------|-----------|
| asset_id | TEXT | NOT NULL |
| tag_id | TEXT | NOT NULL |
| timestamp_utc | TIMESTAMPTZ | NOT NULL |
| value | DOUBLE PRECISION | nullable (NaN→NULL) |
| unit | TEXT | nullable |
| quality_code | SMALLINT | nullable |
| source | TEXT | NOT NULL DEFAULT 'live' |

Hypertable settings:
- partition: `timestamp_utc`
- chunk_time_interval: 1 day
- compression: segmentby=(asset_id, tag_id), orderby=(timestamp_utc DESC)
- compression policy: compress_after 7 days
- retention policy: drop_after 90 days

Indexes (per query pattern Phase 4 agents):
- `idx_sensor_events_asset_time (asset_id, timestamp_utc DESC)` — query per asset
- `idx_sensor_events_tag_time (tag_id, timestamp_utc DESC)` — query per tag

Env var contract:
- `TIMESCALE_DSN=postgresql://sft:sft_dev_pass@timescaledb:5432/sft` (default Phase 1 compose)

Migration file naming: `NNN_<description>.sql` (sortable). Phase 3 ships solo `001_create_sensor_events.sql`. Phase 4+ aggiungerà `002_create_langgraph_checkpoints.sql` ecc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration SQL idempotente + runner asyncpg + Makefile target + test testcontainers</name>
  <files>infra/migrations/timescale/001_create_sensor_events.sql, infra/migrations/timescale/migrate.py, infra/migrations/timescale/__init__.py, infra/migrations/timescale/pyproject.toml, infra/migrations/timescale/tests/__init__.py, infra/migrations/timescale/tests/conftest.py, infra/migrations/timescale/tests/test_migration_idempotent.py, scripts/timescale-migrate.py, Makefile</files>
  <read_first>
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Code Examples "TimescaleDB migration SQL idempotente" (lines 878-936) — COPY DDL ESATTO
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 2 (TimescaleDB 2.18 Old API compression — commento "Using legacy compression API" obbligatorio in SQL)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 6 (asyncpg statement_cache_size=0 per TimescaleDB)
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md sezione "scripts/timescale-migrate.py" (lines 372-431) — analog `scripts/sync-python-versions.py` per argparse + --dry-run + exit codes
    - scripts/sync-python-versions.py (analog argparse pattern, lines 1-131)
    - infra/compose/core.yml (verifica image TimescaleDB versionata 2.18.x-pg16)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-49 (chunk=1d/compress=7d/drop=90d con SQL explicit) + OQ2 resolution (single hypertable + source column)
    - Makefile (esistente, .PHONY line 16 + pattern target lines 137-149)
  </read_first>
  <behavior>
    - Test 1 (test_migration_idempotent.test_first_run): contro TimescaleDB testcontainer fresh, run `migrate(dsn)` → return 0; verify via SQL: `SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name='sensor_events'` ritorna 1 row con `compression_enabled=true`.
    - Test 2 (test_migration_idempotent.test_second_run_no_error): doppia esecuzione di `migrate(dsn)` → exit 0 entrambe; verify zero error in pg log; row count `sensor_events` invariante.
    - Test 3 (test_migration_idempotent.test_retention_policy_set): query `SELECT * FROM timescaledb_information.policies WHERE hypertable='sensor_events' AND policy_name LIKE '%retention%'` ritorna 1 row con `config->>'drop_after' = '90 days'`.
    - Test 4 (test_migration_idempotent.test_compression_policy_set): analogo retention test ma per compression policy con `compress_after = 7 days`.
    - Test 5 (test_migration_idempotent.test_indexes_exist): query `pg_indexes WHERE tablename='sensor_events'` include `idx_sensor_events_asset_time` e `idx_sensor_events_tag_time`.
    - Test 6 (test_migration_idempotent.test_insert_after_migration): dopo migrate, `INSERT INTO sensor_events (asset_id, tag_id, timestamp_utc, value, unit, quality_code, source) VALUES ($1,...)` accetta 1 row; `SELECT COUNT(*)` ritorna 1.
    - Test 7 (test_migration_idempotent.test_dry_run_no_side_effect): contro fresh testcontainer, `migrate(dsn, dry_run=True)` → exit 0 + `SELECT to_regclass('sensor_events')` ritorna NULL (no table created).
  </behavior>
  <action>
    1. **infra/migrations/timescale/001_create_sensor_events.sql** — COPY ESATTO RESEARCH §Code Examples lines 880-936:
       ```sql
       -- Source: D-49 + docs.timescale.com/api/latest/compression/
       -- Phase 3 — Plan 03-05
       -- Idempotent: safe to run multiple times.
       -- NOTE: Using legacy compression API (TimescaleDB 2.18.x); migration to columnstore (hypercore) deferred to Phase 11 if/when hypercore semantics settles (Pitfall 2).

       CREATE TABLE IF NOT EXISTS sensor_events (
         asset_id      TEXT NOT NULL,
         tag_id        TEXT NOT NULL,
         timestamp_utc TIMESTAMPTZ NOT NULL,
         value         DOUBLE PRECISION,
         unit          TEXT,
         quality_code  SMALLINT,
         source        TEXT NOT NULL DEFAULT 'live'  -- 'live' | 'replay_cmapss' | 'replay_uci' (OQ2)
       );

       SELECT create_hypertable(
         'sensor_events',
         'timestamp_utc',
         chunk_time_interval => INTERVAL '1 day',
         if_not_exists => TRUE
       );

       DO $$
       BEGIN
         IF NOT EXISTS (
           SELECT 1 FROM timescaledb_information.hypertables
           WHERE hypertable_name = 'sensor_events' AND compression_enabled = true
         ) THEN
           ALTER TABLE sensor_events SET (
             timescaledb.compress,
             timescaledb.compress_segmentby = 'asset_id, tag_id',
             timescaledb.compress_orderby = 'timestamp_utc DESC'
           );
         END IF;
       END $$;

       SELECT add_compression_policy(
         'sensor_events',
         compress_after => INTERVAL '7 days',
         if_not_exists => TRUE
       );

       SELECT add_retention_policy(
         'sensor_events',
         drop_after => INTERVAL '90 days',
         if_not_exists => TRUE
       );

       CREATE INDEX IF NOT EXISTS idx_sensor_events_asset_time
         ON sensor_events (asset_id, timestamp_utc DESC);
       CREATE INDEX IF NOT EXISTS idx_sensor_events_tag_time
         ON sensor_events (tag_id, timestamp_utc DESC);
       ```

       Conteggi: ≥40 righe SQL (RESEARCH ha exact reference).

    2. **infra/migrations/timescale/migrate.py** — Idempotent runner asyncpg:
       - Shebang + docstring exit codes (0/1).
       - `_MIGRATIONS_DIR = Path(__file__).parent` (relative).
       - `async def migrate(dsn: str, dry_run: bool = False) -> int`:
         - `migration_files = sorted(_MIGRATIONS_DIR.glob("[0-9][0-9][0-9]_*.sql"))`
         - Se `dry_run`: stampa la lista di file che sarebbero eseguiti + 0. NO connection.
         - `conn = await asyncpg.connect(dsn, statement_cache_size=0, command_timeout=60.0)` (Pitfall 6 + long DDL).
         - Per ogni file: `sql = file.read_text(encoding="utf-8")`; `try: await conn.execute(sql) except asyncpg.PostgresError as e: log + return 1`.
         - Stampa `OK [001_create_sensor_events.sql]: applied`.
         - `await conn.close()`; return 0.
       - `def main() -> None`: argparse `--dsn` (default `os.environ.get("TIMESCALE_DSN")`, raise se None), `--dry-run`. `sys.exit(asyncio.run(migrate(args.dsn, args.dry_run)))`.

    3. **infra/migrations/timescale/__init__.py** — empty file (Python package marker).

    4. **infra/migrations/timescale/pyproject.toml** — Nx Python project minimale:
       - `[project] name = "timescale-migrations"`, `version = "0.1.0"`, `requires-python = ">=3.12,<3.13"`, dependencies `["asyncpg>=0.30"]`, dev `["pytest>=8.0", "pytest-asyncio>=0.24", "testcontainers[postgres]>=4.14"]`.
       - `[tool.hatch.build.targets.wheel] packages = ["."]` (o omit se non publish).
       - `[tool.pytest.ini_options] asyncio_mode = "auto"`, `testpaths = ["tests"]`.

    5. **infra/migrations/timescale/tests/conftest.py** — testcontainers fixture:
       - `import pytest`
       - `from testcontainers.postgres import PostgresContainer`
       - `@pytest.fixture(scope="session") def timescale_container() -> Generator`:
         - `with PostgresContainer("timescale/timescaledb:2.18.0-pg16", username="sft", password="sft_dev_pass", dbname="sft") as container:`
         - `dsn = f"postgresql://sft:sft_dev_pass@{container.get_container_host_ip()}:{container.get_exposed_port(5432)}/sft"`
         - `yield dsn`
       - Marker `@pytest.mark.testcontainers` (skip se docker non disponibile).

    6. **infra/migrations/timescale/tests/test_migration_idempotent.py** — Implementare Test 1-7. Esempio test_first_run:
       ```python
       @pytest.mark.testcontainers
       @pytest.mark.asyncio
       async def test_first_run(timescale_container: str) -> None:
           from infra.migrations.timescale.migrate import migrate
           rc = await migrate(timescale_container)
           assert rc == 0
           conn = await asyncpg.connect(timescale_container, statement_cache_size=0)
           row = await conn.fetchrow(
               "SELECT compression_enabled FROM timescaledb_information.hypertables WHERE hypertable_name = $1",
               "sensor_events",
           )
           assert row is not None and row["compression_enabled"] is True
           await conn.close()
       ```
       Tutti i test usano placeholder `$1` (no f-string SQL).

    7. **scripts/timescale-migrate.py** — Wrapper CLI top-level (Pattern S-3):
       - Shebang + docstring.
       - `WORKSPACE_ROOT = Path(__file__).parent.parent`.
       - argparse `--dsn` (default env `TIMESCALE_DSN`), `--dry-run`, `--migrations-dir` (default `WORKSPACE_ROOT / "infra/migrations/timescale"`).
       - Import dinamico: `sys.path.insert(0, str(WORKSPACE_ROOT))`; `from infra.migrations.timescale.migrate import migrate`; `sys.exit(asyncio.run(migrate(args.dsn, args.dry_run)))`.
       - Exit 0 / 1.

    8. **Makefile** — Aggiungere target:
       ```
       # Phase 3: TimescaleDB migration
       migrate-timescale:
       \tpython3 scripts/timescale-migrate.py

       migrate-timescale-dry:
       \tpython3 scripts/timescale-migrate.py --dry-run
       ```
       Aggiungere `migrate-timescale migrate-timescale-dry` a `.PHONY` line 16.

    Decisioni: D-49 (chunk=1d/compress=7d/drop=90d), IOT-06 (hypertable + compression + retention created), OQ2 (single hypertable + `source` column), Pitfall 2 (legacy compression API documented).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && python3 scripts/timescale-migrate.py --dry-run --dsn postgresql://x:x@localhost:5432/x && grep -c "create_hypertable" infra/migrations/timescale/001_create_sensor_events.sql && grep -c "add_compression_policy" infra/migrations/timescale/001_create_sensor_events.sql && grep -c "add_retention_policy" infra/migrations/timescale/001_create_sensor_events.sql</automated>
  </verify>
  <acceptance_criteria>
    - `test -f infra/migrations/timescale/001_create_sensor_events.sql` exit 0.
    - `grep -c "create_hypertable" infra/migrations/timescale/001_create_sensor_events.sql` ≥ 1.
    - `grep -c "add_compression_policy" infra/migrations/timescale/001_create_sensor_events.sql` ≥ 1.
    - `grep -c "add_retention_policy" infra/migrations/timescale/001_create_sensor_events.sql` ≥ 1.
    - `grep -c "if_not_exists => TRUE" infra/migrations/timescale/001_create_sensor_events.sql` ≥ 3 (hypertable + compression + retention).
    - `grep -c "CREATE TABLE IF NOT EXISTS sensor_events" infra/migrations/timescale/001_create_sensor_events.sql` == 1.
    - `grep -c "source        TEXT NOT NULL DEFAULT 'live'" infra/migrations/timescale/001_create_sensor_events.sql` == 1 (OQ2).
    - `grep -c "CREATE INDEX IF NOT EXISTS idx_sensor_events_asset_time" infra/migrations/timescale/001_create_sensor_events.sql` == 1.
    - `grep -c "statement_cache_size=0" infra/migrations/timescale/migrate.py` ≥ 1 (Pitfall 6).
    - `python3 scripts/timescale-migrate.py --dry-run --dsn postgresql://x:x@localhost:5432/x` exits 0 e stdout contiene "001_create_sensor_events.sql".
    - `grep -v '^#' Makefile | grep -c "^migrate-timescale:"` == 1.
    - `grep -v '^#' Makefile | grep -c "^migrate-timescale-dry:"` == 1.
  </acceptance_criteria>
  <done>Migration SQL idempotent committato; runner Python e wrapper CLI funzionanti in dry-run; Makefile target attivi.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-schema-push">
  <name>Task 2: [BLOCKING] schema-push verso TimescaleDB del compose stack</name>
  <files>(no file changes — questa è una operazione di runtime)</files>
  <read_first>
    - infra/compose/core.yml (verifica `timescaledb` service + image + healthcheck)
    - .planning/phases/01-foundation-monorepo/01-02-compose-SUMMARY.md (TimescaleDB service config)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md schema push detection (planner block — questo task è obbligatorio prima di plan 03-06)
  </read_first>
  <what-built>
    Migration SQL `001_create_sensor_events.sql` + runner Python `migrate.py` + wrapper script `scripts/timescale-migrate.py` + test pytest con testcontainers (Task 1 sopra).

    Idempotenza testata in testcontainers; ora il database TIMESCALEDB del compose dev stack (Phase 1) deve essere effettivamente migrato perché Plan 03-06 (E2E test sim → bridge → NATS → Timescale) NON funzioni in mancanza della hypertable.
  </what-built>
  <how-to-verify>
    Eseguire SEQUENZIALMENTE i seguenti comandi e confermare exit code 0 per ognuno (l'executor li lancerà come bash; il checkpoint si chiude quando l'utente conferma o autoadvance dopo verifica automatica):

    1. **Avvia stack core (timescaledb)** (se non già avviato):
       ```bash
       docker compose -f infra/compose/core.yml up -d timescaledb
       # Attendi healthy:
       docker compose -f infra/compose/core.yml exec timescaledb pg_isready -U sft -d sft
       # Attesa massimo 30s.
       ```

    2. **Test pytest con testcontainers** (verifica idempotenza in container isolato):
       ```bash
       uv run --with pytest --with pytest-asyncio --with 'testcontainers[postgres]' --with asyncpg -- python -m pytest infra/migrations/timescale/tests -m testcontainers -x -v
       ```
       Atteso: exit 0, ≥ 7 test PASSED.

    3. **Apply migration al dev compose stack TIMESCALEDB**:
       ```bash
       export TIMESCALE_DSN="postgresql://sft:sft_dev_pass@localhost:5432/sft"
       python3 scripts/timescale-migrate.py --dry-run  # exit 0
       python3 scripts/timescale-migrate.py            # exit 0 — applica per davvero
       ```
       Stdout atteso: `OK [001_create_sensor_events.sql]: applied`.

    4. **Verifica schema applicato**:
       ```bash
       docker compose -f infra/compose/core.yml exec timescaledb psql -U sft -d sft -c "SELECT hypertable_name, compression_enabled FROM timescaledb_information.hypertables WHERE hypertable_name = 'sensor_events';"
       ```
       Atteso: 1 row con `sensor_events | t`.

    5. **Verifica retention + compression policy**:
       ```bash
       docker compose -f infra/compose/core.yml exec timescaledb psql -U sft -d sft -c "SELECT proc_name, config FROM timescaledb_information.jobs WHERE hypertable_name = 'sensor_events';"
       ```
       Atteso: ≥ 2 row (compression + retention policy jobs).

    6. **Re-run idempotency check**:
       ```bash
       python3 scripts/timescale-migrate.py  # secondo run: exit 0 senza errori
       ```

    Se tutti 6 step ritornano exit 0 e stdout atteso: schema-push COMPLETO. Plan 03-06 può iniziare.
  </how-to-verify>
  <resume-signal>
    Type "approved" se i 6 step sono completati senza errori. Type "blocked: <reason>" se TimescaleDB non è raggiungibile / migration fallisce (riprovare dopo aver corretto).

    Note: se docker non è disponibile localmente (ambiente CI), il checkpoint può essere chiuso come "skipped: testcontainers green, docker unavailable" — Plan 03-06 dovrà includere un equivalente step nel suo task di setup.
  </resume-signal>
  <done>Schema applicato sul TimescaleDB del dev compose stack; idempotency verificata in test e in produzione locale; Plan 03-06 può iniziare.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration.sql → TimescaleDB | DDL eseguito con permessi superuser `sft`; ad alto privilegio (CREATE/ALTER TABLE, CREATE INDEX, add policies). |
| migrate.py → env var | `TIMESCALE_DSN` da env solo (no hardcoded); script raise se mancante. |
| testcontainers → docker | Test integration richiede docker socket; isolated container per test session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-ddl-injection | Injection / Tampering | `migrate.py` esegue raw SQL files | mitigate | SQL files sono artifact build, audited via PR review + pre-commit (Phase 1 gitleaks); migrate.py NON accetta input utente arbitrario, solo glob su `_MIGRATIONS_DIR`. Riferisce T-V5-sql (path-restricted variant). |
| T-03-05-old-compression | Tampering (silent deprecation) | `001_create_sensor_events.sql` | mitigate | Commento esplicito "legacy compression API" + image pin `timescale/timescaledb:2.18.0-pg16` in compose; Phase 11 review per migrare a `add_columnstore_policy`. Riferisce Pitfall 2. |
| T-03-05-statement-cache | Tampering (stale plans) | `migrate.py` asyncpg.connect | mitigate | `statement_cache_size=0` esplicito (Pitfall 6). DDL execution non beneficia di cache comunque. |
| T-03-05-dsn-leak | Information Disclosure | `migrate.py` log via print | mitigate | Print stampa `OK [filename]: applied` ma MAI il DSN intero; nessun structlog log di env vars; `--dry-run` non si connette. |
| T-03-05-retention-loss | Tampering | retention policy=90d | accept | Phase 3 PoC; Phase 11 audit data ha tabelle separate con retention 7 anni; documentato in D-49. |
| T-03-05-supply-chain | Tampering | `testcontainers, asyncpg` dev install | mitigate | RESEARCH §Package Legitimacy Audit: testcontainers approved (slopcheck OK), asyncpg approved. |

`security_enforcement: enabled` (ASVS V5 + V8 + V14). Migration non logga payload application data; solo DDL.
</threat_model>

<verification>
- Task 1 acceptance criteria green (8+ assertion).
- Task 2 (BLOCKING checkpoint): tutti 6 step in how-to-verify exit 0 (o `skipped: testcontainers green, docker unavailable`).
- `uv run -m pytest infra/migrations/timescale/tests -m testcontainers` exit 0 quando docker disponibile.
- `grep -c "statement_cache_size=0" infra/migrations/timescale/migrate.py` ≥ 1.
- Makefile target `migrate-timescale` invocabile.
- Idempotenza dimostrata: doppia esecuzione exit 0 entrambe.
</verification>

<success_criteria>
- IOT-06 chiuso: TimescaleDB hypertable `sensor_events` + compression(7d) + retention(90d) + indici creati.
- OQ2 resolved: single hypertable con `source` column documentato in DDL + commento.
- Migration idempotente e ri-eseguibile, testato con testcontainers (7 test).
- Schema effettivamente applicato al dev compose stack (Task 2 BLOCKING).
- Pitfall 2 (legacy compression API) documentato in commento SQL.
- Nessuna regressione: `infra/compose/core.yml` non modificato.
</success_criteria>

<output>
Create `.planning/phases/03-it-ot-simulation-layer/03-05-SUMMARY.md` con:
- file creati + dimensione
- output pytest testcontainers
- output `python3 scripts/timescale-migrate.py --dry-run` + `python3 scripts/timescale-migrate.py` (Task 2)
- decisione OQ2 documentata
- conferma schema applicato (output `SELECT hypertable_name...`)
</output>
