---
phase: 03-it-ot-simulation-layer
plan: 07
type: execute
wave: 4
depends_on:
  - "03-06"
files_modified:
  - tests/load/test_ingestion_throughput.py
  - tests/load/conftest.py
  - tests/load/README.md
  - docs/docs/it-ot/index.md
  - docs/docs/it-ot/ingest-schema.md
  - docs/docs/it-ot/opcua-schema.md
  - docs/docs/en/it-ot/index.md
  - docs/docs/en/it-ot/ingest-schema.md
  - docs/docs/en/it-ot/opcua-schema.md
  - docs/mkdocs.yml
  - .github/workflows/ci.yml
  - Makefile
autonomous: false
requirements:
  - IOT-09
  - IOT-10
tags:
  - load-test-full
  - docs
  - mkdocs-it-ot
  - ingest-schema

must_haves:
  truths:
    - "Full load test 5k msg/s × 60s steady-state asset mix (60% loom, 20% spinning, 10% dyeing/finishing, 10% warping per D-48) gated da PR-label `load-test` — IOT-10 full"
    - "MkDocs sezione `it-ot/` esiste con `index.md`, `ingest-schema.md`, `opcua-schema.md` bilingue IT (default) + EN (mirror) — IOT-09 + D-15"
    - "`ingest-schema.md` documenta: asset registry layout (tabella ~30 asset), tag dictionary (~24 tag distinti con unit + sample_rate_hz + semantic_type), sample SensorEvent JSON, NATS subject hierarchy D-52, hypertable schema, retention policy D-49"
    - "`opcua-schema.md` documenta: namespace pattern `urn:mantis:<family>`, BrowsePath `<family>/<asset_id>/<tag_id>`, security policy A-018 (NoSecurity PoC + Phase 11 hardening)"
    - "`mkdocs build --strict` exits 0 (no broken link, no warning)"
    - "CI workflow ha step opzionale `Run IT/OT full load test` gated da PR label `load-test` — D-48 + claudes_discretion CI strategy"
    - "Plan 03-07 chiude IOT-09 (ingest schema docs) e IOT-10 (full load gate definito anche se non eseguito di default)"
  artifacts:
    - path: "tests/load/test_ingestion_throughput.py"
      provides: "Full 5k×60s test con custom asyncio harness D-48 asset mix"
      contains: "test_5k_60s"
    - path: "docs/docs/it-ot/ingest-schema.md"
      provides: "IT — Ingest schema documentation (IOT-09)"
      contains: "asset registry"
      min_lines: 80
    - path: "docs/docs/en/it-ot/ingest-schema.md"
      provides: "EN mirror"
      min_lines: 80
    - path: "docs/docs/it-ot/opcua-schema.md"
      provides: "IT — OPC-UA namespace pattern"
      contains: "urn:mantis"
    - path: "docs/mkdocs.yml"
      provides: "Nav extension con sezione IT/OT IT+EN"
      contains: "it-ot"
    - path: ".github/workflows/ci.yml"
      provides: "Step opzionale `Run IT/OT full load test` PR-label-gated"
      contains: "load-test"
  key_links:
    - from: "tests/load/test_ingestion_throughput.py"
      to: "tests/load/harness.py"
      via: "import run_load from Plan 03-06"
      pattern: "from tests.load.harness import"
    - from: "docs/docs/it-ot/ingest-schema.md"
      to: "packages/sft-assets/src/sft_assets/registry.yaml"
      via: "tabella asset/tag estratta o referenziata"
      pattern: "asset_id"
    - from: "docs/mkdocs.yml"
      to: "docs/docs/it-ot/"
      via: "nav entry IT-OT IT + EN mirror"
      pattern: "it-ot/"
---

<objective>
Chiudere Phase 3: (a) implementare il full load test 5k msg/s × 60s con asset mix realistico D-48 (gated da PR-label `load-test` in CI), (b) scrivere la documentazione MkDocs `docs/docs/it-ot/` bilingue (IT default + EN mirror) per IOT-09 — ingest schema con esempi, OPC-UA namespace pattern, sample SensorEvent JSON.

Purpose: 
- IOT-09: l'ingest schema deve essere DOCUMENTED + ESEMPI per onboarding Phase 4+ agents e per audit della Phase 11.
- IOT-10 full: definire la spec del 5k×60s test (eseguito su demand via PR-label, non blocking CI default come da claudes_discretion + D-48).

Output: `mkdocs build --strict` verde, full load test eseguibile via `make load-test-full` o PR-label CI, documentazione completa committata.

**HAS CHECKPOINT**: 1 `checkpoint:human-verify` per docs review (mkdocs preview) — IOT-09 è uno dei 5 success criteria di Phase 3 ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md
@.planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md
@.planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md
@.planning/phases/03-it-ot-simulation-layer/03-VALIDATION.md
@.planning/phases/03-it-ot-simulation-layer/03-06-PLAN.md

# Existing MkDocs scaffold
@docs/mkdocs.yml

# Plan 03-06 outputs (harness + smoke)
@.planning/phases/03-it-ot-simulation-layer/03-06-PLAN.md

# Phase 1 MkDocs i18n
@.planning/phases/01-foundation-monorepo/01-07-mkdocs-PLAN.md

<interfaces>
<!-- D-48 full load asset mix (60s steady-state) -->

Asset mix (D-48):
- 60% loom: 100 telai × 10Hz = ~3000 msg/s (warp_tension + pick_density + creel_speed + broken_pick_count + loom_temperature; ma in Phase 3 abbiamo solo 12 loom in registry → harness usa subset rappresentativo)
- 20% spinning: ~1000 msg/s
- 10% dyeing/finishing: ~500 msg/s
- 10% warping: ~500 msg/s

Total target: ~5000 msg/s × 60s = 300_000 events.

Per Phase 3 (registry da 30 asset reali): harness deve "amplificare" il pubblicare ripetendo asset_id già esistenti per raggiungere il rate target — è valido per misurare ingestion path, NON simulare 100 telai reali.

Threshold (IOT-10): p99 < 200ms.

MkDocs nav extension:
```yaml
nav:
  - IT/OT:
    - Panoramica: it-ot/index.md
    - Schema ingest: it-ot/ingest-schema.md
    - Schema OPC-UA: it-ot/opcua-schema.md
```
EN mirror nav già esiste in Phase 1 i18n (`docs/docs/en/`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full load test 5k×60s + CI PR-label gating + Makefile target</name>
  <files>tests/load/test_ingestion_throughput.py, tests/load/conftest.py, tests/load/README.md, .github/workflows/ci.yml, Makefile</files>
  <read_first>
    - tests/load/harness.py (Plan 03-06 — `run_load(dsn, nats_url, target_rate, duration_s, assets) -> LoadResult`)
    - tests/load/test_ingestion_smoke.py (Plan 03-06 — pattern test)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-48 (mix exact + 5k target + 60s steady-state)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md claudes_discretion CI strategy (full test gated da `load-test` PR label)
    - .github/workflows/ci.yml (Plan 03-06 Task 3 — esistente)
    - packages/sft-assets/src/sft_assets/registry.yaml (Plan 03-01 — 30 asset, 24+ tag distinti)
  </read_first>
  <action>
    1. **tests/load/test_ingestion_throughput.py** — Full 5k×60s test:
       - Costanti: `FULL_RATE = 5000`, `FULL_DURATION = 60`, `FULL_P99_MS_TARGET = 200`.
       - Marker: `@pytest.mark.load_full` (NON `load`). Anche `@pytest.mark.integration`.
       - `@pytest.fixture def full_load_enabled(pytestconfig)`: ritorna `pytestconfig.getoption("--full-load-test", default=False)`; se False → `pytest.skip("Pass --full-load-test or set FULL_LOAD_TEST=1 to run")`.
       - `def pytest_addoption(parser)` in `tests/load/conftest.py`: aggiunge `--full-load-test` flag.
       - Asset list D-48 mix (per Phase 3 amplifica i 30 asset reali fino al target rate):
         - 60% rate: 18 loom-tag combinations (12 loom × 5 tag = 60 combinazioni; subset 18 a 10Hz effective rate × replica)
         - 20% rate: spinning subset
         - 10% rate: dyeing + finishing
         - 10% rate: warping
       - `@pytest.mark.load_full @pytest.mark.integration @pytest.mark.asyncio async def test_5k_60s(compose_stack, full_load_enabled)`:
         - Costruisci `assets_mix: list[tuple[str, str, str]]` con 5x replication pesata.
         - `result = await run_load(dsn=compose_stack["TIMESCALE_DSN"], nats_url=compose_stack["NATS_URL"], target_rate=FULL_RATE, duration_s=FULL_DURATION, assets=assets_mix)`.
         - assert `result.total_events >= FULL_RATE * FULL_DURATION * 0.95` (95% target = ≥285k events).
         - assert `result.p99_ms < FULL_P99_MS_TARGET` (200ms IOT-10).
         - Stampa report (per PR description): `print(f"FULL LOAD: total={result.total_events}, p50={result.p50_ms:.0f}ms, p99={result.p99_ms:.0f}ms, rate={result.rate_per_s:.0f}/s")`.

    2. **tests/load/conftest.py** — pytest options:
       ```python
       def pytest_addoption(parser):
           parser.addoption("--full-load-test", action="store_true", default=False,
                            help="Enable full 5k×60s load test (IOT-10 full gate)")
       ```

    3. **tests/load/README.md** — Documento breve:
       - Smoke (1k×10s): sempre in CI, default `make smoke-load`.
       - Full (5k×60s): gated da PR-label `load-test` o flag `--full-load-test`. Comando: `make load-test-full`. Atteso runtime ~75s su hardware nominale (CI Github Actions standard runner).
       - Constraints D-48: steady-state (no ramp-up), asset mix realistico (60/20/10/10), 256-512 byte payload.
       - Threshold IOT-10: p99 < 200ms.
       - Output: stampa `FULL LOAD: total=, p50=, p99=, rate=` per PR description copy-paste.

    4. **.github/workflows/ci.yml** — Aggiungere step CONDIZIONALE su PR label `load-test`:
       ```yaml
       - name: Run IT/OT full load test (5k×60s, PR-label gated)
         if: contains(github.event.pull_request.labels.*.name, 'load-test')
         run: |
           docker compose -f infra/compose/core.yml up -d --wait timescaledb nats
           export TIMESCALE_DSN="postgresql://sft:sft_dev_pass@localhost:5432/sft"
           python3 scripts/timescale-migrate.py
           python3 scripts/nats-bootstrap-streams.py --server nats://localhost:4222
           docker compose -f infra/compose/core.yml -f infra/compose/sim.yml up -d --wait
           uv run --with pytest --with pytest-asyncio --with asyncpg --with nats-py -- python -m pytest tests/load/test_ingestion_throughput.py -v -m load_full --full-load-test
           docker compose -f infra/compose/core.yml -f infra/compose/sim.yml down -v
       ```
       Pos: DOPO `Run IT/OT load test (smoke 1k×10s)` di Plan 03-06.

    5. **Makefile** — Aggiungere target:
       ```makefile
       load-test-full:
       \tdocker compose -f infra/compose/core.yml -f infra/compose/sim.yml up -d --wait
       \tuv run pytest tests/load/test_ingestion_throughput.py -v -m load_full --full-load-test
       \tdocker compose -f infra/compose/core.yml -f infra/compose/sim.yml down -v
       ```
       Aggiungere `load-test-full` a `.PHONY` line 16.

    Decisioni: D-48 asset mix exact + custom asyncio (no Locust), claudes_discretion CI strategy (PR-label gating), IOT-10 full coverage definito.
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && python3 -c "import ast; ast.parse(open('tests/load/test_ingestion_throughput.py').read()); ast.parse(open('tests/load/conftest.py').read())" && grep -c "FULL_P99_MS_TARGET = 200" tests/load/test_ingestion_throughput.py && grep -c "load-test" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('tests/load/test_ingestion_throughput.py').read())"` exits 0.
    - `grep -c "FULL_RATE = 5000" tests/load/test_ingestion_throughput.py` == 1.
    - `grep -c "FULL_DURATION = 60" tests/load/test_ingestion_throughput.py` == 1.
    - `grep -c "FULL_P99_MS_TARGET = 200" tests/load/test_ingestion_throughput.py` == 1.
    - `grep -c "pytest_addoption" tests/load/conftest.py` == 1.
    - `grep -c "--full-load-test" tests/load/conftest.py` ≥ 1.
    - `grep -c "load-test" .github/workflows/ci.yml` ≥ 1 (PR-label gating).
    - `grep -c "if: contains(github.event.pull_request.labels" .github/workflows/ci.yml` ≥ 1.
    - `grep -v '^#' Makefile | grep -c "^load-test-full:"` == 1.
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
    - `test -f tests/load/README.md` exit 0.
  </acceptance_criteria>
  <done>Full 5k×60s test definito e sintatticamente valido; CI PR-label gate configurato; Makefile target attivo; README documenta procedura.</done>
</task>

<task type="auto">
  <name>Task 2: MkDocs docs/docs/it-ot/ sezione IT + EN mirror (IOT-09)</name>
  <files>docs/docs/it-ot/index.md, docs/docs/it-ot/ingest-schema.md, docs/docs/it-ot/opcua-schema.md, docs/docs/en/it-ot/index.md, docs/docs/en/it-ot/ingest-schema.md, docs/docs/en/it-ot/opcua-schema.md, docs/mkdocs.yml</files>
  <read_first>
    - docs/mkdocs.yml (esistente — pattern nav i18n Phase 1)
    - .planning/phases/01-foundation-monorepo/01-07-mkdocs-PLAN.md (i18n pattern + IT default + EN mirror)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-52 (subject hierarchy) + D-49 (retention) + D-44 (fault profile schema) + claudes_discretion (OPC-UA namespace pattern)
    - packages/sft-assets/src/sft_assets/registry.yaml (Plan 03-01 — fonte truth per tabella asset/tag)
    - services/ot-bridge/src/svc_ot_bridge/models.py (Plan 03-04 — SensorEvent schema)
    - infra/migrations/timescale/001_create_sensor_events.sql (Plan 03-05 — hypertable schema)
  </read_first>
  <action>
    1. **docs/docs/it-ot/index.md** — Pagina overview (IT):
       - Titolo "# IT/OT Simulation Layer".
       - Intro: scopo della Phase 3, decisioni chiave (D-44 fault profiles, D-49 retention, D-50 single asyncio process, D-51 data-diode 3-layer, D-52 subject hierarchy), riferimenti a `.planning/phases/03-*`.
       - Diagramma Mermaid: copia da RESEARCH §System Architecture Diagram (lines 252-294) — sim-textile → ot-bridge → NATS+TimescaleDB.
       - Lista 5 success criteria della Phase 3 (da ROADMAP).
       - Link a `ingest-schema.md` e `opcua-schema.md`.
       - Sezione "Validazione" con comandi `make integration-test`, `make smoke-load`, `make load-test-full`.

    2. **docs/docs/it-ot/ingest-schema.md** — Documentazione ingest schema (IT — IOT-09 PRIMARY ARTIFACT):
       - Titolo "# Schema di ingest sensori".
       - Sezione "Asset registry":
         - Path file `packages/sft-assets/src/sft_assets/registry.yaml`.
         - Tabella riassuntiva (in markdown): per family (loom, spinning, warping, dyeing, finishing) → conteggio asset + sample asset_id + tag dictionary count.
         - Esempio YAML estratto (1 LOOM-01 + 1 DYE-01 con 2-3 tag ciascuno).
       - Sezione "Tag dictionary":
         - Tabella di ~24 tag distinti con colonne: `tag_id | unit | sample_rate_hz | semantic_type | family principale`.
         - Esempi: `warp_tension | N | 10 | tension | loom`, `pick_density | picks_per_cm | 1 | density | loom`, `bath_temperature | degC | 1 | temperature | dyeing`, ecc.
       - Sezione "Units of measure":
         - Tabella unit utilizzati: `N, picks_per_cm, rpm, count, degC, mm_s, cN, m_min, l_min, pct, bar, mm, pH, pct_rh`.
       - Sezione "SensorEvent JSON sample" — Esempio payload pubblicato su NATS (da `SensorEvent.model_dump_json()`):
         ```json
         {
           "asset_id": "LOOM-01",
           "asset_family": "loom",
           "tag_id": "warp_tension",
           "timestamp_utc": "2026-05-18T12:00:00+00:00",
           "value": 25.3,
           "unit": "N",
           "quality_code": 0,
           "source": "live",
           "server_received_ts": "2026-05-18T12:00:00.045+00:00"
         }
         ```
       - Sezione "NATS subject hierarchy (D-52)":
         - `sensor.events.<asset_family>.<asset_id>.<tag_id>` — example `sensor.events.loom.LOOM-01.warp_tension`.
         - `sensor.alarms.<asset_family>.<asset_id>` — example `sensor.alarms.dyeing.DYE-01`.
         - `audit.ot.bridge` — formato AuditEvent.
       - Sezione "Hypertable TimescaleDB":
         - DDL excerpt da `infra/migrations/timescale/001_create_sensor_events.sql` (CREATE TABLE + create_hypertable + add_compression_policy + add_retention_policy).
         - Spiegare colonne: `asset_id, tag_id, timestamp_utc, value, unit, quality_code, source`.
         - Retention: chunk=1d, compress=7d, drop=90d (D-49).
         - Indici: `idx_sensor_events_asset_time`, `idx_sensor_events_tag_time`.
       - Sezione "Query patterns esempio" per agent (Phase 4+):
         ```sql
         -- Tutti i warp_tension di LOOM-01 nelle ultime 24h
         SELECT * FROM sensor_events
         WHERE asset_id = $1 AND tag_id = $2
           AND timestamp_utc > NOW() - INTERVAL '24 hours'
         ORDER BY timestamp_utc DESC;
         ```
       Min lines: 80.

    3. **docs/docs/it-ot/opcua-schema.md** — Documentazione OPC-UA namespace (IT):
       - Titolo "# Schema OPC-UA".
       - Sezione "Endpoint": `opc.tcp://sim-textile:4840/sft-textile/` (claudes_discretion).
       - Sezione "Security policy": `NoSecurity` (A-018 PoC; Phase 11 introduce Sign+Encrypt).
       - Sezione "Namespace pattern":
         - 5 namespace registrati: `urn:mantis:loom`, `urn:mantis:spinning`, `urn:mantis:warping`, `urn:mantis:dyeing`, `urn:mantis:finishing`.
         - BrowsePath: `<family>/<asset_id>/<tag_id>` → es. `loom/LOOM-01/warp_tension`.
       - Sezione "Variable nodes":
         - VariantType: `Double` per ogni tag.
         - Writable: `False` (data-diode protocol-level, D-51 Layer 0).
       - Sezione "Esempio Python client":
         ```python
         async with asyncua.Client("opc.tcp://sim-textile:4840/sft-textile/") as client:
             ns_idx = await client.get_namespace_index("urn:mantis:loom")
             var = await client.nodes.objects.get_child([
                 f"{ns_idx}:loom", f"{ns_idx}:LOOM-01", f"{ns_idx}:warp_tension"
             ])
             value = await var.read_value()
         ```
       - Sezione "Data-diode enforcement": link a `tests/integration/test_data_diode.py` + 3 layer descrizione concisa.

    4. **docs/docs/en/it-ot/{index,ingest-schema,opcua-schema}.md** — EN mirror dei 3 file IT. Traduzione fedele (NOT auto-generated — traduzione manuale di base, accettabile bullet-points).

       Format: stessa struttura, stessi anchors (per cross-link `[See IT version](../it-ot/...)` o lasciare link automatici).

    5. **docs/mkdocs.yml** — Estendere `nav`:
       ```yaml
       nav:
         # Esistente
         - Home: index.md
         - ...
         - IT/OT:
           - Panoramica: it-ot/index.md
           - Schema ingest: it-ot/ingest-schema.md
           - Schema OPC-UA: it-ot/opcua-schema.md
       ```
       Per EN: stessa struttura ma sotto `docs/docs/en/`. Verificare che il plugin i18n Phase 1 (mkdocs-material i18n) catturi automaticamente l'EN mirror — verificare in `01-07-mkdocs-PLAN.md` come è configurato.

    Decisioni: IOT-09 chiusura (asset registry + tag dictionary + UoM table + sample event JSON + ingest schema documented); D-15 i18n bilingue mantenuto.
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && cd docs && mkdocs build --strict 2>&1 | tail -10 && cd .. && test -f docs/docs/it-ot/ingest-schema.md && test -f docs/docs/en/it-ot/ingest-schema.md && test -f docs/docs/it-ot/opcua-schema.md && grep -c "it-ot" docs/mkdocs.yml</automated>
  </verify>
  <acceptance_criteria>
    - `cd docs && mkdocs build --strict` exits 0 (no warning, no broken link).
    - `test -f docs/docs/it-ot/index.md` exit 0.
    - `test -f docs/docs/it-ot/ingest-schema.md` exit 0; `wc -l docs/docs/it-ot/ingest-schema.md` ≥ 80.
    - `test -f docs/docs/it-ot/opcua-schema.md` exit 0.
    - `test -f docs/docs/en/it-ot/index.md` exit 0.
    - `test -f docs/docs/en/it-ot/ingest-schema.md` exit 0; `wc -l docs/docs/en/it-ot/ingest-schema.md` ≥ 80.
    - `test -f docs/docs/en/it-ot/opcua-schema.md` exit 0.
    - `grep -c "urn:mantis" docs/docs/it-ot/opcua-schema.md` ≥ 1.
    - `grep -c "sensor.events" docs/docs/it-ot/ingest-schema.md` ≥ 1.
    - `grep -c "asset_id" docs/docs/it-ot/ingest-schema.md` ≥ 1.
    - `grep -c "it-ot" docs/mkdocs.yml` ≥ 2 (IT + EN nav entries).
  </acceptance_criteria>
  <done>3 pagine MkDocs IT + 3 pagine EN mirror; `mkdocs build --strict` verde; nav esteso; IOT-09 chiuso.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Docs preview review + full load test sanity</name>
  <files>(no file changes)</files>
  <read_first>
    - docs/docs/it-ot/ingest-schema.md (Task 2)
    - docs/docs/en/it-ot/ingest-schema.md (Task 2)
    - tests/load/test_ingestion_throughput.py (Task 1)
  </read_first>
  <what-built>
    - Full 5k×60s load test PR-label gated (Task 1).
    - 3 pagine MkDocs IT + 3 EN mirror per IOT-09 (Task 2).
    - CI workflow extended con full-load-test step opzionale (Task 1).
  </what-built>
  <how-to-verify>
    Eseguire SEQUENZIALMENTE:

    1. **MkDocs preview locale**:
       ```bash
       cd docs && mkdocs serve --strict
       ```
       Aprire `http://localhost:8000` e navigare alle 3 nuove pagine IT (Panoramica, Schema ingest, Schema OPC-UA). Verificare:
       - [ ] Diagramma Mermaid in `index.md` renderizza correttamente.
       - [ ] Tabella asset registry in `ingest-schema.md` è leggibile.
       - [ ] Esempio JSON SensorEvent + esempio SQL renderizzano con highlighting.
       - [ ] Sezione "NATS subject hierarchy" mostra i 3 pattern.
       - [ ] Link interni funzionanti.

    2. **Verificare EN mirror**: navigare alla versione EN delle 3 pagine. Verificare contenuto presente e coerente con IT.

    3. **Strict build**:
       ```bash
       mkdocs build --strict 2>&1 | tee /tmp/mkdocs-build.log
       grep -i "warning\|broken" /tmp/mkdocs-build.log
       ```
       Atteso: 0 warning/broken.

    4. **Full load test sanity (OPZIONALE — richiede docker + ~2-3 min)**:
       ```bash
       # Stack già su da Plan 03-06 checkpoint? Se no:
       docker compose -f infra/compose/core.yml -f infra/compose/sim.yml up -d --wait
       python3 scripts/timescale-migrate.py
       python3 scripts/nats-bootstrap-streams.py
       # Run full test:
       uv run pytest tests/load/test_ingestion_throughput.py -v -m load_full --full-load-test
       ```
       Atteso: PASSED con stampa `FULL LOAD: total≈300000, p99<200ms`. Se p99 ≥ 200ms: documentare in PR description; eventualmente switch a COPY fallback (RESEARCH §Pattern 4 alternativa) in plan retrospective Phase 11.

    5. **Tear down**:
       ```bash
       docker compose -f infra/compose/core.yml -f infra/compose/sim.yml down -v
       ```

  </how-to-verify>
  <resume-signal>
    Type "approved" se docs render bene e (opzionalmente) full load test green. Type "approved-docs-only" se non si esegue full load test localmente (delegato al CI workflow + PR-label `load-test`). Type "blocked: <reason>" se mkdocs strict fallisce o docs hanno gap evidenti.
  </resume-signal>
  <done>Docs IT+EN renderizzano correttamente; full load test sintatticamente eseguibile e (opzionalmente) p99 confermato < 200ms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown content → mkdocs build | Static documentation; nessun input runtime untrusted. |
| Full load test → docker stack | Test consuma risorse stack (5k msg/s × 60s); CI-only on demand via PR label. |
| PR label `load-test` → CI workflow | GitHub event trigger; trust nel maintainer che labella. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-docs-leak | Information Disclosure | `docs/docs/it-ot/*.md` | mitigate | Nessun secret in docs (DSN solo `${...}` placeholder); examples Python NON contengono `TIMESCALE_DSN` raw. |
| T-03-07-full-load-dos | DoS | full load test 5k×60s | accept | PR-label gated (manual trigger); resource budget GitHub Actions standard runner (4 CPU / 16GB RAM sufficient per ~5k msg/s). |
| T-03-07-mkdocs-supply | Tampering | mkdocs build CI | mitigate | mkdocs versions pin from Phase 1 (`01-07-mkdocs-PLAN.md`); `--strict` flag fail-fast on warning. |
| T-03-07-doc-drift | Tampering | ingest-schema.md vs sft-assets/registry.yaml | mitigate | Phase 11 può aggiungere drift-check CI (`--check` flag pattern Phase 2 generators); Phase 3 ships docs manuali con commit annotato `Source: registry.yaml@<commit-sha>`. |

`security_enforcement: enabled` (ASVS V14 limited applicability — config + docs only).
</threat_model>

<verification>
- Task 1: `python3 -c "import ast; ast.parse(...)"` exits 0; CI workflow YAML valido; FULL_RATE/FULL_DURATION/FULL_P99_MS_TARGET configurati.
- Task 2: `mkdocs build --strict` exits 0; 6 file MD presenti (3 IT + 3 EN); ingest-schema ≥ 80 righe.
- Task 3 (checkpoint): docs preview leggibile, opzionalmente full load test verde.
- `make load-test-full` invocabile (richiede docker).
</verification>

<success_criteria>
- IOT-09 CHIUSO: ingest schema (asset registry, tag dictionary, UoM, sample event JSON, NATS subjects, hypertable schema) documentato in MkDocs bilingue.
- IOT-10 CHIUSO: full 5k×60s load test definito, PR-label gated in CI; smoke 1k×10s green permanente (Plan 03-06).
- D-15 (MkDocs i18n bilingue) rispettato: nav IT + EN entries.
- Phase 3 ready for retrospective + Phase 4 unblock.
</success_criteria>

<output>
Create `.planning/phases/03-it-ot-simulation-layer/03-07-SUMMARY.md` con:
- File creati/modificati
- Output `mkdocs build --strict`
- (Opzionale) Output `uv run pytest tests/load/test_ingestion_throughput.py --full-load-test` con p99 misurato
- Conferma checkpoint approved
- Lista requirement IDs chiusi (IOT-09, IOT-10) — coverage finale Phase 3
</output>
