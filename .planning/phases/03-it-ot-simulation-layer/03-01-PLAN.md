---
phase: 03-it-ot-simulation-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sft-assets/pyproject.toml
  - packages/sft-assets/project.json
  - packages/sft-assets/package.json
  - packages/sft-assets/src/sft_assets/__init__.py
  - packages/sft-assets/src/sft_assets/_models.py
  - packages/sft-assets/src/sft_assets/models.py
  - packages/sft-assets/src/sft_assets/_loader.py
  - packages/sft-assets/src/sft_assets/loader.py
  - packages/sft-assets/src/sft_assets/registry.yaml
  - packages/sft-assets/src/sft_assets/schemas/asset.schema.json
  - packages/sft-assets/tests/conftest.py
  - packages/sft-assets/tests/test_models.py
  - packages/sft-assets/tests/test_loader.py
  - packages/sft-assets/tests/test_registry_validation.py
  - scripts/validate-asset-registry.py
  - Makefile
autonomous: true
requirements:
  - IOT-09
tags:
  - asset-registry
  - pydantic
  - json-schema
  - tag-dictionary
user_setup: []

must_haves:
  truths:
    - "Il pacchetto Nx `sft-assets` esiste come `library` ed è importabile da sim-textile, ot-bridge, sft-tools, agents Phase 4+"
    - "Il file `registry.yaml` contiene almeno 30 asset (12 LOOM + 8 SPIN + 4 WARP + 4 DYE + 2 STEN) e ≥50 tag distribuiti su 5 asset_family"
    - "`load_assets()` ritorna `tuple[Asset, ...]` valide; `load_tag_dict()` ritorna lookup O(1) `dict[str, Tag]` con assertion di consistenza inter-asset"
    - "Il registry YAML è validato in pytest e dallo script CLI `scripts/validate-asset-registry.py` contro `asset.schema.json` Draft 2020-12"
    - "`Asset.opcua_namespace` ha validator che impone prefisso `urn:mantis:`"
    - "Nessun match per `yaml.load(` in `packages/sft-assets/` (solo `yaml.safe_load`)"
  artifacts:
    - path: "packages/sft-assets/pyproject.toml"
      provides: "Nx Python project config con deps pydantic, pyyaml, jsonschema"
      contains: "name = \"sft-assets\""
    - path: "packages/sft-assets/project.json"
      provides: "Nx targets test, lint, validate-registry"
      contains: "validate-registry"
    - path: "packages/sft-assets/src/sft_assets/_models.py"
      provides: "Pydantic v2 frozen Asset, Tag, TagRef, AssetFamily enum, SemanticType enum"
      contains: "model_config"
    - path: "packages/sft-assets/src/sft_assets/_loader.py"
      provides: "load_assets, load_assets_dict, load_tag_dict con lru_cache(maxsize=1) + invalidate_cache()"
      contains: "yaml.safe_load"
    - path: "packages/sft-assets/src/sft_assets/registry.yaml"
      provides: "~30 asset Mantis-realistic con ~50 tag"
      contains: "LOOM-01"
      min_lines: 200
    - path: "packages/sft-assets/src/sft_assets/schemas/asset.schema.json"
      provides: "JSON Schema Draft 2020-12 per Asset entity con $id e additionalProperties=false"
      contains: "\"$schema\": \"https://json-schema.org/draft/2020-12/schema\""
    - path: "packages/sft-assets/tests/test_registry_validation.py"
      provides: "Test pytest che valida registry.yaml contro asset.schema.json + asset_id uniqueness"
    - path: "scripts/validate-asset-registry.py"
      provides: "CLI validator standalone con argparse + --dry-run + exit codes documentati"
      contains: "Draft202012Validator"
  key_links:
    - from: "packages/sft-assets/src/sft_assets/loader.py"
      to: "packages/sft-assets/src/sft_assets/registry.yaml"
      via: "yaml.safe_load + Pydantic model_validate"
      pattern: "yaml\\.safe_load"
    - from: "packages/sft-assets/tests/test_registry_validation.py"
      to: "packages/sft-assets/src/sft_assets/schemas/asset.schema.json"
      via: "jsonschema.Draft202012Validator.iter_errors"
      pattern: "Draft202012Validator"
    - from: "scripts/validate-asset-registry.py"
      to: "packages/sft-assets/src/sft_assets/registry.yaml"
      via: "WORKSPACE_ROOT relative path"
      pattern: "WORKSPACE_ROOT"
---

<objective>
Creare il pacchetto Nx `packages/sft-assets/` (single source of truth platform metadata) con modelli Pydantic v2 frozen, loader YAML idempotente, JSON Schema Draft 2020-12, seed di ~30 asset Mantis-realistic + ~50 tag, test pytest e script CLI di validazione.

Purpose (per D-45 + IOT-09): centralizza asset_id/asset_family/opcua_namespace/tag/UoM per consentire a `sim-textile` di generare nodi OPC-UA, a `ot-bridge` di derivare routing key NATS e ai tool/agent Phase 4+ di filtrare eventi. Distinto da `sft-domain` (concetti di dominio) che resta intoccato.

Output: pacchetto importabile (`from sft_assets import Asset, Tag, load_assets, load_assets_dict, load_tag_dict`), registry seed committato, schema validato in pytest + CLI script, target Nx + Makefile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md
@.planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md
@.planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md
@.planning/phases/03-it-ot-simulation-layer/03-VALIDATION.md

# Analoghi Phase 2 (sft-domain è canonico per nuovi package)
@packages/sft-domain/pyproject.toml
@packages/sft-domain/project.json
@packages/sft-domain/package.json
@packages/sft-domain/src/sft_domain/glossary/__init__.py
@packages/sft-domain/src/sft_domain/glossary/_models.py
@packages/sft-domain/src/sft_domain/glossary/_loader.py
@packages/sft-domain/src/sft_domain/glossary/models.py
@packages/sft-domain/src/sft_domain/glossary/loader.py
@packages/sft-domain/src/sft_domain/schemas/glossary.schema.json
@packages/sft-domain/tests/conftest.py
@packages/sft-domain/tests/test_glossary_models.py
@packages/sft-domain/tests/test_glossary_loader.py
@packages/sft-domain/tests/test_glossary_schema.py
@scripts/validate-glossary-schema.py
@Makefile

<interfaces>
<!-- Esportazioni canoniche da Phase 2 da copiare/adattare. Executor: NON esplorare il codebase, usa questi. -->

From `packages/sft-domain/src/sft_domain/glossary/_models.py`:
- `class Category(str, Enum)` con 9 valori (`textile-process`, `textile-asset`, ecc.)
- `class Term(BaseModel)` con `model_config = {"frozen": True, "extra": "forbid"}` e `Annotated[..., Field(...)]`

From `packages/sft-domain/src/sft_domain/glossary/_loader.py`:
- `@lru_cache(maxsize=1) def load_terms(lang) -> tuple[Term, ...]`
- `def invalidate_cache() -> None`

From `packages/sft-domain/src/sft_domain/glossary/__init__.py`:
- Pattern barrel: `from sft_domain.glossary._loader import load_terms, load_terms_dict` + `__all__ = [...]`

Nuovi tipi da esportare in `sft_assets/__init__.py`:
- `Asset, Tag, TagRef, AssetFamily, SemanticType, load_assets, load_assets_dict, load_tag_dict, invalidate_cache`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold pacchetto Nx sft-assets + Pydantic models + JSON Schema + barrel exports</name>
  <files>packages/sft-assets/pyproject.toml, packages/sft-assets/project.json, packages/sft-assets/package.json, packages/sft-assets/src/sft_assets/__init__.py, packages/sft-assets/src/sft_assets/_models.py, packages/sft-assets/src/sft_assets/models.py, packages/sft-assets/src/sft_assets/_loader.py, packages/sft-assets/src/sft_assets/loader.py, packages/sft-assets/src/sft_assets/schemas/asset.schema.json, packages/sft-assets/tests/conftest.py, packages/sft-assets/tests/test_models.py, packages/sft-assets/tests/test_loader.py, packages/sft-assets/tests/test_registry_validation.py</files>
  <read_first>
    - packages/sft-domain/pyproject.toml (analog esatto, lines 1-28)
    - packages/sft-domain/project.json (analog esatto, lines 1-30)
    - packages/sft-domain/package.json (analog esatto)
    - packages/sft-domain/src/sft_domain/glossary/_models.py (analog Pydantic frozen, lines 1-69)
    - packages/sft-domain/src/sft_domain/glossary/_loader.py (analog lru_cache + yaml.safe_load, lines 1-80)
    - packages/sft-domain/src/sft_domain/glossary/models.py + loader.py (analog re-export Pattern S-7)
    - packages/sft-domain/src/sft_domain/glossary/__init__.py (analog barrel, lines 1-13)
    - packages/sft-domain/src/sft_domain/schemas/glossary.schema.json (analog Draft 2020-12, lines 1-35)
    - packages/sft-domain/tests/conftest.py + test_glossary_models.py + test_glossary_loader.py + test_glossary_schema.py
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md sezioni "models.py" (lines 92-144), "loader.py" (lines 148-218), "asset.schema.json" (lines 220-251), "pyproject.toml" (lines 254-296), "project.json" (lines 299-345), "barrel" (lines 347-369), "conftest.py" (lines 521-572), "test_registry_validation.py" (lines 575-624)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md sezione D-45 (asset registry layout) + claudes_discretion (30 asset seed breakdown)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Security checks 2 (Asset.opcua_namespace deve iniziare con `urn:mantis:`)
  </read_first>
  <behavior>
    - Test 1 (test_models.py): `Asset(asset_id="LOOM-01", asset_family="loom", line_id="weaving-line-1", opcua_namespace="urn:mantis:loom:LOOM-01", tags=(...))` istanziato OK; mutazione `asset.asset_id = "X"` solleva `ValidationError` (frozen=True).
    - Test 2 (test_models.py): `Asset(..., opcua_namespace="http://x")` solleva `ValidationError` (validator prefisso `urn:mantis:` RESEARCH §Security checks 2).
    - Test 3 (test_models.py): `Asset(..., extra_field="x")` solleva `ValidationError` (extra="forbid").
    - Test 4 (test_models.py): `AssetFamily` enum contiene esattamente 5 valori `loom`, `spinning`, `warping`, `dyeing`, `finishing` (D-21 + D-44).
    - Test 5 (test_loader.py): `load_assets()` ritorna `tuple[Asset, ...]` non vuota; chiamato 2 volte ritorna stesso oggetto (lru_cache).
    - Test 6 (test_loader.py): `load_assets_dict()` ha key uguali a `asset.asset_id`; `load_tag_dict()` solleva AssertionError se due asset hanno stesso tag_id con definizioni divergenti (consistency check).
    - Test 7 (test_loader.py): `invalidate_cache()` rimuove cache → ricaricamento dopo modifica fixture path (uso tmp_path + monkeypatch del `_REGISTRY_PATH`).
    - Test 8 (test_registry_validation.py): `asset.schema.json` è auto-valido (`Draft202012Validator.check_schema(schema) is None`).
    - Test 9 (test_registry_validation.py): ogni entry di `registry.yaml` valida via `Draft202012Validator.iter_errors` → 0 errori.
    - Test 10 (test_registry_validation.py): tutti gli `asset_id` in `registry.yaml` sono unici (case-sensitive).
  </behavior>
  <action>
    Replicare layout `packages/sft-domain/` per `packages/sft-assets/`. In dettaglio:

    1. `pyproject.toml` — `name = "sft-assets"`, `version = "0.1.0"`, `requires-python = ">=3.12,<3.13"`, dependencies `["pydantic>=2.13.4", "pyyaml>=6.0", "jsonschema>=4.23"]`, dev `["pytest>=8.0", "pytest-asyncio>=0.24"]`, build system hatchling con `packages = ["src/sft_assets"]`, `[tool.pytest.ini_options]` con `asyncio_mode = "auto"` e `testpaths = ["tests"]` (PATTERNS lines 254-296).

    2. `project.json` — Nx project `name: "sft-assets"`, `projectType: "library"`, `sourceRoot: "packages/sft-assets/src"`, targets `test` (uv run pytest), `lint` (uv run ruff check src), `validate-registry` (`python3 ../../scripts/validate-asset-registry.py`, cwd ".") (PATTERNS lines 299-345).

    3. `package.json` — analogo a `packages/sft-domain/package.json` (name `@sft/sft-assets`, version `0.1.0`).

    4. `_models.py` (private) — enum `AssetFamily(str, Enum)` con 5 valori `LOOM="loom"`, `SPINNING="spinning"`, `WARPING="warping"`, `DYEING="dyeing"`, `FINISHING="finishing"`. Enum `SemanticType(str, Enum)` con valori `tension`, `density`, `temperature`, `humidity`, `vibration`, `speed`, `pressure`, `flow`, `ph`, `level` (10 valori canonici). Classe `Tag(BaseModel)` frozen + extra=forbid con campi `tag_id: Annotated[str, Field(min_length=1, pattern=r"^[a-z][a-z0-9_]+$")]`, `unit: Annotated[str, Field(min_length=1)]`, `sample_rate_hz: Annotated[float, Field(gt=0, le=1000)]`, `semantic_type: SemanticType`. Classe `Asset(BaseModel)` frozen + extra=forbid con campi `asset_id: Annotated[str, Field(min_length=1, pattern=r"^[A-Z]+-\d{2}$")]`, `asset_family: AssetFamily`, `line_id: str`, `opcua_namespace: Annotated[str, Field(min_length=1)]` con `@field_validator("opcua_namespace")` che assert startswith `"urn:mantis:"`, `tags: tuple[Tag, ...] = Field(default_factory=tuple)`, `status: Annotated[str, Field(pattern=r"^(active|inactive|maintenance)$")] = "active"`. NO `model_config = {"frozen": True, "extra": "forbid"}` come dict literal (replica esatta pattern Phase 2).

    5. `models.py` (public re-export, Pattern S-7) — `from sft_assets._models import Asset, Tag, AssetFamily, SemanticType` + `__all__`.

    6. `_loader.py` (private) — `WORKSPACE_ROOT` non necessario qui (lookup interno via `Path(__file__).parent / "registry.yaml"`). `@lru_cache(maxsize=1) def load_assets() -> tuple[Asset, ...]` legge `registry.yaml` via `yaml.safe_load`, ritorna `tuple(Asset.model_validate(item) for item in raw)`. `@lru_cache(maxsize=1) def load_assets_dict() -> dict[str, Asset]` mappa per `asset_id`. `@lru_cache(maxsize=1) def load_tag_dict() -> dict[str, Tag]` itera assets+tags + assert consistency se stesso tag_id appare in più asset (RESEARCH §Code Examples lines 866-875). `def invalidate_cache() -> None` chiama `load_assets.cache_clear()`, `load_assets_dict.cache_clear()`, `load_tag_dict.cache_clear()` (per test).

    7. `loader.py` (public re-export) — `from sft_assets._loader import load_assets, load_assets_dict, load_tag_dict, invalidate_cache` + `__all__`.

    8. `__init__.py` — `from sft_assets._models import Asset, Tag, AssetFamily, SemanticType` + `from sft_assets._loader import load_assets, load_assets_dict, load_tag_dict, invalidate_cache` + `__all__`.

    9. `schemas/asset.schema.json` — JSON Schema Draft 2020-12 con `$id="https://sft-assets/schemas/asset.schema.json"`, `title="SFT Asset"`, `type: "object"`, `required: ["asset_id","asset_family","line_id","opcua_namespace","tags"]`, `additionalProperties: false`. Properties: `asset_id` (pattern `^[A-Z]+-\d{2}$`), `asset_family` (enum 5 valori), `line_id` (string), `opcua_namespace` (pattern `^urn:mantis:`), `tags` (array di objects con required `tag_id, unit, sample_rate_hz, semantic_type` e additionalProperties=false), `status` (enum `["active","inactive","maintenance"]`, default `active`). Pattern PATTERNS lines 220-251.

    10. `tests/conftest.py` — fixtures `sample_asset_dict` (LOOM-01 con 2 tag warp_tension/pick_density), `sample_tag_dict`, `sample_registry_minimal` (lista con 1 asset valido). PATTERNS lines 521-572.

    11. `tests/test_models.py` — i 4 test su Asset frozen, namespace validator, extra=forbid, AssetFamily 5 valori.

    12. `tests/test_loader.py` — i 3 test su lru_cache + dict + consistency + invalidate_cache (usa monkeypatch per puntare a fixture YAML temporaneo).

    13. `tests/test_registry_validation.py` — i 3 test su schema auto-valido + registry valido contro schema + asset_id uniqueness (PATTERNS lines 575-624).

    CRITICAL: usare SOLO `yaml.safe_load` mai `yaml.load`. Tutti i datetime (se aggiunti) devono essere tz-aware (RESEARCH §Pitfall 7) — per Phase 3-01 non ci sono datetime nei modelli. Decisioni implementate: D-45 (registry location + Pydantic+JSON Schema), parte di IOT-09 (asset registry schema documented).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && uv run --project packages/sft-assets pytest packages/sft-assets/tests -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test packages/sft-assets/pyproject.toml` esiste e contiene la stringa `name = "sft-assets"` (`grep -c 'name = "sft-assets"' packages/sft-assets/pyproject.toml` ≥ 1).
    - `test packages/sft-assets/src/sft_assets/__init__.py` esporta `Asset, Tag, AssetFamily, load_assets, load_assets_dict, load_tag_dict, invalidate_cache` (`grep -v '^#' packages/sft-assets/src/sft_assets/__init__.py | grep -c 'load_assets_dict'` ≥ 1).
    - `test asset.schema.json` dichiara Draft 2020-12 (`grep -c '"https://json-schema.org/draft/2020-12/schema"' packages/sft-assets/src/sft_assets/schemas/asset.schema.json` == 1).
    - `test no unsafe yaml`: `grep -rE "yaml\.load\(" packages/sft-assets/` exit code 1 (no match).
    - `cd packages/sft-assets && uv run pytest tests -x -v` exits 0; stdout contiene "passed" e ≥ 10 test items.
    - `python3 -c "from sft_assets import Asset, Tag, load_assets, load_tag_dict; assert isinstance(load_assets(), tuple)"` exits 0 (post-install).
  </acceptance_criteria>
  <done>Il pacchetto `sft-assets` è importabile, modelli Pydantic frozen sono testati, lo schema JSON è valido e auto-validato, i test loader passano (lru_cache + consistency).</done>
</task>

<task type="auto">
  <name>Task 2: Seed registry.yaml con 30 asset Mantis-realistic + 50 tag + CLI validator + integrazione CI/Makefile</name>
  <files>packages/sft-assets/src/sft_assets/registry.yaml, scripts/validate-asset-registry.py, Makefile, packages/sft-assets/tests/test_registry_validation.py</files>
  <read_first>
    - packages/sft-assets/src/sft_assets/schemas/asset.schema.json (creato da Task 1 — schema target)
    - packages/sft-assets/src/sft_assets/_models.py (creato da Task 1 — Pydantic constraints da rispettare)
    - scripts/validate-glossary-schema.py (analog esatto, lines 1-244 — argparse + Draft202012Validator.iter_errors + exit codes)
    - scripts/sync-python-versions.py (analog WORKSPACE_ROOT + argparse + --dry-run pattern, lines 1-131)
    - Makefile (esistente, sezione validate lines 137-149 + .PHONY line 16)
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md sezione "validate-asset-registry.py" (lines 434-518) + sezione Makefile (lines 752-800)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md claudes_discretion (30 asset breakdown: 12 LOOM-01..12, 8 SPIN-01..08, 4 WARP-01..04, 4 DYE-01..04, 2 STEN-01..02)
  </read_first>
  <action>
    1. **registry.yaml** — Lista YAML di 30 asset secondo breakdown CONTEXT claudes_discretion:
       - 12 looms `LOOM-01..LOOM-12` su `line_id: weaving-line-1` (8) e `weaving-line-2` (4). Ogni loom ha tag `warp_tension` (unit `N`, 10Hz, tension), `pick_density` (unit `picks_per_cm`, 1Hz, density), `creel_speed` (unit `rpm`, 5Hz, speed), `broken_pick_count` (unit `count`, 1Hz, density), `loom_temperature` (unit `degC`, 2Hz, temperature).
       - 8 spinning frames `SPIN-01..SPIN-08` su `line_id: spinning-line-1`. Tag: `spindle_speed` (unit `rpm`, 5Hz, speed), `yarn_tension` (unit `cN`, 5Hz, tension), `roller_temperature` (unit `degC`, 1Hz, temperature), `broken_end_count` (unit `count`, 1Hz, density), `spindle_vibration` (unit `mm_s`, 10Hz, vibration).
       - 4 warpers `WARP-01..WARP-04` su `line_id: warping-line-1`. Tag: `tension_imbalance` (unit `N`, 2Hz, tension), `creel_feed_rate` (unit `m_min`, 2Hz, speed), `warp_count` (unit `count`, 1Hz, density), `winding_speed` (unit `rpm`, 2Hz, speed).
       - 4 dyeing vasche `DYE-01..DYE-04` su `line_id: dyeing-line-1`. Tag: `bath_temperature` (unit `degC`, 1Hz, temperature), `bath_ph` (unit `pH`, 1Hz, ph), `bath_level` (unit `mm`, 1Hz, level), `flow_rate` (unit `l_min`, 1Hz, flow), `recipe_deviation` (unit `pct`, 1Hz, density), `bath_pressure` (unit `bar`, 1Hz, pressure).
       - 2 stenter `STEN-01..STEN-02` su `line_id: finishing-line-1`. Tag: `fabric_tension` (unit `N`, 5Hz, tension), `chamber_temperature` (unit `degC`, 1Hz, temperature), `humidity` (unit `pct_rh`, 1Hz, humidity), `fabric_speed` (unit `m_min`, 2Hz, speed).

       Per ogni asset: `asset_id`, `asset_family`, `line_id`, `opcua_namespace: "urn:mantis:<family>:<asset_id>"`, `tags: [...]`, `status: active`.

       Conteggio finale: 12 + 8 + 4 + 4 + 2 = 30 asset. Tag distinti totali ≥ 24 (5+5+4+6+4 con `loom_temperature`, `roller_temperature`, `bath_temperature`, `chamber_temperature` distinti per family + ogni asset replica i propri 4-6 tag → ≥ 50 occorrenze tag, ≥ 24 distinct tag_id).

       CRITICAL constraint coverage: ogni `asset_id` deve match pattern `^[A-Z]+-\d{2}$`; ogni `tag_id` deve match `^[a-z][a-z0-9_]+$`; ogni `opcua_namespace` parte con `urn:mantis:`. Tag con stesso `tag_id` ma in asset diversi DEVONO avere stesso `unit` + `sample_rate_hz` + `semantic_type` (consistency check di `load_tag_dict`).

    2. **scripts/validate-asset-registry.py** — Copiare struttura `scripts/validate-glossary-schema.py`:
       - Shebang `#!/usr/bin/env python3` + docstring usage + exit codes (0=ok, 1=errore).
       - `WORKSPACE_ROOT = Path(__file__).parent.parent`.
       - `_DEFAULT_REGISTRY = WORKSPACE_ROOT / "packages/sft-assets/src/sft_assets/registry.yaml"`.
       - `_DEFAULT_SCHEMA = WORKSPACE_ROOT / "packages/sft-assets/src/sft_assets/schemas/asset.schema.json"`.
       - argparse: `--registry PATH` (default _DEFAULT_REGISTRY), `--schema PATH` (default _DEFAULT_SCHEMA), `--dry-run` (stampa cosa farebbe senza altro side effect, exit 0).
       - Load YAML via `yaml.safe_load` (PATTERN S-2 obbligatorio).
       - Validate via `Draft202012Validator(schema).iter_errors(entry)` per ogni asset; raccogli errori con field path `" -> ".join(str(p) for p in error.path)` (analog lines 96-116).
       - Uniqueness check: assert `len(asset_ids) == len(set(asset_ids))` con report `[REGISTRY] duplicate asset_id 'LOOM-01' at entries [3, 12]`.
       - Stampa output `OK [packages/sft-assets/src/sft_assets/registry.yaml]: 30 assets valid` o errori dettagliati.
       - Exit 0 se ok, 1 se errori.

    3. **Makefile** — Aggiungere DOPO la sezione validate-* esistente:
       ```
       validate-assets:
           python3 scripts/validate-asset-registry.py

       validate-all: validate-glossary validate-corpus validate-assets
           # (preservare body esistente di validate-all + appendere)
       ```
       NB: modificare la riga `validate-all:` esistente per aggiungere `validate-assets` ai prereq.
       Aggiornare `.PHONY` (line 16) appendendo `validate-assets` alla lista.

    4. **packages/sft-assets/tests/test_registry_validation.py** — Estendere con `test_registry_count_matches_breakdown`: assert che `load_assets()` ritorni esattamente 30 asset e i conteggi per family siano `{loom:12, spinning:8, warping:4, dyeing:4, finishing:2}`. Assert che `len(load_tag_dict()) >= 20` (tag distinti).

    Decisioni implementate: D-45 (30 asset seed Mantis-realistic), D-21 (5 asset_family allineamento Phase 2), IOT-09 (asset registry + tag dictionary + UoM documented and validated).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && python3 scripts/validate-asset-registry.py && uv run --project packages/sft-assets pytest packages/sft-assets/tests -x -v -k registry</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/validate-asset-registry.py` exits 0 e stdout contiene "30 assets valid".
    - `python3 scripts/validate-asset-registry.py --dry-run` exits 0.
    - `grep -c "^- asset_id:" packages/sft-assets/src/sft_assets/registry.yaml` == 30.
    - `grep -c "^  asset_family: loom$" packages/sft-assets/src/sft_assets/registry.yaml` == 12; `^  asset_family: spinning$` == 8; `^  asset_family: warping$` == 4; `^  asset_family: dyeing$` == 4; `^  asset_family: finishing$` == 2.
    - `grep -v '^#' Makefile | grep -c "^validate-assets:"` == 1.
    - `make validate-assets` exits 0.
    - `uv run --project packages/sft-assets pytest packages/sft-assets/tests/test_registry_validation.py -x -v` exits 0 e i test sui count breakdown passano.
  </acceptance_criteria>
  <done>Registry committed con 30 asset reali, CLI validator funzionante, target Makefile attivo, test pytest verifica struttura e conteggi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file → Pydantic loader | `registry.yaml` è artifact di build (no input runtime untrusted), ma deve essere parsato in modo sicuro per evitare RCE su contributor PR malevoli. |
| CLI invocation → file system | `scripts/validate-asset-registry.py` legge file da WORKSPACE_ROOT; nessun escape path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-yaml | Tampering | `_loader.py`, `validate-asset-registry.py` | mitigate | `yaml.safe_load` obbligatorio; CI grep gate `grep -rE "yaml\.load\(" packages/sft-assets` exit 1 (Pattern S-2, RESEARCH §Pitfall Tampering). Riferisce T-V5-yaml. |
| T-03-01-pydantic | Tampering | `_models.py` Asset/Tag | mitigate | `model_config = {"frozen": True, "extra": "forbid"}` + `@field_validator("opcua_namespace")` startswith `urn:mantis:` + pattern regex `^[A-Z]+-\d{2}$` su asset_id. Riferisce T-V5-pydantic. |
| T-03-01-asset | Tampering | `registry.yaml` | mitigate | CLI `scripts/validate-asset-registry.py` + pytest auto-run via `make validate-assets` + pre-commit (Phase 1). Asset_id uniqueness assertion (T-V12-asset). |
| T-03-01-info-leak | Information Disclosure | structlog usage (futuri loader log) | accept | Phase 3-01 non logga credentials/PII; registry.yaml è platform metadata pubblico. |

`security_enforcement: enabled` (ASVS V5 Input Validation). Nessuna installazione npm/pip nuova qui (`pyproject.toml` lista pacchetti già auditati in RESEARCH §Package Legitimacy Audit: pydantic, pyyaml, jsonschema — tutti approved).
</threat_model>

<verification>
- `uv run --project packages/sft-assets pytest packages/sft-assets/tests -x -v` exits 0.
- `python3 scripts/validate-asset-registry.py` exits 0.
- `python3 scripts/validate-asset-registry.py --dry-run` exits 0.
- `make validate-assets` exits 0 (Makefile target wiring).
- `npx nx run sft-assets:test` exits 0 (Nx target wiring).
- `npx nx run sft-assets:validate-registry` exits 0 (Nx target verify).
- `grep -rE "yaml\.load\(" packages/sft-assets/` exits 1 (no match).
- `python3 -c "from sft_assets import load_assets, load_tag_dict; assert len(load_assets()) == 30; td = load_tag_dict(); assert len(td) >= 20"` exits 0.
</verification>

<success_criteria>
- Tutti i task hanno exit 0 sui rispettivi verify automated.
- Frontmatter `must_haves` realizzato: registry yaml committato con 30 asset, loader esposto, schema validato, CI wiring attivo.
- IOT-09 parzialmente coperto: "ingest schema (asset registry, tag dictionary, units of measure)" è documentato come Pydantic+JSON Schema+YAML — la parte MkDocs "Ingest schema" è completata in plan 03-07.
- Nessuna regressione su `packages/sft-domain/` (zero file modificati lì).
</success_criteria>

<output>
Create `.planning/phases/03-it-ot-simulation-layer/03-01-SUMMARY.md` al termine con:
- Path file creati/modificati
- Conteggi asset/tag/test
- Output `make validate-assets` e `pytest`
- Eventuali deviazioni dalle decisioni
</output>
