---
phase: 03-it-ot-simulation-layer
plan: 06
type: execute
wave: 3
depends_on:
  - "03-03"
  - "03-04"
  - "03-05"
files_modified:
  - infra/compose/sim.yml
  - infra/compose/core.yml
  - tests/conftest.py
  - tests/integration/__init__.py
  - tests/integration/conftest.py
  - tests/integration/test_data_diode.py
  - tests/integration/test_opcua_browseable.py
  - tests/integration/test_nats_subjects.py
  - tests/integration/test_e2e_sim_to_timescale.py
  - tests/load/__init__.py
  - tests/load/harness.py
  - tests/load/test_ingestion_smoke.py
  - tests/integration/README.md
  - .github/workflows/ci.yml
  - Makefile
  - pyproject.toml
autonomous: false
requirements:
  - IOT-02
  - IOT-04
  - IOT-05
  - IOT-10
tags:
  - integration
  - data-diode
  - e2e
  - docker-compose
  - dual-network
  - smoke-load
  - ci

must_haves:
  truths:
    - "docker-compose estende `sim.yml` con `sim-textile` e `ot-bridge` services + networks `sft-ot` e `sft-core` — D-51 Layer 1"
    - "NATS si trova SU `sft-core` (NON `sft-sim`) — Phase 1 misalignment risolto qui (PATTERNS line 726)"
    - "sim-textile è SOLO su `sft-ot`; ot-bridge è su `[sft-ot, sft-core]`; nats/timescaledb solo su `sft-core`"
    - "Test data-diode verifica 3 layers: Layer 1 (network ACL via container fake-agent), Layer 2 (pytest enforcement), Layer 3 (grep static analysis) — D-51 + IOT-05"
    - "Test E2E lancia compose, attende healthy, verifica ≥1 row in `sensor_events` entro 30s"
    - "Test NATS subjects subscribe a `sensor.events.>` e verifica formato + stream `SENSOR_EVENTS` esistente"
    - "Test OPC-UA browseable verifica 5 namespace `urn:mantis:<family>` + write rifiutato (set_writable(False))"
    - "Smoke load test 1k msg/s × 10s via custom asyncio harness; assert p99 ingest latency < 200ms — IOT-10 smoke gate"
    - "CI workflow ha step 'Validate IT/OT artifacts' + 'Run IT/OT integration tests' + 'Run IT/OT load test (smoke)'"
  artifacts:
    - path: "infra/compose/sim.yml"
      provides: "Dual-network compose extension (sft-ot + sft-core)"
      contains: "sft-ot"
    - path: "tests/integration/test_data_diode.py"
      provides: "3-layer enforcement test (D-51 Layer 1+2+3)"
      contains: "pytest.raises"
    - path: "tests/integration/test_e2e_sim_to_timescale.py"
      provides: "E2E roundtrip sim → bridge → NATS+Timescale entro 30s"
      contains: "asyncpg"
    - path: "tests/integration/test_opcua_browseable.py"
      provides: "OPC-UA Client connect + browse 5 namespace urn:mantis:*"
      contains: "urn:mantis"
    - path: "tests/integration/test_nats_subjects.py"
      provides: "Subscribe sensor.events.> + verify JetStream stream config"
      contains: "sensor.events"
    - path: "tests/load/harness.py"
      provides: "Custom asyncio publisher harness con asyncpg pool"
      contains: "asyncpg"
    - path: "tests/load/test_ingestion_smoke.py"
      provides: "1k msg/s × 10s smoke gate p99 < 200ms"
      contains: "p99"
    - path: ".github/workflows/ci.yml"
      provides: "Step IT/OT validate + integration + smoke load"
      contains: "IT/OT"
  key_links:
    - from: "infra/compose/sim.yml"
      to: "simulators/sim-textile/Dockerfile + services/ot-bridge/Dockerfile"
      via: "build.context ../../ (monorepo root)"
      pattern: "context:"
    - from: "tests/integration/test_data_diode.py"
      to: "infra/compose/sim.yml dual-network"
      via: "container fake-agent su sft-core only"
      pattern: "TimeoutError|ConnectionRefusedError"
    - from: ".github/workflows/ci.yml"
      to: "tests/integration + tests/load + scripts/timescale-migrate.py"
      via: "docker compose up + pytest"
      pattern: "docker compose"
---

<objective>
Integrare Phase 3 end-to-end: estendere `docker-compose` con `sim-textile`+`ot-bridge` su dual-network (D-51 Layer 1), implementare i 3-layer data-diode test (network ACL + pytest enforcement + grep static analysis), test E2E sim→bridge→NATS+TimescaleDB, smoke load 1k×10s (IOT-10 gate), wiring CI.

Purpose (IOT-02/04/05/10 + D-51 enforcement): chiudere il loop OT↔IT end-to-end, dimostrare data-diode con test runtime, definire il smoke gate CI permanente (full 5k×60s in Plan 03-07).

Output: `make integration-test` lancia full stack + green test; CI nuovi step green; D-51 3-layer verificato.

**HAS CHECKPOINT**: 1 `checkpoint:human-verify` finale per validazione manuale dello stack avviato + log inspection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md
@.planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md
@.planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md
@.planning/phases/03-it-ot-simulation-layer/03-VALIDATION.md
@.planning/phases/03-it-ot-simulation-layer/03-03-PLAN.md
@.planning/phases/03-it-ot-simulation-layer/03-04-PLAN.md
@.planning/phases/03-it-ot-simulation-layer/03-05-PLAN.md

# Existing infra
@infra/compose/core.yml
@infra/compose/sim.yml
@.github/workflows/ci.yml
@Makefile
@tests/conftest.py

<interfaces>
<!-- Final docker-compose topology -->

Networks (D-51 Layer 1):
- `sft-ot` (rinominato/aliased da `sft-sim`): bridge driver
- `sft-core` (esistente Phase 1): it-network equivalent

Service network assignments:
| Service       | Networks               | Notes |
|---------------|------------------------|-------|
| sim-textile   | sft-ot                 | OPC-UA :4840, metrics :8080 |
| ot-bridge     | sft-ot, sft-core       | unique dual-network |
| nats          | sft-core               | MOVE from sft-sim (PATTERNS line 726 fix) |
| timescaledb   | sft-core               | confermato Phase 1 |

Build context: `context: ../../` per entrambi i Dockerfile (monorepo root — Dockerfile copia `packages/sft-assets`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose dual-network extension + NATS realignment + Makefile target up-it-ot</name>
  <files>infra/compose/sim.yml, infra/compose/core.yml, Makefile, tests/integration/README.md</files>
  <read_first>
    - infra/compose/sim.yml (esistente — pattern healthcheck + nats su sft-sim attualmente)
    - infra/compose/core.yml (esistente — verifica nats NON è duplicato qui)
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md sezione "infra/compose/sim.yml" (lines 679-748) — CRITICAL nat misalignment fix
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Code Examples "docker-compose dual-network excerpt" (lines 940-993)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 4 (`internal: true` non L4 enforcement)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-51 + claudes_discretion (network naming sft-ot/sft-core)
    - .planning/phases/01-foundation-monorepo/01-02-compose-SUMMARY.md (Phase 1 services scaffolded)
  </read_first>
  <action>
    1. **infra/compose/sim.yml** — Estensione completa.

       Top-level `networks` block: dichiarare `sft-ot` (driver bridge) + `sft-core` con `external: true` (definita in core.yml). Rimuovere riferimenti a `sft-sim` (rename a `sft-ot` o alias se necessario per backward-compat).

       Service `nats` (esistente): cambiare `networks: [sft-sim]` → `networks: [sft-core]` (CRITICAL alignment D-51). Mantenere healthcheck, command, volumes invariati.

       Aggiungere service `sim-textile`:
       - `build.context: ../..` (monorepo root); `build.dockerfile: simulators/sim-textile/Dockerfile`.
       - `image: sft-sim-textile:dev`; `container_name: sft-sim-textile`.
       - environment: `SIM_PROFILES=${SIM_PROFILES:-loom,spinning,warping,dyeing,finishing}`, `SIM_TIME_SCALE=${SIM_TIME_SCALE:-1.0}`, `OPCUA_BIND=0.0.0.0:4840`, `METRICS_PORT=8080`.
       - ports: `${OPCUA_PORT:-4840}:4840`, `${SIM_METRICS_PORT:-8081}:8080`.
       - `networks: [sft-ot]` ONLY.
       - `restart: unless-stopped`.

       Aggiungere service `ot-bridge`:
       - `build.context: ../..`; `build.dockerfile: services/ot-bridge/Dockerfile`.
       - `image: sft-ot-bridge:dev`; `container_name: sft-ot-bridge`.
       - environment: `OPCUA_URL=opc.tcp://sim-textile:4840`, `NATS_URL=nats://nats:4222`, `TIMESCALE_DSN=postgresql://${POSTGRES_USER:-sft}:${POSTGRES_PASSWORD:-sft_dev_pass}@timescaledb:5432/${POSTGRES_DB:-sft}`, `METRICS_PORT=8080`.
       - ports: `${BRIDGE_METRICS_PORT:-8082}:8080`.
       - `networks: [sft-ot, sft-core]` (UNIQUE dual-network).
       - `depends_on`: `sim-textile (service_started)`, `nats (service_healthy)`, `timescaledb (service_healthy)`.

       Mantenere `volumes: nats-data:` (riusato).

    2. **infra/compose/core.yml** (modify se serve) — Aggiungere blocco top-level `networks: sft-core: {name: sft-core, driver: bridge}` se non già presente; ogni service esistente (`timescaledb`, `postgres`) deve avere `networks: [sft-core]`.

    3. **Makefile** — Aggiungere target Phase 3 (usare tab-indent canonical):
       - `up-it-ot`: `docker compose -f infra/compose/core.yml -f infra/compose/sim.yml up -d --wait`.
       - `down-it-ot`: `docker compose ... down -v`.
       - `integration-test`: up-it-ot → `uv run pytest tests/integration/ -v -m integration` → down-it-ot.
       - `smoke-load`: up-it-ot → `uv run pytest tests/load/test_ingestion_smoke.py -v` → down-it-ot.
       Aggiungere `up-it-ot down-it-ot integration-test smoke-load` a `.PHONY` line 16.

    4. **tests/integration/README.md** — Documento breve con:
       - Prerequisiti (docker, `make migrate-timescale` eseguito).
       - Run command (`make integration-test`).
       - Test list (data_diode, opcua_browseable, nats_subjects, e2e_sim_to_timescale).
       - Edge cases: GitHub Actions services context (D-51 Layer 1 testato in CI via docker-compose stesso); Docker DNS shared caveat (RESEARCH §Pitfall 4 / Assumption A5).

    Decisioni: D-51 Layer 1 (network ACL via compose), Pitfall 4 mitigation (3-layer combinato), PATTERNS line 726 (NATS realignment sft-sim → sft-core).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && docker compose -f infra/compose/core.yml -f infra/compose/sim.yml config > /tmp/compose-resolved.yml; grep -c "sft-ot" /tmp/compose-resolved.yml && grep -c "sft-core" /tmp/compose-resolved.yml</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f infra/compose/core.yml -f infra/compose/sim.yml config` exits 0 (compose YAML valido).
    - Compose resolved: `sim-textile` ha networks `[sft-ot]` SOLO.
    - Compose resolved: `ot-bridge` ha networks `[sft-ot, sft-core]`.
    - Compose resolved: `nats` ha networks `[sft-core]` (NON `sft-sim`).
    - Compose resolved: `timescaledb` ha networks `[sft-core]`.
    - `grep -c "context: ../.." infra/compose/sim.yml` ≥ 2 (sim-textile + ot-bridge build context monorepo-root).
    - `grep -v '^#' Makefile | grep -c "^up-it-ot:"` == 1.
    - `grep -v '^#' Makefile | grep -c "^integration-test:"` == 1.
    - `test -f tests/integration/README.md` exit 0.
  </acceptance_criteria>
  <done>Compose dual-network configurato; NATS realineato su sft-core; Makefile target attivi; README scritto.</done>
</task>

<task type="auto">
  <name>Task 2: 3-layer data-diode test + OPC-UA browseable + NATS subjects + E2E + smoke load harness</name>
  <files>tests/conftest.py, tests/integration/__init__.py, tests/integration/conftest.py, tests/integration/test_data_diode.py, tests/integration/test_opcua_browseable.py, tests/integration/test_nats_subjects.py, tests/integration/test_e2e_sim_to_timescale.py, tests/load/__init__.py, tests/load/harness.py, tests/load/test_ingestion_smoke.py</files>
  <read_first>
    - tests/conftest.py (esistente — pattern Phase 2 path fixture)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Code Examples "pytest data-diode integration test" (lines 995-1032) — COPY exact struct
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-51 (3 layers detailed) + D-48 (load scenario steady-state asyncio harness)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 3 (NATS consumer durable + ack) + §Pattern 4 (asyncpg pool batch)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 4 (docker DNS shared edge — Layer 1 fake-agent container ESPLICITO con --network sft-core)
    - services/ot-bridge/src/svc_ot_bridge/ (Plan 03-04 runtime dep)
    - simulators/sim-textile/src/sim_textile/ (Plan 03-03 runtime dep)
  </read_first>
  <action>
    1. **tests/conftest.py** (modify, estensione non-rottura) — Aggiungere fixture session-scoped `compose_stack`:
       - Detect `docker info` rc != 0 → `pytest.skip("docker not available")`.
       - `subprocess.run(["docker","compose","-f","infra/compose/core.yml","-f","infra/compose/sim.yml","up","-d","--wait"], check=True)`.
       - `time.sleep(5)` (estensione per sim-textile emission warmup).
       - Yield dict `{TIMESCALE_DSN, NATS_URL, OPCUA_URL}` con localhost endpoints.
       - Teardown: `docker compose ... down -v`.
       Registrare marker `pytest.mark.integration` e `pytest.mark.load` via `pytest_configure(config)`.

    2. **tests/integration/{__init__,conftest}.py** — empty + marker re-export se necessario.

    3. **tests/integration/test_data_diode.py** — IOT-05 D-51 3-layer enforcement. Replica RESEARCH §Code Examples lines 995-1032:
       - `test_layer1_agent_cannot_reach_sim_via_it_network(compose_stack)`: lancia subprocess `docker run --rm --network sft-core python:3.12-slim python -c "..."` con script Python inline che tenta `asyncua.Client('opc.tcp://sft-sim-textile:4840')` entro `asyncio.timeout(5)`. Assertion: subprocess returncode 0 ed expect `OK_BLOCKED` in stdout (script Python catch `ConnectionRefusedError | asyncio.TimeoutError | OSError | socket.gaierror` → print OK_BLOCKED + exit 0; succesful connect → exit 99).
       - `test_layer2_agent_cannot_open_opcua_session(compose_stack)`: `asyncua.Client(url="opc.tcp://sft-sim-textile:4840", timeout=5)` host-side; aspettare `pytest.raises((ConnectionRefusedError, asyncio.TimeoutError, OSError))`. Documentare caveat A5 (host docker DNS resolution).
       - `test_layer3_ot_bridge_has_no_opcua_write_calls()`: `subprocess.run(["grep","-rE","set_value|write_attribute|write_value","services/ot-bridge/src/"])`; assert returncode == 1 (no match = grep exit 1).

    4. **tests/integration/test_opcua_browseable.py** — IOT-02:
       - `test_namespaces_browseable(compose_stack)`: `asyncua.Client(...)`; `ns_array = await client.get_namespace_array()`; per ogni `fam` in 5 family, assert `f"urn:mantis:{fam}" in ns_array`.
       - `test_loom_01_variable_writable_false(compose_stack)`: connetti, naviga al Variable `LOOM-01/warp_tension`, tenta `await var.write_value(99.9)` — `pytest.raises(asyncua.ua.UaStatusCodeError)` (protocol-level data-diode in azione).

    5. **tests/integration/test_nats_subjects.py** — IOT-04 + D-52:
       - `test_jetstream_stream_exists(compose_stack)`: `nats.connect(...)`; `js.stream_info("SENSOR_EVENTS")` → assert non-None e `"sensor.events.>"` in `info.config.subjects`.
       - `test_event_subject_format(compose_stack)`: subscribe a `sensor.events.>` con durable consumer `test_subject_check`; callback raccoglie subjects; poll fino a 30s o primo evento; assert received subjects matchano formato `sensor.events.<family>.<asset_id>.<tag_id>` (5 parts, parts[0:2] = ["sensor","events"], parts[2] in 5 family names).

    6. **tests/integration/test_e2e_sim_to_timescale.py** — IOT-06 + IOT-04 roundtrip:
       - `test_sim_to_timescale_roundtrip(compose_stack)`: `await asyncio.sleep(10)` (allow buffer flush); `asyncpg.connect(DSN, statement_cache_size=0)`; `await conn.fetchrow("SELECT COUNT(*) AS n FROM sensor_events WHERE timestamp_utc > NOW() - INTERVAL '1 minute' AND source = $1", "live")`; assert `n > 0`. Verify ≥1 distinct asset_id ingested.

    7. **tests/load/__init__.py** — empty.

    8. **tests/load/harness.py** — Custom asyncio publisher D-48:
       - `@dataclass class LoadResult` con `total_events, duration_s, p50_ms, p99_ms, rate_per_s`.
       - `async def run_load(dsn, nats_url, target_rate, duration_s, assets) -> LoadResult`:
         - `nc = await nats.connect(nats_url)`; `js = nc.jetstream()`.
         - `pool = await asyncpg.create_pool(dsn, min_size=10, max_size=20, statement_cache_size=0)`.
         - Loop principale: per `target_rate * duration_s` eventi totali, costruisci payload JSON `{asset_id, asset_family, tag_id, timestamp_utc(UTC isoformat), value, unit, quality_code, source:"live", server_received_ts}`; `await js.publish(f"sensor.events.{fam}.{asset_id}.{tag_id}", json.dumps(event).encode())`; rate control via sleep.
         - Post-loop: `await asyncio.sleep(1)` per flush ot-bridge.
         - Misura: `await conn.fetchrow("SELECT EXTRACT(EPOCH FROM (NOW() - MIN(timestamp_utc))) * 1000 AS oldest_ms FROM sensor_events WHERE timestamp_utc > $1", start_dt)`; per smoke gate, `p99_ms = oldest_ms` (worst-case ingestion lag). Per plan 03-07 raffineremo con pg_stat_statements.
         - Cleanup: `await pool.close(); await nc.close()`. Return `LoadResult`.

    9. **tests/load/test_ingestion_smoke.py** — IOT-10 smoke gate:
       - Costanti: `SMOKE_RATE = 1000`, `SMOKE_DURATION = 10`, `SMOKE_P99_MS_TARGET = 200`.
       - `@pytest.mark.load @pytest.mark.integration @pytest.mark.asyncio async def test_smoke_1k_10s(compose_stack)`: definisci `assets` list di 5 tuples copertura family; `result = await run_load(...)`; assert `result.total_events >= SMOKE_RATE * SMOKE_DURATION * 0.95` (95% target); assert `result.p99_ms < SMOKE_P99_MS_TARGET` (IOT-10 smoke gate threshold).

    Decisioni: D-51 3-layer enforcement runtime, D-52 subject hierarchy verified, D-48 smoke gate (full plan 03-07), IOT-02/04/05/10 smoke coverage. Tutti i test usano placeholder SQL `$1`/`$2` (no f-string).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && python3 -c "import ast; ast.parse(open('tests/integration/test_data_diode.py').read()); ast.parse(open('tests/integration/test_opcua_browseable.py').read()); ast.parse(open('tests/integration/test_nats_subjects.py').read()); ast.parse(open('tests/integration/test_e2e_sim_to_timescale.py').read()); ast.parse(open('tests/load/harness.py').read()); ast.parse(open('tests/load/test_ingestion_smoke.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "(set_value|write_attribute|write_value)" services/ot-bridge/src/` exits 1 (Layer 3 PASS).
    - `grep -c "sft-core" tests/integration/test_data_diode.py` ≥ 1 (Layer 1 fake-agent network).
    - `grep -c "urn:mantis" tests/integration/test_opcua_browseable.py` ≥ 1.
    - `grep -c "stream_info" tests/integration/test_nats_subjects.py` ≥ 1.
    - `grep -c "asyncpg" tests/integration/test_e2e_sim_to_timescale.py` ≥ 1.
    - `grep -c "SMOKE_P99_MS_TARGET = 200" tests/load/test_ingestion_smoke.py` == 1 (IOT-10 gate).
    - `grep -c "min_size=10" tests/load/harness.py` ≥ 1 (D-48 asyncpg pool).
    - `grep -c "statement_cache_size=0" tests/load/harness.py` ≥ 1 (Pitfall 6).
    - Tutti i 6 file .py parsano senza syntax error (verify automated).
    - `grep -rE 'f"(INSERT|SELECT|UPDATE|DELETE)' tests/` exits 1 (no f-string SQL in test).
  </acceptance_criteria>
  <done>4 file integration test + 1 load smoke + harness sintatticamente validi; D-51 3-layer pattern rispettato; harness conforme D-48.</done>
</task>

<task type="auto">
  <name>Task 3: CI workflow extension + workspace pyproject.toml dev deps</name>
  <files>.github/workflows/ci.yml, pyproject.toml</files>
  <read_first>
    - .github/workflows/ci.yml (esistente — step "Validate content" lines 81-94)
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md sezione ".github/workflows/ci.yml" (lines 803-845)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Security checks 1 (CI grep gates list completa)
    - pyproject.toml (workspace root — Phase 1 dev deps pattern)
  </read_first>
  <action>
    1. **.github/workflows/ci.yml** — Aggiungere step DOPO "Validate content" (preservare quello esistente):

       Step "Validate IT/OT artifacts (Phase 3)":
       - run: pipe multi-line:
         - `python3 scripts/validate-asset-registry.py`
         - `python3 scripts/validate-fault-profiles.py`
         - 5 CI grep gates (RESEARCH §Security checks 1):
           - `! grep -rE "yaml\.load\(" packages/sft-assets packages/sft-tools simulators/sim-textile services/ot-bridge`
           - `! grep -rE 'f"(INSERT|SELECT|UPDATE|DELETE)' services/ot-bridge packages/sft-tools`
           - `! grep -rE "(set_value|write_attribute|write_value)" services/ot-bridge/src/`
           - `! grep -rE "datetime\.now\(\)" simulators/sim-textile/src services/ot-bridge/src`
           - `! grep -rE "(pydantic\.v1|from langchain\.pydantic_v1)" packages/sft-tools/src`

       Step "Run IT/OT integration tests (Phase 3)":
       - run pipe: avvia `timescaledb` + `nats` via `docker compose up -d --wait`, esegui migration via `python3 scripts/timescale-migrate.py`, bootstrap NATS via `python3 scripts/nats-bootstrap-streams.py`, full stack `up`, esegui `uv run --with pytest --with pytest-asyncio --with asyncpg --with asyncua --with nats-py -- python -m pytest tests/integration/ -v -m integration`, tear down.

       Step "Run IT/OT load test (smoke 1k×10s)":
       - run pipe: ripeti up + migrate + bootstrap + pytest `tests/load/test_ingestion_smoke.py`, tear down.

       NB: lo step "Validate content" esistente (Phase 2) NON va modificato — Phase 3 aggiunge nuovi step in coda.

    2. **pyproject.toml** (workspace root, modify) — Aggiungere a `[dependency-groups] dev` (o `[tool.uv.dependency-groups]`) testcontainers, asyncpg, asyncua, nats-py per i test workspace-root:
       - Se il blocco esiste, append `"testcontainers[postgres]>=4.14", "asyncpg>=0.30", "asyncua>=1.1.8", "nats-py>=2.14.0"` alla lista esistente.
       - Se non esiste, crearlo con quei pacchetti + pytest+pytest-asyncio.

    Decisioni: CI gates per IOT-01..10 enforcement permanente; full 5k×60s NOT in CI default (Plan 03-07 via PR label).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && grep -c "Validate IT/OT artifacts" .github/workflows/ci.yml && grep -c "Run IT/OT integration tests" .github/workflows/ci.yml && grep -c "Run IT/OT load test" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Validate IT/OT artifacts" .github/workflows/ci.yml` ≥ 1.
    - `grep -c "Run IT/OT integration tests" .github/workflows/ci.yml` ≥ 1.
    - `grep -c "Run IT/OT load test" .github/workflows/ci.yml` ≥ 1.
    - `grep -c "validate-asset-registry.py" .github/workflows/ci.yml` ≥ 1.
    - `grep -c "validate-fault-profiles.py" .github/workflows/ci.yml` ≥ 1.
    - `grep -c "set_value" .github/workflows/ci.yml` ≥ 1 (grep gate Layer 3 in CI).
    - `grep -c "timescale-migrate.py" .github/workflows/ci.yml` ≥ 1.
    - `grep -c "nats-bootstrap-streams.py" .github/workflows/ci.yml` ≥ 1.
    - `grep -c "tests/integration/" .github/workflows/ci.yml` ≥ 1.
    - `grep -c "tests/load/test_ingestion_smoke.py" .github/workflows/ci.yml` ≥ 1.
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (YAML valido).
    - `grep -c "testcontainers" pyproject.toml` ≥ 1.
  </acceptance_criteria>
  <done>CI workflow ha i 3 nuovi step Phase 3 (validate + integration + smoke load); workspace dev deps include testcontainers + asyncpg + asyncua + nats-py.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Avvio full stack + log inspection + verifica E2E manuale</name>
  <files>(no file changes — runtime verification)</files>
  <read_first>
    - infra/compose/sim.yml + core.yml (Task 1)
    - tests/integration/ (Task 2)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md decisioni globali D-51, D-52
  </read_first>
  <what-built>
    - docker-compose esteso con sim-textile + ot-bridge su dual-network sft-ot / sft-core (Task 1).
    - 5 file di test integration + 1 load smoke + harness (Task 2).
    - CI workflow esteso con 3 step Phase 3 (Task 3).
  </what-built>
  <how-to-verify>
    Eseguire SEQUENZIALMENTE e confermare exit code 0 (lo Stack è già migrato perché Plan 03-05 BLOCKING è già fatto):

    1. **Build immagini docker** (può richiedere 3-5 min al primo build):
       ```bash
       docker compose -f infra/compose/core.yml -f infra/compose/sim.yml build sim-textile ot-bridge
       ```
       Atteso: exit 0.

    2. **Avvia full stack**:
       ```bash
       docker compose -f infra/compose/core.yml -f infra/compose/sim.yml up -d --wait
       docker compose -f infra/compose/core.yml -f infra/compose/sim.yml ps
       ```
       Atteso: tutti i container `running (healthy)` o `running (started)`.

    3. **Bootstrap NATS stream**:
       ```bash
       python3 scripts/nats-bootstrap-streams.py --server nats://localhost:4222
       ```
       Atteso: exit 0, stdout `OK [SENSOR_EVENTS]: created/updated` + `OK [AUDIT_OT]: created/updated`.

    4. **Verifica logs sim-textile**:
       ```bash
       docker compose -f infra/compose/core.yml -f infra/compose/sim.yml logs --tail=50 sim-textile
       ```
       Atteso: no errori; emissione attiva (log structlog JSON).

    5. **Verifica logs ot-bridge**:
       ```bash
       docker compose -f infra/compose/core.yml -f infra/compose/sim.yml logs --tail=50 ot-bridge
       ```
       Atteso: connessione OPC-UA OK, NATS publish attivo, asyncpg pool started.

    6. **Verifica metrics sim-textile**:
       ```bash
       curl -s http://localhost:8081/metrics | grep -c "sim_events_emitted_total"
       ```
       Atteso: ≥ 1 (almeno 1 timeseries Counter visibile).

    7. **Verifica metrics ot-bridge**:
       ```bash
       curl -s http://localhost:8082/metrics | grep -c "events_published_total"
       ```
       Atteso: ≥ 1.

    8. **Esegui test integration**:
       ```bash
       uv run --with pytest --with pytest-asyncio --with asyncpg --with asyncua --with nats-py --with structlog -- python -m pytest tests/integration -v -m integration
       ```
       Atteso: tutti i 5+ test integration PASSED.

    9. **Esegui smoke load**:
       ```bash
       uv run --with pytest --with pytest-asyncio --with asyncpg --with nats-py -- python -m pytest tests/load/test_ingestion_smoke.py -v
       ```
       Atteso: PASSED con `p99_ms < 200`.

    10. **Verifica TimescaleDB ha dati**:
        ```bash
        docker compose -f infra/compose/core.yml -f infra/compose/sim.yml exec timescaledb psql -U sft -d sft -c "SELECT COUNT(*) AS total, COUNT(DISTINCT asset_id) AS assets FROM sensor_events WHERE timestamp_utc > NOW() - INTERVAL '5 minutes';"
        ```
        Atteso: `total > 0`, `assets > 0`.

    11. **Tear down**:
        ```bash
        docker compose -f infra/compose/core.yml -f infra/compose/sim.yml down -v
        ```

    Tutti gli step verdi: Phase 3 integration GREEN.
  </how-to-verify>
  <resume-signal>
    Type "approved" se tutti gli 11 step exit 0 con output atteso. Type "blocked: <reason>" se uno fallisce.

    Note: se docker non è disponibile localmente, type "approved-ci-only" e affidati al CI workflow (Task 3) per la validazione automatizzata sul prossimo push.
  </resume-signal>
  <done>Full stack avviato, tutti i test integration verdi, smoke load < 200ms p99, dati ingeriti in TimescaleDB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host docker → sft-ot network | Per dev tooling (`localhost:4840`), OPC-UA discoverable; in prod (Phase 11) chiuso. |
| sft-core ↔ sft-ot via ot-bridge | UNICO path tra le reti; bridge è app-level data-diode (D-51 Layer 3). |
| External docker container → sft-ot | DEVE essere bloccato (D-51 Layer 1) — test Layer 1 verifica. |
| CI runner → docker daemon | GitHub Actions docker-in-docker; trust nel runner host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-layer1-bypass | Tampering / EoP | docker network ACL | mitigate | Layer 1 testato esplicitamente in `test_data_diode.test_layer1_agent_cannot_reach_sim_via_it_network` con container fake-agent su `sft-core` only. Riferisce T-DATA-DIODE + Pitfall 4. |
| T-03-06-layer2-edge | Tampering | Layer 2 test from host | accept | Caveat A5: docker DNS shared può false-pass se sft-sim-textile risolve da host; Layer 1 (container-based) è primary gate, Layer 2 + 3 sono difese in profondità. Documentato in README. |
| T-03-06-protocol-write | Tampering | sim-textile Variable.set_writable(False) | mitigate | Protocol-level rejection testato in `test_opcua_browseable.test_loom_01_variable_writable_false`. Riferisce T-DATA-DIODE Layer 0 (server-side). |
| T-03-06-supply-chain-image | Tampering | docker image pull (python:3.12-slim, timescale/timescaledb, nats) | mitigate | Image pin esatto (Phase 1 + Plan 03-05 commento legacy compression); SBOM scanner CI esistente (Phase 1). |
| T-03-06-dos-load | DoS | smoke load test 1k×10s | accept | Smoke è limit-bound; full 5k×60s gated da PR-label in Plan 03-07. Risorse CI workflow proporzionate (single runner). |
| T-03-06-secret-env | Information Disclosure | docker-compose env vars TIMESCALE_DSN | mitigate | Default `sft_dev_pass` solo per dev (override possibile via env in prod); GitHub Actions usa secrets. NESSUNA env var sensible loggata. |

`security_enforcement: enabled` (ASVS V4 + V14). Layer 1 enforcement validato in CI.
</threat_model>

<verification>
- Task 1: `docker compose ... config` exits 0; topology dual-network verificata.
- Task 2: tutti 6 file Python parsano; pattern data-diode 3-layer presente; harness conforme D-48.
- Task 3: CI ha 3 nuovi step; YAML valido; grep gates documentate.
- Task 4: checkpoint manuale (11 step) verde o `approved-ci-only`.
- `make integration-test` end-to-end exit 0 (richiede docker).
- `npx nx run-many --target=test --all` non regredisce.
</verification>

<success_criteria>
- IOT-02 chiuso (runtime): OPC-UA server browseable, 5 namespace, Variable non-writable.
- IOT-04 chiuso: NATS subjects formato D-52 verificato in test integration.
- IOT-05 chiuso: 3-layer data-diode enforcement testato (Layer 1 container fake-agent + Layer 2 host-side + Layer 3 grep static analysis).
- IOT-10 smoke gate chiuso (1k×10s p99<200ms); full gate (5k×60s) ⇒ Plan 03-07.
- E2E sim → bridge → NATS → TimescaleDB verificato; CI permanente.
</success_criteria>

<output>
Create `.planning/phases/03-it-ot-simulation-layer/03-06-SUMMARY.md` con:
- File creati/modificati
- Output docker compose + nx test
- Conferma 3-layer data-diode verde
- Output smoke load: p99_ms misurato
- Eventuali deviazioni / caveat sul Layer 2 (RESEARCH A5)
</output>
