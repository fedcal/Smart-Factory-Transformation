---
phase: 03-it-ot-simulation-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sft-tools/pyproject.toml
  - packages/sft-tools/project.json
  - packages/sft-tools/package.json
  - packages/sft-tools/src/sft_tools/__init__.py
  - packages/sft-tools/src/sft_tools/replay/__init__.py
  - packages/sft-tools/src/sft_tools/replay/models.py
  - packages/sft-tools/src/sft_tools/replay/cmapss.py
  - packages/sft-tools/src/sft_tools/replay/uci.py
  - packages/sft-tools/src/sft_tools/timescale/__init__.py
  - packages/sft-tools/src/sft_tools/timescale/query.py
  - packages/sft-tools/tests/conftest.py
  - packages/sft-tools/tests/test_replay_models.py
  - packages/sft-tools/tests/test_replay_cmapss.py
  - packages/sft-tools/tests/test_replay_uci.py
  - packages/sft-tools/tests/test_query_timescale.py
  - packages/sft-tools/tests/fixtures/cmapss_fd001_sample.txt
  - packages/sft-tools/tests/fixtures/uci_production_sample.csv
  - scripts/download-replay-datasets.py
autonomous: true
requirements:
  - IOT-07
  - IOT-08
tags:
  - langchain-tools
  - replay-loader
  - cmapss
  - uci
  - asyncpg

must_haves:
  truths:
    - "Il pacchetto Nx `sft-tools` esiste come `library` con dependencies `langchain-core>=1.0,<2.0`, `pandas>=2.2,<3.0`, `asyncpg>=0.30`, `pydantic>=2.13.4`, `sft-assets`"
    - "`ReplayCMAPSSTool`, `ReplayUCITool`, `QueryTimescaleTool` sono sottoclassi `BaseTool` con `args_schema` Pydantic v2, `_arun` async, `_run` che solleva NotImplementedError"
    - "`replay_cmapss` mappa `unit_id` → `asset_id` via parametro opzionale `target_asset_id` con default deterministic-hash su registry assets (OQ5 resolution)"
    - "Le funzioni `_arun` ritornano `pandas.DataFrame` con colonne `asset_id, timestamp, sensor_id, value, unit, source_dataset, source_unit` (schema unificato D-46)"
    - "Sample fixtures C-MAPSS FD001 + UCI production sono presenti in `tests/fixtures/` per evitare download in CI"
    - "Nessun match per `pydantic.v1` o `from langchain.pydantic_v1` in `packages/sft-tools/` (Pitfall 5)"
    - "Nessun f-string SQL injection (`grep -rE 'f\"(INSERT|SELECT|UPDATE|DELETE)' packages/sft-tools` exits 1)"
    - "`scripts/download-replay-datasets.py` con SHA256 verify + --dry-run è committato (A4/A10 mitigation)"
  artifacts:
    - path: "packages/sft-tools/pyproject.toml"
      provides: "Nx Python project deps langchain-core, pandas, asyncpg, sft-assets"
      contains: "name = \"sft-tools\""
    - path: "packages/sft-tools/project.json"
      provides: "Nx test target + implicitDependencies sft-assets"
      contains: "implicitDependencies"
    - path: "packages/sft-tools/src/sft_tools/replay/models.py"
      provides: "Pydantic ReplayRecord, ReplayCMAPSSArgs, ReplayUCIArgs frozen"
      contains: "ReplayRecord"
    - path: "packages/sft-tools/src/sft_tools/replay/cmapss.py"
      provides: "ReplayCMAPSSTool(BaseTool) async + target_asset_id deterministic hash"
      contains: "class ReplayCMAPSSTool"
    - path: "packages/sft-tools/src/sft_tools/replay/uci.py"
      provides: "ReplayUCITool(BaseTool) async per air_quality/energy/production"
      contains: "class ReplayUCITool"
    - path: "packages/sft-tools/src/sft_tools/timescale/query.py"
      provides: "QueryTimescaleTool(BaseTool) async asyncpg con $1,$2 placeholders"
      contains: "class QueryTimescaleTool"
    - path: "scripts/download-replay-datasets.py"
      provides: "argparse + SHA256 + --dry-run; gitignored data dir"
      contains: "hashlib.sha256"
  key_links:
    - from: "packages/sft-tools/src/sft_tools/replay/cmapss.py"
      to: "packages/sft-assets/src/sft_assets/_loader.py"
      via: "import load_assets to derive deterministic target_asset_id"
      pattern: "from sft_assets import"
    - from: "packages/sft-tools/src/sft_tools/timescale/query.py"
      to: "asyncpg connection (env TIMESCALE_DSN)"
      via: "asyncpg.connect with $1, $2 placeholders"
      pattern: "\\$1"
---

<objective>
Creare il pacchetto Nx `packages/sft-tools/` (LangChain Tools cross-cutting per Phase 4+ agents) con 3 tool: `replay_cmapss` (NASA C-MAPSS FD001), `replay_uci` (UCI Manufacturing), `query_timescale` (read-only proxy hypertable). Tutti async-first, Pydantic v2 `args_schema`, schema output unificato `ReplayRecord`.

Purpose (per D-46 + D-47 + IOT-07 + IOT-08): centralizzare i Tool in un solo Nx project importabile da agents Phase 4+ via `langgraph.prebuilt.ToolNode` standard. Sblocca Phase 7 PredictiveMaintenance (training su replay data) e Phase 4 chat-with-data agents (query historical sensor events).

Output: pacchetto importabile (`from sft_tools.replay import REPLAY_TOOLS`, `from sft_tools.timescale import TIMESCALE_TOOLS`), test pytest verdi (mocked DataFrame contracts), script idempotente download dataset + SHA256.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md
@.planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md
@.planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md
@.planning/phases/03-it-ot-simulation-layer/03-VALIDATION.md

# Analogs
@packages/sft-domain/pyproject.toml
@packages/sft-domain/project.json
@packages/sft-domain/src/sft_domain/glossary/__init__.py
@packages/sft-domain/src/sft_domain/glossary/_models.py
@packages/sft-domain/tests/conftest.py
@scripts/sync-python-versions.py

<interfaces>
<!-- Sft-tools API surface — Phase 4+ agents importeranno questi simboli. -->

From `sft_tools` (target exports):
```python
# packages/sft-tools/src/sft_tools/__init__.py
from sft_tools.replay import REPLAY_TOOLS, ReplayCMAPSSTool, ReplayUCITool, ReplayRecord
from sft_tools.timescale import TIMESCALE_TOOLS, QueryTimescaleTool

__all__ = ["REPLAY_TOOLS", "TIMESCALE_TOOLS", "ReplayCMAPSSTool", "ReplayUCITool", "QueryTimescaleTool", "ReplayRecord"]
```

From `sft_tools.replay.models`:
```python
class ReplayRecord(BaseModel):
    model_config = {"frozen": True, "extra": "forbid"}
    asset_id: str
    timestamp: datetime  # tz-aware UTC required
    sensor_id: str
    value: float
    unit: str
    source_dataset: Literal["cmapss", "uci"]
    source_unit: str

class ReplayCMAPSSArgs(BaseModel):
    unit_id: int  # 1..260 across FD001..FD004
    time_range: tuple[datetime, datetime] | None = None
    sensor_subset: list[str] | None = None
    dataset: Literal["FD001", "FD002", "FD003", "FD004"] = "FD001"
    target_asset_id: str | None = None  # OQ5: optional override

class ReplayUCIArgs(BaseModel):
    dataset: Literal["air_quality", "energy", "production"]
    asset_id: str  # required (no auto-mapping)
    time_range: tuple[datetime, datetime] | None = None

class QueryTimescaleArgs(BaseModel):
    asset_id: str
    time_range: tuple[datetime, datetime]
    tags: list[str] | None = None
```

CRITICAL imports (Pitfall 5): `from pydantic import BaseModel, Field` (v2). NEVER `from langchain.pydantic_v1`. Pin `langchain-core>=1.0,<2.0`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold sft-tools + Pydantic ReplayRecord + LangChain Tool skeletons + test contracts</name>
  <files>packages/sft-tools/pyproject.toml, packages/sft-tools/project.json, packages/sft-tools/package.json, packages/sft-tools/src/sft_tools/__init__.py, packages/sft-tools/src/sft_tools/replay/__init__.py, packages/sft-tools/src/sft_tools/replay/models.py, packages/sft-tools/src/sft_tools/replay/cmapss.py, packages/sft-tools/src/sft_tools/replay/uci.py, packages/sft-tools/src/sft_tools/timescale/__init__.py, packages/sft-tools/src/sft_tools/timescale/query.py, packages/sft-tools/tests/conftest.py, packages/sft-tools/tests/test_replay_models.py, packages/sft-tools/tests/test_replay_cmapss.py, packages/sft-tools/tests/test_replay_uci.py, packages/sft-tools/tests/test_query_timescale.py, packages/sft-tools/tests/fixtures/cmapss_fd001_sample.txt, packages/sft-tools/tests/fixtures/uci_production_sample.csv</files>
  <read_first>
    - packages/sft-domain/pyproject.toml (analog Python pkg config)
    - packages/sft-domain/project.json (analog Nx project shape)
    - packages/sft-domain/src/sft_domain/glossary/_models.py (analog Pydantic frozen)
    - packages/sft-domain/tests/conftest.py (analog fixtures)
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md sezione "pyproject.toml" deps for sft-tools (line 291) + "project.json" (lines 338-339 implicitDependencies)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 5 (BaseTool async + args_schema Pydantic v2, lines 615-662) + §Pattern 6 (C-MAPSS FD001 schema, lines 664-682)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 5 (no pydantic.v1, lines 786-800) + §Security checks 1 (CI grep gates)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-46 (ReplayRecord schema) + D-47 (tool registry location) + claudes_discretion replay-data paths
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md OQ5 resolution (target_asset_id arg)
  </read_first>
  <behavior>
    - Test 1 (test_replay_models): `ReplayRecord(asset_id="LOOM-01", timestamp=datetime(2026,5,18,tzinfo=UTC), sensor_id="sensor_2", value=1.23, unit="raw", source_dataset="cmapss", source_unit="FD001#unit1#cycle1")` istanziato OK; mutazione solleva ValidationError (frozen).
    - Test 2 (test_replay_models): `ReplayRecord(..., timestamp=datetime(2026,5,18))` (naive) solleva ValidationError via field_validator `tzinfo is not None` (RESEARCH §Pitfall 7).
    - Test 3 (test_replay_models): `ReplayCMAPSSArgs(unit_id=261)` solleva ValidationError (range 1..260).
    - Test 4 (test_replay_cmapss): `ReplayCMAPSSTool().name == "replay_cmapss"`; `ReplayCMAPSSTool().description` non vuota; `ReplayCMAPSSTool().args_schema == ReplayCMAPSSArgs`.
    - Test 5 (test_replay_cmapss): `_run` solleva NotImplementedError; `await _arun(unit_id=1, dataset="FD001")` su fixture `cmapss_fd001_sample.txt` ritorna `pd.DataFrame` con colonne `["asset_id","timestamp","sensor_id","value","unit","source_dataset","source_unit"]` e ≥ 21 righe (21 sensori × 1 cycle minimo).
    - Test 6 (test_replay_cmapss): senza `target_asset_id` esplicito, due chiamate con stesso `unit_id` ritornano stesso `asset_id` (deterministic hash su `load_assets()`).
    - Test 7 (test_replay_cmapss): con `target_asset_id="LOOM-05"` esplicito, output DataFrame ha `df["asset_id"].unique() == ["LOOM-05"]`.
    - Test 8 (test_replay_cmapss): `sensor_subset=["sensor_2","sensor_7"]` filtra correttamente.
    - Test 9 (test_replay_uci): `ReplayUCITool().name == "replay_uci"`; `await _arun(dataset="production", asset_id="LOOM-01")` su fixture sample.csv ritorna DataFrame con schema unificato.
    - Test 10 (test_query_timescale): `QueryTimescaleTool().name == "query_timescale"`; SQL costruito usa placeholder `$1, $2, $3` (no f-string SQL) — assertion via mock di `asyncpg.connect`.
    - Test 11 (test_query_timescale): chiamata con `tags=["warp_tension","pick_density"]` produce SQL con `tag_id = ANY($N)` parameter binding.
  </behavior>
  <action>
    1. **pyproject.toml** — Copia pattern `packages/sft-domain/pyproject.toml`. `name = "sft-tools"`, `version = "0.1.0"`, `requires-python = ">=3.12,<3.13"`. Dependencies: `["pydantic>=2.13.4", "langchain-core>=1.0,<2.0", "pandas>=2.2,<3.0", "asyncpg>=0.30", "pyyaml>=6.0", "sft-assets", "sft-domain"]`. Dev: `["pytest>=8.0", "pytest-asyncio>=0.24"]`. Build hatchling `packages = ["src/sft_tools"]`. `[tool.uv.sources]` con `sft-assets = { workspace = true }` e `sft-domain = { workspace = true }` (Phase 1 workspace pattern). `[tool.pytest.ini_options]` `asyncio_mode = "auto"`.

    2. **project.json** — Replica `packages/sft-domain/project.json` con `name: "sft-tools"`, `projectType: "library"`, `sourceRoot: "packages/sft-tools/src"`, `implicitDependencies: ["sft-assets", "sft-domain"]`. Targets `test` (uv run pytest), `lint` (uv run ruff check src).

    3. **package.json** — analog `packages/sft-domain/package.json` con `name: "@sft/sft-tools"`.

    4. **src/sft_tools/replay/models.py** — Tutti i modelli con `model_config = {"frozen": True, "extra": "forbid"}`:
       - `ReplayRecord` con campi della interfaces sopra. `@field_validator("timestamp")` enforce `v.tzinfo is not None`.
       - `ReplayCMAPSSArgs` con `unit_id: Annotated[int, Field(ge=1, le=260)]`, `dataset: Literal["FD001","FD002","FD003","FD004"] = "FD001"`, `target_asset_id: str | None = None`, `time_range` e `sensor_subset` opzionali.
       - `ReplayUCIArgs` con `dataset: Literal["air_quality","energy","production"]`, `asset_id: str` required.
       - `QueryTimescaleArgs` con `asset_id: str`, `time_range: tuple[datetime, datetime]`, `tags: list[str] | None = None`. `@field_validator("time_range")` enforce both timestamps tz-aware.

    5. **src/sft_tools/replay/cmapss.py** — `class ReplayCMAPSSTool(BaseTool)` con:
       - `name: str = "replay_cmapss"`
       - `description: str = "..."` (PATTERNS sample lines 642-647 in RESEARCH)
       - `args_schema: type[BaseModel] = ReplayCMAPSSArgs`
       - `_run` solleva NotImplementedError("Use async _arun")
       - `_arun(self, unit_id, time_range=None, sensor_subset=None, dataset="FD001", target_asset_id=None) -> pd.DataFrame`:
         - Risolve `target_asset_id`: se `None`, chiama `_deterministic_asset_for_unit(unit_id)` (private helper) che fa `from sft_assets import load_assets; assets = load_assets(); return assets[hash(unit_id) % len(assets)].asset_id` (deterministic per unit_id stesso).
         - Path CSV: `WORKSPACE_ROOT / "simulators/sim-textile/replay-data" / f"train_{dataset}.txt"`. Se non esiste, fallback a `packages/sft-tools/tests/fixtures/cmapss_fd001_sample.txt` (per test e dev).
         - Lettura via `pandas.read_csv(path, sep=r"\s+", header=None, names=["unit_id","cycle","op_setting_1","op_setting_2","op_setting_3"] + [f"sensor_{i}" for i in range(1,22)])`.
         - Filter `df[df.unit_id == unit_id]`.
         - Pivot da wide a long: ogni (cycle, sensor_X) → row `(asset_id, timestamp, sensor_id, value, unit, source_dataset, source_unit)`. `timestamp = datetime(2026,1,1,tzinfo=UTC) + timedelta(seconds=cycle*60)` (mapping cycle → minuto, A-002 invariante).
         - Se `sensor_subset` non None, filtra `df[df["sensor_id"].isin(sensor_subset)]`.
         - `unit = "raw"`, `source_dataset = "cmapss"`, `source_unit = f"{dataset}#unit{unit_id}#cycle{cycle}"`.
         - Ritorna `pd.DataFrame` con esattamente le 7 colonne canoniche.

       Decisione: NESSUNA chiamata `f"INSERT ..."` né SQL string injection (CI grep gate, RESEARCH §Security check 1).

    6. **src/sft_tools/replay/uci.py** — `class ReplayUCITool(BaseTool)` analogo:
       - `name="replay_uci"`, `args_schema=ReplayUCIArgs`.
       - Path fixture per dataset `production`: `packages/sft-tools/tests/fixtures/uci_production_sample.csv` (test); production path `simulators/sim-textile/replay-data/uci_<dataset>.csv`.
       - `_arun(dataset, asset_id, time_range=None) -> pd.DataFrame` con stesso schema unificato. `source_dataset="uci"`, `source_unit=f"uci_{dataset}#row<N>"`.

    7. **src/sft_tools/timescale/query.py** — `class QueryTimescaleTool(BaseTool)` con:
       - `name="query_timescale"`, `args_schema=QueryTimescaleArgs`.
       - `_arun(self, asset_id, time_range, tags=None) -> pd.DataFrame`:
         - Connessione via `asyncpg.connect(os.environ["TIMESCALE_DSN"])` (env solo, no hardcode — security.md).
         - Query base: `SELECT asset_id, tag_id, timestamp_utc, value, unit FROM sensor_events WHERE asset_id = $1 AND timestamp_utc >= $2 AND timestamp_utc <= $3` con parametri `$1, $2, $3`. Se `tags` non None, append `AND tag_id = ANY($4::text[])` e passa `tags` come list. Fetch via `await conn.fetch(SQL, *params)`, converte `records` in DataFrame.
         - Statement cache: `await asyncpg.connect(dsn, statement_cache_size=0)` (Pitfall 6).
       - Mai f-string SQL (RESEARCH §Anti-Patterns + Security check 1).

    8. **src/sft_tools/__init__.py + replay/__init__.py + timescale/__init__.py** — Barrel exports:
       ```python
       # sft_tools/__init__.py
       from sft_tools.replay import REPLAY_TOOLS, ReplayRecord, ReplayCMAPSSTool, ReplayUCITool
       from sft_tools.timescale import TIMESCALE_TOOLS, QueryTimescaleTool
       __all__ = ["REPLAY_TOOLS", "TIMESCALE_TOOLS", "ReplayCMAPSSTool", "ReplayUCITool", "QueryTimescaleTool", "ReplayRecord"]

       # sft_tools/replay/__init__.py
       from sft_tools.replay.cmapss import ReplayCMAPSSTool
       from sft_tools.replay.uci import ReplayUCITool
       from sft_tools.replay.models import ReplayRecord, ReplayCMAPSSArgs, ReplayUCIArgs
       REPLAY_TOOLS = [ReplayCMAPSSTool(), ReplayUCITool()]
       __all__ = [...]

       # sft_tools/timescale/__init__.py
       from sft_tools.timescale.query import QueryTimescaleTool
       TIMESCALE_TOOLS = [QueryTimescaleTool()]
       __all__ = [...]
       ```

    9. **tests/fixtures/cmapss_fd001_sample.txt** — Minimal C-MAPSS FD001 fixture: 5 unit_id × 3 cycles × 26 colonne whitespace-separated (5*3=15 righe). Valori sintetici realistici (numpy-style sensor_1..21 in range tipici NASA). Format exact per `pandas.read_csv(sep=r"\s+", header=None)`.

    10. **tests/fixtures/uci_production_sample.csv** — Minimal UCI production fixture: 10 righe con colonne `timestamp,sensor_id,value,unit` (CSV header presente).

    11. **tests/conftest.py** — Fixtures `sample_replay_record_dict`, `frozen_utc_now` (`datetime(2026,5,18,12,0,0,tzinfo=UTC)`), `mock_asyncpg_conn` (AsyncMock con `.fetch` ritornante records sintetici), `cmapss_fixture_path` (path fissa a `tests/fixtures/cmapss_fd001_sample.txt`).

    12. **tests/test_replay_models.py + test_replay_cmapss.py + test_replay_uci.py + test_query_timescale.py** — Implementare i Test 1-11 sopra descritti. Per `test_query_timescale`, mock `asyncpg.connect` con `unittest.mock.AsyncMock` e cattura argomenti passati a `.fetch()` per assert su placeholder `$1, $2, $3, $4`.

    CRITICAL: zero `from langchain.pydantic_v1`; zero `from pydantic.v1`. Solo `from pydantic import BaseModel, Field, field_validator, ConfigDict`. Le tool description devono essere ricche perché Phase 4+ LLM le legge come prompt.

    Decisioni implementate: D-46 (ReplayRecord schema unificato), D-47 (sft-tools location), OQ5 resolution (target_asset_id opzionale + deterministic), IOT-07 (replay_cmapss + integrato come tool), IOT-08 (replay_uci + integrato come tool).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && uv run --project packages/sft-tools pytest packages/sft-tools/tests -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project packages/sft-tools pytest packages/sft-tools/tests -x -v` exits 0; ≥ 11 test items passed.
    - `grep -rE "(pydantic\.v1|from langchain\.pydantic_v1)" packages/sft-tools/src` exits 1 (no match — Pitfall 5).
    - `grep -rE 'f"(INSERT|SELECT|UPDATE|DELETE)' packages/sft-tools/src` exits 1 (no match — Security check 1).
    - `grep -c "class ReplayCMAPSSTool(BaseTool)" packages/sft-tools/src/sft_tools/replay/cmapss.py` == 1.
    - `grep -c "class ReplayUCITool(BaseTool)" packages/sft-tools/src/sft_tools/replay/uci.py` == 1.
    - `grep -c "class QueryTimescaleTool(BaseTool)" packages/sft-tools/src/sft_tools/timescale/query.py` == 1.
    - `grep -c "\$1" packages/sft-tools/src/sft_tools/timescale/query.py` ≥ 1 (placeholder SQL).
    - `grep -c "statement_cache_size=0" packages/sft-tools/src/sft_tools/timescale/query.py` == 1 (Pitfall 6).
    - `python3 -c "from sft_tools import REPLAY_TOOLS, TIMESCALE_TOOLS; assert len(REPLAY_TOOLS) == 2 and len(TIMESCALE_TOOLS) == 1"` exits 0.
  </acceptance_criteria>
  <done>Pacchetto sft-tools committato, 3 LangChain Tools async con args_schema Pydantic v2, fixture C-MAPSS+UCI per test, tutti i contratti DataFrame coperti da test, nessun pattern v1 o SQL injection.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/download-replay-datasets.py con SHA256 verify + .gitignore data dir</name>
  <files>scripts/download-replay-datasets.py, simulators/sim-textile/replay-data/.gitkeep, simulators/sim-textile/replay-data/CHECKSUMS.txt, .gitignore</files>
  <read_first>
    - scripts/sync-python-versions.py (analog argparse + --dry-run + WORKSPACE_ROOT + exit codes, lines 1-131)
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md sezione "scripts/timescale-migrate.py" + "download-replay-datasets.py" (lines 372-431)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md claudes_discretion sezione "Replay loader dataset paths" + RESEARCH §Environment Availability (NASA C-MAPSS + UCI download-on-demand)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Common Pitfalls + §Assumption A4, A10 (license mitigation)
    - .gitignore (esistente, esamina pattern per appending)
  </read_first>
  <action>
    1. **scripts/download-replay-datasets.py** — Script Python con shebang + docstring + argparse:
       - `WORKSPACE_ROOT = Path(__file__).parent.parent`
       - `DEFAULT_DEST = WORKSPACE_ROOT / "simulators/sim-textile/replay-data"`
       - argparse: `--dest PATH` (default), `--dataset {cmapss-fd001,uci-air-quality,uci-energy,uci-production,all}` (default `all`), `--dry-run`, `--force` (override existing).
       - URL map (commented inline as best-effort; user may override via env):
         - CMAPSS FD001: `os.environ.get("CMAPSS_FD001_URL", "https://data.nasa.gov/download/ff5v-kuh6/application%2Fzip")` (NASA Open Data portal).
         - UCI Air Quality: `https://archive.ics.uci.edu/static/public/360/air+quality.zip`
         - UCI Energy: `https://archive.ics.uci.edu/static/public/374/appliances+energy+prediction.zip`
         - UCI Production: `https://archive.ics.uci.edu/static/public/477/real+time+election+results+portugal+2019.zip` (placeholder — note: production dataset URL può cambiare; documentare).
       - Per ogni dataset target: scarica via `urllib.request.urlretrieve` (no requests dep), calcola SHA256 con `hashlib.sha256`, confronta con `CHECKSUMS.txt`, scrive il file in `dest/`. Se mismatch → exit 1. Se URL fail → exit 1 con messaggio "Verify URL or set CMAPSS_FD001_URL env".
       - `--dry-run`: stampa cosa scaricherebbe + path SHA256 atteso, no I/O di rete.
       - Exit 0 su successo, 1 su errore.

    2. **simulators/sim-textile/replay-data/.gitkeep** — File vuoto per preservare la directory in git.

    3. **simulators/sim-textile/replay-data/CHECKSUMS.txt** — Stub iniziale con commento istruzioni:
       ```
       # SHA256 checksums per replay dataset files.
       # Format: <sha256>  <filename>
       # Eseguire `scripts/download-replay-datasets.py --dataset all` per popolare automaticamente al primo download.
       # I valori SHA256 reali sono validati al primo download contro UPSTREAM (NASA / UCI).
       # MAI committare i CSV/TXT dei dataset (vedi .gitignore).
       ```
       Lista vuota — verrà aggiornata in fase di esecuzione manuale prima della Phase 4.

    4. **.gitignore** — Appendere blocco:
       ```
       # Phase 3: replay-data files (NASA C-MAPSS / UCI download-on-demand)
       simulators/sim-textile/replay-data/*
       !simulators/sim-textile/replay-data/.gitkeep
       !simulators/sim-textile/replay-data/CHECKSUMS.txt
       ```
       Verificare prima che non esista già.

    Decisioni: A4/A10 mitigation (no dataset redistribution, SHA256 download-on-demand pattern), claudes_discretion replay-data layout, IOT-07/08 supporting infrastructure.
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && python3 scripts/download-replay-datasets.py --dry-run --dataset all && test -f simulators/sim-textile/replay-data/.gitkeep && test -f simulators/sim-textile/replay-data/CHECKSUMS.txt && grep -q "simulators/sim-textile/replay-data/\*" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/download-replay-datasets.py --dry-run --dataset all` exits 0; stdout include almeno 4 lines `[dry-run] would download ...`.
    - `python3 scripts/download-replay-datasets.py --help` exits 0 (argparse working).
    - `test -f simulators/sim-textile/replay-data/.gitkeep` exit 0.
    - `grep -q "simulators/sim-textile/replay-data/\\*" .gitignore` exit 0.
    - `grep -c "hashlib.sha256" scripts/download-replay-datasets.py` ≥ 1.
    - `grep -c "WORKSPACE_ROOT" scripts/download-replay-datasets.py` ≥ 1 (Pattern S-3).
    - `grep -c "argparse" scripts/download-replay-datasets.py` ≥ 1.
  </acceptance_criteria>
  <done>Script idempotente con SHA256, directory replay-data gitignored, executor può scaricare manualmente i dataset prima di Phase 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent (Phase 4+) → Tool args | LangChain `BaseTool.invoke()` riceve dict da LLM; `args_schema` Pydantic è il primo gate. |
| Tool → TimescaleDB | `QueryTimescaleTool` esegue SQL contro hypertable; deve usare ESCLUSIVAMENTE `$1, $2` placeholders. |
| External dataset → local fs | `download-replay-datasets.py` scarica da NASA/UCI URL; deve verificare SHA256. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-pydantic | Tampering / Injection | `args_schema` Pydantic v2 frozen + extra=forbid | mitigate | Tutti i tool args sono validati prima del `_arun`; tz-aware datetime validator (RESEARCH §Pitfall 7). Riferisce T-V5-pydantic. |
| T-03-02-sql | Injection | `QueryTimescaleTool.query.py` | mitigate | `$1, $2` placeholders esclusivi; CI grep gate `grep -rE 'f"(INSERT|SELECT|UPDATE|DELETE)' packages/sft-tools` exit 1. Riferisce T-V5-sql. |
| T-03-02-replay-tamper | Tampering | `replay-data/*.csv` | mitigate | SHA256 verify in `download-replay-datasets.py` contro `CHECKSUMS.txt`; dataset gitignored (no redistribution / no commit). Riferisce A10. |
| T-03-02-pydantic-v1 | Tampering | LangChain `BaseTool.args_schema` | mitigate | CI grep gate `grep -rE "(pydantic\.v1|from langchain\.pydantic_v1)" packages/sft-tools/src` exit 1 (Pitfall 5). Pin `langchain-core>=1.0,<2.0`. |
| T-03-02-dos-pool | DoS | `asyncpg.connect` in QueryTimescaleTool | accept | Phase 3 ships single connection per query (no pool here — pool è ot-bridge writer, plan 03-04). Phase 11 può aggiungere connection limit per agent. |
| T-03-02-package | Supply chain | `langchain-core, pandas, asyncpg` install | mitigate | RESEARCH §Package Legitimacy Audit: tutti approved (slopcheck OK); pin esatti in `pyproject.toml`. Riferisce T-V5-SC. |

`security_enforcement: enabled`. ASVS V5 + V8 applicabili. Nessun secret hardcoded (`TIMESCALE_DSN` solo via env).
</threat_model>

<verification>
- `uv run --project packages/sft-tools pytest packages/sft-tools/tests -x -v` exits 0.
- `python3 scripts/download-replay-datasets.py --dry-run` exits 0.
- `npx nx run sft-tools:test` exits 0 (Nx target wiring).
- CI grep gates: nessun match per `pydantic.v1`, `from langchain.pydantic_v1`, `f"INSERT|SELECT|UPDATE|DELETE`, `datetime.now()` (no UTC) dentro `packages/sft-tools/`.
- `python3 -c "from sft_tools import REPLAY_TOOLS; print([t.name for t in REPLAY_TOOLS])"` stdout `['replay_cmapss', 'replay_uci']`.
</verification>

<success_criteria>
- Pacchetto sft-tools committed; 3 tool importabili da Phase 4 agents via `langgraph.prebuilt.ToolNode`.
- Test contratto schema unificato `ReplayRecord` verde.
- Script download script funzionante in --dry-run; SHA256 logic presente.
- IOT-07, IOT-08 chiusi (replay loader exposed come Tool).
</success_criteria>

<output>
Create `.planning/phases/03-it-ot-simulation-layer/03-02-SUMMARY.md` con: file creati, output pytest, contratto DataFrame verificato, decisioni OQ5 (deterministic asset mapping) documentate.
</output>
