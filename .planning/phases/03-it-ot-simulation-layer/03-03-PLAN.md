---
phase: 03-it-ot-simulation-layer
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - simulators/sim-textile/pyproject.toml
  - simulators/sim-textile/project.json
  - simulators/sim-textile/src/sim_textile/__init__.py
  - simulators/sim-textile/src/sim_textile/main.py
  - simulators/sim-textile/src/sim_textile/server.py
  - simulators/sim-textile/src/sim_textile/emitter.py
  - simulators/sim-textile/src/sim_textile/models.py
  - simulators/sim-textile/src/sim_textile/cli.py
  - simulators/sim-textile/src/sim_textile/metrics.py
  - simulators/sim-textile/src/sim_textile/profile_loader.py
  - simulators/sim-textile/src/sim_textile/faults/__init__.py
  - simulators/sim-textile/src/sim_textile/faults/nan.py
  - simulators/sim-textile/src/sim_textile/faults/drift.py
  - simulators/sim-textile/src/sim_textile/faults/jitter.py
  - simulators/sim-textile/src/sim_textile/faults/burst.py
  - simulators/sim-textile/src/sim_textile/faults/alarm_storm.py
  - simulators/sim-textile/profiles/loom.yaml
  - simulators/sim-textile/profiles/spinning.yaml
  - simulators/sim-textile/profiles/warping.yaml
  - simulators/sim-textile/profiles/dyeing.yaml
  - simulators/sim-textile/profiles/finishing.yaml
  - simulators/sim-textile/schemas/fault-profile.schema.json
  - simulators/sim-textile/tests/conftest.py
  - simulators/sim-textile/tests/test_faults.py
  - simulators/sim-textile/tests/test_emitter.py
  - simulators/sim-textile/tests/test_profile_loader.py
  - simulators/sim-textile/tests/test_profile_validation.py
  - simulators/sim-textile/Dockerfile
  - scripts/validate-fault-profiles.py
autonomous: true
requirements:
  - IOT-01
  - IOT-02
  - IOT-03
tags:
  - simulator
  - opcua
  - asyncua
  - fault-injection
  - prometheus

must_haves:
  truths:
    - "`sim-textile` è un singolo processo asyncio che emette eventi sensori per 5 asset_family (loom, spinning, warping, dyeing, finishing) — IOT-01"
    - "Un `asyncua.Server` espone OPC-UA `opc.tcp://0.0.0.0:4840/sft-textile/` con un namespace per asset_family (`urn:mantis:<family>`); ogni Variable è `set_writable(False)` (data-diode protocol-level) — IOT-02"
    - "5 file YAML profiles in `simulators/sim-textile/profiles/` definiscono fault injection per family — D-44"
    - "Le funzioni fault (`nan`, `drift`, `jitter`, `burst`, `alarm_storm`) sono pure su `EmitterState` frozen dataclass + replace-based mutation — IOT-03"
    - "Prometheus `/metrics` su porta 8080 espone `sim_events_emitted_total{asset_family,asset_id,tag_id}`, `sim_fault_injected_total{type,asset_family}`, `sim_message_rate_per_second`"
    - "Tutti i timestamp emessi sono `datetime.now(UTC)` (tz-aware) — Pattern S-6 + Pitfall 7"
    - "Tutti i YAML loader usano `yaml.safe_load` — Pattern S-2"
    - "Dockerfile multi-stage (Python 3.12 slim) builda il container `sft-sim-textile`"
  artifacts:
    - path: "simulators/sim-textile/src/sim_textile/server.py"
      provides: "asyncua.Server con namespace per family + Variable set_writable(False)"
      contains: "set_writable(False)"
    - path: "simulators/sim-textile/src/sim_textile/emitter.py"
      provides: "asset_emitter(profile, asset, opcua_vars) asyncio task con baseline + fault apply"
      contains: "asyncio.create_task"
    - path: "simulators/sim-textile/src/sim_textile/faults/jitter.py"
      provides: "apply_jitter pure function ±band_pct"
    - path: "simulators/sim-textile/profiles/loom.yaml"
      provides: "fault profile loom asset_family con jitter, drift, alarm_storm"
      contains: "asset_family: loom"
    - path: "simulators/sim-textile/schemas/fault-profile.schema.json"
      provides: "JSON Schema Draft 2020-12 per fault profile YAML"
      contains: "$schema"
    - path: "simulators/sim-textile/Dockerfile"
      provides: "Multi-stage build python:3.12-slim + uv sync + CMD sim-textile"
      contains: "python:3.12-slim"
    - path: "scripts/validate-fault-profiles.py"
      provides: "CLI validator per 5 profiles + range check nan_probability + cross-check tag_id contro sft-assets"
      contains: "Draft202012Validator"
  key_links:
    - from: "simulators/sim-textile/src/sim_textile/server.py"
      to: "packages/sft-assets/src/sft_assets/_loader.py"
      via: "from sft_assets import load_assets, load_tag_dict"
      pattern: "from sft_assets import"
    - from: "simulators/sim-textile/src/sim_textile/emitter.py"
      to: "simulators/sim-textile/src/sim_textile/faults/"
      via: "apply_drift, apply_jitter, maybe_nan, check_alarm_storm pure functions"
      pattern: "from sim_textile.faults"
    - from: "simulators/sim-textile/src/sim_textile/main.py"
      to: "simulators/sim-textile/profiles/*.yaml"
      via: "SIM_PROFILES env + yaml.safe_load via profile_loader"
      pattern: "profile_loader"
---

<objective>
Riempire il container `simulators/sim-textile/` (scaffold da Phase 1) con: singolo processo asyncio che genera eventi sensori realistici per 5 asset_family, server OPC-UA `asyncua` multi-namespace, fault injection profile-driven (5 YAML), state machine pura per fault, metrics Prometheus, CLI argparse, Dockerfile.

Purpose (D-44 + D-50 + IOT-01/02/03): produrre la sorgente OT-side della pipeline. Phase 4+ agents (AnomalyDetector, PredictiveMaintenance) richiedono stream realistici con fault calibrati per training/test. asyncua Variables impostati `writable=False` rappresentano il primo layer di protocol-level data-diode.

Output: container build-able, `uv run sim-textile --dry-run` esegue localmente, pytest unit verde su faults + emitter + profile loader, Prometheus `/metrics` espone counters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md
@.planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md
@.planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md
@.planning/phases/03-it-ot-simulation-layer/03-VALIDATION.md
@.planning/phases/03-it-ot-simulation-layer/03-01-PLAN.md

# Existing scaffold
@simulators/sim-textile/pyproject.toml
@simulators/sim-textile/project.json
@simulators/sim-textile/README.md

# Analogs
@packages/sft-domain/src/sft_domain/glossary/_models.py
@packages/sft-domain/src/sft_domain/schemas/glossary.schema.json
@scripts/validate-glossary-schema.py

<interfaces>
<!-- Contracts: sft-assets è dipendenza upstream, profile YAML schema è constraint -->

From `sft_assets` (Plan 03-01):
- `load_assets() -> tuple[Asset, ...]` (30 asset, 5 family)
- `load_tag_dict() -> dict[str, Tag]` (≥ 24 tag distinti)
- `AssetFamily` enum (5 valori), `SemanticType` enum

Profile YAML schema (D-44):
```yaml
asset_family: loom            # required, enum 5 values
sample_rate_hz: 10            # required, > 0
fault_injection:              # required
  nan_probability: 0.001      # required, 0..1
  drift: {enabled: true, rate_per_hour: 0.005, affected_tags: [creel_speed]}
  jitter: {enabled: true, band_pct: 5, affected_tags: [warp_tension]}
  burst_noise: {enabled: false}
  alarm_storm: {enabled: true, threshold_events: 50, affected_tags: [broken_pick_count]}
default_baseline:             # required
  warp_tension: {value: 25, unit: N}
  pick_density: {value: 24, unit: picks_per_cm}
```

Pure fault function contracts (RESEARCH §Pattern 2):
- `apply_drift(state, rate_per_hour, dt_seconds) -> (new_state, drift_value)`
- `apply_jitter(value, band_pct) -> jittered_value`
- `maybe_nan(value, nan_probability) -> float | nan`
- `check_alarm_storm(state, now, threshold, window_s=30) -> (new_state, is_storm)`
- `apply_burst(value, magnitude_pct, t_in_burst, duration_s) -> burst_value`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fault state machine pure functions + Pydantic profile models + 5 fault profiles YAML + schema + validator</name>
  <files>simulators/sim-textile/src/sim_textile/__init__.py, simulators/sim-textile/src/sim_textile/models.py, simulators/sim-textile/src/sim_textile/profile_loader.py, simulators/sim-textile/src/sim_textile/faults/__init__.py, simulators/sim-textile/src/sim_textile/faults/nan.py, simulators/sim-textile/src/sim_textile/faults/drift.py, simulators/sim-textile/src/sim_textile/faults/jitter.py, simulators/sim-textile/src/sim_textile/faults/burst.py, simulators/sim-textile/src/sim_textile/faults/alarm_storm.py, simulators/sim-textile/profiles/loom.yaml, simulators/sim-textile/profiles/spinning.yaml, simulators/sim-textile/profiles/warping.yaml, simulators/sim-textile/profiles/dyeing.yaml, simulators/sim-textile/profiles/finishing.yaml, simulators/sim-textile/schemas/fault-profile.schema.json, simulators/sim-textile/tests/conftest.py, simulators/sim-textile/tests/test_faults.py, simulators/sim-textile/tests/test_profile_loader.py, simulators/sim-textile/tests/test_profile_validation.py, simulators/sim-textile/pyproject.toml, simulators/sim-textile/project.json, scripts/validate-fault-profiles.py</files>
  <read_first>
    - simulators/sim-textile/pyproject.toml (esistente — riempire deps)
    - simulators/sim-textile/project.json (esistente — aggiungere implicitDependencies + target validate-profiles)
    - packages/sft-domain/src/sft_domain/glossary/_models.py (analog Pydantic frozen + extra=forbid)
    - packages/sft-domain/src/sft_domain/schemas/glossary.schema.json (analog Draft 2020-12)
    - scripts/validate-glossary-schema.py (analog CLI validator)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 2 (fault state machine, lines 468-504) — copia EXACT signatures
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-44 (5 profile YAML structure completa)
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md Pattern S-1, S-2, S-4, S-5 + sezione validate-fault-profiles.py guidance (lines 515-518)
    - packages/sft-assets/src/sft_assets/registry.yaml (Plan 03-01 — i tag_id referenziati nei profile devono matchare registry)
  </read_first>
  <behavior>
    - Test 1 (test_faults.test_apply_jitter): `apply_jitter(100.0, band_pct=10)` ritorna valore in [90,110]; con `random.seed(42)` deterministic.
    - Test 2 (test_faults.test_apply_drift): `apply_drift(EmitterState(drift_accumulated=0,...), rate_per_hour=0.5, dt_seconds=3600)` ritorna `new_state.drift_accumulated == 0.5`.
    - Test 3 (test_faults.test_apply_drift_immutability): chiamare `apply_drift(state,...)` non muta `state` originale (frozen dataclass — replace-based).
    - Test 4 (test_faults.test_maybe_nan): `maybe_nan(1.0, 0.0)` sempre 1.0; `maybe_nan(1.0, 1.0)` sempre NaN (`math.isnan`).
    - Test 5 (test_faults.test_check_alarm_storm_triggers): dopo `threshold` chiamate consecutive entro window_s, `is_storm == True`.
    - Test 6 (test_faults.test_check_alarm_storm_prune): eventi più vecchi di window_s sono prunati e non contano.
    - Test 7 (test_faults.test_apply_burst): `apply_burst(base=10, magnitude_pct=200, t_in_burst=1, duration_s=2)` ritorna valore con magnitude amplificata.
    - Test 8 (test_profile_loader.test_load_loom): `load_profile("loom")` ritorna `FaultProfile` con `asset_family==AssetFamily.LOOM` e `fault_injection.jitter.enabled==True`.
    - Test 9 (test_profile_loader.test_load_all): `load_all_profiles()` ritorna dict con 5 chiavi `{loom,spinning,warping,dyeing,finishing}`.
    - Test 10 (test_profile_validation.test_schema_self_valid): `fault-profile.schema.json` è valido Draft 2020-12 (`Draft202012Validator.check_schema(schema) is None`).
    - Test 11 (test_profile_validation.test_all_profiles_schema_valid): tutti i 5 YAML validano contro schema (0 errors).
    - Test 12 (test_profile_validation.test_invalid_nan_probability_rejected): profile YAML con `nan_probability: 1.5` solleva ValidationError.
    - Test 13 (test_profile_validation.test_tags_match_registry): tutti gli `affected_tags` e le chiavi di `default_baseline` referenziano tag_id presenti in `load_tag_dict()` (cross-check con sft-assets).
  </behavior>
  <action>
    1. **simulators/sim-textile/pyproject.toml** (modify scaffold) — Aggiungere dependencies: `["asyncua>=1.1.8,<2.0", "pydantic>=2.13.4", "pyyaml>=6.0", "structlog>=24", "prometheus-client>=0.21", "jsonschema>=4.23", "sft-assets"]`. Dev: `["pytest>=8.0", "pytest-asyncio>=0.24"]`. `[tool.uv.sources]` con `sft-assets = { workspace = true }`. `[project.scripts]` `sim-textile = "sim_textile.cli:main"`. `[tool.pytest.ini_options]` `asyncio_mode = "auto"`, `testpaths = ["tests"]`.

    2. **simulators/sim-textile/project.json** (modify scaffold) — Estendere `implicitDependencies` includendo `"sft-assets"`. Aggiungere target `validate-profiles`: `{"executor": "@nxlv/python:run-commands", "options": {"command": "python3 ../../scripts/validate-fault-profiles.py", "cwd": "."}}`.

    3. **src/sim_textile/models.py** — Pydantic models con `model_config = {"frozen": True, "extra": "forbid"}` (Pattern S-1):
       - `class BaselineValue(BaseModel)`: `value: float`, `unit: str`.
       - `class DriftConfig(BaseModel)`: `enabled: bool`, `rate_per_hour: Annotated[float, Field(ge=0)] = 0.0`, `affected_tags: tuple[str, ...] = ()`.
       - `class JitterConfig(BaseModel)`: `enabled: bool`, `band_pct: Annotated[float, Field(ge=0, le=100)] = 0.0`, `affected_tags: tuple[str, ...] = ()`.
       - `class BurstNoiseConfig(BaseModel)`: `enabled: bool`, `magnitude_pct: float = 0.0`, `duration_s: float = 0.0`, `period_s: float = 0.0`, `affected_tags: tuple[str, ...] = ()`.
       - `class AlarmStormConfig(BaseModel)`: `enabled: bool`, `threshold_events: Annotated[int, Field(ge=1)] = 50`, `window_s: float = 30.0`, `affected_tags: tuple[str, ...] = ()`.
       - `class FaultInjection(BaseModel)`: `nan_probability: Annotated[float, Field(ge=0, le=1)] = 0.0`, `drift: DriftConfig`, `jitter: JitterConfig`, `burst_noise: BurstNoiseConfig`, `alarm_storm: AlarmStormConfig`.
       - `class FaultProfile(BaseModel)`: `asset_family: AssetFamily` (import da sft_assets), `sample_rate_hz: Annotated[float, Field(gt=0, le=1000)]`, `fault_injection: FaultInjection`, `default_baseline: dict[str, BaselineValue]`.
       - `@dataclass(frozen=True) class EmitterState`: `drift_accumulated: float = 0.0`, `recent_alarms: tuple[datetime, ...] = ()`, `burst_t_started: datetime | None = None`. (RESEARCH §Pattern 2.)

    4. **src/sim_textile/faults/nan.py** — `def maybe_nan(value: float, nan_probability: float) -> float`: `return float("nan") if random.random() < nan_probability else value`.

    5. **src/sim_textile/faults/drift.py** — `def apply_drift(state: EmitterState, rate_per_hour: float, dt_seconds: float) -> tuple[EmitterState, float]`: usa `dataclasses.replace(state, drift_accumulated=state.drift_accumulated + rate_per_hour*(dt_seconds/3600))` (RESEARCH lines 487-490).

    6. **src/sim_textile/faults/jitter.py** — `def apply_jitter(value: float, band_pct: float) -> float`: `value * (1.0 + random.uniform(-band_pct/100.0, band_pct/100.0))`.

    7. **src/sim_textile/faults/burst.py** — `def apply_burst(base_value: float, magnitude_pct: float, t_in_burst: float, duration_s: float) -> float`: durante `0 <= t_in_burst <= duration_s`, ritorna `base_value * (1 + magnitude_pct/100)` con decay lineare verso fine durata.

    8. **src/sim_textile/faults/alarm_storm.py** — `def check_alarm_storm(state: EmitterState, now: datetime, threshold: int, window_s: float = 30) -> tuple[EmitterState, bool]`. Body: cutoff = `now.timestamp() - window_s`; `pruned = tuple(t for t in state.recent_alarms if t.timestamp() >= cutoff)`; `new_state = replace(state, recent_alarms=pruned + (now,))`; `storm = len(new_state.recent_alarms) >= threshold`; ritorna `(new_state, storm)` (RESEARCH lines 498-503).

    9. **src/sim_textile/faults/__init__.py** — barrel exports tutte le funzioni pure.

    10. **src/sim_textile/profile_loader.py** — `_PROFILES_DIR = Path(__file__).parent.parent.parent / "profiles"`. `@lru_cache(maxsize=5) def load_profile(family: str) -> FaultProfile`: legge `{family}.yaml` via `yaml.safe_load`, ritorna `FaultProfile.model_validate(raw)`. `def load_all_profiles() -> dict[str, FaultProfile]`: itera `["loom","spinning","warping","dyeing","finishing"]`. `def invalidate_cache() -> None`: `load_profile.cache_clear()`.

    11. **profiles/loom.yaml** — Struttura esatta D-44:
        ```
        asset_family: loom
        sample_rate_hz: 10
        fault_injection:
          nan_probability: 0.001
          drift: {enabled: true, rate_per_hour: 0.005, affected_tags: [creel_speed]}
          jitter: {enabled: true, band_pct: 5, affected_tags: [warp_tension]}
          burst_noise: {enabled: false}
          alarm_storm: {enabled: true, threshold_events: 50, window_s: 30, affected_tags: [broken_pick_count]}
        default_baseline:
          warp_tension: {value: 25, unit: N}
          pick_density: {value: 24, unit: picks_per_cm}
          creel_speed: {value: 850, unit: rpm}
          broken_pick_count: {value: 0, unit: count}
          loom_temperature: {value: 32, unit: degC}
        ```

    12. **profiles/spinning.yaml** — `asset_family: spinning`, `sample_rate_hz: 5`, jitter su `spindle_vibration` band_pct=8 affected_tags=[spindle_vibration], drift su `roller_temperature` rate_per_hour=0.01, alarm_storm su `broken_end_count` threshold_events=30, baseline `spindle_speed=14000 rpm`, `yarn_tension=15 cN`, `roller_temperature=45 degC`, `broken_end_count=0 count`, `spindle_vibration=2.5 mm_s`.

    13. **profiles/warping.yaml** — `asset_family: warping`, `sample_rate_hz: 2`, drift su `tension_imbalance` rate_per_hour=0.02, burst_noise su `creel_feed_rate` magnitude_pct=30 duration_s=5 period_s=600, baseline `tension_imbalance=8 N`, `creel_feed_rate=120 m_min`, `warp_count=0 count`, `winding_speed=400 rpm`.

    14. **profiles/dyeing.yaml** — `asset_family: dyeing`, `sample_rate_hz: 1`, `nan_probability=0.005` (bath_ph sensor disconnect), drift su `bath_temperature` rate_per_hour=0.02, alarm_storm su `recipe_deviation` threshold_events=10, baseline `bath_temperature=85 degC`, `bath_ph=6.5 pH`, `bath_level=1500 mm`, `flow_rate=120 l_min`, `recipe_deviation=0 pct`, `bath_pressure=2.5 bar`.

    15. **profiles/finishing.yaml** — `asset_family: finishing`, `sample_rate_hz: 5`, drift su `humidity` rate_per_hour=0.03, burst_noise su `fabric_tension` magnitude_pct=20 duration_s=3 period_s=300, baseline `fabric_tension=180 N`, `chamber_temperature=170 degC`, `humidity=45 pct_rh`, `fabric_speed=35 m_min`.

    16. **schemas/fault-profile.schema.json** — JSON Schema Draft 2020-12: `$id="https://sim-textile/schemas/fault-profile.schema.json"`, required `[asset_family, sample_rate_hz, fault_injection, default_baseline]`, `additionalProperties: false`. `asset_family` enum 5 valori, `sample_rate_hz` `{type:"number", exclusiveMinimum:0, maximum:1000}`, `fault_injection.properties.nan_probability` `{type:"number", minimum:0, maximum:1}`. Sub-schemas per drift, jitter, burst_noise, alarm_storm con required `enabled` + optional config fields.

    17. **tests/conftest.py** — fixtures `sample_fault_profile_dict` (loom valido), `sample_invalid_profile_dict` (`nan_probability: 1.5`), `frozen_time` (`datetime(2026,5,18,12,0,0,tzinfo=UTC)`), `random_seed_42` (`autouse=True` chiamando `random.seed(42)`).

    18. **tests/test_faults.py + test_profile_loader.py + test_profile_validation.py** — Implementare i 13 test descritti in `<behavior>`.

    19. **scripts/validate-fault-profiles.py** — Copia struttura `scripts/validate-glossary-schema.py`:
        - `WORKSPACE_ROOT = Path(__file__).parent.parent`
        - `_PROFILES_DIR = WORKSPACE_ROOT / "simulators/sim-textile/profiles"`
        - `_SCHEMA = WORKSPACE_ROOT / "simulators/sim-textile/schemas/fault-profile.schema.json"`
        - argparse `--profiles-dir`, `--schema`, `--dry-run`.
        - Per ogni `*.yaml` in profiles dir: load `yaml.safe_load`, validate via `Draft202012Validator(schema).iter_errors`, cross-check `affected_tags ∪ default_baseline.keys()` esistano in `load_tag_dict()` (import sft_assets), cross-check `asset_family` matcha `AssetFamily` enum.
        - Stampa per ogni profile `OK [profiles/loom.yaml]: schema-valid, 5 tags registry-resolved`.
        - Exit 0 / 1.

    20. **src/sim_textile/__init__.py** — barrel: `from sim_textile.models import FaultProfile, EmitterState, BaselineValue, FaultInjection` + `from sim_textile.profile_loader import load_profile, load_all_profiles`.

    Decisioni: D-44 (5 YAML profiles + schema), IOT-03 (5 fault types implementati come pure functions), Pattern S-1/S-2/S-4/S-5.
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && uv run --project simulators/sim-textile pytest simulators/sim-textile/tests -x -v && python3 scripts/validate-fault-profiles.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project simulators/sim-textile pytest simulators/sim-textile/tests -x -v` exits 0; ≥ 13 test items passed.
    - `python3 scripts/validate-fault-profiles.py` exits 0; stdout contiene "5 profiles".
    - `ls simulators/sim-textile/profiles/*.yaml | wc -l` == 5.
    - `grep -c "^asset_family: loom" simulators/sim-textile/profiles/loom.yaml` == 1 (analog per altri 4).
    - `grep -rE "yaml\.load\(" simulators/sim-textile/` exits 1 (no match — Pattern S-2).
    - `grep -c "frozen=True" simulators/sim-textile/src/sim_textile/models.py` ≥ 1 (EmitterState dataclass).
    - `python3 -c "from sim_textile import load_all_profiles; p = load_all_profiles(); assert set(p.keys()) == {'loom','spinning','warping','dyeing','finishing'}"` exits 0.
  </acceptance_criteria>
  <done>Fault state machine pure-functional, 5 profiles YAML committati, schema + validator + 13 test verdi.</done>
</task>

<task type="auto">
  <name>Task 2: asyncua server multi-namespace + per-asset emitter + Prometheus metrics + CLI + Dockerfile</name>
  <files>simulators/sim-textile/src/sim_textile/server.py, simulators/sim-textile/src/sim_textile/emitter.py, simulators/sim-textile/src/sim_textile/main.py, simulators/sim-textile/src/sim_textile/cli.py, simulators/sim-textile/src/sim_textile/metrics.py, simulators/sim-textile/tests/test_emitter.py, simulators/sim-textile/Dockerfile</files>
  <read_first>
    - simulators/sim-textile/src/sim_textile/models.py + faults/ (Task 1 — pure functions)
    - simulators/sim-textile/profiles/*.yaml (Task 1)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 1 (asyncua Server multi-namespace, lines 411-466) — copia struttura `setup_server` con adattamenti
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 1 (asyncua subscription throttling — informativo per ot-bridge)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 7 (datetime tz-aware) + §Security checks 1 (CI grep gates)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-50 (single asyncio process + env SIM_PROFILES, SIM_TIME_SCALE, OPCUA_BIND, METRICS_PORT)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md claudes_discretion (Prometheus metric names) + structlog JSON logging
    - packages/sft-assets/src/sft_assets/_loader.py (Plan 03-01)
    - scripts/sync-python-versions.py (analog argparse + exit codes per cli.py pattern)
  </read_first>
  <action>
    1. **src/sim_textile/metrics.py**:
       - `from prometheus_client import Counter, Gauge, start_http_server`
       - `sim_events_emitted_total = Counter("sim_events_emitted_total", "Eventi emessi", ["asset_family","asset_id","tag_id"])`
       - `sim_fault_injected_total = Counter("sim_fault_injected_total", "Fault applicati", ["type","asset_family"])`
       - `sim_message_rate_gauge = Gauge("sim_message_rate_per_second", "Rate eventi/s")`
       - `def start_metrics(port: int = 8080) -> None`: chiama `start_http_server(port)`.

    2. **src/sim_textile/server.py** — asyncua Server setup. Replica struttura RESEARCH §Pattern 1:
       - `async def setup_server() -> tuple[Server, dict[tuple[str,str], "Node"]]`:
         - `server = Server(); await server.init()`
         - `endpoint = os.environ.get("OPCUA_BIND", "opc.tcp://0.0.0.0:4840") + "/sft-textile/"`; `server.set_endpoint(endpoint)`
         - `server.set_security_policy([ua.SecurityPolicyType.NoSecurity])`  # A-018 PoC; doc commento "Phase 11 hardening: Sign+Encrypt"
         - `from sft_assets import load_assets, load_tag_dict`
         - Per ogni `family` ∈ `sorted({a.asset_family.value for a in assets})`: `idx = await server.register_namespace(f"urn:mantis:{family}")`; `family_root = await server.nodes.objects.add_folder(idx, family)`.
         - Per ogni `asset`: `idx = family_to_ns_idx[asset.asset_family.value]`; `asset_obj = await family_root.add_object(idx, asset.asset_id)`; per ogni tag in asset: `var = await asset_obj.add_variable(idx, tag.tag_id, 0.0, varianttype=ua.VariantType.Double)`; `await var.set_writable(False)`  # CRITICAL protocol-level data-diode; `vars_map[(asset.asset_id, tag.tag_id)] = var`.
         - Ritorna `(server, vars_map)`.

    3. **src/sim_textile/emitter.py** — Per-asset asyncio task:
       - `async def asset_emitter(asset: Asset, profile: FaultProfile, vars_map: dict, *, time_scale: float = 1.0) -> None`:
         - `state = EmitterState()`; `dt = (1.0 / profile.sample_rate_hz) / time_scale`.
         - Loop infinito:
           - `now = datetime.now(UTC)`  # Pattern S-6 (NO `datetime.now()` naive — Pitfall 7).
           - Per ogni `tag` in `asset.tags`:
             - `tag_id = tag.tag_id`; `baseline_obj = profile.default_baseline.get(tag_id)`; `if baseline_obj is None: continue`.
             - `value = baseline_obj.value`.
             - Chain di applicazione fault (in ordine `drift → jitter → burst → maybe_nan`):
               - `if profile.fault_injection.drift.enabled and tag_id in profile.fault_injection.drift.affected_tags`: `state, drift = apply_drift(state, profile.fault_injection.drift.rate_per_hour, dt)`; `value += drift * baseline_obj.value`; incrementa `sim_fault_injected_total.labels(type="drift", asset_family=asset.asset_family.value).inc()`.
               - `if profile.fault_injection.jitter.enabled and tag_id in profile.fault_injection.jitter.affected_tags`: `value = apply_jitter(value, profile.fault_injection.jitter.band_pct)`; `sim_fault_injected_total.labels(type="jitter", asset_family=asset.asset_family.value).inc()`.
               - `if profile.fault_injection.burst_noise.enabled and tag_id in profile.fault_injection.burst_noise.affected_tags`: stato locale per `burst_t_started`; durante burst, `value = apply_burst(value, magnitude_pct, t_in_burst, duration_s)`.
               - `value = maybe_nan(value, profile.fault_injection.nan_probability)`; se NaN, `sim_fault_injected_total.labels(type="nan", asset_family=asset.asset_family.value).inc()`.
             - `quality_code = ua.StatusCode(0x80AF0000) if math.isnan(value) else ua.StatusCode(0)`.
             - `if profile.fault_injection.alarm_storm.enabled and tag_id in profile.fault_injection.alarm_storm.affected_tags`:
               - `state, is_storm = check_alarm_storm(state, now, profile.fault_injection.alarm_storm.threshold_events, profile.fault_injection.alarm_storm.window_s)`.
               - `if is_storm`: `quality_code = ua.StatusCode(0x80AF0000)`; `sim_fault_injected_total.labels(type="alarm_storm", asset_family=asset.asset_family.value).inc()`.
             - `var_node = vars_map[(asset.asset_id, tag_id)]`; `dv = ua.DataValue(ua.Variant(value, ua.VariantType.Double), SourceTimestamp=now, StatusCode_=quality_code)`; `await var_node.write_value(dv)`.
             - `sim_events_emitted_total.labels(asset_family=asset.asset_family.value, asset_id=asset.asset_id, tag_id=tag_id).inc()`.
           - `await asyncio.sleep(dt)`.
         - Wrap del loop in `try` / `except asyncio.CancelledError: raise` / `except Exception as e: logger.error("emitter_error", asset_id=asset.asset_id, exc_info=e); await asyncio.sleep(1)` (no silent swallow — coding-style.md).

    4. **src/sim_textile/main.py**:
       - `async def run(profiles_enabled: list[str], time_scale: float = 1.0, metrics_port: int = 8080) -> None`:
         - `logger = structlog.get_logger("sim-textile")`
         - `start_metrics(metrics_port)`.
         - `(server, vars_map) = await setup_server()`.
         - `profile_dict = load_all_profiles()`; `profile_dict_enabled = {f: profile_dict[f] for f in profiles_enabled if f in profile_dict}`.
         - `assets = [a for a in load_assets() if a.asset_family.value in profile_dict_enabled]`.
         - `async with server:` → `tasks = [asyncio.create_task(asset_emitter(a, profile_dict_enabled[a.asset_family.value], vars_map, time_scale=time_scale)) for a in assets]` → `await asyncio.gather(*tasks)`.

    5. **src/sim_textile/cli.py**:
       - `def main() -> None`:
         - argparse:
           - `--profile FAMILY` (action="append", default `None` → fallback env `SIM_PROFILES` o all 5).
           - `--time-scale FLOAT` (default 1.0, env fallback `SIM_TIME_SCALE`).
           - `--metrics-port INT` (default 8080, env fallback `METRICS_PORT`).
           - `--dry-run` (print config risolto + exit 0).
         - Risolvi `profiles` come `args.profile` se non None, altrimenti `os.environ.get("SIM_PROFILES","loom,spinning,warping,dyeing,finishing").split(",")`.
         - Se `--dry-run`: stampa `Resolved config: profiles={profiles}, time_scale={ts}, metrics_port={mp}` → exit 0.
         - Altrimenti `asyncio.run(run(profiles, time_scale, metrics_port))`.

    6. **tests/test_emitter.py** — Unit test:
       - `test_emitter_uses_tz_aware_now`: monkeypatch `sim_textile.emitter.datetime` con mock, run emitter per 1 ciclo via `asyncio.wait_for(timeout=0.2)` (catch TimeoutError), assert ogni `datetime.now(...)` call ha `UTC` kw.
       - `test_main_dry_run_resolves_5_families` (test di smoke su CLI): subprocess run `uv run sim-textile --dry-run --profile loom --profile spinning --profile warping --profile dyeing --profile finishing` → exit 0 e stdout contiene 5 family names.
       - `test_all_families_emit` (IOT-01 coverage): monkeypatch `start_metrics` e `Server`/`setup_server` con mock; chiama `await asyncio.wait_for(run(["loom","spinning","warping","dyeing","finishing"], time_scale=100.0), timeout=0.3)`; cattura `TimeoutError`; assert `sim_events_emitted_total._metrics` ha entries per tutte 5 family.

    7. **simulators/sim-textile/Dockerfile** — Multi-stage:
        - Stage `builder`:
          - `FROM python:3.12-slim AS builder`
          - `RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates && rm -rf /var/lib/apt/lists/*`
          - `RUN pip install --no-cache-dir uv==0.5.0`
          - `WORKDIR /build`
          - `COPY packages/sft-assets /build/packages/sft-assets`
          - `COPY simulators/sim-textile /build/simulators/sim-textile`
          - `WORKDIR /build/simulators/sim-textile`
          - `RUN uv pip install --system --no-cache-dir .`
        - Stage runtime:
          - `FROM python:3.12-slim`
          - `WORKDIR /app`
          - `COPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages`
          - `COPY --from=builder /usr/local/bin/sim-textile /usr/local/bin/sim-textile`
          - `COPY simulators/sim-textile/profiles /app/profiles`  (richiede build context monorepo-root)
          - `EXPOSE 4840 8080`
          - `ENV SIM_PROFILES=loom,spinning,warping,dyeing,finishing  SIM_TIME_SCALE=1.0  OPCUA_BIND=0.0.0.0:4840  METRICS_PORT=8080`
          - `USER 1000:1000`
          - `CMD ["sim-textile"]`
        - Comment header: "Build context: monorepo root (set in docker-compose `context: ../../`). Plan 03-06 configura compose."

    Decisioni: D-50 (single asyncio process + env config), IOT-01 (5 family emit), IOT-02 (OPC-UA server browseable + Variable writable=False), Pattern S-6 (UTC datetime), no datetime.now() naive (CI grep gate).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && uv run --project simulators/sim-textile pytest simulators/sim-textile/tests/test_emitter.py -x -v && grep -c "set_writable(False)" simulators/sim-textile/src/sim_textile/server.py && grep -c "datetime.now(UTC)" simulators/sim-textile/src/sim_textile/emitter.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project simulators/sim-textile pytest simulators/sim-textile/tests/test_emitter.py -x -v` exits 0.
    - `grep -c "set_writable(False)" simulators/sim-textile/src/sim_textile/server.py` ≥ 1 (protocol-level data-diode).
    - `grep -rE "datetime\.now\(\)" simulators/sim-textile/src` exits 1 (no naive — Pattern S-6).
    - `grep -c "datetime.now(UTC)" simulators/sim-textile/src/sim_textile/emitter.py` ≥ 1.
    - `uv run --project simulators/sim-textile sim-textile --dry-run` exits 0; stdout contiene "Resolved config: profiles=" e 5 family names.
    - `test -f simulators/sim-textile/Dockerfile` exit 0; `grep -c "FROM python:3.12-slim" simulators/sim-textile/Dockerfile` ≥ 2 (multi-stage).
    - `grep -c "set_value\|write_attribute\|write_value" simulators/sim-textile/src/` non-zero (sim-textile EMITS via `write_value` server-side: questo è OK — è sim, NON ot-bridge; il gate D-51 Layer 3 si applica solo a `services/ot-bridge/src/`).
    - `npx nx run sim-textile:validate-profiles` exits 0.
  </acceptance_criteria>
  <done>Container build-able, server OPC-UA esposto su 4840 con set_writable(False), emitter timestamp tz-aware, metrics esposti su 8080, CLI dry-run verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML profile → Pydantic loader | profile YAML è artifact build; safe_load + Pydantic frozen è gate. |
| OPC-UA client → Variable nodes | sim-textile è SERVER; non accetta write client-side (Variable `set_writable(False)`). |
| Process → Prometheus endpoint | `/metrics` su porta 8080 espone counter+labels; no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-yaml | Tampering / RCE | `profile_loader.py`, `validate-fault-profiles.py` | mitigate | `yaml.safe_load` esclusivo; CI grep gate `grep -rE "yaml\.load\(" simulators/sim-textile/` exit 1. Riferisce T-V5-yaml. |
| T-03-03-pydantic | Tampering | `FaultProfile`, `FaultInjection` | mitigate | `frozen=True, extra="forbid"`; field validators range (`nan_probability` 0..1, `band_pct` 0..100, `sample_rate_hz > 0`). Riferisce T-V5-pydantic. |
| T-03-03-opcua-write | Tampering / EoP | `server.py` Variable nodes | mitigate | `set_writable(False)` su ogni Variable (protocol-level rejection); A-018 anonymous PoC documentato; Phase 11 hardening Sign+Encrypt. Riferisce T-DATA-DIODE Layer 1 (server-side). |
| T-03-03-tz-naive | Tampering | `emitter.py` timestamp | mitigate | Pattern S-6: solo `datetime.now(UTC)`; CI grep gate `grep -rE "datetime\.now\(\)" simulators/sim-textile/src` exit 1. Riferisce T-V5-pydantic + Pitfall 7. |
| T-03-03-dos-emit | DoS | asyncio emitter loop | mitigate | `sample_rate_hz` Field(le=1000) impedisce profili con rate insostenibili; backpressure via Prometheus `sim_message_rate_gauge` (alerting Phase 11). |
| T-03-03-supply-chain | Tampering | `asyncua, prometheus-client, structlog` install | mitigate | RESEARCH §Package Legitimacy Audit: asyncua approved, prometheus-client WARN ma slopcheck established (PATTERNS approves); pin versioni in pyproject.toml. |
| T-03-03-anon-opcua | Spoofing | OPC-UA endpoint 4840 | accept | A-018 PoC NoSecurity (locked). Mitigation: docker-compose network ACL (Plan 03-06 D-51 Layer 1) limita accesso a `sft-sim`/`sft-ot` only; Phase 11 introduce Sign+Encrypt. |

`security_enforcement: enabled` (ASVS V4 + V5 + V14). Nessuna installazione npm/pip nuova oltre quelle già auditate.
</threat_model>

<verification>
- `uv run --project simulators/sim-textile pytest simulators/sim-textile/tests -x -v` exits 0.
- `python3 scripts/validate-fault-profiles.py` exits 0.
- `uv run --project simulators/sim-textile sim-textile --dry-run` exits 0.
- `grep -c "set_writable(False)" simulators/sim-textile/src/sim_textile/server.py` ≥ 1.
- `grep -rE "yaml\.load\(" simulators/sim-textile/` exits 1.
- `grep -rE "datetime\.now\(\)" simulators/sim-textile/src` exits 1 (no naive).
- `npx nx run sim-textile:test` + `npx nx run sim-textile:validate-profiles` entrambi exit 0.
- `docker build -t sft-sim-textile:plan-03 -f simulators/sim-textile/Dockerfile .` exit 0 (build context monorepo-root). Build accettato come "ok" anche se docker non disponibile (Plan 03-06 verifica via compose).
</verification>

<success_criteria>
- IOT-01 chiuso: simulator emette per 5 asset family (test).
- IOT-02 chiuso (server-side): OPC-UA server con namespace browseable + Variable non-writable.
- IOT-03 chiuso: 5 fault type implementati come pure functions con test deterministici (seed=42).
- Container build-able; profile_loader idempotente con lru_cache.
- Nessuna regressione in `packages/sft-assets/`.
</success_criteria>

<output>
Create `.planning/phases/03-it-ot-simulation-layer/03-03-SUMMARY.md` con: file creati, test output, eventuali deviazioni (es. burst implementation), conferma `set_writable(False)` su ogni Variable.
</output>
