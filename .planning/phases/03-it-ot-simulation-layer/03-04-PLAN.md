---
phase: 03-it-ot-simulation-layer
plan: 04
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - services/ot-bridge/pyproject.toml
  - services/ot-bridge/project.json
  - services/ot-bridge/src/svc_ot_bridge/__init__.py
  - services/ot-bridge/src/svc_ot_bridge/main.py
  - services/ot-bridge/src/svc_ot_bridge/opcua_client.py
  - services/ot-bridge/src/svc_ot_bridge/normalizer.py
  - services/ot-bridge/src/svc_ot_bridge/nats_publisher.py
  - services/ot-bridge/src/svc_ot_bridge/timescale_writer.py
  - services/ot-bridge/src/svc_ot_bridge/models.py
  - services/ot-bridge/src/svc_ot_bridge/metrics.py
  - services/ot-bridge/tests/conftest.py
  - services/ot-bridge/tests/test_normalizer.py
  - services/ot-bridge/tests/test_subject_derivation.py
  - services/ot-bridge/tests/test_publisher.py
  - services/ot-bridge/tests/test_writer.py
  - services/ot-bridge/Dockerfile
  - scripts/nats-bootstrap-streams.py
autonomous: true
requirements:
  - IOT-04
  - IOT-05
tags:
  - ot-bridge
  - nats-jetstream
  - asyncpg
  - data-diode
  - subject-hierarchy

must_haves:
  truths:
    - "`ot-bridge` subscribe via `asyncua.Client` a `opc.tcp://sim-textile:4840`, riceve `DataChangeNotification`, normalizza in `SensorEvent` Pydantic frozen — IOT-05 bridge"
    - "NATS publisher emette su subject `sensor.events.<asset_family>.<asset_id>.<tag_id>` via JetStream — IOT-04 + D-52"
    - "asyncpg writer batcha 500 eventi o flush 100ms → `executemany INSERT INTO sensor_events` con placeholder $1..$7 — IOT-06 ingest path"
    - "Zero chiamate `set_value`/`write_attribute`/`write_value` in `services/ot-bridge/src/` — D-51 Layer 3 (CI grep gate)"
    - "Zero f-string SQL nei writer/query — Security check 1"
    - "asyncpg pool: min_size=10, max_size=20, statement_cache_size=0, command_timeout=10s — Pitfall 6"
    - "ot-bridge pubblica anche su `audit.ot.bridge` events {ts, level, event_type, details} — OQ3 resolution (sim-textile NON pubblica audit)"
    - "structlog JSON output con fields `service, asset_id, tag_id, event_type`"
    - "Dockerfile multi-stage builda container `sft-ot-bridge`"
  artifacts:
    - path: "services/ot-bridge/src/svc_ot_bridge/opcua_client.py"
      provides: "asyncua.Client subscribe con publishing_interval=50, samples_per_publish=10 (Pitfall 1)"
      contains: "asyncua.Client"
    - path: "services/ot-bridge/src/svc_ot_bridge/normalizer.py"
      provides: "Pure transform DataChangeNotification → SensorEvent Pydantic frozen"
      contains: "SensorEvent"
    - path: "services/ot-bridge/src/svc_ot_bridge/nats_publisher.py"
      provides: "JetStream publish con subject derivation + audit.ot.bridge"
      contains: "sensor.events"
    - path: "services/ot-bridge/src/svc_ot_bridge/timescale_writer.py"
      provides: "asyncpg.Pool + executemany batch=500 + flush=100ms + statement_cache_size=0"
      contains: "statement_cache_size=0"
    - path: "scripts/nats-bootstrap-streams.py"
      provides: "Idempotent JetStream stream SENSOR_EVENTS setup (add_stream → update_stream fallback)"
      contains: "StreamConfig"
    - path: "services/ot-bridge/Dockerfile"
      provides: "Multi-stage build python:3.12-slim"
      contains: "python:3.12-slim"
  key_links:
    - from: "services/ot-bridge/src/svc_ot_bridge/normalizer.py"
      to: "packages/sft-assets/src/sft_assets/_loader.py"
      via: "load_assets_dict, load_tag_dict per resolve asset_family + unit"
      pattern: "from sft_assets import"
    - from: "services/ot-bridge/src/svc_ot_bridge/nats_publisher.py"
      to: "NATS JetStream stream SENSOR_EVENTS"
      via: "subject derivation: f'sensor.events.{family}.{asset_id}.{tag_id}'"
      pattern: "sensor\\.events"
    - from: "services/ot-bridge/src/svc_ot_bridge/timescale_writer.py"
      to: "TimescaleDB sensor_events hypertable"
      via: "asyncpg.executemany con $1..$7 placeholders"
      pattern: "\\$1.*\\$2.*\\$3.*\\$4.*\\$5.*\\$6.*\\$7"
---

<objective>
Riempire `services/ot-bridge/` (scaffold Phase 1): unico container DMZ-bridge che subscribe a OPC-UA su `sft-ot` network e fan-out a NATS JetStream + TimescaleDB su `sft-core` network. Implementa **layer applicativo del data-diode** (D-51 Layer 3: zero write call OPC-UA), normalizzazione `SensorEvent` Pydantic frozen, subject derivation D-52, batch insert asyncpg.

Purpose (D-51 + D-52 + IOT-04/05/06 partial): produce IT-side substrate consumabile da Phase 4 agents. Audit-ready: structlog JSON + `audit.ot.bridge` NATS subject (OQ3).

Output: container build-able, pytest unit verde (normalizer/subject derivation/publisher mock/writer mock), bootstrap NATS script idempotente, gate D-51 Layer 3 grep verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md
@.planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md
@.planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md
@.planning/phases/03-it-ot-simulation-layer/03-VALIDATION.md
@.planning/phases/03-it-ot-simulation-layer/03-01-PLAN.md

# Existing scaffold
@services/ot-bridge/pyproject.toml
@services/ot-bridge/project.json
@services/ot-bridge/README.md

# Analogs
@packages/sft-domain/src/sft_domain/glossary/_models.py
@scripts/sync-python-versions.py

<interfaces>
<!-- Contratti chiave -->

`SensorEvent` Pydantic frozen + extra=forbid:
```python
class SensorEvent(BaseModel):
    model_config = {"frozen": True, "extra": "forbid"}
    asset_id: str             # match registry
    asset_family: AssetFamily # da sft-assets
    tag_id: str               # match tag dict
    timestamp_utc: datetime   # tz-aware (validator)
    value: float | None       # None se NaN, oppure float
    unit: str
    quality_code: int         # OPC-UA StatusCode int
    source: Literal["live", "replay_cmapss", "replay_uci"] = "live"
    server_received_ts: datetime  # tz-aware, when bridge received from OPC-UA
```

NATS subject derivation (D-52):
- `sensor.events.<family>.<asset_id>.<tag_id>` — main event stream
- `sensor.alarms.<family>.<asset_id>` — alarm storm aggregate (quality_code != 0)
- `audit.ot.bridge` — connection events, publish failures, pool exhaustion (OQ3)

JetStream stream config:
- name: `SENSOR_EVENTS`
- subjects: `["sensor.events.>", "sensor.alarms.>"]`
- retention: WorkQueuePolicy
- max_age: 7d (allineato D-49 compression)
- storage: FileStorage

asyncpg INSERT SQL (placeholder solo):
```
INSERT INTO sensor_events (asset_id, tag_id, timestamp_utc, value, unit, quality_code, source)
VALUES ($1, $2, $3, $4, $5, $6, $7)
```

asyncpg.Pool config: `min_size=10, max_size=20, statement_cache_size=0, command_timeout=10.0` (RESEARCH §Pattern 4 + Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SensorEvent + normalizer + subject derivation + nats publisher + timescale writer (con CI gates)</name>
  <files>services/ot-bridge/pyproject.toml, services/ot-bridge/project.json, services/ot-bridge/src/svc_ot_bridge/__init__.py, services/ot-bridge/src/svc_ot_bridge/models.py, services/ot-bridge/src/svc_ot_bridge/normalizer.py, services/ot-bridge/src/svc_ot_bridge/nats_publisher.py, services/ot-bridge/src/svc_ot_bridge/timescale_writer.py, services/ot-bridge/src/svc_ot_bridge/metrics.py, services/ot-bridge/tests/conftest.py, services/ot-bridge/tests/test_normalizer.py, services/ot-bridge/tests/test_subject_derivation.py, services/ot-bridge/tests/test_publisher.py, services/ot-bridge/tests/test_writer.py</files>
  <read_first>
    - services/ot-bridge/pyproject.toml (esistente — riempire deps)
    - services/ot-bridge/project.json (esistente — aggiungere implicitDependencies sft-assets)
    - packages/sft-domain/src/sft_domain/glossary/_models.py (analog Pydantic frozen)
    - packages/sft-assets/src/sft_assets/_models.py + _loader.py (Plan 03-01 — Asset, Tag, AssetFamily importati)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 1 (asyncua Client subscribe, lines 411-466 — informativo)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 3 (NATS JetStream idempotent + durable consumer, lines 506-558)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 4 (asyncpg executemany batch + statement_cache_size=0, lines 560-613)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 1, 3, 6, 7 (subscription throttling, JetStream config drift, statement_cache, datetime tz)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Security checks 1 (grep gates) + §Security checks 2 (validators)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-51 (3-layer data-diode + Layer 3 zero write call) + D-52 (subject hierarchy) + OQ3 (audit.ot.bridge scope) + claudes_discretion (asyncpg pool config, structlog)
  </read_first>
  <behavior>
    - Test 1 (test_normalizer.test_normalize_basic): `normalize_datachange(asset_id="LOOM-01", tag_id="warp_tension", opcua_value=25.3, opcua_status=0, source_ts=datetime(2026,5,18,tzinfo=UTC), server_received=datetime(...,tzinfo=UTC))` ritorna `SensorEvent` con `asset_family=AssetFamily.LOOM`, `unit="N"` risolti via `load_tag_dict()`.
    - Test 2 (test_normalizer.test_nan_value): se opcua_value è NaN, `SensorEvent.value is None` (mapping NaN→None per JSON-safe).
    - Test 3 (test_normalizer.test_immutability): `event.asset_id = "X"` solleva ValidationError (frozen).
    - Test 4 (test_normalizer.test_timestamp_tz_required): passare `source_ts=datetime(2026,5,18)` (naive) solleva ValidationError (validator `tzinfo is not None`).
    - Test 5 (test_normalizer.test_quality_code_alarm_passthrough): `opcua_status=0x80AF0000` → `event.quality_code == 0x80AF0000`.
    - Test 6 (test_subject_derivation.test_event_subject): `derive_event_subject(SensorEvent(asset_family=AssetFamily.LOOM, asset_id="LOOM-01", tag_id="warp_tension", ...))` ritorna `"sensor.events.loom.LOOM-01.warp_tension"`.
    - Test 7 (test_subject_derivation.test_alarm_subject): per evento con `quality_code != 0` → `derive_alarm_subject(event) == "sensor.alarms.loom.LOOM-01"`.
    - Test 8 (test_subject_derivation.test_audit_subject): `derive_audit_subject() == "audit.ot.bridge"` (constant).
    - Test 9 (test_publisher.test_publish_event): mock `nats.js.JetStreamContext.publish`; verify chiamato con `subject="sensor.events.loom.LOOM-01.warp_tension"` e payload JSON con i campi `SensorEvent.model_dump_json()`.
    - Test 10 (test_publisher.test_publish_audit): publisher emette evento con level=ERROR su `audit.ot.bridge`; payload include `ts, level, event_type, details`.
    - Test 11 (test_writer.test_executemany_placeholder): mock `asyncpg.Pool.acquire` → mock `conn.executemany`; verify SQL contiene esattamente `$1, $2, $3, $4, $5, $6, $7` e nessun f-string interpolation.
    - Test 12 (test_writer.test_batch_size): writer accumula 500 eventi prima di flush (mock `Pool.acquire` invocato solo dopo 500° push).
    - Test 13 (test_writer.test_flush_interval): writer flush dopo 100ms anche se buffer ha < 500 (uso `asyncio.wait_for + monkeypatch time`).
    - Test 14 (test_writer.test_pool_config): `TimescaleWriter.start()` chiama `asyncpg.create_pool` con kwargs `min_size=10, max_size=20, statement_cache_size=0, command_timeout=10.0` (mock + cattura kwargs).
  </behavior>
  <action>
    1. **services/ot-bridge/pyproject.toml** (modify) — Dependencies: `["asyncua>=1.1.8,<2.0", "nats-py>=2.14.0,<3.0", "asyncpg>=0.30,<1.0", "pydantic>=2.13.4", "structlog>=24", "prometheus-client>=0.21", "sft-assets"]`. Dev: `["pytest>=8.0", "pytest-asyncio>=0.24", "testcontainers>=4.14"]`. `[tool.uv.sources]` `sft-assets = { workspace = true }`. `[project.scripts]` `ot-bridge = "svc_ot_bridge.main:main"`. `[tool.pytest.ini_options]` `asyncio_mode = "auto"`, `testpaths = ["tests"]`.

    2. **services/ot-bridge/project.json** (modify) — Estendere `implicitDependencies` con `"sft-assets"`. Target `test` come Phase 1.

    3. **src/svc_ot_bridge/models.py** — `class SensorEvent(BaseModel)` con `model_config = {"frozen": True, "extra": "forbid"}`. Campi come da `<interfaces>` sopra. `@field_validator("timestamp_utc", "server_received_ts")` enforce `tzinfo is not None`. `@field_validator("asset_id")` enforce match registry (informativo — la validation forte è in normalizer pre-construction). `@model_validator(mode="after")` enforce `value is None ⇔ math.isnan(input)` (already handled in normalizer; doc).

       Inoltre `class AuditEvent(BaseModel)` frozen + extra=forbid: `ts: datetime` (tz-aware), `level: Literal["INFO","WARN","ERROR"]`, `event_type: str`, `details: dict[str, str | int | float | bool]`.

    4. **src/svc_ot_bridge/normalizer.py** — Pure transform:
       - `from sft_assets import load_assets_dict, load_tag_dict, AssetFamily`
       - `def normalize_datachange(asset_id: str, tag_id: str, opcua_value: float, opcua_status: int, source_ts: datetime, server_received: datetime) -> SensorEvent`:
         - `assets = load_assets_dict()`; `asset = assets.get(asset_id)`; se None: raise ValueError("Unknown asset_id ...").
         - `tag = load_tag_dict().get(tag_id)`; se None: raise ValueError.
         - `value = None if math.isnan(opcua_value) else opcua_value`.
         - return `SensorEvent(asset_id=asset_id, asset_family=asset.asset_family, tag_id=tag_id, timestamp_utc=source_ts, value=value, unit=tag.unit, quality_code=opcua_status, source="live", server_received_ts=server_received)`.
       - Test path: pure function, no I/O — facile da unit-testare.

    5. **src/svc_ot_bridge/nats_publisher.py**:
       - `def derive_event_subject(event: SensorEvent) -> str`: `f"sensor.events.{event.asset_family.value}.{event.asset_id}.{event.tag_id}"`.
       - `def derive_alarm_subject(event: SensorEvent) -> str`: `f"sensor.alarms.{event.asset_family.value}.{event.asset_id}"`.
       - `_AUDIT_SUBJECT = "audit.ot.bridge"`; `def derive_audit_subject() -> str`: return constant.
       - `class NatsPublisher`:
         - `async def start(self, url: str) -> None`: `self._nc = await nats.connect(url)`; `self._js = self._nc.jetstream()`.
         - `async def publish_event(self, event: SensorEvent) -> None`:
           - `subject = derive_event_subject(event)`; `payload = event.model_dump_json().encode("utf-8")`.
           - `await self._js.publish(subject, payload)`.
           - Se `event.quality_code != 0`: publish anche su alarm subject (compact summary).
         - `async def publish_audit(self, audit: AuditEvent) -> None`: `await self._js.publish(_AUDIT_SUBJECT, audit.model_dump_json().encode("utf-8"))`.
         - `async def close(self) -> None`: `await self._nc.drain()`.

       CRITICAL: no f-string SQL (no SQL here at all). No write_value/set_value — questo è un publisher, mai un OPC-UA writer.

    6. **src/svc_ot_bridge/timescale_writer.py** — Replica RESEARCH §Pattern 4:
       - `_INSERT_SQL = "INSERT INTO sensor_events (asset_id, tag_id, timestamp_utc, value, unit, quality_code, source) VALUES ($1, $2, $3, $4, $5, $6, $7)"` (constant module-level — no f-string).
       - `class TimescaleWriter`:
         - `__init__(self, dsn: str, batch_size: int = 500, flush_interval_s: float = 0.1)`: `self._buffer: list[SensorEvent] = []`; `self._lock = asyncio.Lock()`.
         - `async def start(self) -> None`: `self._pool = await asyncpg.create_pool(dsn=self._dsn, min_size=10, max_size=20, statement_cache_size=0, command_timeout=10.0)` (Pitfall 6). Avvia task `self._flush_loop()` come `asyncio.create_task`.
         - `async def push(self, event: SensorEvent) -> None`: `async with self._lock: self._buffer.append(event)`; se `len(self._buffer) >= batch_size`: chiama `await self._flush()`.
         - `async def _flush(self) -> None`: drain buffer sotto lock; se vuoto, return. Costruisci `rows = [(e.asset_id, e.tag_id, e.timestamp_utc, e.value, e.unit, e.quality_code, e.source) for e in batch]`; `async with self._pool.acquire() as conn: await conn.executemany(_INSERT_SQL, rows)`. Try/except: log via structlog + emetti AuditEvent ERROR (Phase 11 propagation).
         - `async def _flush_loop(self) -> None`: while True → `await asyncio.sleep(flush_interval_s)` → `await self._flush()`. Wrap in try/except CancelledError raise + Exception log.
         - `async def close(self) -> None`: cancel flush_loop, final flush, `await self._pool.close()`.

       CRITICAL: zero `f"INSERT|SELECT|UPDATE|DELETE"` — CI gate. Solo placeholder `$1..$7`.

    7. **src/svc_ot_bridge/metrics.py** — Prometheus:
       - `nats_pending_acks_gauge = Gauge("nats_pending_acks", "Pending JetStream acks")`
       - `ingest_latency_histogram = Histogram("ingest_latency_seconds", "Latency event→db ingest", buckets=[0.01, 0.05, 0.1, 0.2, 0.5, 1, 2, 5])`
       - `events_published_total = Counter("events_published_total", "Eventi pubblicati", ["subject"])`
       - `asyncpg_pool_size_used_gauge = Gauge("asyncpg_pool_size_used", "Connessioni asyncpg in uso")`
       - `def start_metrics(port: int = 8080) -> None`: `start_http_server(port)`.

    8. **tests/conftest.py** — Fixtures:
       - `sample_sensor_event` (LOOM-01 warp_tension valid).
       - `mock_pool` (AsyncMock di asyncpg.Pool con `acquire()` context manager che ritorna AsyncMock `conn` con `executemany`).
       - `mock_jetstream_ctx` (AsyncMock di JetStreamContext con `.publish` capturable).
       - `frozen_utc_now` (datetime fisso).

    9. **tests/test_normalizer.py + test_subject_derivation.py + test_publisher.py + test_writer.py** — Implementare i Test 1-14.

    10. **src/svc_ot_bridge/__init__.py** — barrel: `from svc_ot_bridge.models import SensorEvent, AuditEvent` + `from svc_ot_bridge.normalizer import normalize_datachange` + `from svc_ot_bridge.nats_publisher import NatsPublisher, derive_event_subject, derive_alarm_subject, derive_audit_subject` + `from svc_ot_bridge.timescale_writer import TimescaleWriter`.

    CRITICAL grep gates (D-51 Layer 3 + Security check 1):
    - `grep -rE "(set_value|write_attribute|write_value)" services/ot-bridge/src/` deve exit 1 (no match) — Layer 3 enforcement.
    - `grep -rE 'f"(INSERT|SELECT|UPDATE|DELETE)' services/ot-bridge/src/` deve exit 1.
    - `grep -rE "datetime\.now\(\)" services/ot-bridge/src/` deve exit 1 (no naive — sempre `datetime.now(UTC)`).

    Decisioni: IOT-04 (NATS subjects D-52), IOT-05 (data-diode Layer 3 application-level), IOT-06 (asyncpg writer hypertable ingest path), OQ3 (audit.ot.bridge solo da ot-bridge).
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && uv run --project services/ot-bridge pytest services/ot-bridge/tests -x -v && bash -c '! grep -rE "(set_value|write_attribute|write_value)" services/ot-bridge/src/' && bash -c '! grep -rE "f\"(INSERT|SELECT|UPDATE|DELETE)" services/ot-bridge/src/'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project services/ot-bridge pytest services/ot-bridge/tests -x -v` exits 0; ≥ 14 test items passed.
    - `grep -rE "(set_value|write_attribute|write_value)" services/ot-bridge/src/` exits 1 (D-51 Layer 3 PASS).
    - `grep -rE 'f"(INSERT|SELECT|UPDATE|DELETE)' services/ot-bridge/src/` exits 1.
    - `grep -rE "datetime\.now\(\)" services/ot-bridge/src/` exits 1.
    - `grep -c "statement_cache_size=0" services/ot-bridge/src/svc_ot_bridge/timescale_writer.py` ≥ 1 (Pitfall 6).
    - `grep -c "min_size=10" services/ot-bridge/src/svc_ot_bridge/timescale_writer.py` ≥ 1.
    - `grep -c "max_size=20" services/ot-bridge/src/svc_ot_bridge/timescale_writer.py` ≥ 1.
    - `grep -cE '\$1.*\$2.*\$3.*\$4.*\$5.*\$6.*\$7' services/ot-bridge/src/svc_ot_bridge/timescale_writer.py` ≥ 1 (placeholder SQL).
    - `grep -c "sensor\.events\." services/ot-bridge/src/svc_ot_bridge/nats_publisher.py` ≥ 1.
    - `grep -c "audit\.ot\.bridge" services/ot-bridge/src/svc_ot_bridge/nats_publisher.py` ≥ 1.
  </acceptance_criteria>
  <done>Tutti i building block del bridge in test pytest verde; CI grep gate D-51 Layer 3 verde; SQL parameterized; asyncpg pool configurato per TimescaleDB.</done>
</task>

<task type="auto">
  <name>Task 2: asyncua client subscribe + main asyncio + Dockerfile + NATS bootstrap script</name>
  <files>services/ot-bridge/src/svc_ot_bridge/opcua_client.py, services/ot-bridge/src/svc_ot_bridge/main.py, services/ot-bridge/Dockerfile, scripts/nats-bootstrap-streams.py</files>
  <read_first>
    - services/ot-bridge/src/svc_ot_bridge/{normalizer.py, nats_publisher.py, timescale_writer.py} (Task 1)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 1 (asyncua client subscribe — Server pattern adattabile) + §Pitfall 1 (publishing_interval=50, samples_per_publish=10)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pattern 3 (JetStream stream config + nats.connect, lines 506-540)
    - .planning/phases/03-it-ot-simulation-layer/03-RESEARCH.md §Pitfall 3 (stream config drift idempotent via try/except update_stream)
    - .planning/phases/03-it-ot-simulation-layer/03-CONTEXT.md D-50 OPC-UA endpoint + D-52 subjects + claudes_discretion env vars (TIMESCALE_DSN, NATS_URL, OPCUA_URL)
    - scripts/sync-python-versions.py (analog argparse + WORKSPACE_ROOT + exit codes per nats-bootstrap script)
    - .planning/phases/03-it-ot-simulation-layer/03-PATTERNS.md sezione scripts (PATTERN S-3, S-4)
  </read_first>
  <action>
    1. **src/svc_ot_bridge/opcua_client.py** — asyncua subscription:
       - `class OpcUaSubscriber`:
         - `__init__(self, url: str, queue: asyncio.Queue, publishing_interval_ms: int = 50, samples_per_publish: int = 10)`: `self._url = url`; `self._queue = queue` (per backpressure handoff).
         - `async def start(self) -> None`:
           - `self._client = asyncua.Client(url=self._url)`; `await self._client.connect()`.
           - Carica `from sft_assets import load_assets`; `assets = load_assets()`.
           - Risolvi i Variable nodes via `client.get_node(...)` con namespace `urn:mantis:<family>` + BrowsePath `<family>/<asset_id>/<tag_id>`.
           - Crea `subscription = await self._client.create_subscription(publishing_interval_ms, self)` (handler self).
           - Per ogni (asset, tag) → `await subscription.subscribe_data_change(var_node)`.
         - `async def datachange_notification(self, node, val, data) -> None`:
           - CRITICAL: callback NON deve fare work pesante. Solo `self._queue.put_nowait((node, val, data))` (Pitfall 1).
           - Se `queue.full()`: emit Audit ERROR (Phase 11 alerting) + log + drop (drop esplicito, no silent).
         - `async def close(self) -> None`: `await self._client.disconnect()`.

       Notare: ZERO chiamate `write_value`, `set_value`, `write_attribute` — solo subscribe. Verificato CI grep gate (D-51 Layer 3).

    2. **src/svc_ot_bridge/main.py** — Asyncio entry orchestrator:
       - `async def run() -> None`:
         - Env: `OPCUA_URL = os.environ.get("OPCUA_URL","opc.tcp://sim-textile:4840")`; `NATS_URL = os.environ.get("NATS_URL","nats://nats:4222")`; `TIMESCALE_DSN = os.environ.get("TIMESCALE_DSN")` (required, raise se None).
         - `start_metrics(int(os.environ.get("METRICS_PORT","8080")))`.
         - `publisher = NatsPublisher(); await publisher.start(NATS_URL)`.
         - `writer = TimescaleWriter(TIMESCALE_DSN); await writer.start()`.
         - Audit start: `await publisher.publish_audit(AuditEvent(ts=datetime.now(UTC), level="INFO", event_type="bridge_start", details={"opcua_url": OPCUA_URL}))`.
         - `event_queue = asyncio.Queue(maxsize=10000)`.
         - `subscriber = OpcUaSubscriber(OPCUA_URL, event_queue)`; `await subscriber.start()`.
         - Worker loop:
           ```
           async def worker():
               while True:
                   (node, val, data) = await event_queue.get()
                   # Estrai asset_id, tag_id da BrowsePath / NodeId; source_ts da data.monitored_item.Value.SourceTimestamp
                   # asset_id/tag_id resolution via lookup nodeid_map (popolato in subscriber.start)
                   event = normalize_datachange(asset_id, tag_id, val, data.monitored_item.Value.StatusCode.value, source_ts, datetime.now(UTC))
                   await publisher.publish_event(event)
                   await writer.push(event)
                   event_queue.task_done()
           ```
         - Lancia N worker tasks (es. N=4) via `asyncio.gather`.
         - Wrap shutdown: SIGINT/SIGTERM → cancel tasks → await close di publisher/writer/subscriber → publish AuditEvent "bridge_stop".
       - `def main() -> None`: `asyncio.run(run())`.

    3. **services/ot-bridge/Dockerfile** — Multi-stage analog a sim-textile Plan 03-03:
       - `FROM python:3.12-slim AS builder`
       - `RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates && rm -rf /var/lib/apt/lists/*`
       - `RUN pip install --no-cache-dir uv==0.5.0`
       - `WORKDIR /build`
       - `COPY packages/sft-assets /build/packages/sft-assets`
       - `COPY services/ot-bridge /build/services/ot-bridge`
       - `WORKDIR /build/services/ot-bridge`
       - `RUN uv pip install --system --no-cache-dir .`
       - Runtime stage:
         - `FROM python:3.12-slim`
         - `COPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages`
         - `COPY --from=builder /usr/local/bin/ot-bridge /usr/local/bin/ot-bridge`
         - `EXPOSE 8080`
         - `ENV OPCUA_URL=opc.tcp://sim-textile:4840  NATS_URL=nats://nats:4222  METRICS_PORT=8080`
         - `USER 1000:1000`
         - `CMD ["ot-bridge"]`
       - Comment header: "Build context: monorepo root (configurato in docker-compose Plan 03-06)".

    4. **scripts/nats-bootstrap-streams.py** — Idempotent JetStream bootstrap. Replica RESEARCH §Pattern 3 lines 519-540:
       - Shebang + docstring usage + exit codes (0/1).
       - `WORKSPACE_ROOT = Path(__file__).parent.parent` (Pattern S-3).
       - argparse: `--server URL` (default `nats://localhost:4222`, env `NATS_URL` fallback), `--dry-run`.
       - `async def bootstrap(server: str, dry_run: bool) -> int`:
         - `nc = await nats.connect(server)`; `js = nc.jetstream()`.
         - `cfg = StreamConfig(name="SENSOR_EVENTS", subjects=["sensor.events.>", "sensor.alarms.>"], retention=RetentionPolicy.WORK_QUEUE, max_age=7*24*3600*1_000_000_000, storage=StorageType.FILE, max_msgs_per_subject=-1, discard=DiscardPolicy.OLD)`.
         - `cfg_audit = StreamConfig(name="AUDIT_OT", subjects=["audit.ot.>"], retention=RetentionPolicy.LIMITS, max_age=30*24*3600*1_000_000_000, storage=StorageType.FILE)`.
         - `if dry_run`: print configs, exit 0.
         - Per ogni cfg: `try: await js.add_stream(config=cfg) except nats.js.errors.BadRequestError: await js.update_stream(config=cfg)` (Pitfall 3 mitigation).
         - Stampa `OK [SENSOR_EVENTS]: stream created/updated`.
         - `await nc.close()`; return 0.
       - `def main() -> None`: argparse + `sys.exit(asyncio.run(bootstrap(...)))`.

    Decisioni: D-50/D-51/D-52 (subscribe-only client, no write API, subject hierarchy), Pitfall 1 (publishing_interval=50, queue handoff in callback), Pitfall 3 (idempotent JetStream config), claudes_discretion env vars.
  </action>
  <verify>
    <automated>cd /media/federicocalo/D1/prj/"Smart Factory Transformation" && bash -c '! grep -rE "(set_value|write_attribute|write_value)" services/ot-bridge/src/' && bash -c '! grep -rE "datetime\.now\(\)" services/ot-bridge/src/' && python3 scripts/nats-bootstrap-streams.py --dry-run --server nats://localhost:4222 && grep -c "subjects=\[\"sensor\.events\.>\"" scripts/nats-bootstrap-streams.py && grep -c "FROM python:3.12-slim" services/ot-bridge/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "(set_value|write_attribute|write_value)" services/ot-bridge/src/` exits 1 (D-51 Layer 3 PASS — verifica anche post-Task-2).
    - `grep -c "publishing_interval" services/ot-bridge/src/svc_ot_bridge/opcua_client.py` ≥ 1 (Pitfall 1 mitigation).
    - `grep -c "queue.put_nowait\|put_nowait" services/ot-bridge/src/svc_ot_bridge/opcua_client.py` ≥ 1 (callback handoff).
    - `python3 scripts/nats-bootstrap-streams.py --dry-run --server nats://localhost:4222` exits 0; stdout contiene "SENSOR_EVENTS" e "AUDIT_OT".
    - `grep -c "BadRequestError" scripts/nats-bootstrap-streams.py` ≥ 1 (Pitfall 3 idempotent fallback).
    - `grep -c "StreamConfig" scripts/nats-bootstrap-streams.py` ≥ 1.
    - `test -f services/ot-bridge/Dockerfile` exit 0; `grep -c "FROM python:3.12-slim" services/ot-bridge/Dockerfile` ≥ 2.
    - `grep -rE "datetime\.now\(\)" services/ot-bridge/src/` exits 1.
  </acceptance_criteria>
  <done>Orchestrator main.py wiring publisher+writer+subscriber via queue handoff, Dockerfile pronto, nats-bootstrap-streams.py idempotente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OPC-UA server (sft-ot) → ot-bridge | Client-only API (mai write); A-018 NoSecurity PoC. |
| ot-bridge → NATS JetStream (sft-core) | Publish-only verso `sensor.events.>`, `sensor.alarms.>`, `audit.ot.>`. |
| ot-bridge → TimescaleDB (sft-core) | Write-only via asyncpg pool, SQL parametrized $1..$7. |
| Env vars (TIMESCALE_DSN, NATS_URL, OPCUA_URL) | Secrets via env, no hardcoded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-diode-app | Tampering / EoP | `services/ot-bridge/src/svc_ot_bridge/` codebase | mitigate | D-51 Layer 3: zero `set_value`/`write_attribute`/`write_value` API call; CI grep gate (multi-task assertion). Riferisce T-DATA-DIODE. |
| T-03-04-sql | Injection | `timescale_writer.py` | mitigate | Constant module-level `_INSERT_SQL` con `$1..$7` placeholders; CI grep gate `grep -rE 'f"(INSERT|SELECT|UPDATE|DELETE)' services/ot-bridge/src/` exit 1. Riferisce T-V5-sql. |
| T-03-04-nats-subj | DoS (subject explosion) | `nats_publisher.py` subject derivation | mitigate | Subject schema enforced via `derive_event_subject()` (no user-controlled string interpolation); `max_msgs_per_subject=-1` ma stream-level rate limit applicabile in Phase 11. Riferisce T-NATS-subject. |
| T-03-04-pool-dos | DoS | asyncpg pool | mitigate | `max_size=20` + `command_timeout=10s` + Prometheus `asyncpg_pool_size_used` Gauge (Phase 11 alerting). Riferisce T-PERF. |
| T-03-04-tz-naive | Tampering | `models.py` SensorEvent + `main.py` datetime | mitigate | `@field_validator` enforce `tzinfo is not None`; CI grep gate `grep -rE "datetime\.now\(\)" services/ot-bridge/src/` exit 1. Riferisce Pitfall 7. |
| T-03-04-secret-leak | Information Disclosure | structlog logger | mitigate | Logger formatter NON serializza `TIMESCALE_DSN` raw; pattern bind `service, asset_id, tag_id` only; NESSUN log di env vars (Phase 11 redaction processor formale). |
| T-03-04-jetstream-drift | Tampering / DoS | `nats-bootstrap-streams.py` | mitigate | Try/except `BadRequestError` → `update_stream` fallback (Pattern 3 + Pitfall 3). Schema source of truth = solo questo script. |
| T-03-04-replay | Tampering | `SensorEvent.source` field | mitigate | `Literal["live","replay_cmapss","replay_uci"]` enforced via Pydantic; `live` è default; replay path passa per `sft-tools` con source-tagging esplicito. |

`security_enforcement: enabled` (ASVS V5 + V4 + V8 + V14). Tutti i pacchetti (`asyncua, nats-py, asyncpg, structlog, prometheus-client`) approved in RESEARCH §Package Legitimacy Audit.
</threat_model>

<verification>
- `uv run --project services/ot-bridge pytest services/ot-bridge/tests -x -v` exits 0.
- `grep -rE "(set_value|write_attribute|write_value)" services/ot-bridge/src/` exits 1 (D-51 Layer 3 PASS).
- `grep -rE 'f"(INSERT|SELECT|UPDATE|DELETE)' services/ot-bridge/src/` exits 1.
- `grep -rE "datetime\.now\(\)" services/ot-bridge/src/` exits 1.
- `python3 scripts/nats-bootstrap-streams.py --dry-run` exits 0.
- `npx nx run ot-bridge:test` exits 0.
</verification>

<success_criteria>
- IOT-04: NATS subject hierarchy `sensor.events.<family>.<asset_id>.<tag_id>` + `sensor.alarms.<family>.<asset_id>` + `audit.ot.bridge` implementato e testato.
- IOT-05 (parte applicativa): bridge è subscribe-only verso OPC-UA, zero write API; D-51 Layer 3 grep gate verde.
- IOT-06 (path ingest): asyncpg pool + executemany batch SQL parametrized; readiness per migration di Plan 03-05.
- Audit subject (OQ3) implementato in ot-bridge; sim-textile NON pubblica audit.
- Container build-able; bootstrap script idempotente.
</success_criteria>

<output>
Create `.planning/phases/03-it-ot-simulation-layer/03-04-SUMMARY.md` con: file creati, output pytest, conferma CI grep gates verdi, decisione OQ3 documentata.
</output>
