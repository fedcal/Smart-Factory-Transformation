[project]
name = "sim-textile"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Textile line simulator: loom, spinning, warping machine event generation (Fase 3)"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sim_textile"]
