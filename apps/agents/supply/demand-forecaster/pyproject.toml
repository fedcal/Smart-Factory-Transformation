[project]
name = "scm-demand-forecaster"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Supply chain agent: demand forecasting using historical orders and market signals"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/scm_demand_forecaster"]
