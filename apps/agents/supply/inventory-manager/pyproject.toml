[project]
name = "scm-inventory-manager"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Supply chain agent: inventory level monitoring and reorder recommendations"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/scm_inventory_manager"]
