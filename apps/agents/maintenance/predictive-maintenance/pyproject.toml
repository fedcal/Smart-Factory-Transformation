[project]
name = "mnt-predictive-maintenance"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Maintenance agent: predictive maintenance using sensor history and ML models"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mnt_predictive_maintenance"]
