[project]
name = "mnt-predictive-maintenance"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Maintenance agent: predictive maintenance using C-MAPSS RUL inference on textile sensor streams"
dependencies = [
  "asyncpg>=0.30,<0.31",
  "nats-py>=2.10.0,<3.0.0",
  "pandas>=2.2,<3",
  "pydantic>=2.9,<3",
  "structlog>=24.4",
  "sft-agents",
  "sft-assets",
  "sft-domain",
  "sft-ml",
  "sft-tools",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.24",
  "pytest-mock>=3.14",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mnt_predictive_maintenance"]

[tool.uv.sources]
sft-agents = { workspace = true }
sft-assets = { workspace = true }
sft-domain = { workspace = true }
sft-ml = { workspace = true }
sft-tools = { workspace = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
  "integration: requires docker compose / testcontainers",
]
