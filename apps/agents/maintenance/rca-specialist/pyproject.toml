[project]
name = "mnt-rca-specialist"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Maintenance agent: root cause analysis specialist for equipment failures"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mnt_rca_specialist"]
