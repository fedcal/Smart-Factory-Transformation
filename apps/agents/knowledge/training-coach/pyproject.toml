[project]
name = "trn-training-coach"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Knowledge agent: personalized training coach for factory operators"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trn_training_coach"]
