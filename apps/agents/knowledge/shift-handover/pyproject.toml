[project]
name = "trn-shift-handover"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Knowledge agent: generates shift handover reports and action items"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trn_shift_handover"]
