[project]
name = "trn-knowledge-curator"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Knowledge agent: curates and indexes factory knowledge for RAG retrieval"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trn_knowledge_curator"]
