"""Wave 0 stub for plan 07-10 — implementation lands in 07-10.

Tests for api-gateway maintenance endpoints (REST surface for the 4
maintenance agents — MNT-01..MNT-04). Real assertions land with plan
07-10 (api-gateway maintenance endpoints).
"""

from __future__ import annotations

import pytest


def test_placeholder() -> None:
    pytest.skip("Wave 0 stub — implemented in plan 07-10.")
