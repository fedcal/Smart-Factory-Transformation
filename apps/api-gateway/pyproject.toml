[project]
name = "svc-api-gateway"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "FastAPI API gateway: REST + WebSocket bridge to the agent cluster"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/svc_api_gateway"]
