[project]
name = "svc-orchestrator"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Supervisor orchestrator: LangGraph-based multi-agent coordinator"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/svc_orchestrator"]
