"""Wave 0 stub for plan 07-04 — implementation lands in 07-04.

Tests for ``build_maintenance_subgraph`` — maintenance cluster intra-subgraph
router (mirrors Phase 6 build_ops_subgraph pattern, D-MNT cluster routing).
Real assertions land with plan 07-04 (maintenance-cluster-routing).
"""

from __future__ import annotations

import pytest


def test_placeholder() -> None:
    pytest.skip("Wave 0 stub — implemented in plan 07-04.")
