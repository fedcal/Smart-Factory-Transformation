"""Wave 0 stub for plan 07-04 — implementation lands in 07-04.

Tests for the ``request_help`` tool (MaintenanceCoach wrapper around
``escalate_to_supervisor`` + audit marker — MNT-03 / T-V7-double-write).
Real assertions land with plan 07-04 (maintenance shared tools).
"""

from __future__ import annotations

import pytest


def test_placeholder() -> None:
    pytest.skip("Wave 0 stub — implemented in plan 07-04.")
