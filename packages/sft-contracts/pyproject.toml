[project]
name = "sft-contracts"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Single source of truth: Pydantic models shared by Python services and Angular frontend"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sft_contracts"]
