"""Wave 0 stub for plan 07-02 — implementation lands in 07-02.

Tests for the ``failure_modes.yaml`` schema extension (MNT-05 / V5 — schema
loads + reason_code unique + intervention_steps_sop_id presence in Phase 5
corpus). Real assertions land with plan 07-02 (taxonomy extension).
"""

from __future__ import annotations

import pytest


def test_placeholder() -> None:
    pytest.skip("Wave 0 stub — implemented in plan 07-02.")
