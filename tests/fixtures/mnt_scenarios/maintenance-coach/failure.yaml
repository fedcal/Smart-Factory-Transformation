# MaintenanceCoach — failure scenario (Wave 0 stub).
#
# Real seed (LLM produces step ID not in SOP corpus → validator fails →
# escalation) lands in plan 07-12.
scenario:
  name: maintenance-coach-failure
  note: Wave 0 stub — replace in plan 07-12
