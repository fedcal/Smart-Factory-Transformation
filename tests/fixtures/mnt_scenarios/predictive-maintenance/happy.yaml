# PredictiveMaintenance — happy scenario (Wave 0 stub).
#
# Real seed (sensor window inside healthy range → RUL estimate emitted +
# Decision.AUTO audit row) lands in plan 07-12.
scenario:
  name: predictive-maintenance-happy
  note: Wave 0 stub — replace in plan 07-12
