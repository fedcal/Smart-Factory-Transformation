# PredictiveMaintenance — failure scenario (Wave 0 stub).
#
# Real seed (malformed sensor input → predictable error + audit row) lands
# in plan 07-12.
scenario:
  name: predictive-maintenance-failure
  note: Wave 0 stub — replace in plan 07-12
