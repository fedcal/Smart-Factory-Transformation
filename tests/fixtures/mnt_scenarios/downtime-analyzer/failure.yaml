# DowntimeAnalyzer — failure scenario (Wave 0 stub).
#
# Real seed (NATS message references missing asset_id → nak + audit row
# Decision.SUPPRESSED) lands in plan 07-12.
scenario:
  name: downtime-analyzer-failure
  note: Wave 0 stub — replace in plan 07-12
