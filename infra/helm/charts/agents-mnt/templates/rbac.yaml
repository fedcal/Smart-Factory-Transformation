{{- if .Values.serviceAccount.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "agents-mnt.fullname" . }}
  labels:
    {{- include "agents-mnt.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "agents-mnt.fullname" . }}
  labels:
    {{- include "agents-mnt.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "agents-mnt.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "agents-mnt.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
