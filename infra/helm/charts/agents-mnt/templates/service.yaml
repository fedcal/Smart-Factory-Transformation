apiVersion: v1
kind: Service
metadata:
  name: {{ include "agents-mnt.fullname" . }}
  labels:
    {{- include "agents-mnt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agents-mnt.selectorLabels" . | nindent 4 }}
