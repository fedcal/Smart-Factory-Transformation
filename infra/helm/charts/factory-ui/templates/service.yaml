apiVersion: v1
kind: Service
metadata:
  name: {{ include "factory-ui.fullname" . }}
  labels:
    {{- include "factory-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "factory-ui.selectorLabels" . | nindent 4 }}
