apiVersion: v1
kind: Service
metadata:
  name: {{ include "agents-trn.fullname" . }}
  labels:
    {{- include "agents-trn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agents-trn.selectorLabels" . | nindent 4 }}
