apiVersion: v1
kind: Service
metadata:
  name: {{ include "agents-ops.fullname" . }}
  labels:
    {{- include "agents-ops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agents-ops.selectorLabels" . | nindent 4 }}
