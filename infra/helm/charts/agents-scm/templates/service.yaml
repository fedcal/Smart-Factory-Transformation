apiVersion: v1
kind: Service
metadata:
  name: {{ include "agents-scm.fullname" . }}
  labels:
    {{- include "agents-scm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agents-scm.selectorLabels" . | nindent 4 }}
